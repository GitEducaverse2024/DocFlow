---
phase: 45-ui-pagina-agentes-rediseñada
plan: 03
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - app/src/components/canvas/nodes/agent-node.tsx
  - app/src/components/canvas/node-config-panel.tsx
  - app/src/components/canvas/node-palette.tsx
  - app/src/app/tasks/new/page.tsx
  - app/src/components/process/process-panel.tsx
autonomous: true
requirements: [UI-07, UI-08, UI-09]

must_haves:
  truths:
    - "En CatBrain pipeline, el selector de Worker/procesador muestra CatPaws con mode IN (processor, hybrid)"
    - "En el wizard de Tareas, el selector de agente muestra CatPaws en lugar de custom_agents"
    - "En Canvas, el nodo AGENT muestra icono PawPrint y el selector apunta a CatPaws"
    - "El nodo AGENT en el palette muestra icono catpaw.png"
  artifacts:
    - path: "app/src/components/canvas/nodes/agent-node.tsx"
      provides: "Nodo AGENT con PawPrint icon y badge de modo"
      contains: "catpaw.png"
    - path: "app/src/components/canvas/node-config-panel.tsx"
      provides: "Config panel con selector de CatPaw en vez de custom_agents"
      contains: "cat-paws"
    - path: "app/src/app/tasks/new/page.tsx"
      provides: "Wizard tareas con selector de CatPaw"
      contains: "cat-paws"
    - path: "app/src/components/process/process-panel.tsx"
      provides: "Selector de CatPaw procesador en lugar de DocsWorker"
      contains: "cat-paws"
  key_links:
    - from: "app/src/components/canvas/node-config-panel.tsx"
      to: "/api/cat-paws"
      via: "fetch GET for agent selector"
      pattern: "fetch.*api/cat-paws"
    - from: "app/src/app/tasks/new/page.tsx"
      to: "/api/cat-paws"
      via: "fetch GET for agent selector"
      pattern: "fetch.*api/cat-paws"
    - from: "app/src/components/process/process-panel.tsx"
      to: "/api/cat-paws?mode=processor"
      via: "fetch GET for processor CatPaw selector"
      pattern: "fetch.*api/cat-paws.*mode=processor"
---

<objective>
Update Canvas agent node to use PawPrint icon and CatPaw selector, update task wizard agent selector to fetch from CatPaws API, and update CatBrain pipeline process-panel to show CatPaw processors instead of DocsWorkers.

Purpose: All agent selectors across the app must point to the unified CatPaw entity instead of the old custom_agents/docs_workers tables.
Output: Updated Canvas nodes, task wizard, and pipeline processor selectors
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/lib/types/catpaw.ts
@app/src/components/canvas/nodes/agent-node.tsx
@app/src/components/canvas/node-config-panel.tsx
@app/src/components/canvas/node-palette.tsx
@app/src/app/tasks/new/page.tsx
@app/src/components/process/process-panel.tsx

<interfaces>
<!-- CatPaw types -->
```typescript
export interface CatPawWithCounts extends CatPaw {
  skills_count: number;
  catbrains_count: number;
  connectors_count: number;
  agents_count: number;
}
// mode: 'chat' | 'processor' | 'hybrid'
```

<!-- API: GET /api/cat-paws?mode=processor returns CatPawWithCounts[] filtered by mode -->
<!-- API: GET /api/cat-paws returns all CatPaws -->

<!-- Current node-config-panel fetches from /api/agents for agent type nodes (line 68) -->
<!-- Current tasks/new/page.tsx fetches from /api/agents (line 505) -->
<!-- Node palette uses Bot icon for agent type (line 18) -->

<!-- Current process-panel.tsx: -->
<!-- Imports DocsWorker from @/lib/types -->
<!-- State: workers (DocsWorker[]), processMode ('agent'|'worker'), selectedWorkerId -->
<!-- Fetches from /api/workers (line 116) -->
<!-- Worker selector renders list of DocsWorkers with click-to-select (lines 705-730) -->
<!-- Submits worker_id in process request body (lines 330, 352, 374, 391) -->

<!-- Icon: /Images/icon/catpaw.png exists -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canvas node palette + agent-node + config panel — use CatPaw icon and selector</name>
  <files>app/src/components/canvas/node-palette.tsx, app/src/components/canvas/nodes/agent-node.tsx, app/src/components/canvas/node-config-panel.tsx</files>
  <action>
**node-palette.tsx:**
- Replace the agent entry's `icon: Bot` with `icon: null` and add `customIcon: <Image src="/Images/icon/catpaw.png" alt="CatPaw" width={20} height={20} />` (same pattern as catbrain entry on line 19).
- Import `Image` from 'next/image' (already imported for catbrain icon).
- Remove `Bot` from lucide-react imports if no longer used in this file.

**agent-node.tsx:**
- Replace the `Bot` icon with an `Image` component: `<Image src="/Images/icon/catpaw.png" alt="CatPaw" width={16} height={16} className="shrink-0" />`
- Import `Image` from 'next/image'.
- Add mode badge if `nodeData.mode` exists: show small badge below agent name with mode-specific colors (violet for chat, teal for processor, amber for hybrid). Add `mode?: string` to the nodeData type.
- Keep all execution status styling unchanged.

**node-config-panel.tsx:**
- Change the agent fetch (line 68): replace `fetch('/api/agents')` with `fetch('/api/cat-paws')`. The response is CatPawWithCounts[] (flat array). Update the `Agent` interface to match: `{ id: string; name: string; model?: string; mode?: string; avatar_emoji?: string }`.
- In the agent selector dropdown, show each CatPaw with its avatar_emoji prefix and mode indicator.
- When an agent is selected, also store `mode` in node data: `update({ agentId: e.target.value, agentName: agent?.name, model: agent?.model, mode: agent?.mode })`.
- For 'catbrain' and 'project' type nodes, keep existing catbrains fetch unchanged.
- For merge node agent selector (around line 335): also update to fetch from /api/cat-paws.
- Update NODE_TYPE_META for 'agent': change icon to `<Image src="/Images/icon/catpaw.png" alt="CatPaw" width={16} height={16} />`.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Canvas palette shows catpaw.png for Agent. Agent node displays PawPrint icon and mode badge. Config panel fetches from /api/cat-paws and shows CatPaw names in selector. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Task wizard agent selector + CatBrain pipeline processor selector</name>
  <files>app/src/app/tasks/new/page.tsx, app/src/components/process/process-panel.tsx</files>
  <action>
**Task wizard agent selector (tasks/new/page.tsx):**
- Find the agents fetch (around line 505): change `fetch('/api/agents')` to `fetch('/api/cat-paws')`.
- The response shape changes: old API returned `{ id, name, emoji, model, source, ... }`, new API returns `CatPawWithCounts[]` with `{ id, name, avatar_emoji, model, mode, ... }`.
- Update the local Agent interface to match CatPaw fields: add `mode`, rename `emoji` references to `avatar_emoji` (or map during fetch).
- In the agent Select dropdown in StepEditor, display the CatPaw avatar_emoji + name. Show a small mode indicator (text color) to help users distinguish chat/processor/hybrid agents.
- In the `onValueChange` handler, update `agent_name` from paw.name, `agent_model` from paw.model.
- For backward compat: if existing task steps reference old agent IDs that happen to be in cat_paws (they should be, due to migration), they will resolve correctly since IDs were preserved during Phase 42 migration.
- Update the agent connector fetching if needed — currently fetches from `/api/agents/${agentId}` for connector access. Change to `/api/cat-paws/${agentId}/relations` and extract connectors from the relations response.

**CatBrain pipeline processor selector (process-panel.tsx — UI-07):**
Modify `app/src/components/process/process-panel.tsx` to replace DocsWorker state with CatPaw processor state:

1. **Imports:** Replace `DocsWorker` import with `CatPawWithCounts` from `@/lib/types/catpaw` (or inline a minimal interface). Remove `AgentCreator` and `AgentListSelector` imports if they become unused.

2. **State:** Rename `workers` state to `processorPaws` (`CatPawWithCounts[]`). Keep `selectedWorkerId` renamed to `selectedProcessorId`. Change `processMode` type from `'agent' | 'worker'` to `'agent' | 'catpaw-processor'`.

3. **Fetch:** Change the workers fetch (line 116) from `fetch('/api/workers')` to `fetch('/api/cat-paws?mode=processor')`. This returns CatPaws with mode IN (processor, hybrid). Update the response handling to set `processorPaws`.

4. **Selector UI (lines 705-730):** Replace the "Seleccionar Docs Worker" card with "Seleccionar CatPaw Procesador". Map over `processorPaws` instead of `workers`. Show each CatPaw's `avatar_emoji` + `name` + mode badge (teal for processor, amber for hybrid). Highlight selected with violet border as before.

5. **Mode selector buttons (around line 633):** Change the "Docs Worker" button label to "CatPaw Procesador". Update the onClick to set `processMode` to `'catpaw-processor'`. Update the conditional checks from `processMode === 'worker'` to `processMode === 'catpaw-processor'` throughout the file.

6. **Submit logic (lines 306, 330, 352, 374, 391):** Change `worker_id` references to `processor_paw_id` in the process request body. The validation at line 306 should check `selectedProcessorId` when mode is `'catpaw-processor'`.

7. **Active run display (line 489):** Update the "Procesando con..." text to show the selected CatPaw processor name instead of DocsWorker name.

8. **Empty state (line 713-715):** Change "No hay workers creados" to "No hay CatPaws procesadores. Ve a Agentes para crear uno." with link to `/agents`.

All UI text in Spanish.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Task wizard shows CatPaw names in agent selector (fetched from /api/cat-paws). CatBrain pipeline process-panel.tsx replaces DocsWorker selector with CatPaw processor selector fetching from /api/cat-paws?mode=processor. Agent emojis and mode indicators display correctly. Backward compat maintained for existing task steps. Build passes.</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npm run build` completes without errors
2. Canvas palette shows PawPrint for Agent node type
3. Canvas agent node config panel fetches CatPaws from /api/cat-paws
4. Task wizard agent selector shows CatPaws
5. CatBrain pipeline process-panel shows CatPaw processor selector (fetched from /api/cat-paws?mode=processor)
</verification>

<success_criteria>
- Canvas agent nodes use PawPrint icon and select from CatPaws
- Task wizard fetches agents from /api/cat-paws instead of /api/agents
- CatBrain pipeline process-panel.tsx uses CatPaw processor selector replacing DocsWorker
- All selectors show CatPaw avatar_emoji + name + mode indicator
- Backward compatibility maintained for existing canvas/task data
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/45-ui-pagina-agentes-rediseñada/45-03-SUMMARY.md`
</output>
