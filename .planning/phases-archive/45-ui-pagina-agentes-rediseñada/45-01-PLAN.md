---
phase: 45-ui-pagina-agentes-rediseñada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/layout/sidebar.tsx
  - app/src/app/agents/page.tsx
  - app/src/components/agents/catpaw-card.tsx
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "El sidebar muestra 'Agentes' con icono PawPrint (catpaw.png) y NO muestra 'Docs Workers'"
    - "La pagina /agents muestra grid de cards CatPaw (3 cols desktop, 1 mobile)"
    - "Los filtros por modo (Todos/Chat/Procesador/Hibrido), busqueda por nombre, y filtro por departamento funcionan"
    - "Cada card muestra badge de modo con color correcto (violet=chat, teal=processor, amber=hybrid)"
  artifacts:
    - path: "app/src/components/layout/sidebar.tsx"
      provides: "Sidebar con Agentes (PawPrint icon) sin Workers"
      contains: "catpaw.png"
    - path: "app/src/app/agents/page.tsx"
      provides: "Pagina listado CatPaws con grid, filtros, busqueda"
      min_lines: 100
    - path: "app/src/components/agents/catpaw-card.tsx"
      provides: "Card component reutilizable para CatPaw"
      exports: ["CatPawCard"]
  key_links:
    - from: "app/src/app/agents/page.tsx"
      to: "/api/cat-paws"
      via: "fetch GET with mode/department/search filters"
      pattern: "fetch.*api/cat-paws"
    - from: "app/src/app/agents/page.tsx"
      to: "app/src/components/agents/catpaw-card.tsx"
      via: "import CatPawCard"
      pattern: "import.*CatPawCard"
---

<objective>
Update sidebar to show "Agentes" with PawPrint icon and remove "Docs Workers" entry. Rewrite /agents page to display CatPaws in a grid layout with mode filters, department filter, and name search. Create reusable CatPawCard component.

Purpose: The sidebar and agents list page are the entry point for the unified CatPaw experience. Users must see their agents in a visual grid with mode badges and filtering.
Output: Updated sidebar, new /agents page, CatPawCard component
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/lib/types/catpaw.ts
@app/src/components/layout/sidebar.tsx
@app/src/app/agents/page.tsx
@app/src/app/api/cat-paws/route.ts

<interfaces>
<!-- CatPaw types from app/src/lib/types/catpaw.ts -->
```typescript
export interface CatPaw {
  id: string;
  name: string;
  description: string | null;
  avatar_emoji: string;
  avatar_color: string;
  department_tags: string | null;  // JSON array string
  system_prompt: string | null;
  tone: string;
  mode: 'chat' | 'processor' | 'hybrid';
  model: string;
  temperature: number;
  max_tokens: number;
  processing_instructions: string | null;
  output_format: string;
  openclaw_id: string | null;
  openclaw_synced_at: string | null;
  is_active: number;
  times_used: number;
  created_at: string;
  updated_at: string;
}

export interface CatPawWithCounts extends CatPaw {
  skills_count: number;
  catbrains_count: number;
  connectors_count: number;
  agents_count: number;
}
```

<!-- Existing API: GET /api/cat-paws?mode=chat&department=ventas&active=1 returns CatPawWithCounts[] -->
<!-- Icon path: /Images/icon/catpaw.png (already exists in app/public/Images/icon/) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sidebar — PawPrint icon for Agentes, remove Workers</name>
  <files>app/src/components/layout/sidebar.tsx</files>
  <action>
In the sidebar navItems array:
1. Change the Agentes entry: replace `icon: Bot` with a custom icon using `Image` from next/image pointing to `/Images/icon/catpaw.png` (same pattern as CatBrain uses ico_catbrain.png in node-palette.tsx). Since navItems expects a component, create a small inline wrapper: `const CatPawIcon = ({ className }: { className?: string }) => <Image src="/Images/icon/catpaw.png" alt="CatPaw" width={20} height={20} className={className} />;` and use it.
2. Remove the Workers entry entirely: delete `{ href: '/workers', label: 'Docs Workers', icon: FileOutput }`.
3. Remove the `FileOutput` import from lucide-react if no longer used.
4. Keep all other sidebar items unchanged.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx next lint --dir src/components/layout/sidebar.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Sidebar shows "Agentes" with catpaw.png icon. "Docs Workers" entry is gone. No TypeScript/lint errors.</done>
</task>

<task type="auto">
  <name>Task 2: CatPawCard component + /agents page rewrite with grid, filters, search</name>
  <files>app/src/components/agents/catpaw-card.tsx, app/src/app/agents/page.tsx</files>
  <action>
**CatPawCard component** (`app/src/components/agents/catpaw-card.tsx`):
- "use client" component
- Props: `{ paw: CatPawWithCounts; onClick?: () => void }`
- Import `CatPawWithCounts` from `@/lib/types/catpaw`
- Card layout (rounded-xl, bg-zinc-900, border-zinc-800, hover:border-zinc-700, transition-colors, cursor-pointer):
  - Top: avatar_emoji (text-2xl) + name (font-semibold text-zinc-50) + mode badge
  - Mode badge colors: chat = `bg-violet-500/20 text-violet-400 border-violet-500/30`, processor = `bg-teal-500/20 text-teal-400 border-teal-500/30`, hybrid = `bg-amber-500/20 text-amber-400 border-amber-500/30`
  - Mode badge labels: chat = "Chat", processor = "Procesador", hybrid = "Hibrido"
  - Middle: description truncated (2 lines, text-sm text-zinc-400)
  - Bottom row: model badge (bg-zinc-800 text-zinc-300 text-xs), department_tags as small pills (parse JSON), counts row (skills_count, catbrains_count, connectors_count as small icons+numbers)
  - If !is_active, show muted opacity-50

**Agents page** (`app/src/app/agents/page.tsx`):
- Complete rewrite. "use client".
- State: `paws: CatPawWithCounts[]`, `loading`, `search: string`, `modeFilter: 'all' | 'chat' | 'processor' | 'hybrid'`, `departmentFilter: string`
- Fetch from `GET /api/cat-paws` on mount. Derive unique departments from all paws' department_tags (parse JSON, collect unique).
- PageHeader with title "Agentes", description "Gestiona tus CatPaws: agentes de chat, procesadores y hibridos", icon using Image catpaw.png (same pattern as sidebar), action button "Crear CatPaw" (Plus icon, gradient violet button) that navigates to wizard (use router.push or set showWizard state — for now just `onClick={() => router.push('/agents/new')}`, the wizard page will be created in plan 02).
- Filter bar below header: 4 mode toggle buttons (Todos/Chat/Procesador/Hibrido) styled as tab-like buttons with active state matching mode badge color, Input for search (placeholder "Buscar por nombre..."), Select for department filter (Todos departamentos + unique tags).
- Client-side filtering: filter paws by modeFilter (if not 'all'), then by search (name.toLowerCase includes), then by department (if selected, check department_tags JSON contains it).
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
- Each card: `<CatPawCard paw={paw} onClick={() => router.push(`/agents/${paw.id}`)} />`
- Empty state: if no paws after filtering, show centered message with catpaw icon and "No se encontraron agentes" + suggestion to create one.
- Import `useRouter` from `next/navigation`, `CatPawCard` from components, `CatPawWithCounts` from types.
- All UI text in Spanish.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>The /agents page shows a grid of CatPaw cards fetched from /api/cat-paws. Mode filter buttons work (Todos/Chat/Procesador/Hibrido). Search by name filters in real-time. Department filter shows unique tags. Cards show emoji, name, mode badge with correct color, model, description, and relation counts. Build passes.</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npm run build` completes without errors
2. Sidebar shows "Agentes" with PawPrint icon, no "Docs Workers" entry
3. `/agents` page loads and shows CatPaw cards from API
4. Mode filter toggles work correctly
5. Search filters by name
</verification>

<success_criteria>
- Sidebar updated: PawPrint icon on Agentes, Workers removed
- /agents page shows CatPaw grid with functional filters and search
- CatPawCard component is reusable with correct mode badge colors
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/45-ui-pagina-agentes-rediseñada/45-01-SUMMARY.md`
</output>
