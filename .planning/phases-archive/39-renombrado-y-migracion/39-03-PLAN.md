---
phase: 39-renombrado-y-migracion
plan: 03
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - app/src/components/canvas/node-palette.tsx
  - app/src/components/canvas/node-config-panel.tsx
  - app/src/components/canvas/canvas-wizard.tsx
  - app/src/components/canvas/canvas-editor.tsx
  - app/src/components/canvas/canvas-card.tsx
  - app/src/components/canvas/nodes/catbrain-node.tsx
  - app/src/lib/services/canvas-executor.ts
  - app/src/lib/services/task-executor.ts
  - app/src/lib/services/catbot-tools.ts
  - app/src/lib/services/catbot-sudo-tools.ts
  - app/src/lib/services/rag.ts
  - app/src/lib/error-formatter.ts
  - app/src/app/api/mcp/[projectId]/route.ts
  - app/src/app/api/catbot/chat/route.ts
  - app/src/app/api/catbot/search-docs/route.ts
  - app/src/app/api/dashboard/storage/route.ts
  - app/src/app/api/dashboard/summary/route.ts
  - app/src/app/api/health/route.ts
  - app/src/app/api/tasks/route.ts
  - app/src/app/api/tasks/[id]/route.ts
  - app/src/app/api/tasks/from-template/route.ts
  - app/src/app/api/testing/generate/route.ts
  - app/src/app/api/testing/run/route.ts
  - app/src/app/tasks/new/page.tsx
  - app/src/app/tasks/[id]/page.tsx
  - app/src/app/tasks/page.tsx
  - app/src/app/canvas/page.tsx
  - app/src/components/catbot/catbot-panel.tsx
  - app/src/components/testing/test-ai-generator.tsx
  - app/src/components/testing/test-section-list.tsx
  - app/src/app/settings/page.tsx
autonomous: true
requirements: [REN-05, REN-06, REN-07]

must_haves:
  truths:
    - "En Canvas, el nodo tipo CATBRAIN aparece en la paleta con icono ico_catbrain.png en lugar del antiguo nodo PROJECT"
    - "El nodo CATBRAIN en Canvas muestra badges: punto de RAG status (verde/amarillo/gris) y contador de conectores (placeholder '0 conectores')"
    - "El canvas-executor ejecuta nodos tipo 'catbrain' consultando la tabla catbrains"
    - "En Tareas, el paso tipo CATBRAIN aparece en el wizard con icono ico_catbrain.png"
    - "El task-executor ejecuta pasos tipo 'catbrain' consultando la tabla catbrains"
    - "El MCP endpoint funciona en /api/mcp/[catbrainId] consultando catbrains"
    - "CatBot tools usan catbrains en vez de projects"
  artifacts:
    - path: "app/src/components/canvas/node-palette.tsx"
      provides: "CATBRAIN node type in palette"
      contains: "catbrain"
    - path: "app/src/components/canvas/nodes/catbrain-node.tsx"
      provides: "Renamed catbrain-node visual component with RAG status badge and conectores count"
      contains: "CatBrainNode"
    - path: "app/src/lib/services/canvas-executor.ts"
      provides: "Canvas executor handling catbrain node type"
      contains: "case 'catbrain'"
    - path: "app/src/lib/services/task-executor.ts"
      provides: "Task executor handling catbrain step type"
      contains: "catbrains"
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "CatBot tools querying catbrains table"
      contains: "FROM catbrains"
  key_links:
    - from: "app/src/components/canvas/node-palette.tsx"
      to: "app/src/components/canvas/nodes/catbrain-node.tsx"
      via: "node type registry"
      pattern: "type.*catbrain"
    - from: "app/src/lib/services/canvas-executor.ts"
      to: "app/src/lib/db.ts"
      via: "SQL query on catbrains table"
      pattern: "FROM catbrains"
    - from: "app/src/lib/services/task-executor.ts"
      to: "app/src/lib/db.ts"
      via: "SQL query on catbrains table"
      pattern: "FROM catbrains"
---

<objective>
Rename Canvas node type PROJECT to CATBRAIN, Task step type PROJECT to CATBRAIN, and update all internal references (MCP, executors, CatBot tools, dashboard, testing routes) to use `catbrains` instead of `projects`.

Purpose: Complete the rename by updating the execution layer and internal services so no code path references the old `projects` table.
Output: Canvas shows CATBRAIN nodes, Tasks wizard shows CATBRAIN steps, all internal SQL queries use catbrains table.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-renombrado-y-migracion/39-01-SUMMARY.md

@app/src/components/canvas/node-palette.tsx
@app/src/components/canvas/node-config-panel.tsx
@app/src/components/canvas/nodes/project-node.tsx
@app/src/lib/services/canvas-executor.ts
@app/src/lib/services/task-executor.ts
@app/src/lib/services/catbot-tools.ts
@app/src/app/api/mcp/[projectId]/route.ts
</context>

<interfaces>
<!-- Canvas node type registry from node-palette.tsx -->
```typescript
// Current:
{ type: 'project', label: 'Proyecto', icon: FolderKanban, color: 'text-blue-400' },
// Must become:
{ type: 'catbrain', label: 'CatBrain', icon: Brain, color: 'text-violet-400' },

// Mode filters:
projects: new Set(['start', 'project', 'checkpoint', 'merge', 'condition', 'output']),
// Must become:
catbrains: new Set(['start', 'catbrain', 'checkpoint', 'merge', 'condition', 'output']),
```

<!-- Canvas executor switch case -->
```typescript
// Current: case 'project': { ... queries FROM projects ... }
// Must become: case 'catbrain': { ... queries FROM catbrains ... }
```

<!-- Task executor -->
```typescript
// Current: queries FROM projects WHERE id = ? for linked_projects
// Must become: queries FROM catbrains WHERE id = ?
```

<!-- CatBot tools -->
```typescript
// Current: name: 'list_projects', queries FROM projects
// Must become: name: 'list_catbrains', queries FROM catbrains
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename Canvas node PROJECT to CATBRAIN in palette, nodes, config, wizard, executor</name>
  <files>
    app/src/components/canvas/node-palette.tsx,
    app/src/components/canvas/node-config-panel.tsx,
    app/src/components/canvas/canvas-wizard.tsx,
    app/src/components/canvas/canvas-editor.tsx,
    app/src/components/canvas/canvas-card.tsx,
    app/src/components/canvas/nodes/catbrain-node.tsx,
    app/src/lib/services/canvas-executor.ts
  </files>
  <action>
**node-palette.tsx:**
- Change the node type entry: `{ type: 'project', label: 'Proyecto', icon: FolderKanban, color: 'text-blue-400' }` -> `{ type: 'catbrain', label: 'CatBrain', icon: Brain, color: 'text-violet-400' }`
- Import `Brain` from lucide-react (remove FolderKanban if unused elsewhere)
- Update mode filters: rename `projects` mode to `catbrains`, update the Set to include `'catbrain'` instead of `'project'`
- Update descriptions: `project: 'Proyecto — consulta RAG de un proyecto'` -> `catbrain: 'CatBrain — consulta RAG de un CatBrain'`

**node-config-panel.tsx:**
- Update the config panel case for project node to catbrain: any `case 'project':` -> `case 'catbrain':`
- Update labels: "Proyecto" -> "CatBrain"
- Update fetch URLs from `/api/projects` to `/api/catbrains`
- Update `projectId` data field references to `catbrainId` in the node data (this affects what the executor reads)

**canvas-wizard.tsx:**
- Change mode type: `'projects'` -> `'catbrains'`
- Update mode labels in UI: "Proyectos" -> "CatBrains"
- Update any default node data referencing project type

**canvas-editor.tsx:**
- Update node type mappings: `'project'` -> `'catbrain'` in nodeTypes registry
- Import the renamed component from `catbrain-node.tsx` (see below)
- Register BOTH 'catbrain' and 'project' pointing to CatBrainNode for backward compat with existing canvas data

**canvas-card.tsx:**
- Update any references to project nodes in card preview/stats
- Replace "proyecto" text with "catbrain"
- Update links from `/projects/` to `/catbrains/`

**nodes/project-node.tsx -> nodes/catbrain-node.tsx:**
- Create new file `catbrain-node.tsx` based on `project-node.tsx`
- Delete or leave `project-node.tsx` (prefer delete to avoid confusion; backward compat handled by canvas-editor registering both types to the new component)
- Rename component: `ProjectNode` -> `CatBrainNode`
- Replace FolderKanban icon with: `<Image src="/Images/icon/ico_catbrain.png" alt="CatBrain" width={20} height={20} />` (import Image from next/image)
- Update labels: "Proyecto" -> "CatBrain"
- Use violet-400/500 color theme instead of blue-400
- **REN-05 Badges:** Add two badges below the label/name area of the node:
  1. **RAG status dot:** Read `rag_status` from node data (add to nodeData type: `rag_status?: 'ready' | 'processing' | 'none' | 'stale' | null`). Render a small colored dot (8x8 px, rounded-full) with label text:
     - `'ready'` -> green dot (`bg-emerald-400`) + text "RAG listo"
     - `'processing'` -> yellow dot (`bg-amber-400 animate-pulse`) + text "Procesando..."
     - `'none'`, `'stale'`, null, or undefined -> grey dot (`bg-zinc-500`) + text "Sin RAG"
  2. **Conectores count badge:** Hardcode `0 conectores` for now (connectors table will be added in Phase 40). Render as a small pill/badge: `<span className="text-[10px] bg-zinc-700/60 text-zinc-400 px-1.5 py-0.5 rounded-full">0 conectores</span>`
  Place both badges in a `flex items-center gap-2 mt-1` row between the name row and the subtitle row (projectName). Keep the node compact — use text-[10px] for badge text.

**canvas-executor.ts:**
- Change `case 'project':` to `case 'catbrain':`
- Update ALL SQL queries: `FROM projects WHERE` -> `FROM catbrains WHERE`
- Update data field: `data.projectId` -> `data.catbrainId`
- Update log messages: "proyecto" -> "catbrain"
- Update helper function `getRagContext`: change SQL query from `projects` to `catbrains`
- Update notification links: `/projects/${canvasId}` -> `/catbrains/${canvasId}`

IMPORTANT: The node data stored in existing canvases flow_data JSON will still have `type: 'project'`. Add a compatibility fallback in the executor:
```typescript
case 'catbrain':
case 'project': // backward compat for old canvas data
{
  const catbrainId = data.catbrainId || data.projectId; // fallback
  ...
}
```
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Canvas palette shows CATBRAIN node type with ico_catbrain.png icon in violet, catbrain-node.tsx displays RAG status dot (green/yellow/grey) and "0 conectores" badge, canvas executor handles catbrain (and legacy project) node types querying catbrains table, node component renamed and styled</done>
</task>

<task type="auto">
  <name>Task 2: Rename Task step type PROJECT to CATBRAIN and update services layer</name>
  <files>
    app/src/lib/services/task-executor.ts,
    app/src/lib/services/catbot-tools.ts,
    app/src/lib/services/catbot-sudo-tools.ts,
    app/src/lib/services/rag.ts,
    app/src/lib/error-formatter.ts
  </files>
  <action>
**task-executor.ts:**
- Update ALL SQL queries: `FROM projects WHERE` -> `FROM catbrains WHERE`
- Update log messages: "proyecto" -> "catbrain"
- The step type `use_project_rag` field stays as-is (DB column name), but add comment `// legacy column name, refers to catbrain RAG`
- Update the helper function `getRagContext` to query `catbrains` table
- Keep `linked_projects` field parsing as-is (DB column name), but update comments

**catbot-tools.ts:**
- Rename tool: `'list_projects'` -> `'list_catbrains'`
- Rename tool: `'create_project'` -> `'create_catbrain'`
- Update ALL SQL queries: `FROM projects` -> `FROM catbrains`, `INTO projects` -> `INTO catbrains`
- Update tool descriptions in Spanish: "proyectos" -> "catbrains"
- Update action URLs: `/projects/` -> `/catbrains/`
- Update permission check: `'create_projects'` -> `'create_catbrains'`

**catbot-sudo-tools.ts:**
- Same pattern: update SQL queries and tool names from projects to catbrains

**rag.ts:**
- Update SQL queries: `FROM projects` -> `FROM catbrains`
- Update any log messages

**error-formatter.ts:**
- Update any "proyecto" text in error messages

CRITICAL: Do NOT rename DB column names in tasks table (`linked_projects`, `use_project_rag`) — these require table recreation. Only rename SQL table references and visible text.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Task executor queries catbrains table, CatBot tools renamed to list_catbrains/create_catbrain, RAG service queries catbrains, error messages updated</done>
</task>

<task type="auto">
  <name>Task 3: Update API routes and UI pages for tasks, canvas, catbot, dashboard, testing</name>
  <files>
    app/src/app/api/mcp/[projectId]/route.ts,
    app/src/app/api/catbot/chat/route.ts,
    app/src/app/api/catbot/search-docs/route.ts,
    app/src/app/api/dashboard/storage/route.ts,
    app/src/app/api/dashboard/summary/route.ts,
    app/src/app/api/health/route.ts,
    app/src/app/api/tasks/route.ts,
    app/src/app/api/tasks/[id]/route.ts,
    app/src/app/api/tasks/from-template/route.ts,
    app/src/app/api/testing/generate/route.ts,
    app/src/app/api/testing/run/route.ts,
    app/src/app/tasks/new/page.tsx,
    app/src/app/tasks/[id]/page.tsx,
    app/src/app/tasks/page.tsx,
    app/src/app/canvas/page.tsx,
    app/src/components/catbot/catbot-panel.tsx,
    app/src/components/testing/test-ai-generator.tsx,
    app/src/components/testing/test-section-list.tsx,
    app/src/app/settings/page.tsx
  </files>
  <action>
**MCP route (app/api/mcp/[projectId]/route.ts):**
- Update SQL: `FROM projects` -> `FROM catbrains`
- Update variable names in code: `project` -> `catbrain` where it refers to the entity
- The route param folder is `[projectId]` — leave it as-is for backward compat (changing the folder name would break existing MCP URLs). The param just maps to a catbrain ID now.

**CatBot chat route (api/catbot/chat/route.ts):**
- Update SQL queries from `projects` to `catbrains`
- Update any text references

**CatBot search-docs route:**
- Update references

**Dashboard routes (storage, summary):**
- Update SQL queries: `FROM projects` -> `FROM catbrains`
- Update any "proyectos" text in responses

**Health route:**
- Update any project references

**Task routes (tasks/route.ts, tasks/[id]/route.ts, tasks/from-template/route.ts):**
- Update SQL queries that reference `projects` table to `catbrains`
- Keep `linked_projects` column name as-is in tasks table (would need table recreation to rename)

**Testing routes (generate, run):**
- Update any references to "projects" in test generation or run logic

**Task UI pages (tasks/new, tasks/[id], tasks):**
- Update fetch URLs from `/api/projects/` to `/api/catbrains/`
- Update visible text: "Proyecto" -> "CatBrain" in step type labels
- In task wizard step type selector, change "Proyecto" label to "CatBrain"
- **REN-04: Render ico_catbrain.png next to the 'CatBrain' step type label.** In the step type selector (tasks/new/page.tsx and tasks/[id]/page.tsx), add: `import Image from 'next/image'` then `<Image src="/Images/icon/ico_catbrain.png" alt="CatBrain" width={20} height={20} className="inline-block mr-1" />` next to the "CatBrain" label in the step type dropdown/selector and in the step display when viewing a step of type catbrain.

**Canvas page (canvas/page.tsx):**
- Update any references to "proyecto" or mode "projects" to "catbrains"

**CatBot panel (catbot-panel.tsx):**
- Update any `/api/projects/` URLs to `/api/catbrains/`
- Update visible text

**Testing components (test-ai-generator, test-section-list):**
- Update any "proyecto" references

**Settings page:**
- Update any "proyecto" references

CRITICAL: Do NOT rename DB column names in tasks table (`linked_projects`, `use_project_rag`) — these require table recreation. Only rename SQL table references (`FROM projects` -> `FROM catbrains`), visible text, and code comments.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All API routes query catbrains table, MCP endpoint queries catbrains, task wizard shows "CatBrain" step type with ico_catbrain.png icon, all internal SQL references use catbrains table, zero remaining "FROM projects" in codebase</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npx tsc --noEmit` — no type errors
2. `cd /home/deskmath/docflow/app && npm run build` — build succeeds
3. `grep -rn "FROM projects\|INTO projects\|TABLE projects" app/src/ --include="*.ts" --include="*.tsx"` — zero results (only catbrains)
4. `grep -rn "'project'" app/src/components/canvas/ --include="*.tsx" --include="*.ts"` — only backward compat fallback in executor/editor
5. Manual: Open Canvas, verify CATBRAIN node appears in palette with violet icon, and shows RAG status dot + "0 conectores" badge
6. Manual: Create a task, verify step type shows "CatBrain" with ico_catbrain.png icon instead of "Proyecto"
</verification>

<success_criteria>
- Canvas palette shows CATBRAIN node type (violet, ico_catbrain.png icon) instead of PROJECT
- CATBRAIN node displays RAG status badge (green/yellow/grey dot) and "0 conectores" count
- Canvas executor handles both 'catbrain' and legacy 'project' node types
- Task wizard shows CATBRAIN step type with ico_catbrain.png icon
- Task executor queries catbrains table
- CatBot tools use list_catbrains and create_catbrain
- MCP endpoint queries catbrains table
- No remaining `FROM projects` SQL in codebase (except maybe in redirect stubs)
- All dashboard and internal service routes query catbrains
</success_criteria>

<output>
After completion, create `.planning/phases/39-renombrado-y-migracion/39-03-SUMMARY.md`
</output>
