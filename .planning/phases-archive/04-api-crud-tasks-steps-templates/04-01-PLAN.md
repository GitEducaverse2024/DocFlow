---
phase: 04-api-crud-tasks-steps-templates
plan: 01
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - app/src/app/api/tasks/route.ts
  - app/src/app/api/tasks/[id]/route.ts
  - app/src/app/api/tasks/[id]/steps/route.ts
  - app/src/app/api/tasks/[id]/steps/[stepId]/route.ts
  - app/src/app/api/tasks/[id]/steps/reorder/route.ts
  - app/src/app/api/tasks/templates/route.ts
  - app/src/app/api/tasks/from-template/route.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
  - API-08
  - API-09
  - API-10
  - API-11
  - API-12

must_haves:
  truths:
    - "GET /api/tasks devuelve lista de tareas con conteo de pasos, agentes y proyectos vinculados"
    - "POST /api/tasks crea tarea con status 'draft'"
    - "DELETE /api/tasks/{id} elimina tarea y sus pasos en cascada"
    - "POST /api/tasks/{id}/steps crea paso con validacion de maximo 10"
    - "DELETE /api/tasks/{id}/steps/{stepId} reordena los restantes automaticamente"
    - "POST /api/tasks/from-template crea tarea con pasos pre-configurados"
    - "npm run build pasa sin errores"
  artifacts:
    - path: "app/src/app/api/tasks/route.ts"
      provides: "GET lista + POST crear tarea"
      contains: "NextResponse.json"
    - path: "app/src/app/api/tasks/[id]/route.ts"
      provides: "GET detalle + PATCH actualizar + DELETE eliminar"
      contains: "NextResponse.json"
    - path: "app/src/app/api/tasks/[id]/steps/route.ts"
      provides: "GET listar + POST crear paso"
      contains: "order_index"
    - path: "app/src/app/api/tasks/[id]/steps/[stepId]/route.ts"
      provides: "PATCH editar + DELETE eliminar paso"
      contains: "NextResponse.json"
    - path: "app/src/app/api/tasks/from-template/route.ts"
      provides: "POST crear desde template"
      contains: "task_templates"
  key_links:
    - from: "app/src/app/api/tasks/route.ts"
      to: "app/src/lib/db.ts"
      via: "import db — queries tasks, task_steps tables"
      pattern: "from '@/lib/db'"
---

<objective>
Create the full REST API for tasks, steps, and templates: 7 route files with 12 endpoint operations total. No execution logic — just CRUD.

Purpose: The UI (phases 6-8) and execution engine (phase 5) depend on these endpoints.

Output: All 12 API endpoints functional, build passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- API route pattern from existing codebase -->

Standard route pattern (from agents/route.ts):
```typescript
import { NextResponse } from 'next/server';
import db from '@/lib/db';
import { v4 as uuidv4 } from 'uuid';

export const dynamic = 'force-dynamic';

export async function GET() {
  try {
    const rows = db.prepare('SELECT * FROM table ORDER BY created_at DESC').all();
    return NextResponse.json(rows);
  } catch (error) {
    console.error('[API] Error:', error);
    return NextResponse.json({ error: 'Error interno' }, { status: 500 });
  }
}

export async function POST(request: Request) {
  try {
    const body = await request.json();
    // validate
    const id = uuidv4();
    db.prepare('INSERT INTO table (...) VALUES (...)').run(...);
    return NextResponse.json({ id, ... }, { status: 201 });
  } catch (error) {
    console.error('[API] Error:', error);
    return NextResponse.json({ error: 'Error interno' }, { status: 500 });
  }
}
```

Param route pattern (from agents/[id]/route.ts):
```typescript
export async function GET(request: Request, { params }: { params: { id: string } }) {
  const row = db.prepare('SELECT * FROM table WHERE id = ?').get(params.id);
  if (!row) return NextResponse.json({ error: 'No encontrado' }, { status: 404 });
  return NextResponse.json(row);
}
```

Tables from Phase 3 (db.ts):
- tasks: id, name, description, expected_output, status, linked_projects, result_output, total_tokens, total_duration, created_at, updated_at, started_at, completed_at
- task_steps: id, task_id, order_index, type, name, agent_id, agent_name, agent_model, instructions, context_mode, context_manual, rag_query, use_project_rag, skill_ids, status, output, tokens_used, duration_seconds, started_at, completed_at, human_feedback, created_at
- task_templates: id, name, description, emoji, category, steps_config, required_agents, times_used, created_at

Types from Phase 3 (types.ts): Task, TaskStep, TaskTemplate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET/POST /api/tasks — Lista y crear tareas</name>
  <files>app/src/app/api/tasks/route.ts</files>
  <action>
Create `app/src/app/api/tasks/route.ts` with:

**GET** — Lista tareas con filtro por status:
1. Leer query param `status` de la URL (optional filter)
2. Query principal: `SELECT * FROM tasks` + WHERE si hay filtro + `ORDER BY updated_at DESC`
3. Para cada tarea, enriquecer con:
   - `steps_count`: `SELECT COUNT(*) FROM task_steps WHERE task_id = ?`
   - `steps_completed`: `SELECT COUNT(*) FROM task_steps WHERE task_id = ? AND status = 'completed'`
   - `agents`: `SELECT DISTINCT agent_name FROM task_steps WHERE task_id = ? AND agent_name IS NOT NULL` → array de nombres
   - `project_names`: Si `linked_projects` no es null, parsear JSON array de IDs, consultar `SELECT name FROM projects WHERE id IN (...)` → array de nombres
4. Retornar array de tareas enriquecidas

**POST** — Crear tarea:
1. Leer body: `name` (required), `description`, `expected_output`
2. Validar que `name` existe, retornar 400 si no
3. Generar `id` con `uuidv4()`
4. `INSERT INTO tasks (id, name, description, expected_output) VALUES (?, ?, ?, ?)`
5. Retornar la tarea creada con status 201

Incluir `export const dynamic = 'force-dynamic'`. Logs en espanol: `[Tasks]`.
  </action>
  <verify>
    <automated>grep "GET\|POST" app/src/app/api/tasks/route.ts | head -5; grep "force-dynamic" app/src/app/api/tasks/route.ts</automated>
  </verify>
  <done>GET /api/tasks lista con filtros y enriquecimiento. POST /api/tasks crea tarea.</done>
</task>

<task type="auto">
  <name>Task 2: GET/PATCH/DELETE /api/tasks/{id} — Detalle, actualizar, eliminar</name>
  <files>app/src/app/api/tasks/[id]/route.ts</files>
  <action>
Create `app/src/app/api/tasks/[id]/route.ts` with:

**GET** — Detalle completo:
1. `SELECT * FROM tasks WHERE id = ?` → 404 si no existe
2. `SELECT * FROM task_steps WHERE task_id = ? ORDER BY order_index ASC` → array de pasos
3. Retornar `{ ...task, steps }`

**PATCH** — Actualizar tarea:
1. Leer body: name, description, expected_output, linked_projects, status (todos opcionales)
2. Construir SET dinámico solo con campos presentes en el body
3. Siempre añadir `updated_at = datetime('now')` al SET
4. `UPDATE tasks SET ... WHERE id = ?`
5. Retornar `{ success: true }`

**DELETE** — Eliminar tarea:
1. `DELETE FROM tasks WHERE id = ?` (task_steps se borran por CASCADE)
2. Retornar `{ success: true }`

Incluir `export const dynamic = 'force-dynamic'`.
  </action>
  <verify>
    <automated>grep "GET\|PATCH\|DELETE" app/src/app/api/tasks/[id]/route.ts | head -5</automated>
  </verify>
  <done>GET/PATCH/DELETE para /api/tasks/{id}.</done>
</task>

<task type="auto">
  <name>Task 3: GET/POST /api/tasks/{id}/steps — Listar y crear pasos</name>
  <files>app/src/app/api/tasks/[id]/steps/route.ts</files>
  <action>
Create `app/src/app/api/tasks/[id]/steps/route.ts` with:

**GET** — Listar pasos:
1. `SELECT * FROM task_steps WHERE task_id = ? ORDER BY order_index ASC`
2. Retornar array

**POST** — Crear paso:
1. Verificar que la tarea existe
2. Contar pasos existentes: `SELECT COUNT(*) FROM task_steps WHERE task_id = ?`
3. Si count >= 10, retornar 400: `{ error: 'Maximo 10 pasos por tarea' }`
4. Leer body: type (required), name, agent_id, agent_name, agent_model, instructions, context_mode, context_manual, rag_query, use_project_rag, skill_ids, order_index
5. Si `order_index` no se especifica, usar count (añadir al final)
6. Si `order_index` se especifica, desplazar los existentes: `UPDATE task_steps SET order_index = order_index + 1 WHERE task_id = ? AND order_index >= ?`
7. Generar `id` con `uuidv4()`
8. INSERT con todos los campos
9. Actualizar `updated_at` de la tarea padre
10. Retornar paso creado con status 201

Incluir `export const dynamic = 'force-dynamic'`.
  </action>
  <verify>
    <automated>grep "order_index" app/src/app/api/tasks/[id]/steps/route.ts | head -3; grep "Maximo 10" app/src/app/api/tasks/[id]/steps/route.ts</automated>
  </verify>
  <done>GET/POST para pasos con validacion de max 10 y auto-reorder.</done>
</task>

<task type="auto">
  <name>Task 4: PATCH/DELETE /api/tasks/{id}/steps/{stepId}</name>
  <files>app/src/app/api/tasks/[id]/steps/[stepId]/route.ts</files>
  <action>
Create `app/src/app/api/tasks/[id]/steps/[stepId]/route.ts` with:

**PATCH** — Editar paso:
1. Verificar que el paso existe: `SELECT * FROM task_steps WHERE id = ? AND task_id = ?`
2. Leer body: name, agent_id, agent_name, agent_model, instructions, context_mode, context_manual, rag_query, use_project_rag, skill_ids (todos opcionales)
3. Construir SET dinámico con campos presentes
4. UPDATE task_steps SET ... WHERE id = ?
5. Actualizar `updated_at` de la tarea padre
6. Retornar `{ success: true }`

**DELETE** — Eliminar paso:
1. Obtener el paso para saber su `order_index`
2. `DELETE FROM task_steps WHERE id = ? AND task_id = ?`
3. Reordenar: `UPDATE task_steps SET order_index = order_index - 1 WHERE task_id = ? AND order_index > ?`
4. Actualizar `updated_at` de la tarea padre
5. Retornar `{ success: true }`

Incluir `export const dynamic = 'force-dynamic'`.
  </action>
  <verify>
    <automated>grep "PATCH\|DELETE" app/src/app/api/tasks/[id]/steps/[stepId]/route.ts | head -4</automated>
  </verify>
  <done>PATCH/DELETE para pasos individuales con auto-reorder en delete.</done>
</task>

<task type="auto">
  <name>Task 5: POST /api/tasks/{id}/steps/reorder</name>
  <files>app/src/app/api/tasks/[id]/steps/reorder/route.ts</files>
  <action>
Create `app/src/app/api/tasks/[id]/steps/reorder/route.ts` with:

**POST** — Reordenar pasos:
1. Leer body: `{ order: string[] }` — array de step IDs en el nuevo orden
2. Validar que `order` es un array
3. Usar transaction para atomicidad:
   ```typescript
   const reorder = db.transaction((taskId: string, order: string[]) => {
     for (let i = 0; i < order.length; i++) {
       db.prepare('UPDATE task_steps SET order_index = ? WHERE id = ? AND task_id = ?').run(i, order[i], taskId);
     }
     db.prepare("UPDATE tasks SET updated_at = datetime('now') WHERE id = ?").run(taskId);
   });
   reorder(params.id, order);
   ```
4. Retornar `{ success: true }`

Incluir `export const dynamic = 'force-dynamic'`.
  </action>
  <verify>
    <automated>grep "reorder\|order_index" app/src/app/api/tasks/[id]/steps/reorder/route.ts | head -3</automated>
  </verify>
  <done>POST reorder con transaccion atomica.</done>
</task>

<task type="auto">
  <name>Task 6: GET /api/tasks/templates</name>
  <files>app/src/app/api/tasks/templates/route.ts</files>
  <action>
Create `app/src/app/api/tasks/templates/route.ts` with:

**GET** — Listar templates:
1. `SELECT * FROM task_templates ORDER BY times_used DESC, name ASC`
2. Retornar array

Incluir `export const dynamic = 'force-dynamic'`.
  </action>
  <verify>
    <automated>grep "task_templates" app/src/app/api/tasks/templates/route.ts</automated>
  </verify>
  <done>GET /api/tasks/templates lista templates.</done>
</task>

<task type="auto">
  <name>Task 7: POST /api/tasks/from-template</name>
  <files>app/src/app/api/tasks/from-template/route.ts</files>
  <action>
Create `app/src/app/api/tasks/from-template/route.ts` with:

**POST** — Crear tarea desde template:
1. Leer body: `template_id` (required), `name` (required), `description`, `expected_output`, `linked_projects`
2. Obtener template: `SELECT * FROM task_templates WHERE id = ?` → 404 si no existe
3. Generar task ID con `uuidv4()`
4. INSERT tarea con name, description, expected_output, linked_projects, status='draft'
5. Parsear `template.steps_config` (JSON array)
6. Para cada step config en el array:
   - Generar step ID con `uuidv4()`
   - INSERT task_step con: task_id, order_index (posicion en array), type, name, instructions, context_mode, use_project_rag (del config)
   - agent_id, agent_name, agent_model quedan NULL (el usuario los asignara en el wizard)
7. Incrementar `times_used` del template: `UPDATE task_templates SET times_used = times_used + 1 WHERE id = ?`
8. Retornar la tarea completa con sus pasos (como GET /api/tasks/{id})

Incluir `export const dynamic = 'force-dynamic'`.
  </action>
  <verify>
    <automated>grep "from-template\|steps_config\|template_id" app/src/app/api/tasks/from-template/route.ts | head -3</automated>
  </verify>
  <done>POST from-template crea tarea con pasos pre-configurados desde template.</done>
</task>

<task type="auto">
  <name>Task 8: Verificar build completo</name>
  <files>app/src/app/api/tasks/route.ts, app/src/app/api/tasks/[id]/route.ts</files>
  <action>
1. Run `cd /home/deskmath/docflow/app && npm run build` and verify it passes.
2. If there are errors, fix them (typical: missing imports, type issues, path issues).
3. Verify all 7 route files exist and have correct exports.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build passes with all Phase 4 API routes.</done>
</task>

</tasks>

<verification>
1. `ls app/src/app/api/tasks/route.ts` — tasks list/create
2. `ls app/src/app/api/tasks/[id]/route.ts` — task detail/update/delete
3. `ls app/src/app/api/tasks/[id]/steps/route.ts` — steps list/create
4. `ls app/src/app/api/tasks/[id]/steps/[stepId]/route.ts` — step edit/delete
5. `ls app/src/app/api/tasks/[id]/steps/reorder/route.ts` — reorder
6. `ls app/src/app/api/tasks/templates/route.ts` — templates list
7. `ls app/src/app/api/tasks/from-template/route.ts` — create from template
8. `grep "force-dynamic" app/src/app/api/tasks/route.ts` — dynamic export
9. `cd app && npm run build` — passes
</verification>

<success_criteria>
1. 7 route files created with correct Next.js App Router structure
2. GET /api/tasks returns enriched task list with step counts, agents, projects
3. POST /api/tasks creates task with draft status
4. PATCH /api/tasks/{id} does dynamic field updates
5. DELETE /api/tasks/{id} cascades to steps
6. POST /api/tasks/{id}/steps validates max 10, auto-reorders
7. DELETE /api/tasks/{id}/steps/{stepId} auto-reorders remaining
8. POST /api/tasks/{id}/steps/reorder uses transaction
9. GET /api/tasks/templates returns templates
10. POST /api/tasks/from-template creates task with pre-configured steps
11. All routes have force-dynamic export
12. npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-crud-tasks-steps-templates/04-01-SUMMARY.md`
</output>
