---
phase: 52-cors-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/catbrains/new/page.tsx
  - app/src/components/process/process-panel.tsx
  - app/src/components/process/version-history.tsx
  - app/src/components/projects/project-settings-sheet.tsx
  - app/src/app/api/agents/route.ts
  - app/src/app/api/agents/[id]/route.ts
autonomous: true
requirements: [CORS-01, CORS-02, CORS-03]

must_haves:
  truths:
    - "GET /api/agents devuelve JSON array sin error CORS ni redirect a 0.0.0.0"
    - "GET/PUT/DELETE /api/agents/[id] devuelven respuestas correctas via proxy interno"
    - "Ningun fetch en /catbrains/[id] ni componentes hijos referencia /api/agents o /api/workers"
  artifacts:
    - path: "app/src/app/api/agents/route.ts"
      provides: "Proxy interno via NextResponse.rewrite a /api/cat-paws"
      contains: "NextResponse.rewrite"
    - path: "app/src/app/api/agents/[id]/route.ts"
      provides: "Proxy interno via NextResponse.rewrite a /api/cat-paws/[id]"
      contains: "NextResponse.rewrite"
  key_links:
    - from: "app/src/components/process/process-panel.tsx"
      to: "/api/cat-paws"
      via: "fetch directo"
      pattern: "fetch\\('/api/cat-paws'\\)"
    - from: "app/src/components/process/version-history.tsx"
      to: "/api/cat-paws?mode=processor"
      via: "fetch directo"
      pattern: "fetch\\('/api/cat-paws\\?mode=processor'\\)"
---

<objective>
Eliminar errores CORS y redirects en llamadas a /api/agents desde paginas CatBrain.

Purpose: Las paginas de CatBrain hacen fetch a /api/agents que internamente importa funciones de /api/cat-paws, causando redirects a 0.0.0.0 en Docker. Cambiamos los fetch del frontend a /api/cat-paws directamente y reescribimos los alias routes para usar NextResponse.rewrite como fallback.
Output: 5 archivos frontend actualizados + 2 alias routes reescritas.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-cors-fix/52-RESEARCH.md
@app/src/app/api/cat-paws/route.ts
@app/src/app/api/cat-paws/[id]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reemplazar fetch URLs en frontend de /api/agents a /api/cat-paws</name>
  <files>
    app/src/app/catbrains/new/page.tsx,
    app/src/components/process/process-panel.tsx,
    app/src/components/process/version-history.tsx,
    app/src/components/projects/project-settings-sheet.tsx
  </files>
  <action>
Cambiar las URLs de fetch en estos 4 archivos. Cambios exactos:

1. `app/src/app/catbrains/new/page.tsx` linea 61:
   - `fetch('/api/agents')` -> `fetch('/api/cat-paws')`

2. `app/src/components/process/process-panel.tsx` linea 117:
   - `fetch('/api/agents')` -> `fetch('/api/cat-paws')`

3. `app/src/components/process/version-history.tsx` linea 82:
   - `fetch('/api/agents')` -> `fetch('/api/cat-paws')`

4. `app/src/components/process/version-history.tsx` linea 83:
   - `fetch('/api/workers')` -> `fetch('/api/cat-paws?mode=processor')`

5. `app/src/components/projects/project-settings-sheet.tsx` linea 97:
   - `fetch('/api/agents')` -> `fetch('/api/cat-paws')`

IMPORTANTE: NO tocar estos archivos (usan rutas reales de agents, no alias):
- `app/src/components/agents/agent-creator.tsx` — usa `/api/agents/generate`, `/api/agents/create`, `/api/agents/${id}` que son rutas propias, no alias.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && grep -rn "fetch('/api/agents')" app/src/app/catbrains/ app/src/components/process/ app/src/components/projects/project-settings-sheet.tsx; grep -rn "fetch('/api/workers')" app/src/components/process/; echo "Si no hay output, OK"</automated>
  </verify>
  <done>Cero ocurrencias de fetch('/api/agents') en catbrains/, process/, project-settings-sheet.tsx. Cero ocurrencias de fetch('/api/workers') en process/.</done>
</task>

<task type="auto">
  <name>Task 2: Reescribir alias routes /api/agents y /api/agents/[id] con NextResponse.rewrite</name>
  <files>
    app/src/app/api/agents/route.ts,
    app/src/app/api/agents/[id]/route.ts
  </files>
  <action>
Reemplazar la importacion directa de funciones de cat-paws con NextResponse.rewrite. Esto mantiene compatibilidad hacia atras para cualquier llamada externa que aun use /api/agents.

**app/src/app/api/agents/route.ts** — Reescribir completamente:
```typescript
import { NextRequest, NextResponse } from 'next/server';

export const dynamic = 'force-dynamic';

export async function GET(request: NextRequest) {
  const url = request.nextUrl.clone();
  url.pathname = '/api/cat-paws';
  return NextResponse.rewrite(url);
}

export async function POST(request: NextRequest) {
  const url = request.nextUrl.clone();
  url.pathname = '/api/cat-paws';
  return NextResponse.rewrite(url);
}
```

**app/src/app/api/agents/[id]/route.ts** — Reescribir completamente:
```typescript
import { NextRequest, NextResponse } from 'next/server';

export const dynamic = 'force-dynamic';

export async function GET(request: NextRequest, context: { params: Promise<{ id: string }> }) {
  const { id } = await context.params;
  const url = request.nextUrl.clone();
  url.pathname = `/api/cat-paws/${id}`;
  return NextResponse.rewrite(url);
}

export async function PATCH(request: NextRequest, context: { params: Promise<{ id: string }> }) {
  const { id } = await context.params;
  const url = request.nextUrl.clone();
  url.pathname = `/api/cat-paws/${id}`;
  return NextResponse.rewrite(url);
}

export async function DELETE(request: NextRequest, context: { params: Promise<{ id: string }> }) {
  const { id } = await context.params;
  const url = request.nextUrl.clone();
  url.pathname = `/api/cat-paws/${id}`;
  return NextResponse.rewrite(url);
}
```

NOTA: Usar NextRequest (no Request) para tener acceso a nextUrl.clone(). Mantener `export const dynamic = 'force-dynamic'` en ambos archivos.

NO tocar: /api/agents/create/route.ts, /api/agents/generate/route.ts, /api/agents/[id]/files/route.ts — son implementaciones reales.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && grep -n "import.*from.*cat-paws" app/src/app/api/agents/route.ts app/src/app/api/agents/\[id\]/route.ts; echo "Si no hay output, OK — ya no importan de cat-paws" && grep -n "NextResponse.rewrite" app/src/app/api/agents/route.ts app/src/app/api/agents/\[id\]/route.ts</automated>
  </verify>
  <done>Ambos alias routes usan NextResponse.rewrite en lugar de importacion directa. Cero imports de cat-paws en estos archivos.</done>
</task>

<task type="auto">
  <name>Task 3: Verificar build y ausencia total de fetch legacy</name>
  <files></files>
  <action>
Ejecutar verificaciones finales:

1. `npm run build` en app/ — debe pasar sin errores
2. Grep global para confirmar que NO queda ningun fetch('/api/agents') en componentes de catbrains ni process (excluyendo agent-creator.tsx que es correcto)
3. Grep global para confirmar que NO queda fetch('/api/workers') en componentes de catbrains ni process

Si el build falla, corregir errores de tipo (probablemente Request vs NextRequest).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build exitoso. Cero fetch legacy en componentes de catbrains/process.</done>
</task>

</tasks>

<verification>
1. `grep -rn "fetch('/api/agents')" app/src/app/catbrains/ app/src/components/process/ app/src/components/projects/project-settings-sheet.tsx` — sin resultados
2. `grep -rn "fetch('/api/workers')" app/src/components/process/` — sin resultados
3. `grep -n "import.*from.*cat-paws" app/src/app/api/agents/route.ts app/src/app/api/agents/[id]/route.ts` — sin resultados
4. `grep -n "NextResponse.rewrite" app/src/app/api/agents/route.ts app/src/app/api/agents/[id]/route.ts` — 5 ocurrencias (2 en route.ts, 3 en [id]/route.ts)
5. `cd app && npm run build` — exit code 0
</verification>

<success_criteria>
- GET /api/agents retorna JSON via rewrite a /api/cat-paws (sin redirect HTTP, sin CORS)
- GET/PATCH/DELETE /api/agents/[id] retorna respuestas via rewrite a /api/cat-paws/[id]
- Todos los fetch en componentes de CatBrain apuntan directamente a /api/cat-paws
- Build de Next.js pasa sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/52-cors-fix/52-01-SUMMARY.md`
</output>
