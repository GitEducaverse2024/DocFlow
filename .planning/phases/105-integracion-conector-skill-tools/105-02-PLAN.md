---
phase: 105-integracion-conector-skill-tools
plan: 02
type: execute
wave: 2
depends_on: [105-01]
files_modified:
  - app/src/lib/services/execute-catpaw.ts
  - app/src/lib/services/canvas-executor.ts
  - app/e2e/api/email-templates.api.spec.ts
autonomous: true
requirements: [INT-06, INT-07]
must_haves:
  truths:
    - "executeCatPaw con conector email_template carga las 3 tools en el loop de tool-calling"
    - "email_template se salta en la fase pre-fetch de conectores (no intenta HTTP fetch)"
    - "Canvas connector node tipo email_template renderiza HTML y lo pasa downstream"
    - "Test E2E verifica CRUD de conector, existencia de skill seed, y render de template via API"
  artifacts:
    - path: "app/src/lib/services/execute-catpaw.ts"
      provides: "Wiring de email_template tools en el tool-calling loop"
      contains: "emailTemplateToolDispatch"
    - path: "app/src/lib/services/canvas-executor.ts"
      provides: "Nodo connector email_template en canvas"
      contains: "email_template"
    - path: "app/e2e/api/email-templates.api.spec.ts"
      provides: "Tests E2E para INT-01 a INT-05 y INT-07 parcial"
  key_links:
    - from: "execute-catpaw.ts"
      to: "catpaw-email-template-tools.ts"
      via: "import getEmailTemplateToolsForPaw"
      pattern: "getEmailTemplateToolsForPaw"
    - from: "execute-catpaw.ts"
      to: "catpaw-email-template-executor.ts"
      via: "import executeEmailTemplateToolCall"
      pattern: "executeEmailTemplateToolCall"
    - from: "canvas-executor.ts"
      to: "template-renderer.ts"
      via: "import renderTemplate for email_template node"
      pattern: "renderTemplate"
---

<objective>
Conectar las tools y executor del Plan 01 al loop de tool-calling de execute-catpaw.ts, anadir soporte en canvas-executor.ts, y crear el test E2E.

Purpose: Sin este wiring, las tools existen pero nunca se cargan. Este plan cierra el circuito: CatPaw -> tools -> executor -> BD -> HTML.
Output: execute-catpaw.ts carga tools email_template, canvas-executor.ts renderiza templates, test E2E valida el flujo.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-integracion-conector-skill-tools/105-RESEARCH.md
@.planning/phases/105-integracion-conector-skill-tools/105-01-SUMMARY.md

@app/src/lib/services/execute-catpaw.ts
@app/src/lib/services/canvas-executor.ts
@app/e2e/api/gmail.api.spec.ts

<interfaces>
<!-- Exportados por Plan 01 que este plan consume -->

From app/src/lib/services/catpaw-email-template-tools.ts:
```typescript
export interface EmailTemplateToolDispatch {
  connectorId: string;
  connectorName: string;
  operation: string;
}

export function getEmailTemplateToolsForPaw(
  pawId: string,
  connectors: { connectorId: string; connectorName: string }[]
): { tools: ToolDefinition[]; dispatch: Map<string, EmailTemplateToolDispatch> }
```

From app/src/lib/services/catpaw-email-template-executor.ts:
```typescript
export async function executeEmailTemplateToolCall(
  pawId: string,
  dispatch: EmailTemplateToolDispatch,
  args: Record<string, unknown>
): Promise<string>
```

From app/src/lib/services/template-renderer.ts:
```typescript
export function renderTemplate(
  structure: TemplateStructure,
  variables?: Record<string, string>
): { html: string; text: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiring en execute-catpaw.ts y canvas-executor.ts</name>
  <files>
    app/src/lib/services/execute-catpaw.ts,
    app/src/lib/services/canvas-executor.ts
  </files>
  <action>
    **Parte A — execute-catpaw.ts: Skip en pre-fetch (linea 121):**

    Cambiar:
    ```typescript
    if (['google_drive', 'gmail'].includes(conn.connector_type)) {
    ```
    Por:
    ```typescript
    if (['google_drive', 'gmail', 'email_template'].includes(conn.connector_type)) {
    ```

    CRITICO: Sin esto, el sistema intentara hacer HTTP fetch a un conector sin URL y fallara.

    **Parte B — execute-catpaw.ts: Imports (al inicio del archivo):**

    Anadir imports:
    ```typescript
    import { getEmailTemplateToolsForPaw, EmailTemplateToolDispatch } from './catpaw-email-template-tools';
    import { executeEmailTemplateToolCall } from './catpaw-email-template-executor';
    ```

    **Parte C — execute-catpaw.ts: Declarar dispatch map (junto a lineas 307-308):**

    Despues de `const gmailToolDispatch = new Map<string, GmailToolDispatch>();` anadir:
    ```typescript
    const emailTemplateToolDispatch = new Map<string, EmailTemplateToolDispatch>();
    ```

    **Parte D — execute-catpaw.ts: Bloque de tools (despues del bloque MCP, ~linea 400):**

    Anadir DESPUES del bloque de MCP tools (buscar el cierre del `if (mcpConnectors.length > 0) {`):
    ```typescript
    // Email Template tools
    const emailTemplateConnectors = linkedConnectors.filter(c => c.connector_type === 'email_template');
    if (emailTemplateConnectors.length > 0) {
      const etInfos = emailTemplateConnectors.map(c => ({
        connectorId: c.connector_id,
        connectorName: c.connector_name,
      }));
      const { tools: etTools, dispatch: etDispatchMap } = getEmailTemplateToolsForPaw(pawId, etInfos);
      openAITools.push(...etTools);
      etDispatchMap.forEach((info, name) => emailTemplateToolDispatch.set(name, info));

      systemParts.push(`\n--- EMAIL TEMPLATES ---\nTienes herramientas para trabajar con plantillas de email corporativas: list_email_templates, get_email_template, render_email_template. Usa get_email_template para ver las instrucciones (variables) que debes rellenar. Las claves de variable deben coincidir EXACTAMENTE con el campo "text" de cada bloque instruction. Luego usa render_email_template para generar el HTML final.\n--- FIN EMAIL TEMPLATES ---`);
      logger.info('cat-paws', `Loaded ${etTools.length} email template tools for executeCatPaw`, { pawId });
    }
    ```

    **Parte E — execute-catpaw.ts: Dispatch en tool-calling loop (~linea 480):**

    En el bloque `for (const tc of toolCalls)`, despues del `else if (mcpDispatch)` y ANTES del `else { result = JSON.stringify({ error: 'Unknown tool' }) }`, anadir:

    ```typescript
    } else if (emailTemplateToolDispatch.has(tc.function.name)) {
      const etDispatch = emailTemplateToolDispatch.get(tc.function.name)!;
      try {
        result = await executeEmailTemplateToolCall(pawId, etDispatch, args);
        logger.info('cat-paws', `Email template tool ${tc.function.name} completed`, { pawId });
      } catch (err) {
        result = JSON.stringify({ error: `Email template tool error: ${(err as Error).message}` });
      }
    ```

    Asegurar que el `else` final (Unknown tool) queda al final de la cadena if/else if.

    **Parte F — canvas-executor.ts: Nodo connector email_template (~despues de linea 574):**

    Dentro del case 'connector' de executeCanvasNode, despues del bloque de Google Drive (que termina con un return) y antes de cualquier bloque MCP, anadir:

    ```typescript
    // Email Template connector: render template with predecessor output as variables
    if ((connector.type as string) === 'email_template') {
      const config = connector.config ? JSON.parse(connector.config as string) : {};
      const templateId = (data.template_id as string) || config.default_template_id;

      if (!templateId) {
        logger.warn('canvas', 'Email template connector: no template_id configured', { canvasId, nodeId: node.id });
        return { output: predecessorOutput };
      }

      try {
        const template = db.prepare('SELECT * FROM email_templates WHERE id = ? AND is_active = 1').get(templateId) as Record<string, unknown> | undefined;
        if (!template) {
          logger.warn('canvas', 'Email template not found or inactive', { canvasId, nodeId: node.id, templateId });
          return { output: predecessorOutput };
        }

        const structure: TemplateStructure = JSON.parse(template.structure as string);

        // Try to parse predecessor output as JSON variables map
        let variables: Record<string, string> = {};
        try {
          const parsed = JSON.parse(predecessorOutput);
          if (typeof parsed === 'object' && parsed !== null && !Array.isArray(parsed)) {
            variables = parsed as Record<string, string>;
          }
        } catch {
          // Not JSON — use predecessor output as content for first instruction block
          const firstInstruction = findFirstInstructionKey(structure);
          if (firstInstruction) {
            variables = { [firstInstruction]: predecessorOutput };
          }
        }

        const { html, text } = renderTemplate(structure, variables);

        logger.info('canvas', 'Email template rendered', {
          canvasId, nodeId: node.id, templateId, templateName: template.name,
          htmlLength: html.length, variableCount: Object.keys(variables).length,
        });

        // Pass rendered HTML downstream (next node, e.g. gmail connector, uses it as email body)
        return { output: html };
      } catch (err) {
        logger.error('canvas', 'Email template render failed', {
          canvasId, nodeId: node.id, templateId, error: (err as Error).message,
        });
        return { output: predecessorOutput };
      }
    }
    ```

    Tambien anadir una funcion helper (al final del archivo o cerca del bloque connector):
    ```typescript
    /** Find the text of the first instruction block in a template structure */
    function findFirstInstructionKey(structure: TemplateStructure): string | null {
      for (const section of Object.values(structure.sections)) {
        for (const row of section.rows) {
          for (const col of row.columns) {
            if (col.block.type === 'instruction' && col.block.text) {
              return col.block.text;
            }
          }
        }
      }
      return null;
    }
    ```

    Imports necesarios en canvas-executor.ts (si no existen ya):
    ```typescript
    import { renderTemplate } from '@/lib/services/template-renderer';
    import type { TemplateStructure } from '@/lib/types';
    ```
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - execute-catpaw.ts salta email_template en pre-fetch (linea 121)
    - execute-catpaw.ts carga 3 tools cuando hay conector email_template vinculado
    - execute-catpaw.ts despacha tool calls a executeEmailTemplateToolCall
    - canvas-executor.ts renderiza template con variables del nodo predecessor
    - TypeScript compila sin errores
  </done>
</task>

<task type="auto">
  <name>Task 2: Test E2E — email-templates.api.spec.ts</name>
  <files>app/e2e/api/email-templates.api.spec.ts</files>
  <action>
    Crear test E2E siguiendo patron exacto de `gmail.api.spec.ts`. Usar Playwright test runner con `BASE_URL = process.env.BASE_URL || 'http://localhost:3500'`.

    Definir el mismo type alias `APIRequest` que usan los demas tests.

    Tests a incluir (test.describe.serial):

    **1. "GET /api/email-templates - lista templates activos" (INT-01)**
    - GET `${BASE_URL}/api/email-templates`
    - Expect status 200
    - Expect response.templates es array con al menos 1 item (seed template)
    - Expect primer template tiene campos: id, name, description, category
    - Expect seed template 'seed-template-basic' existe

    **2. "GET /api/email-templates/[id] - obtiene estructura con bloques" (INT-02)**
    - GET `${BASE_URL}/api/email-templates/seed-template-basic`
    - Expect status 200
    - Expect response tiene id, name, structure
    - Expect structure.sections tiene header, body, footer
    - Expect al menos un bloque instruction en body (el seed tiene "Contenido principal del email")

    **3. "POST /api/email-templates/[id]/render - genera HTML final" (INT-03)**
    - POST `${BASE_URL}/api/email-templates/seed-template-basic/render`
    - Body: `{ variables: { "Contenido principal del email": "<p>Hola, este es un test de renderizado.</p>" } }`
    - Expect status 200
    - Expect response.html contiene "Hola, este es un test de renderizado"
    - Expect response.html contiene "<!DOCTYPE html"
    - Expect response.html contiene "table" (table-based layout)

    **4. "POST /api/connectors - crear conector email_template" (INT-04)**
    - POST `${BASE_URL}/api/connectors`
    - Body: `{ name: '[TEST] Email Template E2E', type: 'email_template', emoji: '\u{1F3A8}', config: { tools: ['list_email_templates', 'get_email_template', 'render_email_template'] } }`
    - Expect status 201
    - Guardar connectorId para cleanup
    - Expect response.type === 'email_template'

    **5. "GET /api/connectors - email_template aparece en lista" (INT-04)**
    - GET `${BASE_URL}/api/connectors`
    - Expect algun conector con type 'email_template'

    **6. "DELETE /api/connectors/[id] - cleanup conector test" (INT-04)**
    - DELETE `${BASE_URL}/api/connectors/${connectorId}`
    - Expect status 200

    **7. "Seed connectors - seed-email-template existe" (INT-04)**
    - GET `${BASE_URL}/api/connectors`
    - Expect existe conector con id que contiene 'email-template' o name que contiene 'Plantillas Email'

    **8. "Seed skills - maquetador-email existe" (INT-05)**
    - GET `${BASE_URL}/api/skills`
    - Expect existe skill con id 'maquetador-email' o name 'Maquetador de Email'

    Usar `test.afterAll` para cleanup: si connectorId existe, DELETE.

    NOTA: INT-06 (execute-catpaw integration) y INT-07 (canvas E2E completo) requieren LLM calls que no se prueban en API spec. El test de render (test 3) valida la parte mas critica del pipeline sin necesitar LLM.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx playwright test e2e/api/email-templates.api.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 8 tests en email-templates.api.spec.ts
    - Cubren INT-01 (list), INT-02 (get con estructura), INT-03 (render HTML), INT-04 (CRUD conector), INT-05 (skill seed)
    - Todos los tests pasan contra servidor en localhost:3500
  </done>
</task>

</tasks>

<verification>
```bash
# 1. TypeScript compila
cd /home/deskmath/docflow/app && npx tsc --noEmit

# 2. Build pasa
cd /home/deskmath/docflow/app && npm run build

# 3. Tests E2E pasan
cd /home/deskmath/docflow/app && npx playwright test e2e/api/email-templates.api.spec.ts --reporter=list

# 4. Wiring verificado
grep "email_template" app/src/lib/services/execute-catpaw.ts | wc -l  # Expect >= 4 matches
grep "email_template" app/src/lib/services/canvas-executor.ts | wc -l  # Expect >= 2 matches

# 5. Skip en pre-fetch verificado
grep "'email_template'" app/src/lib/services/execute-catpaw.ts | head -1  # En el array de skip
```
</verification>

<success_criteria>
1. execute-catpaw.ts carga email_template tools cuando hay conector vinculado
2. execute-catpaw.ts salta email_template en pre-fetch connector loop
3. canvas-executor.ts renderiza HTML para nodos connector tipo email_template
4. Tests E2E pasan: list templates, get structure, render HTML, CRUD connector, seeds existen
5. npm run build pasa sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/105-integracion-conector-skill-tools/105-02-SUMMARY.md`
</output>
