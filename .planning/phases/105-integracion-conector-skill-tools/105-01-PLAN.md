---
phase: 105-integracion-conector-skill-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/types.ts
  - app/src/app/api/connectors/route.ts
  - app/src/lib/services/catbrain-connector-executor.ts
  - app/src/lib/services/catbot-tools.ts
  - app/src/lib/services/catpaw-email-template-tools.ts
  - app/src/lib/services/catpaw-email-template-executor.ts
  - app/src/lib/db.ts
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05]
must_haves:
  truths:
    - "El tipo 'email_template' es valido en todas las validaciones del sistema"
    - "Existen 3 tools (list, get, render) con definiciones OpenAI-format"
    - "El executor devuelve datos reales de la BD para las 3 operaciones"
    - "Existe conector seed 'seed-email-template' en BD tras iniciar app"
    - "Existe skill seed 'maquetador-email' en BD tras iniciar app"
  artifacts:
    - path: "app/src/lib/services/catpaw-email-template-tools.ts"
      provides: "Definiciones de tools + dispatch map"
      exports: ["getEmailTemplateToolsForPaw", "EmailTemplateToolDispatch"]
    - path: "app/src/lib/services/catpaw-email-template-executor.ts"
      provides: "Ejecucion de las 3 operaciones contra BD"
      exports: ["executeEmailTemplateToolCall"]
    - path: "app/src/lib/db.ts"
      provides: "Seeds de conector y skill"
      contains: "seed-email-template"
  key_links:
    - from: "catpaw-email-template-tools.ts"
      to: "catpaw-email-template-executor.ts"
      via: "dispatch.operation matches switch cases"
      pattern: "operation.*list_templates|get_template|render_template"
    - from: "catpaw-email-template-executor.ts"
      to: "template-renderer.ts"
      via: "import renderTemplate"
      pattern: "renderTemplate\\(structure.*variables\\)"
---

<objective>
Crear los archivos de tools y executor para email templates, registrar el tipo de conector en todo el sistema, y seedear el conector + skill "Maquetador de Email".

Purpose: Establece los bloques fundamentales (tools, executor, tipo registrado, seeds) que el Plan 02 conectara al loop de tool-calling y canvas.
Output: 2 archivos nuevos (tools + executor), tipo registrado en 6 ubicaciones, 2 seeds en db.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-integracion-conector-skill-tools/105-RESEARCH.md

@app/src/lib/services/catpaw-gmail-tools.ts
@app/src/lib/services/catpaw-gmail-executor.ts
@app/src/lib/services/template-renderer.ts
@app/src/lib/types.ts
@app/src/app/api/connectors/route.ts
@app/src/lib/services/catbrain-connector-executor.ts
@app/src/lib/services/catbot-tools.ts
@app/src/lib/db.ts

<interfaces>
<!-- Tipos y funciones que el executor necesita importar -->

From app/src/lib/services/template-renderer.ts (linea 114):
```typescript
export function renderTemplate(
  structure: TemplateStructure,
  variables?: Record<string, string>
): { html: string; text: string }
```

From app/src/lib/types.ts (linea 208-222):
```typescript
export interface Connector {
  id: string;
  name: string;
  type: 'n8n_webhook' | 'http_api' | 'mcp_server' | 'email' | 'gmail' | 'google_drive';
  // ... resto de campos
}
```

From app/src/lib/types.ts — TemplateStructure (usado por renderTemplate):
```typescript
export interface TemplateStructure {
  sections: { header: TemplateSection; body: TemplateSection; footer: TemplateSection };
  styles?: { backgroundColor: string; fontFamily: string; primaryColor: string; textColor: string; maxWidth: number };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar tipo email_template en todo el sistema + crear tools y executor</name>
  <files>
    app/src/lib/types.ts,
    app/src/app/api/connectors/route.ts,
    app/src/lib/services/catbrain-connector-executor.ts,
    app/src/lib/services/catbot-tools.ts,
    app/src/lib/services/catpaw-email-template-tools.ts,
    app/src/lib/services/catpaw-email-template-executor.ts
  </files>
  <action>
    **Parte A — Registrar tipo en 4 archivos existentes:**

    1. `app/src/lib/types.ts` linea 213: Cambiar union de Connector.type a:
       `'n8n_webhook' | 'http_api' | 'mcp_server' | 'email' | 'gmail' | 'google_drive' | 'email_template'`

    2. `app/src/lib/types.ts` linea 228: Cambiar union de CatBrainConnector.type a:
       `'n8n_webhook' | 'http_api' | 'mcp_server' | 'email' | 'email_template'`

    3. `app/src/app/api/connectors/route.ts` linea 7: Anadir 'email_template' al array VALID_TYPES:
       `const VALID_TYPES = ['n8n_webhook', 'http_api', 'mcp_server', 'email', 'gmail', 'google_drive', 'email_template'];`

    4. `app/src/lib/services/catbrain-connector-executor.ts` linea 22: Anadir al union de ConnectorRow.type:
       `type: 'n8n_webhook' | 'http_api' | 'mcp_server' | 'email' | 'gmail' | 'google_drive' | 'email_template';`

    5. `app/src/lib/services/catbot-tools.ts` linea 110: Anadir 'email_template' al enum del tool create_connector:
       `type: { type: 'string', enum: ['n8n_webhook', 'http_api', 'mcp_server', 'email', 'email_template'], description: 'Tipo de conector' }`

    **Parte B — Crear catpaw-email-template-tools.ts:**

    Seguir EXACTAMENTE el patron de catpaw-gmail-tools.ts. Exportar:
    - `EmailTemplateToolDispatch` interface con `connectorId`, `connectorName`, `operation`
    - `getEmailTemplateToolsForPaw(pawId, connectors)` que devuelve `{ tools, dispatch }`

    Definir 3 tools en formato OpenAI function-calling:
    - `list_email_templates`: parametro opcional `category` (string). Descripcion: "Lista las plantillas de email disponibles con nombre, descripcion y categoria."
    - `get_email_template`: parametro requerido `template_id` (string). Descripcion: "Obtiene estructura completa de una plantilla con bloques e instrucciones. Los campos 'text' de bloques instruction son las claves de variable para render."
    - `render_email_template`: parametros requeridos `template_id` (string) y `variables` (object). Descripcion: "Renderiza plantilla con variables. Las claves DEBEN coincidir exactamente con el campo 'text' de los bloques instruction. Devuelve HTML final."

    NO usar name-prefixing (a diferencia de Gmail que soporta multiples cuentas). Solo una instancia de connector email_template tiene sentido (templates son globales).

    **Parte C — Crear catpaw-email-template-executor.ts:**

    Seguir patron de catpaw-gmail-executor.ts. Exportar:
    - `executeEmailTemplateToolCall(pawId, dispatch, args)` que devuelve `Promise<string>`

    Switch en `dispatch.operation`:
    - `list_templates`: Query `SELECT id, name, description, category FROM email_templates WHERE is_active = 1`, con filtro opcional por `args.category`. ORDER BY updated_at DESC. Devolver `JSON.stringify(templates)`.
    - `get_template`: Query template por `args.template_id`. Parsear `structure` como `TemplateStructure`. Recorrer sections->rows->columns->block, extraer los que tienen `type === 'instruction'` y su `text`. Devolver JSON con id, name, description, category, instructions (array de strings = claves de variable), y structure completa.
    - `render_template`: Query template por `args.template_id`. Parsear structure. Llamar `renderTemplate(structure, variables)` de template-renderer.ts. Actualizar `times_used` con `UPDATE email_templates SET times_used = times_used + 1, updated_at = ? WHERE id = ?`. Devolver JSON con html, text, template_id, template_name.

    Imports necesarios:
    ```typescript
    import db from '@/lib/db';
    import { renderTemplate } from '@/lib/services/template-renderer';
    import type { TemplateStructure } from '@/lib/types';
    import type { EmailTemplateToolDispatch } from './catpaw-email-template-tools';
    ```

    Tipar el row de BD como `{ id: string; name: string; description: string; category: string; structure: string; is_active: number; times_used: number } | undefined`.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Tipo 'email_template' aceptado en VALID_TYPES, Connector.type, CatBrainConnector.type, ConnectorRow.type, y create_connector enum
    - catpaw-email-template-tools.ts exporta getEmailTemplateToolsForPaw con 3 definiciones de tool
    - catpaw-email-template-executor.ts exporta executeEmailTemplateToolCall con 3 operaciones
    - TypeScript compila sin errores
  </done>
</task>

<task type="auto">
  <name>Task 2: Seeds en db.ts — conector email_template + skill Maquetador de Email</name>
  <files>app/src/lib/db.ts</files>
  <action>
    Anadir justo ANTES de la linea `export default db;` (linea 4463) dos bloques de seed:

    **Seed 1 — Conector email_template:**
    ```typescript
    // Seed: Email Template connector
    try {
      const etConnectorExists = (db.prepare(
        "SELECT COUNT(*) as c FROM connectors WHERE id = 'seed-email-template'"
      ).get() as { c: number }).c;
      if (etConnectorExists === 0) {
        const now = new Date().toISOString();
        db.prepare(`
          INSERT OR IGNORE INTO connectors (id, name, type, config, description, emoji, is_active, created_at, updated_at)
          VALUES (?, ?, ?, ?, ?, ?, 1, ?, ?)
        `).run(
          'seed-email-template',
          'Plantillas Email Corporativas',
          'email_template',
          JSON.stringify({ tools: ['list_email_templates', 'get_email_template', 'render_email_template'] }),
          'Conector para acceder a las plantillas de email de DoCatFlow. Permite listar, consultar y renderizar templates HTML corporativos.',
          '\u{1F3A8}',
          now, now
        );
      }
    } catch (e) { logger.error('system', 'Email template connector seed error', { error: (e as Error).message }); }
    ```

    **Seed 2 — Skill "Maquetador de Email":**
    Seguir patron exacto de los skill seeds existentes (lineas 4293-4370 de db.ts). ID fijo: `'maquetador-email'`.

    ```typescript
    // Seed: Maquetador de Email skill
    try {
      const maquetadorCount = (db.prepare(
        "SELECT COUNT(*) as count FROM skills WHERE id = 'maquetador-email'"
      ).get() as { count: number }).count;
      if (maquetadorCount === 0) {
        const now = new Date().toISOString();
        db.prepare(`INSERT INTO skills (id, name, description, category, tags, instructions, output_template, constraints, source, version, is_featured, created_at, updated_at)
          VALUES (?, ?, ?, ?, ?, ?, ?, ?, 'built-in', '1.0', 1, ?, ?)`).run(
          'maquetador-email',
          'Maquetador de Email',
          'Selecciona y rellena plantillas de email corporativas automaticamente segun el contexto del mensaje (remitente, destinatario, tipo de comunicacion).',
          'strategy',
          '["email","template","maquetador","html","corporativo"]',
          `Eres un maquetador de emails profesional. Tu trabajo es seleccionar la plantilla de email mas apropiada y rellenarla con contenido real.

## Protocolo de Seleccion Inteligente

1. **Analiza el contexto**: Quien envia, a quien va dirigido, tipo de comunicacion (comercial, informativo, notificacion, informe).
2. **Lista plantillas disponibles**: Usa list_email_templates para ver opciones. Si conoces la categoria, filtra directamente.
3. **Selecciona la plantilla**: Elige la que mejor encaja con el tipo de comunicacion. Reglas:
   - Email comercial/ventas -> categoria "comercial"
   - Informes/resumenes -> categoria "informe"
   - Notificaciones/alertas -> categoria "notificacion"
   - Comunicacion general -> categoria "general" o "corporativa"
4. **Obtiene estructura**: Usa get_email_template para ver los bloques instruction (variables a rellenar).
5. **Rellena variables**: Cada bloque instruction tiene un campo "text" que es la CLAVE de variable. Genera contenido apropiado para cada una. El contenido puede incluir HTML basico (negrita, listas, links).
6. **Renderiza**: Usa render_email_template con template_id y el mapa de variables. Las claves DEBEN coincidir exactamente con los textos de instruccion.

## Reglas de Contenido
- Tono profesional pero cercano
- Parrafos cortos (2-3 lineas maximo)
- Incluir saludo personalizado si se conoce el nombre del destinatario
- Usar negrita para destacar datos clave
- Si hay URLs o links, incluirlos como <a href="...">texto</a>
- NO inventar datos: usar solo la informacion proporcionada en el contexto`,
          null, // output_template
          null, // constraints
          now, now
        );
      }
    } catch (e) { logger.error('system', 'Maquetador skill seed error', { error: (e as Error).message }); }
    ```
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    - Conector 'seed-email-template' se inserta en BD si no existe
    - Skill 'maquetador-email' se inserta en BD si no existe
    - Ambos usan INSERT OR IGNORE / COUNT check pattern consistente con seeds existentes
    - TypeScript compila sin errores
  </done>
</task>

</tasks>

<verification>
```bash
# 1. TypeScript compila
cd /home/deskmath/docflow/app && npx tsc --noEmit

# 2. Los archivos nuevos existen y exportan lo esperado
grep -n "export function getEmailTemplateToolsForPaw" app/src/lib/services/catpaw-email-template-tools.ts
grep -n "export async function executeEmailTemplateToolCall" app/src/lib/services/catpaw-email-template-executor.ts

# 3. Tipo registrado en todas las ubicaciones
grep "email_template" app/src/lib/types.ts
grep "email_template" app/src/app/api/connectors/route.ts
grep "email_template" app/src/lib/services/catbrain-connector-executor.ts
grep "email_template" app/src/lib/services/catbot-tools.ts

# 4. Seeds existen
grep "seed-email-template" app/src/lib/db.ts
grep "maquetador-email" app/src/lib/db.ts
```
</verification>

<success_criteria>
1. 'email_template' aparece en los 6 puntos de registro del sistema (types.ts x2, connectors/route.ts, catbrain-connector-executor.ts, catbot-tools.ts)
2. catpaw-email-template-tools.ts define 3 tools con formato OpenAI function-calling
3. catpaw-email-template-executor.ts ejecuta list/get/render contra BD real
4. render_template usa renderTemplate() de template-renderer.ts
5. Seeds de conector y skill presentes en db.ts
6. npm run build pasa (o al menos tsc --noEmit)
</success_criteria>

<output>
After completion, create `.planning/phases/105-integracion-conector-skill-tools/105-01-SUMMARY.md`
</output>
