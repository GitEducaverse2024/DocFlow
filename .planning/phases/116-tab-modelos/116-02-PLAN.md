---
phase: 116-tab-modelos
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - src/components/settings/model-center/tab-modelos.tsx
  - src/app/settings/page.tsx
  - app/messages/es.json
  - app/messages/en.json
autonomous: true
requirements: [MODELOS-05, MODELOS-06]

must_haves:
  truths:
    - "El usuario puede editar cost_notes inline en la card MID sin abrir dialog"
    - "La tabla de Costes separada (ModelPricingSettings) ya no existe en el codigo"
    - "La seccion Embeddings placeholder ya no existe en el codigo ni en i18n"
  artifacts:
    - path: "src/components/settings/model-center/tab-modelos.tsx"
      provides: "Inline cost editing on MID cards"
      contains: "cost_notes"
    - path: "src/app/settings/page.tsx"
      provides: "Clean page without ModelPricingSettings function"
  key_links:
    - from: "src/components/settings/model-center/tab-modelos.tsx"
      to: "/api/mid/{id}"
      via: "PATCH for inline cost save"
      pattern: "fetch.*api/mid.*PATCH"
---

<objective>
Add inline cost editing to MID cards and remove legacy sections (ModelPricingSettings function, Embeddings placeholder) from the settings page.

Purpose: Cost management happens directly on model cards instead of a separate table, and obsolete code is cleaned up.
Output: Inline cost editing works on cards, old pricing table and embeddings code removed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-tab-modelos/116-RESEARCH.md
@.planning/phases/116-tab-modelos/116-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From src/components/settings/model-center/tab-modelos.tsx:
```typescript
// Each card in TabModelos renders MidEntryExt with display_name, provider, tier, etc.
// Need to add click-to-edit on cost_notes field within each card.
// PATCH /api/mid/{id} accepts { cost_notes: string } and returns { updated: true }
```

From src/app/settings/page.tsx (line ~428):
```typescript
// function ModelPricingSettings() { ... }
// This function is defined but NOT rendered (already replaced by ModelCenterShell)
// Safe to delete entirely
```

From src/app/api/mid/[id]/route.ts:
```typescript
// PATCH handler accepts JSON body with partial MidEntry fields
// Returns { updated: true } on success
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline cost editing to MID cards in TabModelos</name>
  <files>src/components/settings/model-center/tab-modelos.tsx, app/messages/es.json, app/messages/en.json</files>
  <action>
1. In `tab-modelos.tsx`, enhance each MID card to support inline cost editing:
   - Show `cost_notes` text on the card (below best_use or in a dedicated cost section)
   - Also show `cost_tier` as a small badge if present (e.g., "$$" or "Free")
   - On click of the cost_notes area, switch to an Input field (click-to-edit pattern)
   - Use local state per card: `editingCostId: number | null` and `costValue: string`
   - On blur or Enter key: PATCH `/api/mid/${model.id}` with `{ cost_notes: costValue }`
   - On successful save: update local state optimistically (update the model in the models array), show toast success via sonner
   - On error: revert to original value, show toast error
   - Show a small Pencil icon next to cost_notes to indicate it's editable
   - If cost_notes is empty, show placeholder text "Sin notas de coste" / "No cost notes" that is also clickable to start editing

2. Add i18n keys under `settings.modelCenter.modelos`:
   - `costNotes`: "Notas de coste" / "Cost notes"
   - `costNotesPlaceholder`: "Sin notas de coste" / "No cost notes"
   - `costSaved`: "Coste actualizado" / "Cost updated"
   - `costError`: "Error al guardar coste" / "Error saving cost"
  </action>
  <verify>
    <automated>cd ~/docflow/app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Each MID card shows cost_notes with click-to-edit. Saving PATCHes the API and updates the card optimistically. Toast confirms save or shows error.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ModelPricingSettings and Embeddings dead code</name>
  <files>src/app/settings/page.tsx, app/messages/es.json, app/messages/en.json</files>
  <action>
1. In `src/app/settings/page.tsx`:
   - Delete the entire `function ModelPricingSettings() { ... }` block (around line 428). It is NOT rendered in JSX -- already replaced by ModelCenterShell. Remove the eslint-disable comment above it too.
   - Search for any `embeddings` section JSX or reference. Per research, the comment on line ~1673 confirms ModelCenterShell replaced Embeddings. If there's any remaining Embeddings JSX or commented-out code, remove it.
   - Remove any unused imports that were only used by ModelPricingSettings (check for model_pricing state, handlers, etc.)

2. In `app/messages/es.json` and `app/messages/en.json`:
   - Remove `settings.embeddings` keys (title, description, comingSoon) if they exist -- these are orphaned after ModelCenterShell replaced that section
   - Keep any keys that are still referenced by other components (verify with grep before deleting)

3. Verify no remaining references:
   - `grep -r "ModelPricingSettings" src/` should return 0 results
   - `grep -r "embeddings.title\|embeddings.description\|embeddings.comingSoon" src/` should return 0 results (only i18n files, which we cleaned)
  </action>
  <verify>
    <automated>cd ~/docflow/app && npm run build 2>&1 | tail -20 && echo "---" && grep -r "ModelPricingSettings" src/ ; echo "grep exit: $?"</automated>
  </verify>
  <done>ModelPricingSettings function deleted from page.tsx. Embeddings placeholder keys removed from i18n. No references remain to either. Build passes.</done>
</task>

</tasks>

<verification>
- `cd ~/docflow/app && npm run build` passes
- `grep -r "ModelPricingSettings" src/` returns nothing
- `grep -r "embeddings.comingSoon" src/` returns nothing
- Navigate to `/settings?tab=modelos`: click on cost_notes on any card, edit value, save works
- No separate Costes table visible anywhere in Settings
</verification>

<success_criteria>
- Inline cost editing works: click cost_notes on card, type, blur/Enter saves via PATCH
- ModelPricingSettings function completely removed from page.tsx
- Embeddings placeholder i18n keys cleaned up
- Build passes with no errors or warnings related to removed code
</success_criteria>

<output>
After completion, create `.planning/phases/116-tab-modelos/116-02-SUMMARY.md`
</output>
