---
phase: 152-kb-catbot-consume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - app/src/lib/knowledge-tree.ts
  - app/src/lib/services/kb-index-cache.ts
  - app/src/lib/__tests__/kb-test-utils.ts
  - app/src/lib/__tests__/knowledge-tree.test.ts
  - app/src/lib/__tests__/kb-index-cache.test.ts
autonomous: true
requirements: [KB-18]

must_haves:
  truths:
    - "KB-15..KB-18 registered in REQUIREMENTS.md with Phase 152 mapping (foundation registration — full delivery of KB-15/16/17 happens in Plans 02/03/04 respectively; KB-18 is fully delivered in this plan)"
    - "ROADMAP.md Phase 152 section shows Goal + Requirements + Success Criteria (no TBD)"
    - "knowledge-tree.ts Zod schema accepts string | {term,definition} | {__redirect} in concepts/howto/dont arrays — catboard.json parses without throw"
    - "kb-index-cache.ts module exists with getKbIndex(), invalidateKbIndex(), resolveKbEntry(table,id), searchKb(params), getKbEntry(id), parseKbFile(path)"
    - "kb-index-cache reads KB root from KB_ROOT env var with default resolution (app/ → repo-root/.docflow-kb via path.join(process.cwd(), '..', '.docflow-kb'))"
    - "resolveKbEntry builds byTableId map by reading frontmatter from each resource .md file (because _index.json.entries[] does NOT expose source_of_truth — CONFLICT #1 from RESEARCH)"
    - "kb-test-utils.ts exports createFixtureKb(tmpDir) that writes 7-entry fixture (6 resources + 1 rule) with valid frontmatter including source_of_truth for resolveKbEntry to find"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "KB-15, KB-16, KB-17, KB-18 entries under '### Knowledge Base CatBot Consume (Phase 152)' + traceability rows"
      contains: "KB-15"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 152 section with Goal, Requirements (KB-15..KB-18), Success Criteria (no TBD placeholders)"
      contains: "Phase 152: KB CatBot Consume"
    - path: "app/src/lib/knowledge-tree.ts"
      provides: "Extended Zod schema with ConceptItemSchema union (string | {term,definition} | {__redirect})"
      contains: "ConceptItemSchema"
    - path: "app/src/lib/services/kb-index-cache.ts"
      provides: "KB read-path helpers: getKbIndex, invalidateKbIndex, resolveKbEntry, searchKb, getKbEntry, parseKbFile, buildSourceOfTruthCache"
      exports: ["getKbIndex", "invalidateKbIndex", "resolveKbEntry", "searchKb", "getKbEntry", "parseKbFile"]
    - path: "app/src/lib/__tests__/kb-test-utils.ts"
      provides: "createFixtureKb(tmpDir) helper producing fixture KB with 7 entries + valid frontmatter"
      exports: ["createFixtureKb"]
    - path: "app/src/lib/__tests__/kb-index-cache.test.ts"
      provides: "Unit tests for cache TTL, invalidation, resolver hit/miss, frontmatter YAML parse via js-yaml"
    - path: "app/src/lib/__tests__/knowledge-tree.test.ts"
      provides: "Union schema acceptance tests (string/{term,definition}/{__redirect}) + catboard.json parse success"
  key_links:
    - from: "kb-index-cache.ts"
      to: ".docflow-kb/_index.json"
      via: "fs.readFileSync + JSON.parse in getKbIndex()"
      pattern: "readFileSync.*_index\\.json"
    - from: "kb-index-cache.ts buildSourceOfTruthCache"
      to: ".docflow-kb/resources/**/*.md"
      via: "js-yaml.load(frontmatter) for each resource entry"
      pattern: "yaml\\.load"
    - from: "knowledge-tree.ts ConceptItemSchema"
      to: "query_knowledge (Plan 02 consumer)"
      via: "Zod union — KnowledgeEntrySchema.parse(catboard.json) must not throw on {term,definition} nor {__redirect}"
      pattern: "z\\.union"
    - from: "kb-test-utils.ts createFixtureKb"
      to: "kb-index-cache.test.ts, kb-tools.test.ts (Plan 02), kb-tools-integration.test.ts (Plan 03)"
      via: "shared test helper"
      pattern: "createFixtureKb"
---

<objective>
Fundar el consume-side del KB registrando los requirements KB-15..KB-18 en REQUIREMENTS.md y ROADMAP.md, creando el módulo `kb-index-cache.ts` (sin el cual Plans 02/03 no tienen sobre qué construir), extendiendo el Zod schema de `knowledge-tree.ts` para que `query_knowledge` no rompa sobre objetos `{term,definition}` y `{__redirect}` (CONFLICT #2 del RESEARCH), y creando el fixture test helper compartido.

Purpose: Plan 01 es la FUNDACIÓN. Plans 02/03/04 importan `kb-index-cache.ts` y usan `createFixtureKb` — si este plan no cierra, las ondas 2/3/4 fallan. Además registra los IDs de requisitos que de otro modo quedarían huérfanos del ROADMAP.

**Nota sobre requirements frontmatter:** Plan 01 **registra** los 4 IDs (KB-15..KB-18) en REQUIREMENTS.md + ROADMAP.md, pero solo **entrega** KB-18 (Zod schema fix). La entrega completa de KB-15 se materializa en Plan 03 (assembler + list_* kb_entry), KB-16 en Plan 02 (tools), KB-17 en Plan 03 (list_* kb_entry field) y Plan 04 (catboard.json tripwire). Por eso `requirements: [KB-18]` en el frontmatter — los otros IDs aparecen en los frontmatter de Plans 02/03/04 donde se entregan de verdad.

Output:
- REQUIREMENTS.md actualizado con 4 entries nuevos (KB-15..KB-18) + traceability rows
- ROADMAP.md Phase 152 con Goal + Requirements + Success Criteria (no TBD)
- `app/src/lib/knowledge-tree.ts` con `ConceptItemSchema` union y `KnowledgeEntrySchema.passthrough()` para preservar `__redirect*` top-level keys
- `app/src/lib/services/kb-index-cache.ts` nuevo módulo completo (~250 líneas) con getKbIndex/resolveKbEntry/searchKb/getKbEntry/parseKbFile
- `app/src/lib/__tests__/kb-test-utils.ts` con `createFixtureKb(tmpDir)` reusable
- `app/src/lib/__tests__/kb-index-cache.test.ts` con cobertura unit del módulo nuevo
- `app/src/lib/__tests__/knowledge-tree.test.ts` extendido para las 3 shapes (string/object/redirect)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-kb-catbot-consume/152-CONTEXT.md
@.planning/phases/152-kb-catbot-consume/152-RESEARCH.md
@.planning/phases/152-kb-catbot-consume/152-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@app/src/lib/knowledge-tree.ts
@app/src/lib/__tests__/catbot-prompt-assembler.test.ts
@CLAUDE.md

<interfaces>
<!-- Key contracts extracted from codebase so Plan 02/03 executors can build without exploration -->

From `.docflow-kb/_index.json` (entry shape — 126 entries):
```json
{
  "schema_version": "2.0",
  "entry_count": 126,
  "entries": [
    {
      "id": "string",                          // canonical KB id (unique across KB)
      "path": "string",                        // relative to .docflow-kb/, e.g. "resources/catpaws/aaa-foo.md"
      "type": "resource|concept|taxonomy|rule|protocol|runtime|incident|feature|guide|state",
      "subtype": "string | null",              // e.g. "catpaw", "connector", "skill", etc.
      "title": "string",
      "summary": "string",
      "tags": ["string", ...],
      "audience": ["catbot", "architect", ...],
      "status": "active|deprecated|draft|experimental",
      "updated": "ISO date string",
      "search_hints": "string[] | null"
    }
  ],
  "indexes": { "by_type": {}, "by_tag": {}, "by_audience": {} }
}
```

**CRITICAL (CONFLICT #1):** `entries[]` does NOT contain `source_of_truth`. That lives only in the YAML frontmatter of each resource `.md` file. `resolveKbEntry(table, id)` MUST read frontmatter from 66 resource files to build a `byTableId` map.

From `.docflow-kb/resources/catpaws/*.md` frontmatter (sample shape):
```yaml
---
id: string
type: resource
subtype: catpaw
lang: es
title: string
summary: string
tags: [catpaw, ...]
audience: [catbot, architect]
status: active
source_of_truth:
  - db: sqlite
    table: cat_paws
    id: "<full-uuid>"             # e.g. "72ef0fe5-9132-4a08-bc4d-37e8bbb2e6bc"
    fields_from_db: [name, ...]
---
body markdown content...
```

From `app/src/lib/knowledge-tree.ts:13-27` (current Zod schema — to be extended):
```typescript
export const KnowledgeEntrySchema = z.object({
  id: z.string(),
  name: z.string(),
  path: z.string(),
  description: z.string(),
  endpoints: z.array(z.string()),
  tools: z.array(z.string()),
  concepts: z.array(z.string()),    // <- BREAKS on catboard.json[18..20] ({term,definition}) and __redirect objects
  howto: z.array(z.string()),
  dont: z.array(z.string()),
  common_errors: z.array(CommonErrorSchema),
  success_cases: z.array(z.string()),
  sources: z.array(z.string()),
  updated_at: z.string(),
});
```

From `app/src/lib/services/knowledge-sync.ts:398`: internal `parseFrontmatter()` exists but is NOT exported. DO NOT reuse it — create our own read-path parser using `js-yaml@4.1.1` (already in `app/node_modules/`).

From `app/package.json`: `vitest@4.1.0`, `zod@3.25.76` (transitive), `js-yaml@4.1.1` (transitive). NO npm install needed.

From `app/src/lib/__tests__/catbot-tools-retry-job.test.ts:21-32` (vi.hoisted pattern to reuse):
```typescript
vi.hoisted(() => {
  const nodePath = require('path');
  const nodeFs = require('fs');
  const nodeOs = require('os');
  const tmpDir = nodeFs.mkdtempSync(nodePath.join(nodeOs.tmpdir(), 'kbtest-'));
  process['env']['KB_ROOT'] = nodePath.join(tmpDir, '.docflow-kb');
});
```

**CRITICAL — NO `list_connectors` tool exists in catbot-tools.ts.** Grep confirms only `list_email_connectors` (L310). The 6 list_* tools that WILL carry the `kb_entry` field in Plan 03 are: `list_cat_paws` (+ aliases `list_workers`/`list_agents`), `list_catbrains`, `list_skills`, `list_email_templates`, `canvas_list`, and **`list_email_connectors`**. KB-17 spec in REQUIREMENTS.md (this plan's Task 1) enumerates 5 **canonical** list_* tools (`list_cat_paws, list_catbrains, list_skills, list_email_templates, canvas_list`) and marks `list_connectors` as **deferred** (tool does not exist; if connectors exposure becomes necessary later, a new tool would be registered in a future phase). Plan 03 may additionally extend `list_email_connectors` to be consistent, but that is NOT part of the 5-tool contract enshrined in REQUIREMENTS.md KB-17.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register KB-15..KB-18 in REQUIREMENTS.md and finalize ROADMAP.md Phase 152 section</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <behavior>
    Post-task state:
    - REQUIREMENTS.md contains 4 new entries under new section "### Knowledge Base CatBot Consume (Phase 152)" with IDs KB-15, KB-16, KB-17, KB-18 (unchecked `[ ]`).
    - REQUIREMENTS.md Traceability table gains 4 new rows (KB-15..KB-18 mapped to Phase 152, status Pending).
    - REQUIREMENTS.md Coverage section updated: v1 requirements count 26 → 30; Mapped to phases 26/26 → 30/30.
    - ROADMAP.md `### Phase 152: KB CatBot Consume` section has Goal (paragraph from CONTEXT §Phase Boundary), Requirements line listing `KB-15, KB-16, KB-17, KB-18`, Success Criteria block with 5 numbered truths, **Plans:** 4 plans, and the `Plans:` bullet list referencing all 4 PLAN files.
    - KB-17 text enumerates **5** canonical list_* tools (NOT 6), with explicit note that a hypothetical `list_connectors` is deferred (tool does not exist in catbot-tools.ts).
    - No other phase sections modified.
  </behavior>
  <action>
    Use the Edit tool on `.planning/REQUIREMENTS.md`:

    **Add new section** after the existing `### Knowledge Base Migration (Phase 151)` block (around line 54-58) and before `## Future Requirements` (line 60):

    ```markdown
    ### Knowledge Base CatBot Consume (Phase 152)

    - [ ] **KB-15**: `catbot-prompt-assembler.ts` inyecta `.docflow-kb/_header.md` como sección P1 `kb_header` en cada prompt (fresh-read filesystem, posicionada antes de `platform_overview`, graceful si archivo no existe). `buildKnowledgeProtocol()` reescrito para mencionar `search_kb` y `get_kb_entry` como tools primarias, con orden `search_kb → get_kb_entry → query_knowledge (legacy) → search_documentation → log_knowledge_gap`.
    - [ ] **KB-16**: Tools nuevas `search_kb({type?,subtype?,tags?,audience?,status?,search?,limit?})` y `get_kb_entry({id})` registradas en `catbot-tools.ts TOOLS[]` como always-allowed (read-only). `search_kb` devuelve `{total, results[]}` con entries resumidas (summary truncada a 200 chars), ranking por field weights (title×3, summary×2, tags/hints×1), default `status:'active'`, limit default 10 cap 50. `get_kb_entry` devuelve `{id, path, frontmatter, body, related_resolved[]}` con YAML parseado via js-yaml y related resuelto contra `_index.json`.
    - [ ] **KB-17**: Campo `kb_entry: string | null` presente en results de los 5 tools canónicos de listado: `list_cat_paws`, `list_catbrains`, `list_skills`, `list_email_templates`, `canvas_list`. Resuelto vía módulo `app/src/lib/services/kb-index-cache.ts` con cache TTL 60s (mapa byTableId construido leyendo frontmatter de los 66 resource files — `_index.json.entries[]` NO expone `source_of_truth`, por eso hace falta el cold-start read). **Nota:** un hipotético `list_connectors` queda **deferred** — ese tool no existe en `catbot-tools.ts` actualmente (solo `list_email_connectors` en L310); si en el futuro se necesita exponer conectores genéricos como tool de listado, se añadirá en una fase posterior y heredará el mismo patrón `kb_entry`. Plan 03 puede opcionalmente extender `list_email_connectors` por consistencia, pero NO es parte del contrato mínimo de KB-17.
    - [ ] **KB-18**: Zod schema de `knowledge-tree.ts` extendido a `z.union([z.string(), z.object({term,definition}), z.object({__redirect})])` para `concepts/howto/dont` arrays. `KnowledgeEntrySchema` con `.passthrough()` para preservar `__redirect` top-level keys. `query_knowledge` devuelve hint `{type:'redirect', target_kb_path}` cuando encuentra entry migrado. NO throw Zod en `catboard.json.concepts[18..20]` pre-existentes `{term,definition}` (root cause real identificado en RESEARCH §Conflict 2).
    ```

    **Append to Traceability table** (after KB-14 row around line 111):
    ```markdown
    | KB-15 | Phase 152 | Pending |
    | KB-16 | Phase 152 | Pending |
    | KB-17 | Phase 152 | Pending |
    | KB-18 | Phase 152 | Pending |
    ```

    **Update Coverage block** (around line 113-115):
    - `- v1 requirements: 26 total` → `- v1 requirements: 30 total`
    - `- Mapped to phases: 26/26` → `- Mapped to phases: 30/30`
    - Update the "Last updated" footer: `*Last updated: 2026-04-20 after Phase 152 planning (KB-15..KB-18 added)*`

    Use the Edit tool on `.planning/ROADMAP.md`, replacing the entire `### Phase 152: KB CatBot Consume` block (currently ~lines 180-194) with:

    ```markdown
    ### Phase 152: KB CatBot Consume

    **Goal:** CatBot consume el Knowledge Base estructurado. `catbot-prompt-assembler.ts` inyecta `.docflow-kb/_header.md` como sección P1 `kb_header` en cada sesión (fresh-read, antes de `platform_overview`). Nuevas tools `search_kb({type?,subtype?,tags?,audience?,status?,search?,limit?})` y `get_kb_entry({id})` registradas always-allowed en `catbot-tools.ts`. Los 5 tools canónicos de listado (`list_cat_paws`, `list_catbrains`, `list_skills`, `list_email_templates`, `canvas_list`) devuelven campo nuevo `kb_entry: string | null` con path relativo al KB, resuelto vía módulo `kb-index-cache.ts` con cache TTL 60s + byTableId map construido leyendo frontmatter de 66 resource files. (`list_connectors` NO está en scope — no existe como tool; si se necesita exposure genérico de connectors, será otra fase). Fix heredado de Phase 151: Zod schema de `query_knowledge` extendido para admitir `{term,definition}` y `{__redirect}` objects en concepts/howto/dont arrays (root cause: `catboard.json.concepts[18..20]` pre-existentes eran `{term,definition}` — `__redirect` top-level era red herring).
    **Requirements**: KB-15, KB-16, KB-17, KB-18
    **Depends on:** Phase 150 (KB poblado desde DB — completada). Phase 151 enriquece el KB con rules/concepts/protocols pero Phase 152 funciona con solo header+resources de Phase 150.
    **Success Criteria** (what must be TRUE):
      1. Prompt system de CatBot contiene sección `kb_header` con el contenido literal de `.docflow-kb/_header.md` (126 entradas, counts de resources + knowledge), inyectada antes de `platform_overview`. Assembler funciona graceful si el archivo no existe.
      2. Tools `search_kb` y `get_kb_entry` operativas, registradas en TOOLS[] + always-allowed en `getToolsForLLM`. `search_kb({type:'resource',subtype:'catpaw'})` devuelve los 9 catpaws. `get_kb_entry('R10-preserve-fields')` devuelve frontmatter + body + related_resolved.
      3. Los 5 tools canónicos de listado (`list_cat_paws`, `list_catbrains`, `list_skills`, `list_email_templates`, `canvas_list`) devuelven items con campo `kb_entry: string | null` resuelto correctamente contra el frontmatter de los archivos KB. (Nota: `list_connectors` no existe como tool en el codebase actual; queda deferred a una fase futura si se necesita.)
      4. `query_knowledge` NO rompe con Zod validation error al cargar `catboard.json` (que tiene `concepts[18..20]` como objetos `{term,definition}`). Cuando encuentra un entry con `__redirect` top-level, emite hint `{type:'redirect', target_kb_path}` sin throw.
      5. CatBot oracle test (POST `/api/catbot/chat`) ejecuta 3 prompts post-Docker-rebuild: (a) "¿Qué sabes del KB de DocFlow?" → respuesta factual con counts del header; (b) "¿Qué CatPaws existen?" → invoca `list_cat_paws`, response incluye `kb_entry: "resources/catpaws/..."`; (c) "Busca reglas de seguridad" → invoca `search_kb({type:'rule',tags:['safety']})`. Evidence pegada en `152-VERIFICATION.md`.
    **Plans**: 4 plans

    Plans:
    - [ ] 152-01-PLAN.md — Foundation: register KB-15..KB-18, crear `kb-index-cache.ts` module, extender Zod schema de `knowledge-tree.ts`, crear `createFixtureKb` test helper
    - [ ] 152-02-PLAN.md — Tools: registrar `search_kb` + `get_kb_entry` en TOOLS[] + executeTool switch, fix `query_knowledge` redirect hint, actualizar tool description de `query_knowledge` a "Legacy fallback"
    - [ ] 152-03-PLAN.md — Assembler + list_* field: añadir `buildKbHeader()` + insertar sección `kb_header` en assembler, reescribir `buildKnowledgeProtocol()`, inyectar campo `kb_entry` en los 5 list_* tools canónicos
    - [ ] 152-04-PLAN.md — Close: actualizar `catboard.json.tools[]` (tripwire `knowledge-tools-sync`), Docker rebuild, oracle CatBot con 3 prompts, `152-VERIFICATION.md` con evidencia KB-15..KB-18
    ```

    Also update the Progress table row for Phase 152 from `**Plans:** 4/4 plans complete` (which was wrong — it said complete but 0 plans exist) to show `0/4` and the "Plans" count row. Specifically, replace:
    - Line 108 status: `| 152. KB CatBot Consume | 0/? | Not started | - |` → keep `| 152. KB CatBot Consume | 0/4 | Not started | - |`
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8'); if(!r.includes('KB-15')||!r.includes('KB-16')||!r.includes('KB-17')||!r.includes('KB-18')) process.exit(1); if(!r.includes('Phase 152')) process.exit(2); if(!r.includes('list_cat_paws')||!r.includes('canvas_list')) process.exit(3); if(r.match(/KB-17[^\n]*6 tools/)||r.match(/KB-17[^\n]*list_connectors[^\n]*list_cat_paws/)) process.exit(4); console.log('REQUIREMENTS OK — KB-17 enumerates 5 tools, list_connectors marked deferred');"</automated>
    <automated>node -e "const r=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); const m=r.match(/### Phase 152:[\s\S]*?(?=### Phase 153:)/); if(!m) process.exit(1); const s=m[0]; if(!s.includes('KB-15, KB-16, KB-17, KB-18')) process.exit(2); if(s.includes('TBD')) process.exit(3); if(!s.match(/5 tools canónicos|list_cat_paws.*list_catbrains.*list_skills.*list_email_templates.*canvas_list/s)) process.exit(4); console.log('ROADMAP Phase 152 OK — 5-tool enumeration');"</automated>
  </verify>
  <done>
    - REQUIREMENTS.md contains 4 new bullets for KB-15..KB-18 under "### Knowledge Base CatBot Consume (Phase 152)".
    - REQUIREMENTS.md KB-17 text enumerates exactly 5 canonical list_* tools (`list_cat_paws`, `list_catbrains`, `list_skills`, `list_email_templates`, `canvas_list`) and marks `list_connectors` as deferred with rationale (tool does not exist).
    - REQUIREMENTS.md Traceability table has 4 new rows mapping KB-15..KB-18 to Phase 152 (Pending).
    - Coverage section updated to 30/30.
    - ROADMAP.md Phase 152 section has Goal paragraph (with 5-tool enumeration), Requirements line with KB-15..KB-18, Success Criteria with 5 truths (criterion #3 names the 5 canonical tools explicitly), **Plans:** 4 plans, and enumerated list of 4 plan files.
    - No "TBD" remains in Phase 152 ROADMAP section.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend knowledge-tree.ts Zod schema (ConceptItemSchema union) and create kb-index-cache.ts module + test helper + tests</name>
  <files>app/src/lib/knowledge-tree.ts, app/src/lib/services/kb-index-cache.ts, app/src/lib/__tests__/kb-test-utils.ts, app/src/lib/__tests__/knowledge-tree.test.ts, app/src/lib/__tests__/kb-index-cache.test.ts</files>
  <behavior>
    RED first (write tests that fail):
    - `knowledge-tree.test.ts`: Test that `KnowledgeEntrySchema.parse()` accepts `{concepts: ["string-item", {term:'X',definition:'Y'}, {__redirect:'path/foo.md'}]}` without throwing.
    - `knowledge-tree.test.ts`: Test that parsing the real `app/data/knowledge/catboard.json` (has `__redirect` top-level + `concepts[18..20]` as `{term,definition}`) succeeds.
    - `kb-index-cache.test.ts`: Test cache TTL (2 calls within 60s → 1 file read). Test `invalidateKbIndex()` forces re-read.
    - `kb-index-cache.test.ts`: Test `resolveKbEntry('cat_paws', '<uuid-from-fixture>')` returns correct path.
    - `kb-index-cache.test.ts`: Test `resolveKbEntry('cat_paws', 'bogus-id')` returns null.
    - `kb-index-cache.test.ts`: Test `parseKbFile('resources/catpaws/aaa11111-test-catpaw.md')` returns `{frontmatter, body}` where frontmatter has `id`, `type`, `source_of_truth` parsed via js-yaml.
    - `kb-index-cache.test.ts`: Test `getKbIndex()` returns null when _index.json missing (graceful).

    GREEN (implement to make tests pass):
    - `knowledge-tree.ts`: Add `ConceptItemSchema = z.union([z.string(), z.object({term:z.string(),definition:z.string()}).passthrough(), z.object({__redirect:z.string()}).passthrough()])`. Change `concepts/howto/dont: z.array(z.string())` → `z.array(ConceptItemSchema)`. Add `.passthrough()` to `KnowledgeEntrySchema` (preserves `__redirect`/`__redirect_destinations` top-level keys so query_knowledge can detect them in Plan 02).
    - `kb-index-cache.ts`: New module with getKbIndex / invalidateKbIndex / resolveKbEntry / searchKb / getKbEntry / parseKbFile / buildSourceOfTruthCache. Reads KB_ROOT env var with fallback `path.join(process.cwd(), '..', '.docflow-kb')`. Uses `js-yaml.load()` for frontmatter. Cache TTL 60s. byTableId built by reading frontmatter of every entry where `type === 'resource'`.
    - `kb-test-utils.ts`: Export `createFixtureKb(tmpDir)` that creates `.docflow-kb/` structure + `_header.md` + `_index.json` + 7 resource files with valid frontmatter (including `source_of_truth` so resolveKbEntry can map to DB ids).
  </behavior>
  <action>
    **Step 1 — Extend `app/src/lib/knowledge-tree.ts`:**

    Replace lines 7-27 of `knowledge-tree.ts` with:

    ```typescript
    const CommonErrorSchema = z.object({
      error: z.string(),
      cause: z.string(),
      solution: z.string(),
    });

    /**
     * ConceptItemSchema — accepts 3 shapes for concepts/howto/dont arrays:
     * 1. Plain string (canonical pre-Phase-151)
     * 2. {term, definition} object (found in catboard.json[18..20] pre-existing to Phase 151)
     * 3. {__redirect: "path/in/kb"} object (introduced by Phase 151 migrations)
     * `.passthrough()` preserves extra keys (e.g. __redirect_destinations paired with __redirect).
     */
    export const ConceptItemSchema = z.union([
      z.string(),
      z.object({ term: z.string(), definition: z.string() }).passthrough(),
      z.object({ __redirect: z.string() }).passthrough(),
    ]);

    export const KnowledgeEntrySchema = z.object({
      id: z.string(),
      name: z.string(),
      path: z.string(),
      description: z.string(),
      endpoints: z.array(z.string()),
      tools: z.array(z.string()),
      concepts: z.array(ConceptItemSchema),
      howto: z.array(ConceptItemSchema),
      dont: z.array(ConceptItemSchema),
      common_errors: z.array(CommonErrorSchema),
      success_cases: z.array(z.string()),
      sources: z.array(z.string()),
      updated_at: z.string(),
    }).passthrough();  // preserve __redirect + __redirect_destinations top-level keys for Plan 02 consumer

    export type ConceptItem = z.infer<typeof ConceptItemSchema>;
    ```

    Keep lines 29+ (KnowledgeIndexSchema + types + loaders) unchanged. Add `ConceptItem` to the exported types block.

    **Step 2 — Create `app/src/lib/services/kb-index-cache.ts`:**

    Full module following the shape in RESEARCH §Pattern 2 (lines 237-429 of RESEARCH). Key requirements:
    - Use `process['env']['KB_ROOT']` (bracket notation per MEMORY.md) with fallback `path.join(process.cwd(), '..', '.docflow-kb')`.
    - `INDEX_CACHE_TTL_MS = 60_000`.
    - `getKbIndex()`: returns cached `KbIndex | null`. Re-read only if `Date.now() - loadedAt >= TTL`. Wrap in `try/catch`, return null on ENOENT or parse error.
    - `invalidateKbIndex()`: resets both indexCache and sotCache.
    - `buildSourceOfTruthCache(index)`: iterates `index.entries.filter(e=>e.type==='resource')`, reads each `.md` file's frontmatter via js-yaml, extracts `source_of_truth: [{db,table,id,...}]`, populates Map where key is `${table}:${id}`.
    - `resolveKbEntry(dbTable, dbId)`: returns `string | null` — the entry.path from the byTableId map.
    - `parseKbFile(filePath)`: returns `{frontmatter: Record<string,unknown>, body: string} | null`. Splits on `---\n` boundaries. Uses `yaml.load()` from js-yaml.
    - `searchKb(params)`: returns `{total, results}`. Status default 'active'. Limit clamped `Math.min(Math.max(limit ?? 10, 1), 50)`. AND match on tags. Case-insensitive on `search` across title/summary/tags/search_hints. Ranking title=3, summary=2, tag=1, hint=1. Without `search`: order by `updated DESC`. Results: summary truncated to 200 chars via `truncate()` helper.
    - `getKbEntry(id)`: returns `{id, path, frontmatter, body, related_resolved} | null`. If entry not in index → null. `related_resolved`: iterate `frontmatter.related`, tolerate both `{type,id}` objects and plain strings, lookup each against `idx.entries.find(e=>e.id===idVal)`. Unmatched → `{type, id, title:null, path:null}`.

    Full type definitions in module (exported):
    ```typescript
    export interface KbIndexEntry { id: string; path: string; type: string; subtype: string | null; title: string; summary: string; tags: string[]; audience: string[]; status: string; updated: string; search_hints: string[] | null; }
    export interface KbIndex { schema_version: string; entry_count: number; entries: KbIndexEntry[]; indexes: { by_type: Record<string,string[]>; by_tag: Record<string,string[]>; by_audience: Record<string,string[]>; }; }
    export interface SearchKbParams { type?: string; subtype?: string; tags?: string[]; audience?: string; status?: string; search?: string; limit?: number; }
    export interface SearchKbResult { total: number; results: Array<Pick<KbIndexEntry,'id'|'path'|'type'|'subtype'|'title'|'summary'|'tags'|'audience'|'status'|'updated'>>; }
    export interface GetKbEntryResult { id: string; path: string; frontmatter: Record<string, unknown>; body: string; related_resolved: Array<{ type: string; id: string; title: string | null; path: string | null }>; }
    ```

    All exports: `getKbIndex`, `invalidateKbIndex`, `resolveKbEntry`, `searchKb`, `getKbEntry`, `parseKbFile`, `KbIndex`, `KbIndexEntry`, `SearchKbParams`, `SearchKbResult`, `GetKbEntryResult`.

    DO NOT use `process.env.X` — always `process['env']['X']` (bracket notation). NO dependency on `knowledge-sync.ts` (separation of read vs write path).

    **Step 3 — Create `app/src/lib/__tests__/kb-test-utils.ts`:**

    Export a single function `createFixtureKb(tmpDir: string): { kbRoot: string }`. See RESEARCH §Proposed createFixtureKb helper (lines 817-900) for the reference implementation. Minimum 7 entries (6 resources 1-per-subtype + 1 rule). Each resource file MUST have `source_of_truth: [{db: sqlite, table: <table>, id: <uuid>, fields_from_db: [name]}]` in frontmatter (otherwise resolveKbEntry tests fail).

    Mapping expected (document inline as comments):
    ```
    resources/catpaws/aaa11111-test-catpaw.md    → cat_paws:aaa11111-1111-1111-1111-111111111111
    resources/connectors/bbb22222-test-connector.md → connectors:bbb22222-2222-2222-2222-222222222222
    resources/skills/test-skill-writer.md        → skills:writer-skill
    resources/catbrains/ccc33333-test-catbrain.md → catbrains:ccc33333-3333-3333-3333-333333333333
    resources/email-templates/tpl-test-welcome.md → email_templates:tpl-welcome
    resources/canvases/ddd44444-test-canvas.md   → canvases:ddd44444-4444-4444-4444-444444444444
    rules/R10-preserve-fields.md                 → (no source_of_truth — rules are hand-authored)
    ```

    Also write a minimal `_header.md` with 7 entries count.

    **Step 4 — Create `app/src/lib/__tests__/knowledge-tree.test.ts` (extend if exists or create):**

    Tests to add:
    - `describe('ConceptItemSchema')`:
      - `it('accepts plain string')` → `ConceptItemSchema.parse('plain concept')` no throw.
      - `it('accepts {term, definition}')` → `ConceptItemSchema.parse({term:'X',definition:'Y'})` no throw.
      - `it('accepts {__redirect}')` → `ConceptItemSchema.parse({__redirect:'domain/concepts/foo.md'})` no throw.
      - `it('rejects number')` → throws.
    - `describe('KnowledgeEntrySchema with mixed concepts')`:
      - `it('accepts entry with mixed shapes in concepts array')` — fixture object with 3 items (string, {term,definition}, {__redirect}).
      - `it('preserves __redirect top-level via passthrough')` — fixture with `__redirect: 'X'` at top level, parsed object retains the key.
      - `it('parses real catboard.json without throwing')` — reads `app/data/knowledge/catboard.json` via `fs.readFileSync` + JSON.parse, calls `KnowledgeEntrySchema.parse(raw)`.

    **Step 5 — Create `app/src/lib/__tests__/kb-index-cache.test.ts`:**

    Use `vi.hoisted()` to set `process['env']['KB_ROOT']` to a tmp dir BEFORE imports (see catbot-tools-retry-job.test.ts:21 pattern).

    Tests (all required):
    - `it('getKbIndex returns null when _index.json missing')` → empty tmpdir, no KB.
    - `it('getKbIndex returns parsed KbIndex when file exists')` — uses `createFixtureKb`, calls getKbIndex, asserts entry_count === 7.
    - `it('getKbIndex caches within TTL')` — spy on fs.readFileSync, call twice, assert 1 call.
    - `it('invalidateKbIndex forces re-read')` — spy, call, invalidate, call → 2 reads.
    - `it('resolveKbEntry returns path for existing (table, id)')` — call with `('cat_paws','aaa11111-1111-1111-1111-111111111111')` → `'resources/catpaws/aaa11111-test-catpaw.md'`.
    - `it('resolveKbEntry returns null for unknown (table, id)')`.
    - `it('parseKbFile returns frontmatter and body')` — asserts frontmatter.id, frontmatter.source_of_truth, body contains "## Descripción".
    - `it('parseKbFile returns null on missing file')`.
    - `it('searchKb filters by type')` — `searchKb({type:'resource'})` returns 6 (the 6 resource fixture entries).
    - `it('searchKb filters by subtype')` — `searchKb({subtype:'catpaw'})` returns exactly 1.
    - `it('searchKb AND-matches tags')` — `searchKb({tags:['catpaw','business']})` returns 1; `searchKb({tags:['catpaw','nonexistent']})` returns 0.
    - `it('searchKb ranks by weights')` — with fixture entry "Gmail Test" summary "Test gmail connector", `searchKb({search:'gmail'})` ranks title+tag above summary.
    - `it('searchKb caps limit at 50')` — pass limit=999, actually cap.
    - `it('searchKb defaults to status active')` — no status param → only active entries (fixture has all 7 active; add a deprecated entry to fixture if needed OR document as tested in Plan 02).
    - `it('getKbEntry returns entry with frontmatter + body + related_resolved')` — fixture entry with `related: [{type:'resource',id:'bbb22222-test-connector'}]` in its frontmatter → related_resolved populated.
    - `it('getKbEntry returns null when id missing')`.
    - `it('getKbEntry related_resolved includes null title/path for stale refs')` — entry with `related: [{type:'resource',id:'ghost-id'}]` → null title/path.

    Run: `cd app && npm run test:unit -- knowledge-tree kb-index-cache` — all tests must be GREEN before commit.

    **Commit structure (atomic per RED-GREEN):**
    1. `test(152-01): add failing tests for knowledge-tree Zod union + kb-index-cache` (RED — tests exist but modules don't yet)
    2. `feat(152-01): extend knowledge-tree Zod schema with ConceptItemSchema union + passthrough` (GREEN for knowledge-tree tests)
    3. `feat(152-01): add kb-index-cache.ts module with getKbIndex/resolveKbEntry/searchKb/getKbEntry` (GREEN for kb-index-cache tests)
    4. `test(152-01): add kb-test-utils createFixtureKb helper` (support file — can be included in commit 1)
  </action>
  <verify>
    <automated>cd app && npm run test:unit -- knowledge-tree 2>&1 | tail -20</automated>
    <automated>cd app && npm run test:unit -- kb-index-cache 2>&1 | tail -20</automated>
    <automated>cd app && npx tsc --noEmit src/lib/services/kb-index-cache.ts 2>&1 | head -20</automated>
    <automated>node -e "const f=require('fs').readFileSync('app/src/lib/services/kb-index-cache.ts','utf8'); const reqs=['getKbIndex','invalidateKbIndex','resolveKbEntry','searchKb','getKbEntry','parseKbFile']; const re=new RegExp('export\\\\s+(function|const|interface|type)\\\\s+('+reqs.join('|')+')\\\\b'); const missing=reqs.filter(r=>!new RegExp('export\\\\s+(function|const|interface|type)\\\\s+'+r+'\\\\b').test(f)); if(missing.length){console.error('Missing strict exports:',missing); process.exit(1);} if(!f.includes('js-yaml')){console.error('Missing js-yaml import'); process.exit(2);} if(f.match(/process\\.env\\.[A-Z_]+/)){console.error('Uses dot notation process.env — must use bracket'); process.exit(3);} console.log('kb-index-cache.ts exports OK (strict regex)');"</automated>
  </verify>
  <done>
    - `app/src/lib/knowledge-tree.ts` exports `ConceptItemSchema` (z.union of 3 shapes) + `ConceptItem` type. `KnowledgeEntrySchema` uses `ConceptItemSchema` for concepts/howto/dont and has `.passthrough()`.
    - `app/src/lib/services/kb-index-cache.ts` exists with all required exports (getKbIndex, invalidateKbIndex, resolveKbEntry, searchKb, getKbEntry, parseKbFile, plus type exports). Uses js-yaml. Uses `process['env']['KB_ROOT']` bracket notation.
    - `app/src/lib/__tests__/kb-test-utils.ts` exports `createFixtureKb(tmpDir)` with 7 entries in fixture KB including valid `source_of_truth` frontmatter on 6 resource files.
    - `app/src/lib/__tests__/knowledge-tree.test.ts` passes with new tests covering union schema + catboard.json parse.
    - `app/src/lib/__tests__/kb-index-cache.test.ts` passes with ≥15 tests covering cache, resolver, parseKbFile, searchKb, getKbEntry.
    - `cd app && npm run test:unit -- knowledge-tree kb-index-cache` exits 0.
    - `cd app && npm run build` exits 0 (no TS/ESLint breakage that would block Docker rebuild in Plan 04).
  </done>
</task>

</tasks>

<verification>
Phase-level checks after Plan 01 completes:

1. **Requirements registered:** `grep -c "KB-1[5-8]" .planning/REQUIREMENTS.md` returns at least 8 (4 in bullets + 4 in traceability table).
2. **ROADMAP complete:** `grep -A 30 "^### Phase 152" .planning/ROADMAP.md | grep -c "KB-1[5-8]"` returns 4 (requirements line lists all 4 IDs).
3. **No TBD in Phase 152:** `grep -A 30 "^### Phase 152" .planning/ROADMAP.md | grep -c "TBD"` returns 0.
4. **KB-17 enumerates 5 tools, not 6:** `grep -A 2 "KB-17" .planning/REQUIREMENTS.md | grep -o "list_[a-z_]*\\|canvas_list" | sort -u` returns exactly `canvas_list, list_catbrains, list_cat_paws, list_email_templates, list_skills` (5 unique; no `list_connectors`).
5. **Zod fix:** `cd app && node -e "const {KnowledgeEntrySchema}=require('./src/lib/knowledge-tree'); const raw=JSON.parse(require('fs').readFileSync('data/knowledge/catboard.json','utf8')); KnowledgeEntrySchema.parse(raw); console.log('catboard.json parses OK');"` — must succeed.
6. **Tests green:** `cd app && npm run test:unit -- knowledge-tree kb-index-cache` exits 0 with ≥18 assertions passing.
7. **Module exports verified:** `cd app && node -e "const m=require('./src/lib/services/kb-index-cache'); ['getKbIndex','invalidateKbIndex','resolveKbEntry','searchKb','getKbEntry','parseKbFile'].forEach(fn=>{if(typeof m[fn]!=='function')throw new Error('missing '+fn);}); console.log('exports OK');"` — must succeed.
8. **Build not broken:** `cd app && npm run build 2>&1 | tail -5` — no TS/ESLint errors.
</verification>

<success_criteria>
- [ ] REQUIREMENTS.md §"Knowledge Base CatBot Consume (Phase 152)" section exists with 4 bullets (KB-15/16/17/18).
- [ ] REQUIREMENTS.md KB-17 enumerates exactly 5 canonical list_* tools; `list_connectors` explicitly marked deferred.
- [ ] REQUIREMENTS.md Traceability table has 4 new rows for KB-15..KB-18 → Phase 152 Pending.
- [ ] REQUIREMENTS.md Coverage block reads `v1 requirements: 30 total` / `Mapped to phases: 30/30`.
- [ ] ROADMAP.md Phase 152 section has Goal paragraph (no `TBD`, mentions 5 canonical tools), Requirements line `KB-15, KB-16, KB-17, KB-18`, 5 Success Criteria bullets (criterion #3 names the 5 canonical tools), Plans list with 4 PLAN refs.
- [ ] `knowledge-tree.ts` exports `ConceptItemSchema` union. `KnowledgeEntrySchema.parse(catboard.json)` succeeds without throw.
- [ ] `kb-index-cache.ts` exists with 6 function exports + type exports, uses `js-yaml`, uses bracket-notation for env vars, implements byTableId map via frontmatter read (CONFLICT #1 fix).
- [ ] `kb-test-utils.ts` exports `createFixtureKb(tmpDir)` producing a 7-entry fixture KB with valid frontmatter including `source_of_truth`.
- [ ] `kb-index-cache.test.ts` has ≥15 unit tests all green. `knowledge-tree.test.ts` has ≥4 new tests all green.
- [ ] `cd app && npm run build` exits 0 (no ESLint/TS errors — Plan 04 Docker rebuild must not be blocked).
</success_criteria>

<output>
After completion, create `.planning/phases/152-kb-catbot-consume/152-01-SUMMARY.md` following the summary template. Emphasize:
- 4 requirements IDs registered (KB-15..KB-18); frontmatter declares only KB-18 as delivered-here (full delivery of KB-15/16/17 in Plans 02/03/04).
- `kb-index-cache.ts` module exports documented (6 functions + 5 types)
- Zod union schema covers 3 shapes (string / {term,definition} / {__redirect})
- byTableId cache strategy (CONFLICT #1 resolution — reads 66 frontmatters once per 60s TTL)
- Test coverage (≥15 kb-index-cache + ≥4 knowledge-tree new tests)
- Confirm that KB_ROOT resolution path chosen (likely `path.join(process.cwd(), '..', '.docflow-kb')` with override via env)
- Note: KB-17 contract explicitly 5 canonical list_* tools (`list_cat_paws`, `list_catbrains`, `list_skills`, `list_email_templates`, `canvas_list`); `list_connectors` deferred because tool does not exist.
</output>
</output>
