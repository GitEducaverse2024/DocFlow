---
phase: 141-skill-prompt-enrichment-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/db.ts
  - app/data/knowledge/canvas.json
autonomous: true
requirements: [SKILL-01]

must_haves:
  truths:
    - "La Skill Orquestador contiene data contracts explicitos para la cadena normalizador->clasificador->respondedor->connector"
    - "La Skill Orquestador incluye mapeo de modelos recomendados por tipo de tarea (canvas-classifier, canvas-formatter, canvas-writer)"
    - "La Skill Orquestador incluye instrucciones validadas por tipo de nodo con campos de output esperados"
    - "La Skill Orquestador incluye protocolo de diagnostico de nodos (prompt primero, modelo ultimo recurso)"
  artifacts:
    - path: "app/src/lib/db.ts"
      provides: "Migration que actualiza Skill Orquestador con data contracts, modelos, e instrucciones validadas"
      contains: "Orquestador CatFlow"
    - path: "app/data/knowledge/canvas.json"
      provides: "Knowledge tree con conceptos de data contracts y modelos por nodo"
      contains: "data contract"
  key_links:
    - from: "app/src/lib/db.ts"
      to: "skills table"
      via: "UPDATE skills SET instructions"
      pattern: "UPDATE skills.*Orquestador"
---

<objective>
Actualizar la Skill "Orquestador CatFlow" en la DB con data contracts entre nodos, mapeo de modelos por tipo de tarea, instrucciones validadas por tipo de nodo, y protocolo de diagnostico. Actualizar el knowledge tree canvas.json con conceptos relacionados.

Purpose: CatBot necesita instrucciones concretas sobre que JSON produce cada nodo y que modelo usar para que pueda redactar instructions de nodos de calidad al construir CatFlows.
Output: Skill actualizada en DB via migracion en db.ts, knowledge tree actualizado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-skill-prompt-enrichment-skill/141-CONTEXT.md
@skill_orquestador_catbot_enriched.md
@app/data/knowledge/canvas.json
@app/src/lib/db.ts

<interfaces>
<!-- Patron de update de skill en db.ts (ejemplo de maquetador-email ~linea 4755): -->
```typescript
const existingSkill = db.prepare("SELECT instructions FROM skills WHERE id = 'maquetador-email'").get() as { instructions: string };
if (!existingSkill.instructions.includes('REGLAS DE DISEÑO DE EMAIL')) {
  db.prepare("UPDATE skills SET instructions = ?, description = ?, updated_at = ? WHERE id = 'maquetador-email'").run(
    `...new instructions...`,
    'nueva descripcion',
    new Date().toISOString()
  );
}
```

<!-- La skill Orquestador se busca por nombre, no por ID hardcodeado: -->
```typescript
// En catbot-prompt-assembler.ts linea 284:
get_skill(name: "Orquestador CatFlow")

// En catbot-tools.ts get_skill handler (~linea 1956):
skill = db.prepare('SELECT ... FROM skills WHERE name = ?').get(args.name);
```

<!-- Aliases de modelo disponibles (de Phase 140): -->
- canvas-classifier -> gemma-local (clasificacion/extraccion, tareas mecanicas)
- canvas-formatter -> gemma-local (formateo mecanico)
- canvas-writer -> gemini-main (redaccion, requiere calidad)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar Skill Orquestador en db.ts con data contracts y protocolos</name>
  <files>app/src/lib/db.ts</files>
  <action>
Agregar un bloque de migracion al final de db.ts (antes de los exports, despues de los otros bloques try/catch de seeds) que actualice la skill "Orquestador CatFlow" con contenido enriquecido.

**Patron:** Buscar la skill por nombre (no por ID, ya que fue creada manualmente y el ID puede variar). Usar UPDATE condicional con check de contenido para idempotencia:

```typescript
try {
  const orqSkill = db.prepare("SELECT id, instructions FROM skills WHERE name = 'Orquestador CatFlow' LIMIT 1").get() as { id: string; instructions: string } | undefined;
  if (orqSkill && !orqSkill.instructions.includes('DATA CONTRACTS ENTRE NODOS')) {
    db.prepare("UPDATE skills SET instructions = ?, updated_at = ? WHERE id = ?").run(
      ENRICHED_INSTRUCTIONS,
      new Date().toISOString(),
      orqSkill.id
    );
    logger.info('system', 'Phase 141: Skill Orquestador CatFlow enriched with data contracts');
  }
} catch (e) { logger.error('system', 'Phase 141 skill update error', { error: (e as Error).message }); }
```

**Contenido de ENRICHED_INSTRUCTIONS** — definir como const string ANTES del bloque try. Usar el contenido base de `skill_orquestador_catbot_enriched.md` (las 14 PARTEs existentes) y AGREGAR al final estas secciones nuevas:

**PARTE 15 — DATA CONTRACTS ENTRE NODOS** (por decision del usuario, solo contratos basados en evidencia real del flujo inbound):

```
## PARTE 15 — DATA CONTRACTS ENTRE NODOS

REGLA: Solo documentar contratos validados en produccion. No especular sobre flujos no probados.

### Cadena Email Inbound (validada parcialmente)

Normalizador → produce JSON:
{
  "remitente": "string — email del remitente",
  "asunto": "string — asunto original del email",
  "fecha": "string — fecha ISO del email",
  "cuerpo_limpio": "string — texto sin firmas ni disclaimers",
  "idioma": "string — es|en|ca detectado",
  "resumen": "string — 1-2 frases del contenido principal"
}

Clasificador → consume JSON del normalizador, produce:
{
  "producto": "string — uno de: K12, Simulator, REVI, Educaverse, Otro",
  "template": "string — Pro-K12 | Pro-Simulator | Pro-REVI | Pro-Educaverse | generico",
  "confianza": "number — 0.0 a 1.0",
  "razon": "string — por que se asigno este producto"
}

Condition → consume JSON del clasificador:
- Evalua: producto != "Otro" AND confianza >= 0.7
- YES → sigue al respondedor
- NO → sigue a OUTPUT (spam/irrelevante)

Respondedor → consume JSON del clasificador + contexto RAG:
- Genera email HTML usando el template indicado
- Produce: string con HTML del email listo para enviar

Gmail Connector → consume HTML del respondedor:
- Envia email via Gmail API
- Requiere: to, subject, html_body

### Protocolo semi-automatico de documentacion de contracts
Tras ejecucion exitosa de un canvas:
1. CatBot extrae los campos reales del output de cada nodo
2. CatBot sugiere el data contract al usuario
3. El usuario aprueba antes de registrarlo en esta skill
```

**PARTE 16 — MODELOS RECOMENDADOS POR TIPO DE TAREA**:

```
## PARTE 16 — MODELOS RECOMENDADOS POR TIPO DE TAREA

Aliases semanticos disponibles (configurados en LiteLLM Phase 140):

| Tipo de nodo | Alias recomendado | Razon |
|---|---|---|
| Normalizador (extraccion/limpieza) | canvas-classifier | Tarea mecanica, coste cero |
| Clasificador (clasificacion/routing) | canvas-classifier | Pattern matching, no requiere creatividad |
| Formateador (templates/HTML) | canvas-formatter | Tarea mecanica de formato |
| Respondedor (redaccion) | canvas-writer | Requiere calidad de redaccion |
| Condition evaluator | canvas-classifier | Evaluacion booleana simple |

REGLA: Asignar modelo al crear el nodo via parametro `model` de canvas_add_node.
Si no se especifica, el nodo hereda el modelo del CatPaw asignado.
```

**PARTE 17 — PROTOCOLO DE DIAGNOSTICO DE NODOS**:

```
## PARTE 17 — PROTOCOLO DE DIAGNOSTICO DE NODOS

Cuando un nodo de canvas falla o produce output inesperado, seguir este orden estricto:

1. MEJORAR EL PROMPT (90% de los casos)
   - Revisar las instructions del nodo
   - Hacer mas explicito el formato de output esperado
   - Anadir ejemplos concretos de input→output

2. AISLAR Y PROBAR
   - Ejecutar el nodo aislado con input conocido
   - Probar variantes del prompt hasta encontrar la que funciona
   - Documentar la variante ganadora

3. AJUSTAR SKILL O REGLAS
   - Si el nodo tiene skills vinculadas, revisar si las instrucciones de la skill son claras
   - Anadir constraints o reglas especificas

4. CAMBIAR MODELO (ultimo recurso)
   - Solo si los pasos 1-3 no resuelven
   - Reportar: "El problema era el modelo, no el prompt"
   - Probar con canvas-writer (mas capaz) antes de escalar a gemini-main directo

Consultar CatBrain DoCatFlow para errores y soluciones conocidas antes de diagnosticar desde cero.
```

**IMPORTANTE:** 
- El contenido total de la skill (14 partes existentes + 3 nuevas) sera largo. Definir la constante como template literal multillinea.
- Tomar las 14 PARTEs de `skill_orquestador_catbot_enriched.md` TAL CUAL y anadir las 3 PARTEs nuevas al final.
- Usar `process['env']` si se referencia alguna variable de entorno (no aplica aqui pero respetar la regla).
- NO dejar imports sin usar (rompe Docker build).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>db.ts compila sin errores. La migracion actualiza la skill Orquestador con PARTEs 15-17 (data contracts, modelos por tarea, protocolo diagnostico) de forma idempotente.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar knowledge tree canvas.json con conceptos de data contracts y modelos</name>
  <files>app/data/knowledge/canvas.json</files>
  <action>
Agregar a canvas.json los siguientes elementos:

**concepts** (agregar al array existente):
- "Data contracts entre nodos: cada nodo debe producir un JSON con campos definidos que el siguiente nodo consume. El normalizador produce 6 campos (remitente, asunto, fecha, cuerpo_limpio, idioma, resumen), el clasificador produce 4 campos (producto, template, confianza, razon)"
- "Modelos por tipo de nodo: normalizador y clasificador usan canvas-classifier (mecanico, coste cero), formateador usa canvas-formatter, respondedor usa canvas-writer (requiere calidad). Asignar via parametro model de canvas_add_node"
- "Protocolo de diagnostico: cuando un nodo falla, orden estricto: 1) mejorar prompt, 2) aislar y probar, 3) ajustar skill/reglas, 4) cambiar modelo como ultimo recurso"

**howto** (agregar al array existente):
- "Para asignar modelo por tipo de nodo: canvas_add_node con model='canvas-classifier' para normalizador/clasificador, model='canvas-formatter' para formateador, model='canvas-writer' para respondedor"

**dont** (agregar al array existente):
- "No cambiar el modelo como primera medida ante un nodo que falla — el 90% de los problemas son de prompt, skill o reglas"

Mantener TODO el contenido existente del JSON intacto. Solo agregar elementos a los arrays.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && node -e "const j = require('./app/data/knowledge/canvas.json'); const c = j.concepts.join(' '); if (!c.includes('Data contracts')) throw 'missing data contracts concept'; if (!c.includes('canvas-classifier')) throw 'missing model concept'; console.log('OK: ' + j.concepts.length + ' concepts, ' + j.howto.length + ' howto, ' + j.dont.length + ' dont')"</automated>
  </verify>
  <done>canvas.json contiene conceptos de data contracts entre nodos, modelos recomendados por tipo de nodo, y protocolo de diagnostico. El JSON es valido y parseable.</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npx tsc --noEmit` — compila sin errores
2. `node -e "JSON.parse(require('fs').readFileSync('app/data/knowledge/canvas.json'))"` — JSON valido
3. Grep en db.ts confirma presencia de "DATA CONTRACTS ENTRE NODOS" y "MODELOS RECOMENDADOS"
</verification>

<success_criteria>
- La skill Orquestador se actualiza con data contracts, modelos por tarea, y protocolo de diagnostico al iniciar la app
- El knowledge tree canvas.json refleja los nuevos conceptos para que search_knowledge los encuentre
- Todo compila sin errores TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/141-skill-prompt-enrichment-skill/141-01-SUMMARY.md`
</output>
