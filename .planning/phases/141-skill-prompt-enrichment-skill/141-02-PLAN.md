---
phase: 141-skill-prompt-enrichment-skill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/services/catbot-prompt-assembler.ts
  - app/src/lib/__tests__/catbot-prompt-assembler.test.ts
autonomous: true
requirements: [SKILL-02, SKILL-03]

must_haves:
  truths:
    - "Cuando CatBot ejecuta un tool call exitoso, reporta con check; cuando falla, reporta con error — el usuario ve resumen al final"
    - "Cuando el usuario pregunta por recursos existentes, CatBot usa tools de listado en vez de responder de memoria"
    - "CatBot anuncia 'Voy a consultar...' antes de ejecutar un tool de listado"
  artifacts:
    - path: "app/src/lib/services/catbot-prompt-assembler.ts"
      provides: "Secciones de reporting protocol y tool-use-first rule en system prompt"
      contains: "reporting_protocol"
    - path: "app/src/lib/__tests__/catbot-prompt-assembler.test.ts"
      provides: "Tests para las nuevas secciones del system prompt"
      contains: "reporting_protocol"
  key_links:
    - from: "app/src/lib/services/catbot-prompt-assembler.ts"
      to: "CatBot system prompt"
      via: "sections.push reporting_protocol y tool_use_first"
      pattern: "reporting_protocol|tool_use_first"
---

<objective>
Agregar al system prompt de CatBot dos protocolos obligatorios: (1) protocolo de reporting con marcas check/error al finalizar operaciones de canvas, y (2) regla tool-use-first que obliga a usar tools de listado antes de responder de memoria. Con tests.

Purpose: SKILL-02 exige feedback visual de progreso paso a paso; SKILL-03 exige que CatBot consulte datos reales en vez de inventar.
Output: Dos nuevas secciones P1 en el prompt assembler, con tests unitarios.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-skill-prompt-enrichment-skill/141-CONTEXT.md
@app/src/lib/services/catbot-prompt-assembler.ts
@app/src/lib/__tests__/catbot-prompt-assembler.test.ts

<interfaces>
<!-- Patron de seccion en prompt assembler: -->
```typescript
function buildNewSection(): string {
  return `## Titulo de la seccion
Contenido de las instrucciones...`;
}

// En build():
sections.push({ id: 'section_id', priority: 1, content: buildNewSection() });
```

<!-- Patron de test existente: -->
```typescript
it('includes section text', () => {
  const prompt = build(baseCtx);
  expect(prompt).toContain('texto esperado');
});
```

<!-- Decisiones del usuario (141-CONTEXT.md): -->
// Reporting: resumen al FINAL, no progresivo. Solo texto legible.
// En error: CatBot para, reporta con error mark, propone solucion.
// Tool-use: agresividad maxima — siempre que exista un tool. 
// Transparente: CatBot anuncia "Voy a consultar..." antes del tool.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agregar protocolo de reporting y regla tool-use-first al prompt assembler</name>
  <files>app/src/lib/services/catbot-prompt-assembler.ts, app/src/lib/__tests__/catbot-prompt-assembler.test.ts</files>
  <behavior>
    - Test 1: build() output contiene "Protocolo de Reporting" 
    - Test 2: build() output contiene la marca de check (unicode checkmark reference)
    - Test 3: build() output contiene "Tool-Use-First"
    - Test 4: build() output contiene "Voy a consultar"
    - Test 5: build() output contiene "list_cat_paws" (referencia a tool de listado)
  </behavior>
  <action>
**En catbot-prompt-assembler.ts:**

1. Crear funcion `buildReportingProtocol()` que retorne:

```
## Protocolo de Reporting (OBLIGATORIO)

Cuando construyas o modifiques un canvas (multiples tool calls), al FINALIZAR toda la operacion
presenta un resumen legible con el resultado de cada paso:

### Formato del resumen
- Cada paso exitoso: "✓ [NombreNodo]: [resultado breve]"
  Ejemplo: "✓ Normalizador: 6 campos extraidos"
  Ejemplo: "✓ Clasificador: producto=K12, template=Pro-K12, confianza=0.95"
  Ejemplo: "✓ Edge Normalizador→Clasificador: conectado"

- Cada paso fallido: "✗ [NombreNodo]: [error] — [solucion propuesta]"
  Ejemplo: "✗ Gmail Connector: ECONNREFUSED — verificar que el conector Gmail esta configurado"

### Reglas
- NO reportes paso a paso durante la ejecucion. Ejecuta TODO y reporta al final.
- Si un paso falla, PARA inmediatamente. Reporta lo completado con ✓ y el fallo con ✗.
- Propone una solucion o revision para el fallo.
- Solo texto legible para el usuario — nada de JSON tecnico ni dumps de datos.
- Consulta CatBrain DoCatFlow para errores y soluciones conocidas antes de proponer una solucion nueva.
- Granularidad: reporta por nodo individual (no por bloques).
```

2. Crear funcion `buildToolUseFirstRule()` que retorne:

```
## Regla Tool-Use-First (OBLIGATORIO)

SIEMPRE que el usuario pregunte por recursos existentes en DoCatFlow, ejecuta el tool de
listado correspondiente EN VEZ de responder de memoria. Esto incluye:

| Pregunta del usuario | Tool a ejecutar |
|---|---|
| "Que CatPaws tengo" / "que agentes hay" | list_cat_paws |
| "Que templates de email hay" | list_email_templates |
| "Que skills hay disponibles" | list_skills |
| "Que conectores tengo" | list_connectors |
| "Que canvas hay" / "que flujos hay" | canvas_list |
| "Que CatBrains tengo" | list_catbrains |
| "Que CatFlows hay" | list_catflows |
| "Que modelos hay disponibles" | list_models (si existe) o search_knowledge |

### Protocolo
1. Anuncia: "Voy a consultar [recurso] para darte datos actualizados..."
2. Ejecuta el tool correspondiente
3. Presenta los resultados formateados al usuario
4. NUNCA respondas de memoria — los datos pueden haber cambiado

### Alcance
Esta regla aplica a CUALQUIER dato consultable via tool, no solo a listados.
Si existe un tool que pueda responder la pregunta, usalo. Principio: dato real > memoria.

### Para el futuro
Cada feature nueva debe incluir su tool de listado correspondiente. Los tools actuales son suficientes para esta fase.
```

3. En la funcion `build()`, agregar las dos secciones DESPUES de `canvas_protocols` (aprox linea 907) y ANTES de las secciones P2:

```typescript
// P1: Reporting protocol (Phase 141 SKILL-02)
try {
  sections.push({ id: 'reporting_protocol', priority: 1, content: buildReportingProtocol() });
} catch { /* graceful */ }

// P1: Tool-use-first rule (Phase 141 SKILL-03)
try {
  sections.push({ id: 'tool_use_first', priority: 1, content: buildToolUseFirstRule() });
} catch { /* graceful */ }
```

**En catbot-prompt-assembler.test.ts:**

Agregar un nuevo describe block "Phase 141 — Reporting & Tool-Use-First" con los 5 tests definidos en <behavior>.

**IMPORTANTE:**
- NO dejar imports sin usar (rompe Docker build)
- Las funciones son puras (solo retornan strings), no necesitan imports adicionales
- Prioridad 1 (P1) — importante pero truncable bajo presion de budget
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx jest --testPathPattern="catbot-prompt-assembler" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>El system prompt de CatBot incluye protocolo de reporting (resumen con check/error al final) y regla tool-use-first (usar tools de listado, anunciar "Voy a consultar..."). Todos los tests pasan.</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npx jest --testPathPattern="catbot-prompt-assembler" --no-coverage` — todos los tests pasan
2. `cd /home/deskmath/docflow/app && npx tsc --noEmit` — compila sin errores
3. Grep confirma presencia de "Protocolo de Reporting" y "Tool-Use-First" en el prompt assembler
</verification>

<success_criteria>
- El system prompt de CatBot contiene protocolo de reporting con marcas check/error y resumen al final
- El system prompt contiene regla tool-use-first con tabla de mapeo pregunta→tool
- CatBot anuncia "Voy a consultar..." antes de ejecutar tools de listado
- Tests unitarios validan la presencia de ambas secciones
- Todo compila sin errores TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/141-skill-prompt-enrichment-skill/141-02-SUMMARY.md`
</output>
