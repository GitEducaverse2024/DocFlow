---
phase: 125-knowledge-tree-hardening
plan: 02
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - app/src/lib/services/catbot-tools.ts
  - app/data/knowledge/catboard.json
  - app/data/knowledge/catbrains.json
  - app/data/knowledge/catpaw.json
  - app/data/knowledge/catflow.json
  - app/data/knowledge/canvas.json
  - app/data/knowledge/catpower.json
  - app/data/knowledge/settings.json
  - app/src/lib/__tests__/knowledge-tools-sync.test.ts
  - app/src/lib/__tests__/knowledge-tree.test.ts
autonomous: true
requirements:
  - KTREE-02
  - KTREE-03

must_haves:
  truths:
    - "Every tool in catbot-tools.ts TOOLS[] is documented in at least one knowledge JSON"
    - "No phantom tools exist in knowledge JSONs (tools listed in JSON but not in TOOLS[])"
    - "Every source path in every knowledge JSON points to a real file on disk"
  artifacts:
    - path: "app/src/lib/__tests__/knowledge-tools-sync.test.ts"
      provides: "Bidirectional tool sync test"
      min_lines: 40
    - path: "app/src/lib/__tests__/knowledge-tree.test.ts"
      provides: "Updated source existence test using fs.existsSync"
      contains: "existsSync"
  key_links:
    - from: "app/src/lib/__tests__/knowledge-tools-sync.test.ts"
      to: "app/src/lib/services/catbot-tools.ts"
      via: "imports TOOLS array or reads file to extract tool names"
      pattern: "catbot-tools"
    - from: "app/src/lib/__tests__/knowledge-tools-sync.test.ts"
      to: "app/data/knowledge/*.json"
      via: "reads all area JSONs and collects tools[] arrays"
      pattern: "knowledge.*json"
---

<objective>
Create bidirectional tool sync tests and source existence tests, fixing all data gaps (23 missing tools in JSONs, 2 phantom tools) so the tests pass.

Purpose: Guarantee the knowledge tree stays in sync with the actual tool definitions and that all source references are valid. These tests become CI guardrails.
Output: New test file for tool sync, updated source test, all knowledge JSONs with corrected tool arrays.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/125-knowledge-tree-hardening/125-RESEARCH.md
@.planning/phases/125-knowledge-tree-hardening/125-01-SUMMARY.md

@app/src/lib/knowledge-tree.ts
@app/src/lib/__tests__/knowledge-tree.test.ts
@app/src/lib/services/catbot-tools.ts
@app/data/knowledge/_index.json

<interfaces>
<!-- From knowledge-tree.ts (post Plan 01): -->
KnowledgeEntrySchema includes: id, name, path, description, endpoints, tools[], concepts, howto, dont, common_errors, success_cases, sources[], updated_at
KnowledgeIndexSchema includes: version, updated, areas[{id, file, name, description, updated_at}]

<!-- From catbot-tools.ts: -->
const TOOLS: CatBotTool[] = [{ type, function: { name, description, parameters } }, ...]
- TOOLS is NOT exported (must be exported or parsed via regex)
- Tool names extracted via `name: 'tool_name'` pattern in the TOOLS array
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export TOOLS + create bidirectional tool sync test + fix knowledge JSON tool arrays</name>
  <files>
    app/src/lib/services/catbot-tools.ts,
    app/src/lib/__tests__/knowledge-tools-sync.test.ts,
    app/data/knowledge/catboard.json,
    app/data/knowledge/catbrains.json,
    app/data/knowledge/catpaw.json,
    app/data/knowledge/catflow.json,
    app/data/knowledge/canvas.json,
    app/data/knowledge/catpower.json,
    app/data/knowledge/settings.json
  </files>
  <behavior>
    - Test: Every tool name in TOOLS[] appears in at least one knowledge JSON tools[]
    - Test: Every tool name in any knowledge JSON tools[] exists in TOOLS[]
    - Test: No duplicate tool names across knowledge JSONs (optional, warn only)
  </behavior>
  <action>
    1. In catbot-tools.ts, change `const TOOLS` to `export const TOOLS` (one word change, no other modifications).

    2. Create app/src/lib/__tests__/knowledge-tools-sync.test.ts:
       - Import TOOLS from catbot-tools.ts (adjust path: `../../services/catbot-tools`)
       - Extract tool names: `TOOLS.map(t => t.function.name)`
       - Read all knowledge JSON files from data/knowledge/ (exclude _index.json and _template.json)
       - Collect all tools[] arrays into a single Set
       - Test "every TOOLS[] tool in at least one knowledge JSON": find missing, assert empty array
       - Test "every knowledge JSON tool exists in TOOLS[]": find phantoms, assert empty array

    3. Fix knowledge JSON tool arrays — add missing tools to correct JSONs (update updated_at to today's date for each modified JSON):

       **catboard.json** — add: get_dashboard, get_system_status
       **catpaw.json** — add: get_cat_paw, update_cat_paw, link_connector_to_catpaw, link_skill_to_catpaw
       **catflow.json** — add: create_task, list_tasks
       **canvas.json** — add: canvas_delete_edge, canvas_generate_iterator_end
       **catpower.json** — add: create_connector, create_email_template, delete_email_template, get_email_template, list_email_templates, render_email_template, update_email_template. REMOVE: list_connectors, mcp_bridge (phantom tools that don't exist in TOOLS[])
       **settings.json** — add: explain_feature, query_knowledge, read_error_history, save_learned_entry, get_summary, list_my_summaries

    4. IMPORTANT: Before adding tools, verify each tool actually exists in TOOLS[] by reading catbot-tools.ts. The research identified 23 missing tools but the list may have shifted. Use the ACTUAL TOOLS[] as source of truth, not the research list.

    5. After fixing JSONs, run the sync test. If any tools still fail, fix them iteratively until both directions pass.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tools-sync.test.ts</automated>
  </verify>
  <done>
    - TOOLS is exported from catbot-tools.ts
    - Bidirectional sync test passes: every TS tool in a JSON, every JSON tool in TOOLS[]
    - No phantom tools remain in knowledge JSONs
    - All 23+ missing tools added to correct area JSONs
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace source regex test with fs.existsSync validation</name>
  <files>
    app/src/lib/__tests__/knowledge-tree.test.ts
  </files>
  <behavior>
    - Test: Every path in sources[] of every knowledge JSON resolves to an existing file
    - Test: Test fails if a source path points to nonexistent file (validates the test itself works)
  </behavior>
  <action>
    1. In knowledge-tree.test.ts, find the "sources point to existing files" test (currently line ~142-150).

    2. Replace the regex-only check with actual file existence validation:
       ```typescript
       it('all sources[] paths resolve to existing files', () => {
         const areas = getAllKnowledgeAreas();
         // Sources are relative to project root (docflow/), vitest cwd is app/
         const projectRoot = path.resolve(process.cwd(), '..');

         for (const area of areas) {
           for (const source of area.sources) {
             const fullPath = path.join(projectRoot, source);
             expect(
               fs.existsSync(fullPath),
               `${area.id}: source "${source}" not found at ${fullPath}`
             ).toBe(true);
           }
         }
       });
       ```

    3. If any sources fail (point to files that were moved/deleted), fix the source paths in the corresponding knowledge JSONs. Update updated_at for any modified JSON.

    4. Keep the file extension regex test as a secondary validation (sources should end in .md, .json, .txt, or .ts).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts -t "sources"</automated>
  </verify>
  <done>
    - Source existence test uses fs.existsSync instead of regex-only
    - All source paths in all knowledge JSONs point to real files
    - Any broken source paths have been fixed
  </done>
</task>

</tasks>

<verification>
cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts && npx vitest run src/lib/__tests__/knowledge-tools-sync.test.ts
</verification>

<success_criteria>
- Bidirectional tool sync test passes (0 missing in either direction)
- Source existence test uses fs.existsSync and passes for all areas
- TOOLS exported from catbot-tools.ts for test access
- All knowledge JSONs have corrected tool arrays
- No phantom tools remain
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/125-knowledge-tree-hardening/125-02-SUMMARY.md`
</output>
