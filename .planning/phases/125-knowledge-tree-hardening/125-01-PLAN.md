---
phase: 125-knowledge-tree-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/knowledge-tree.ts
  - app/data/knowledge/_index.json
  - app/data/knowledge/_template.json
  - app/data/knowledge/catboard.json
  - app/data/knowledge/catbrains.json
  - app/data/knowledge/catpaw.json
  - app/data/knowledge/catflow.json
  - app/data/knowledge/canvas.json
  - app/data/knowledge/catpower.json
  - app/data/knowledge/settings.json
  - app/src/lib/__tests__/knowledge-tree.test.ts
autonomous: true
requirements:
  - KTREE-01
  - KTREE-04
  - KTREE-05

must_haves:
  truths:
    - "Zod schema rejects any knowledge JSON missing updated_at field"
    - "_index.json has per-area updated_at synced with individual JSONs"
    - "_template.json exists and passes schema validation"
  artifacts:
    - path: "app/src/lib/knowledge-tree.ts"
      provides: "Updated KnowledgeEntrySchema and KnowledgeIndexSchema with updated_at"
      contains: "updated_at: z.string()"
    - path: "app/data/knowledge/_template.json"
      provides: "Template for creating new knowledge areas"
      contains: "updated_at"
    - path: "app/data/knowledge/_index.json"
      provides: "Index with per-area updated_at"
      contains: "updated_at"
  key_links:
    - from: "app/src/lib/knowledge-tree.ts"
      to: "app/data/knowledge/*.json"
      via: "zod parse validates updated_at on every load"
      pattern: "updated_at.*z\\.string"
---

<objective>
Add updated_at timestamps to the knowledge tree schema, update all 7 area JSONs + _index.json, and create _template.json for new area onboarding.

Purpose: Make the knowledge tree self-documenting with traceable timestamps, and provide a template so new knowledge areas can be created without guessing the schema.
Output: Updated zod schemas, 7 updated JSONs, new _template.json, updated _index.json, passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/125-knowledge-tree-hardening/125-RESEARCH.md
@.planning/phases/125-knowledge-tree-hardening/125-VALIDATION.md

@app/src/lib/knowledge-tree.ts
@app/src/lib/__tests__/knowledge-tree.test.ts
@app/data/knowledge/_index.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add updated_at to zod schemas + update all knowledge JSONs + _index.json</name>
  <files>
    app/src/lib/knowledge-tree.ts,
    app/data/knowledge/catboard.json,
    app/data/knowledge/catbrains.json,
    app/data/knowledge/catpaw.json,
    app/data/knowledge/catflow.json,
    app/data/knowledge/canvas.json,
    app/data/knowledge/catpower.json,
    app/data/knowledge/settings.json,
    app/data/knowledge/_index.json,
    app/src/lib/__tests__/knowledge-tree.test.ts
  </files>
  <behavior>
    - Test: KnowledgeEntrySchema rejects an object missing updated_at
    - Test: KnowledgeEntrySchema accepts an object with valid updated_at string
    - Test: Each of the 7 knowledge JSONs has an updated_at field (string, ISO date format)
    - Test: _index.json areas[] entries each have an updated_at field matching the individual JSON's updated_at
    - Test: loadKnowledgeIndex() returns areas with updated_at defined
  </behavior>
  <action>
    1. In knowledge-tree.ts, add `updated_at: z.string()` to KnowledgeEntrySchema (after sources).
    2. In knowledge-tree.ts, add `updated_at: z.string()` to the areas[] object in KnowledgeIndexSchema.
    3. Add `"updated_at": "2026-04-08"` to each of the 7 knowledge JSONs (catboard, catbrains, catpaw, catflow, canvas, catpower, settings).
    4. Update _index.json: add `"updated_at": "2026-04-08"` to each area object in the areas[] array, matching the value in each individual JSON.
    5. Update knowledge-tree.test.ts:
       - In the "schema" describe block, add test: parse a valid JSON and confirm updated_at is present.
       - Add a new describe "updated_at" with tests:
         - KnowledgeEntrySchema.safeParse rejects object without updated_at
         - Each area JSON file has updated_at matching ISO date format (/^\d{4}-\d{2}-\d{2}$/)
         - _index.json areas[i].updated_at matches the corresponding individual JSON's updated_at
       - In the "index valid" test, verify areas[0] has updated_at defined.
    6. Ensure existing tests still pass after schema change (the EXPECTED_FILES list remains the same, _template.json is NOT in EXPECTED_FILES yet).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts</automated>
  </verify>
  <done>
    - All 7 knowledge JSONs have updated_at field
    - _index.json areas[] have updated_at matching individual JSONs
    - Zod schemas enforce updated_at as required
    - All existing + new tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create _template.json with documented schema and instructions</name>
  <files>
    app/data/knowledge/_template.json,
    app/src/lib/__tests__/knowledge-tree.test.ts
  </files>
  <action>
    1. Create app/data/knowledge/_template.json with this structure:
       ```json
       {
         "_instructions": [
           "1. Copy this file and rename to {area_id}.json",
           "2. Replace all placeholder values (AREA_ID, Area Name, etc.)",
           "3. Add the area to _index.json areas[] array",
           "4. Run: cd app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts",
           "5. Update updated_at to today's date"
         ],
         "id": "AREA_ID",
         "name": "Area Name",
         "path": "/route-path",
         "description": "Brief description of what this knowledge area covers",
         "endpoints": [],
         "tools": [],
         "concepts": [],
         "howto": [
           "Example: To do X, go to Y and click Z"
         ],
         "dont": [
           "Example: Do not do X because Y"
         ],
         "common_errors": [
           {
             "error": "Example error message",
             "cause": "What causes this error",
             "solution": "How to fix it"
           }
         ],
         "success_cases": [],
         "sources": [],
         "updated_at": "2026-04-08"
       }
       ```
    2. NOTE: _template.json has _instructions field which is NOT in the zod schema. It will NOT be loaded by loadKnowledgeArea (it's not in _index.json areas[]), so schema validation on load is not an issue. However, the existing test iterates EXPECTED_FILES (hardcoded list), not fs.readdir, so _template.json is automatically excluded from schema tests.
    3. Add a new describe "template" in knowledge-tree.test.ts:
       - Test: _template.json exists in data/knowledge/
       - Test: _template.json has all required fields from KnowledgeEntrySchema (id, name, path, description, endpoints, tools, concepts, howto, dont, common_errors, success_cases, sources, updated_at)
       - Test: _template.json has _instructions array with at least 3 items
    4. For the field presence test, do NOT use KnowledgeEntrySchema.safeParse (it would reject _instructions as unknown key OR the placeholder values might not be valid). Instead, check each required key exists with `expect(Object.keys(raw)).toContain('id')` etc.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts -t "template"</automated>
  </verify>
  <done>
    - _template.json exists with documented _instructions array
    - _template.json has all schema-required fields
    - Test validates template structure
  </done>
</task>

</tasks>

<verification>
cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts
</verification>

<success_criteria>
- All 7 knowledge JSONs + _index.json have updated_at field
- Zod schemas enforce updated_at as required (tests prove rejection on missing)
- _template.json exists with instructions and all required fields
- _index.json areas[].updated_at matches individual JSON updated_at values
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/125-knowledge-tree-hardening/125-01-SUMMARY.md`
</output>
