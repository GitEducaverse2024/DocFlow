---
phase: 111-ui-de-inteligencia-de-modelos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/api/alias-routing/route.ts
  - app/src/app/api/alias-routing/__tests__/route.test.ts
  - app/src/lib/ui/tier-styles.ts
  - app/messages/es.json
  - app/messages/en.json
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "GET /api/alias-routing returns all 8 aliases with model_key + description"
    - "PATCH /api/alias-routing updates an alias model_key and persists"
    - "PATCH returns 400 when alias or model_key missing or empty"
    - "Tier styles map (Elite/Pro/Libre) available as shared helper for all UI components"
    - "i18n keys under settings.modelIntelligence.* exist in both es.json and en.json"
  artifacts:
    - path: app/src/app/api/alias-routing/route.ts
      provides: "REST wrapper for getAllAliases() + updateAlias()"
      exports: ["GET", "PATCH"]
    - path: app/src/app/api/alias-routing/__tests__/route.test.ts
      provides: "UI-04 API contract tests"
    - path: app/src/lib/ui/tier-styles.ts
      provides: "TIER_STYLES map + getTierStyle() helper shared by all tier badges"
      exports: ["TIER_STYLES", "getTierStyle"]
    - path: app/messages/es.json
      provides: "settings.modelIntelligence.* Spanish strings"
    - path: app/messages/en.json
      provides: "settings.modelIntelligence.* English strings"
  key_links:
    - from: app/src/app/api/alias-routing/route.ts
      to: app/src/lib/services/alias-routing.ts
      via: "getAllAliases + updateAlias imports"
      pattern: "import.*alias-routing"
---

<objective>
Build the missing backend REST wrapper for alias routing (the only backend piece Phase 111 needs), create shared UI helpers (tier styles), and add all i18n strings the UI layer will consume. This is foundation work that unblocks Plans 02 and 03.

Purpose: Plans 02 and 03 need a working `/api/alias-routing` endpoint (UI-04) and shared tier styling + i18n strings. Build fails if i18n keys are missing when components reference them, so they must land first.
Output: New API route + tests, shared tier-styles module, i18n namespace populated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/111-ui-de-inteligencia-de-modelos/111-RESEARCH.md
@app/src/lib/services/alias-routing.ts
@app/src/app/api/mid/route.ts

<interfaces>
<!-- Existing contracts the executor reuses. No codebase exploration needed. -->

From app/src/lib/services/alias-routing.ts:
```typescript
export interface AliasRow {
  alias: string;
  model_key: string;
  description: string;
  is_active: number;
  created_at: string;
  updated_at: string;
}
export function getAllAliases(opts?: { active_only?: boolean }): AliasRow[];
export function updateAlias(alias: string, newModelKey: string): AliasRow;
// updateAlias throws Error('New model key cannot be empty') on blank input
// updateAlias throws Error when alias does not exist
```

Route pattern to follow (from app/src/app/api/mid/route.ts):
- First line: export const dynamic = 'force-dynamic' (MANDATORY)
- Error contract: return 200 with { error } for service errors, 400 for validation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create /api/alias-routing route with TDD</name>
  <files>app/src/app/api/alias-routing/route.ts, app/src/app/api/alias-routing/__tests__/route.test.ts</files>
  <behavior>
    - GET returns { aliases: AliasRow[] } with all 8 seeded aliases
    - GET returns 200 with { aliases: [], error } on service error (never 500)
    - PATCH with { alias: "catbot", model_key: "gpt-4o" } returns { updated: AliasRow }
    - PATCH with missing alias returns 400 { error: "Missing alias or model_key" }
    - PATCH with missing model_key returns 400
    - PATCH with empty/whitespace model_key returns 400
    - PATCH with unknown alias returns 200 { error } (service throws, caught gracefully)
  </behavior>
  <action>
    1. Write failing tests in `app/src/app/api/alias-routing/__tests__/route.test.ts`:
       - Use vitest with vi.mock('@/lib/services/alias-routing', () => ({ getAllAliases: vi.fn(), updateAlias: vi.fn() }))
       - Import GET, PATCH from ../route; build requests via `new NextRequest('http://localhost/api/alias-routing', { method: 'PATCH', body: JSON.stringify({...}) })`
       - Read response via `await res.json()` and `res.status`
       - Assert all behaviors above
    2. Run tests to confirm RED: `cd app && npx vitest run src/app/api/alias-routing --reporter=basic`
    3. Implement `app/src/app/api/alias-routing/route.ts`:
       - First line: `export const dynamic = 'force-dynamic';`
       - Import { NextRequest, NextResponse } from 'next/server'
       - Import { getAllAliases, updateAlias } from '@/lib/services/alias-routing'
       - Import { logger } from '@/lib/logger'
       - GET: try { return NextResponse.json({ aliases: getAllAliases() }) } catch (e) { logger.error('alias-routing', 'Error listing aliases', { error: (e as Error).message }); return NextResponse.json({ aliases: [], error: (e as Error).message }, { status: 200 }) }
       - PATCH: parse body { alias, model_key }; validate both are non-empty strings after trim, else return NextResponse.json({ error: 'Missing alias or model_key' }, { status: 400 }); call updateAlias(alias, model_key); return NextResponse.json({ updated }); on error log + return { error: msg } status 200
    4. Run tests to confirm GREEN.
    Mirror error-handling exactly from `app/src/app/api/mid/route.ts`.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/app/api/alias-routing --reporter=basic</automated>
  </verify>
  <done>Tests pass. GET returns aliases array, PATCH updates model_key, validation returns 400, service errors return 200 with { error }.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared tier-styles helper + i18n strings</name>
  <files>app/src/lib/ui/tier-styles.ts, app/messages/es.json, app/messages/en.json</files>
  <action>
    1. Create directory `app/src/lib/ui/` if needed, then create `app/src/lib/ui/tier-styles.ts`:
       ```typescript
       export const TIER_STYLES: Record<string, string> = {
         Elite: 'bg-gradient-to-r from-violet-600/20 to-purple-700/20 border-violet-500/40 text-violet-300',
         Pro: 'bg-emerald-600/20 border-emerald-500/40 text-emerald-300',
         Libre: 'bg-zinc-700/30 border-zinc-600/40 text-zinc-300',
       };
       const FALLBACK = 'bg-zinc-800 border-zinc-700 text-zinc-400';
       export function getTierStyle(tier: string | null | undefined): string {
         if (!tier) return FALLBACK;
         return TIER_STYLES[tier] ?? FALLBACK;
       }
       ```
    2. Edit `app/messages/es.json`: inside the top-level `"settings"` object, add key `"modelIntelligence"` with Spanish strings (see structure below). Use the Read tool first to see the current file, then use Edit to add the namespace without breaking JSON.
    3. Edit `app/messages/en.json`: same structure, English strings.

    Required i18n structure (both languages):
    ```
    settings.modelIntelligence.title                  (ES: "Modelos" / EN: "Models")
    settings.modelIntelligence.description            ("Inventario, fichas MID y routing de aliases" / "Inventory, MID cards, and alias routing")
    settings.modelIntelligence.inventory.title        ("Inventario Discovery" / "Discovery Inventory")
    settings.modelIntelligence.inventory.refresh      ("Refrescar" / "Refresh")
    settings.modelIntelligence.inventory.refreshing   ("Refrescando..." / "Refreshing...")
    settings.modelIntelligence.inventory.lastUpdate   ("Última actualización" / "Last update")
    settings.modelIntelligence.inventory.stale        ("Caché obsoleta" / "Stale cache")
    settings.modelIntelligence.inventory.providers    ("Proveedores" / "Providers")
    settings.modelIntelligence.inventory.models       ("Modelos" / "Models")
    settings.modelIntelligence.inventory.empty        ("Sin modelos detectados" / "No models detected")
    settings.modelIntelligence.mid.title              ("Fichas MID" / "MID Cards")
    settings.modelIntelligence.mid.edit               ("Editar" / "Edit")
    settings.modelIntelligence.mid.save               ("Guardar" / "Save")
    settings.modelIntelligence.mid.cancel             ("Cancelar" / "Cancel")
    settings.modelIntelligence.mid.tier               ("Tier" / "Tier")
    settings.modelIntelligence.mid.bestUse            ("Mejor uso" / "Best use")
    settings.modelIntelligence.mid.capabilities       ("Capacidades" / "Capabilities")
    settings.modelIntelligence.mid.addCapability      ("Añadir capacidad..." / "Add capability...")
    settings.modelIntelligence.mid.scores             ("Puntuaciones" / "Scores")
    settings.modelIntelligence.mid.reasoning          ("Razonamiento" / "Reasoning")
    settings.modelIntelligence.mid.coding             ("Código" / "Coding")
    settings.modelIntelligence.mid.creativity         ("Creatividad" / "Creativity")
    settings.modelIntelligence.mid.speed              ("Velocidad" / "Speed")
    settings.modelIntelligence.mid.multilingual       ("Multilingüe" / "Multilingual")
    settings.modelIntelligence.mid.status             ("Estado" / "Status")
    settings.modelIntelligence.mid.active             ("Activo" / "Active")
    settings.modelIntelligence.mid.retired            ("Retirado" / "Retired")
    settings.modelIntelligence.mid.costNotes          ("Coste" / "Cost")
    settings.modelIntelligence.mid.saved              ("Ficha actualizada" / "Card updated")
    settings.modelIntelligence.mid.saveError          ("Error al guardar" / "Save error")
    settings.modelIntelligence.routing.title          ("Routing de Aliases" / "Alias Routing")
    settings.modelIntelligence.routing.description    ("Qué modelo usa cada tipo de tarea" / "Which model each task type uses")
    settings.modelIntelligence.routing.alias          ("Alias" / "Alias")
    settings.modelIntelligence.routing.model          ("Modelo" / "Model")
    settings.modelIntelligence.routing.updated        ("Routing actualizado (efectivo en <5min por caché)" / "Routing updated (effective in <5min due to cache)")
    settings.modelIntelligence.routing.updateError    ("Error al actualizar routing" / "Update error")
    ```

    4. Validate JSON parses without errors and contains the new namespace.
  </action>
  <verify>
    <automated>cd app && node -e "const es=JSON.parse(require('fs').readFileSync('messages/es.json','utf8'));const en=JSON.parse(require('fs').readFileSync('messages/en.json','utf8'));const required=['title','description','inventory','mid','routing'];for(const k of required){if(!es.settings.modelIntelligence[k])throw new Error('es missing '+k);if(!en.settings.modelIntelligence[k])throw new Error('en missing '+k);}if(!es.settings.modelIntelligence.inventory.title||!es.settings.modelIntelligence.mid.tier||!es.settings.modelIntelligence.routing.alias)throw new Error('missing nested key');console.log('i18n ok');" && cd app && npx tsc --noEmit --skipLibCheck src/lib/ui/tier-styles.ts</automated>
  </verify>
  <done>tier-styles.ts compiles, both es.json and en.json contain the complete settings.modelIntelligence namespace with all nested keys.</done>
</task>

</tasks>

<verification>
- API route responds to GET/PATCH with correct shapes (tests pass)
- Shared tier-styles module exports TIER_STYLES and getTierStyle
- i18n keys exist in both languages (build won't fail)
</verification>

<success_criteria>
- `cd app && npx vitest run src/app/api/alias-routing --reporter=basic` passes
- `cd app && npm run build` succeeds (no missing i18n keys)
- Plans 02 and 03 can import from @/lib/ui/tier-styles and @/api/alias-routing
</success_criteria>

<output>
After completion, create `.planning/phases/111-ui-de-inteligencia-de-modelos/111-01-SUMMARY.md`
</output>
