---
phase: 80-safe-delete-email-confirmation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/deskmath/holded-mcp/src/utils/pending-deletes.ts
  - /home/deskmath/holded-mcp/src/utils/email-sender.ts
  - /home/deskmath/holded-mcp/src/utils/safe-delete-routes.ts
  - /home/deskmath/holded-mcp/src/index.ts
  - /home/deskmath/holded-mcp/src/__tests__/pending-deletes.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/email-sender.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/safe-delete-routes.test.ts
autonomous: true
requirements: [SDEL-01, SDEL-02, SDEL-03, SDEL-04, SDEL-05, SDEL-07, SDEL-08, SDEL-09, SDEL-10, SDEL-13, SDEL-14]

must_haves:
  truths:
    - "requestDelete() crea un token, envia email, y devuelve status pending_confirmation"
    - "GET /confirm/:token ejecuta el DELETE real en Holded y muestra pagina de exito"
    - "GET /cancel/:token marca el token como cancelado y muestra pagina de cancelacion"
    - "Tokens expirados (24h) devuelven pagina de error"
    - "Tokens ya usados devuelven pagina de error (doble-click seguro)"
    - "Si el email falla, el token se cancela y se devuelve error al caller"
  artifacts:
    - path: "src/utils/pending-deletes.ts"
      provides: "Token store + requestDelete() wrapper"
      exports: ["createPendingDelete", "getPendingDelete", "markConfirmed", "markCancelled", "markError", "requestDelete", "PendingDelete"]
    - path: "src/utils/email-sender.ts"
      provides: "Nodemailer setup + email HTML template"
      exports: ["sendConfirmationEmail", "buildConfirmationEmailHtml", "escapeHtml"]
    - path: "src/utils/safe-delete-routes.ts"
      provides: "Express router con /confirm/:token y /cancel/:token"
      exports: ["createSafeDeleteRouter"]
  key_links:
    - from: "src/utils/pending-deletes.ts"
      to: "src/utils/email-sender.ts"
      via: "requestDelete() llama sendConfirmationEmail()"
      pattern: "sendConfirmationEmail"
    - from: "src/utils/safe-delete-routes.ts"
      to: "src/utils/pending-deletes.ts"
      via: "GET /confirm llama getPendingDelete + markConfirmed"
      pattern: "getPendingDelete.*markConfirmed"
    - from: "src/utils/safe-delete-routes.ts"
      to: "src/holded-client.ts"
      via: "GET /confirm ejecuta client.delete()"
      pattern: "client\\.delete"
    - from: "src/index.ts"
      to: "src/utils/safe-delete-routes.ts"
      via: "app.use(createSafeDeleteRouter(client))"
      pattern: "createSafeDeleteRouter"
---

<objective>
Crear la infraestructura completa de Safe Delete: token store en memoria, envio de email con nodemailer, rutas Express /confirm/:token y /cancel/:token, e integracion en index.ts.

Purpose: Establecer los 3 modulos nuevos (pending-deletes, email-sender, safe-delete-routes) que Plan 02 consumira al refactorizar los 14 DELETE tools.
Output: 3 ficheros nuevos en src/utils/, modificacion de index.ts, 3 ficheros de tests, nodemailer instalado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-safe-delete-email-confirmation/RESEARCH.md

<interfaces>
<!-- Tipos existentes que el executor necesita -->

From src/holded-client.ts:
```typescript
export type HoldedModule = 'invoicing' | 'crm' | 'projects' | 'team';

// HoldedClient methods used:
async get<T>(endpoint: string, queryParams?: Record<string, string | number>, module?: HoldedModule): Promise<T>;
async delete<T>(endpoint: string, body?: unknown, module?: HoldedModule): Promise<T>;
```

From src/index.ts (HTTP branch, lines 219-244):
```typescript
const app = express();
app.use(express.json());
// ... existing routes ...
const PORT = parseInt(process['env']['PORT'] || '8766');
app.listen(PORT, '0.0.0.0', () => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instalar nodemailer + crear pending-deletes.ts y email-sender.ts con tests</name>
  <files>
    /home/deskmath/holded-mcp/package.json
    /home/deskmath/holded-mcp/src/utils/pending-deletes.ts
    /home/deskmath/holded-mcp/src/utils/email-sender.ts
    /home/deskmath/holded-mcp/src/__tests__/pending-deletes.test.ts
    /home/deskmath/holded-mcp/src/__tests__/email-sender.test.ts
  </files>
  <behavior>
    - createPendingDelete() genera token hex de 64 chars y almacena entrada con status 'pending'
    - getPendingDelete() devuelve null para token inexistente
    - getPendingDelete() devuelve entry con status 'expired' si han pasado >TTL horas
    - markConfirmed/markCancelled/markError actualizan el status correctamente
    - requestDelete() crea token + llama sendConfirmationEmail; si email OK devuelve { status: 'pending_confirmation', message, token }
    - requestDelete() si email falla, marca token como 'error' y lanza Error
    - requestDelete() en modo stdio (sin PORT env) lanza error descriptivo
    - Cleanup periodico elimina tokens con >2x TTL de antiguedad
    - buildConfirmationEmailHtml() genera HTML con resourceType, resourceLabel, confirmUrl, cancelUrl
    - escapeHtml() escapa &, <, >, "
    - sendConfirmationEmail() llama transporter.sendMail con HTML correcto y destinatario de SAFE_DELETE_NOTIFY_EMAIL
  </behavior>
  <action>
    1. Instalar dependencias: `cd ~/holded-mcp && npm install nodemailer && npm install -D @types/nodemailer`

    2. Crear `src/utils/pending-deletes.ts`:
       - Interface PendingDelete: { token, toolName, resourceType, resourceId, resourceLabel, deleteEndpoint, deleteModule?: HoldedModule, deleteBody?: unknown, createdAt, status: 'pending'|'confirmed'|'cancelled'|'expired'|'error' }
       - Map<string, PendingDelete> como store interno (NO exportar el Map)
       - TTL configurable via process['env']['SAFE_DELETE_TOKEN_TTL_HOURS'] (default 24)
       - createPendingDelete(): genera token con crypto.randomBytes(32).toString('hex'), guarda en Map
       - getPendingDelete(): busca en Map, si createdAt + TTL < now marca como 'expired'
       - markConfirmed(), markCancelled(), markError(): actualizan status
       - clearAllPendingDeletes(): para tests, limpia el Map completo
       - startCleanupInterval() / stopCleanupInterval(): setInterval cada 1h que elimina tokens con >2x TTL. Exportar para que tests puedan parar el timer
       - requestDelete(client, params): funcion principal que orquesta todo el flujo:
         a. Verifica que process['env']['PORT'] existe (modo HTTP), si no lanza Error("Safe delete requiere modo HTTP")
         b. Llama createPendingDelete(params)
         c. Construye confirmUrl y cancelUrl con process['env']['SAFE_DELETE_BASE_URL'] || 'http://192.168.1.49:8766'
         d. Llama sendConfirmationEmail({resourceType, resourceLabel, confirmUrl, cancelUrl})
         e. Si email falla: markError(token), throw new Error con mensaje descriptivo
         f. Devuelve { status: 'pending_confirmation', message: '...en espanol...', token }
       - IMPORTANTE: Todos los process.env con bracket notation: process['env']['VAR']

    3. Crear `src/utils/email-sender.ts`:
       - escapeHtml(str): reemplaza &, <, >, " por entidades HTML
       - buildConfirmationEmailHtml({resourceType, resourceLabel, confirmUrl, cancelUrl}): genera HTML completo con:
         - meta charset utf-8, meta referrer no-referrer
         - Titulo "Confirmacion de eliminacion"
         - Tabla con Tipo y Nombre del recurso
         - Texto "Esta accion es irreversible"
         - Boton rojo "Confirmar eliminacion" (href=confirmUrl)
         - Boton gris "Cancelar" (href=cancelUrl)
         - Pie: "Este enlace expira en 24 horas"
       - getTransporter(): singleton de nodemailer.createTransport con config de env vars SAFE_DELETE_SMTP_*
       - sendConfirmationEmail(params): envia email al SAFE_DELETE_NOTIFY_EMAIL con subject "Confirmar eliminacion: {resourceType} {resourceLabel}"
         - Devuelve { ok: true } o { ok: false, error: string }
       - resetTransporter(): para tests, resetea el singleton
       - IMPORTANTE: Todos los process.env con bracket notation

    4. Escribir tests ANTES de implementar (TDD red-green):
       - `src/__tests__/pending-deletes.test.ts`: mock de sendConfirmationEmail, test de token format, expiracion, status transitions, requestDelete flow, stdio rejection, cleanup
       - `src/__tests__/email-sender.test.ts`: mock de nodemailer.createTransport, test de HTML content, escapeHtml, sendConfirmationEmail success/failure

    NOTA: Los tests deben mockear nodemailer (vi.mock('nodemailer')) y NO enviar emails reales. Para pending-deletes tests, mockear '../utils/email-sender.js' para controlar el resultado de sendConfirmationEmail.
  </action>
  <verify>
    <automated>cd /home/deskmath/holded-mcp && npx vitest run src/__tests__/pending-deletes.test.ts src/__tests__/email-sender.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - pending-deletes.ts exporta createPendingDelete, getPendingDelete, markConfirmed, markCancelled, markError, requestDelete, clearAllPendingDeletes, PendingDelete (type)
    - email-sender.ts exporta sendConfirmationEmail, buildConfirmationEmailHtml, escapeHtml, resetTransporter
    - Token es hex de 64 chars (crypto.randomBytes(32))
    - requestDelete en modo stdio lanza error
    - Si email falla, token se marca 'error' y se lanza excepcion
    - Todos los tests pasan
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear safe-delete-routes.ts + integrar en index.ts con tests</name>
  <files>
    /home/deskmath/holded-mcp/src/utils/safe-delete-routes.ts
    /home/deskmath/holded-mcp/src/index.ts
    /home/deskmath/holded-mcp/src/__tests__/safe-delete-routes.test.ts
  </files>
  <behavior>
    - GET /confirm/:token con token valido y pending: ejecuta client.delete(), marca confirmed, devuelve HTML success con "eliminado correctamente"
    - GET /confirm/:token con token inexistente: devuelve 404 con HTML "Token no encontrado"
    - GET /confirm/:token con token expirado: devuelve 410 con HTML "Token expirado"
    - GET /confirm/:token con token ya usado (confirmed/cancelled): devuelve 409 con HTML "Token ya utilizado"
    - GET /confirm/:token doble-click rapido: segundo request ve status !== 'pending' y devuelve 409
    - GET /confirm/:token si client.delete() falla: marca error, devuelve 500 con HTML de error
    - GET /cancel/:token con token valido y pending: marca cancelled, devuelve HTML "cancelada"
    - GET /cancel/:token con token inexistente: devuelve 404
    - GET /cancel/:token con token ya usado: devuelve 409
    - Todas las paginas HTML tienen meta referrer no-referrer y charset utf-8
  </behavior>
  <action>
    1. Crear `src/utils/safe-delete-routes.ts`:
       - Importar Router de express, getPendingDelete/markConfirmed/markCancelled/markError de pending-deletes
       - Importar HoldedClient de holded-client
       - htmlPage(message, type: 'success'|'cancelled'|'error'): genera HTML de respuesta con:
         - meta charset utf-8, meta referrer no-referrer
         - Titulo "DoCatFlow - Eliminacion"
         - Icono segun tipo (check verde, X gris, warning rojo)
         - Mensaje escapado con escapeHtml
         - Pie: "Puedes cerrar esta ventana"
       - createSafeDeleteRouter(client: HoldedClient): Router
         - GET /confirm/:token:
           a. getPendingDelete(token) -- si null -> 404
           b. Si status === 'expired' -> 410
           c. Si status !== 'pending' -> 409 "Token ya utilizado"
           d. Inmediatamente markConfirmed(token) ANTES de llamar client.delete (previene doble-click race condition)
           e. await client.delete(entry.deleteEndpoint, entry.deleteBody, entry.deleteModule)
           f. Si delete falla: markError(token), 500 con error
           g. Si OK: 200 con HTML success
         - GET /cancel/:token:
           a. getPendingDelete(token) -- si null -> 404
           b. Si status !== 'pending' -> 409
           c. markCancelled(token)
           d. 200 con HTML cancelled

    NOTA IMPORTANTE sobre race condition (SDEL-10): Marcar como confirmed ANTES de ejecutar el delete. Si el delete falla, cambiar a error. Esto garantiza que un segundo click simultaneo ve status !== 'pending' y recibe 409. Es single-threaded (Node.js) asi que la lectura + escritura del Map es atomica dentro del mismo tick.

    2. Modificar `src/index.ts`:
       - Anadir import: `import { createSafeDeleteRouter } from './utils/safe-delete-routes.js';`
       - En el bloque HTTP (despues de `app.use(express.json())`), anadir: `app.use(createSafeDeleteRouter(client));`
       - Ubicar ANTES de las rutas /mcp para que /confirm y /cancel no pasen por el MCP handler

    3. Escribir tests (TDD):
       - `src/__tests__/safe-delete-routes.test.ts`:
         - Crear Express app de test con el router
         - Mockear HoldedClient con un objeto { delete: vi.fn() }
         - Para cada test: crear un pending delete manualmente con createPendingDelete, luego hacer request con supertest o directamente con el router
         - NOTA: Si supertest no esta instalado, usar directamente request/response mocks de express. Alternativa: instalar supertest como devDependency.
         - Tests para: confirm success, confirm 404, confirm expired, confirm already used, confirm delete failure, cancel success, cancel 404, cancel already used, HTML content verification
  </action>
  <verify>
    <automated>cd /home/deskmath/holded-mcp && npx vitest run src/__tests__/safe-delete-routes.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - safe-delete-routes.ts exporta createSafeDeleteRouter
    - GET /confirm/:token ejecuta delete real y devuelve HTML success/error
    - GET /cancel/:token cancela token y devuelve HTML cancelled
    - Tokens expirados/usados devuelven paginas de error apropiadas
    - Doble-click en confirm es seguro (segundo request recibe 409)
    - index.ts registra el router en el Express app (modo HTTP)
    - Todos los tests pasan, incluyendo suite completa: `npx vitest run --reporter=verbose`
  </done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/holded-mcp && npx vitest run --reporter=verbose` -- toda la suite pasa (tests existentes + 3 nuevos ficheros de test)
2. `grep -r "process\.env\." /home/deskmath/holded-mcp/src/utils/pending-deletes.ts /home/deskmath/holded-mcp/src/utils/email-sender.ts /home/deskmath/holded-mcp/src/utils/safe-delete-routes.ts` -- NO debe devolver resultados (todo bracket notation)
3. Los 3 ficheros nuevos existen en src/utils/
4. index.ts contiene import y uso de createSafeDeleteRouter
</verification>

<success_criteria>
- Los 3 modulos de infraestructura (pending-deletes, email-sender, safe-delete-routes) estan creados y testeados
- requestDelete() funciona end-to-end: crea token -> envia email -> devuelve pending_confirmation
- Las rutas /confirm y /cancel procesan tokens correctamente con paginas HTML
- La suite completa de tests pasa sin regresiones
- nodemailer esta instalado como dependencia
</success_criteria>

<output>
After completion, create `.planning/phases/80-safe-delete-email-confirmation/80-01-SUMMARY.md`
</output>
