---
phase: 80-safe-delete-email-confirmation
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - /home/deskmath/holded-mcp/src/tools/contacts.ts
  - /home/deskmath/holded-mcp/src/tools/documents.ts
  - /home/deskmath/holded-mcp/src/tools/products.ts
  - /home/deskmath/holded-mcp/src/tools/payments.ts
  - /home/deskmath/holded-mcp/src/tools/services.ts
  - /home/deskmath/holded-mcp/src/tools/contact-groups.ts
  - /home/deskmath/holded-mcp/src/tools/sales-channels.ts
  - /home/deskmath/holded-mcp/src/tools/warehouses.ts
  - /home/deskmath/holded-mcp/src/tools/numbering-series.ts
  - /home/deskmath/holded-mcp/src/tools/expenses-accounts.ts
  - /home/deskmath/holded-mcp/src/tools/projects.ts
  - /home/deskmath/holded-mcp/src/tools/project-tasks.ts
  - /home/deskmath/holded-mcp/src/tools/time-tracking.ts
  - /home/deskmath/holded-mcp/src/tools/employee-timesheets.ts
  - /home/deskmath/holded-mcp/src/__tests__/contacts.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/documents.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/products.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/payments.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/services.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/contact-groups.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/sales-channels.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/warehouses.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/numbering-series.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/expenses-accounts.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/projects.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/project-tasks.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/time-tracking.test.ts
  - /home/deskmath/holded-mcp/src/__tests__/employee-timesheets.test.ts
autonomous: true
requirements: [SDEL-06, SDEL-11, SDEL-12, SDEL-15]

must_haves:
  truths:
    - "Los 14 DELETE tools llaman a requestDelete() en vez de client.delete()"
    - "No quedan llamadas directas a client.delete() en ningun fichero de tools/"
    - "Cada tool pasa resourceType en espanol y resourceLabel legible"
    - "Los tools que pueden resolver nombre (contacts, products, services, projects, project-tasks, contact-groups, documents) hacen GET previo para obtener el label"
    - "Los tools sin nombre resuelble (payments, sales-channels, warehouses, numbering-series, expenses-accounts, time-tracking, employee-timesheets) usan el ID como label"
    - "Todos los env vars usan bracket notation process['env']['VAR']"
  artifacts:
    - path: "src/tools/contacts.ts"
      provides: "delete_contact usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/documents.ts"
      provides: "delete_document usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/products.ts"
      provides: "delete_product usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/payments.ts"
      provides: "delete_payment usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/services.ts"
      provides: "delete_service usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/contact-groups.ts"
      provides: "delete_contact_group usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/sales-channels.ts"
      provides: "delete_sales_channel usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/warehouses.ts"
      provides: "delete_warehouse usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/numbering-series.ts"
      provides: "delete_numbering_serie usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/expenses-accounts.ts"
      provides: "delete_expenses_account usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/projects.ts"
      provides: "holded_delete_project usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/project-tasks.ts"
      provides: "holded_delete_project_task usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/time-tracking.ts"
      provides: "holded_delete_time_entry usa requestDelete"
      contains: "requestDelete"
    - path: "src/tools/employee-timesheets.ts"
      provides: "holded_delete_timesheet usa requestDelete"
      contains: "requestDelete"
  key_links:
    - from: "src/tools/*.ts (14 files)"
      to: "src/utils/pending-deletes.ts"
      via: "import { requestDelete } from '../utils/pending-deletes.js'"
      pattern: "requestDelete"
---

<objective>
Refactorizar los 14 DELETE tool handlers para que usen requestDelete() en vez de client.delete() directo. Actualizar los tests existentes para esperar respuesta pending_confirmation.

Purpose: Completar la cadena de Safe Delete -- tras Plan 01 (infraestructura), este plan cierra el circuito conectando todos los tools al sistema de confirmacion por email.
Output: 14 tool files modificados, 14 test files actualizados, cero llamadas directas a client.delete() en tools/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-safe-delete-email-confirmation/RESEARCH.md
@.planning/phases/80-safe-delete-email-confirmation/80-01-SUMMARY.md

<interfaces>
<!-- Contratos creados en Plan 01 que este plan consume -->

From src/utils/pending-deletes.ts:
```typescript
import { HoldedModule } from '../holded-client.js';

export interface PendingDelete {
  token: string;
  toolName: string;
  resourceType: string;
  resourceId: string;
  resourceLabel: string;
  deleteEndpoint: string;
  deleteModule?: HoldedModule;
  deleteBody?: unknown;
  createdAt: number;
  status: 'pending' | 'confirmed' | 'cancelled' | 'expired' | 'error';
}

export async function requestDelete(
  client: HoldedClient,
  params: {
    toolName: string;
    resourceType: string;
    resourceId: string;
    resourceLabel: string;
    deleteEndpoint: string;
    deleteModule?: HoldedModule;
    deleteBody?: unknown;
  }
): Promise<{ status: 'pending_confirmation'; message: string; token: string }>;
```

From src/holded-client.ts:
```typescript
export type HoldedModule = 'invoicing' | 'crm' | 'projects' | 'team';
async get<T>(endpoint: string, queryParams?: Record<string, string | number>, module?: HoldedModule): Promise<T>;
async delete<T>(endpoint: string, body?: unknown, module?: HoldedModule): Promise<T>;
```
</interfaces>

<!-- Patron actual de cada DELETE handler (todos siguen el mismo patron) -->
<current_pattern>
// Ejemplo tipico (contacts.ts):
handler: withValidation(contactIdSchema, async (args) => {
  return client.delete(`/contacts/${args.contactId}`);
}),

// Variaciones:
// - Algunos usan async (args: { id: string }) => sin withValidation
// - Algunos pasan module: client.delete(endpoint, undefined, 'projects')
// - Algunos pasan module: client.delete(endpoint, undefined, 'team')
// - Ningun DELETE handler actual usa body (todos undefined)
</current_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorizar 7 DELETE tools del modulo invoicing</name>
  <files>
    /home/deskmath/holded-mcp/src/tools/contacts.ts
    /home/deskmath/holded-mcp/src/tools/documents.ts
    /home/deskmath/holded-mcp/src/tools/products.ts
    /home/deskmath/holded-mcp/src/tools/payments.ts
    /home/deskmath/holded-mcp/src/tools/services.ts
    /home/deskmath/holded-mcp/src/tools/contact-groups.ts
    /home/deskmath/holded-mcp/src/tools/sales-channels.ts
    /home/deskmath/holded-mcp/src/__tests__/contacts.test.ts
    /home/deskmath/holded-mcp/src/__tests__/documents.test.ts
    /home/deskmath/holded-mcp/src/__tests__/products.test.ts
    /home/deskmath/holded-mcp/src/__tests__/payments.test.ts
    /home/deskmath/holded-mcp/src/__tests__/services.test.ts
    /home/deskmath/holded-mcp/src/__tests__/contact-groups.test.ts
    /home/deskmath/holded-mcp/src/__tests__/sales-channels.test.ts
  </files>
  <action>
    Para cada uno de los 7 tool files de invoicing, aplicar esta transformacion:

    1. Anadir import al inicio del fichero:
       `import { requestDelete } from '../utils/pending-deletes.js';`

    2. Reemplazar el handler del DELETE tool. El patron de transformacion es:

    **contacts.ts** (delete_contact) -- CON label resolution:
    ```typescript
    handler: withValidation(contactIdSchema, async (args) => {
      const contact = await client.get<{ name?: string }>(`/contacts/${args.contactId}`);
      return requestDelete(client, {
        toolName: 'delete_contact',
        resourceType: 'Contacto',
        resourceId: args.contactId,
        resourceLabel: contact?.name || args.contactId,
        deleteEndpoint: `/contacts/${args.contactId}`,
      });
    }),
    ```

    **documents.ts** (delete_document) -- CON label resolution:
    ```typescript
    handler: withValidation(documentIdSchema, async (args) => {
      const doc = await client.get<{ docNumber?: string; ref?: string }>(`/documents/${args.docType}/${args.documentId}`);
      return requestDelete(client, {
        toolName: 'delete_document',
        resourceType: 'Documento',
        resourceId: args.documentId,
        resourceLabel: doc?.docNumber || doc?.ref || args.documentId,
        deleteEndpoint: `/documents/${args.docType}/${args.documentId}`,
      });
    }),
    ```

    **products.ts** (delete_product) -- CON label resolution:
    ```typescript
    handler: withValidation(productIdSchema, async (args) => {
      const product = await client.get<{ name?: string }>(`/products/${args.productId}`);
      return requestDelete(client, {
        toolName: 'delete_product',
        resourceType: 'Producto',
        resourceId: args.productId,
        resourceLabel: product?.name || args.productId,
        deleteEndpoint: `/products/${args.productId}`,
      });
    }),
    ```

    **payments.ts** (delete_payment) -- SIN label resolution (no hay nombre util):
    ```typescript
    handler: withValidation(paymentIdSchema, async (args) => {
      return requestDelete(client, {
        toolName: 'delete_payment',
        resourceType: 'Pago',
        resourceId: args.paymentId,
        resourceLabel: args.paymentId,
        deleteEndpoint: `/payments/${args.paymentId}`,
      });
    }),
    ```

    **services.ts** (delete_service) -- CON label resolution:
    ```typescript
    handler: withValidation(serviceIdSchema, async (args) => {
      const service = await client.get<{ name?: string }>(`/services/${args.serviceId}`);
      return requestDelete(client, {
        toolName: 'delete_service',
        resourceType: 'Servicio',
        resourceId: args.serviceId,
        resourceLabel: service?.name || args.serviceId,
        deleteEndpoint: `/services/${args.serviceId}`,
      });
    }),
    ```

    **contact-groups.ts** (delete_contact_group) -- CON label resolution:
    ```typescript
    handler: async (args: { groupId: string }) => {
      const group = await client.get<{ name?: string }>(`/contactgroups/${args.groupId}`);
      return requestDelete(client, {
        toolName: 'delete_contact_group',
        resourceType: 'Grupo de contactos',
        resourceId: args.groupId,
        resourceLabel: group?.name || args.groupId,
        deleteEndpoint: `/contactgroups/${args.groupId}`,
      });
    },
    ```

    **sales-channels.ts** (delete_sales_channel) -- SIN label resolution:
    ```typescript
    handler: async (args: { channelId: string }) => {
      return requestDelete(client, {
        toolName: 'delete_sales_channel',
        resourceType: 'Canal de ventas',
        resourceId: args.channelId,
        resourceLabel: args.channelId,
        deleteEndpoint: `/saleschannels/${args.channelId}`,
      });
    },
    ```

    3. Actualizar tests existentes para cada tool:
       - Mockear el modulo '../utils/pending-deletes.js' con vi.mock:
         `vi.mock('../utils/pending-deletes.js', () => ({ requestDelete: vi.fn() }));`
       - En los tests de DELETE, verificar que requestDelete fue llamado con los parametros correctos
       - El mock de requestDelete debe devolver { status: 'pending_confirmation', message: 'test', token: 'test-token' }
       - Si el test actual verifica que client.delete fue llamado, cambiar a verificar requestDelete
       - IMPORTANTE: No romper tests de otros tools (GET, POST, PUT) en el mismo fichero -- solo modificar los tests del DELETE tool

    NOTA: Cada tool file ya tiene `client` disponible como parametro de la funcion getXxxTools(client). No es necesario importar HoldedClient.
  </action>
  <verify>
    <automated>cd /home/deskmath/holded-mcp && npx vitest run src/__tests__/contacts.test.ts src/__tests__/documents.test.ts src/__tests__/products.test.ts src/__tests__/payments.test.ts src/__tests__/services.test.ts src/__tests__/contact-groups.test.ts src/__tests__/sales-channels.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - 7 tool files de invoicing usan requestDelete() en vez de client.delete()
    - contacts, documents, products, services, contact-groups hacen GET previo para label
    - payments, sales-channels usan ID como label
    - 7 test files actualizados y pasan
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar 7 DELETE tools restantes (warehouses, numbering, expenses, projects, tasks, time, timesheets) + verificacion global</name>
  <files>
    /home/deskmath/holded-mcp/src/tools/warehouses.ts
    /home/deskmath/holded-mcp/src/tools/numbering-series.ts
    /home/deskmath/holded-mcp/src/tools/expenses-accounts.ts
    /home/deskmath/holded-mcp/src/tools/projects.ts
    /home/deskmath/holded-mcp/src/tools/project-tasks.ts
    /home/deskmath/holded-mcp/src/tools/time-tracking.ts
    /home/deskmath/holded-mcp/src/tools/employee-timesheets.ts
    /home/deskmath/holded-mcp/src/__tests__/warehouses.test.ts
    /home/deskmath/holded-mcp/src/__tests__/numbering-series.test.ts
    /home/deskmath/holded-mcp/src/__tests__/expenses-accounts.test.ts
    /home/deskmath/holded-mcp/src/__tests__/projects.test.ts
    /home/deskmath/holded-mcp/src/__tests__/project-tasks.test.ts
    /home/deskmath/holded-mcp/src/__tests__/time-tracking.test.ts
    /home/deskmath/holded-mcp/src/__tests__/employee-timesheets.test.ts
  </files>
  <action>
    Misma transformacion que Task 1, para los 7 tools restantes:

    **warehouses.ts** (delete_warehouse) -- SIN label resolution:
    ```typescript
    handler: withValidation(warehouseIdSchema, async (args) => {
      return requestDelete(client, {
        toolName: 'delete_warehouse',
        resourceType: 'Almacen',
        resourceId: args.warehouseId,
        resourceLabel: args.warehouseId,
        deleteEndpoint: `/warehouses/${args.warehouseId}`,
      });
    }),
    ```

    **numbering-series.ts** (delete_numbering_serie) -- SIN label resolution:
    ```typescript
    handler: withValidation(numberingSerieIdSchema, async (args) => {
      return requestDelete(client, {
        toolName: 'delete_numbering_serie',
        resourceType: 'Serie de numeracion',
        resourceId: args.serieId,
        resourceLabel: args.serieId,
        deleteEndpoint: `/numberseries/${args.docType}/${args.serieId}`,
      });
    }),
    ```

    **expenses-accounts.ts** (delete_expenses_account) -- SIN label resolution:
    ```typescript
    handler: async (args: { accountId: string }) => {
      return requestDelete(client, {
        toolName: 'delete_expenses_account',
        resourceType: 'Cuenta de gastos',
        resourceId: args.accountId,
        resourceLabel: args.accountId,
        deleteEndpoint: `/expensesaccounts/${args.accountId}`,
      });
    },
    ```

    **projects.ts** (holded_delete_project) -- CON label resolution:
    ```typescript
    handler: withValidation(projectIdSchema, async (args) => {
      const project = await client.get<{ name?: string }>(`/projects/${args.projectId}`, undefined, 'projects');
      return requestDelete(client, {
        toolName: 'holded_delete_project',
        resourceType: 'Proyecto',
        resourceId: args.projectId,
        resourceLabel: project?.name || args.projectId,
        deleteEndpoint: `/projects/${args.projectId}`,
        deleteModule: 'projects',
      });
    }),
    ```

    **project-tasks.ts** (holded_delete_project_task) -- CON label resolution:
    ```typescript
    handler: withValidation(projectTaskIdSchema, async (args) => {
      const task = await client.get<{ name?: string }>(`/tasks/${args.taskId}`, undefined, 'projects');
      return requestDelete(client, {
        toolName: 'holded_delete_project_task',
        resourceType: 'Tarea de proyecto',
        resourceId: args.taskId,
        resourceLabel: task?.name || args.taskId,
        deleteEndpoint: `/tasks/${args.taskId}`,
        deleteModule: 'projects',
      });
    }),
    ```

    **time-tracking.ts** (holded_delete_time_entry) -- SIN label resolution:
    ```typescript
    handler: withValidation(timeEntryIdSchema, async (args) => {
      return requestDelete(client, {
        toolName: 'holded_delete_time_entry',
        resourceType: 'Registro de tiempo',
        resourceId: args.timeTrackingId,
        resourceLabel: `${args.timeTrackingId} (proyecto: ${args.projectId})`,
        deleteEndpoint: `/projects/${args.projectId}/times/${args.timeTrackingId}`,
        deleteModule: 'projects',
      });
    }),
    ```

    **employee-timesheets.ts** (holded_delete_timesheet) -- SIN label resolution:
    ```typescript
    handler: withValidation(timesheetIdSchema, async (args) => {
      return requestDelete(client, {
        toolName: 'holded_delete_timesheet',
        resourceType: 'Fichaje de empleado',
        resourceId: args.timeId,
        resourceLabel: args.timeId,
        deleteEndpoint: `/employees/times/${args.timeId}`,
        deleteModule: 'team',
      });
    }),
    ```

    Actualizar tests de cada fichero igual que en Task 1: mockear requestDelete, verificar llamadas correctas.

    VERIFICACION FINAL: Despues de todos los cambios, ejecutar:
    1. `grep -rn "client\.delete(" /home/deskmath/holded-mcp/src/tools/` -- debe devolver CERO resultados (SDEL-15)
    2. `grep -rn "process\.env\." /home/deskmath/holded-mcp/src/utils/pending-deletes.ts /home/deskmath/holded-mcp/src/utils/email-sender.ts` -- debe devolver CERO resultados (SDEL-11)
    3. `npx vitest run --reporter=verbose` -- toda la suite pasa

    Si grep encuentra client.delete() en tools/, hay un tool que se quedo sin refactorizar. Corregir antes de dar por terminado.
  </action>
  <verify>
    <automated>cd /home/deskmath/holded-mcp && npx vitest run --reporter=verbose && echo "--- SDEL-15 check ---" && (grep -rn "client\.delete(" src/tools/ && echo "FAIL: direct client.delete found" || echo "PASS: no direct client.delete in tools/")</automated>
  </verify>
  <done>
    - 14/14 DELETE tools usan requestDelete() -- cero llamadas directas a client.delete() en tools/
    - 7 tools con label resolution (contacts, documents, products, services, contact-groups, projects, project-tasks) hacen GET previo
    - 7 tools sin label (payments, sales-channels, warehouses, numbering-series, expenses-accounts, time-tracking, employee-timesheets) usan ID o ID contextual
    - Suite completa de tests pasa sin regresiones
    - grep confirma cero client.delete() en tools/ y cero process.env. en utils/
  </done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/holded-mcp && npx vitest run --reporter=verbose` -- toda la suite pasa (300+ tests)
2. `grep -rn "client\.delete(" /home/deskmath/holded-mcp/src/tools/` -- CERO resultados (SDEL-15: no direct DELETE calls)
3. `grep -c "requestDelete" /home/deskmath/holded-mcp/src/tools/*.ts` -- 14 ficheros con al menos 1 match cada uno (SDEL-06)
4. `grep -rn "process\.env\." /home/deskmath/holded-mcp/src/tools/contacts.ts /home/deskmath/holded-mcp/src/tools/documents.ts /home/deskmath/holded-mcp/src/utils/pending-deletes.ts /home/deskmath/holded-mcp/src/utils/email-sender.ts` -- CERO resultados (SDEL-11)
</verification>

<success_criteria>
- Los 14 DELETE tools llaman a requestDelete() -- la cadena completa funciona
- Cero llamadas directas a client.delete() en tools/
- Los tools con nombre resolvible (7) hacen GET previo para obtener label legible
- Todos los tests pasan sin regresiones
- El sistema de Safe Delete esta completo: tool handler -> requestDelete -> email -> confirm/cancel HTTP -> client.delete
</success_criteria>

<output>
After completion, create `.planning/phases/80-safe-delete-email-confirmation/80-02-SUMMARY.md`
</output>
