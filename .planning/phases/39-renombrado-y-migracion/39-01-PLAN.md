---
phase: 39-renombrado-y-migracion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/db.ts
  - app/src/lib/types.ts
  - app/src/app/api/catbrains/route.ts
  - app/src/app/api/catbrains/[id]/route.ts
  - app/src/app/api/catbrains/[id]/chat/route.ts
  - app/src/app/api/catbrains/[id]/sources/route.ts
  - app/src/app/api/catbrains/[id]/sources/[sid]/route.ts
  - app/src/app/api/catbrains/[id]/sources/reorder/route.ts
  - app/src/app/api/catbrains/[id]/rag/route.ts
  - app/src/app/api/catbrains/[id]/rag/create/route.ts
  - app/src/app/api/catbrains/[id]/rag/info/route.ts
  - app/src/app/api/catbrains/[id]/rag/query/route.ts
  - app/src/app/api/catbrains/[id]/rag/status/route.ts
  - app/src/app/api/catbrains/[id]/stats/route.ts
  - app/src/app/api/catbrains/[id]/bot/create/route.ts
  - app/src/app/api/catbrains/[id]/process/route.ts
  - app/src/app/api/catbrains/[id]/process/status/route.ts
  - app/src/app/api/catbrains/[id]/process/clean/route.ts
  - app/src/app/api/catbrains/[id]/process/history/route.ts
  - app/src/app/api/catbrains/[id]/process/callback/route.ts
  - app/src/app/api/catbrains/[id]/process/[vid]/route.ts
  - app/src/app/api/catbrains/[id]/process/[vid]/output/route.ts
  - app/src/app/api/projects/route.ts
  - app/src/app/api/projects/[id]/route.ts
  - app/src/app/api/projects/[id]/chat/route.ts
  - app/src/app/api/projects/[id]/sources/route.ts
  - app/src/app/api/projects/[id]/sources/[sid]/route.ts
  - app/src/app/api/projects/[id]/sources/reorder/route.ts
  - app/src/app/api/projects/[id]/rag/route.ts
  - app/src/app/api/projects/[id]/rag/create/route.ts
  - app/src/app/api/projects/[id]/rag/info/route.ts
  - app/src/app/api/projects/[id]/rag/query/route.ts
  - app/src/app/api/projects/[id]/rag/status/route.ts
  - app/src/app/api/projects/[id]/stats/route.ts
  - app/src/app/api/projects/[id]/bot/create/route.ts
  - app/src/app/api/projects/[id]/process/route.ts
  - app/src/app/api/projects/[id]/process/status/route.ts
  - app/src/app/api/projects/[id]/process/clean/route.ts
  - app/src/app/api/projects/[id]/process/history/route.ts
  - app/src/app/api/projects/[id]/process/callback/route.ts
  - app/src/app/api/projects/[id]/process/[vid]/route.ts
  - app/src/app/api/projects/[id]/process/[vid]/output/route.ts
autonomous: true
requirements: [REN-01, REN-02]

must_haves:
  truths:
    - "La tabla catbrains existe con columnas system_prompt, mcp_enabled, icon_color y todos los datos migrados de projects"
    - "La tabla projects ya no existe en la base de datos"
    - "GET /api/catbrains devuelve la lista de catbrains"
    - "GET /api/projects devuelve 301 redirect a /api/catbrains"
    - "Todas las sub-rutas de /api/catbrains/[id]/... responden correctamente"
    - "Todas las sub-rutas de /api/projects/[id]/... devuelven 301 redirect a /api/catbrains/[id]/..."
  artifacts:
    - path: "app/src/lib/db.ts"
      provides: "Migration logic: CREATE TABLE catbrains + DROP projects + ALTER for new columns"
      contains: "catbrains"
    - path: "app/src/lib/types.ts"
      provides: "Updated type interfaces using catbrain_id instead of project_id"
      contains: "catbrain"
    - path: "app/src/app/api/catbrains/route.ts"
      provides: "Main catbrains list/create API"
      exports: ["GET", "POST"]
    - path: "app/src/app/api/projects/route.ts"
      provides: "301 redirect to /api/catbrains"
      exports: ["GET", "POST"]
  key_links:
    - from: "app/src/app/api/catbrains/route.ts"
      to: "app/src/lib/db.ts"
      via: "SQL queries on catbrains table"
      pattern: "FROM catbrains"
    - from: "app/src/app/api/projects/route.ts"
      to: "/api/catbrains"
      via: "301 redirect"
      pattern: "redirect.*catbrains"
---

<objective>
Migrate the database table from `projects` to `catbrains` and create all new API routes under `/api/catbrains/...`, then convert old `/api/projects/...` routes to 301 redirects.

Purpose: This is the foundation for the entire rename — the data layer and API layer must be correct before UI and internal references can be updated.
Output: Working catbrains table with new columns, full API CRUD at `/api/catbrains/...`, 301 redirects from old routes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/lib/db.ts
@app/src/lib/types.ts
@app/src/app/api/projects/route.ts
@app/src/app/api/projects/[id]/route.ts
</context>

<interfaces>
<!-- Current types from app/src/lib/types.ts that reference project_id -->
From app/src/lib/types.ts:
```typescript
// These interfaces have project_id fields that must become catbrain_id:
// Source.project_id, ProcessingRun.project_id, UsageLog.project_id
// Task.linked_projects -> linked_catbrains
// TaskStep.use_project_rag -> use_catbrain_rag
```

From app/src/lib/db.ts:
```typescript
// Current: CREATE TABLE IF NOT EXISTS projects (...)
// Current: REFERENCES projects(id) ON DELETE CASCADE
// Must migrate to: catbrains table with new columns
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DB table projects to catbrains and update types</name>
  <files>app/src/lib/db.ts, app/src/lib/types.ts</files>
  <action>
In db.ts, add migration logic AFTER the existing `CREATE TABLE IF NOT EXISTS projects` block (keep that block so the table exists on fresh installs too — but rename it to `catbrains`). The migration strategy:

1. **Rename the initial CREATE TABLE** from `projects` to `catbrains` — change `CREATE TABLE IF NOT EXISTS projects` to `CREATE TABLE IF NOT EXISTS catbrains` with the same columns.

2. **Add migration block** (right after the CREATE TABLE catbrains):
```
// Migration: projects -> catbrains
try {
  const hasProjects = db.prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='projects'").get();
  if (hasProjects) {
    db.exec('INSERT OR IGNORE INTO catbrains SELECT * FROM projects');
    db.exec('DROP TABLE projects');
    logger.info('system', 'Migration: projects -> catbrains complete');
  }
} catch (e) { logger.error('system', 'Migration error', { error: (e as Error).message }); }
```

3. **Add new columns** via try-catch ALTER TABLE pattern (existing convention):
   - `ALTER TABLE catbrains ADD COLUMN system_prompt TEXT`
   - `ALTER TABLE catbrains ADD COLUMN mcp_enabled INTEGER DEFAULT 1`
   - `ALTER TABLE catbrains ADD COLUMN icon_color TEXT DEFAULT 'violet'`

4. **Update ALL existing ALTER TABLE statements** that reference `projects` to reference `catbrains` (bot_created, bot_agent_id, default_model, rag_indexed_version, rag_indexed_at, rag_model).

5. **Update ALL REFERENCES** in other CREATE TABLE statements: `sources` table FK `REFERENCES projects(id)` -> `REFERENCES catbrains(id)`, `processing_runs` table FK similarly.

6. **Update the `usage_logs` table**: the column `project_id` stays as-is in the table definition (backward compat for existing data), but add comment that it means catbrain_id.

7. **Update canvas_templates seed data**: In the JSON template data, change `type: 'project'` to `type: 'catbrain'` and update labels from "Proyecto" to "CatBrain" and mode from `'projects'` to `'catbrains'`.

In types.ts:
- Rename `project_id` fields to `catbrain_id` in Source, ProcessingRun interfaces (but keep DB column names the same for now — the type alias is just for code clarity in Phase 40+). Actually NO — keep the TypeScript interface field names matching the DB column names for now. The DB columns in `sources` and `processing_runs` still have `project_id` column — renaming those columns requires recreating those tables which is risky. Instead, just add a comment `// FK to catbrains.id (legacy column name)` next to each `project_id` field.
- In Task interface: keep `linked_projects` as-is (the column stores JSON array of catbrain IDs, renaming it requires data migration of the tasks table too — defer to a separate concern).
- In TaskStep: keep `use_project_rag` as-is for same reason.
- Add a new type alias: `export type CatBrain = Project;` — NO, there is no `Project` type. The projects data is just queried inline. Leave types.ts minimal for now.

CRITICAL: Use `process['env']` bracket notation for any env var access. Use generateId() not crypto.randomUUID.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>db.ts creates catbrains table on fresh install, migrates from projects on existing install, new columns exist, all FK references updated, TypeScript compiles with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Create /api/catbrains routes and convert /api/projects to 301 redirects</name>
  <files>
    app/src/app/api/catbrains/route.ts,
    app/src/app/api/catbrains/[id]/route.ts,
    app/src/app/api/catbrains/[id]/chat/route.ts,
    app/src/app/api/catbrains/[id]/sources/route.ts,
    app/src/app/api/catbrains/[id]/sources/[sid]/route.ts,
    app/src/app/api/catbrains/[id]/sources/reorder/route.ts,
    app/src/app/api/catbrains/[id]/rag/route.ts,
    app/src/app/api/catbrains/[id]/rag/create/route.ts,
    app/src/app/api/catbrains/[id]/rag/info/route.ts,
    app/src/app/api/catbrains/[id]/rag/query/route.ts,
    app/src/app/api/catbrains/[id]/rag/status/route.ts,
    app/src/app/api/catbrains/[id]/stats/route.ts,
    app/src/app/api/catbrains/[id]/bot/create/route.ts,
    app/src/app/api/catbrains/[id]/process/route.ts,
    app/src/app/api/catbrains/[id]/process/status/route.ts,
    app/src/app/api/catbrains/[id]/process/clean/route.ts,
    app/src/app/api/catbrains/[id]/process/history/route.ts,
    app/src/app/api/catbrains/[id]/process/callback/route.ts,
    app/src/app/api/catbrains/[id]/process/[vid]/route.ts,
    app/src/app/api/catbrains/[id]/process/[vid]/output/route.ts,
    app/src/app/api/projects/route.ts,
    app/src/app/api/projects/[id]/route.ts,
    app/src/app/api/projects/[id]/chat/route.ts,
    app/src/app/api/projects/[id]/sources/route.ts,
    app/src/app/api/projects/[id]/sources/[sid]/route.ts,
    app/src/app/api/projects/[id]/sources/reorder/route.ts,
    app/src/app/api/projects/[id]/rag/route.ts,
    app/src/app/api/projects/[id]/rag/create/route.ts,
    app/src/app/api/projects/[id]/rag/info/route.ts,
    app/src/app/api/projects/[id]/rag/query/route.ts,
    app/src/app/api/projects/[id]/rag/status/route.ts,
    app/src/app/api/projects/[id]/stats/route.ts,
    app/src/app/api/projects/[id]/bot/create/route.ts,
    app/src/app/api/projects/[id]/process/route.ts,
    app/src/app/api/projects/[id]/process/status/route.ts,
    app/src/app/api/projects/[id]/process/clean/route.ts,
    app/src/app/api/projects/[id]/process/history/route.ts,
    app/src/app/api/projects/[id]/process/callback/route.ts,
    app/src/app/api/projects/[id]/process/[vid]/route.ts,
    app/src/app/api/projects/[id]/process/[vid]/output/route.ts
  </files>
  <action>
**Step 1: Copy all /api/projects route files to /api/catbrains.**

For each route file under `app/src/app/api/projects/`, create the corresponding file under `app/src/app/api/catbrains/` with the SAME logic but:
- Replace ALL SQL references from `projects` table to `catbrains` table (e.g., `FROM projects` -> `FROM catbrains`, `INTO projects` -> `INTO catbrains`)
- Replace `projectId` param name: the folder uses `[id]` so the param extraction stays as `params.id`. No change needed here.
- Replace any log messages mentioning "proyecto" to "catbrain"
- Keep `export const dynamic = 'force-dynamic'` on all routes
- Keep `process['env']` bracket notation
- The directory structure of the new routes MUST mirror the old ones exactly:
  - `/api/catbrains/route.ts` (GET list, POST create)
  - `/api/catbrains/[id]/route.ts` (GET detail, PUT update, DELETE)
  - `/api/catbrains/[id]/chat/route.ts`
  - `/api/catbrains/[id]/sources/route.ts`
  - `/api/catbrains/[id]/sources/[sid]/route.ts`
  - `/api/catbrains/[id]/sources/reorder/route.ts`
  - `/api/catbrains/[id]/rag/route.ts` (and all sub-routes: create, info, query, status)
  - `/api/catbrains/[id]/stats/route.ts`
  - `/api/catbrains/[id]/bot/create/route.ts`
  - `/api/catbrains/[id]/process/route.ts` (and all sub-routes: status, clean, history, callback, [vid], [vid]/output)

**Step 2: Replace all /api/projects route files with 301 redirects.**

For EACH route file under `app/src/app/api/projects/`, replace its content with a redirect handler. The redirect pattern:

```typescript
import { NextRequest, NextResponse } from 'next/server';

export const dynamic = 'force-dynamic';

export async function GET(request: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const url = new URL(request.url);
  const newPath = url.pathname.replace('/api/projects/', '/api/catbrains/');
  return NextResponse.redirect(new URL(newPath + url.search, url.origin), 301);
}
// Repeat for POST, PUT, DELETE, PATCH as needed per route
```

For routes without `[id]` param (like the root route.ts), simplify accordingly. For routes with `[vid]` or `[sid]` params, the pathname.replace approach handles it automatically since the path structure is preserved.

Each old route file must export the SAME HTTP methods as the original, all returning 301 redirects. This ensures clients using old URLs get properly redirected.

IMPORTANT: In Next.js 15 App Router, params is a Promise. Use `await params` for destructuring.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All /api/catbrains/... routes work with catbrains table, all /api/projects/... routes return 301 redirect to corresponding /api/catbrains/... path, TypeScript compiles</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npx tsc --noEmit` — no type errors
2. `cd /home/deskmath/docflow/app && npm run build` — build succeeds
3. Manual: Start app, confirm catbrains table exists in SQLite, projects table does not
4. Manual: `curl -s -o /dev/null -w '%{http_code}' http://localhost:3500/api/catbrains` returns 200
5. Manual: `curl -s -o /dev/null -w '%{http_code} %{redirect_url}' http://localhost:3500/api/projects` returns 301 with redirect to /api/catbrains
</verification>

<success_criteria>
- catbrains table exists with all original columns plus system_prompt, mcp_enabled, icon_color
- projects table no longer exists (dropped after migration)
- All 20+ API routes under /api/catbrains respond correctly
- All old /api/projects routes return 301 redirect to /api/catbrains equivalents
- TypeScript compilation and Next.js build succeed
</success_criteria>

<output>
After completion, create `.planning/phases/39-renombrado-y-migracion/39-01-SUMMARY.md`
</output>
