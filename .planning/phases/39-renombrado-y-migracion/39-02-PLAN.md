---
phase: 39-renombrado-y-migracion
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - app/src/components/layout/sidebar.tsx
  - app/src/components/layout/breadcrumb.tsx
  - app/src/app/catbrains/page.tsx
  - app/src/app/catbrains/new/page.tsx
  - app/src/app/catbrains/[id]/page.tsx
  - app/src/app/catbrains/error.tsx
  - app/src/app/projects/page.tsx
  - app/src/app/projects/new/page.tsx
  - app/src/app/projects/[id]/page.tsx
  - app/src/app/projects/error.tsx
  - app/src/components/projects/pipeline-nav.tsx
  - app/src/components/projects/pipeline-footer.tsx
  - app/src/components/projects/connection-status-bar.tsx
  - app/src/components/projects/delete-project-dialog.tsx
  - app/src/components/projects/project-settings-sheet.tsx
  - app/src/components/chat/chat-panel.tsx
  - app/src/components/sources/source-manager.tsx
  - app/src/components/sources/source-list.tsx
  - app/src/components/sources/note-editor.tsx
  - app/src/components/sources/url-input.tsx
  - app/src/components/sources/youtube-input.tsx
  - app/src/components/sources/file-upload-zone.tsx
  - app/src/components/process/process-panel.tsx
  - app/src/components/process/version-history.tsx
  - app/src/components/rag/rag-panel.tsx
  - app/src/components/system/system-health-panel.tsx
  - app/src/app/page.tsx
autonomous: true
requirements: [REN-03, REN-04]

must_haves:
  truths:
    - "La sidebar muestra 'CatBrains' en lugar de 'Proyectos' con icono de cerebro"
    - "La URL del listado es /catbrains y muestra cards con ico_catbrain.png"
    - "La URL de detalle es /catbrains/[id] y muestra header con ico_catbrain.png"
    - "Breadcrumbs muestran 'CatBrains' en lugar de 'Proyectos'"
    - "Todos los textos visibles al usuario dicen 'CatBrain(s)' en vez de 'Proyecto(s)'"
    - "Las rutas antiguas /projects/... redirigen a /catbrains/..."
  artifacts:
    - path: "app/src/app/catbrains/page.tsx"
      provides: "CatBrains list page"
      contains: "CatBrains"
    - path: "app/src/app/catbrains/[id]/page.tsx"
      provides: "CatBrain detail page"
      contains: "CatBrain"
    - path: "app/src/components/layout/sidebar.tsx"
      provides: "Sidebar with CatBrains link"
      contains: "CatBrains"
  key_links:
    - from: "app/src/components/layout/sidebar.tsx"
      to: "/catbrains"
      via: "href in sidebar item"
      pattern: "href.*catbrains"
    - from: "app/src/app/catbrains/page.tsx"
      to: "/api/catbrains"
      via: "fetch call"
      pattern: "fetch.*api/catbrains"
    - from: "app/src/app/catbrains/[id]/page.tsx"
      to: "/api/catbrains"
      via: "fetch call"
      pattern: "fetch.*api/catbrains"
---

<objective>
Rename all user-facing UI from "Proyectos" to "CatBrains" — sidebar, pages, breadcrumbs, components, and add ico_catbrain.png icon throughout.

Purpose: The user should see "CatBrains" everywhere in the interface, with no remaining "Proyecto" text.
Output: New /catbrains/ page routes, updated sidebar, breadcrumbs, all component text in Spanish using "CatBrain(s)".
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-renombrado-y-migracion/39-01-SUMMARY.md

@app/src/components/layout/sidebar.tsx
@app/src/components/layout/breadcrumb.tsx
@app/src/app/projects/page.tsx
@app/src/app/projects/[id]/page.tsx
@app/src/app/projects/new/page.tsx
</context>

<interfaces>
<!-- ico_catbrain.png location -->
Icon file exists at: app/Images/icon/ico_catbrain.png
Import pattern: `import Image from 'next/image'` then `<Image src="/Images/icon/ico_catbrain.png" ... />`

<!-- Sidebar item structure from sidebar.tsx -->
```typescript
{ href: '/projects', label: 'Proyectos', icon: FolderKanban },
// Must become:
{ href: '/catbrains', label: 'CatBrains', icon: Brain },
```

<!-- All API calls must now point to /api/catbrains/... -->
<!-- Example: fetch(`/api/projects`) -> fetch(`/api/catbrains`) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create /catbrains page routes and redirect old /projects pages</name>
  <files>
    app/src/app/catbrains/page.tsx,
    app/src/app/catbrains/new/page.tsx,
    app/src/app/catbrains/[id]/page.tsx,
    app/src/app/catbrains/error.tsx,
    app/src/app/projects/page.tsx,
    app/src/app/projects/new/page.tsx,
    app/src/app/projects/[id]/page.tsx,
    app/src/app/projects/error.tsx
  </files>
  <action>
**Pre-check: Verify icon file exists.**
Run: `ls /home/deskmath/docflow/app/Images/icon/ico_catbrain.png` — must exist. If not, the icon must be created or sourced before proceeding.

**Step 1: Create new /catbrains pages** by copying from /projects pages with these changes:
- Copy `app/src/app/projects/page.tsx` to `app/src/app/catbrains/page.tsx`:
  - Replace all fetch URLs from `/api/projects` to `/api/catbrains`
  - Replace all visible text: "Proyectos" -> "CatBrains", "Proyecto" -> "CatBrain", "proyecto" -> "catbrain"
  - Replace the page icon (if FolderKanban) with Brain from lucide-react
  - Add ico_catbrain.png in cards: `<Image src="/Images/icon/ico_catbrain.png" alt="CatBrain" width={24} height={24} />` next to each card title
  - Replace link hrefs: `/projects/` -> `/catbrains/`
  - Replace `/projects/new` -> `/catbrains/new`

- Copy `app/src/app/projects/new/page.tsx` to `app/src/app/catbrains/new/page.tsx`:
  - Same replacements: API URLs, visible text, link hrefs
  - On success redirect to `/catbrains/[id]` instead of `/projects/[id]`

- Copy `app/src/app/projects/[id]/page.tsx` to `app/src/app/catbrains/[id]/page.tsx`:
  - Same replacements: all `/api/projects/` to `/api/catbrains/`, all visible text
  - Add ico_catbrain.png in the detail header
  - Replace any router.push to `/projects/` with `/catbrains/`

- Copy `app/src/app/projects/error.tsx` to `app/src/app/catbrains/error.tsx`:
  - Update visible text

**Step 2: Convert old /projects pages to redirects.**
Replace each old page file with a simple redirect component:

```tsx
import { redirect } from 'next/navigation';
export default function ProjectsRedirect() {
  redirect('/catbrains');
}
```

For `[id]/page.tsx`, extract the id param and redirect to `/catbrains/${id}`.
For `new/page.tsx`, redirect to `/catbrains/new`.
For `error.tsx`, redirect to `/catbrains`.

Colors: Use violet-500/600 accent, bg zinc-950, primary mauve (#8B6D8B) as established.
All UI text in Spanish.
  </action>
  <verify>
    <automated>ls /home/deskmath/docflow/app/Images/icon/ico_catbrain.png && cd /home/deskmath/docflow/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ico_catbrain.png exists, four new /catbrains page files exist, all fetch from /api/catbrains, show "CatBrains" text, include ico_catbrain.png. Four old /projects pages redirect to /catbrains equivalents.</done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar, breadcrumbs, and layout components</name>
  <files>
    app/src/components/layout/sidebar.tsx,
    app/src/components/layout/breadcrumb.tsx,
    app/src/app/page.tsx,
    app/src/components/system/system-health-panel.tsx
  </files>
  <action>
**Sidebar (sidebar.tsx):**
- Change: `{ href: '/projects', label: 'Proyectos', icon: FolderKanban }` -> `{ href: '/catbrains', label: 'CatBrains', icon: Brain }`
- Import `Brain` from `lucide-react` (remove `FolderKanban` if no longer used)

**Breadcrumb (breadcrumb.tsx):**
- Update the path-to-label mapping: `/projects` -> label "CatBrains", `/projects/new` -> "Nuevo CatBrain"
- Add mappings for `/catbrains` -> "CatBrains", `/catbrains/new` -> "Nuevo CatBrain"
- Replace any breadcrumb hrefs from `/projects` to `/catbrains`

**Homepage (app/page.tsx):**
- Replace any "Proyectos" text in welcome/dashboard cards
- Replace links from `/projects` to `/catbrains`

**System health panel:**
- Replace any "proyectos" text with "catbrains" in health checks

All UI text in Spanish. Keep the same color scheme.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Sidebar shows "CatBrains" with Brain icon, breadcrumbs updated, homepage links to /catbrains, system health references catbrains</done>
</task>

<task type="auto">
  <name>Task 3: Update domain components — projects, sources, chat, process, RAG</name>
  <files>
    app/src/components/projects/pipeline-nav.tsx,
    app/src/components/projects/pipeline-footer.tsx,
    app/src/components/projects/connection-status-bar.tsx,
    app/src/components/projects/delete-project-dialog.tsx,
    app/src/components/projects/project-settings-sheet.tsx,
    app/src/components/chat/chat-panel.tsx,
    app/src/components/sources/source-manager.tsx,
    app/src/components/sources/source-list.tsx,
    app/src/components/sources/note-editor.tsx,
    app/src/components/sources/url-input.tsx,
    app/src/components/sources/youtube-input.tsx,
    app/src/components/sources/file-upload-zone.tsx,
    app/src/components/process/process-panel.tsx,
    app/src/components/process/version-history.tsx,
    app/src/components/rag/rag-panel.tsx
  </files>
  <action>
**Components in components/projects/:**
These components are used inside the [id] detail page. Update ALL visible text:
- `pipeline-nav.tsx`: "Proyecto" -> "CatBrain" in any labels/tabs
- `pipeline-footer.tsx`: Any "proyecto" text
- `connection-status-bar.tsx`: Any "proyecto" references
- `delete-project-dialog.tsx`: "Eliminar proyecto" -> "Eliminar CatBrain", all dialog text
- `project-settings-sheet.tsx`: "Configuracion del proyecto" -> "Configuracion del CatBrain", all settings labels

**Update all fetch URLs** in these components from `/api/projects/` to `/api/catbrains/`.

**Source components (source-manager, source-list, note-editor, url-input, youtube-input, file-upload-zone):**
- Replace any `/api/projects/` fetch URLs with `/api/catbrains/`
- Replace any visible text "proyecto" -> "catbrain"

**Process components (process-panel, version-history):**
- Replace fetch URLs: `/api/projects/` -> `/api/catbrains/`
- Replace visible text

**Chat panel (chat-panel.tsx):**
- Replace fetch URL: `/api/projects/${id}/chat` -> `/api/catbrains/${id}/chat`

**RAG panel (rag-panel.tsx):**
- Replace all fetch URLs: `/api/projects/${id}/rag/...` -> `/api/catbrains/${id}/rag/...`

All UI text in Spanish. Keep the same color scheme.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All component fetch URLs use /api/catbrains, all visible text says CatBrain(s) instead of Proyecto(s)</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npx tsc --noEmit` — no type errors
2. `cd /home/deskmath/docflow/app && npm run build` — build succeeds
3. `grep -r "Proyectos\|/api/projects\b\|/projects/" app/src/ --include="*.tsx" --include="*.ts" -l` — only redirect files should match
4. Manual: Visit /catbrains — shows list with ico_catbrain.png in cards
5. Manual: Visit /projects — redirects to /catbrains
</verification>

<success_criteria>
- Sidebar shows "CatBrains" with Brain icon linking to /catbrains
- /catbrains page shows list with ico_catbrain.png
- /catbrains/[id] detail page shows ico_catbrain.png in header
- All breadcrumbs show "CatBrains"
- Zero "Proyectos" visible text remains (except in redirect stubs)
- All component fetch URLs point to /api/catbrains
- Old /projects pages redirect to /catbrains equivalents
</success_criteria>

<output>
After completion, create `.planning/phases/39-renombrado-y-migracion/39-02-SUMMARY.md`
</output>
