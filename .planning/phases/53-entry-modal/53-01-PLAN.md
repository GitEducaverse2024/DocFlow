---
phase: 53-entry-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/catbrains/catbrain-entry-modal.tsx
  - app/src/app/catbrains/page.tsx
  - app/src/app/catbrains/[id]/page.tsx
  - app/messages/es.json
  - app/messages/en.json
autonomous: true
requirements: [MODAL-01, MODAL-02, MODAL-03, MODAL-04, MODAL-05, MODAL-06, MODAL-07]

must_haves:
  truths:
    - "Clicking a CatBrain card on /catbrains opens a Dialog modal, does NOT navigate"
    - "Modal header shows CatBrain icon, name, description, source count, and RAG status"
    - "Modal presents 3 clickable action cards: Chatear, Nuevas Fuentes, Resetear"
    - "Chatear navigates to /catbrains/[id]?step=chat"
    - "Nuevas Fuentes navigates to /catbrains/[id]?step=sources"
    - "Resetear card has destructive styling (red border, warning icon)"
    - "All modal text uses i18n t() from catbrains.modal namespace"
  artifacts:
    - path: "app/src/components/catbrains/catbrain-entry-modal.tsx"
      provides: "CatBrainEntryModal Dialog component"
      contains: "Dialog"
    - path: "app/src/app/catbrains/page.tsx"
      provides: "List page with modal integration"
      contains: "CatBrainEntryModal"
  key_links:
    - from: "app/src/app/catbrains/page.tsx"
      to: "app/src/components/catbrains/catbrain-entry-modal.tsx"
      via: "import + render"
      pattern: "CatBrainEntryModal"
---

<objective>
Create the CatBrain Entry Modal — a shadcn/ui Dialog that opens when clicking a CatBrain card on the list page.

Purpose: Replace direct navigation to the detail page with a contextual modal showing CatBrain status (source count, RAG vectors) and 3 action paths: Chatear (→ chat), Nuevas Fuentes (→ sources), Resetear (→ reset flow).
Output: 1 new component + 3 modified files + i18n keys in both languages.
</objective>

<context>
@.planning/phases/53-entry-modal/53-RESEARCH.md
@app/src/app/catbrains/page.tsx
@app/src/app/catbrains/[id]/page.tsx
@app/src/app/api/catbrains/[id]/stats/route.ts
@app/src/components/projects/delete-project-dialog.tsx
@app/messages/es.json
@app/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CatBrainEntryModal component</name>
  <files>
    app/src/components/catbrains/catbrain-entry-modal.tsx
  </files>
  <action>
Create new file `app/src/components/catbrains/catbrain-entry-modal.tsx`.

Component spec:
```typescript
interface CatBrainEntryModalProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  catbrain: Project | null;
}
```

**Behavior:**
- When `open` becomes true and `catbrain` is set, fetch `GET /api/catbrains/${catbrain.id}/stats` to get `sources_count` and `vectors_count`
- Show loading spinner while stats load
- On close, reset stats state

**Layout (Dialog from @/components/ui/dialog):**
- `DialogContent` with `max-w-lg bg-zinc-950 border-zinc-800`
- **Header section:**
  - CatBrain icon (Image from `/Images/icon/ico_catbrain.png`, 32x32) + name (text-xl font-semibold text-zinc-50)
  - Description below name (text-sm text-zinc-400, line-clamp-2)
  - Status badge using same `getStatusColor` function as list page
- **Info row** (flex gap-4, text-sm text-zinc-400, mt-3):
  - Source count: `📄 {sources_count} fuentes` (use FileText icon from lucide)
  - RAG status: if vectors_count > 0 → green badge "RAG activo · {vectors_count} vectores" (use Database icon); else → gray badge "Sin RAG"
- **Action cards** (grid grid-cols-1 gap-3, mt-6):
  - **Chatear**: Card with MessageCircle icon (violet-500), title "Chatear", subtitle "Conversa con tu CatBrain". `onClick` → `router.push('/catbrains/${catbrain.id}?step=chat')` then close modal
  - **Nuevas Fuentes**: Card with Upload icon (blue-500), title "Nuevas Fuentes", subtitle "Agrega documentos y URLs". `onClick` → `router.push('/catbrains/${catbrain.id}?step=sources')` then close modal
  - **Resetear**: Card with AlertTriangle icon (red-500), title "Resetear", subtitle "Elimina fuentes y RAG". Styled with `border-red-500/30 hover:border-red-500/60` (destructive look). `onClick` → `router.push('/catbrains/${catbrain.id}?step=sources')` then close modal (phase 55 will replace this target with the reset flow)
- **Footer link**: Small "Vista avanzada →" text link (text-xs text-zinc-500 hover:text-zinc-300) that navigates to `/catbrains/${catbrain.id}` (full pipeline view)

**Action card styling:**
- Each card: `bg-zinc-900 border border-zinc-800 hover:border-zinc-600 rounded-lg p-4 cursor-pointer transition-colors`
- Icon + text layout: icon on left (w-10 h-10 rounded-lg flex items-center justify-center bg-{color}-500/10), title + subtitle on right
- Resetear card override: `border-red-500/30 hover:border-red-500/60`

**i18n:** Use `useTranslations('catbrains')` and access keys under `t('modal.title')`, `t('modal.chat')`, etc.

**Imports needed:**
- `Dialog, DialogContent, DialogHeader, DialogTitle` from `@/components/ui/dialog`
- `Badge` from `@/components/ui/badge`
- `Project` from `@/lib/types`
- `useRouter` from `next/navigation`
- `useTranslations` from `next-intl`
- `Image` from `next/image`
- Icons from `lucide-react`: `MessageCircle, Upload, AlertTriangle, FileText, Database, Loader2`
  </action>
  <verify>
    <automated>test -f /home/deskmath/docflow/app/src/components/catbrains/catbrain-entry-modal.tsx && grep -c "Dialog" /home/deskmath/docflow/app/src/components/catbrains/catbrain-entry-modal.tsx && grep -c "router.push" /home/deskmath/docflow/app/src/components/catbrains/catbrain-entry-modal.tsx</automated>
  </verify>
  <done>CatBrainEntryModal component exists with Dialog, 3 action cards, stats fetching, and i18n.</done>
</task>

<task type="auto">
  <name>Task 2: Modify catbrains list page to use modal instead of navigation</name>
  <files>
    app/src/app/catbrains/page.tsx
  </files>
  <action>
Modify `app/src/app/catbrains/page.tsx`:

1. **Add imports:**
   - `import { CatBrainEntryModal } from '@/components/catbrains/catbrain-entry-modal';`

2. **Add state:**
   - `const [selectedCatBrain, setSelectedCatBrain] = useState<Project | null>(null);`

3. **Make the entire card clickable** — Replace the Card element to add an onClick handler and cursor-pointer:
   - Add `onClick={() => setSelectedCatBrain(catbrain)}` and `cursor-pointer` to the Card
   - **Remove** the "View Details" `<Link>` button at the bottom of the card — instead show a subtle right-arrow or just keep the updated date

4. **Render modal** at the end of the return, before the closing `</div>`:
   ```tsx
   <CatBrainEntryModal
     open={selectedCatBrain !== null}
     onOpenChange={(open) => { if (!open) setSelectedCatBrain(null); }}
     catbrain={selectedCatBrain}
   />
   ```

5. **Remove** the `Link` import if no longer needed (but it's still used for "New CatBrain" button, so keep it).

IMPORTANT: The card should be entirely clickable. Remove the existing Link/Button for "View Details" and instead make the whole card an onClick handler.
  </action>
  <verify>
    <automated>grep -n "CatBrainEntryModal" /home/deskmath/docflow/app/src/app/catbrains/page.tsx && grep -n "selectedCatBrain" /home/deskmath/docflow/app/src/app/catbrains/page.tsx && echo "Check no Link to /catbrains/id:" && grep -c "href={\`/catbrains/" /home/deskmath/docflow/app/src/app/catbrains/page.tsx</automated>
  </verify>
  <done>List page uses modal on card click. No direct Link navigation to detail page from cards.</done>
</task>

<task type="auto">
  <name>Task 3: Add query param step support to detail page</name>
  <files>
    app/src/app/catbrains/[id]/page.tsx
  </files>
  <action>
Modify `app/src/app/catbrains/[id]/page.tsx` to support `?step=` query parameter for initial step selection:

1. **Add import:** `import { useSearchParams } from 'next/navigation';`

2. **Add hook** near the top of the component:
   ```typescript
   const searchParams = useSearchParams();
   ```

3. **Modify the initial activeStep** — Change:
   ```typescript
   const [activeStep, setActiveStep] = useState('sources');
   ```
   To:
   ```typescript
   const [activeStep, setActiveStep] = useState(searchParams.get('step') || 'sources');
   ```

This allows the modal to navigate to `/catbrains/[id]?step=chat` and the detail page will open directly on the chat tab.

Valid step values: 'sources', 'process', 'history', 'rag', 'connectors', 'websearch', 'config', 'chat'
  </action>
  <verify>
    <automated>grep -n "useSearchParams" /home/deskmath/docflow/app/src/app/catbrains/\[id\]/page.tsx && grep -n "searchParams.get" /home/deskmath/docflow/app/src/app/catbrains/\[id\]/page.tsx</automated>
  </verify>
  <done>Detail page reads ?step= query param to set initial active step.</done>
</task>

<task type="auto">
  <name>Task 4: Add i18n keys for modal in both languages</name>
  <files>
    app/messages/es.json,
    app/messages/en.json
  </files>
  <action>
Add modal keys inside the `catbrains` namespace in both language files.

**Add to `app/messages/es.json`** inside the `"catbrains"` object:
```json
"modal": {
  "chat": "Chatear",
  "chatDescription": "Conversa con tu CatBrain",
  "newSources": "Nuevas Fuentes",
  "newSourcesDescription": "Agrega documentos y URLs",
  "reset": "Resetear",
  "resetDescription": "Elimina fuentes y RAG",
  "sourcesCount": "{count} fuentes",
  "ragActive": "RAG activo",
  "ragVectors": "{count} vectores",
  "noRag": "Sin RAG",
  "advancedView": "Vista avanzada",
  "loading": "Cargando..."
}
```

**Add to `app/messages/en.json`** inside the `"catbrains"` object:
```json
"modal": {
  "chat": "Chat",
  "chatDescription": "Talk to your CatBrain",
  "newSources": "New Sources",
  "newSourcesDescription": "Add documents and URLs",
  "reset": "Reset",
  "resetDescription": "Delete sources and RAG",
  "sourcesCount": "{count} sources",
  "ragActive": "RAG active",
  "ragVectors": "{count} vectors",
  "noRag": "No RAG",
  "advancedView": "Advanced view",
  "loading": "Loading..."
}
```

Add these keys right after the existing `"detail"` block inside `"catbrains"`.
  </action>
  <verify>
    <automated>grep -c "modal" /home/deskmath/docflow/app/messages/es.json && grep -c "modal" /home/deskmath/docflow/app/messages/en.json</automated>
  </verify>
  <done>Modal i18n keys present in both es.json and en.json under catbrains.modal namespace.</done>
</task>

<task type="auto">
  <name>Task 5: Build verification</name>
  <files></files>
  <action>
Run `npm run build` in app/ directory. Fix any TypeScript errors.

Common issues to watch for:
- Missing imports in the new component
- Type mismatches with Project interface
- i18n key format issues
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes with zero errors.</done>
</task>

</tasks>

<verification>
1. `test -f app/src/components/catbrains/catbrain-entry-modal.tsx` — component exists
2. `grep "CatBrainEntryModal" app/src/app/catbrains/page.tsx` — modal integrated in list page
3. `grep "selectedCatBrain" app/src/app/catbrains/page.tsx` — state management for modal
4. `grep "useSearchParams" app/src/app/catbrains/[id]/page.tsx` — step param support
5. `grep "modal" app/messages/es.json app/messages/en.json` — i18n keys present
6. `cd app && npm run build` — exit code 0
</verification>

<success_criteria>
- Clicking a CatBrain card opens a Dialog modal (not a page navigation)
- Modal shows CatBrain name, icon, description, source count, RAG status (vector count or "Sin RAG")
- 3 action cards: Chatear (→ chat), Nuevas Fuentes (→ sources), Resetear (destructive styling)
- All text uses i18n from catbrains.modal namespace
- Detail page supports ?step= query param
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/53-entry-modal/53-01-SUMMARY.md`
</output>
