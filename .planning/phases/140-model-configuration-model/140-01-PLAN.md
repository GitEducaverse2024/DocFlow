---
phase: 140-model-configuration-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/deskmath/open-antigravity-workspace/config/routing.yaml
  - app/src/lib/services/alias-routing.ts
  - app/data/knowledge/settings.json
autonomous: true
requirements: [MODEL-01, MODEL-02]

must_haves:
  truths:
    - "Los aliases canvas-classifier, canvas-formatter, canvas-writer existen en la tabla model_aliases y resuelven a modelos funcionales"
    - "Gemma4:e4b esta configurado en LiteLLM routing.yaml y aparece en GET /v1/models"
    - "Gemma4:31b esta documentado como no viable por limitacion de VRAM (16GB < 19GB requeridos)"
    - "GET /api/models devuelve gemma-local entre los modelos disponibles"
  artifacts:
    - path: "/home/deskmath/open-antigravity-workspace/config/routing.yaml"
      provides: "Gemma4:e4b model definition via Ollama"
      contains: "gemma-local"
    - path: "app/src/lib/services/alias-routing.ts"
      provides: "Seed aliases canvas-classifier, canvas-formatter, canvas-writer"
      contains: "canvas-classifier"
    - path: "app/data/knowledge/settings.json"
      provides: "Knowledge tree updated with new aliases"
      contains: "canvas-classifier"
  key_links:
    - from: "alias-routing.ts seedAliases"
      to: "model_aliases table"
      via: "INSERT OR IGNORE"
      pattern: "canvas-classifier.*canvas-formatter.*canvas-writer"
    - from: "routing.yaml gemma-local"
      to: "Ollama gemma4:e4b"
      via: "LiteLLM proxy"
      pattern: "ollama/gemma4:e4b"
---

<objective>
Configurar Gemma4 en LiteLLM y crear aliases semanticos para canvas (canvas-classifier, canvas-formatter, canvas-writer) que permitan a CatBot asignar modelos apropiados por tipo de tarea.

Purpose: CatBot necesita aliases semanticos para asignar modelos a nodos de canvas sin conocer nombres internos. Gemma local reduce costes para tareas mecanicas.
Output: routing.yaml con Gemma, alias-routing.ts con 3 nuevos aliases, knowledge tree actualizado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/lib/services/alias-routing.ts
@/home/deskmath/open-antigravity-workspace/config/routing.yaml
@app/data/knowledge/settings.json

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/src/lib/services/alias-routing.ts:
```typescript
export interface AliasRow {
  alias: string;
  model_key: string;
  description: string;
  is_active: number;
  created_at: string;
  updated_at: string;
}

export function seedAliases(): void;
export function getAllAliases(opts?: { active_only?: boolean }): AliasRow[];
export function updateAlias(alias: string, newModelKey: string): AliasRow;
export async function resolveAlias(alias: string): Promise<string>;
```

Existing seed aliases (8 total):
- chat-rag -> gemini-main
- process-docs -> gemini-main
- agent-task -> gemini-main
- catbot -> gemini-main
- generate-content -> gemini-main
- embed -> text-embedding-3-small
- canvas-agent -> gemini-main
- canvas-format -> gemini-main

LiteLLM models currently in routing.yaml:
- gemini-main (vertex_ai/gemini-3.1-pro-preview)
- gemini-search (vertex_ai/gemini-3.1-pro-preview)
- claude-opus (anthropic/claude-opus-4-6)
- claude-sonnet (anthropic/claude-sonnet-4-6)
- gpt-5.4 (openai/gpt-5.4)
- gpt-5.4-pro (openai/gpt-5.4-pro)

Ollama models available:
- gemma4:e4b (9163 MB) -- FITS in RTX 5080 16GB VRAM
- gemma4:31b (18948 MB) -- DOES NOT FIT in 16GB VRAM

GPU: NVIDIA RTX 5080, 16GB VRAM, ~14.5GB free

LiteLLM config: /home/deskmath/open-antigravity-workspace/config/routing.yaml
LiteLLM process: litellm --config /app/config/routing.yaml --port 4000
Ollama URL: http://192.168.1.49:11434
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configurar Gemma4 en LiteLLM y crear aliases semanticos</name>
  <files>
    /home/deskmath/open-antigravity-workspace/config/routing.yaml,
    app/src/lib/services/alias-routing.ts
  </files>
  <action>
**Paso 1 — Agregar Gemma4:e4b a routing.yaml:**

Editar `/home/deskmath/open-antigravity-workspace/config/routing.yaml`. Agregar una nueva seccion GEMMA LOCAL despues de la seccion GPT-5.4 PRO:

```yaml
  # ============================================
  # GEMMA 4 LOCAL (Ollama - RTX 5080)
  # ============================================
  - model_name: gemma-local
    litellm_params:
      model: ollama/gemma4:e4b
      api_base: http://192.168.1.49:11434

  # Nota: gemma4:31b (19GB) no viable — RTX 5080 tiene 16GB VRAM.
  # El alias gemma-local usa gemma4:e4b (9GB) que cabe en GPU.
```

NO agregar la variante openai/gemma-local (no es necesaria para uso interno via aliases).

**Paso 2 — Reiniciar LiteLLM para cargar config:**

El config se monta en `/app/config/routing.yaml` — verificar si es un symlink o mount. Reiniciar LiteLLM:
```bash
# Verificar si el config es accesible
ls -la /app/config/routing.yaml 2>/dev/null
# Si LiteLLM corre como systemd:
sudo systemctl restart litellm 2>/dev/null || sudo kill -HUP $(pgrep -f litellm) 2>/dev/null
```

Esperar 5 segundos y verificar que gemma-local aparece:
```bash
curl -s -H "Authorization: Bearer sk-antigravity-gateway" http://192.168.1.49:4000/v1/models | python3 -c "import json,sys; d=json.load(sys.stdin); models=[m['id'] for m in d['data'] if not m['id'].startswith('openai/')]; print('\n'.join(models))"
```

Si LiteLLM no reinicia automaticamente, buscar como se gestiona el proceso (systemd, supervisor, manual).

**Paso 3 — Verificar que Gemma responde via LiteLLM:**
```bash
curl -s -X POST -H "Authorization: Bearer sk-antigravity-gateway" -H "Content-Type: application/json" http://192.168.1.49:4000/v1/chat/completions -d '{"model":"gemma-local","messages":[{"role":"user","content":"Responde solo: OK"}],"max_tokens":5}' | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['choices'][0]['message']['content'])"
```

**Paso 4 — Agregar aliases semanticos en alias-routing.ts:**

Editar `app/src/lib/services/alias-routing.ts`, funcion `seedAliases()`. Despues de la linea `stmt.run('canvas-format', ...)` agregar 3 nuevos aliases:

```typescript
  stmt.run('canvas-classifier', 'gemma-local', 'Canvas classification/extraction nodes — lightweight local model');
  stmt.run('canvas-formatter', 'gemma-local', 'Canvas formatting/mechanical transform nodes — lightweight local model');
  stmt.run('canvas-writer', 'gemini-main', 'Canvas content writing/redaction nodes — requires quality model');
```

Logica de asignacion:
- `canvas-classifier` -> `gemma-local` (clasificacion es mecanica, modelo local suficiente)
- `canvas-formatter` -> `gemma-local` (formateo es mecanico, modelo local suficiente)
- `canvas-writer` -> `gemini-main` (redaccion requiere calidad, modelo cloud)

Actualizar el logger count de 8 a 11:
```typescript
  logger.info('alias-routing', 'Seeded 11 model aliases');
```

IMPORTANTE: `seedAliases()` solo ejecuta si count == 0. Como la BD ya tiene 8 aliases, los nuevos NO se insertaran automaticamente. Para insertar los nuevos sin destruir los existentes, agregar logica de "upsert missing" despues del guard:

```typescript
export function seedAliases(): void {
  const count = (db.prepare('SELECT COUNT(*) as c FROM model_aliases').get() as { c: number }).c;
  
  const stmt = db.prepare(
    'INSERT OR IGNORE INTO model_aliases (alias, model_key, description, is_active) VALUES (?, ?, ?, 1)'
  );

  if (count === 0) {
    // Full seed for fresh installs
    stmt.run('chat-rag', 'gemini-main', 'Chat RAG conversations');
    stmt.run('process-docs', 'gemini-main', 'Document processing');
    stmt.run('agent-task', 'gemini-main', 'Agent task execution');
    stmt.run('catbot', 'gemini-main', 'CatBot assistant');
    stmt.run('generate-content', 'gemini-main', 'Content generation (agents, skills, workers)');
    stmt.run('embed', 'text-embedding-3-small', 'Embedding generation');
    stmt.run('canvas-agent', 'gemini-main', 'Canvas agent nodes');
    stmt.run('canvas-format', 'gemini-main', 'Canvas output/storage formatting');
  }

  // Always ensure canvas semantic aliases exist (added Phase 140)
  stmt.run('canvas-classifier', 'gemma-local', 'Canvas classification/extraction nodes — lightweight local model');
  stmt.run('canvas-formatter', 'gemma-local', 'Canvas formatting/mechanical transform nodes — lightweight local model');
  stmt.run('canvas-writer', 'gemini-main', 'Canvas content writing/redaction nodes — requires quality model');

  logger.info('alias-routing', 'Model aliases seeded/verified (11 total)');
}
```

La clave es que `INSERT OR IGNORE` no fallara si ya existen — simplemente se saltara los duplicados. Y los 3 nuevos siempre se intentan insertar.

**Paso 5 — Verificar via API de DocFlow:**

Hacer build local para confirmar que compila:
```bash
cd ~/docflow/app && npm run build
```

Verificar que los aliases aparecen despues del restart de la app (o via test):
```bash
curl -s http://192.168.1.49:3500/api/models/aliases | python3 -c "import json,sys; d=json.load(sys.stdin); [print(a['alias'], '->', a['model_key']) for a in d]"
```
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/canvas-tools-fixes.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - routing.yaml tiene gemma-local apuntando a ollama/gemma4:e4b
    - alias-routing.ts tiene canvas-classifier, canvas-formatter, canvas-writer en seedAliases
    - seedAliases usa INSERT OR IGNORE para los 3 nuevos aliases (idempotente)
    - npm run build pasa sin errores
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar knowledge tree y documentar decision Gemma</name>
  <files>
    app/data/knowledge/settings.json
  </files>
  <action>
**Paso 1 — Actualizar settings.json knowledge tree:**

En `app/data/knowledge/settings.json`, agregar al array `concepts`:
```json
"canvas-classifier: alias semantico para nodos de clasificacion/extraccion en canvas. Apunta a gemma-local (modelo local ligero). CatBot usa este alias al crear nodos clasificadores.",
"canvas-formatter: alias semantico para nodos de formateo mecanico en canvas. Apunta a gemma-local. CatBot usa este alias al crear nodos formateadores.",
"canvas-writer: alias semantico para nodos de redaccion/generacion de contenido en canvas. Apunta a gemini-main (requiere calidad). CatBot usa este alias al crear nodos redactores.",
"gemma-local: modelo Gemma4:e4b corriendo en Ollama local (RTX 5080, 16GB VRAM). Usado para tareas mecanicas de canvas (clasificacion, formateo) donde la velocidad y coste cero importan mas que la calidad maxima."
```

En el array `howto`, agregar:
```json
"Para asignar modelo a un nodo de canvas: usar aliases semanticos (canvas-classifier, canvas-formatter, canvas-writer) en vez de nombres de modelo directos. CatBot debe pasar el alias como parametro model en canvas_add_node."
```

En el array `dont`, agregar:
```json
"No usar gemma-local para tareas de redaccion (canvas-writer) — la calidad es insuficiente para generar emails o contenido. Usar gemini-main o superior.",
"No usar gemma4:31b — excede los 16GB de VRAM del RTX 5080. Solo gemma4:e4b (9GB) es viable."
```

**Paso 2 — Documentar decision de Gemma4:31b en STATE.md:**

Agregar en la seccion Decisions de STATE.md:
```
- Phase 140 (MODEL): gemma4:e4b viable (9GB en RTX 5080 16GB). gemma4:31b NO viable (19GB > 16GB VRAM). Alias gemma-local apunta a gemma4:e4b via Ollama.
- Phase 140 (MODEL): canvas-classifier y canvas-formatter -> gemma-local (tareas mecanicas, coste cero). canvas-writer -> gemini-main (requiere calidad de redaccion).
```
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && node -e "const d=require('./data/knowledge/settings.json'); const s=JSON.stringify(d); console.log('classifier:', s.includes('canvas-classifier')); console.log('formatter:', s.includes('canvas-formatter')); console.log('writer:', s.includes('canvas-writer')); console.log('gemma-local:', s.includes('gemma-local'));"</automated>
  </verify>
  <done>
    - settings.json knowledge tree tiene concepts para los 3 aliases y gemma-local
    - settings.json tiene howto para asignar modelo via aliases semanticos
    - settings.json tiene dont para gemma-local en redaccion y gemma4:31b
    - STATE.md documenta la decision de viabilidad de Gemma
  </done>
</task>

</tasks>

<verification>
1. `curl -s -H "Authorization: Bearer sk-antigravity-gateway" http://192.168.1.49:4000/v1/models` incluye gemma-local
2. `curl -s http://192.168.1.49:3500/api/models/aliases` incluye canvas-classifier, canvas-formatter, canvas-writer
3. `grep canvas-classifier app/src/lib/services/alias-routing.ts` encuentra la linea de seed
4. `npm run build` pasa sin errores
</verification>

<success_criteria>
- Los aliases canvas-classifier, canvas-formatter, canvas-writer estan en seedAliases y se insertan al arrancar la app
- gemma-local esta en routing.yaml apuntando a ollama/gemma4:e4b
- gemma4:31b documentado como no viable (19GB > 16GB VRAM) sin bloquear el milestone
- Knowledge tree actualizado con conceptos, howto y dont para los aliases
- GET /api/models devuelve gemma-local entre modelos disponibles
</success_criteria>

<output>
After completion, create `.planning/phases/140-model-configuration-model/140-01-SUMMARY.md`
</output>
