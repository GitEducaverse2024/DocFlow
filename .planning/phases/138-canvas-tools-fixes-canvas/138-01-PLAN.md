---
phase: 138-canvas-tools-fixes-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/__tests__/canvas-tools-fixes.test.ts
  - app/src/lib/services/catbot-tools.ts
  - app/data/knowledge/canvas.json
autonomous: true
requirements: [CANVAS-01, CANVAS-02, CANVAS-03]

must_haves:
  truths:
    - "canvas_add_node con instructions y model persiste ambos campos en flow_data al recargar"
    - "canvas_add_node con instructions='' (string vacio) NO persiste instructions (es intencional — truthy check)"
    - "canvas_add_node rechaza con error si label esta vacio, ausente, o menor a 3 caracteres"
    - "canvas_add_edge rechaza con error si el nodo source es OUTPUT (terminal)"
    - "canvas_add_edge rechaza con error si START ya tiene 1 edge de salida"
    - "canvas_add_edge rechaza con error si CONDITION no tiene sourceHandle valido (yes/no)"
    - "canvas_add_edge rechaza con error si CONDITION ya tiene un edge en la misma rama"
  artifacts:
    - path: "app/src/lib/__tests__/canvas-tools-fixes.test.ts"
      provides: "Tests unitarios para los 3 bugs CANVAS"
      min_lines: 100
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "Fixes en canvas_add_node y canvas_add_edge"
      contains: "args.model"
    - path: "app/data/knowledge/canvas.json"
      provides: "Knowledge tree actualizado con validaciones de canvas tools"
      contains: "label obligatorio"
  key_links:
    - from: "app/src/lib/__tests__/canvas-tools-fixes.test.ts"
      to: "app/src/lib/services/catbot-tools.ts"
      via: "import executeTool, TOOLS"
      pattern: "executeTool.*canvas_add_node|canvas_add_edge"
---

<objective>
Fix los 3 bugs criticos en canvas tools de CatBot (canvas_add_node y canvas_add_edge) que impiden construir CatFlows funcionales, con tests TDD y actualizacion del knowledge tree.

Purpose: Sin estos fixes, CatBot no puede construir CatFlows de calidad — pierde instructions/model al crear nodos, permite conexiones invalidas (edges desde OUTPUT, duplicar ramas CONDITION), y acepta nodos sin label descriptivo. Score auditoria 60/100 no puede mejorar sin esto.
Output: catbot-tools.ts parcheado, tests verdes, canvas.json actualizado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/138-canvas-tools-fixes-canvas/138-RESEARCH.md

@app/src/lib/services/catbot-tools.ts
@app/src/lib/__tests__/catbot-tools-retry-job.test.ts
@app/data/knowledge/canvas.json

<interfaces>
<!-- Key code locations the executor needs. Extracted from catbot-tools.ts. -->

Tool definition canvas_add_node (line 568-592):
```typescript
{
  type: 'function',
  function: {
    name: 'canvas_add_node',
    parameters: {
      properties: {
        canvasId: { type: 'string' },
        nodeType: { type: 'string', enum: ['AGENT', 'PROJECT', 'CONNECTOR', 'CHECKPOINT', 'MERGE', 'CONDITION', 'ITERATOR', 'OUTPUT'] },
        label: { type: 'string' },
        agentId: { type: 'string' },
        connectorId: { type: 'string' },
        instructions: { type: 'string' },
        // MISSING: model parameter — needs to be added
        positionX: { type: 'number' },
        positionY: { type: 'number' },
        // ... iterator fields, insert_between
      },
      required: ['canvasId', 'nodeType', 'label'],
    },
  },
}
```

Tool definition canvas_add_edge (line 594-610):
```typescript
{
  type: 'function',
  function: {
    name: 'canvas_add_edge',
    parameters: {
      properties: {
        canvasId: { type: 'string' },
        sourceNodeId: { type: 'string' },
        targetNodeId: { type: 'string' },
        sourceHandle: { type: 'string' }, // CONDITION: yes/no
      },
      required: ['canvasId', 'sourceNodeId', 'targetNodeId'],
    },
  },
}
```

Handler canvas_add_node (line 2183-2296):
- Line 2221: `const nodeData: Record<string, unknown> = { label: args.label };`
- Line 2222-2228: agentId + CatPaw lookup (sets model from paw)
- Line 2235: `if (args.instructions) nodeData.instructions = args.instructions;`
- Line 2238: `const nodeTypeLower = (args.nodeType as string).toLowerCase();`
- NO label validation, NO explicit model param

Handler canvas_add_edge (line 2298-2340):
- Line 2312-2316: Only validates source/target exist
- NO structural rules (OUTPUT terminal, START max 1, CONDITION sourceHandle)
- NO duplicate edge check

Test mock pattern (from catbot-tools-retry-job.test.ts):
```typescript
vi.hoisted(() => {
  const nodePath = require('path');
  const nodeFs = require('fs');
  const nodeOs = require('os');
  const tmpDir = nodeFs.mkdtempSync(nodePath.join(nodeOs.tmpdir(), 'test-'));
  process['env']['CATBOT_DB_PATH'] = nodePath.join(tmpDir, 'catbot-test.db');
});
vi.mock('@/lib/db', () => ({ default: { prepare: vi.fn(() => ({ run: vi.fn(), get: vi.fn(), all: vi.fn() })) } }));
// ... other mocks for heavy deps
// Then: const tools = await import('@/lib/services/catbot-tools');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for CANVAS-01, CANVAS-02, CANVAS-03</name>
  <files>app/src/lib/__tests__/canvas-tools-fixes.test.ts</files>
  <behavior>
    - CANVAS-01a: canvas_add_node con instructions="Clasifica emails por producto" persiste instructions en flow_data node data
    - CANVAS-01b: canvas_add_node con model="canvas-classifier" persiste model en flow_data node data (incluso sin agentId)
    - CANVAS-01c: canvas_add_node con agentId + model explicito: model explicito gana sobre el del CatPaw
    - CANVAS-02a: canvas_add_edge con source tipo OUTPUT retorna error "OUTPUT es un nodo terminal"
    - CANVAS-02b: canvas_add_edge con source tipo START que ya tiene 1 edge retorna error "START solo puede tener 1 edge de salida"
    - CANVAS-02c: canvas_add_edge con source tipo CONDITION sin sourceHandle retorna error "CONDITION requiere sourceHandle"
    - CANVAS-02d: canvas_add_edge con source tipo CONDITION con sourceHandle="yes" cuando ya existe edge en "yes" retorna error de duplicado
    - CANVAS-03a: canvas_add_node con label="" retorna error "label es obligatorio"
    - CANVAS-03b: canvas_add_node con label="AB" (< 3 chars) retorna error de label descriptivo
  </behavior>
  <action>
    Crear `app/src/lib/__tests__/canvas-tools-fixes.test.ts` siguiendo el patron de mock establecido en `catbot-tools-retry-job.test.ts`:

    1. Usar `vi.hoisted()` para crear tmpDir y setear CATBOT_DB_PATH
    2. Mockear las mismas dependencias pesadas: `@/lib/db`, `@/lib/logger`, `@/lib/services/catbot-holded-tools`, `@/lib/services/template-renderer`, `@/lib/services/template-asset-resolver`, `@/lib/services/alias-routing`, `@/lib/services/discovery`, `@/lib/services/mid`, `@/lib/services/health`, `@/lib/knowledge-tree`, `@/lib/services/catbot-learned`
    3. Import dinamico de `catbot-tools` en `beforeAll`
    4. Helper `seedCanvas(flowData)` que inserta un canvas en catbotDb con flow_data JSON
    5. Helper `getFlowData(canvasId)` que lee flow_data de vuelta
    6. Escribir los 9 tests descritos en behavior — cada uno debe FALLAR (RED) porque:
       - CANVAS-01b/c fallan porque no existe param `model` en tool ni handler
       - CANVAS-02a/b/c/d fallan porque no hay validacion de reglas en canvas_add_edge
       - CANVAS-03a/b fallan porque no hay validacion de label vacio en runtime
    7. CANVAS-01a (instructions) podria pasar si el truthy check funciona — verificar
    8. Ejecutar tests y confirmar que al menos 7 de 9 fallan (CANVAS-01a puede pasar)

    Nota: El mock de `@/lib/db` necesita mockear `prepare().get()` para que devuelva `{ instructions: 'fake skill' }` (mismo patron que retry-job.test.ts), pero para los tests de canvas se usa `catbotDb` (de `@/lib/catbot-db`) directamente, no el mock de `@/lib/db`.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/canvas-tools-fixes.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>Test file existe con 9 tests. Los tests de CANVAS-01b/c, CANVAS-02a/b/c/d, CANVAS-03a/b fallan (RED). CANVAS-01a puede pasar (instructions ya se persiste con truthy check).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fixes in catbot-tools.ts (GREEN)</name>
  <files>app/src/lib/services/catbot-tools.ts</files>
  <behavior>
    - Todos los 9 tests de Task 1 pasan
    - El tool definition de canvas_add_node incluye parametro model
    - El handler de canvas_add_node valida label no vacio y >= 3 chars antes de procesar
    - El handler de canvas_add_node persiste args.model despues del CatPaw lookup (override)
    - El handler de canvas_add_edge valida: OUTPUT terminal, START max 1, CONDITION sourceHandle valido, duplicados
  </behavior>
  <action>
    Aplicar 3 fixes quirurgicos en `catbot-tools.ts`:

    **Fix CANVAS-01 — Persistir model explicito:**
    1. En tool definition (~linea 581), anadir parametro: `model: { type: 'string', description: 'Modelo LLM para el nodo (ej: canvas-classifier, gemini-main)' }`
    2. En handler (~linea 2235, DESPUES del bloque de CatPaw lookup), anadir: `if (args.model) nodeData.model = args.model;`
       - Va despues del CatPaw lookup para que model explicito override el del CatPaw

    **Fix CANVAS-02 — Validar reglas estructurales en canvas_add_edge:**
    Insertar entre linea 2316 (verificacion de nodos) y linea 2318 (creacion de edgeId):
    1. Buscar sourceNode en flowData.nodes, extraer type (lowercase)
    2. Rule OUTPUT terminal: `if (sourceType === 'output')` return error
    3. Rule START max 1: contar edges existentes con source === sourceNodeId, si > 0 return error
    4. Rule CONDITION sourceHandle: si sourceType === 'condition', validar que handle es 'yes' o 'no', y que no exista ya un edge con ese handle
    5. Rule duplicados: si ya existe edge source->target, return error
    Mensajes de error en espanol, claros y descriptivos para que CatBot entienda que hizo mal.

    **Fix CANVAS-03 — Validar label obligatorio:**
    Insertar al inicio del handler canvas_add_node (despues de linea 2185):
    1. `const label = (args.label as string || '').trim();`
    2. Si `!label` return error "El label es obligatorio — proporciona un nombre descriptivo"
    3. Si `label.length < 3` return error "El label debe ser descriptivo (minimo 3 caracteres)"

    Ejecutar tests. TODOS deben pasar (GREEN).
    Ejecutar suite completa para verificar que no se rompio nada: `cd ~/docflow/app && npx vitest run`
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/canvas-tools-fixes.test.ts && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Los 9 tests de canvas-tools-fixes.test.ts pasan (GREEN). Suite completa sin regresiones. catbot-tools.ts tiene: model en tool definition, model override en handler, validacion de label, y 4 reglas de validacion en canvas_add_edge.</done>
</task>

<task type="auto">
  <name>Task 3: Actualizar knowledge tree canvas.json</name>
  <files>app/data/knowledge/canvas.json</files>
  <action>
    Actualizar `app/data/knowledge/canvas.json` segun protocolo de CLAUDE.md:

    1. En `concepts[]`, anadir:
       - "canvas_add_node requiere label descriptivo obligatorio (minimo 3 caracteres) — sin label, CatBot recibe error de validacion"
       - "canvas_add_node acepta parametro model para asignar modelo LLM explicito por nodo, que override el modelo del CatPaw"
       - "canvas_add_edge valida reglas estructurales: OUTPUT es terminal (no puede tener edges de salida), START max 1 edge de salida, CONDITION requiere sourceHandle yes/no sin duplicar ramas"

    2. En `dont[]`, anadir:
       - "No intentar conectar un edge de salida desde un nodo OUTPUT — es terminal"
       - "No crear nodos sin label descriptivo — el tool rechazara la llamada"
       - "No conectar un segundo edge de salida desde START — solo permite 1"
       - "No duplicar ramas de CONDITION — cada sourceHandle (yes/no) solo acepta 1 edge"

    3. En `common_errors[]` (crear si no existe), anadir:
       - { "error": "El label es obligatorio", "cause": "canvas_add_node llamado con label vacio o ausente", "fix": "Proporcionar label descriptivo de 3+ caracteres, ej: 'Clasificador de emails'" }
       - { "error": "OUTPUT es un nodo terminal", "cause": "canvas_add_edge intenta crear edge de salida desde nodo OUTPUT", "fix": "OUTPUT es el ultimo nodo del flujo. Conectar el nodo anterior a OUTPUT, no al reves." }
       - { "error": "CONDITION requiere sourceHandle valido", "cause": "canvas_add_edge a CONDITION sin sourceHandle o con handle invalido", "fix": "Usar sourceHandle: 'yes' o 'no' al conectar desde un nodo CONDITION" }

    No modificar los campos id, name, path, description, endpoints, tools, howto existentes.
  </action>
  <verify>
    <automated>cd ~/docflow/app && node -e "const j=require('./data/knowledge/canvas.json'); const c=j.concepts.join(' '); const d=j.dont.join(' '); console.log('label-ok:', c.includes('label descriptivo')); console.log('model-ok:', c.includes('parametro model')); console.log('output-ok:', d.includes('OUTPUT')); console.log('condition-ok:', d.includes('CONDITION'));"</automated>
  </verify>
  <done>canvas.json contiene: 3 conceptos nuevos (label, model, reglas edge), 4 donts nuevos, 3 common_errors. CatBot tiene el conocimiento de las validaciones nuevas para no tropezar con ellas.</done>
</task>

</tasks>

<verification>
1. Tests unitarios verdes: `cd ~/docflow/app && npx vitest run src/lib/__tests__/canvas-tools-fixes.test.ts`
2. Suite completa sin regresiones: `cd ~/docflow/app && npx vitest run`
3. Build local OK: `cd ~/docflow/app && npm run build 2>&1 | tail -5`
4. Knowledge tree valido: `node -e "JSON.parse(require('fs').readFileSync('app/data/knowledge/canvas.json','utf8'))"`
</verification>

<success_criteria>
- canvas_add_node con model="X" persiste model en flow_data
- canvas_add_node con label vacio/corto retorna error de validacion
- canvas_add_edge desde OUTPUT retorna error terminal
- canvas_add_edge desde START con edge existente retorna error max 1
- canvas_add_edge desde CONDITION sin sourceHandle valido retorna error
- canvas_add_edge desde CONDITION con rama duplicada retorna error
- Suite vitest completa pasa sin regresiones
- canvas.json actualizado con conceptos, donts y common_errors de las validaciones
</success_criteria>

<output>
After completion, create `.planning/phases/138-canvas-tools-fixes-canvas/138-01-SUMMARY.md`
</output>
