---
phase: 139-canvas-tools-capabilities-tools
plan: 02
type: execute
wave: 2
depends_on: [139-01]
files_modified:
  - app/data/knowledge/canvas.json
autonomous: true
requirements: [TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04]

must_haves:
  truths:
    - "canvas.json incluye canvas_set_start_input en el array tools"
    - "canvas.json documenta los parametros extra_skill_ids y extra_connector_ids"
    - "canvas.json documenta la respuesta enriquecida y sus campos"
    - "canvas.json incluye howto para configurar modelo, start input, y skills/conectores"
  artifacts:
    - path: "app/data/knowledge/canvas.json"
      provides: "Knowledge tree actualizado con TOOLS-01..04"
      contains: "canvas_set_start_input"
  key_links:
    - from: "canvas.json tools array"
      to: "CatBot PromptAssembler"
      via: "knowledge-tree.ts loadKnowledgeArea"
      pattern: "canvas_set_start_input"
---

<objective>
Actualizar el knowledge tree de canvas con las nuevas tools y parametros para que CatBot conozca canvas_set_start_input, los parametros extra_skill_ids/extra_connector_ids, y la respuesta enriquecida.

Purpose: CatBot necesita saber que estas tools existen y como usarlas para construir CatFlows completos.
Output: canvas.json actualizado con tools, conceptos, howtos, y donts nuevos.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/139-canvas-tools-capabilities-tools/139-CONTEXT.md
@.planning/phases/139-canvas-tools-capabilities-tools/139-01-SUMMARY.md
@app/data/knowledge/canvas.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update canvas.json knowledge tree</name>
  <files>app/data/knowledge/canvas.json</files>
  <action>
    Leer canvas.json completo. Actualizar:

    1. **tools array**: Agregar `"canvas_set_start_input"` despues de `"canvas_update_node"`

    2. **concepts array**: Agregar:
       - "canvas_set_start_input configura el initialInput (datos de entrada) y opcionalmente listen_mode del nodo START de un canvas. Usa esta tool despues de crear el canvas y el nodo START."
       - "Respuesta enriquecida: canvas_add_node, canvas_update_node, canvas_add_edge y canvas_set_start_input devuelven nodeId, label, type, model, has_instructions, has_agent, has_skills, has_connectors, total_nodes, total_edges — no necesitas llamar a canvas_get_flow para verificar."
       - "extra_skill_ids y extra_connector_ids: parametros opcionales en canvas_add_node y canvas_update_node. Se pasan como strings separados por coma (ej: 'sk1,sk2'). Los IDs se validan contra la DB — usa list_skills y list_connectors antes para verificar disponibilidad."
       - "model en canvas_update_node: puedes cambiar el modelo LLM de un nodo existente. Enviar string vacio resetea el override y el nodo usa el modelo del CatPaw."

    3. **howto array** (crear si no existe): Agregar:
       - "Para configurar un CatFlow completo: 1) canvas_create, 2) canvas_add_node para START, 3) canvas_set_start_input con initialInput, 4) canvas_add_node para cada nodo de procesamiento (con model, instructions, extra_skill_ids si necesario), 5) canvas_add_edge para conectar, 6) verificar con la respuesta enriquecida que total_nodes y total_edges son correctos"
       - "Para cambiar el modelo de un nodo existente: canvas_update_node con model='nombre-modelo'. Para resetear al default del CatPaw: model=''"

    4. **dont array**: Agregar:
       - "No inventes IDs de skills o conectores — usa list_skills y list_connectors para obtener IDs reales antes de pasar extra_skill_ids o extra_connector_ids"
       - "No llames a canvas_get_flow despues de cada canvas_add_node — la respuesta enriquecida ya incluye total_nodes, total_edges y resumen del nodo"

    5. **common_errors array**: Agregar:
       - { "error": "Skills no encontradas: sk-xxx", "cause": "El ID de skill no existe en la base de datos", "solution": "Ejecuta list_skills para ver IDs disponibles antes de usar extra_skill_ids" }
       - { "error": "Este canvas no tiene nodo START", "cause": "Se llamo a canvas_set_start_input en un canvas sin nodo START", "solution": "Crea primero un nodo START con canvas_add_node nodeType=AGENT no — usa nodeType START... en realidad START no esta en el enum. Crea el canvas con canvas_create que auto-genera START, o verifica con canvas_get_flow" }

    Mantener el JSON valido. No borrar entries existentes.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && node -e "const j=require('./data/knowledge/canvas.json'); const t=j.tools; console.log('tools:', t.includes('canvas_set_start_input') ? 'OK' : 'MISSING'); console.log('concepts:', j.concepts.length); console.log('valid JSON: OK')"</automated>
  </verify>
  <done>canvas.json incluye canvas_set_start_input en tools, 4+ conceptos nuevos, 2+ howtos, 2+ donts, 2+ common_errors. JSON valido. CatBot puede descubrir y usar las nuevas capabilities.</done>
</task>

</tasks>

<verification>
- canvas.json es JSON valido y contiene canvas_set_start_input
- Conceptos documentan respuesta enriquecida, extra_skill_ids, extra_connector_ids, model en update_node
- `npm run build` sigue pasando (JSON se incluye en build)
</verification>

<success_criteria>
- Knowledge tree canvas.json documenta las 4 capabilities nuevas
- CatBot puede descubrir canvas_set_start_input, extra_skill_ids/extra_connector_ids, respuesta enriquecida, y model en update_node
- JSON valido, build limpio
</success_criteria>

<output>
After completion, create `.planning/phases/139-canvas-tools-capabilities-tools/139-02-SUMMARY.md`
</output>
