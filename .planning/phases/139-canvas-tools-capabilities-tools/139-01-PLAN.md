---
phase: 139-canvas-tools-capabilities-tools
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/lib/__tests__/canvas-tools-fixes.test.ts
  - app/src/lib/services/catbot-tools.ts
autonomous: true
requirements: [TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04]

must_haves:
  truths:
    - "canvas_update_node acepta model y lo persiste en flow_data del nodo"
    - "canvas_update_node acepta extra_skill_ids y extra_connector_ids y los persiste"
    - "canvas_set_start_input configura initialInput y listen_mode del nodo START"
    - "canvas_set_start_input devuelve error claro si no hay nodo START"
    - "extra_skill_ids y extra_connector_ids se validan contra DB — IDs inexistentes producen error"
    - "canvas_add_node incluye extra_skill_ids y extra_connector_ids en nodeData"
    - "Respuesta de canvas_add_node incluye nodeId, label, type, model, has_instructions, has_agent, has_skills, has_connectors, total_nodes, total_edges"
    - "Respuesta de canvas_update_node incluye los mismos campos enriquecidos"
    - "Respuesta de canvas_add_edge incluye total_nodes y total_edges"
    - "Respuesta de canvas_set_start_input incluye initialInput, listen_mode, total_nodes, total_edges"
  artifacts:
    - path: "app/src/lib/__tests__/canvas-tools-fixes.test.ts"
      provides: "Tests TDD para TOOLS-01..04"
      min_lines: 400
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "Implementacion de canvas tools capabilities"
      contains: "canvas_set_start_input"
  key_links:
    - from: "catbot-tools.ts canvas_update_node"
      to: "flow_data UPDATE"
      via: "model field merge in data object"
      pattern: "if.*args\\.model.*data\\.model"
    - from: "catbot-tools.ts canvas_set_start_input"
      to: "canvases UPDATE"
      via: "initialInput + listen_mode on START node data"
      pattern: "initialInput"
    - from: "catbot-tools.ts canvas_add_node"
      to: "skills DB + connectors DB"
      via: "SELECT validation of extra_skill_ids and extra_connector_ids"
      pattern: "skills WHERE id|connectors WHERE id"
---

<objective>
Implementar las 4 capabilities nuevas de canvas tools mediante TDD: model en update_node (TOOLS-01), nueva tool canvas_set_start_input (TOOLS-02), extra_skill_ids/extra_connector_ids en add_node y update_node (TOOLS-03), y respuesta enriquecida en todas las tools de mutacion (TOOLS-04).

Purpose: CatBot puede configurar completamente un nodo de canvas — modelo, input del START, skills, conectores — y confirmar el estado sin llamadas adicionales.
Output: Tests verdes + tools funcionales en catbot-tools.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-canvas-tools-capabilities-tools/139-CONTEXT.md
@.planning/phases/138-canvas-tools-fixes-canvas/138-01-SUMMARY.md
@app/src/lib/__tests__/canvas-tools-fixes.test.ts
@app/src/lib/services/catbot-tools.ts

<interfaces>
<!-- Existing patterns from Phase 138 test file — reuse mock structure -->

From canvas-tools-fixes.test.ts (mock DB):
```typescript
// DB mock exposes _canvases Map and _catPaws Map for seeding
dbMock._canvases.set(id, { id, flow_data: JSON.stringify({nodes, edges}), node_count });
dbMock._catPaws.set(id, { name, model?, mode? });

// executeTool signature:
executeTool(toolName, args, baseUrl, { userId, sudoActive })
// Returns: { name, result: {...}, actions?: [...] }
```

From catbot-tools.ts canvas_add_node response (current — NOT enriched):
```typescript
return {
  name,
  result: {
    nodeId, label: args.label, type: args.nodeType, position: { x: posX, y: posY },
    ...(edgesCreated.length > 0 ? { inserted_between, edges_created } : {}),
  },
  actions: [{ type: 'navigate', url: `/canvas/${canvasId}`, label: 'Ver canvas →' }],
};
```

From canvas-executor.ts (START node reads):
```typescript
// initialInput is read from data.initialInput
return { output: (data.initialInput as string) || '' };
```

From canvases table:
```sql
-- listen_mode is on the canvases TABLE (0 or 1), NOT on node data
-- canvas_set_start_input configures initialInput on START node DATA
-- and optionally toggles listen_mode on the CANVAS ROW
```

Validation tables:
```sql
SELECT id, name FROM skills WHERE id = ?
SELECT id, name FROM connectors WHERE id = ?
```

Existing canvas_toggle_listen_mode tool (line 281):
```typescript
// Already exists: toggles listen_mode on canvases table row
// canvas_set_start_input uses SAME table column for listen_mode
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests (RED) for TOOLS-01..04</name>
  <files>app/src/lib/__tests__/canvas-tools-fixes.test.ts</files>
  <behavior>
    TOOLS-01 — model en canvas_update_node:
    - Test 01a: canvas_update_node con model='canvas-classifier' persiste model en node data
    - Test 01b: canvas_update_node con model='' resetea el override (model se borra del data)

    TOOLS-02 — canvas_set_start_input:
    - Test 02a: canvas_set_start_input con initialInput='3 emails JSON' persiste en START node data.initialInput
    - Test 02b: canvas_set_start_input en canvas sin nodo START devuelve error en espanol
    - Test 02c: canvas_set_start_input con listen_mode=true activa listen_mode=1 en la row de canvases
    - Test 02d: respuesta incluye initialInput configurado, total_nodes, total_edges

    TOOLS-03 — extra_skill_ids y extra_connector_ids:
    - Test 03a: canvas_add_node con extra_skill_ids='sk1,sk2' persiste data.skills=['sk1','sk2'] (mock skills table con sk1,sk2 existentes)
    - Test 03b: canvas_add_node con extra_connector_ids='conn1' persiste data.extraConnectors=['conn1'] (mock connectors table)
    - Test 03c: canvas_add_node con extra_skill_ids='sk1,sk-bad' devuelve error con IDs invalidos
    - Test 03d: canvas_update_node con extra_skill_ids y extra_connector_ids persiste ambos
    
    TOOLS-04 — respuesta enriquecida:
    - Test 04a: canvas_add_node response incluye nodeId, label, type, model, has_instructions, has_agent, has_skills, has_connectors, total_nodes, total_edges
    - Test 04b: canvas_update_node response incluye campos enriquecidos del nodo + total_nodes, total_edges
    - Test 04c: canvas_add_edge response incluye total_nodes, total_edges
    - Test 04d: canvas_set_start_input response incluye initialInput, listen_mode, total_nodes, total_edges
  </behavior>
  <action>
    Extender el test file canvas-tools-fixes.test.ts con 4 nuevas secciones describe.

    Para el mock de DB, necesitas extender el mock existente para:
    1. Soportar `SELECT id, name FROM skills WHERE id = ?` — agregar un Map `_skills` al mock
    2. Soportar `SELECT id, name FROM connectors WHERE id = ?` — agregar un Map `_connectors` al mock
    3. Soportar `SELECT id, listen_mode FROM canvases WHERE id = ?` — el mock actual de SELECT canvases ya devuelve por id, pero necesita incluir listen_mode
    4. Soportar `UPDATE canvases SET listen_mode = ? WHERE id = ?` — agregar handler para este UPDATE

    Para los tests de canvas_set_start_input: la tool aun no existe en TOOLS[]. El test de existencia fallara en RED hasta Task 2.

    Para los tests de respuesta enriquecida (TOOLS-04): los tests verifican campos del result object que la implementacion actual no devuelve.

    Patron de cada test (RED):
    ```typescript
    it('XYa: descripcion', async () => {
      seedCanvas('c-XYa', { nodes: [...], edges: [...] });
      // Seed skills/connectors si necesario
      dbMock._skills.set('sk1', { id: 'sk1', name: 'Skill 1' });
      
      const result = await executeTool('canvas_xxx', { ... }, 'http://test', { userId: 'u-test', sudoActive: false });
      const body = result.result as Record<string, unknown>;
      
      expect(body.field).toBe(expectedValue);
    });
    ```

    Ejecutar tests: DEBEN fallar (RED). Commit con prefijo test(139-01).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/canvas-tools-fixes.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>14+ tests nuevos escritos en las 4 secciones describe, todos FALLAN (RED). Commit atomico creado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canvas tools capabilities (GREEN)</name>
  <files>app/src/lib/services/catbot-tools.ts</files>
  <action>
    Implementar en catbot-tools.ts para que todos los tests pasen. Cambios necesarios:

    **TOOLS-01 — model en canvas_update_node:**
    1. Agregar `model: { type: 'string', description: 'Modelo LLM para el nodo. Enviar string vacio para resetear el override.' }` al schema de canvas_update_node (linea ~665)
    2. En la implementacion (linea ~2541): agregar handler para model:
       ```typescript
       if (args.model !== undefined) {
         if (args.model === '' || args.model === null) {
           delete data.model; // Reset override
         } else {
           data.model = args.model;
         }
       }
       ```

    **TOOLS-02 — canvas_set_start_input:**
    1. Agregar schema tool al array TOOLS (despues de canvas_update_node):
       ```typescript
       {
         type: 'function',
         function: {
           name: 'canvas_set_start_input',
           description: 'Configura el input inicial y opcionalmente el modo escucha del nodo START de un canvas',
           parameters: {
             type: 'object',
             properties: {
               canvasId: { type: 'string', description: 'ID del canvas' },
               initialInput: { type: 'string', description: 'Input inicial para el nodo START' },
               listen_mode: { type: 'boolean', description: 'Activar/desactivar modo escucha del CatFlow' },
             },
             required: ['canvasId', 'initialInput'],
           },
         },
       }
       ```
    2. Agregar case en executeTool:
       - Leer canvas de DB, parsear flow_data
       - Buscar nodo con type='start'. Si no existe: `{ error: 'Este canvas no tiene nodo START — crea uno primero con canvas_add_node' }`
       - Actualizar data.initialInput = args.initialInput
       - Si args.listen_mode !== undefined: `UPDATE canvases SET listen_mode = ?, flow_data = ?, updated_at = ? WHERE id = ?`
       - Si no: solo `UPDATE canvases SET flow_data = ?, updated_at = ? WHERE id = ?`
       - Devolver respuesta enriquecida (TOOLS-04)
    3. Registrar en permission gate como always_allowed (no requiere permisos especiales, similar a canvas_add_node)

    **TOOLS-03 — extra_skill_ids y extra_connector_ids:**
    1. Agregar al schema de canvas_add_node (linea ~576):
       ```typescript
       extra_skill_ids: { type: 'string', description: 'IDs de skills extra separados por coma' },
       extra_connector_ids: { type: 'string', description: 'IDs de conectores extra separados por coma' },
       ```
    2. Agregar al schema de canvas_update_node (linea ~665):
       ```typescript
       extra_skill_ids: { type: 'string', description: 'IDs de skills extra separados por coma' },
       extra_connector_ids: { type: 'string', description: 'IDs de conectores extra separados por coma' },
       ```
    3. En canvas_add_node impl (linea ~2240), ANTES de crear el nodo:
       ```typescript
       // Validate and parse extra_skill_ids
       if (args.extra_skill_ids) {
         const ids = (args.extra_skill_ids as string).split(',').map(s => s.trim()).filter(Boolean);
         const invalid: string[] = [];
         for (const id of ids) {
           const exists = db.prepare('SELECT id FROM skills WHERE id = ?').get(id);
           if (!exists) invalid.push(id);
         }
         if (invalid.length > 0) return { name, result: { error: `Skills no encontradas: ${invalid.join(', ')}` } };
         nodeData.skills = ids;
       }
       // Same for extra_connector_ids
       if (args.extra_connector_ids) {
         const ids = (args.extra_connector_ids as string).split(',').map(s => s.trim()).filter(Boolean);
         const invalid: string[] = [];
         for (const id of ids) {
           const exists = db.prepare('SELECT id FROM connectors WHERE id = ?').get(id);
           if (!exists) invalid.push(id);
         }
         if (invalid.length > 0) return { name, result: { error: `Conectores no encontrados: ${invalid.join(', ')}` } };
         nodeData.extraConnectors = ids;
       }
       ```
    4. En canvas_update_node impl: mismo patron de validacion + merge para ambos campos

    **TOOLS-04 — Respuesta enriquecida:**
    Crear helper function `buildEnrichedResponse` para DRY:
    ```typescript
    function buildNodeSummary(node: Record<string, unknown>): Record<string, unknown> {
      const data = node.data as Record<string, unknown>;
      return {
        nodeId: node.id,
        label: data.label,
        type: node.type,
        model: data.model || null,
        has_instructions: Boolean(data.instructions),
        has_agent: Boolean(data.agentId),
        has_skills: Array.isArray(data.skills) && data.skills.length > 0,
        has_connectors: Boolean(data.connectorId) || (Array.isArray(data.extraConnectors) && data.extraConnectors.length > 0),
        position: node.position,
      };
    }
    ```
    Aplicar en:
    - canvas_add_node: reemplazar result actual con `{ ...buildNodeSummary(newNode), total_nodes: flowData.nodes.length, total_edges: flowData.edges.length, ...(edgesCreated?) }`
    - canvas_update_node: reemplazar `{ updated: true, nodeId, newData: data }` con `{ updated: true, ...buildNodeSummary(node), total_nodes, total_edges }`
    - canvas_add_edge: agregar `total_nodes: flowData.nodes.length, total_edges: flowData.edges.length` al result
    - canvas_set_start_input: devolver `{ initialInput, listen_mode, ...buildNodeSummary(startNode), total_nodes, total_edges }`

    Errores en espanol. Usar `process['env']` si se necesita env vars. No tocar canvas-executor.ts.

    Ejecutar tests: DEBEN pasar (GREEN). Ejecutar `npm run build` para verificar build limpio.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/canvas-tools-fixes.test.ts 2>&1 | tail -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Todos los tests pasan (GREEN). Build limpio sin errores. canvas_update_node acepta model, canvas_set_start_input funciona, extra_skill_ids/extra_connector_ids validados contra DB, respuestas enriquecidas en las 4 tools de mutacion. Commit atomico creado.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/canvas-tools-fixes.test.ts` — todos los tests pasan
- `npm run build` — build limpio sin unused imports ni errores de tipo
- Tests cubren: model en update_node, canvas_set_start_input (happy path + error), extra_skill_ids/extra_connector_ids (validacion + persistencia), respuesta enriquecida en las 4 tools
</verification>

<success_criteria>
- canvas_update_node acepta model y lo persiste (TOOLS-01)
- canvas_set_start_input configura initialInput + listen_mode del START (TOOLS-02)
- extra_skill_ids y extra_connector_ids se parsean, validan contra DB, y persisten en data (TOOLS-03)
- Respuesta enriquecida con node summary + totals en add_node, update_node, add_edge, set_start_input (TOOLS-04)
- Todos los tests verdes, build limpio
</success_criteria>

<output>
After completion, create `.planning/phases/139-canvas-tools-capabilities-tools/139-01-SUMMARY.md`
</output>
