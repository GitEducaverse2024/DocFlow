---
phase: 155-kb-cleanup-final
plan: 04
type: execute
wave: 4
depends_on: ["155-03"]
files_modified:
  - .docflow-kb/_manual.md
  - .planning/phases/155-kb-cleanup-final/155-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [KB-37, KB-38, KB-39]

must_haves:
  truths:
    - ".docflow-kb/_manual.md contains a `## Rollback de la migración v29.1 (Phase 155)` section with 3 clear git-revert recipes"
    - ".docflow-kb/_manual.md has a `## Phase 155 Cleanup` section summarizing the cleanup (legacy layers deleted, R26-R29 added, backfill applied)"
    - "155-VERIFICATION.md contains the 3-prompt CatBot oracle response verbatim (KB-only consultation, no query_knowledge invoked, R26 cited when asked)"
    - "155-VERIFICATION.md contains grep invariants, docker build log tail, vitest tail (only orthogonal reds), validate-kb exit 0"
    - "REQUIREMENTS.md Traceability table shows KB-28..KB-39 rows marked Complete (updated by this plan)"
    - "Human UAT checkpoint: rollback recipes read-through + 3 oracle prompts approved"
  artifacts:
    - path: ".docflow-kb/_manual.md"
      provides: "Updated manual with Phase 155 closeout + rollback plan"
      contains: "Rollback de la migración v29.1"
    - path: ".planning/phases/155-kb-cleanup-final/155-VERIFICATION.md"
      provides: "Full evidence pack for phase close: grep invariants, docker build log, vitest summary, CatBot oracle verbatim"
      contains: "## CatBot Oracle Evidence"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability updated: KB-28..KB-39 marked Complete"
      contains: "KB-39 | Phase 155 | Complete"
  key_links:
    - from: ".docflow-kb/_manual.md Rollback section"
      to: "git history (commits from Plans 02 + 03)"
      via: "SHA placeholders filled by orchestrator at commit time"
      pattern: "git revert <SHA"
    - from: "155-VERIFICATION.md Oracle section"
      to: "running Docker container on localhost:3500"
      via: "POST /api/catbot/chat verbatim responses"
      pattern: "list_cat_paws|search_kb|R26"
    - from: ".planning/REQUIREMENTS.md Traceability"
      to: "ROADMAP phase 155 closeout"
      via: "Complete markers + coverage 51/51"
      pattern: "KB-3\\d \\| Phase 155"
---

<objective>
Cierre formal de Phase 155. Tres operaciones:
1. **Actualizar `.docflow-kb/_manual.md`** con sección de rollback (3 recipes) + sección Phase 155 Cleanup resumiendo el trabajo.
2. **Ejecutar el oracle CatBot de cierre** (3 prompts via POST /api/catbot/chat) con el container corriendo post-Plan-03 (rebuild + backfill aplicados).
3. **Escribir `155-VERIFICATION.md`** con toda la evidencia (grep invariants, build log, oracle verbatim, validate-kb exit 0, vitest summary) + **actualizar traceability de REQUIREMENTS.md** marcando KB-28..KB-39 como Complete.

**Checkpoint humano:** paused gate al final del Task 3 para que el usuario inspeccione el rollback, los 3 oracle prompts + evidencia, y apruebe `/gsd:complete-phase 155`.

Purpose: Transformar los artefactos técnicos de Plans 01-03 en un phase close formal con evidencia auditable. Sin oracle, el phase no cumple el CLAUDE.md §"Protocolo de Testing: CatBot como Oráculo".

Output: _manual.md extendido, VERIFICATION.md completo, REQUIREMENTS.md traceability cerrada, aprobación humana para close.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/155-kb-cleanup-final/155-CONTEXT.md
@.planning/phases/155-kb-cleanup-final/155-RESEARCH.md
@.planning/phases/155-kb-cleanup-final/155-VALIDATION.md
@.planning/phases/155-kb-cleanup-final/155-01-SUMMARY.md
@.planning/phases/155-kb-cleanup-final/155-02-SUMMARY.md
@.planning/phases/155-kb-cleanup-final/155-03-SUMMARY.md
@.docflow-kb/_manual.md
@.planning/phases/154-kb-dashboard-knowledge/154-VERIFICATION.md
@.planning/phases/152-kb-catbot-consume/152-VERIFICATION.md

<interfaces>
<!-- State established by Plans 01-03 after merge: -->

Container status (Plan 03 Step A completed):
- Docker image built with Plan 02 code (no query_knowledge, no knowledge-tree.ts, no TabKnowledgeTree).
- Volume `.docflow-kb` mounted rw (Phase 153-04 preserved).
- Container healthy on localhost:3500.

KB status (Plan 01 + Plan 03 completed):
- `.docflow-kb/rules/` contains R01-R29 + SE01-SE03 + DA01-DA04 (36 atoms total).
- `.docflow-kb/_schema/tag-taxonomy.json` includes `critical` + R26-R29.
- `.docflow-kb/resources/` backfilled from live DB (Plan 03 Step B).
- `_index.json` + `_header.md` regenerated.

REQUIREMENTS.md snapshot pre-Plan-04:
- KB-28..KB-39 rows exist in Traceability marked `Pending`.
- KB-01..KB-05 rows exist (added during planning).

Oracle pattern (from Phase 154-VERIFICATION.md KB-26):
```bash
curl -sX POST http://localhost:3500/api/catbot/chat \
  -H "Cookie: docatflow_locale=es" \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"<prompt>"}],"channel":"web"}' \
  | jq '{reply, tool_calls: [.tool_calls[]?.function?.name]}'
```

Expected oracle signals (post-155):
- tool_calls includes `search_kb`, `get_kb_entry`, or `list_*` tools.
- tool_calls does NOT include `query_knowledge` or `explain_feature` (removed).
- Responses cite R26-R29 when asked about restrictions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend _manual.md with Rollback + Phase 155 Cleanup sections</name>
  <files>.docflow-kb/_manual.md</files>
  <action>
Append two sections to `.docflow-kb/_manual.md` (after the existing Phase 153 + 154 sections). These document Phase 155 closeout and the rollback playbook for post-merge safety.

**Section 1 — Append at end of existing file:**

```markdown

## Phase 155 Cleanup (2026-04-20)

Phase 155 cerró el ciclo KB v29.1 eliminando la deuda técnica de los dos knowledge layers legacy. Trabajo consolidado:

### Archivos borrados (23+ totales)
- `app/data/knowledge/` completo (11 archivos: 7 áreas JSON + `_index.json` + `_template.json` + 2 MDs `canvas-nodes-catalog.md` + `canvas-rules-index.md`)
- `.planning/knowledge/` completo (12 archivos)
- `skill_orquestador_catbot_enriched.md` (raíz)
- `app/src/lib/knowledge-tree.ts` + 4 tests asociados
- `app/src/app/api/catbot/knowledge/tree/route.ts`
- `app/src/components/settings/catbot-knowledge/tab-knowledge-tree.tsx`

### Código barrido
- `catbot-tools.ts`: imports de knowledge-tree, TOOLS entries `query_knowledge` + `explain_feature`, helpers `mapConceptItem`/`renderConceptItem`/`formatKnowledge*`/`scoreKnowledgeMatch`, case handlers.
- `catbot-prompt-assembler.ts`: import knowledge-tree, `PAGE_TO_AREA`, `getPageKnowledge`, `renderConceptItem`, `formatKnowledgeForPrompt`, `buildKnowledgeProtocol` simplificado (solo search_kb + get_kb_entry + search_documentation + log_knowledge_gap).
- `canvas-rules.ts`: reescrito de raíz — ahora lee `.docflow-kb/rules/R*.md + SE*.md + DA*.md` (Plan 01).
- 9 test files con `vi.mock('@/lib/knowledge-tree', ...)` blocks removidos + 2 tests con fixture reads de `app/data/knowledge/` adaptados.
- `catpaw-gmail-executor.ts` + `catpaw-drive-executor.ts`: comment references repuntados a `.docflow-kb/protocols/connector-logs-redaction.md`.
- `search-docs/route.ts`: DOC_PATHS sin `.planning/knowledge/`.
- `app/docker-entrypoint.sh` reducido a 2 líneas (sin cp de data-seed/knowledge).
- `app/Dockerfile`: sin COPY de `/app/data/knowledge`.

### Docs simplificados
- `CLAUDE.md` (80 → ~50 líneas): §"Protocolo de Documentación" reemplazado por puntero a `.docflow-kb/_manual.md`; §"Documentación de referencia" repuntada a `.docflow-kb/`; §"Restricciones absolutas" reemplazada por pointer a `search_kb({tags:["critical"]})` (R26-R29).
- `.planning/Index.md`: §"Catalogos de Conocimiento (knowledge/)" borrada; §"Knowledge Base" simplificada (sin "en construcción").

### KB extendido
- `.docflow-kb/rules/` gana 11 atoms nuevos:
  - 7 migrados desde `canvas-rules-index.md` (Plan 01): SE01, SE02, SE03, DA01, DA02, DA03, DA04.
  - 4 nuevos `critical` (Plan 03): R26 (canvas-executor inmutable), R27 (agentId UUID), R28 (process['env']), R29 (Docker rebuild tras execute-catpaw).
- `_schema/tag-taxonomy.json`: `critical` añadido a cross_cutting; R26-R29 añadidos a rules.

### Backfill live-DB
- Commit separado `chore(kb): backfill resources from live DB post-155`. Captura el estado real de las 6 tablas DB (cat_paws, connectors, skills, catbrains, email_templates, canvases). Cierra el drift `kb_entry: null` heredado de Phase 152.

## Rollback de la migración v29.1 (Phase 155)

Si tras Phase 155 surge un problema crítico que requiere restaurar el estado pre-cleanup, los siguientes reverts son seguros:

### Recipe 1: Restaurar archivos legacy + código consumidor (big atomic commit)
```bash
git revert <SHA-del-commit-Plan-155-02>
```
Restaura:
- `app/data/knowledge/` (11 archivos)
- `.planning/knowledge/` (12 archivos)
- `skill_orquestador_catbot_enriched.md` (raíz)
- `app/src/lib/knowledge-tree.ts` + 4 tests
- `app/src/app/api/catbot/knowledge/tree/route.ts`
- `app/src/components/settings/catbot-knowledge/tab-knowledge-tree.tsx`
- `query_knowledge` + `explain_feature` cases en `catbot-tools.ts`
- `mapConceptItem`/`renderConceptItem` helpers
- Dockerfile + docker-entrypoint.sh con data-seed/knowledge
- CLAUDE.md §Protocolo de Documentación + §Restricciones absolutas (pre-155 shape)

Tras revert: `cd app && npm install && docker compose build docflow && docker compose up -d`. Esperado: build passes, vitest verde, container healthy. 32/32 tests verdes en `knowledge-tree.test.ts` (el subject vive otra vez).

### Recipe 2: Restaurar solo canvas-rules.ts al modo MD-catalog (Plan 01 revert)
```bash
git revert <SHA-del-commit-Plan-155-01>
```
Restaura la versión pre-155 de `canvas-rules.ts` (lee de `app/data/knowledge/canvas-*.md`). NOTA: solo tiene sentido SI también se reverte Plan 02 (Recipe 1), porque `canvas-rules.ts` pre-155 busca archivos que Plan 02 borró.

### Recipe 3: Restaurar estado KB pre-backfill
```bash
git revert <SHA-del-commit-chore(kb):-backfill>
```
Devuelve el snapshot `.docflow-kb/resources/*.md` al estado pre-Plan-03. Si prefieres regenerar desde DB live (en lugar de volver al snapshot), correr:
```bash
cd /home/deskmath/docflow && node scripts/kb-sync.cjs --full-rebuild --source db
```

### Recipe 4: Rollback de R26-R29 + taxonomy extension
```bash
git revert <SHA-del-commit-Plan-155-03>
```
Borra R26-R29 + devuelve tag-taxonomy.json al estado pre-extensión. Sólo hacer esto si las reglas R26-R29 han sido reemplazadas por otro mecanismo de discovery (ej. volver a CLAUDE.md §Restricciones absolutas).

### Nota sobre reverts tardíos

Los 4 reverts son seguros durante ~30 días. Si se mergea a prod + pasan semanas con la DB evolucionando:
- Revert del backfill (Recipe 3) puede chocar con rows nuevas en DB live. Re-correr `kb-sync.cjs --full-rebuild --source db` es más seguro.
- Revert de archivos legacy (Recipe 1): los JSONs `app/data/knowledge/*.json` restaurados drifted del formato que actualmente espera `query_knowledge` (Phase 152 hizo Zod extensions). Probable que requiera patch manual a `_index.json.areas[].updated_at` o re-extender Zod.

### Verificación post-rollback
- `cd /home/deskmath/docflow/app && npx vitest run` → suite verde (asumiendo Phase 152 Zod extensions se preserven — están en una commit anterior separada).
- `ls app/data/knowledge/` → 11 archivos restaurados.
- `curl http://localhost:3500/api/catbot/chat` con "knowledge tree areas" → CatBot cita los 7 áreas legacy.
```

**Section 2 — Update the existing pre-155 "## Estado actual: bootstrap" section (line ~13 of _manual.md) to reflect productivo-post-155 state:**

Find the line `**Estado actual: bootstrap.** Esta carpeta se creó en Phase 149 del roadmap. La estructura, schemas y herramientas están listas; **el contenido se migra en fases posteriores** del PRD` and replace the whole paragraph with:

```markdown
**Estado actual: productivo (post-155).** El KB es la única fuente canónica de documentación DocFlow desde Phase 155 (2026-04-20). Los layers legacy (`app/data/knowledge/*.json` + `.planning/knowledge/*.md` + `skill_orquestador_catbot_enriched.md`) fueron borrados físicamente; el módulo TS `knowledge-tree.ts` + tools `query_knowledge`/`explain_feature` removidos. CatBot consume el KB via `search_kb`/`get_kb_entry` (Phase 152) con `_header.md` inyectado automáticamente en cada session; creation tools sincronizan DB↔KB via hooks (Phase 153).
```

**Verification after edits:**
- `wc -l .docflow-kb/_manual.md` — expect significantly longer than pre-edit (~400+ extra lines from Rollback + Phase 155 sections).
- `grep -c "Rollback de la migración v29.1" .docflow-kb/_manual.md` — expect 1.
- `grep -c "Phase 155 Cleanup" .docflow-kb/_manual.md` — expect 1.

**NOTE:** Do NOT manually fill in `<SHA-del-commit-*>` placeholders — leave them as literal `<SHA-del-commit-Plan-155-02>` etc. The execute-phase workflow or human operator will resolve SHAs post-commit; the placeholders are a standard Phase-153 pattern.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && grep -c "Rollback de la migración v29.1" .docflow-kb/_manual.md && grep -c "Phase 155 Cleanup" .docflow-kb/_manual.md && grep -c "productivo (post-155)" .docflow-kb/_manual.md && node scripts/validate-kb.cjs 2>&1 | tail -3</automated>
  </verify>
  <done>_manual.md contains "Rollback de la migración v29.1" section, "Phase 155 Cleanup" section, and the pre-155 "bootstrap" paragraph updated to "productivo (post-155)". validate-kb.cjs exit 0 (manual.md isn't schema-validated but full KB run stays clean).</done>
</task>

<task type="auto">
  <name>Task 2: Run 3-prompt CatBot oracle + collect evidence</name>
  <files>.planning/phases/155-kb-cleanup-final/155-VERIFICATION.md</files>
  <action>
Run the 3 oracle prompts documented in CLAUDE.md §"Protocolo de Testing" against the running Docker container (Plan 03 Step A rebuilt the image). Capture verbatim responses + tool_calls traces into `155-VERIFICATION.md`.

**Pre-check:**
```bash
curl -s http://localhost:3500/api/ops/self-check 2>&1 | head -5
docker ps --filter name=docflow --format '{{.Status}}'
```
Container MUST be up and responsive. If not, `docker compose up -d` + wait 5s.

**Oracle prompt 1 — List CatPaws + kb_entry resolution:**
```bash
curl -sX POST http://localhost:3500/api/catbot/chat \
  -H "Cookie: docatflow_locale=es" \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"Lista los CatPaws activos y dime el kb_entry del primero. ¿Qué devuelve?"}],"channel":"web"}' \
  | tee /tmp/oracle-155-prompt-1.json \
  | jq '{reply, tool_calls: [.tool_calls[]?.function?.name], kb_entry_sample: (.tool_results // [])}'
```

Expected signals:
- `tool_calls` includes `list_cat_paws` (NOT `query_knowledge`).
- Response surfaces a path like `"kb_entry": "resources/catpaws/..."` for Operador Holded (53f19c51) — non-null.
- No "knowledge tree" phrase in the reply text.

**Oracle prompt 2 — Critical rule citation (R26):**
```bash
curl -sX POST http://localhost:3500/api/catbot/chat \
  -H "Cookie: docatflow_locale=es" \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"¿Puedo editar canvas-executor.ts? ¿Por qué?"}],"channel":"web"}' \
  | tee /tmp/oracle-155-prompt-2.json \
  | jq '{reply, tool_calls: [.tool_calls[]?.function?.name]}'
```

Expected signals:
- `tool_calls` includes `search_kb({tags:["critical"]})` OR `get_kb_entry({id:"rule-r26-canvas-executor-immutable"})` OR `search_kb({search:"canvas-executor"})`.
- Reply cites R26 and says "no, no se puede editar" or equivalent.
- Reply mentions "canvas-executor.ts está congelado" or "inmutable" or similar from the R26 body.

**Oracle prompt 3 — Design rules discovery (post-SE/DA migration):**
```bash
curl -sX POST http://localhost:3500/api/catbot/chat \
  -H "Cookie: docatflow_locale=es" \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"¿Qué reglas de diseño canvas existen? Cuéntame algunas SE y DA."}],"channel":"web"}' \
  | tee /tmp/oracle-155-prompt-3.json \
  | jq '{reply, tool_calls: [.tool_calls[]?.function?.name]}'
```

Expected signals:
- `tool_calls` includes `search_kb({type:"rule"})` or similar.
- Reply enumerates R01-R29 + SE01-SE03 + DA01-DA04 (or a representative subset).
- `query_knowledge` is NOT invoked (confirms Plan 02 code sweep).

**Write `155-VERIFICATION.md`** with the following structure:

```markdown
---
phase: 155-kb-cleanup-final
status: verified
verified_at: <ISO-timestamp>
requirements_covered: [KB-28, KB-29, KB-30, KB-31, KB-32, KB-33, KB-34, KB-35, KB-36, KB-37, KB-38, KB-39]
---

# Phase 155 — Verification Evidence

## Summary

Phase 155 completa. Legacy knowledge layers borrados, KB es única fuente canónica, CatBot consume solo KB, rollback plan documentado.

## KB-28/29: canvas-rules.ts migration + SE/DA atoms

<!-- Paste: output of `cd app && npx vitest run src/lib/__tests__/canvas-rules.test.ts --reporter=verbose` tail -20 -->
<!-- Paste: `ls .docflow-kb/rules/{SE,DA}*.md` (7 paths) -->
<!-- Paste: `node scripts/validate-kb.cjs | tail -3` -->

## KB-30/31/32: Deletion + code sweep + Docker

### Filesystem invariants (all PASS)
<!-- Paste: output of the `test !` one-liner from Plan 02 Task 3 -->

### Grep invariants (expect 0 hits)
<!-- Paste: output of the expanded grep from Plan 02 Task 3 -->

### Docker build log
<!-- Paste: last 20 lines of `docker compose build docflow` from Plan 03 Step A -->

### vitest suite summary
<!-- Paste: `npx vitest run --reporter=dot | tail -20` — expect only 10 orthogonal reds from task-scheduler + alias-routing + catbot-holded-tools -->

## KB-33: CLAUDE.md simplification

<!-- Paste: `wc -l CLAUDE.md` (expect ≤55) -->
<!-- Paste: `! grep -i "knowledge tree" CLAUDE.md` + echo PASS -->
<!-- Paste: full CLAUDE.md content (slim after edit) -->

## KB-34/35: R26-R29 + taxonomy

<!-- Paste: `ls .docflow-kb/rules/R2[6789]*.md` (4 paths) -->
<!-- Paste: `node -e "const t = require('.../tag-taxonomy.json'); console.log(...)"` -->
<!-- Paste: `node scripts/validate-kb.cjs | tail -3` -->

## KB-36: Live-DB backfill

### First pass (writes)
<!-- Paste: tail -30 of `kb-sync.cjs --full-rebuild --source db` from Plan 03 Step B -->

### Second pass (idempotence)
<!-- Paste: tail -30 of second run — expect "0 writes" / "0 bumps" -->

### validate-kb.cjs post-backfill
<!-- Paste: exit 0 -->

## KB-37: _manual.md rollback + Phase 155 section

<!-- Paste: head -50 of `.docflow-kb/_manual.md` (Rollback section verbatim) -->
<!-- Paste: `grep "Phase 155 Cleanup" .docflow-kb/_manual.md` line + ~20 surrounding lines -->

## KB-38: CatBot Oracle Evidence (3 prompts)

### Prompt 1: List CatPaws + kb_entry
**Query:** "Lista los CatPaws activos y dime el kb_entry del primero. ¿Qué devuelve?"
**Response:**
<!-- Paste verbatim /tmp/oracle-155-prompt-1.json reply + tool_calls -->
**Signals verified:**
- [x] tool_calls includes `list_cat_paws`
- [x] tool_calls does NOT include `query_knowledge`
- [x] kb_entry for Operador Holded is non-null: `resources/catpaws/...`
- [x] No "knowledge tree" phrase in reply

### Prompt 2: R26 citation
**Query:** "¿Puedo editar canvas-executor.ts? ¿Por qué?"
**Response:**
<!-- Paste verbatim /tmp/oracle-155-prompt-2.json reply + tool_calls -->
**Signals verified:**
- [x] tool_calls includes `search_kb` or `get_kb_entry`
- [x] Reply cites R26 and says "no"
- [x] Reply mentions "canvas-executor.ts inmutable" or equivalent

### Prompt 3: Design rules discovery
**Query:** "¿Qué reglas de diseño canvas existen? Cuéntame algunas SE y DA."
**Response:**
<!-- Paste verbatim /tmp/oracle-155-prompt-3.json reply + tool_calls -->
**Signals verified:**
- [x] tool_calls includes `search_kb({type:"rule"})` or equivalent
- [x] Reply enumerates R-series + SE-series + DA-series
- [x] query_knowledge NOT invoked (confirms Plan 02 code sweep)

## KB-39: REQUIREMENTS.md Traceability

<!-- Paste: tail -20 of .planning/REQUIREMENTS.md §Traceability showing KB-28..KB-39 Complete + KB-01..KB-05 Complete rows -->

## Known gaps / deferred

Documented in CONTEXT §deferred, NOT part of Phase 155 close:
- 10 orthogonal red tests (task-scheduler 5, alias-routing 3, catbot-holded-tools 2) — hotfix in v29.2.
- catbrains migration column drift (23 cols but 18 values) — hotfix aparte.
- Multi-worker cache invalidation (kb-index-cache 60s TTL) — single-worker Docker is safe today.
- Nyquist backfill 149-154 VALIDATION.md — deferred to `/gsd:validate-phase` before `/gsd:complete-milestone v29.1`.

## Conclusion

Phase 155 closes milestone v29.1. The KB is now the single canonical source. CatBot consumes it exclusively. Rollback is documented. Ready for `/gsd:complete-phase 155`.
```

**Fill ALL `<!-- Paste -->` placeholders with actual command output.** If any command output is >30 lines, truncate with "... [N more lines]" marker but keep the critical signal lines (errors, success indicators).

**Temp files (`/tmp/oracle-155-prompt-*.json`) can be cleaned up after pasting, but the verbatim JSON should land in VERIFICATION.md.**
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && test -f .planning/phases/155-kb-cleanup-final/155-VERIFICATION.md && grep -c "CatBot Oracle Evidence" .planning/phases/155-kb-cleanup-final/155-VERIFICATION.md && grep -c "tool_calls" .planning/phases/155-kb-cleanup-final/155-VERIFICATION.md</automated>
  </verify>
  <done>155-VERIFICATION.md exists, includes 3 oracle responses verbatim with tool_calls traces, grep/build/backfill evidence, and conclusion. All KB-28..KB-39 requirements have paste evidence inline.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md traceability + commit prep</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update the Traceability table at the bottom of `.planning/REQUIREMENTS.md` to mark KB-28..KB-39 as `Complete` (the plan bodies above established the spec; Tasks 1-2 above produced the evidence).

**Edits:**

1. Find lines `| KB-28 | Phase 155 | Pending |` through `| KB-39 | Phase 155 | Pending |`. Change `Pending` → `Complete` on each (12 rows).
2. Find the sub-requirement lines in §"Knowledge Base Cleanup Final (Phase 155)" (top of file around lines 79-111). Change each leading `- [ ]` to `- [x]` for KB-28 through KB-39 (12 checkboxes).
3. Find the footer line `*Last updated: 2026-04-20 — Phase 155 KB-28..KB-39 registered during /gsd:plan-phase; KB-01..KB-05 Traceability rows patched.*`. Update to: `*Last updated: 2026-04-20 after Phase 155 close — KB-28..KB-39 verified via 3-prompt CatBot oracle + filesystem invariants + docker build + live-DB backfill. KB-01..KB-05 Traceability rows patched (audit v29.1 side-fix).*`
4. Update coverage line (around line ~260): change count from `v1 requirements: 51 total` / `Mapped to phases: 51/51` — if that's still accurate, leave it. Otherwise recount (should be 51 = 12 Phase 155 + 39 prior).

**Verification:**
```bash
grep -c "KB-[0-9]* | Phase 155 | Complete" /home/deskmath/docflow/.planning/REQUIREMENTS.md
# Expected: 12 (KB-28 through KB-39)

grep -c "KB-0[12345] | Phase 149 | Complete" /home/deskmath/docflow/.planning/REQUIREMENTS.md
# Expected: 5 (KB-01 through KB-05)

grep -cE "- \[x\] \*\*KB-(2[89]|3[0-9])\*\*" /home/deskmath/docflow/.planning/REQUIREMENTS.md
# Expected: 12 (all KB-28..KB-39 checkboxes ticked)
```

No other REQUIREMENTS.md edits. Do NOT touch FLOW-* or CRM-* rows.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && count_complete=$(grep -cE "KB-[0-9]+ \| Phase 155 \| Complete" .planning/REQUIREMENTS.md) ; count_ticked=$(grep -cE "- \[x\] \*\*KB-(2[89]|3[0-9])\*\*" .planning/REQUIREMENTS.md) ; echo "complete=$count_complete ticked=$count_ticked" ; test "$count_complete" -eq 12 && test "$count_ticked" -eq 12 && echo OK</automated>
  </verify>
  <done>12 Phase 155 traceability rows show Complete; 12 sub-requirement checkboxes ticked; footer updated with post-close note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (checkpoint): Human approval of Phase 155 close</name>
  <files>(human review of filesystem + running container + VERIFICATION.md)</files>
  <action>
**Human operator**: pause here and verify the phase close before the orchestrator proceeds to `/gsd:complete-phase 155`.

**What was built (context)**
Phase 155 full deliverables: legacy layers deleted, KB is single source, R26-R29 + SE01-SE03 + DA01-DA04 atoms live, rollback documented in `_manual.md`, 3 CatBot oracle prompts executed with evidence in VERIFICATION.md, REQUIREMENTS.md traceability updated.

**Before approving, manually verify 5 items:**

1. **Rollback recipes read-through (5 min):**
   - Open `.docflow-kb/_manual.md`, scroll to `## Rollback de la migración v29.1 (Phase 155)`.
   - Read the 4 recipes. Is each command actually runnable? Are file paths correct? Do the SHA placeholders make sense?
   - Acceptable: recipes are copy-paste-ready modulo SHA resolution.

2. **CatBot oracle evidence inspection:**
   - Open `.planning/phases/155-kb-cleanup-final/155-VERIFICATION.md`, scroll to `## KB-38: CatBot Oracle Evidence`.
   - Prompt 1: Does the reply include a non-null `kb_entry` path? Did tool_calls NOT include `query_knowledge`?
   - Prompt 2: Does the reply cite R26 and answer "no, no se puede editar"?
   - Prompt 3: Does the reply mention at least R01, R10, SE01, DA01 by ID?
   - Acceptable: all 3 prompts show expected signals. Unacceptable: any prompt produced `query_knowledge` in tool_calls (that would mean Plan 02 code sweep was incomplete).

3. **Docker container sanity:**
   - Run `docker ps --filter name=docflow --format "{{.Names}} {{.Status}}"` — expect `docflow-app Up ...`.
   - Visit `http://localhost:3500/` in browser or curl — expect 200 response.
   - Visit `http://localhost:3500/knowledge` — expect the Phase 154 dashboard to render with updated counts (post-backfill delta visible).

4. **Grep sanity spot-check:**
   - `grep -rn 'knowledge-tree|query_knowledge|explain_feature|loadKnowledgeArea' /home/deskmath/docflow/app/src/` — expect NO matches.
   - `ls /home/deskmath/docflow/app/data/knowledge/ 2>&1` — expect `No such file or directory`.
   - `ls /home/deskmath/docflow/.planning/knowledge/ 2>&1` — expect `No such file or directory`.

5. **REQUIREMENTS.md traceability:**
   - `grep "KB-[0-9]* | Phase 155" /home/deskmath/docflow/.planning/REQUIREMENTS.md` — expect 12 rows all marked `Complete`.

**Resume signals:**
- If all 5 items pass: type `approved` to proceed to `/gsd:complete-phase 155` + close milestone v29.1 preparation.
- If any item fails: type what failed with paths/signals. The planner / execute-plan will open a gap closure plan to fix.
  </action>
  <verify>Manual human inspection per the 5-item checklist above. No automated command — this is the UAT gate per CLAUDE.md §Protocolo de Testing.</verify>
  <done>Human operator types `approved` after verifying rollback recipes, oracle evidence, container sanity, grep invariants, and traceability table. Orchestrator unblocks to `/gsd:complete-phase 155`.</done>
  <what-built>Phase 155 full deliverables: legacy layers deleted, KB is single source, R26-R29 + SE01-SE03 + DA01-DA04 atoms live, rollback documented in _manual.md, 3 CatBot oracle prompts executed with evidence in VERIFICATION.md, REQUIREMENTS.md traceability updated.</what-built>
  <how-to-verify>Follow the 5-item checklist in the action block above.</how-to-verify>
  <resume-signal>Type `approved` or describe issues (e.g., "oracle prompt 2 reply didn't mention R26 — investigate")</resume-signal>
</task>

</tasks>

<verification>
Plan-level gate:
- Task 1: _manual.md has Rollback + Phase 155 Cleanup sections.
- Task 2: 155-VERIFICATION.md contains 3 oracle responses verbatim.
- Task 3: REQUIREMENTS.md traceability shows 12 Complete rows.
- Task 4 (checkpoint): human operator approves the rollback recipes + oracle signals + container sanity.
</verification>

<success_criteria>
- KB-37: _manual.md extended with rollback plan (4 recipes) + Phase 155 Cleanup section.
- KB-38: 3-prompt CatBot oracle executed, all 3 responses pasted verbatim into 155-VERIFICATION.md with signal checklists marked.
- KB-39: REQUIREMENTS.md Traceability shows KB-28..KB-39 Complete (12 rows); KB-01..KB-05 rows present (5 rows).
- Human checkpoint: user approves `approved` after reviewing rollback + oracle + container + grep + traceability.
- Phase 155 ready for `/gsd:complete-phase 155`.
</success_criteria>

<output>
After completion, create `.planning/phases/155-kb-cleanup-final/155-04-SUMMARY.md` with:
- _manual.md diff summary (rollback + Phase 155 Cleanup sections added, bootstrap → productivo status updated)
- 3 oracle prompts + responses summary (pass/fail per signal)
- REQUIREMENTS.md diff summary (12 Complete markers + 12 checkboxes ticked)
- Human verification result (approved / issues)
- Next step: `/gsd:complete-phase 155` (followed by `/gsd:validate-phase` per CONTEXT §deferred note, then `/gsd:complete-milestone v29.1`).
</output>
