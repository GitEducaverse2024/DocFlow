---
phase: 155-kb-cleanup-final
plan: 02
type: execute
wave: 2
depends_on: ["155-01"]
files_modified:
  - app/data/knowledge/
  - .planning/knowledge/
  - skill_orquestador_catbot_enriched.md
  - app/src/lib/knowledge-tree.ts
  - app/src/lib/__tests__/knowledge-tree.test.ts
  - app/src/lib/__tests__/knowledge-tools-sync.test.ts
  - app/src/lib/__tests__/catbot-tools-query-knowledge.test.ts
  - app/src/lib/__tests__/canvas-rules-scope.test.ts
  - app/src/app/api/catbot/knowledge/tree/route.ts
  - app/src/components/settings/catbot-knowledge/tab-knowledge-tree.tsx
  - app/src/lib/services/catbot-tools.ts
  - app/src/lib/services/catbot-prompt-assembler.ts
  - app/src/lib/services/catbot-user-profile.ts
  - app/src/lib/services/catpaw-gmail-executor.ts
  - app/src/lib/services/catpaw-drive-executor.ts
  - app/src/app/api/catbot/search-docs/route.ts
  - app/src/components/settings/catbot-knowledge/catbot-knowledge-shell.tsx
  - app/src/lib/__tests__/catbot-tools-retry-job.test.ts
  - app/src/lib/__tests__/catbot-learned.test.ts
  - app/src/lib/__tests__/canvas-tools-fixes.test.ts
  - app/src/lib/__tests__/catbot-intents.test.ts
  - app/src/lib/__tests__/intent-jobs.test.ts
  - app/src/lib/__tests__/catbot-knowledge-gap.test.ts
  - app/src/lib/__tests__/catbot-tools-user-patterns.test.ts
  - app/src/lib/__tests__/catbot-prompt-assembler.test.ts
  - app/src/lib/__tests__/catpaw-gmail-executor.test.ts
  - app/src/lib/__tests__/intent-job-executor-proposal.test.ts
  - app/docker-entrypoint.sh
  - app/Dockerfile
  - CLAUDE.md
  - .planning/Index.md
autonomous: true
requirements: [KB-30, KB-31, KB-32, KB-33]

must_haves:
  truths:
    - "Legacy knowledge layers physically deleted: app/data/knowledge/, .planning/knowledge/, skill_orquestador_catbot_enriched.md, knowledge-tree.ts"
    - "Dead tool cases (query_knowledge, explain_feature) removed from catbot-tools.ts; TypeScript build exits 0"
    - "UI tab TabKnowledgeTree deleted; catbot-knowledge-shell.tsx imports clean"
    - "Docker build (entrypoint + Dockerfile stripped of data-seed/knowledge) succeeds"
    - "CLAUDE.md simplified: Knowledge Tree section replaced with _manual.md pointer; Restricciones absolutas section replaced with rules/ pointer (R26-R29 created in Plan 03)"
    - "Grep sweep confirms 0 occurrences of loadKnowledgeArea/query_knowledge/knowledge-tree/ConceptItemSchema in app/src"
    - "vitest full suite green (excluding the pre-existing orthogonal reds listed in CONTEXT §deferred)"
  artifacts:
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "CatBot tool dispatcher without query_knowledge + explain_feature"
      contains: "search_kb"
    - path: "app/src/lib/services/catbot-prompt-assembler.ts"
      provides: "Prompt assembler without page_knowledge + query_knowledge references"
      contains: "buildKnowledgeProtocol"
    - path: "app/src/lib/services/canvas-rules.ts"
      provides: "(unchanged — already migrated in Plan 01)"
      contains: "getKbRoot"
    - path: "app/docker-entrypoint.sh"
      provides: "Minimal entrypoint, no data-seed sync"
      min_lines: 2
    - path: "CLAUDE.md"
      provides: "Simplified project instructions pointing at .docflow-kb/_manual.md"
      max_lines: 55
  key_links:
    - from: "CLAUDE.md"
      to: ".docflow-kb/_manual.md"
      via: "prose pointer + rule references"
      pattern: "\\.docflow-kb"
    - from: "app/src/lib/services/catbot-tools.ts"
      to: "app/src/lib/services/kb-index-cache.ts"
      via: "search_kb + get_kb_entry cases (unchanged — Phase 152)"
      pattern: "searchKb|getKbEntry"
    - from: "app/Dockerfile"
      to: "(removes COPY of /app/data/knowledge)"
      via: "build no longer requires app/data/knowledge/"
      pattern: "(absence of data-seed/knowledge)"
---

<objective>
Borrar físicamente los dos silos de conocimiento legacy + el archivo skill en raíz + knowledge-tree.ts + 4 tests muertos + 2 UI routes/components, barrer TODOS los consumers del código, simplificar Dockerfile + entrypoint + CLAUDE.md, y actualizar `.planning/Index.md`. Esta es la "big atomic" commit de Phase 155: todo depende de todo, no puede dividirse porque la TS compilation del container depende de que ambas caras (código y archivos) desaparezcan atómicamente.

Purpose: Eliminar la deuda técnica. Ya no hay dos layers — `.docflow-kb/` es la única fuente canónica. El research identificó 40+ edit points disjoint (Research §"Code Consumer Inventory"); todos se ejecutan en este plan. El usuario autorizó "full delete, sin tombstones" en CONTEXT §deletion-strategy.

Output: Repo working tree sin archivos legacy; TypeScript compila; vitest verde (excluidos los 10 rojos orthogonal documentados en CONTEXT §deferred); Docker build exit 0; CLAUDE.md reducido a ≤55 líneas.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/155-kb-cleanup-final/155-CONTEXT.md
@.planning/phases/155-kb-cleanup-final/155-RESEARCH.md
@.planning/phases/155-kb-cleanup-final/155-VALIDATION.md
@.planning/phases/155-kb-cleanup-final/155-01-SUMMARY.md
@CLAUDE.md
@.planning/Index.md
@app/docker-entrypoint.sh
@app/Dockerfile
@app/src/lib/services/catbot-tools.ts
@app/src/lib/services/catbot-prompt-assembler.ts
@app/src/lib/services/catbot-user-profile.ts
@app/src/app/api/catbot/search-docs/route.ts
@app/src/components/settings/catbot-knowledge/catbot-knowledge-shell.tsx

<interfaces>
<!-- Contracts that MUST still work after this plan: -->

From app/src/lib/services/kb-index-cache.ts (Phase 152, UNCHANGED):
```typescript
export function getKbIndex(): KbIndex;
export function getKbEntry(id: string): GetKbEntryResult | null;
export function searchKb(args: SearchKbArgs): SearchKbResult;
export function invalidateKbIndex(): void;
```

From app/src/lib/services/catbot-tools.ts (CatBot tool registry — Phase 152 shape):
```typescript
// TOOLS[] keeps: list_cat_paws, list_catbrains, list_skills, list_email_templates,
//   canvas_list, search_kb, get_kb_entry, log_knowledge_gap, navigate_to,
//   search_documentation (legacy docs fallback to .planning/ only), and creation/update/delete tools.
// TOOLS[] LOSES: query_knowledge, explain_feature.
```

From .docflow-kb/ (Phase 154, UNCHANGED):
```
rules/R01..R29     — R26-R29 created by Plan 03 (this plan does NOT create them)
protocols/         — includes catflow-inbound-review.md, orquestador-catflow.md
resources/         — 66+ auto-synced resources
_manual.md         — target of CLAUDE.md pointer
_header.md         — injected by catbot-prompt-assembler kb_header section
```

CLAUDE.md target shape after simplification (≤55 lines):
```markdown
# DocFlow — Project Instructions

## Protocolo de Testing: CatBot como Oráculo
[UNCHANGED — keep entire existing section lines 3-27]

## Documentación canónica

Toda la documentación de DocFlow vive en `.docflow-kb/`. Ver `.docflow-kb/_manual.md`
para nomenclatura, estructura de carpetas, semver lifecycle y flujos. CatBot consume
el KB automáticamente via `search_kb` + `get_kb_entry` (Phase 152); las creation tools
sincronizan DB↔KB automáticamente (Phase 153 hooks).

### Rutas de referencia rápida
- Proceso de revisión Inbound → `.docflow-kb/protocols/catflow-inbound-review.md`
- Contratos de nodos canvas → `.docflow-kb/rules/R01..R29` + `.docflow-kb/domain/concepts/canvas-node.md`
- Incidentes conocidos → `.docflow-kb/incidents/`
- Catálogo de CatPaws, connectors, skills, templates → `.docflow-kb/resources/`

### Restricciones absolutas

Las 4 restricciones inmutables viven como rules en el KB con tag `critical`:
`search_kb({tags:["critical"]})` → R26-R29 (canvas-executor inmutable, agentId UUID-only,
process['env'] bracket notation, Docker rebuild tras execute-catpaw.ts).
```

IMPORTANT: R26-R29 atoms do NOT exist yet — Plan 03 creates them. The CLAUDE.md pointer written in this plan is forward-referencing; post-Plan-03 merge, `search_kb({tags:["critical"]})` returns those 4 rules. Reviewers: no gap — pointer + rule creation land in the same phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Physical deletion of 23+ legacy files + test files + routes</name>
  <files>app/data/knowledge/*, .planning/knowledge/*, skill_orquestador_catbot_enriched.md, app/src/lib/knowledge-tree.ts, app/src/lib/__tests__/knowledge-tree.test.ts, app/src/lib/__tests__/knowledge-tools-sync.test.ts, app/src/lib/__tests__/catbot-tools-query-knowledge.test.ts, app/src/lib/__tests__/canvas-rules-scope.test.ts, app/src/app/api/catbot/knowledge/tree/route.ts, app/src/components/settings/catbot-knowledge/tab-knowledge-tree.tsx</files>
  <action>
Delete ALL legacy knowledge-layer files via `git rm`. Order matters: canvas-rules.ts migration from Plan 01 MUST be merged before this step so `getCanvasRule()` still works when `app/data/knowledge/canvas-*.md` disappears.

**Commands to run (at repo root `/home/deskmath/docflow`):**

```bash
cd /home/deskmath/docflow

# Silo A (Phase 151 redirected, Phase 155 deletes):
git rm -r app/data/knowledge/

# Silo B (Phase 151 redirected, Phase 155 deletes):
git rm -r .planning/knowledge/

# Silo C (root skill file):
git rm skill_orquestador_catbot_enriched.md

# TS module + its tests (subject dies):
git rm app/src/lib/knowledge-tree.ts
git rm app/src/lib/__tests__/knowledge-tree.test.ts
git rm app/src/lib/__tests__/knowledge-tools-sync.test.ts
git rm app/src/lib/__tests__/catbot-tools-query-knowledge.test.ts

# canvas-rules-scope.test.ts — subject canvas-rules-index.md disappears:
git rm app/src/lib/__tests__/canvas-rules-scope.test.ts

# UI consumer + API route:
git rm -r app/src/app/api/catbot/knowledge/tree/
git rm app/src/components/settings/catbot-knowledge/tab-knowledge-tree.tsx
```

**Expected final count:**
- `app/data/knowledge/` → dir gone (11 files)
- `.planning/knowledge/` → dir gone (12 files)
- Root: `skill_orquestador_catbot_enriched.md` gone
- `app/src/lib/`: `knowledge-tree.ts` gone
- `app/src/lib/__tests__/`: 4 test files gone
- `app/src/app/api/catbot/knowledge/tree/`: directory gone
- `app/src/components/settings/catbot-knowledge/`: `tab-knowledge-tree.tsx` gone (shell.tsx stays — edited in Task 2)

**Do NOT run git commit here** — deletion + consumer sweep are a single atomic commit owned by execute-plan. Tests and build will be red in between; that's expected.

**Scope note:** canvas-rules-scope.test.ts had scope-annotation assertions on R10 `[scope: transformer,synthesizer]`. Those annotations live in Plan 01's `SCOPE_ANNOTATIONS` constant inside canvas-rules.ts (hard-coded from the original canvas-rules-index.md source). No assertions lost — moved to Plan 01 Task 2 tests.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && test ! -d app/data/knowledge && test ! -d .planning/knowledge && test ! -f skill_orquestador_catbot_enriched.md && test ! -f app/src/lib/knowledge-tree.ts && test ! -f app/src/lib/__tests__/knowledge-tree.test.ts && test ! -f app/src/lib/__tests__/knowledge-tools-sync.test.ts && test ! -f app/src/lib/__tests__/catbot-tools-query-knowledge.test.ts && test ! -f app/src/lib/__tests__/canvas-rules-scope.test.ts && test ! -d app/src/app/api/catbot/knowledge/tree && test ! -f app/src/components/settings/catbot-knowledge/tab-knowledge-tree.tsx && echo OK</automated>
  </verify>
  <done>All 9 paths above are gone. `git status` shows 23+ deletions staged.</done>
</task>

<task type="auto">
  <name>Task 2: Code sweep — remove consumers from runtime + tests + UI shell + Docker</name>
  <files>app/src/lib/services/catbot-tools.ts, app/src/lib/services/catbot-prompt-assembler.ts, app/src/lib/services/catbot-user-profile.ts, app/src/lib/services/catpaw-gmail-executor.ts, app/src/lib/services/catpaw-drive-executor.ts, app/src/app/api/catbot/search-docs/route.ts, app/src/components/settings/catbot-knowledge/catbot-knowledge-shell.tsx, app/src/lib/__tests__/catbot-tools-retry-job.test.ts, app/src/lib/__tests__/catbot-learned.test.ts, app/src/lib/__tests__/canvas-tools-fixes.test.ts, app/src/lib/__tests__/catbot-intents.test.ts, app/src/lib/__tests__/intent-jobs.test.ts, app/src/lib/__tests__/catbot-knowledge-gap.test.ts, app/src/lib/__tests__/catbot-tools-user-patterns.test.ts, app/src/lib/__tests__/catbot-prompt-assembler.test.ts, app/src/lib/__tests__/catpaw-gmail-executor.test.ts, app/src/lib/__tests__/intent-job-executor-proposal.test.ts, app/docker-entrypoint.sh, app/Dockerfile</files>
  <action>
Remove ALL remaining code consumers of the legacy layer so TypeScript compilation + vitest + docker build all succeed after Task 1 deletions. Method: **grep-driven surgical edits** — read Research §"Code Consumer Inventory" for the complete file:line table (40+ edit points).

**Step A — catbot-tools.ts (dispatcher):**
1. L9: Remove `import { loadKnowledgeArea, getAllKnowledgeAreas, type KnowledgeEntry } from '@/lib/knowledge-tree';`
2. L210-222: Remove entire TOOLS[] entry for `explain_feature`.
3. L223-236: Remove entire TOOLS[] entry for `query_knowledge`.
4. L241 search_kb description: Edit — remove the substring "Si no encuentras resultados, cae a query_knowledge (legacy) como fallback." Replace with "Si 0 resultados, llama `log_knowledge_gap`."
5. L1020 log_knowledge_gap description: Edit — change "Usa esto cuando query_knowledge devuelve 0 resultados" → "Usa esto cuando search_kb + get_kb_entry no resolvieron la pregunta."
6. L1355-1387: Remove `mapConceptItem` helper entirely.
7. L1389-1403: Remove `scoreKnowledgeMatch` helper.
8. L1405-1429: Remove `formatKnowledgeResult` helper.
9. L1431-1461: Remove `formatKnowledgeAsText` helper.
10. L1492 always-allowed list: Edit — drop `'explain_feature'` and `'query_knowledge'` from the conditional (keep `'navigate_to'` + everything else). After edit the line reads `if (name === 'navigate_to'` + whatever else stays.
11. L1842-1867: Remove entire `case 'explain_feature':` block.
12. L1869-1962: Remove entire `case 'query_knowledge':` block.

**Step B — catbot-prompt-assembler.ts:**
1. L11: Remove `import { loadKnowledgeIndex, loadKnowledgeArea, type KnowledgeEntry } from '@/lib/knowledge-tree';`
2. L69-80: Remove `PAGE_TO_AREA` constant map.
3. L166-208: Remove `renderConceptItem` + `formatKnowledgeForPrompt` helpers.
4. L210-233: Remove `getPageKnowledge(page)` function.
5. Find and remove the caller of `getPageKnowledge(page)` that builds section `page_knowledge` in the assembled prompt. That entire section's emission is removed — `kb_header` (Phase 152, injected at P1) replaces it.
6. L286: Edit string — remove "usa explain_feature" mention. Replace with "usa search_kb + get_kb_entry".
7. L648: Edit string — remove "query_knowledge" reference. Just keep the search_kb mention.
8. L675-695 (`buildKnowledgeProtocol`): Rewrite to drop query_knowledge + search_documentation bullets. Keep only:
   ```
   1. search_kb({type?, subtype?, tags?, audience?, status?, search?, limit?}) — PRIMARY
   2. get_kb_entry({id}) — detail
   3. search_documentation({query}) — LEGACY fallback para .planning/*.md (PROJECT/STATE/ROADMAP)
   4. log_knowledge_gap — ABSOLUTA. Si search_kb + get_kb_entry + search_documentation devolvieron 0 resultados, DEBES llamarla antes de responder.
   ```
   (search_documentation stays because it still serves PROJECT/STATE/ROADMAP lookups — see Step E).

**Step C — catbot-user-profile.ts:**
1. L70-71: Remove the 2 lines tracking `explainCount = toolNames.filter(n => n === 'explain_feature').length`. Drop any downstream profile field that used this count (check same file for "learning user" heuristic — simplify to `search_kb` count if practical, else drop the metric entirely; explain in SUMMARY).

**Step D — catpaw-*-executor.ts:**
1. `catpaw-gmail-executor.ts` L37: Edit comment reference `.planning/knowledge/connector-logs-redaction-policy.md` → `.docflow-kb/protocols/connector-logs-redaction.md`.
2. `catpaw-drive-executor.ts` L37: Same edit.

**Step E — search-docs route:**
1. `app/src/app/api/catbot/search-docs/route.ts` L29, L42: Edit `DOC_PATHS` + `LOCAL_DOC_PATHS` — remove the entries pointing at `.planning/knowledge` (that dir is gone). Keep `.planning/*.md` at top-level (PROJECT.md, STATE.md, ROADMAP.md) and remove any `.planning/knowledge/` subpath. If the resulting list is empty or degenerate, document in SUMMARY why search_documentation still has value (PROJECT/STATE/ROADMAP), and consider if the whole route + tool can be removed (decision: KEEP the route — it still covers `.planning/*.md` top-level). Do NOT delete the route.

**Step F — UI shell:**
1. `catbot-knowledge-shell.tsx`:
   - L8 (or wherever): Remove `import TabKnowledgeTree from './tab-knowledge-tree';` (the component file was deleted in Task 1).
   - L14: Remove the TABS entry for knowledge-tree (the object `{ key: 'tree', label: ..., icon: ... }` or similar).
   - L70: Remove the render `case 'tree': return <TabKnowledgeTree ... />;` block.
   - If the only remaining tab makes the tab UI a no-op, consider simplifying; document the choice in SUMMARY. Do NOT delete the shell file — other tabs (KB browser via link to /knowledge) stay.

**Step G — Tests (remove dead `vi.mock` + fixture reads):**
1. `catbot-tools-retry-job.test.ts` L63-65: Remove `vi.mock('@/lib/knowledge-tree', ...)` block.
2. `catbot-learned.test.ts` L38-40: Same remove.
3. `canvas-tools-fixes.test.ts` L156-158: Same.
4. `catbot-intents.test.ts` L36-38: Same.
5. `intent-jobs.test.ts` L51-53: Same.
6. `catbot-knowledge-gap.test.ts` L53-55: Same.
7. `catbot-tools-user-patterns.test.ts` L57-59: Same.
8. `catbot-prompt-assembler.test.ts`: Rewrite KPROTO-01 + KPROTO-05 assertions. Tests that assert substring `'query_knowledge'` or `'consulta query_knowledge'` → change to assert `'search_kb'` + `'get_kb_entry'` + `'log_knowledge_gap'` appear in the protocol text. Also find assertions on `'explain_feature'` → remove.
9. `catpaw-gmail-executor.test.ts` L212-229: Remove fixture reads from `app/data/knowledge/canvas.json`, `catflow.json`, `connector-logs-redaction-policy.md`. The underlying behavior probably doesn't need them post-KB migration; if a test genuinely needs connector-logs-redaction content, read it from `.docflow-kb/protocols/connector-logs-redaction.md` instead. If the test becomes trivial after removing fixtures, simplify or delete the test block entirely (document in SUMMARY).
10. `intent-job-executor-proposal.test.ts` L263: Remove the literal `'app/data/knowledge/catboard.json'` (the test should use `.docflow-kb` fixture or skip that assertion).

**Step H — Dockerfile + entrypoint:**
1. `app/docker-entrypoint.sh`: Reduce to 2 lines:
   ```sh
   #!/bin/sh
   exec node server.js
   ```
   (Delete the `if [ -d /app/data-seed/knowledge ]` block entirely — lines 3-9.)
2. `app/Dockerfile` L55-56: Remove the comment line `# Copy knowledge tree JSON files (seed data for CatBot intelligence)` AND the `COPY --from=builder --chown=nextjs:nodejs /app/data/knowledge ./data-seed/knowledge` line. The builder stage does NOT have `/app/data/knowledge` after Task 1 merges (it's not copied in).

**Step I — CLAUDE.md simplification:**
1. Replace §"Protocolo de Documentación: Knowledge Tree + CatBot" (lines 29-63 ≈ 35 lines) with the "## Documentación canónica" section shown in the `<interfaces>` block above (≈ 22 lines — slim pointer).
2. Replace §"Documentación de referencia" (L64-76) — repuntar rutas a `.docflow-kb/` per the `<interfaces>` block.
3. Replace §"Restricciones absolutas" (L77-81) — 1-paragraph pointer mentioning `search_kb({tags:["critical"]})` → R26-R29 (which Plan 03 creates next).
4. Keep §"Protocolo de Testing: CatBot como Oráculo" (L3-27) BYTE-IDENTICAL.
5. Target file length post-edit: ≤55 lines (vs 80 pre-edit). Verify with `wc -l CLAUDE.md`.

**Step J — .planning/Index.md:**
1. Delete §"Catalogos de Conocimiento (knowledge/)" (around lines 42-59). Whole section gone — the directory doesn't exist.
2. Find section "Knowledge Base (en construcción)" (around L9-13) — simplify: drop "(en construcción)" + "Bootstrap completado…". Replace with a 2-line pointer: "Toda la documentación vive en `.docflow-kb/`. Ver `.docflow-kb/_manual.md`."

**Anti-patterns to avoid (Research §Anti-Patterns):**
- DO NOT leave "// removed: query_knowledge" tombstone comments — just delete cleanly (per MEMORY §feedback_no_tombstone_comments).
- DO NOT leave unused imports — ESLint's `no-unused-vars` is error in `next build` (per MEMORY §feedback_unused_imports_build).
- DO NOT skip `search-docs/route.ts` edit — `.planning/knowledge/` dir no longer exists, route would 500 if not trimmed.
- DO NOT delete `search-docs` route wholesale — it still covers PROJECT/STATE/ROADMAP lookups.

**When everything's edited, run the verification commands.** If the build is red (TS errors, unused vars, missing imports), fix in-place, re-run, repeat until green. Docker build is the final gate — if it fails, trace the error to the specific file:line and fix.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && ! grep -rn 'loadKnowledgeArea\|getAllKnowledgeAreas\|getKnowledgeAreaById\|loadKnowledgeIndex\|KnowledgeEntrySchema\|ConceptItemSchema\|stringifyConceptItem\|mapConceptItem\|renderConceptItem\|formatKnowledgeForPrompt\|formatKnowledgeAsText\|formatKnowledgeResult\|scoreKnowledgeMatch\|getPageKnowledge\|PAGE_TO_AREA\|TabKnowledgeTree' app/src/ 2>&1 ; cd /home/deskmath/docflow/app && npx vitest run --reporter=dot 2>&1 | tail -40</automated>
  </verify>
  <done>Grep sweep returns 0 hits in app/src/ for the banned symbols. `npx vitest run` completes with only the pre-existing orthogonal red tests failing (task-scheduler, alias-routing, catbot-holded-tools — documented in CONTEXT §deferred). NO new test failures from Plan 02 edits.</done>
</task>

<task type="auto">
  <name>Task 3: Docker rebuild gate + CLAUDE.md length check + grep invariants</name>
  <files>(verification only — no file writes)</files>
  <action>
Final verification gate for Plan 02. This is the "green light" that unblocks Wave 3 (Plan 03 creates R26-R29 + does live backfill).

**Commands (in order, all must pass):**

1. **TypeScript / Next.js build inside Docker:**
   ```bash
   cd /home/deskmath/docflow && docker compose build docflow 2>&1 | tail -20
   ```
   Expect exit 0. This gates: `knowledge-tree.ts` import removal didn't miss any consumer; `Dockerfile` no longer copies `/app/data/knowledge`; unused imports were all cleaned (ESLint no-unused-vars is error-level in `next build`).

2. **vitest full suite (with tolerance for known orthogonal reds):**
   ```bash
   cd /home/deskmath/docflow/app && npx vitest run --reporter=dot 2>&1 | tail -30
   ```
   Expected behavior: ONLY the pre-existing reds from `task-scheduler.test.ts` (5), `alias-routing.test.ts` (3), `catbot-holded-tools.test.ts` (2) should fail. These are documented in CONTEXT §deferred and Research §Pitfall 7. Any NEW failure (knowledge-related, canvas-rules-related, assembler-related) = BLOCKER — fix before proceeding.

3. **CLAUDE.md length check:**
   ```bash
   wc -l /home/deskmath/docflow/CLAUDE.md
   ```
   Expect `≤55` lines. If >55, re-trim §Documentación (most common overflow).

4. **CLAUDE.md content invariants:**
   ```bash
   ! grep -i "knowledge tree" /home/deskmath/docflow/CLAUDE.md && echo "PASS: no 'knowledge tree' in CLAUDE.md"
   grep -c "\.docflow-kb" /home/deskmath/docflow/CLAUDE.md
   ```
   Expect: zero "knowledge tree" matches; ≥3 `.docflow-kb` mentions (Documentación section + Restricciones pointer + reference paths).

5. **Grep invariant (expanded from Research §Pattern 1 baseline):**
   ```bash
   cd /home/deskmath/docflow && grep -rn 'app/data/knowledge\|\.planning/knowledge\|knowledge-tree\|query_knowledge\|loadKnowledgeArea\|getAllKnowledgeAreas\|getKnowledgeAreaById\|stringifyConceptItem\|mapConceptItem\|renderConceptItem\|ConceptItemSchema\|KnowledgeEntrySchema\|skill_orquestador_catbot_enriched\|TabKnowledgeTree\|data-seed/knowledge\|explain_feature' app/ scripts/ CLAUDE.md docker-compose.yml 2>&1 | head -20
   ```
   Expected result: EMPTY or only git-history-comment strings. NOT expected: any import statement, any function call, any type reference.

6. **Filesystem verification (echoing Task 1):**
   ```bash
   test ! -d app/data/knowledge && test ! -d .planning/knowledge && test ! -f skill_orquestador_catbot_enriched.md && test ! -f app/src/lib/knowledge-tree.ts && echo "PASS: all legacy paths absent"
   ```

**If Docker build fails:**
- Read the error. Most common: unused import of a now-deleted symbol → remove it. Missing file path in Dockerfile → verify Step H Dockerfile edit. TS error in a consumer file → trace back to a missed sweep point in Task 2.
- Do NOT bypass by adding `// @ts-ignore` — fix the root cause.

**If vitest adds new reds:**
- Read the failure. Most common (Research §Pitfall 6): `catbot-prompt-assembler.test.ts` still asserts old query_knowledge strings → revisit Step G Task 2 rewrite.
- Do NOT mark tests `.skip()` — CONTEXT §test-cleanup explicitly says "no skip, no TODO Phase 156".

**Do NOT commit** — execute-plan workflow commits the whole atomic block.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && docker compose build docflow 2>&1 | tail -10 ; cd /home/deskmath/docflow/app && npx vitest run --reporter=dot 2>&1 | tail -10 ; wc -l /home/deskmath/docflow/CLAUDE.md ; ! grep -i "knowledge tree" /home/deskmath/docflow/CLAUDE.md && echo 'no-knowledge-tree-string: PASS'</automated>
  </verify>
  <done>Docker build exit 0; vitest only shows the 10 pre-existing orthogonal reds; CLAUDE.md ≤55 lines with no "knowledge tree" mentions; grep sweep invariant green; filesystem checks green.</done>
</task>

</tasks>

<verification>
Plan-level gate: all three task automated checks green. Specifically:
- Task 1: 23+ files deleted, directories gone.
- Task 2: Code compiles, tests green (except 10 orthogonal reds), no banned symbols in app/src.
- Task 3: Docker build green, CLAUDE.md ≤55 lines, grep sweep empty.

**CatBot Oracle:** Deferred to Plan 04 (post-Wave-3 Docker rebuild + backfill). Plan 02 by itself doesn't produce user-observable behavior change beyond CLAUDE.md shrinking.

**No R26-R29 yet:** CLAUDE.md references `search_kb({tags:["critical"]})` which returns 0 results after this plan merges. Plan 03 creates R26-R29 immediately after and Docker rebuild makes them discoverable. The gap is ~1 plan boundary — acceptable within the phase.
</verification>

<success_criteria>
- KB-30: All 23+ legacy files physically deleted; git status shows deletions staged; filesystem checks green.
- KB-31: Code sweep complete; grep over `app/src/` finds 0 occurrences of loadKnowledgeArea, getAllKnowledgeAreas, knowledge-tree, query_knowledge, explain_feature, ConceptItemSchema, KnowledgeEntrySchema, TabKnowledgeTree, mapConceptItem, renderConceptItem, stringifyConceptItem.
- KB-32: app/docker-entrypoint.sh is 2 lines (shebang + exec); app/Dockerfile has no COPY of /app/data/knowledge; `docker compose build docflow` exit 0.
- KB-33: CLAUDE.md simplified to ≤55 lines; no "knowledge tree" string present; ≥3 `.docflow-kb` references; §Protocolo de Testing unchanged (byte-identical with pre-155).
</success_criteria>

<output>
After completion, create `.planning/phases/155-kb-cleanup-final/155-02-SUMMARY.md` with:
- Files deleted (23+ paths enumerated)
- Files edited (40+ surgical edit points — table of file:lines)
- Decisions (explain_feature removal rationale, search-docs route kept or slimmed, user-profile heuristic replaced or dropped)
- Test outcomes (X tests green, Y tests deleted, Z pre-existing reds confirmed unchanged)
- Docker build log tail
- CLAUDE.md before/after line count
- Known gap before Plan 03: `search_kb({tags:["critical"]})` returns 0 until R26-R29 land (Plan 03)
- Next: Plan 03 creates R26-R29, extends taxonomy, Docker rebuild + backfill
</output>
