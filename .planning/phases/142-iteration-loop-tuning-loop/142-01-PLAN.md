---
phase: 142-iteration-loop-tuning-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/api/catbot/chat/route.ts
  - app/data/knowledge/catboard.json
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02

must_haves:
  truths:
    - "CatBot puede ejecutar 15 tool calls consecutivas sin escalado async prematuro"
    - "El threshold de escalado async esta en iteracion 10+, no 3+"
    - "Cada 4 iteraciones de tool-calling sin texto al usuario, CatBot recibe un system message pidiendo resumen de progreso"
    - "El contador de silencio se resetea cuando CatBot emite texto al usuario"
  artifacts:
    - path: "app/src/app/api/catbot/chat/route.ts"
      provides: "Loop tuning: MAX_TOOL_ITERATIONS=15, ESCALATION_THRESHOLD=10, reporting intermedio cada 4 iter"
      contains: "MAX_TOOL_ITERATIONS"
    - path: "app/data/knowledge/catboard.json"
      provides: "Knowledge tree actualizado con conceptos de iteration loop tuning"
      contains: "MAX_TOOL_ITERATIONS"
  key_links:
    - from: "route.ts streaming path"
      to: "MAX_TOOL_ITERATIONS constant"
      via: "for loop condition"
      pattern: "iteration < MAX_TOOL_ITERATIONS"
    - from: "route.ts non-streaming path"
      to: "ESCALATION_THRESHOLD constant"
      via: "self-check condition"
      pattern: "iteration >= ESCALATION_THRESHOLD"
    - from: "route.ts both paths"
      to: "silentToolIterations counter"
      via: "system message injection at count 4"
      pattern: "silentToolIterations >= REPORT_EVERY"
---

<objective>
Ajustar el loop de iteraciones de CatBot para permitir canvas complejos (8+ nodos, 10+ tool calls) sin escalado prematuro a async, y reportar progreso intermedio durante construcciones largas.

Purpose: El threshold actual (iter 3+) interrumpe la construccion de canvas complejos enviandolos a async innecesariamente. Subir a 15 iteraciones con escalado en 10+ permite que CatBot construya canvas de 8+ nodos en modo sincrono, y el reporting intermedio cada 4 iteraciones mantiene al usuario informado.
Output: route.ts modificado con constantes nombradas, threshold ajustado, y mecanismo de reporting intermedio. Knowledge tree actualizado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-iteration-loop-tuning-loop/142-CONTEXT.md

@app/src/app/api/catbot/chat/route.ts
</context>

<interfaces>
<!-- Key code locations in route.ts that will be modified -->

Line 170 — Current maxIterations:
```typescript
const maxIterations = 8;
```

Line 184 — Streaming loop:
```typescript
for (let iteration = 0; iteration < maxIterations; iteration++) {
  let iterationContent = '';
```

Line 336 — Streaming self-check escalation:
```typescript
if (iteration >= 3 && pendingToolCalls.length > 0 && !isDebuggingRequest(lastUserMessage)) {
```

Line 362 — Streaming escalation message:
```typescript
const escalationMsg = `Esta tarea ha resultado mas compleja de lo esperado. La he encolado como CatFlow asincrono (job ${escalatedJobId}). Te avisare con reportes cada 60 segundos.`;
```

Line 440 — Non-streaming loop:
```typescript
for (let iteration = 0; iteration < maxIterations; iteration++) {
```

Lines 603-604 — Non-streaming self-check escalation:
```typescript
if (
  iteration >= 3 &&
  assistantMessage.tool_calls &&
```

Line 633 — Non-streaming escalation message:
```typescript
finalReply = `Esta tarea ha resultado mas compleja de lo esperado...`;
```

Text detection in streaming path: `iterationContent` (line 185) accumulates tokens per iteration.
Text detection in non-streaming path: `assistantMessage.content` holds the LLM response content.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract constants and raise thresholds (LOOP-01)</name>
  <files>app/src/app/api/catbot/chat/route.ts</files>
  <action>
  En `route.ts`, ANTES de la linea `export const dynamic = 'force-dynamic'` (cerca de linea 19), insertar constantes nombradas:

  ```typescript
  // ─── Phase 142: Iteration loop tuning ───
  const MAX_TOOL_ITERATIONS = 15;
  const ESCALATION_THRESHOLD = 10;
  const REPORT_EVERY_N_SILENT = 4;
  ```

  Cambios en el codigo existente:

  1. **Eliminar** la linea `const maxIterations = 8;` (~linea 170)
  2. **Streaming path** (~linea 184): cambiar `iteration < maxIterations` a `iteration < MAX_TOOL_ITERATIONS`
  3. **Non-streaming path** (~linea 440): cambiar `iteration < maxIterations` a `iteration < MAX_TOOL_ITERATIONS`
  4. **Streaming self-check** (~linea 336): cambiar `iteration >= 3` a `iteration >= ESCALATION_THRESHOLD`
  5. **Non-streaming self-check** (~linea 604): cambiar `iteration >= 3` a `iteration >= ESCALATION_THRESHOLD`
  6. **Ambos mensajes de escalacion** (~lineas 362 y 633): actualizar el texto a:
     `Tras ${iteration + 1} pasos esta tarea necesita ejecucion asincrona. La he encolado como CatFlow (job ${escalatedJobId}). Te avisare con reportes cada 60 segundos.`
  7. **Actualizar el comentario** de linea 169 (`// Tool-calling loop (max 8 iterations...`) para reflejar el nuevo max de 15.

  NO tocar ninguna otra logica del loop. NO cambiar el mecanismo de createIntentJob ni complexity gate.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && grep -n "MAX_TOOL_ITERATIONS\|ESCALATION_THRESHOLD\|REPORT_EVERY" src/app/api/catbot/chat/route.ts | head -20 && echo "---" && grep -c "maxIterations" src/app/api/catbot/chat/route.ts | xargs -I{} test {} -eq 0 && echo "OK: old maxIterations removed" || echo "FAIL: old maxIterations still present" && echo "---" && grep -c "iteration >= 3" src/app/api/catbot/chat/route.ts | xargs -I{} test {} -eq 0 && echo "OK: old threshold removed" || echo "FAIL: old threshold still present"</automated>
  </verify>
  <done>
  - Las 3 constantes (MAX_TOOL_ITERATIONS=15, ESCALATION_THRESHOLD=10, REPORT_EVERY_N_SILENT=4) existen al inicio del archivo
  - No queda ninguna referencia a `maxIterations` ni a `iteration >= 3`
  - Ambos paths (streaming y non-streaming) usan las nuevas constantes
  - Los mensajes de escalacion estan actualizados
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement intermediate reporting mechanism (LOOP-02) + knowledge tree</name>
  <files>app/src/app/api/catbot/chat/route.ts, app/data/knowledge/catboard.json</files>
  <action>
  **Reporting intermedio en route.ts:**

  En AMBOS paths (streaming y non-streaming), implementar un contador de iteraciones consecutivas sin texto al usuario:

  **Streaming path:**
  1. Antes del for-loop (~linea 184), declarar: `let silentToolIterations = 0;`
  2. Despues de ejecutar los tool calls y ANTES del self-check escalation block, anadir la logica de reporting:
     - Si `iterationContent.trim() === ''` (CatBot no emitio texto en esta iteracion, solo tool calls): incrementar `silentToolIterations`
     - Si `iterationContent.trim() !== ''`: resetear `silentToolIterations = 0`
     - Si `silentToolIterations >= REPORT_EVERY_N_SILENT` y hay `pendingToolCalls.length > 0` (va a seguir iterando):
       - Inyectar system message a `llmMessages`: `{ role: 'system', content: 'Llevas ' + silentToolIterations + ' tool calls consecutivas sin informar al usuario. Resume brevemente que has hecho y que queda antes de continuar.' }`
       - `logger.info('catbot', 'Injecting progress report prompt (streaming)', { iteration, silentToolIterations });`
       - Resetear `silentToolIterations = 0`

  **Non-streaming path:**
  1. Antes del for-loop (~linea 440), declarar: `let silentToolIterations = 0;`
  2. Despues de ejecutar los tool calls y ANTES del self-check escalation block (~linea 596), anadir logica identica:
     - Si `!assistantMessage.content || assistantMessage.content.trim() === ''`: incrementar `silentToolIterations`
     - Si `assistantMessage.content && assistantMessage.content.trim() !== ''`: resetear `silentToolIterations = 0`
     - Si `silentToolIterations >= REPORT_EVERY_N_SILENT` y `assistantMessage.tool_calls?.length > 0`:
       - Inyectar system message a `llmMessages`: mismo texto
       - `logger.info('catbot', 'Injecting progress report prompt', { iteration, silentToolIterations });`
       - Resetear `silentToolIterations = 0`

  **IMPORTANTE:** La logica de reporting intermedio va ANTES del bloque de self-check escalation, para que CatBot tenga oportunidad de reportar antes de ser escalado.

  **Knowledge tree (catboard.json):**
  Leer `app/data/knowledge/catboard.json` y anadir en el array `concepts` (o crear si no existe):
  - `{ "term": "MAX_TOOL_ITERATIONS", "definition": "Limite maximo de iteraciones del loop de tool-calling de CatBot (15). Permite canvas complejos de 8+ nodos." }`
  - `{ "term": "ESCALATION_THRESHOLD", "definition": "Iteracion a partir de la cual CatBot escala a async (10+). Antes era 3+." }`
  - `{ "term": "Reporting intermedio", "definition": "Cada 4 iteraciones de tool-calling sin texto al usuario, CatBot recibe un system prompt pidiendo resumen de progreso." }`

  Anadir en `common_errors` (o crear si no existe):
  - `{ "error": "CatBot escala a async demasiado pronto", "cause": "ESCALATION_THRESHOLD muy bajo", "solution": "Actualmente en 10. Si canvas complejos siguen escalando, considerar subir." }`
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && grep -n "silentToolIterations" src/app/api/catbot/chat/route.ts | head -20 && echo "---" && grep -c "silentToolIterations" src/app/api/catbot/chat/route.ts && echo "---" && grep -c "Injecting progress report" src/app/api/catbot/chat/route.ts && echo "---" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
  - `silentToolIterations` declarado y usado en ambos paths (streaming y non-streaming)
  - System message de reporting inyectado cuando silentToolIterations >= 4
  - Contador se resetea cuando CatBot emite texto o despues de inyectar el prompt
  - Logger.info registra cada inyeccion de reporting
  - Knowledge tree catboard.json actualizado con conceptos y common_errors
  - `npm run build` compila sin errores
  </done>
</task>

</tasks>

<verification>
1. `grep -c "MAX_TOOL_ITERATIONS\|ESCALATION_THRESHOLD\|REPORT_EVERY_N_SILENT" app/src/app/api/catbot/chat/route.ts` debe mostrar al menos 6 (3 definiciones + 3+ usos)
2. `grep -c "maxIterations" app/src/app/api/catbot/chat/route.ts` debe ser 0
3. `grep -c "iteration >= 3" app/src/app/api/catbot/chat/route.ts` debe ser 0
4. `grep -c "silentToolIterations" app/src/app/api/catbot/chat/route.ts` debe ser >= 8 (declaracion + checks + resets en ambos paths)
5. `cd app && npm run build` compila sin errores
</verification>

<success_criteria>
- CatBot puede ejecutar hasta 15 iteraciones de tool-calling antes de que el loop termine
- El escalado a async no ocurre hasta la iteracion 10+ (antes era 3+)
- Cada 4 iteraciones consecutivas sin texto al usuario, se inyecta un system message pidiendo resumen de progreso
- El build de Next.js compila sin errores
- Knowledge tree actualizado con la nueva terminologia
</success_criteria>

<output>
After completion, create `.planning/phases/142-iteration-loop-tuning-loop/142-01-SUMMARY.md`
</output>
