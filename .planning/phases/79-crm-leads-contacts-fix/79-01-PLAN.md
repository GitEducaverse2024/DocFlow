---
phase: 79-crm-leads-contacts-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/holded-mcp/src/__tests__/leads.test.ts
  - ~/holded-mcp/src/__tests__/contact-search.test.ts
autonomous: true
requirements: [CFIX-01, CFIX-02, CFIX-03, CFIX-04, CFIX-05]

must_haves:
  truths:
    - "holded_create_lead_note sends { title, desc } to API, not { text }"
    - "holded_create_lead_note Zod schema requires title (string.min(1)) and has optional desc"
    - "holded_create_lead passes stageId value directly to API without transformation"
    - "holded_search_contact fetches all contacts via GET /contacts and filters client-side"
    - "Unit tests explicitly verify note field mapping, stageId passthrough, and client-side search"
  artifacts:
    - path: "~/holded-mcp/src/__tests__/leads.test.ts"
      provides: "Tests for note field mapping and stageId passthrough"
      contains: "should pass stageId directly to API"
    - path: "~/holded-mcp/src/__tests__/contact-search.test.ts"
      provides: "Tests for client-side contact filtering"
      contains: "client-side filtering"
  key_links:
    - from: "~/holded-mcp/src/tools/leads.ts"
      to: "~/holded-mcp/src/validation.ts"
      via: "createLeadNoteSchema with title+desc"
      pattern: "createLeadNoteSchema"
    - from: "~/holded-mcp/src/tools/leads.ts"
      to: "Holded API /leads"
      via: "spread args including stageId into POST body"
      pattern: "client\\.post.*leads.*body"
    - from: "~/holded-mcp/src/tools/contact-search.ts"
      to: "Holded API /contacts"
      via: "fetch all then filter client-side"
      pattern: "client\\.get.*contacts.*filter"
---

<objective>
Verify CRM lead note fields, stageId passthrough, and client-side contact search are implemented correctly, and add targeted unit tests for any coverage gaps.

Purpose: The code already implements CFIX-01 through CFIX-04 correctly (verified by code inspection). This plan adds explicit tests that document the correct behavior as regression guards, specifically: (1) stageId passthrough on holded_create_lead, (2) explicit assertion that note body contains title/desc and NOT text, and (3) explicit assertion that contact search fetches all contacts before filtering.

Output: Updated test files with full coverage for all 5 CFIX requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

Source files (read-only, already correct):
@~/holded-mcp/src/tools/leads.ts
@~/holded-mcp/src/tools/contact-search.ts
@~/holded-mcp/src/validation.ts

<interfaces>
<!-- Current implementation in leads.ts (lines 291-316) — already sends { title, desc } -->
holded_create_lead_note handler:
```typescript
handler: withValidation(createLeadNoteSchema, async (args) => {
  const { leadId, ...body } = args;
  return client.post(`/leads/${leadId}/notes`, body, 'crm');
})
```

<!-- Current implementation in leads.ts (lines 227-231) — stageId spread into body -->
holded_create_lead handler:
```typescript
handler: withValidation(createLeadSchema, async (args) => {
  const resolvedFunnelId = await resolveFunnelId(client, args.funnelId);
  const body: Record<string, unknown> = { ...args, funnelId: resolvedFunnelId };
  return client.post('/leads', body, 'crm');
})
```

<!-- Current Zod schema in validation.ts (lines 368-372) -->
```typescript
export const createLeadNoteSchema = z.object({
  leadId: z.string().min(1),
  title: z.string().min(1),
  desc: z.string().optional(),
});
```

<!-- Current contact-search.ts (lines 46-48) — fetches all, filters client-side -->
```typescript
handler: withValidation(searchContactSchema, async (args) => {
  const allContacts = extractArray<HoldedContact>(await client.get('/contacts'));
  const q = args.query.toLowerCase();
  const filtered = allContacts.filter(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add missing test assertions for leads (note fields + stageId passthrough)</name>
  <files>~/holded-mcp/src/__tests__/leads.test.ts</files>
  <behavior>
    - Test: holded_create_lead_note sends { title, desc } and NOT { text } — assert post body has title and desc keys, does NOT have text key
    - Test: holded_create_lead_note with only title (no desc) sends { title } only
    - Test: holded_create_lead with stageId passes stageId directly in POST body without transformation (stageId value unchanged)
    - Test: holded_create_lead_note Zod rejects missing title (already covered by withValidation but explicit test)
  </behavior>
  <action>
Add the following tests to ~/holded-mcp/src/__tests__/leads.test.ts:

1. In the `holded_create_lead_note` describe block, add:
   - "should send title and desc, NOT text field": Call with { leadId: 'lead-1', title: 'Test', desc: 'Body' }, assert client.post was called with body containing { title: 'Test', desc: 'Body' }, and assert body does NOT contain a 'text' key (use `expect.not.objectContaining({ text: expect.anything() })` or check the actual call args).
   - "should send only title when desc not provided": Call with { leadId: 'lead-1', title: 'Title only' }, assert client.post body is exactly { title: 'Title only' } (no desc key, no text key).
   - "should reject when title is missing": Call with { leadId: 'lead-1', desc: 'no title' }, expect it to throw a Zod validation error.

2. In the `holded_create_lead` describe block, add:
   - "should pass stageId directly to API without transformation": Call with { funnelId: 'direct-id', name: 'Deal', stageId: 'my-stage-name' }, assert client.post body contains { stageId: 'my-stage-name' } (the exact value passed, not resolved or transformed).

Run: `cd ~/holded-mcp && npx vitest run src/__tests__/leads.test.ts`
All tests must pass (existing + new).
  </action>
  <verify>
    <automated>cd ~/holded-mcp && npx vitest run src/__tests__/leads.test.ts</automated>
  </verify>
  <done>4 new test cases added: note sends title+desc not text, note with title only, note rejects missing title, create_lead passes stageId unchanged. All existing + new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit client-side filtering test for contact search</name>
  <files>~/holded-mcp/src/__tests__/contact-search.test.ts</files>
  <behavior>
    - Test: holded_search_contact fetches ALL contacts from /contacts endpoint (no query params) and filters client-side
    - Test: holded_search_contact with name filter does NOT pass name as query param to API call
  </behavior>
  <action>
Add the following tests to ~/holded-mcp/src/__tests__/contact-search.test.ts in the `holded_search_contact` describe block:

1. "should fetch all contacts and filter client-side (no API-level name filter)": Call with { query: 'acme' }, assert:
   - client.get was called exactly once with '/contacts' and NO query parameters (verifying no name filter sent to API)
   - Use `expect(client.get).toHaveBeenCalledTimes(1)` and `expect(client.get).toHaveBeenCalledWith('/contacts')` (no second arg)
   - Result items are filtered (only Acme matches returned, not all 3 contacts)

2. "should perform case-insensitive client-side filtering by name": Call with { query: 'ACME' } (uppercase), assert it still returns matching contacts (verifying toLowerCase is applied).

Run: `cd ~/holded-mcp && npx vitest run src/__tests__/contact-search.test.ts`
All tests must pass (existing + new).
  </action>
  <verify>
    <automated>cd ~/holded-mcp && npx vitest run src/__tests__/contact-search.test.ts</automated>
  </verify>
  <done>2 new test cases added: explicit client-side filtering assertion (no API query params), case-insensitive filtering. All existing + new tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite to ensure no regressions:
```bash
cd ~/holded-mcp && npx vitest run
```
All tests pass, including the new ones that explicitly verify:
- Note field mapping (title+desc, not text)
- StageId passthrough on create_lead
- Client-side contact filtering
</verification>

<success_criteria>
- holded_create_lead_note test proves body is { title, desc } not { text }
- holded_create_lead test proves stageId passes through unchanged
- holded_search_contact test proves all contacts fetched then filtered client-side
- Full vitest suite passes with 0 failures
- New tests serve as regression guards for CFIX-01 through CFIX-05
</success_criteria>

<output>
After completion, create `.planning/phases/79-crm-leads-contacts-fix/79-01-SUMMARY.md`
</output>
