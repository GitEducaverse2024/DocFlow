---
phase: 131-complexity-assessment-catbot-razona-antes-de-ejecutar
plan: 04
type: execute
wave: 4
depends_on: ["131-03"]
files_modified:
  - app/src/lib/services/alert-service.ts
  - app/src/app/api/catbot/chat/route.ts
  - app/src/lib/__tests__/alert-service.test.ts
autonomous: false
requirements: [QA-07]
must_haves:
  truths:
    - "AlertService.checkClassificationTimeouts runs as part of the tick and inserts an alert when count > 5 in the last 24h"
    - "route.ts marks complexity_decisions.outcome='timeout' when the request fatally errors with decisionId in scope"
    - "A CatBot oracle E2E reproducing the Holded Q1 case shows: gate triggers, user question asked, queue_intent_job accepted description, pipeline progresses, progress reports arrive, final outcome recorded"
  artifacts:
    - path: "app/src/lib/services/alert-service.ts"
      provides: "checkClassificationTimeouts method registered in checks array"
      contains: "checkClassificationTimeouts"
    - path: "app/src/app/api/catbot/chat/route.ts"
      provides: "timeout outcome tracking in general catch"
      contains: "updateComplexityOutcome"
  key_links:
    - from: "AlertService.tick() checks array"
      to: "checkClassificationTimeouts → countComplexTimeoutsLast24h → insertAlert"
      via: "async method in checks list"
      pattern: "checkClassificationTimeouts"
    - from: "route.ts general catch(err)"
      to: "updateComplexityOutcome(decisionId, 'timeout')"
      via: "catch block with decisionId in scope"
      pattern: "updateComplexityOutcome.*timeout"
---

<objective>
Close the feedback loop:

1. AlertService gains a new check `checkClassificationTimeouts` that queries complexity_decisions for >5 timeouts/day in complex requests that did NOT take the async path, so the admin can iterate on the casuísticas.

2. route.ts ensures timeouts actually get recorded in complexity_decisions by calling updateComplexityOutcome in the general catch path.

3. CatBot oracle E2E reproduces the real Holded Q1 case via Telegram or web chat to prove the end-to-end flow works: gate -> user question -> user accepts -> queue_intent_job with description -> pipeline runs -> progress reports arrive -> outcome tracked.

Purpose: QA-07 needs the alert; the alert needs the timeout outcome; the oracle is the user's own test that says "the original bug no longer exists." Per CLAUDE.md testing protocol, every phase must be verifiable by CatBot using the oracle pattern.

Output: new check method + insertion into checks array + timeout outcome in route.ts catch + a manual checkpoint that captures the CatBot oracle conversation as evidence.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-complexity-assessment-catbot-razona-antes-de-ejecutar/131-CONTEXT.md
@.planning/phases/131-complexity-assessment-catbot-razona-antes-de-ejecutar/131-RESEARCH.md
@.planning/phases/131-complexity-assessment-catbot-razona-antes-de-ejecutar/131-03-SUMMARY.md
@app/src/lib/services/alert-service.ts
@app/src/app/api/catbot/chat/route.ts

<interfaces>
From Plan 01:
```typescript
export function countComplexTimeoutsLast24h(): number;
```

From Plan 02:
```typescript
// decisionId: string | null is declared at POST handler function scope in route.ts
```

Existing AlertService pattern (~line 87 in alert-service.ts):
```typescript
const checks = [
  () => this.checkIntentsUnresolved(),
  () => this.checkStuckPipelines(),
  // ... other 8-10 checks
];
// this plan appends:
// () => this.checkClassificationTimeouts(),
```

Existing insertAlert signature (used by other checks):
```typescript
this.insertAlert(category: string, alertKey: string, message: string, severity: 'info' | 'warning' | 'error'): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 extension): Failing test for checkClassificationTimeouts</name>
  <files>app/src/lib/__tests__/alert-service.test.ts</files>
  <behavior>
    Extend alert-service.test.ts with a new describe block 'checkClassificationTimeouts (Phase 131)':

    - Test 1: With 0 timeout rows → no alert inserted
    - Test 2: With 3 timeout rows (below threshold) → no alert inserted
    - Test 3: With 6 timeout rows (above threshold of 5) → insertAlert called with category='execution', alert_key='classification_timeouts', severity='warning', message contains the count
    - Test 4: Timeout rows with classification='simple' OR async_path_taken=1 OR outcome != 'timeout' are IGNORED (dedup by the DB query, not by code)
    - Test 5: Rows older than 24h are ignored
  </behavior>
  <action>
    1. Open `app/src/lib/__tests__/alert-service.test.ts`. Follow the existing tmp-DB bootstrap pattern used by other catbot.db-dependent tests (vi.hoisted env override → CATBOT_DB_PATH).

    2. beforeEach: DELETE FROM complexity_decisions, DELETE FROM alerts (or however the existing alert-service tests reset state).

    3. Helper: seed complexity_decisions rows with `saveComplexityDecision` from catbot-db, then directly update via `updateComplexityOutcome(id, 'timeout')`. For the "old row" test, directly execute `UPDATE complexity_decisions SET created_at = datetime('now','-2 day') WHERE id = ?` via `catbotDb` export (the existing named export pattern established in Phase 129).

    4. Invoke the check: `await AlertService.checkClassificationTimeouts()`. Then query the alerts table (using the project's existing alert-reading helper, whatever pattern the current alert-service tests use).

    5. All 5 tests MUST fail at this point (method does not exist). Run:
       ```
       cd ~/docflow/app && npx vitest run src/lib/__tests__/alert-service.test.ts
       ```

    DO NOT implement the method yet.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/alert-service.test.ts 2>&1 | grep -E "(FAIL|PASS|Tests)" | tail -20</automated>
  </verify>
  <done>
    - 5 new failing tests
    - Existing alert-service tests still GREEN
    - Commit: `test(131-04): add failing tests for checkClassificationTimeouts`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement checkClassificationTimeouts + register in checks array + timeout outcome in route.ts</name>
  <files>app/src/lib/services/alert-service.ts, app/src/app/api/catbot/chat/route.ts</files>
  <behavior>
    - All 5 new alert-service tests GREEN
    - route.ts general catch records outcome='timeout' when decisionId is in scope and the request fails
    - Build succeeds
  </behavior>
  <action>
    1. Edit `app/src/lib/services/alert-service.ts`:
       a. Import: `import { countComplexTimeoutsLast24h } from '@/lib/catbot-db';`
       b. Add a new static async method following the style of existing checks (e.g. checkIntentsUnresolved from Phase 129):
          ```typescript
          static async checkClassificationTimeouts(): Promise<void> {
            try {
              const count = countComplexTimeoutsLast24h();
              const THRESHOLD = 5;
              if (count > THRESHOLD) {
                this.insertAlert(
                  'execution',
                  'classification_timeouts',
                  count + ' timeouts en peticiones clasificadas como complex que NO tomaron el path async en las ultimas 24h. Revisa las casuisticas del protocolo de complejidad.',
                  'warning',
                );
              }
            } catch (err) {
              logger.warn('alert-service', 'checkClassificationTimeouts failed', { error: String(err) });
            }
          }
          ```
          Match the actual insertAlert signature used by other checks — if it takes different parameter order or fields, adapt accordingly.
       c. Append `() => this.checkClassificationTimeouts()` to the `checks` array (~line 87). This becomes the 11th check. Ensure the try/catch wrapping in the tick loop already isolates failures per check (Phase 128 decision).

    2. Edit `app/src/app/api/catbot/chat/route.ts`:
       a. Confirm `decisionId` is declared at POST handler function scope (Plan 02 should have already done this; if it's still inside the loop, hoist it to the outer scope now).
       b. In the general try/catch/finally of the POST handler (RESEARCH.md mentions ~line 488), inside the catch block, add:
          ```typescript
          if (decisionId) {
            try { updateComplexityOutcome(decisionId, 'timeout'); } catch { /* swallow */ }
          }
          ```
          This ensures any unhandled error in the chat handler — including upstream LLM timeouts, SSE abortions, and runtime exceptions — marks the decision row as a timeout, feeding the alert's data source.
       c. `updateComplexityOutcome` should already be imported by Plan 02. Verify with grep.

    3. Run tests:
       ```
       cd ~/docflow/app && npx vitest run \
         src/lib/__tests__/alert-service.test.ts \
         src/lib/__tests__/complexity-decisions.test.ts \
         src/lib/__tests__/intent-job-executor.test.ts
       ```
       All GREEN.

    4. Build: `cd ~/docflow/app && npm run build 2>&1 | tail -40`. MUST succeed.

    5. Run the full quick-suite from 131-VALIDATION.md to ensure no regressions:
       ```
       cd ~/docflow/app && npx vitest run \
         src/lib/__tests__/complexity-decisions.test.ts \
         src/lib/__tests__/complexity-parser.test.ts \
         src/lib/__tests__/catbot-prompt-assembler.test.ts \
         src/lib/__tests__/intent-job-executor.test.ts \
         src/lib/__tests__/intent-jobs.test.ts \
         src/lib/__tests__/alert-service.test.ts \
         src/lib/__tests__/catbot-intents.test.ts
       ```
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/alert-service.test.ts src/lib/__tests__/complexity-decisions.test.ts src/lib/__tests__/complexity-parser.test.ts src/lib/__tests__/catbot-prompt-assembler.test.ts src/lib/__tests__/intent-job-executor.test.ts src/lib/__tests__/intent-jobs.test.ts && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - checkClassificationTimeouts exists and is registered in checks array
    - route.ts general catch marks outcome='timeout' when decisionId is in scope
    - All tests GREEN including full quick-suite
    - npm run build GREEN
    - Commit: `feat(131-04): add checkClassificationTimeouts alert + timeout outcome tracking`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CatBot oracle E2E — reproduce Holded Q1 case</name>
  <what-built>
    Complete Phase 131 behavior:
    - P0 complexity protocol injected in PromptAssembler
    - Gate parses [COMPLEXITY:...] prefix in route.ts (both paths)
    - complexity_decisions table logs every turn
    - queue_intent_job accepts description, IntentJobExecutor handles __description__
    - Self-check escalates after iteration >= 3 with pending tool_calls
    - notifyProgress throttles at 60s with force flag for phase changes
    - AlertService.checkClassificationTimeouts monitors the feedback loop
    - Timeout outcome tracked in route.ts catch

    Docker build and deploy:
    ```
    cd ~/docflow && docker compose build --no-cache && docker compose up -d && \
      docker exec -u root docflow-app chown -R nextjs:nodejs /app/data/ && \
      docker restart docflow-app
    ```
  </what-built>
  <how-to-verify>
    CatBot-as-Oracle protocol per CLAUDE.md. Reproduce the original failing case:

    1. Open Telegram (preferred channel since that's where the original bug occurred).

    2. Send to CatBot:
       ```
       quiero que entres en holded y hagas resumen Q1 2026 + Q1 2025 + compara + envia email maquetado a antonio@educa360.com
       ```

    3. Expected CatBot behavior (PASS criteria):
       - Within <5 seconds, CatBot replies WITHOUT executing any holded tools.
       - Reply content (after prefix strip) contains: "Esta tarea es compleja", "CatFlow", "60 segundos", and a question asking for approval.
       - Reply does NOT contain raw text like "[COMPLEXITY:" (the prefix must be stripped).

    4. Reply "sí, prepáralo" to CatBot.

    5. Expected follow-up behavior:
       - CatBot calls queue_intent_job with a description field (not a specific tool_name).
       - CatBot confirms the job is encolado and mentions reportes cada 60s.

    6. Wait up to 3 minutes. Expected:
       - Telegram receives at least one "⏳ ..." progress message showing the pipeline phase (strategist/architect/executor).
       - Progress messages come AT LEAST every 60s while the pipeline is running.
       - Phase transitions trigger immediate notifications (not throttled).

    7. Query the DB to confirm the audit trail:
       ```
       docker exec -it docflow-app sqlite3 /app/data/catbot.db \
         "SELECT id, classification, reason, estimated_duration_s, async_path_taken, outcome, created_at FROM complexity_decisions ORDER BY created_at DESC LIMIT 5;"
       ```
       Expected: most recent row has classification='complex', async_path_taken=1, outcome IN ('queued', 'completed').

    8. Query intent_jobs to confirm the description path:
       ```
       docker exec -it docflow-app sqlite3 /app/data/catbot.db \
         "SELECT id, tool_name, tool_args, status, pipeline_phase FROM intent_jobs ORDER BY created_at DESC LIMIT 3;"
       ```
       Expected: a row with tool_name='__description__' and tool_args containing the description text.

    9. Negative case — send a simple request:
       ```
       lista mis CatBrains
       ```
       Expected: CatBot executes list_catbrains immediately without asking for approval. Audit row shows classification='simple'.

    10. Paste the full conversation transcript AND the SQL query outputs into the SUMMARY as oracle evidence.

    FAIL criteria (any one):
    - CatBot executes tools directly on the complex request without asking
    - Reply contains raw "[COMPLEXITY:" text
    - No progress reports arrive during pipeline execution
    - complexity_decisions row shows classification='simple' for the complex request
    - intent_jobs row uses a specific tool_name instead of '__description__'
    - Simple request is incorrectly gated
  </how-to-verify>
  <files>.planning/phases/131-complexity-assessment-catbot-razona-antes-de-ejecutar/131-04-SUMMARY.md</files>
  <action>Human checkpoint: follow the how-to-verify steps above and paste the oracle transcript + SQL outputs into 131-04-SUMMARY.md under an "Oracle Evidence" section. This is a CatBot-as-Oracle gate per CLAUDE.md and cannot be automated.</action>
  <verify>Human confirms oracle PASS criteria met and pastes evidence into SUMMARY.md</verify>
  <done>Oracle Evidence section in 131-04-SUMMARY.md contains the Telegram conversation transcript, the complexity_decisions SQL output showing classification=complex with async_path_taken=1, and the intent_jobs SQL output showing tool_name=__description__ for the complex request</done>
  <resume-signal>Paste oracle transcript + SQL outputs in 131-04-SUMMARY.md "Oracle Evidence" section, then type "approved" to resume. If any PASS criterion failed, type "failed: {description}" and a gap closure plan will be created.</resume-signal>
</task>

</tasks>

<verification>
Automated quick-suite:
```
cd ~/docflow/app && npx vitest run \
  src/lib/__tests__/complexity-decisions.test.ts \
  src/lib/__tests__/complexity-parser.test.ts \
  src/lib/__tests__/catbot-prompt-assembler.test.ts \
  src/lib/__tests__/intent-job-executor.test.ts \
  src/lib/__tests__/intent-jobs.test.ts \
  src/lib/__tests__/alert-service.test.ts \
  src/lib/__tests__/catbot-intents.test.ts

cd ~/docflow/app && npm run build
```

Then Docker deploy + oracle verification per Task 3.
</verification>

<success_criteria>
- QA-07 ✓ checkClassificationTimeouts registered and tested
- route.ts timeout path updates outcome
- Oracle evidence captured for QA-01..07 holistic behavior
- Full quick-suite GREEN, build GREEN, Docker deploy successful
- Original Holded Q1 case no longer times out — instead gated and queued as async CatFlow
</success_criteria>

<output>
Create `.planning/phases/131-complexity-assessment-catbot-razona-antes-de-ejecutar/131-04-SUMMARY.md` with:
- Test counts and final pass/fail state
- "Oracle Evidence" section with the full Telegram conversation transcript
- SQL query outputs showing complexity_decisions + intent_jobs rows
- Any gaps discovered (→ feeds /gsd:plan-phase --gaps)
- Confirmation that KTREE-02 sync check still passes (no new tools added)
</output>
