# Plan 64-02: ForkDialog Component

## Goal
Create a dialog component for forking (duplicating) a CatFlow with a user-specified name.

## Depends On
- 64-01 (fork API endpoint, CatFlowCard component)

## Requirements Addressed
- PAGE-04: Fork creates a complete copy (task + all steps) with user-specified name

## Tasks

### Task 1: Create ForkDialog component
**File**: `app/src/components/catflow/fork-dialog.tsx` (new)

Props:
```typescript
interface ForkDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  taskId: string | null;
  taskName: string;
  onForked: (newTask: CatFlowTask) => void;
}
```

Component structure:
```tsx
"use client";

import { useState } from 'react';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Loader2, GitFork } from 'lucide-react';
import { toast } from 'sonner';
import { useTranslations } from 'next-intl';

export function ForkDialog({ open, onOpenChange, taskId, taskName, onForked }: ForkDialogProps) {
  const t = useTranslations('catflow');
  const [name, setName] = useState('');
  const [loading, setLoading] = useState(false);

  // Pre-fill name when dialog opens
  // useEffect: when open changes to true, set name to `${taskName} (copia)`

  const handleFork = async () => {
    if (!taskId || !name.trim()) return;
    setLoading(true);
    try {
      const res = await fetch(`/api/tasks/${taskId}/fork`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ name: name.trim() }),
      });
      if (!res.ok) throw new Error('Error');
      const newTask = await res.json();
      toast.success(t('fork.success'));
      onForked(newTask);
      onOpenChange(false);
    } catch {
      toast.error(t('fork.error'));
    } finally {
      setLoading(false);
    }
  };

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="bg-zinc-900 border-zinc-800 max-w-md">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2 text-zinc-200">
            <GitFork className="w-5 h-5 text-violet-400" />
            {t('fork.title')}
          </DialogTitle>
          <DialogDescription className="text-zinc-400">
            {t('fork.description')}
          </DialogDescription>
        </DialogHeader>

        <div className="space-y-3 py-2">
          <div>
            <Label className="text-zinc-300 text-sm">{t('fork.nameLabel')}</Label>
            <Input
              value={name}
              onChange={(e) => setName(e.target.value)}
              placeholder={t('fork.namePlaceholder')}
              className="mt-1 bg-zinc-800 border-zinc-700 text-zinc-200"
              autoFocus
              onKeyDown={(e) => e.key === 'Enter' && !loading && handleFork()}
            />
          </div>
          <p className="text-xs text-zinc-500">
            {t('fork.hint')}
          </p>
        </div>

        <DialogFooter>
          <Button
            variant="outline"
            onClick={() => onOpenChange(false)}
            className="bg-transparent border-zinc-700 text-zinc-300"
          >
            {t('fork.cancel')}
          </Button>
          <Button
            onClick={handleFork}
            disabled={!name.trim() || loading}
            className="bg-gradient-to-r from-violet-600 to-purple-700 hover:from-violet-500 hover:to-purple-600 text-white"
          >
            {loading ? <Loader2 className="w-4 h-4 animate-spin mr-2" /> : <GitFork className="w-4 h-4 mr-2" />}
            {t('fork.confirm')}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

Key behaviors:
- Pre-fills name as `"${taskName} (copia)"` when dialog opens
- Enter key submits
- autoFocus on name input
- Loading spinner on confirm button
- Toast on success/error
- Calls `onForked(newTask)` so parent can add the new task to state
- All text via `catflow.fork.*` i18n keys

## Verification
- [ ] Dialog opens with pre-filled name
- [ ] Empty name disables confirm button
- [ ] Enter key submits the fork
- [ ] Success: toast, dialog closes, parent receives new task
- [ ] Error: toast shown, dialog stays open
- [ ] Loading spinner shown during API call
- [ ] All text uses i18n keys
- [ ] `npm run build` passes

## Estimated Complexity
Low — 1 dialog component (~80 lines)
