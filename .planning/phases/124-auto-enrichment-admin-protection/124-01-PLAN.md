---
phase: 124-auto-enrichment-admin-protection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/lib/services/catbot-learned.ts
  - app/src/lib/__tests__/catbot-learned.test.ts
  - app/src/lib/catbot-db.ts
  - app/src/lib/services/catbot-tools.ts
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03]

must_haves:
  truths:
    - "CatBot puede guardar un learned_entry con knowledge_path, category, content, learned_from"
    - "Los learned_entries empiezan como staging (validated=0) y no se inyectan en el prompt"
    - "Una entry se promueve a validated=1 cuando access_count >= 3 o admin la valida"
    - "Contenido duplicado (Jaccard > 0.8) no se guarda"
    - "Maximo 3 entries por conversacion (rate limiting)"
  artifacts:
    - path: "app/src/lib/services/catbot-learned.ts"
      provides: "LearnedEntryService: staging, dedup, rate limiting, promotion"
      min_lines: 60
    - path: "app/src/lib/__tests__/catbot-learned.test.ts"
      provides: "Tests para save, schema, staging, dedup, rate limiting"
      min_lines: 80
    - path: "app/src/lib/catbot-db.ts"
      provides: "incrementAccessCount, setValidated, deleteLearnedEntry DB helpers"
      exports: ["incrementAccessCount", "setValidated", "deleteLearnedEntry"]
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "save_learned_entry tool definition + handler"
  key_links:
    - from: "app/src/lib/services/catbot-learned.ts"
      to: "app/src/lib/catbot-db.ts"
      via: "saveLearnedEntry, getLearnedEntries, incrementAccessCount, setValidated"
      pattern: "import.*from.*catbot-db"
    - from: "app/src/lib/services/catbot-tools.ts"
      to: "app/src/lib/services/catbot-learned.ts"
      via: "save_learned_entry handler calls LearnedEntryService"
      pattern: "saveLearnedEntryWithStaging"
---

<objective>
LearnedEntryService con TDD + save_learned_entry tool + DB helpers para staging

Purpose: CatBot puede guardar aprendizajes de interacciones exitosas en knowledge_learned con validacion por staging. Esto es la base del auto-enrichment: las entries no se inyectan hasta ser validadas.

Output: LearnedEntryService (staging, dedup, rate limiting, promotion), 3 nuevas funciones DB, save_learned_entry tool en catbot-tools.ts, tests completos.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-auto-enrichment-admin-protection/124-RESEARCH.md

<interfaces>
<!-- Existing types and functions the executor needs -->

From app/src/lib/catbot-db.ts:
```typescript
export interface LearnedRow {
  id: string;
  knowledge_path: string;
  category: string;
  content: string;
  learned_from: string;
  confidence: number;
  validated: number;
  access_count: number;
  created_at: string;
  updated_at: string;
}

export function saveLearnedEntry(entry: {
  knowledgePath: string;
  category: string;
  content: string;
  learnedFrom?: string;
}): string;

export function getLearnedEntries(opts?: {
  knowledgePath?: string;
  validated?: boolean;
}): LearnedRow[];
```

From app/src/lib/services/catbot-tools.ts:
```typescript
export interface CatBotTool {
  type: 'function';
  function: { name: string; description: string; parameters: Record<string, unknown> };
}
export interface ToolCallResult {
  name: string;
  result: unknown;
  actions?: Array<{ type: string; url: string; label: string }>;
}
export function getToolsForLLM(allowedActions?: string[]): CatBotTool[];
export async function executeTool(name: string, args: Record<string, unknown>, baseUrl: string): Promise<ToolCallResult>;
```

From app/src/lib/services/catbot-memory.ts (pattern to follow for Jaccard dedup):
```typescript
export function normalizeQuery(query: string): string[];
// Uses Jaccard similarity with 0.8 threshold for recipe dedup
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LearnedEntryService TDD + DB helpers</name>
  <files>app/src/lib/services/catbot-learned.ts, app/src/lib/__tests__/catbot-learned.test.ts, app/src/lib/catbot-db.ts</files>
  <behavior>
    - Test "save": saveLearnedEntryWithStaging guarda entry con validated=0, confidence=0.5, retorna id
    - Test "schema": entry tiene knowledge_path, category (best_practice|pitfall|troubleshoot), content (max 500 chars), learned_from (usage|development)
    - Test "dedup": si ya existe entry con mismo path+category y Jaccard > 0.8, retorna null
    - Test "rate limit": despues de 3 entries en una conversacion, retorna null con error rate_limited
    - Test "staging": promoteIfReady retorna false si access_count < 3, true si access_count >= 3 y setea validated=1
    - Test "admin promote": adminValidate(id) setea validated=1 independientemente de access_count
    - Test "admin reject": adminReject(id) elimina la entry
  </behavior>
  <action>
    1. Anadir 3 funciones a catbot-db.ts:
       - `incrementAccessCount(id: string)`: UPDATE knowledge_learned SET access_count = access_count + 1, updated_at = datetime WHERE id = ?
       - `setValidated(id: string, validated: boolean)`: UPDATE knowledge_learned SET validated = ? WHERE id = ?
       - `deleteLearnedEntry(id: string)`: DELETE FROM knowledge_learned WHERE id = ?

    2. Crear catbot-learned.ts siguiendo el patron de catbot-memory.ts:
       - VALIDATION_THRESHOLD = 3
       - MAX_ENTRIES_PER_CONVERSATION = 3
       - `saveLearnedEntryWithStaging(entry, conversationTracker)`: dedup check con Jaccard 0.8 (reutilizar patron de normalizeQuery de catbot-memory.ts), rate limit check, llama saveLearnedEntry de catbot-db.ts
       - `promoteIfReady(entryId)`: comprueba access_count >= threshold, llama setValidated
       - `adminValidate(entryId)`: setValidated(id, true) directo
       - `adminReject(entryId)`: deleteLearnedEntry(id)
       - `jaccardSimilarity(a, b)`: reutilizar logica de catbot-memory.ts
       - conversationTracker: Map en memoria por conversationId con contador de entries guardadas

    3. Tests en catbot-learned.test.ts usando el mismo patron de catbot-memory.test.ts (mock de catbot-db o in-memory SQLite).

    IMPORTANTE: content se trunca a 500 chars (mismo patron que profile directives en Phase 121). Category solo acepta best_practice|pitfall|troubleshoot. knowledge_path debe ser un area valida del knowledge tree.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-learned.test.ts</automated>
  </verify>
  <done>LearnedEntryService con staging, dedup, rate limiting y promotion; 3 nuevas funciones DB; 7+ tests pasando</done>
</task>

<task type="auto">
  <name>Task 2: save_learned_entry tool en catbot-tools.ts</name>
  <files>app/src/lib/services/catbot-tools.ts</files>
  <action>
    1. Anadir tool definition a TOOLS array en catbot-tools.ts:
       ```
       name: 'save_learned_entry'
       description: 'Guarda un aprendizaje en la base de conocimiento. Usa esto cuando resuelvas un problema y la solucion sea reutilizable. La entrada pasa por staging y no se inyecta en el prompt hasta ser validada.'
       parameters: knowledge_path (string, area del knowledge tree), category (enum: best_practice|pitfall|troubleshoot), content (string, max 500 chars), learned_from (enum: usage|development, default usage)
       required: [knowledge_path, category, content]
       ```

    2. Anadir case en executeTool:
       ```
       case 'save_learned_entry': {
         import { saveLearnedEntryWithStaging } from catbot-learned
         const result = saveLearnedEntryWithStaging({
           knowledgePath: args.knowledge_path,
           category: args.category,
           content: args.content,
           learnedFrom: args.learned_from || 'usage'
         });
         if (result === null) return { name, result: { saved: false, reason: 'Duplicado o rate limit' } };
         return { name, result: { saved: true, id: result, status: 'staging', message: 'Aprendizaje guardado en staging. Se validara automaticamente tras uso repetido o por admin.' } };
       }
       ```

    3. En getToolsForLLM, permission-gate save_learned_entry:
       - Permitido si allowedActions incluye 'manage_knowledge' O si allowedActions esta vacio (default)
       - Patron: `if (name === 'save_learned_entry' && (allowedActions.includes('manage_knowledge') || !allowedActions.length)) return true;`

    IMPORTANTE: El import de catbot-learned.ts debe ser al inicio del archivo, no lazy. Seguir el patron existente de imports.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-learned.test.ts && npx vitest run</automated>
  </verify>
  <done>save_learned_entry tool disponible en CatBot, permission-gated, handler llama a LearnedEntryService, suite completa verde</done>
</task>

</tasks>

<verification>
- `cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-learned.test.ts` -- tests del servicio pasan
- `cd /home/deskmath/docflow/app && npx vitest run` -- suite completa verde
- `cd /home/deskmath/docflow/app && npx tsc --noEmit` -- sin errores de tipos
</verification>

<success_criteria>
- LearnedEntryService exporta saveLearnedEntryWithStaging, promoteIfReady, adminValidate, adminReject
- catbot-db.ts exporta incrementAccessCount, setValidated, deleteLearnedEntry
- save_learned_entry tool aparece en getToolsForLLM() y se ejecuta via executeTool
- Entries empiezan con validated=0, se promueven con access_count >= 3
- Dedup con Jaccard 0.8, rate limit de 3 por conversacion
- Todos los tests verdes
</success_criteria>

<output>
After completion, create `.planning/phases/124-auto-enrichment-admin-protection/124-01-SUMMARY.md`
</output>
