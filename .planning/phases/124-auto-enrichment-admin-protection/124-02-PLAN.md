---
phase: 124-auto-enrichment-admin-protection
plan: 02
type: execute
wave: 2
depends_on: [124-01]
files_modified:
  - app/src/lib/services/catbot-tools.ts
  - app/src/app/api/catbot/chat/route.ts
  - app/src/lib/__tests__/catbot-learned.test.ts
autonomous: true
requirements: [LEARN-04, ADMIN-01]

must_haves:
  truths:
    - "query_knowledge devuelve learned_entries validadas junto con el knowledge tree estatico"
    - "Las learned_entries no validadas NO aparecen en query_knowledge"
    - "access_count se incrementa cuando una learned_entry es consultada via query_knowledge"
    - "Una entry con access_count que alcanza el threshold se promueve automaticamente"
    - "CatBot nunca revela datos de un usuario a otro usuario"
    - "Si un tool intenta acceder a datos de otro usuario sin sudo, retorna SUDO_REQUIRED"
  artifacts:
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "query_knowledge extendido con learned entries + user-scoped enforcement"
    - path: "app/src/app/api/catbot/chat/route.ts"
      provides: "Pasa userId y sudoActive como contexto a executeTool"
  key_links:
    - from: "app/src/lib/services/catbot-tools.ts"
      to: "app/src/lib/catbot-db.ts"
      via: "getLearnedEntries({validated: true}) en query_knowledge"
      pattern: "getLearnedEntries.*validated.*true"
    - from: "app/src/app/api/catbot/chat/route.ts"
      to: "app/src/lib/services/catbot-tools.ts"
      via: "executeTool(name, args, baseUrl, { userId, sudoActive })"
      pattern: "executeTool.*userId.*sudoActive"
---

<objective>
Extender query_knowledge para incluir learned_entries validadas + enforcement de user_id en tools

Purpose: Completa el ciclo de auto-enrichment (las entries validadas se devuelven en query_knowledge) y cierra la brecha de seguridad mas critica (tools que aceptan user_id del LLM sin verificacion).

Output: query_knowledge devuelve learned entries, executeTool recibe contexto de usuario desde route.ts, tools user-scoped verifican identidad.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/124-auto-enrichment-admin-protection/124-RESEARCH.md
@.planning/phases/124-auto-enrichment-admin-protection/124-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (created in this phase) -->
From app/src/lib/services/catbot-learned.ts:
```typescript
export function saveLearnedEntryWithStaging(entry: {...}): string | null;
export function promoteIfReady(entryId: string): boolean;
export function adminValidate(entryId: string): void;
export function adminReject(entryId: string): void;
```

From app/src/lib/catbot-db.ts (added in Plan 01):
```typescript
export function incrementAccessCount(id: string): void;
export function setValidated(id: string, validated: boolean): void;
export function deleteLearnedEntry(id: string): void;
// Existing:
export function getLearnedEntries(opts?: { knowledgePath?: string; validated?: boolean }): LearnedRow[];
```

From app/src/lib/services/catbot-tools.ts (current signature):
```typescript
export async function executeTool(name: string, args: Record<string, unknown>, baseUrl: string): Promise<ToolCallResult>;
```

From app/src/app/api/catbot/chat/route.ts (current state, L87):
```typescript
const userId = bodyUserId || deriveUserId(effectiveChannel, undefined);
// L232: const toolResult = await executeTool(toolName, toolArgs, baseUrl);
// L420: const toolResult = await executeTool(toolName, toolArgs, baseUrl);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender query_knowledge con learned entries validadas</name>
  <files>app/src/lib/services/catbot-tools.ts, app/src/lib/__tests__/catbot-learned.test.ts</files>
  <action>
    1. En el case 'query_knowledge' de executeTool (linea ~1196), despues de obtener resultados del knowledge tree estatico, anadir bloque para learned entries:

       ```
       // Append validated learned entries
       const learnedEntries = getLearnedEntries({ knowledgePath: area, validated: true });
       const relevantLearned = query
         ? learnedEntries.filter(e =>
             e.content.toLowerCase().includes(query.toLowerCase()) ||
             e.category.toLowerCase().includes(query.toLowerCase())
           ).slice(0, 5)
         : learnedEntries.slice(0, 5);

       if (relevantLearned.length > 0) {
         for (const entry of relevantLearned) {
           incrementAccessCount(entry.id);
           promoteIfReady(entry.id);
         }
         // Append to result (cap content to 200 chars per entry)
         const learnedResults = relevantLearned.map(e => ({
           category: e.category,
           content: e.content.substring(0, 200),
           learned_from: e.learned_from,
           type: 'learned'
         }));
         // result puede ser objeto o array; manejar ambos
       }
       ```

    2. Importar getLearnedEntries, incrementAccessCount de catbot-db.ts y promoteIfReady de catbot-learned.ts al inicio de catbot-tools.ts.

    3. Anadir tests a catbot-learned.test.ts:
       - Test "query includes validated": con entry validated=1, query_knowledge la incluye
       - Test "query excludes unvalidated": con entry validated=0, query_knowledge no la incluye
       - Test "query increments access_count": despues de query, access_count sube
       - Test "auto-promotion on query": entry con access_count=2, query la incrementa a 3, se promueve

    IMPORTANTE: No modificar la firma de executeTool en esta task (eso se hace en Task 2). Los learned entries solo se anaden al resultado, no modifican el knowledge tree estatico. Cap de 5 learned entries por query y 200 chars por entry para proteger token budget (Pitfall 3 de research).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-learned.test.ts -t "query"</automated>
  </verify>
  <done>query_knowledge devuelve learned_entries validadas con cap de 5 entries y 200 chars; access_count se incrementa en cada consulta; auto-promotion funciona</done>
</task>

<task type="auto">
  <name>Task 2: User-scoped tool execution con context desde route.ts</name>
  <files>app/src/app/api/catbot/chat/route.ts, app/src/lib/services/catbot-tools.ts, app/src/lib/__tests__/catbot-learned.test.ts</files>
  <action>
    1. Modificar la firma de executeTool en catbot-tools.ts para aceptar contexto opcional (Pitfall 4 -- no romper callers existentes):
       ```typescript
       export async function executeTool(
         name: string,
         args: Record<string, unknown>,
         baseUrl: string,
         context?: { userId: string; sudoActive: boolean }
       ): Promise<ToolCallResult>
       ```

    2. Dentro de executeTool, antes del switch, anadir enforcement para user-scoped tools:
       ```typescript
       const USER_SCOPED_TOOLS = ['get_user_profile', 'update_user_profile', 'list_my_recipes',
         'forget_recipe', 'list_my_summaries', 'get_summary'];

       if (USER_SCOPED_TOOLS.includes(name) && context?.userId) {
         const requestedUserId = (args.user_id as string) || context.userId;
         if (requestedUserId !== context.userId && !context.sudoActive) {
           return { name, result: { error: 'SUDO_REQUIRED', message: 'Acceder a datos de otro usuario requiere sudo activo.' } };
         }
         args.user_id = context.sudoActive ? requestedUserId : context.userId;
       }
       ```

    3. En route.ts, actualizar TODAS las llamadas a executeTool para pasar contexto:
       - Linea ~232 (streaming): `await executeTool(toolName, toolArgs, baseUrl, { userId, sudoActive: !!sudoActive })`
       - Linea ~420 (non-streaming): `await executeTool(toolName, toolArgs, baseUrl, { userId, sudoActive: !!sudoActive })`
       - Buscar con grep todas las llamadas a executeTool en route.ts para no dejar ninguna sin actualizar.

    4. Anadir tests a catbot-learned.test.ts:
       - Test "isolation": executeTool con user_id distinto al context.userId sin sudo retorna SUDO_REQUIRED
       - Test "sudo bypass": executeTool con user_id distinto pero sudoActive=true permite acceso
       - Test "default userId": executeTool sin args.user_id usa context.userId

    IMPORTANTE: El parametro context es OPCIONAL con default undefined. Cuando undefined, el comportamiento es exactamente como antes (sin enforcement). Esto evita romper tests existentes o callers internos (Pitfall 4).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-learned.test.ts -t "isolation|sudo" && npx vitest run</automated>
  </verify>
  <done>executeTool recibe contexto de usuario; tools user-scoped verifican identidad del caller; cross-user access requiere sudo; suite completa verde</done>
</task>

</tasks>

<verification>
- `cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-learned.test.ts` -- todos los tests pasan
- `cd /home/deskmath/docflow/app && npx vitest run` -- suite completa verde
- `cd /home/deskmath/docflow/app && npx tsc --noEmit` -- sin errores de tipos
</verification>

<success_criteria>
- query_knowledge devuelve learned_entries validadas (max 5, 200 chars cada una)
- query_knowledge NO devuelve entries con validated=0
- access_count se incrementa en cada consulta a query_knowledge
- Auto-promotion funciona cuando access_count alcanza threshold
- executeTool acepta context opcional sin romper callers existentes
- Tools user-scoped retornan SUDO_REQUIRED si userId no coincide y sudo no esta activo
- Todos los tests verdes
</success_criteria>

<output>
After completion, create `.planning/phases/124-auto-enrichment-admin-protection/124-02-SUMMARY.md`
</output>
