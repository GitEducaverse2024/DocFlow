---
phase: 149-kb-foundation-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .docflow-kb/_manual.md
  - .docflow-kb/_index.json
  - .docflow-kb/_header.md
  - .docflow-kb/_schema/.gitkeep
  - .docflow-kb/domain/concepts/.gitkeep
  - .docflow-kb/domain/taxonomies/.gitkeep
  - .docflow-kb/domain/architecture/.gitkeep
  - .docflow-kb/resources/catpaws/.gitkeep
  - .docflow-kb/resources/connectors/.gitkeep
  - .docflow-kb/resources/catbrains/.gitkeep
  - .docflow-kb/resources/email-templates/.gitkeep
  - .docflow-kb/resources/skills/.gitkeep
  - .docflow-kb/resources/canvases/.gitkeep
  - .docflow-kb/rules/.gitkeep
  - .docflow-kb/protocols/.gitkeep
  - .docflow-kb/runtime/.gitkeep
  - .docflow-kb/incidents/.gitkeep
  - .docflow-kb/features/.gitkeep
  - .docflow-kb/guides/.gitkeep
  - .docflow-kb/state/.gitkeep
  - .docflow-legacy/README.md
  - .docflow-legacy/_migration-log.md
  - .docflow-legacy/audits-closed/.gitkeep
  - .docflow-legacy/milestone-retrospectives/.gitkeep
  - .docflow-legacy/catalogs-pre-kb/.gitkeep
  - .docflow-legacy/json-pre-kb/.gitkeep
  - .planning/Index.md
autonomous: true
requirements: [KB-01]
must_haves:
  truths:
    - "Existe .docflow-kb/ con 10 subdirectorios estándar (domain/, resources/, rules/, protocols/, runtime/, incidents/, features/, guides/, state/, _schema/)"
    - "Existe .docflow-kb/_manual.md con documentación real (no TODO placeholder) explicando arquitectura, nomenclatura, navegación"
    - "Existe .docflow-kb/_index.json stub válido contra schema v2 (entry_count: 0, estructura header/entries/indexes completa)"
    - "Existe .docflow-legacy/ con README.md explicativo real + subdirectorios preparados"
    - ".planning/Index.md apunta a .docflow-kb/_manual.md como fuente de conocimiento futura"
  artifacts:
    - path: ".docflow-kb/_manual.md"
      provides: "Documento explicativo de arquitectura KB (10 secciones mínimas)"
      min_lines: 80
    - path: ".docflow-kb/_index.json"
      provides: "Stub de índice v2 válido"
      contains: '"schema_version": "2.0"'
    - path: ".docflow-kb/_header.md"
      provides: "Stub L0 con counts iniciales en cero"
    - path: ".docflow-legacy/README.md"
      provides: "Explicación de zona transitoria + reglas de purga"
      min_lines: 25
    - path: ".docflow-legacy/_migration-log.md"
      provides: "Archivo vacío con frontmatter listo"
  key_links:
    - from: ".planning/Index.md"
      to: ".docflow-kb/_manual.md"
      via: "markdown link"
      pattern: "\\.docflow-kb/_manual\\.md"
---

<objective>
Crear la estructura física base del Knowledge Base (`.docflow-kb/`) y la zona legacy transitoria (`.docflow-legacy/`) en el root del repo, más un `_manual.md` explicativo real y un `_index.json` stub que ya valide contra el schema v2 del PRD. Sin esto no existe el terreno donde los siguientes planes escriben schemas, servicio, CLI ni donde los operadores de limpieza mueven artefactos.

Purpose: Deliverable #1 de Phase 149 (KB-01). Prerequisito estructural de todos los demás planes de esta fase.
Output: Árbol de directorios `.docflow-kb/` completo + `.docflow-legacy/` completo + `_manual.md` real + `_index.json` stub válido + `.planning/Index.md` actualizado apuntando al KB.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-kb-foundation-bootstrap/149-CONTEXT.md
@.planning/ANALYSIS-knowledge-base-architecture.md

<interfaces>
<!-- Shape literal de _index.json v2 (§4.1 del PRD + §specifics del CONTEXT.md) -->

```json
{
  "schema_version": "2.0",
  "generated_at": "ISO8601",
  "generated_by": "knowledge-sync",
  "entry_count": 0,
  "header": {
    "counts": {
      "catpaws_active": 0,
      "connectors_active": 0,
      "catbrains_active": 0,
      "templates_active": 0,
      "skills_active": 0,
      "rules": 0,
      "incidents_resolved": 0,
      "features_documented": 0
    },
    "top_tags": [],
    "last_changes": []
  },
  "entries": [],
  "indexes": {
    "by_type": {},
    "by_tag": {},
    "by_audience": {}
  }
}
```

<!-- Estructura de carpetas .docflow-kb/ (§3.1 + CONTEXT.md) -->
```
.docflow-kb/
├── _index.json
├── _header.md
├── _manual.md
├── _schema/
├── domain/{concepts,taxonomies,architecture}/
├── resources/{catpaws,connectors,catbrains,email-templates,skills,canvases}/
├── rules/
├── protocols/
├── runtime/
├── incidents/
├── features/
├── guides/
└── state/
```

<!-- Estructura .docflow-legacy/ (§D.1 🟡) -->
```
.docflow-legacy/
├── README.md
├── audits-closed/
├── milestone-retrospectives/
├── catalogs-pre-kb/
├── json-pre-kb/
└── _migration-log.md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear esqueleto de `.docflow-kb/` con todos los subdirectorios y archivos stub</name>
  <files>
    .docflow-kb/_index.json,
    .docflow-kb/_header.md,
    .docflow-kb/_schema/.gitkeep,
    .docflow-kb/domain/concepts/.gitkeep,
    .docflow-kb/domain/taxonomies/.gitkeep,
    .docflow-kb/domain/architecture/.gitkeep,
    .docflow-kb/resources/catpaws/.gitkeep,
    .docflow-kb/resources/connectors/.gitkeep,
    .docflow-kb/resources/catbrains/.gitkeep,
    .docflow-kb/resources/email-templates/.gitkeep,
    .docflow-kb/resources/skills/.gitkeep,
    .docflow-kb/resources/canvases/.gitkeep,
    .docflow-kb/rules/.gitkeep,
    .docflow-kb/protocols/.gitkeep,
    .docflow-kb/runtime/.gitkeep,
    .docflow-kb/incidents/.gitkeep,
    .docflow-kb/features/.gitkeep,
    .docflow-kb/guides/.gitkeep,
    .docflow-kb/state/.gitkeep
  </files>
  <action>
Desde el root del repo (`/home/deskmath/docflow/`) crea esta estructura exacta. Usa `mkdir -p` para las carpetas y `touch` para los `.gitkeep`:

```
.docflow-kb/
├── _index.json              (contenido literal abajo)
├── _header.md               (contenido literal abajo)
├── _schema/.gitkeep
├── domain/concepts/.gitkeep
├── domain/taxonomies/.gitkeep
├── domain/architecture/.gitkeep
├── resources/catpaws/.gitkeep
├── resources/connectors/.gitkeep
├── resources/catbrains/.gitkeep
├── resources/email-templates/.gitkeep
├── resources/skills/.gitkeep
├── resources/canvases/.gitkeep
├── rules/.gitkeep
├── protocols/.gitkeep
├── runtime/.gitkeep
├── incidents/.gitkeep
├── features/.gitkeep
├── guides/.gitkeep
└── state/.gitkeep
```

Contenido EXACTO de `.docflow-kb/_index.json` (stub v2 válido; KB vacío; los valores de timestamp se dejan como string ISO de creación real generado en el momento de ejecución):

```json
{
  "schema_version": "2.0",
  "generated_at": "<PONER_ISO8601_ACTUAL>",
  "generated_by": "knowledge-sync",
  "entry_count": 0,
  "header": {
    "counts": {
      "catpaws_active": 0,
      "connectors_active": 0,
      "catbrains_active": 0,
      "templates_active": 0,
      "skills_active": 0,
      "rules": 0,
      "incidents_resolved": 0,
      "features_documented": 0
    },
    "top_tags": [],
    "last_changes": []
  },
  "entries": [],
  "indexes": {
    "by_type": {},
    "by_tag": {},
    "by_audience": {}
  }
}
```

Reemplazar `<PONER_ISO8601_ACTUAL>` por la fecha ISO real (ej. `2026-04-18T00:00:00Z`). Este shape es el que `kb-sync.cjs --full-rebuild` reproduce exactamente cuando KB está vacío (§4.1 PRD).

Contenido EXACTO de `.docflow-kb/_header.md` (stub L0, será regenerado por `knowledge-sync.ts` cuando haya entries):

```markdown
# KB Header (auto-generated)

**Generado:** <ISO8601_ACTUAL>
**Entradas totales:** 0

## Counts

- CatPaws activos: 0
- Connectors activos: 0
- CatBrains activos: 0
- Email templates activos: 0
- Skills activas: 0
- Reglas: 0
- Incidentes resueltos: 0
- Features documentados: 0

## Top tags

_(ninguno — KB vacío)_

## Últimos cambios

_(sin cambios — KB recién creado)_

---

> Este archivo se regenera automáticamente por `knowledge-sync.ts` cada vez que se llama a `syncResource()`. No editar manualmente.
```

NO crear aún `_manual.md` en esta tarea — se crea en Task 2 con contenido largo.
  </action>
  <verify>
    <automated>test -d .docflow-kb/_schema && test -d .docflow-kb/domain/concepts && test -d .docflow-kb/domain/taxonomies && test -d .docflow-kb/domain/architecture && test -d .docflow-kb/resources/catpaws && test -d .docflow-kb/resources/connectors && test -d .docflow-kb/resources/catbrains && test -d .docflow-kb/resources/email-templates && test -d .docflow-kb/resources/skills && test -d .docflow-kb/resources/canvases && test -d .docflow-kb/rules && test -d .docflow-kb/protocols && test -d .docflow-kb/runtime && test -d .docflow-kb/incidents && test -d .docflow-kb/features && test -d .docflow-kb/guides && test -d .docflow-kb/state && test -f .docflow-kb/_index.json && test -f .docflow-kb/_header.md && node -e 'const i=JSON.parse(require("fs").readFileSync(".docflow-kb/_index.json","utf8")); if(i.schema_version!=="2.0"||i.entry_count!==0||!i.header||!i.header.counts||!i.indexes||!i.indexes.by_type||!i.indexes.by_tag||!i.indexes.by_audience) process.exit(1)'</automated>
  </verify>
  <done>19 directorios creados, `_index.json` parsea como JSON válido con `schema_version: "2.0"`, `entry_count: 0`, y las 3 claves top-level `header`, `entries`, `indexes` presentes. `_header.md` existe.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir `_manual.md` explicativo y crear `.docflow-legacy/` con README real + subdirs</name>
  <files>
    .docflow-kb/_manual.md,
    .docflow-legacy/README.md,
    .docflow-legacy/_migration-log.md,
    .docflow-legacy/audits-closed/.gitkeep,
    .docflow-legacy/milestone-retrospectives/.gitkeep,
    .docflow-legacy/catalogs-pre-kb/.gitkeep,
    .docflow-legacy/json-pre-kb/.gitkeep
  </files>
  <action>
**A) Escribir `.docflow-kb/_manual.md`** — documento explicativo real (mínimo 80 líneas, no placeholder). Debe cubrir exactamente estas secciones con contenido substantivo derivado de §3.1, §3.2, §3.3 y §5.3 del PRD (`.planning/ANALYSIS-knowledge-base-architecture.md`):

1. **`# Knowledge Base de DocFlow — Manual de Navegación`** (título)
2. **`## Propósito`** — explica que este árbol es Source of Truth del conocimiento DocFlow, reemplaza silos dispersos (`app/data/knowledge/*.json`, `.planning/knowledge/*.md`, skills sueltas), y que está en **bootstrap**: estructura lista, contenido se migra en Fases 2-7 del PRD.
3. **`## Estructura de carpetas`** — lista las 10 subcarpetas + 4 archivos especiales (`_index.json`, `_header.md`, `_manual.md`, `_schema/`, `_archived/`, `_audit_stale.md`) con una línea cada una explicando qué va dentro.
4. **`## Nomenclatura`** — reglas kebab-case, prefijos id corto (8 chars UUID), reglas con código (R10-, SE01-), incidentes INC-, fases número, archivos especiales `_`.
5. **`## Frontmatter universal`** — lista los 13 campos obligatorios (id, type, subtype, lang, title, summary, tags, audience, status, created_at+created_by, version, updated_at+updated_by, source_of_truth) con una línea cada uno. Menciona bilingüe ES+EN en `title`/`summary`/`search_hints`.
6. **`## Tag taxonomy`** — menciona que `_schema/tag-taxonomy.json` define vocabulario controlado en 8 categorías (domains, entities, modes, connectors, roles, departments, rules, cross_cutting).
7. **`## Lifecycle`** — explica estados `active → deprecated → archived → purged`, soft-delete, workflow 150d/170d/180d/365d con confirmación explícita.
8. **`## Validación y CI`** — cómo correr `node scripts/validate-kb.cjs` local. Documenta que por ahora no hay pipeline CI integrado (no existe `.github/workflows/` en el repo) y que si un futuro plan añade CI este script se integra como step.
9. **`## Estado actual (Phase 149)`** — esta fase solo crea infraestructura. El contenido se migra en fases posteriores del PRD: Fase 2 (pobla desde DB), Fase 3 (migración de `.planning/knowledge/*.md` y `app/data/knowledge/*.json`), Fase 4 (consumo por CatBot), Fase 5 (enganchar creation tools), Fase 6 (dashboard), Fase 7 (limpieza). **Los silos legacy siguen vivos** hasta las fases de migración.
10. **`## Referencias`** — link a `.planning/ANALYSIS-knowledge-base-architecture.md` (PRD completo), link a `_schema/frontmatter.schema.json` (cuando exista), link a `.docflow-legacy/README.md`.

Prosa clara en español, sin TODOs. Si un detalle requiere referencia cruzada al PRD, citar sección (ej. "ver §5.3 del PRD para workflow completo de purga").

**B) Escribir `.docflow-legacy/README.md`** — documento real (mínimo 25 líneas, no placeholder). Secciones:

1. **`# .docflow-legacy — Zona transitoria`**
2. **`## Propósito`** — material cuyo insight ya vive en otro sitio (KB, MILESTONE-CONTEXT, phases) o cuyo milestone está cerrado. Se conserva temporalmente durante la transición.
3. **`## Estructura`** — explica los 4 subdirs:
   - `audits-closed/` → AUDIT-*.md de fases cerradas (v28 y anteriores)
   - `milestone-retrospectives/` → retrospectivas de milestones cerrados
   - `catalogs-pre-kb/` → catálogos manuales que migran al KB
   - `json-pre-kb/` → `app/data/knowledge/*.json` post-migración
4. **`## Reglas de vida`** — explica:
   - Nada se mueve aquí automáticamente. Cada movimiento se registra en `_migration-log.md`.
   - Purga física tras ~180d sin acceso (según §5.3 del PRD), igual que archivos del KB deprecated.
   - Reversible: mientras esté aquí se puede recuperar moviéndolo de vuelta a su origen o al KB.
5. **`## Cuándo NO usar legacy`** — no es para: WIP, drafts vivos, archivos que aún se consultan frecuentemente. Esos viven en `.planning/` o `.docflow-kb/`.
6. **`## Migration log`** — apunta a `.docflow-legacy/_migration-log.md`.

**C) Escribir `.docflow-legacy/_migration-log.md`** — archivo vacío con frontmatter listo para recibir entries:

```markdown
---
id: legacy-migration-log
type: guide
subtype: migration-log
lang: es
title: Migration log — .docflow-legacy
summary: Registro cronológico de movimientos a la zona transitoria `.docflow-legacy/`.
tags: [ops]
audience: [developer]
status: active
created_at: <ISO8601_ACTUAL>
created_by: phase-149
version: 1.0.0
updated_at: <ISO8601_ACTUAL>
updated_by: phase-149
source_of_truth: null
change_log:
  - { version: 1.0.0, date: <YYYY-MM-DD_ACTUAL>, author: phase-149, change: "Creado en Phase 149 — bootstrap de zona legacy" }
ttl: never
---

# Migration log — .docflow-legacy

Registro cronológico de archivos movidos a `.docflow-legacy/`. Cada entry indica: qué se movió, desde dónde, cuándo, autor, razón, y destino real del insight (si lo hay en el KB o en `.planning/`).

## Entries

_(ninguna entry todavía — log recién creado)_

<!--
Formato para nuevas entries:

### YYYY-MM-DD · <archivo>
- **Origen:** <path>
- **Destino:** .docflow-legacy/<subdir>/<file>
- **Insight preservado en:** <link al KB o doc activo>, o "ninguno — archivado puro"
- **Autor:** <user>
- **Razón:** <por qué se movió>
-->
```

**D) Crear los 4 subdirectorios vacíos** con `.gitkeep`:
- `.docflow-legacy/audits-closed/.gitkeep`
- `.docflow-legacy/milestone-retrospectives/.gitkeep`
- `.docflow-legacy/catalogs-pre-kb/.gitkeep`
- `.docflow-legacy/json-pre-kb/.gitkeep`
  </action>
  <verify>
    <automated>test -f .docflow-kb/_manual.md && test -f .docflow-legacy/README.md && test -f .docflow-legacy/_migration-log.md && test -d .docflow-legacy/audits-closed && test -d .docflow-legacy/milestone-retrospectives && test -d .docflow-legacy/catalogs-pre-kb && test -d .docflow-legacy/json-pre-kb && [ $(wc -l < .docflow-kb/_manual.md) -ge 80 ] && [ $(wc -l < .docflow-legacy/README.md) -ge 25 ] && grep -q "## Estructura de carpetas" .docflow-kb/_manual.md && grep -q "## Frontmatter universal" .docflow-kb/_manual.md && grep -q "## Lifecycle" .docflow-kb/_manual.md && grep -q "## Estado actual" .docflow-kb/_manual.md && grep -q "## Propósito" .docflow-legacy/README.md && grep -q "## Reglas de vida" .docflow-legacy/README.md && head -1 .docflow-legacy/_migration-log.md | grep -q "^---$"</automated>
  </verify>
  <done>`_manual.md` contiene las 10 secciones declaradas con ≥80 líneas totales. `.docflow-legacy/README.md` ≥25 líneas con secciones Propósito, Estructura, Reglas de vida. `_migration-log.md` tiene frontmatter válido y está vacío de entries reales. 4 subdirectorios de legacy creados.</done>
</task>

<task type="auto">
  <name>Task 3: Actualizar `.planning/Index.md` para apuntar al KB y marcar catálogos como migrables</name>
  <files>.planning/Index.md</files>
  <action>
Editar `.planning/Index.md` aplicando estos cambios mínimos y localizados (NO reescribir el archivo completo — es un Index activo con muchas referencias válidas):

1. **Añadir nueva sección al principio** (antes de "## Documentos Globales" o como cabecera si ya hay banner), formato:

```markdown
## Knowledge Base (en construcción)

El Knowledge Base unificado de DocFlow vive ahora en [`.docflow-kb/`](../.docflow-kb/). Ver [`.docflow-kb/_manual.md`](../.docflow-kb/_manual.md) para arquitectura, nomenclatura y navegación.

**Estado:** Bootstrap completado en Phase 149 — estructura base, schemas, servicio de sync y CLI. La migración de contenido desde los silos actuales (catálogos de `.planning/knowledge/*.md`, runtime JSONs de `app/data/knowledge/*.json`, skills dispersas, prompts de código) ocurre en fases posteriores del PRD `ANALYSIS-knowledge-base-architecture.md`.

**Material transitorio:** Archivos de milestones cerrados y catálogos post-migración se archivan en [`.docflow-legacy/`](../.docflow-legacy/). Ver `.docflow-legacy/README.md`.
```

2. **Localizar la sección de catálogos de `.planning/knowledge/`** y añadir un banner inline al inicio de esa sección:

```markdown
> **Migrables al KB** — los catálogos listados a continuación migrarán a `.docflow-kb/` en fases posteriores del PRD (Fase 3). Mientras tanto siguen siendo la fuente activa. No borrar manualmente.
```

3. **Añadir puntero a `.planning/reference/auditoria-catflow.md`** en la sección de auditorías activas o al final de la lista de auditorías:

```markdown
| [reference/auditoria-catflow.md](reference/auditoria-catflow.md) | Auditoría técnica de flujo CatFlow (movido desde root en Phase 149) | Referencia arquitectónica general |
```

Nota: `.planning/reference/` se crea en Plan 05 (cleanup). Si en el momento de ejecutar este task el directorio no existe aún, el link seguirá siendo válido apuntando al destino esperado — Plan 05 es orthogonal y Wave 1 también. Si hay lint estricto de Index.md que valide existencia del path, esperar a Plan 05 o comentar temporalmente el puntero con un TODO claro que Plan 05 lo destape.

**NO** borrar referencias a `.planning/knowledge/*.md` existentes — siguen activas hasta las fases de migración del PRD.

**NO** tocar secciones de fases históricas ni REQUIREMENTS/ROADMAP references.
  </action>
  <verify>
    <automated>grep -q "\.docflow-kb/_manual\.md" .planning/Index.md && grep -q "\.docflow-legacy/" .planning/Index.md && grep -qi "migrables al KB\|migran al KB\|migrarán a .docflow-kb" .planning/Index.md</automated>
  </verify>
  <done>`.planning/Index.md` tiene: (a) sección nueva "Knowledge Base (en construcción)" con links a `.docflow-kb/_manual.md` y `.docflow-legacy/`; (b) banner en la sección de catálogos de `.planning/knowledge/` marcándolos como "migrables al KB"; (c) puntero a `.planning/reference/auditoria-catflow.md`. Resto del Index intacto.</done>
</task>

</tasks>

<verification>
**Check integridad de estructura:**
```bash
find .docflow-kb -type d | sort
find .docflow-legacy -type d | sort
```
Debe listar las 19 carpetas del KB (incluyendo root) y las 5 de legacy.

**Check `_index.json` schema v2:**
```bash
node -e 'const i=JSON.parse(require("fs").readFileSync(".docflow-kb/_index.json","utf8")); console.log(i.schema_version, i.entry_count, Object.keys(i.header.counts).length, Object.keys(i.indexes).length)'
```
Debe imprimir: `2.0 0 8 3`.

**Check `_manual.md` tiene las 10 secciones:**
```bash
grep -c "^## " .docflow-kb/_manual.md
```
Debe ser ≥9 (10 secciones ## incluyendo el título #, pero `grep ^## ` cuenta solo h2).

**Check Index.md:**
```bash
grep -c "\.docflow-kb\|\.docflow-legacy" .planning/Index.md
```
Debe ser ≥3.
</verification>

<success_criteria>
1. `.docflow-kb/` existe con 10 subdirectorios + 3 archivos especiales (`_index.json`, `_header.md`, `_manual.md`) + `_schema/` directorio vacío con `.gitkeep`.
2. `.docflow-kb/_manual.md` es un documento explicativo real de ≥80 líneas con las 10 secciones declaradas (Propósito, Estructura, Nomenclatura, Frontmatter, Tag taxonomy, Lifecycle, Validación, Estado actual, Referencias).
3. `.docflow-kb/_index.json` valida como JSON, tiene `schema_version: "2.0"`, `entry_count: 0`, y las 3 claves top-level (`header`, `entries`, `indexes`) con la estructura del §4.1 del PRD.
4. `.docflow-legacy/` existe con 4 subdirectorios + `README.md` (≥25 líneas, no placeholder) + `_migration-log.md` con frontmatter listo.
5. `.planning/Index.md` apunta a `.docflow-kb/_manual.md` como fuente futura y marca catálogos legacy como "migrables".
6. Ninguna modificación a `app/data/knowledge/*.json`, `.planning/knowledge/*.md`, ni `skill_orquestador_catbot_enriched.md` (no están en scope de esta fase).
</success_criteria>

<output>
Al completar, crear `.planning/phases/149-kb-foundation-bootstrap/149-01-SUMMARY.md` con:
- Artefactos creados (árbol .docflow-kb/ y .docflow-legacy/)
- Contenido de _manual.md (índice de secciones)
- Cambios aplicados a .planning/Index.md
- Verificación (outputs de los comandos de verification)
</output>
