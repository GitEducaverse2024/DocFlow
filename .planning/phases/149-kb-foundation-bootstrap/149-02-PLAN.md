---
phase: 149-kb-foundation-bootstrap
plan: 02
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - .docflow-kb/_schema/frontmatter.schema.json
  - .docflow-kb/_schema/tag-taxonomy.json
  - .docflow-kb/_schema/resource.schema.json
  - scripts/validate-kb.cjs
autonomous: true
requirements: [KB-02, KB-03]
must_haves:
  truths:
    - "Existe `_schema/frontmatter.schema.json` que valida los 13 campos obligatorios + campos condicionales del §3.3 del PRD"
    - "El schema acepta el ejemplo bilingüe completo del §Apéndice A del PRD (`53f19c51-operador-holded.md`)"
    - "El schema acepta el ejemplo deprecated del §Apéndice B del PRD (`catpaw-b63164ed`) y EXIGE `deprecated_at/by/reason` cuando `status: deprecated`"
    - "Existe `_schema/tag-taxonomy.json` con las 8 categorías literales del §3.4 del PRD (domains, entities, modes, connectors, roles, departments, rules, cross_cutting)"
    - "Script `scripts/validate-kb.cjs` recorre `.docflow-kb/**/*.md` (excluye `_archived/`, `_header.md`, `_manual.md`), valida frontmatter contra schema, y sale con exit 1 si algún archivo incumple"
    - "El validador rechaza tags que no estén en la taxonomía"
  artifacts:
    - path: ".docflow-kb/_schema/frontmatter.schema.json"
      provides: "JSON Schema para frontmatter universal bilingüe con lifecycle"
      contains: '"id"'
    - path: ".docflow-kb/_schema/tag-taxonomy.json"
      provides: "Vocabulario controlado de tags en 8 categorías"
      contains: '"domains"'
    - path: ".docflow-kb/_schema/resource.schema.json"
      provides: "Schema adicional para subtipos de resource (catpaw, connector, etc.)"
    - path: "scripts/validate-kb.cjs"
      provides: "Validador CLI con exit 1 en fallo"
      min_lines: 80
  key_links:
    - from: "scripts/validate-kb.cjs"
      to: ".docflow-kb/_schema/frontmatter.schema.json"
      via: "require/readFileSync + AJV o validación manual"
      pattern: "frontmatter\\.schema\\.json"
    - from: "scripts/validate-kb.cjs"
      to: ".docflow-kb/_schema/tag-taxonomy.json"
      via: "require/readFileSync"
      pattern: "tag-taxonomy\\.json"
---

<objective>
Definir los schemas JSON que establecen el contrato de frontmatter y tag taxonomy del KB, y escribir el validador CLI que los aplica a todos los archivos `.md` del KB. Sin estos, el servicio `knowledge-sync.ts` (Plan 03) no tiene forma de saber qué generar ni cómo, y el CI (o la validación local) no puede rechazar drift.

Purpose: Deliverables #2 y #3 de Phase 149 (KB-02, KB-03). Establece el contrato que el servicio y el CLI siguientes consumen.
Output: 3 archivos de schema + 1 script validador con tests manuales sobre los ejemplos del PRD.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/149-kb-foundation-bootstrap/149-CONTEXT.md
@.planning/ANALYSIS-knowledge-base-architecture.md
@.planning/phases/149-kb-foundation-bootstrap/149-01-SUMMARY.md

<interfaces>
<!-- Tag taxonomy literal (§3.4 del PRD + CONTEXT.md §Tag taxonomy) -->
```json
{
  "domains": ["crm", "email", "storage", "analytics", "auth", "scheduling"],
  "entities": ["catpaw", "catflow", "canvas", "catbrain", "connector", "skill", "template"],
  "modes": ["chat", "processor", "hybrid"],
  "connectors": ["gmail", "holded", "drive", "mcp", "http", "n8n", "smtp"],
  "roles": ["extractor", "transformer", "synthesizer", "renderer", "emitter", "guard", "reporter"],
  "departments": ["business", "finance", "production", "other"],
  "rules": ["R01", "R02", "R10", "R13", "SE01", "DA01"],
  "cross_cutting": ["safety", "performance", "learning", "ux", "ops", "testing"]
}
```

<!-- Los 13 campos obligatorios del frontmatter (§3.3 del PRD + CONTEXT.md) -->
1. id — kebab-case string único
2. type — enum [concept|taxonomy|resource|rule|protocol|runtime|incident|feature|guide|state]
3. subtype — string | null
4. lang — enum [es|en|es+en]
5. title — string SI lang≠es+en; dict {es,en} SI lang==es+en
6. summary — mismas reglas que title
7. tags — array de strings validados contra tag-taxonomy.json
8. audience — array enum [catbot|architect|developer|user|onboarding]
9. status — enum [active|deprecated|draft|experimental]
10. created_at (ISO8601) + created_by (string)
11. version — string semver M.m.p
12. updated_at (ISO8601) + updated_by (string)
13. source_of_truth — array de {db, id, fields_from_db[]} | null

<!-- Campos condicionales -->
- last_accessed_at, access_count — requeridos si ttl=managed
- deprecated_at, deprecated_by, deprecated_reason — requeridos si status=deprecated
- superseded_by — opcional
- change_log — array obligatorio, min 1 entry
- search_hints — dict {es,en} si lang=es+en
- ttl — enum [never|managed|30d|180d]

<!-- Dependencias Node disponibles en el repo -->
- Root repo: sin package.json propio; scripts root usan node vanilla sin deps.
- app/: vitest 4.1.0, otros. Si se necesita AJV, instalar como devDep en root o hacer validación manual.
- CONVENCIÓN ELEGIDA: validación manual sin deps externas (el repo root no tiene package.json; mantener validador 100% vanilla-node evita introducir nueva dependencia). Usar un parser YAML mínimo — si no, leer frontmatter como líneas y parsear subset soportado (YAML 1.1 subset: scalars, arrays, dicts sencillos). Como fallback, usar `js-yaml` si ya está en `app/node_modules/` (verificar primero: `ls app/node_modules/js-yaml`); de no estar, escribir parser YAML mínimo inline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escribir `frontmatter.schema.json` y `tag-taxonomy.json` literales del PRD</name>
  <files>
    .docflow-kb/_schema/frontmatter.schema.json,
    .docflow-kb/_schema/tag-taxonomy.json,
    .docflow-kb/_schema/resource.schema.json
  </files>
  <behavior>
    - Schema acepta el ejemplo bilingüe completo del §Apéndice A (Operador Holded) sin errores.
    - Schema acepta el ejemplo deprecated completo del §Apéndice B (Consultor CRM) sin errores.
    - Schema rechaza un frontmatter con `status: deprecated` pero SIN `deprecated_at`, `deprecated_by`, `deprecated_reason`.
    - Schema rechaza un frontmatter con `ttl: managed` pero SIN `last_accessed_at` o `access_count`.
    - Schema rechaza un frontmatter con `lang: es+en` cuyo `title` sea string en vez de dict {es,en}.
    - Schema rechaza un `type` fuera del enum.
    - Tag taxonomy: validador acepta un tag si existe en CUALQUIERA de las 8 listas; rechaza si no existe.
  </behavior>
  <action>
**A) `.docflow-kb/_schema/tag-taxonomy.json`** — contenido LITERAL del §3.4 del PRD / §Tag taxonomy del CONTEXT.md. Copiar EXACTO:

```json
{
  "domains": ["crm", "email", "storage", "analytics", "auth", "scheduling"],
  "entities": ["catpaw", "catflow", "canvas", "catbrain", "connector", "skill", "template"],
  "modes": ["chat", "processor", "hybrid"],
  "connectors": ["gmail", "holded", "drive", "mcp", "http", "n8n", "smtp"],
  "roles": ["extractor", "transformer", "synthesizer", "renderer", "emitter", "guard", "reporter"],
  "departments": ["business", "finance", "production", "other"],
  "rules": ["R01", "R02", "R10", "R13", "SE01", "DA01"],
  "cross_cutting": ["safety", "performance", "learning", "ux", "ops", "testing"]
}
```

**B) `.docflow-kb/_schema/frontmatter.schema.json`** — JSON Schema Draft-07 que formaliza el §3.3 del PRD. Estructura:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://docflow.dev/kb/frontmatter.schema.json",
  "title": "DocFlow KB Frontmatter",
  "description": "Schema universal para frontmatter YAML de archivos .md en .docflow-kb/. Ver §3.3 de ANALYSIS-knowledge-base-architecture.md",
  "type": "object",
  "required": [
    "id", "type", "lang", "title", "summary", "tags", "audience", "status",
    "created_at", "created_by", "version", "updated_at", "updated_by",
    "source_of_truth", "change_log", "ttl"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Kebab-case, único, estable"
    },
    "type": {
      "type": "string",
      "enum": ["concept", "taxonomy", "resource", "rule", "protocol", "runtime", "incident", "feature", "guide", "state", "audit"]
    },
    "subtype": { "type": ["string", "null"] },
    "lang": { "type": "string", "enum": ["es", "en", "es+en"] },
    "title": {
      "oneOf": [
        { "type": "string" },
        {
          "type": "object",
          "required": ["es", "en"],
          "properties": {
            "es": { "type": "string" },
            "en": { "type": "string" }
          },
          "additionalProperties": false
        }
      ]
    },
    "summary": {
      "oneOf": [
        { "type": "string" },
        {
          "type": "object",
          "required": ["es", "en"],
          "properties": {
            "es": { "type": "string" },
            "en": { "type": "string" }
          },
          "additionalProperties": false
        }
      ]
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 0,
      "uniqueItems": true
    },
    "audience": {
      "type": "array",
      "items": { "type": "string", "enum": ["catbot", "architect", "developer", "user", "onboarding"] },
      "minItems": 1,
      "uniqueItems": true
    },
    "status": {
      "type": "string",
      "enum": ["active", "deprecated", "draft", "experimental", "archived"]
    },
    "created_at": { "type": "string", "format": "date-time" },
    "created_by": { "type": "string" },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "updated_at": { "type": "string", "format": "date-time" },
    "updated_by": { "type": "string" },
    "last_accessed_at": { "type": "string", "format": "date-time" },
    "access_count": { "type": "integer", "minimum": 0 },
    "deprecated_at": { "type": "string", "format": "date-time" },
    "deprecated_by": { "type": "string" },
    "deprecated_reason": { "type": "string" },
    "superseded_by": { "type": "string" },
    "change_log": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["version", "date", "author", "change"],
        "properties": {
          "version": { "type": "string" },
          "date": { "type": "string" },
          "author": { "type": "string" },
          "change": { "type": "string" }
        }
      }
    },
    "source_of_truth": {
      "oneOf": [
        { "type": "null" },
        {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["db", "id"],
            "properties": {
              "db": { "type": "string" },
              "id": { "type": "string" },
              "fields_from_db": {
                "type": "array",
                "items": { "type": "string" }
              }
            }
          }
        }
      ]
    },
    "enriched_fields": {
      "type": "array",
      "items": { "type": "string" }
    },
    "related": {
      "type": "array",
      "items": { "type": "string" }
    },
    "search_hints": {
      "oneOf": [
        {
          "type": "object",
          "required": ["es", "en"],
          "properties": {
            "es": { "type": "array", "items": { "type": "string" } },
            "en": { "type": "array", "items": { "type": "string" } }
          }
        },
        {
          "type": "array",
          "items": { "type": "string" }
        }
      ]
    },
    "ttl": { "type": "string", "enum": ["never", "managed", "30d", "180d"] }
  },
  "allOf": [
    {
      "description": "Si status=deprecated, deprecated_at/by/reason requeridos",
      "if": { "properties": { "status": { "const": "deprecated" } }, "required": ["status"] },
      "then": { "required": ["deprecated_at", "deprecated_by", "deprecated_reason"] }
    },
    {
      "description": "Si ttl=managed, last_accessed_at y access_count requeridos",
      "if": { "properties": { "ttl": { "const": "managed" } }, "required": ["ttl"] },
      "then": { "required": ["last_accessed_at", "access_count"] }
    },
    {
      "description": "Si lang=es+en, title y summary deben ser dict {es,en}",
      "if": { "properties": { "lang": { "const": "es+en" } }, "required": ["lang"] },
      "then": {
        "properties": {
          "title": {
            "type": "object",
            "required": ["es", "en"]
          },
          "summary": {
            "type": "object",
            "required": ["es", "en"]
          }
        }
      }
    }
  ]
}
```

**C) `.docflow-kb/_schema/resource.schema.json`** — schema adicional que extiende frontmatter.schema.json para el subtipo `type: resource`. Mantener mínimo en esta fase (los detalles de cada subtipo se añadirán cuando se migre contenido real en Fase 2/3 del PRD):

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://docflow.dev/kb/resource.schema.json",
  "title": "DocFlow KB Resource (type: resource)",
  "description": "Restricciones adicionales para archivos con type=resource. Extiende frontmatter.schema.json. Subtipos concretos (catpaw, connector, catbrain, etc.) añaden reglas en el futuro.",
  "allOf": [
    { "$ref": "./frontmatter.schema.json" },
    {
      "type": "object",
      "properties": {
        "type": { "const": "resource" },
        "subtype": {
          "type": "string",
          "enum": ["catpaw", "connector", "catbrain", "email-template", "skill", "canvas"]
        },
        "source_of_truth": {
          "description": "Para type=resource, source_of_truth NO puede ser null — todo resource viene de DB",
          "type": "array",
          "minItems": 1
        }
      },
      "required": ["subtype", "source_of_truth"]
    }
  ]
}
```

Los 3 archivos deben ser JSON válido (sin comentarios — JSON puro).
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs"); const fm=JSON.parse(fs.readFileSync(".docflow-kb/_schema/frontmatter.schema.json","utf8")); const tt=JSON.parse(fs.readFileSync(".docflow-kb/_schema/tag-taxonomy.json","utf8")); const rs=JSON.parse(fs.readFileSync(".docflow-kb/_schema/resource.schema.json","utf8")); const reqd=["id","type","lang","title","summary","tags","audience","status","created_at","created_by","version","updated_at","updated_by","source_of_truth","change_log","ttl"]; for(const k of reqd){ if(!fm.required.includes(k)){ console.error("missing required:",k); process.exit(1);} } if(!tt.domains||!tt.entities||!tt.modes||!tt.connectors||!tt.roles||!tt.departments||!tt.rules||!tt.cross_cutting){console.error("tag taxonomy missing category");process.exit(1);} if(tt.roles.length!==7){console.error("roles length != 7");process.exit(1);} if(!rs.allOf||rs.allOf.length<2){console.error("resource schema malformed");process.exit(1);} console.log("OK: fm.required="+fm.required.length+" tt.categories=8 rs.allOf="+rs.allOf.length)'</automated>
  </verify>
  <done>3 archivos JSON válidos. `frontmatter.schema.json` tiene los 16 campos `required` (13 obligatorios + change_log + ttl + source_of_truth tratado como required aun admitiendo null). `tag-taxonomy.json` tiene las 8 categorías con los valores literales del PRD. `resource.schema.json` extiende frontmatter vía `allOf + $ref` y restringe `subtype` a los 6 valores enum.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escribir `scripts/validate-kb.cjs` que valida frontmatter + tags contra los schemas</name>
  <files>scripts/validate-kb.cjs</files>
  <behavior>
    - Ejecutar `node scripts/validate-kb.cjs` recorre `.docflow-kb/**/*.md` excluyendo `_archived/`, `_header.md`, `_manual.md`.
    - Si KB está vacío de archivos validables (solo stubs excluidos), exit 0 con mensaje "OK: 0 archivos validados".
    - Si un archivo tiene frontmatter incompleto (falta campo requerido) → exit 1 con mensaje que lista el archivo y el campo faltante.
    - Si un archivo tiene un tag fuera de la taxonomía → exit 1 con mensaje que lista el tag inválido.
    - Si un archivo tiene `status: deprecated` sin `deprecated_at/by/reason` → exit 1.
    - Si todo OK → exit 0 con cuenta de archivos validados.
    - Archivos `.md` SIN frontmatter (delimitador `---` inicial ausente) → exit 1 con "archivo sin frontmatter".
  </behavior>
  <action>
Escribir `scripts/validate-kb.cjs` como script Node vanilla (sin deps npm). Estructura:

```javascript
#!/usr/bin/env node
/**
 * validate-kb.cjs — Valida todos los archivos .md de .docflow-kb/ contra el schema.
 * Uso: node scripts/validate-kb.cjs
 * Exit 0 si todo OK, exit 1 si algún archivo incumple.
 *
 * No usa dependencias npm — parser YAML mínimo inline + validación manual del schema.
 * Para el futuro, si se añade AJV al repo, reemplazar validateAgainstSchema() por AJV.compile().
 */
const fs = require('fs');
const path = require('path');

const KB_ROOT = path.resolve(__dirname, '..', '.docflow-kb');
const SCHEMA_PATH = path.join(KB_ROOT, '_schema', 'frontmatter.schema.json');
const TAG_TAXONOMY_PATH = path.join(KB_ROOT, '_schema', 'tag-taxonomy.json');

// Archivos excluidos del validador (stubs y archived)
const EXCLUDED_FILENAMES = new Set(['_header.md', '_manual.md']);
const EXCLUDED_DIRS = new Set(['_archived']);

function walk(dir, results = []) {
  if (!fs.existsSync(dir)) return results;
  for (const name of fs.readdirSync(dir)) {
    if (EXCLUDED_DIRS.has(name)) continue;
    const p = path.join(dir, name);
    const stat = fs.statSync(p);
    if (stat.isDirectory()) walk(p, results);
    else if (name.endsWith('.md') && !EXCLUDED_FILENAMES.has(name)) results.push(p);
  }
  return results;
}

// Parser YAML mínimo — solo cubre el subset que usamos:
// - claves kebab/snake con valor string, number, bool, null
// - listas inline [a, b, c] y multiline con "- "
// - dicts simples {k: v}
// - fechas ISO como strings
// - nested 1 nivel: `field:\n  key: value`
function parseYAML(yamlText) {
  const lines = yamlText.split('\n');
  const result = {};
  let i = 0;

  function parseValue(v) {
    v = v.trim();
    if (v === '' || v === 'null' || v === '~') return null;
    if (v === 'true') return true;
    if (v === 'false') return false;
    if (/^-?\d+$/.test(v)) return parseInt(v, 10);
    if (/^-?\d+\.\d+$/.test(v)) return parseFloat(v);
    if (v.startsWith('[') && v.endsWith(']')) {
      const inner = v.slice(1, -1).trim();
      if (!inner) return [];
      return inner.split(',').map(x => parseValue(x));
    }
    if (v.startsWith('{') && v.endsWith('}')) {
      // dict inline — rara vez usado en nuestro frontmatter
      const inner = v.slice(1, -1);
      const d = {};
      for (const pair of inner.split(',')) {
        const [k, val] = pair.split(':').map(s => s.trim());
        d[k] = parseValue(val);
      }
      return d;
    }
    if ((v.startsWith('"') && v.endsWith('"')) || (v.startsWith("'") && v.endsWith("'"))) {
      return v.slice(1, -1);
    }
    return v;
  }

  while (i < lines.length) {
    const line = lines[i];
    if (!line.trim() || line.trim().startsWith('#')) { i++; continue; }

    const indentMatch = line.match(/^(\s*)/);
    const indent = indentMatch[1].length;
    if (indent > 0) { i++; continue; } // nested, se maneja dentro del bloque padre

    const m = line.match(/^([A-Za-z_][A-Za-z0-9_]*):\s*(.*)$/);
    if (!m) { i++; continue; }
    const key = m[1];
    const valStr = m[2];

    if (valStr === '' || valStr === undefined) {
      // Valor multilínea — lista de "- " o dict nested
      const children = [];
      const childDict = {};
      let childMode = null; // 'list' | 'dict'
      i++;
      while (i < lines.length) {
        const child = lines[i];
        if (!child.trim()) { i++; continue; }
        const childIndent = (child.match(/^(\s*)/) || ['', ''])[1].length;
        if (childIndent === 0) break;
        const trimmed = child.trim();
        if (trimmed.startsWith('- ')) {
          childMode = 'list';
          const itemStr = trimmed.slice(2).trim();
          if (itemStr.startsWith('{') && itemStr.endsWith('}')) {
            children.push(parseValue(itemStr));
          } else if (itemStr.includes(':')) {
            // list de dicts multiline no soportado completamente; intentar inline
            children.push(parseValue(itemStr));
          } else {
            children.push(parseValue(itemStr));
          }
        } else {
          childMode = 'dict';
          const mm = trimmed.match(/^([A-Za-z_][A-Za-z0-9_]*):\s*(.*)$/);
          if (mm) childDict[mm[1]] = parseValue(mm[2]);
        }
        i++;
      }
      result[key] = childMode === 'list' ? children : childDict;
    } else {
      result[key] = parseValue(valStr);
      i++;
    }
  }
  return result;
}

function extractFrontmatter(filePath) {
  const content = fs.readFileSync(filePath, 'utf8');
  if (!content.startsWith('---\n') && !content.startsWith('---\r\n')) {
    return { error: 'archivo sin frontmatter (no empieza con ---)', frontmatter: null };
  }
  const end = content.indexOf('\n---', 4);
  if (end === -1) return { error: 'frontmatter sin cierre ---', frontmatter: null };
  const yamlText = content.slice(4, end);
  try {
    return { error: null, frontmatter: parseYAML(yamlText) };
  } catch (e) {
    return { error: `YAML parse error: ${e.message}`, frontmatter: null };
  }
}

function validateAgainstSchema(fm, schema, tagTaxonomy) {
  const errors = [];
  // Campos requeridos top-level
  for (const req of schema.required || []) {
    if (fm[req] === undefined) errors.push(`Falta campo requerido: ${req}`);
  }
  // Enums
  if (fm.type && schema.properties.type.enum && !schema.properties.type.enum.includes(fm.type)) {
    errors.push(`type "${fm.type}" fuera del enum`);
  }
  if (fm.lang && !['es', 'en', 'es+en'].includes(fm.lang)) {
    errors.push(`lang "${fm.lang}" fuera del enum`);
  }
  if (fm.status && !['active', 'deprecated', 'draft', 'experimental', 'archived'].includes(fm.status)) {
    errors.push(`status "${fm.status}" fuera del enum`);
  }
  if (fm.ttl && !['never', 'managed', '30d', '180d'].includes(fm.ttl)) {
    errors.push(`ttl "${fm.ttl}" fuera del enum`);
  }
  // Condiciones
  if (fm.status === 'deprecated') {
    for (const req of ['deprecated_at', 'deprecated_by', 'deprecated_reason']) {
      if (!fm[req]) errors.push(`status=deprecated requiere ${req}`);
    }
  }
  if (fm.ttl === 'managed') {
    if (fm.last_accessed_at === undefined) errors.push(`ttl=managed requiere last_accessed_at`);
    if (fm.access_count === undefined) errors.push(`ttl=managed requiere access_count`);
  }
  if (fm.lang === 'es+en') {
    if (typeof fm.title !== 'object' || !fm.title || !fm.title.es || !fm.title.en) {
      errors.push(`lang=es+en requiere title como dict {es, en}`);
    }
    if (typeof fm.summary !== 'object' || !fm.summary || !fm.summary.es || !fm.summary.en) {
      errors.push(`lang=es+en requiere summary como dict {es, en}`);
    }
  }
  // Audience enum
  const audienceAllowed = ['catbot', 'architect', 'developer', 'user', 'onboarding'];
  if (Array.isArray(fm.audience)) {
    for (const a of fm.audience) {
      if (!audienceAllowed.includes(a)) errors.push(`audience "${a}" fuera del enum`);
    }
  }
  // Version semver
  if (fm.version && !/^\d+\.\d+\.\d+$/.test(fm.version)) {
    errors.push(`version "${fm.version}" no es semver M.m.p`);
  }
  // Tags contra taxonomía
  const allTags = new Set([
    ...tagTaxonomy.domains, ...tagTaxonomy.entities, ...tagTaxonomy.modes,
    ...tagTaxonomy.connectors, ...tagTaxonomy.roles, ...tagTaxonomy.departments,
    ...tagTaxonomy.rules, ...tagTaxonomy.cross_cutting
  ]);
  if (Array.isArray(fm.tags)) {
    for (const t of fm.tags) {
      if (!allTags.has(t)) errors.push(`tag "${t}" no está en tag-taxonomy.json`);
    }
  }
  return errors;
}

function main() {
  if (!fs.existsSync(SCHEMA_PATH)) {
    console.error(`ERROR: ${SCHEMA_PATH} no existe`);
    process.exit(1);
  }
  if (!fs.existsSync(TAG_TAXONOMY_PATH)) {
    console.error(`ERROR: ${TAG_TAXONOMY_PATH} no existe`);
    process.exit(1);
  }
  const schema = JSON.parse(fs.readFileSync(SCHEMA_PATH, 'utf8'));
  const tagTaxonomy = JSON.parse(fs.readFileSync(TAG_TAXONOMY_PATH, 'utf8'));

  const files = walk(KB_ROOT);
  let errorsTotal = 0;
  for (const f of files) {
    const rel = path.relative(KB_ROOT, f);
    const { error, frontmatter } = extractFrontmatter(f);
    if (error) {
      console.error(`FAIL ${rel}: ${error}`);
      errorsTotal++;
      continue;
    }
    const errs = validateAgainstSchema(frontmatter, schema, tagTaxonomy);
    if (errs.length > 0) {
      console.error(`FAIL ${rel}:`);
      for (const e of errs) console.error(`  - ${e}`);
      errorsTotal++;
    }
  }
  if (errorsTotal > 0) {
    console.error(`\nFAILED: ${errorsTotal}/${files.length} archivos no cumplen el schema`);
    process.exit(1);
  }
  console.log(`OK: ${files.length} archivos validados`);
  process.exit(0);
}

main();
```

Después de escribir el script, hacerlo ejecutable: `chmod +x scripts/validate-kb.cjs`.

**Notas de implementación:**
- El parser YAML es intencionalmente mínimo. Soporta el subset del frontmatter documentado (§3.3 del PRD). Si un archivo futuro usa YAML avanzado (anchors, flow style complejo, multiline strings con `|` o `>`), reescribir el parser o migrar a AJV + js-yaml como deps del repo.
- La validación de `allOf` + `$ref` de `resource.schema.json` NO se hace en esta fase (reservado para AJV cuando se integre). El validador cubre los requisitos del §3.3 del PRD + tag taxonomy, que es lo que KB-02 y KB-03 piden.
- Documentar en `.docflow-kb/_manual.md` (Plan 01 Task 2) sección "Validación y CI" que el validador actual es vanilla-node y la integración CI es manual por ahora — ya fue escrito en Plan 01.

**Tests manuales mínimos** (ejecutables después de escribir el script, antes del commit):

```bash
# 1. KB vacío → exit 0 "OK: 0 archivos validados"
node scripts/validate-kb.cjs && echo "TEST1 PASS"

# 2. Archivo válido (bilingüe ejemplo Apéndice A) → exit 0. Crear temporal:
cat > /tmp/test-valid.md <<'EOF'
---
id: catpaw-53f19c51
type: resource
subtype: catpaw
lang: es+en
title:
  es: Operador Holded
  en: Holded Operator
summary:
  es: CatPaw CRM generalista.
  en: Generalist CRM CatPaw.
tags: [catpaw, crm, holded, processor, business, mcp]
audience: [catbot, architect, developer]
status: active
created_at: "2026-04-17T10:23:00Z"
created_by: phase-145
version: 1.0.0
updated_at: "2026-04-17T10:23:00Z"
updated_by: phase-145
last_accessed_at: "2026-04-18T14:00:00Z"
access_count: 12
source_of_truth:
  - { db: cat_paws, id: "53f19c51-9cac-4b23-87ca-cd4d1b30c5ad" }
change_log:
  - { version: 1.0.0, date: "2026-04-17", author: phase-145, change: "Creado" }
ttl: managed
---
body
EOF
cp /tmp/test-valid.md .docflow-kb/resources/catpaws/
node scripts/validate-kb.cjs && echo "TEST2 PASS"
rm .docflow-kb/resources/catpaws/test-valid.md

# 3. Archivo con status:deprecated sin deprecated_at → exit 1
cat > /tmp/test-invalid.md <<'EOF'
---
id: test-bad
type: resource
subtype: catpaw
lang: es
title: Test
summary: test
tags: [catpaw]
audience: [catbot]
status: deprecated
created_at: "2026-04-17T10:23:00Z"
created_by: test
version: 1.0.0
updated_at: "2026-04-17T10:23:00Z"
updated_by: test
source_of_truth: null
change_log:
  - { version: 1.0.0, date: "2026-04-17", author: test, change: "x" }
ttl: never
---
body
EOF
cp /tmp/test-invalid.md .docflow-kb/resources/catpaws/
node scripts/validate-kb.cjs
[ $? -eq 1 ] && echo "TEST3 PASS (exit 1 esperado)"
rm .docflow-kb/resources/catpaws/test-invalid.md

# 4. Archivo con tag fuera de taxonomía → exit 1
# (similar al test 3 pero con tags: [blahfoo])
```

Estos tests manuales se ejecutan como parte de `<verify>`. No hace falta persistir los archivos temporales.
  </action>
  <verify>
    <automated>test -f scripts/validate-kb.cjs && node scripts/validate-kb.cjs && echo "--- TEST valid file ---" && mkdir -p /tmp/kbtest && cp scripts/validate-kb.cjs /tmp/kbtest/ && cat > /tmp/kbtest-valid.md <<'EOF'
---
id: test-valid
type: resource
subtype: catpaw
lang: es
title: Valid
summary: ok
tags: [catpaw, crm]
audience: [catbot]
status: active
created_at: "2026-04-17T10:23:00Z"
created_by: test
version: 1.0.0
updated_at: "2026-04-17T10:23:00Z"
updated_by: test
source_of_truth:
  - { db: cat_paws, id: "53f19c51" }
change_log:
  - { version: 1.0.0, date: "2026-04-17", author: test, change: "x" }
ttl: never
---
body
EOF
cp /tmp/kbtest-valid.md .docflow-kb/resources/catpaws/_test-valid.md && node scripts/validate-kb.cjs && RC1=$? && rm .docflow-kb/resources/catpaws/_test-valid.md && cat > /tmp/kbtest-bad.md <<'EOF'
---
id: test-bad
type: resource
subtype: catpaw
lang: es
title: Bad
summary: bad
tags: [catpaw, foobar_not_in_taxonomy]
audience: [catbot]
status: active
created_at: "2026-04-17T10:23:00Z"
created_by: test
version: 1.0.0
updated_at: "2026-04-17T10:23:00Z"
updated_by: test
source_of_truth:
  - { db: cat_paws, id: "x" }
change_log:
  - { version: 1.0.0, date: "2026-04-17", author: test, change: "x" }
ttl: never
---
EOF
cp /tmp/kbtest-bad.md .docflow-kb/resources/catpaws/_test-bad.md && ! node scripts/validate-kb.cjs 2>&1 | grep -q "foobar_not_in_taxonomy" && RC2=1 || RC2=0 && rm .docflow-kb/resources/catpaws/_test-bad.md && [ $RC1 -eq 0 ] && [ $RC2 -eq 0 ] && echo "ALL TESTS PASS"</automated>
  </verify>
  <done>`scripts/validate-kb.cjs` existe, es ejecutable, KB vacío pasa (exit 0). Un archivo válido con frontmatter completo pasa. Un archivo con tag fuera de taxonomía falla con mensaje "tag ... no está en tag-taxonomy.json" y exit 1. El parser YAML maneja dicts multilinea (ej. `title:\n  es: ...\n  en: ...`), listas inline `[a, b, c]`, y entries de `change_log` en formato `{ k: v, k: v }` inline.</done>
</task>

</tasks>

<verification>
**Check global tras los dos tasks:**

```bash
# Schemas válidos como JSON
node -e 'JSON.parse(require("fs").readFileSync(".docflow-kb/_schema/frontmatter.schema.json"))' && \
node -e 'JSON.parse(require("fs").readFileSync(".docflow-kb/_schema/tag-taxonomy.json"))' && \
node -e 'JSON.parse(require("fs").readFileSync(".docflow-kb/_schema/resource.schema.json"))'

# Validador sobre KB vacío (solo stubs + schemas excluidos) → exit 0
node scripts/validate-kb.cjs

# Tag taxonomy tiene exactamente las 8 categorías
node -e 'const tt=JSON.parse(require("fs").readFileSync(".docflow-kb/_schema/tag-taxonomy.json")); console.log(Object.keys(tt).sort().join(","))'
# Debe imprimir: connectors,cross_cutting,departments,domains,entities,modes,roles,rules
```
</verification>

<success_criteria>
1. `.docflow-kb/_schema/frontmatter.schema.json` existe, es JSON válido Draft-07, tiene en `required` al menos los 13 campos del §3.3 del PRD + `change_log` + `ttl` + `source_of_truth` (total ≥16).
2. `.docflow-kb/_schema/tag-taxonomy.json` contiene LITERAL las 8 categorías del §3.4 del PRD con los valores exactos.
3. `.docflow-kb/_schema/resource.schema.json` extiende frontmatter via `allOf + $ref` y restringe `subtype` a 6 valores enum.
4. `scripts/validate-kb.cjs` existe y es ejecutable. Sobre un KB vacío sale con exit 0 y mensaje "OK: N archivos validados".
5. El validador rechaza explícitamente: tag fuera de taxonomía, status=deprecated sin deprecated_at/by/reason, ttl=managed sin last_accessed_at, version que no es semver, archivo sin frontmatter.
6. El validador NO falla sobre los stubs del KB (no inspecciona `_header.md`, `_manual.md`, ni `_archived/`).
</success_criteria>

<output>
Al completar, crear `.planning/phases/149-kb-foundation-bootstrap/149-02-SUMMARY.md` con:
- Paths de los 3 schemas creados
- Resumen del validador (líneas, approach sin AJV, cómo se integra en CI cuando CI exista)
- Resultado de tests manuales (archivo válido pasa, archivo bad falla con mensaje correcto)
- Decisión técnica: parser YAML vanilla vs dependencia, razón (repo root sin package.json)
</output>
