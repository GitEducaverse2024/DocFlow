---
phase: 149-kb-foundation-bootstrap
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/MILESTONE-CONTEXT.md
  - .planning/reference/auditoria-catflow.md
autonomous: false
requirements: [KB-01]
must_haves:
  truths:
    - "`.planning/MILESTONE-CONTEXT-AUDIT.md` eliminado (verificado duplicado literal de `.planning/AUDIT-respuestas-funnel-completo.md` via `diff`)"
    - "`milestone-v29-revisado.md` (raíz) fusionado en `.planning/MILESTONE-CONTEXT.md` — el revisado reemplaza, no suma"
    - "`milestone-v29-revisado.md` (raíz) eliminado tras la fusión"
    - "`auditoria-catflow.md` (raíz) movido a `.planning/reference/auditoria-catflow.md`"
    - "`.planning/reference/` existe como directorio"
    - "Root del repo queda limpio de los 2 archivos movidos/borrados"
    - "Git history preserva el contenido original (visible en git log)"
  artifacts:
    - path: ".planning/MILESTONE-CONTEXT.md"
      provides: "Contexto activo del milestone v29, fusionado con contenido de milestone-v29-revisado.md"
    - path: ".planning/reference/auditoria-catflow.md"
      provides: "Auditoría técnica de CatFlow (movida desde root)"
    - path: ".planning/reference/"
      provides: "Directorio de referencias arquitectónicas"
  key_links:
    - from: ".planning/reference/auditoria-catflow.md"
      to: ".planning/Index.md"
      via: "entry en tabla de auditorías (actualizado por Plan 01 Task 3)"
      pattern: "auditoria-catflow"
---

<objective>
Ejecutar las operaciones inmediatas de limpieza del §D.2 del PRD: borrar un duplicado literal, fusionar un archivo de root en el activo `.planning/MILESTONE-CONTEXT.md`, y mover `auditoria-catflow.md` del root a `.planning/reference/`. Cada operación exige una verificación previa de seguridad (diff, confirmación) — nada se pierde silenciosamente.

Purpose: Deliverable #6 de Phase 149 (parte de la cobertura KB-01). Deja root limpio y `.planning/` coherente antes de la migración de contenido en fases posteriores.
Output: 1 archivo borrado, 1 fusionado+borrado, 1 movido, 1 directorio nuevo. Commit individual por cada operación.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/149-kb-foundation-bootstrap/149-CONTEXT.md
@.planning/ANALYSIS-knowledge-base-architecture.md

<interfaces>
<!-- Estado actual verificado (2026-04-18) -->

```
Archivos implicados (sizes confirmados):

.planning/MILESTONE-CONTEXT-AUDIT.md           (124341 bytes)  ← duplicado literal
.planning/AUDIT-respuestas-funnel-completo.md  (124341 bytes)  ← ORIGINAL (se preserva)

milestone-v29-revisado.md                      (13647 bytes, root)   ← fusionar+borrar
.planning/MILESTONE-CONTEXT.md                 (38924 bytes, activo) ← recibe contenido fusionado

auditoria-catflow.md                           (23592 bytes, root)   ← mover

.planning/reference/                           ← NO EXISTE, crear
```

Diff previamente verificado:
```bash
diff -q .planning/MILESTONE-CONTEXT-AUDIT.md .planning/AUDIT-respuestas-funnel-completo.md
# exit 0, sin output → SON IDÉNTICOS
```

<!-- Semántica de la fusión milestone-v29-revisado.md → MILESTONE-CONTEXT.md -->

Según §D.2 del PRD: "El revisado **reemplaza, no suma** (versión post-piloto v28 con los 3 bugs canvas-executor + restricciones aplicadas)".

Interpretación operativa: `milestone-v29-revisado.md` es LA versión canónica del milestone context tras el piloto v28. El activo `.planning/MILESTONE-CONTEXT.md` quedó desactualizado y debe ser reemplazado con el contenido del revisado, ajustando SI Y SOLO SI hay secciones en el actual `MILESTONE-CONTEXT.md` que son únicas y siguen vigentes (ej. referencias live a fases 145-148, progreso actual).

Flujo seguro:
1. Checkpoint human-verify: leer ambos archivos, presentar al usuario un resumen comparativo, pedir confirmación sobre si alguna sección del actual `MILESTONE-CONTEXT.md` DEBE preservarse contra el reemplazo (ej. notas post-145).
2. Si confirma "reemplazo completo": copiar contenido de revisado a MILESTONE-CONTEXT, borrar revisado.
3. Si indica "preservar X": hacer fusión dirigida (revisado como base + secciones X del actual).

NO automatizar la decisión sin checkpoint — el usuario definió el revisado como "reemplaza, no suma" pero el activo `MILESTONE-CONTEXT.md` puede haber evolucionado desde el revisado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar y borrar duplicado literal `.planning/MILESTONE-CONTEXT-AUDIT.md`</name>
  <files>.planning/MILESTONE-CONTEXT-AUDIT.md</files>
  <action>
**Safety check antes del borrado:**

```bash
diff -q .planning/MILESTONE-CONTEXT-AUDIT.md .planning/AUDIT-respuestas-funnel-completo.md
# Debe imprimir NADA y exit 0 (idénticos).
echo $?  # Debe ser 0.
```

Si el exit code es 0 y no hay diff output → son idénticos → seguro borrar:

```bash
git rm .planning/MILESTONE-CONTEXT-AUDIT.md
git commit -m "chore(149-05): delete duplicate MILESTONE-CONTEXT-AUDIT.md (identical to AUDIT-respuestas-funnel-completo.md)"
```

Si el exit code es distinto de 0 o hay diff output → ESCALAR al usuario:
- Imprimir el diff completo.
- Parar la ejecución con mensaje: "ABORT: MILESTONE-CONTEXT-AUDIT.md diverge de AUDIT-respuestas-funnel-completo.md. No es duplicado literal. Decisión de usuario requerida."
- No hacer nada más en este task.

El historial queda en git — el borrado es reversible.
  </action>
  <verify>
    <automated>test ! -f .planning/MILESTONE-CONTEXT-AUDIT.md && test -f .planning/AUDIT-respuestas-funnel-completo.md && git log --oneline -1 .planning/MILESTONE-CONTEXT-AUDIT.md 2>/dev/null | head -1</automated>
  </verify>
  <done>`.planning/MILESTONE-CONTEXT-AUDIT.md` ya no existe. `.planning/AUDIT-respuestas-funnel-completo.md` sigue intacto. Commit creado con mensaje explicativo. El borrado es reversible via `git show HEAD:.planning/MILESTONE-CONTEXT-AUDIT.md` hasta que se garbage-collect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — confirmar estrategia de fusión milestone-v29-revisado → MILESTONE-CONTEXT</name>
  <files>milestone-v29-revisado.md, .planning/MILESTONE-CONTEXT.md</files>
  <action>
Presentar al usuario una comparación estructural de `milestone-v29-revisado.md` (root, 13647 bytes) y `.planning/MILESTONE-CONTEXT.md` (activo, 38924 bytes — ~3x más grande). Ejecutar estos comandos y mostrar su output:

```bash
wc -l milestone-v29-revisado.md .planning/MILESTONE-CONTEXT.md
grep -E "^## " milestone-v29-revisado.md
grep -E "^## " .planning/MILESTONE-CONTEXT.md
```

Después, pedir al usuario que elija una de las 3 opciones (A/B/C) listadas en `<how-to-verify>` y proporcione la instrucción de resume.

**NO** sobrescribir nada en este task. La sobrescritura es trabajo de Task 3, tras la decisión.
  </action>
  <verify>
    <automated>echo "checkpoint — awaiting human decision (A/B/C)"</automated>
  </verify>
  <done>Usuario ha proporcionado la instrucción de resume en forma "A" / "B: preservar [secciones]" / "C". El siguiente task (Task 3) tiene instrucciones claras sobre cómo proceder.</done>
  <what-built>
No se ha construido nada todavía para este task. Es un checkpoint de decisión antes de una operación que modifica un documento activo del proyecto (`.planning/MILESTONE-CONTEXT.md`).

El PRD declara que `milestone-v29-revisado.md` (root, 13647 bytes) "reemplaza, no suma" al activo `.planning/MILESTONE-CONTEXT.md` (38924 bytes). Sin embargo el activo es **~3x más grande** que el revisado, lo que sugiere que puede contener secciones válidas añadidas POST-revisado (ej. progreso de fases 145-148, decisiones recientes).

Antes de sobrescribir, el usuario debe decidir la estrategia exacta.
  </what-built>
  <how-to-verify>
1. **Leer ambos archivos completos:**
   ```bash
   wc -l milestone-v29-revisado.md .planning/MILESTONE-CONTEXT.md
   cat milestone-v29-revisado.md
   cat .planning/MILESTONE-CONTEXT.md
   ```

2. **Comparar estructura (secciones de primer nivel):**
   ```bash
   grep -E "^## " milestone-v29-revisado.md
   grep -E "^## " .planning/MILESTONE-CONTEXT.md
   ```

3. **Decidir uno de estos caminos:**

   **Opción A — Reemplazo total (estricto al PRD):**
   - Copiar LITERAL el contenido de `milestone-v29-revisado.md` a `.planning/MILESTONE-CONTEXT.md`.
   - Borrar `milestone-v29-revisado.md`.
   - Asumir que cualquier info en el viejo `MILESTONE-CONTEXT.md` que no esté en el revisado se pierde (el git log la preserva).

   **Opción B — Fusión dirigida:**
   - Base: contenido del revisado (es la versión post-piloto v28 con restricciones correctas).
   - Añadir secciones específicas del actual `MILESTONE-CONTEXT.md` que: (a) son posteriores al revisado, (b) siguen vigentes (no obsoletas por fases 145+), (c) NO están reflejadas en el revisado.
   - Listar explícitamente las secciones preservadas: "Preserved from current MILESTONE-CONTEXT.md: §[X], §[Y]".

   **Opción C — Preservar actual + archivar revisado:**
   - Si el actual es más fresco de lo que el PRD asume → no hacer la fusión.
   - Mover `milestone-v29-revisado.md` a `.docflow-legacy/milestone-retrospectives/` (creado en Plan 01).
   - Dejar MILESTONE-CONTEXT.md intacto.

4. **Responder con el camino elegido** + (si Opción B) lista explícita de secciones a preservar.
  </how-to-verify>
  <resume-signal>Responder: "A" / "B: preservar [secciones]" / "C" — y proporcionar cualquier instrucción adicional sobre ediciones a hacer durante la fusión.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Ejecutar la estrategia de fusión elegida y borrar milestone-v29-revisado.md</name>
  <files>
    .planning/MILESTONE-CONTEXT.md,
    milestone-v29-revisado.md
  </files>
  <action>
**Implementación según la decisión del checkpoint Task 2:**

**Si Opción A — Reemplazo total:**
```bash
cp milestone-v29-revisado.md .planning/MILESTONE-CONTEXT.md
git rm milestone-v29-revisado.md
git add .planning/MILESTONE-CONTEXT.md
git commit -m "chore(149-05): replace MILESTONE-CONTEXT.md with milestone-v29-revisado content, delete root copy"
```

**Si Opción B — Fusión dirigida:**
1. Leer el revisado como base.
2. Leer el actual `.planning/MILESTONE-CONTEXT.md`.
3. Identificar las secciones a preservar listadas por el usuario en el resume signal.
4. Construir nuevo `MILESTONE-CONTEXT.md`: revisado como base + secciones preservadas añadidas en lugar apropiado (al final, o bajo cabecera `## Post-revision additions`).
5. Al principio del archivo fusionado, añadir:
   ```markdown
   <!-- Merged in Phase 149-05 (2026-04-18): base = milestone-v29-revisado.md; preserved sections from prior MILESTONE-CONTEXT.md: [list] -->
   ```
6. Borrar el archivo raíz.
7. Commit:
   ```bash
   git rm milestone-v29-revisado.md
   git add .planning/MILESTONE-CONTEXT.md
   git commit -m "chore(149-05): merge milestone-v29-revisado into MILESTONE-CONTEXT, preserving [sections]"
   ```

**Si Opción C — Preservar actual + archivar revisado:**
```bash
# .docflow-legacy/ y milestone-retrospectives/ creados por Plan 01
mkdir -p .docflow-legacy/milestone-retrospectives
git mv milestone-v29-revisado.md .docflow-legacy/milestone-retrospectives/milestone-v29-revisado.md
# Añadir entry al _migration-log.md de legacy
cat >> .docflow-legacy/_migration-log.md <<'EOF'

### 2026-04-18 · milestone-v29-revisado.md
- **Origen:** / (root)
- **Destino:** .docflow-legacy/milestone-retrospectives/milestone-v29-revisado.md
- **Insight preservado en:** _(parcialmente en `.planning/MILESTONE-CONTEXT.md` actual; el revisado se conserva como snapshot post-piloto v28)_
- **Autor:** phase-149-05
- **Razón:** El activo MILESTONE-CONTEXT.md ha evolucionado post-revisado; se conserva el revisado como referencia histórica del estado del milestone tras el piloto v28.
EOF
git add .docflow-legacy/_migration-log.md .planning/MILESTONE-CONTEXT.md
git commit -m "chore(149-05): archive milestone-v29-revisado.md to legacy (activo MILESTONE-CONTEXT preserved)"
```

**En TODAS las opciones:** verificar al final que `milestone-v29-revisado.md` ya no existe en el root del repo (o fue movido a legacy en Opción C).
  </action>
  <verify>
    <automated>test ! -f milestone-v29-revisado.md && test -f .planning/MILESTONE-CONTEXT.md && [ $(wc -l < .planning/MILESTONE-CONTEXT.md) -gt 0 ]</automated>
  </verify>
  <done>`milestone-v29-revisado.md` ya no existe en root (borrado en Opción A/B, o movido a `.docflow-legacy/milestone-retrospectives/` en Opción C). `.planning/MILESTONE-CONTEXT.md` tiene el contenido resultante. Commit creado con mensaje que refleja la estrategia elegida.</done>
</task>

<task type="auto">
  <name>Task 4: Crear `.planning/reference/` y mover `auditoria-catflow.md` desde root</name>
  <files>
    auditoria-catflow.md,
    .planning/reference/auditoria-catflow.md
  </files>
  <action>
Operación atómica, sin riesgo:

```bash
mkdir -p .planning/reference
git mv auditoria-catflow.md .planning/reference/auditoria-catflow.md
```

El `git mv` preserva el historial del archivo.

Añadir un `.gitkeep` si `.planning/reference/` quedara vacío en algún escenario, pero no es necesario porque ya contiene `auditoria-catflow.md`.

Commit:
```bash
git add .planning/reference/auditoria-catflow.md
git commit -m "chore(149-05): move auditoria-catflow.md from root to .planning/reference/"
```

Verificar que el archivo es legible desde el nuevo path:
```bash
head -5 .planning/reference/auditoria-catflow.md
```

Plan 01 Task 3 ya añadió el puntero a `.planning/Index.md` referenciando este path — coherente.
  </action>
  <verify>
    <automated>test ! -f auditoria-catflow.md && test -f .planning/reference/auditoria-catflow.md && [ $(wc -l < .planning/reference/auditoria-catflow.md) -gt 10 ] && git log --follow --oneline .planning/reference/auditoria-catflow.md 2>/dev/null | wc -l | awk '{ if ($1 > 0) exit 0; else exit 1 }'</automated>
  </verify>
  <done>`auditoria-catflow.md` ya no existe en root. `.planning/reference/auditoria-catflow.md` existe con el mismo contenido. Git preserva el histórico (verificable con `git log --follow`). `.planning/Index.md` ya apunta al nuevo path (trabajo de Plan 01).</done>
</task>

</tasks>

<verification>
**Check root del repo está limpio:**
```bash
ls /home/deskmath/docflow/ | grep -E "^(milestone-v29-revisado|auditoria-catflow)\.md$"
# No debe imprimir nada.
```

**Check `.planning/` coherente:**
```bash
ls .planning/MILESTONE-CONTEXT-AUDIT.md 2>&1 | grep -q "No such" && echo "OK: MILESTONE-CONTEXT-AUDIT borrado"
test -f .planning/MILESTONE-CONTEXT.md && echo "OK: MILESTONE-CONTEXT vivo"
test -f .planning/reference/auditoria-catflow.md && echo "OK: auditoria-catflow movido"
```

**Check git log preserva contenido borrado/movido:**
```bash
git log --oneline -3 .planning/MILESTONE-CONTEXT-AUDIT.md 2>&1 | head -3  # Debe listar commits históricos
git log --follow --oneline .planning/reference/auditoria-catflow.md 2>&1 | head -5  # Debe seguir al archivo de su ubicación anterior
```

**Check Index.md referencia el nuevo path (ya hecho en Plan 01 Task 3):**
```bash
grep "reference/auditoria-catflow" .planning/Index.md
# Debe imprimir la línea.
```
</verification>

<success_criteria>
1. `.planning/MILESTONE-CONTEXT-AUDIT.md` eliminado del filesystem actual (git preserva el histórico).
2. `milestone-v29-revisado.md` eliminado del root (o movido a `.docflow-legacy/` si se eligió Opción C en el checkpoint).
3. `.planning/MILESTONE-CONTEXT.md` refleja la estrategia elegida (reemplazado / fusionado / preservado).
4. `.planning/reference/` existe como directorio.
5. `.planning/reference/auditoria-catflow.md` existe y es legible; `auditoria-catflow.md` ya NO existe en root.
6. Cada operación produce un commit atómico con mensaje explicativo (chore(149-05): ...).
7. El diff previo confirmó la idempotencia del borrado del duplicado (exit 0); si hubiera divergido, el task 1 hubiera escalado al usuario.
8. No se sobrescribe contenido del activo `MILESTONE-CONTEXT.md` sin pasar por el checkpoint human-verify.
</success_criteria>

<output>
Al completar, crear `.planning/phases/149-kb-foundation-bootstrap/149-05-SUMMARY.md` con:
- Output del `diff` verificando que MILESTONE-CONTEXT-AUDIT.md era duplicado (exit 0)
- Estrategia elegida en el checkpoint (A / B / C) + razón
- Lista de secciones preservadas (si Opción B) o archivo movido a legacy (si Opción C)
- Paths finales (`ls -la` de los archivos implicados antes y después)
- Commits creados (hash + mensaje) — deberían ser 3-4 commits atómicos
</output>
