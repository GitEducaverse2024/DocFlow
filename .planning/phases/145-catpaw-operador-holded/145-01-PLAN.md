---
phase: 145-catpaw-operador-holded
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/knowledge/catpaw.json
  - .planning/knowledge/catpaw-catalog.md
  - .planning/knowledge/connectors-catalog.md
autonomous: false
requirements: [CRM-01, CRM-02, CRM-03, CRM-04]

must_haves:
  truths:
    - "CatPaw 'Operador Holded' existe en /agents con modo processor, modelo gemini-main, conector Holded MCP vinculado"
    - "Operador Holded busca leads en Holded cuando recibe instruccion de busqueda (holded_search_lead)"
    - "Operador Holded busca contactos en Holded cuando recibe instruccion de busqueda (holded_search_contact)"
    - "Operador Holded crea un lead nuevo con funnelId de holded_list_funnels"
    - "Operador Holded anade notas a leads existentes via holded_create_lead_note con title y desc"
    - "connectors-catalog.md tiene la regla 7 indicando usar Operador Holded para canvas CRM generalista"
  artifacts:
    - path: "CatPaw in SQLite (cat_paws table)"
      provides: "Operador Holded agent record"
    - path: "cat_paw_connectors row"
      provides: "Holded MCP connector linked to Operador Holded"
    - path: "app/data/knowledge/catpaw.json"
      provides: "Knowledge tree updated with Operador Holded concept"
    - path: ".planning/knowledge/catpaw-catalog.md"
      provides: "Catalog updated with Operador Holded entry"
    - path: ".planning/knowledge/connectors-catalog.md"
      provides: "Regla 7 para canvas CRM generalista con Operador Holded"
      contains: "Operador Holded"
  key_links:
    - from: "CatPaw Operador Holded"
      to: "Holded MCP connector (seed-holded-mcp)"
      via: "cat_paw_connectors table"
      pattern: "connector_id.*seed-holded-mcp"
    - from: "CatPaw system_prompt"
      to: "MCP tools (holded_search_lead, holded_search_contact, holded_create_lead, holded_list_funnels, holded_create_lead_note)"
      via: "system_prompt instructs agent to use these tools"
      pattern: "holded_search_lead|holded_create_lead|holded_create_lead_note"
---

<objective>
Crear el CatPaw "Operador Holded" generalista con system_prompt amplio para cualquier operacion CRM en Holded, vincular el conector Holded MCP, y actualizar la documentacion.

Purpose: Este CatPaw sera el nodo CRM Handler en el canvas Inbound+CRM de Phase 146. Debe ser generalista (no rigido a un tipo de operacion como el Consultor CRM existente).
Output: CatPaw funcional en /agents con conector Holded MCP, capaz de buscar/crear/actualizar leads y contactos y anadir notas.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/knowledge/catpaw-catalog.md
@.planning/knowledge/connectors-catalog.md
@.planning/knowledge/holded-mcp-api.md

<interfaces>
<!-- API para crear CatPaw -->
POST /api/cat-paws
Body: { name, description, department, system_prompt, tone, mode, model, temperature, max_tokens, output_format, is_active, avatar_emoji, avatar_color }
Response: 201 { id, name, ... }

<!-- API para vincular conector -->
POST /api/cat-paws/{id}/connectors
Body: { connector_id, usage_hint, is_active }
Response: 201 { paw_id, connector_id, ... }

<!-- API para listar conectores del sistema -->
GET /api/connectors
Response: 200 [ { id, name, ... }, ... ]

<!-- API para obtener conectores de un CatPaw -->
GET /api/cat-paws/{id}/connectors
Response: 200 [ { connector_id, usage_hint, ... }, ... ]

<!-- Holded MCP connector ID -->
seed-holded-mcp

<!-- Tools MCP disponibles para CRM (cargadas dinamicamente del MCP server) -->
holded_search_contact(query) - buscar contactos fuzzy
holded_search_lead(query, funnelId?) - buscar leads por nombre
holded_create_lead(name, contact?, funnelId?, stageId?) - crear lead
holded_list_funnels() - listar pipelines con stages
holded_create_lead_note(leadId, title, desc?) - anadir nota a lead
holded_update_lead(leadId, stageId?, value?, status?, name?) - actualizar lead
create_contact(name, email?, phone?, type?, note?, tradename?) - crear contacto
update_contact(contactId, name?, email?, phone?, type?, note?) - actualizar contacto
list_contacts(page?, limit?) - listar contactos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear CatPaw Operador Holded y vincular conector Holded MCP</name>
  <files>app/data/knowledge/catpaw.json, .planning/knowledge/catpaw-catalog.md, .planning/knowledge/connectors-catalog.md</files>
  <action>
PREFLIGHT: Antes de cualquier creacion, verificar que el conector seed-holded-mcp existe en el sistema:
   - curl -s http://localhost:3500/api/connectors | python3 -c "import sys,json; conns=json.load(sys.stdin); ids=[c['id'] for c in conns]; assert 'seed-holded-mcp' in ids, f'FATAL: seed-holded-mcp not found in connectors. Available: {ids}'; print('OK: seed-holded-mcp exists')"
   - Si el preflight falla, DETENERSE y reportar. No continuar sin el conector presente.

1. Crear el CatPaw via API call a POST /api/cat-paws con estos datos exactos:
   - name: "Operador Holded"
   - description: "Operador CRM generalista para Holded. Ejecuta cualquier operacion CRM: buscar leads y contactos, crear leads nuevos con funnelId, actualizar leads, anadir notas a leads. Recibe instrucciones en lenguaje natural y usa las herramientas MCP de Holded para ejecutarlas."
   - department: "business"
   - mode: "processor"
   - model: "gemini-main"
   - temperature: 0.2
   - max_tokens: 4096
   - output_format: "json"
   - is_active: 1
   - avatar_emoji: (elegir algo relacionado con CRM, ej. clipboard o briefcase)
   - avatar_color: "#2563EB" (azul corporativo)
   - system_prompt: Un prompt generalista con esta estructura:

```
ROL: Eres el Operador Holded. Un agente CRM generalista que ejecuta CUALQUIER operacion en Holded CRM usando herramientas MCP.

MISION: Recibes instrucciones en lenguaje natural describiendo una operacion CRM. Tu trabajo es:
1. Interpretar la instruccion
2. Decidir que herramientas MCP usar
3. Ejecutarlas en el orden correcto
4. Devolver el resultado estructurado en JSON

HERRAMIENTAS DISPONIBLES:
- holded_search_lead: Buscar leads por nombre o contacto
- holded_search_contact: Buscar contactos por nombre, email o NIF
- holded_create_lead: Crear lead nuevo (requiere name; funnelId opcional, obtener de holded_list_funnels si no se proporciona)
- holded_list_funnels: Listar pipelines/funnels con stages (SIEMPRE llamar primero si necesitas funnelId)
- holded_create_lead_note: Anadir nota a un lead (requiere leadId, title; desc opcional)
- holded_update_lead: Actualizar lead (stageId, value, status, name)
- create_contact: Crear contacto nuevo en Holded
- update_contact: Actualizar contacto existente
- list_contacts: Listar contactos con paginacion

PROCESO DE DECISION:
- Si la instruccion pide BUSCAR: usar holded_search_lead y/o holded_search_contact segun el contexto
- Si la instruccion pide CREAR LEAD: primero holded_list_funnels para obtener funnelId, luego holded_create_lead
- Si la instruccion pide ANADIR NOTA: usar holded_create_lead_note con title descriptivo y desc con el contenido
- Si la instruccion pide CREAR CONTACTO: usar create_contact
- Si la instruccion pide ACTUALIZAR: usar holded_update_lead o update_contact segun corresponda
- Si la instruccion es COMPUESTA (buscar + crear si no existe): ejecutar secuencialmente

REGLAS:
- NUNCA inventar datos. Si no tienes un ID, buscalo primero.
- Si necesitas funnelId y no lo tienes, llama holded_list_funnels ANTES.
- Si necesitas leadId y no lo tienes, llama holded_search_lead ANTES.
- Si la busqueda devuelve 0 resultados, informa claramente (no crear automaticamente sin instruccion explicita).
- Toda nota debe tener title descriptivo (no generico).

OUTPUT: Siempre JSON con esta estructura:
{
  "crm_action": "search_lead" | "search_contact" | "create_lead" | "create_note" | "update_lead" | "create_contact" | "update_contact" | "compound",
  "success": true | false,
  "result": { ... datos relevantes de la operacion ... },
  "lead_id": "..." (si aplica),
  "contact_id": "..." (si aplica),
  "summary": "Descripcion breve de lo ejecutado"
}
```

2. Guardar el ID del CatPaw creado.

3. Vincular el conector Holded MCP via POST /api/cat-paws/{id}/connectors:
   - connector_id: "seed-holded-mcp"
   - usage_hint: "CRM operations: search/create/update leads and contacts, add notes, list funnels"
   - is_active: 1

4. Actualizar .planning/knowledge/catpaw-catalog.md:
   - Anadir entrada #31 "Operador Holded" en la tabla indice con: Negocio, processor, gemini-main, 0 skills, 1 conector
   - Anadir seccion detallada con ID real, todos los campos, descripcion y system prompt resumido
   - Actualizar el total a 31 agentes

5. Actualizar .planning/knowledge/connectors-catalog.md:
   - En "Reglas criticas para Canvas", anadir regla 7: "Para operaciones CRM generalistas en canvas (buscar/crear/actualizar leads, notas): usar CatPaw **Operador Holded** (id: {id real}). El Consultor CRM (b63164ed) tiene system_prompt rigido — NO usar para canvas Inbound+CRM."

6. Actualizar app/data/knowledge/catpaw.json:
   - En "concepts", anadir: "CatPaw 'Operador Holded' es el agente CRM generalista para canvas. Acepta cualquier instruccion CRM en lenguaje natural. Usar en vez de 'Consultor CRM' para pipelines flexibles."
   - En "howto", anadir: "Para operaciones CRM en canvas: usar CatPaw 'Operador Holded' con conector Holded MCP. Recibe instruccion natural, ejecuta herramientas MCP (search, create, update leads/contacts/notes), devuelve JSON estructurado."

IMPORTANTE: Las llamadas API deben hacerse con curl a http://localhost:3500 (o la URL del server DocFlow local).
  </action>
  <verify>
    <automated>
# Step 1: Verificar CatPaw existe con modo correcto
PAW_ID=$(curl -s http://localhost:3500/api/cat-paws?mode=processor | python3 -c "
import sys,json
paws=json.load(sys.stdin)
matches=[p for p in paws if 'Operador Holded' in p.get('name','')]
assert len(matches)==1, f'Expected 1 Operador Holded, found {len(matches)}'
p=matches[0]
assert p['mode']=='processor', f'Expected mode=processor, got {p[\"mode\"]}'
print(p['id'])
") && \
# Step 2: Verificar conector vinculado es exactamente seed-holded-mcp
curl -s "http://localhost:3500/api/cat-paws/${PAW_ID}/connectors" | python3 -c "
import sys,json
conns=json.load(sys.stdin)
conn_ids=[c['connector_id'] for c in conns]
assert 'seed-holded-mcp' in conn_ids, f'seed-holded-mcp NOT linked. Found: {conn_ids}'
print('OK: seed-holded-mcp connector linked')
" && \
# Step 3: Verificar system_prompt contiene tool names criticos
curl -s "http://localhost:3500/api/cat-paws/${PAW_ID}" | python3 -c "
import sys,json
paw=json.load(sys.stdin)
prompt=paw.get('system_prompt','')
for tool in ['holded_search_lead','holded_create_lead_note','holded_create_lead','holded_list_funnels']:
    assert tool in prompt, f'system_prompt missing tool: {tool}'
print('OK: system_prompt contains all required MCP tool names')
" && \
# Step 4: Verificar connectors-catalog.md tiene regla 7
python3 -c "
content=open('.planning/knowledge/connectors-catalog.md').read()
assert 'Operador Holded' in content, 'connectors-catalog.md missing Operador Holded rule'
assert 'regla 7' in content.lower() or 'Regla 7' in content or '7.' in content, 'connectors-catalog.md missing rule 7 marker'
print('OK: connectors-catalog.md has Operador Holded rule')
" && echo "ALL CHECKS PASSED: Operador Holded id=${PAW_ID}"
    </automated>
  </verify>
  <done>CatPaw "Operador Holded" existe en /agents como processor con gemini-main, conector seed-holded-mcp vinculado (verificado por ID exacto), system_prompt contiene holded_search_lead/holded_create_lead_note/holded_create_lead/holded_list_funnels, y documentacion actualizada en catpaw-catalog.md, connectors-catalog.md (regla 7) y catpaw.json</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar Operador Holded con CatBot (4 escenarios CRM)</name>
  <files>ninguno (verificacion manual)</files>
  <action>
Checkpoint de verificacion humana. CatBot debe poder ejecutar el Operador Holded en los 4 escenarios CRM requeridos. El usuario ejecuta los tests descritos abajo y confirma o reporta fallos.
  </action>
  <verify>
    <automated>MISSING — verificacion humana via CatBot, no automatizable</automated>
  </verify>
  <done>Los 4 escenarios CRM verificados con CatBot: buscar leads/contactos (CRM-02), crear lead con funnelId (CRM-03), anadir nota a lead (CRM-04), y existencia visual del CatPaw (CRM-01). Evidencia pegada por el usuario.</done>
  <what-built>CatPaw "Operador Holded" creado con system_prompt generalista y conector Holded MCP. Debe poder ejecutar las 4 operaciones CRM requeridas.</what-built>
  <how-to-verify>
Ejecutar estos 4 tests via CatBot (Telegram o web chat) para verificar los 4 requirements:

**Test CRM-02 -- Buscar leads/contactos:**
Prompt a CatBot: "Ejecuta el CatPaw Operador Holded con esta instruccion: 'Busca si existe algun lead o contacto de Educa360 en Holded'"
Esperado: CatBot ejecuta el Operador Holded, que llama holded_search_lead y/o holded_search_contact, y devuelve resultados JSON.

**Test CRM-03 -- Crear lead nuevo con funnelId:**
Prompt a CatBot: "Ejecuta el CatPaw Operador Holded con esta instruccion: 'Crea un lead de prueba llamado TEST-v29-Operador con el primer funnel disponible'"
Esperado: CatBot ejecuta el Operador, que primero llama holded_list_funnels para obtener funnelId, luego holded_create_lead con name="TEST-v29-Operador" y el funnelId obtenido. Devuelve JSON con lead_id.

**Test CRM-04 -- Anadir nota a lead:**
Prompt a CatBot: "Ejecuta el CatPaw Operador Holded con esta instruccion: 'Anade una nota al lead TEST-v29-Operador con titulo Verificacion v29 y descripcion Lead de prueba creado para verificar el Operador Holded v29'"
Esperado: CatBot ejecuta el Operador, que primero busca el lead (holded_search_lead), luego llama holded_create_lead_note con leadId, title y desc. Devuelve JSON con success=true.

**Limpieza (manual):**
Despues de verificar, eliminar el lead TEST-v29-Operador de Holded si es necesario.

**Test CRM-01 -- Verificar existencia visual:**
Visitar http://localhost:3500/agents?mode=processor y confirmar que "Operador Holded" aparece con conector Holded MCP vinculado.
  </how-to-verify>
  <resume-signal>Escribir "approved" si los 4 tests pasan, o describir que fallo con la respuesta de CatBot pegada como evidencia</resume-signal>
</task>

</tasks>

<verification>
- CatPaw "Operador Holded" existe en la base de datos con modo processor
- Conector Holded MCP (seed-holded-mcp) vinculado al CatPaw (verificado por connector_id exacto)
- System prompt es generalista y contiene los tool names criticos (holded_search_lead, holded_create_lead_note, etc.)
- catpaw-catalog.md tiene la entrada #31
- connectors-catalog.md tiene la nueva regla 7 para canvas CRM con Operador Holded
- catpaw.json tiene el nuevo concepto y howto
- CatBot puede ejecutar los 4 escenarios CRM a traves del Operador Holded
</verification>

<success_criteria>
1. CatPaw "Operador Holded" existe en /agents con conector Holded MCP vinculado y system_prompt generalista
2. El Operador busca leads/contactos via holded_search_lead y holded_search_contact (CRM-02)
3. El Operador crea lead nuevo con funnelId de holded_list_funnels (CRM-03)
4. El Operador anade notas a leads via holded_create_lead_note con title y desc (CRM-04)
</success_criteria>

<output>
After completion, create `.planning/phases/145-catpaw-operador-holded/145-01-SUMMARY.md`
</output>
