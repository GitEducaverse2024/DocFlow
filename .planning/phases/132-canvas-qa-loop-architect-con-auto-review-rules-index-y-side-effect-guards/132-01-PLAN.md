---
phase: 132-canvas-qa-loop-architect-con-auto-review-rules-index-y-side-effect-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/knowledge/canvas-rules-index.md
  - app/src/lib/services/canvas-rules.ts
  - app/src/lib/__tests__/canvas-rules.test.ts
  - app/data/knowledge/catflow.json
autonomous: true
requirements: [QA2-01, QA2-02]

must_haves:
  truths:
    - "El Pipeline Architect puede cargar un índice de reglas de diseño de canvas sin inflar su prompt con 4KB de detalle"
    - "getCanvasRule(ruleId) está exportado como helper interno y devuelve el texto completo de la regla desde canvas-nodes-catalog.md. Está disponible para un segundo paso opcional del Architect (expansion pass) si éste declara needs_rule_details, pero el primer paso del Architect usa únicamente el index inline"
    - "El administrador puede añadir una nueva regla de diseño editando un único markdown sin tocar código"
  artifacts:
    - path: "app/data/knowledge/canvas-rules-index.md"
      provides: "Índice escalable con >=25 reglas agrupadas (R01-R25, SE01-SE03, DA01-DA04), cada línea <=100 chars"
      contains: "R01"
    - path: "app/src/lib/services/canvas-rules.ts"
      provides: "loadRulesIndex() + getCanvasRule(ruleId) + _resetCache() + RuleDetail interface"
      exports: ["loadRulesIndex", "getCanvasRule", "_resetCache"]
    - path: "app/src/lib/__tests__/canvas-rules.test.ts"
      provides: "Unit tests que verifican parsing del index, longitud de línea <=100, lookup por ID, fallback a null para IDs desconocidos"
    - path: "app/data/knowledge/catflow.json"
      provides: "Entradas SE01-SE03 y DA01-DA04 en concepts[] para que CatBot conozca las nuevas reglas"
      contains: "SE01"
  key_links:
    - from: "app/src/lib/services/canvas-rules.ts"
      to: "app/data/knowledge/canvas-rules-index.md"
      via: "fs.readFileSync con RULES_INDEX_PATH"
      pattern: "canvas-rules-index\\.md"
    - from: "app/src/lib/services/canvas-rules.ts"
      to: ".planning/knowledge/canvas-nodes-catalog.md"
      via: "parseRulesFromCatalog regex /- \\*\\*(R\\d{2})\\*\\*/"
      pattern: "canvas-nodes-catalog\\.md"
---

<objective>
Crear la infraestructura de rules index del Pipeline Architect: un markdown editable con 32 reglas
resumidas + un módulo TypeScript que lo carga y permite lookup on-demand del detalle completo
desde canvas-nodes-catalog.md. Esto es la base del QA loop de Plan 02.

Purpose: Escalabilidad. El Architect debe poder referenciar reglas por ID sin inflar el prompt
base. Añadir 200 reglas futuras no debe crecer el prompt que se envía al LLM en cada iteración.

Output:
  - app/data/knowledge/canvas-rules-index.md (nuevo, ~1.7KB, 32 reglas verbatim del research)
  - app/src/lib/services/canvas-rules.ts (nuevo, ~90 líneas)
  - app/src/lib/__tests__/canvas-rules.test.ts (nuevo, cubre QA2-01 y QA2-02)
  - catflow.json extendido con SE01-SE03 y DA01-DA04 en concepts[]
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-canvas-qa-loop-architect-con-auto-review-rules-index-y-side-effect-guards/132-CONTEXT.md
@.planning/phases/132-canvas-qa-loop-architect-con-auto-review-rules-index-y-side-effect-guards/132-RESEARCH.md
@.planning/knowledge/canvas-nodes-catalog.md
@app/data/knowledge/catflow.json

<interfaces>
<!-- Contrato exportado por este plan, consumido por Plan 02 y Plan 03 -->

From app/src/lib/services/canvas-rules.ts (NEW):
```typescript
export interface RuleDetail {
  id: string;          // "R01", "SE02", "DA04"
  short: string;       // <=100 chars, del index
  long: string;        // párrafo completo del catálogo (para R01-R25); igual a short para SE/DA
  category: 'data_contracts' | 'responsibilities' | 'arrays_loops' | 'instructions'
          | 'planning' | 'templates' | 'resilience' | 'side_effects' | 'anti_patterns' | 'other';
}

export function loadRulesIndex(): string;
export function getCanvasRule(ruleId: string): RuleDetail | null;
export function _resetCache(): void;  // test seam
```

Paths used by module:
- RULES_INDEX_PATH = path.join(process.cwd(), 'app/data/knowledge/canvas-rules-index.md')
- CATALOG_PATH    = path.join(process.cwd(), '.planning/knowledge/canvas-nodes-catalog.md')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — canvas-rules test file with failing stubs</name>
  <files>app/src/lib/__tests__/canvas-rules.test.ts</files>
  <behavior>
    Test suite "canvas-rules" con los siguientes tests (TODOS deben fallar inicialmente porque
    canvas-rules.ts y canvas-rules-index.md aún no existen):

    describe "loadRulesIndex (QA2-01)":
      - Test: "loads index markdown without throwing"
        → llama loadRulesIndex(), espera que devuelva string no vacío
      - Test: "index contains at least 25 rules"
        → cuenta líneas que matchean /^- (R\d{2}|SE\d{2}|DA\d{2}):/, expect >= 25
      - Test: "every rule description line is <=100 chars"
        → para cada línea que matchea el regex anterior, asserta line.length <= 100
      - Test: "index contains expected groups"
        → expect index.includes('## Data Contracts'), '## Side Effects Guards', '## Anti-patterns'
      - Test: "caches after first call"
        → llama 2x, expect devuelve misma referencia

    describe "getCanvasRule (QA2-02)":
      - Test: "returns detail for R01"
        → llama getCanvasRule('R01'), expect .id === 'R01', .short.length <= 100, .long es string no vacío, .category === 'data_contracts'
      - Test: "returns detail for R10 and R13 (same category)"
        → expect ambos tienen category === 'data_contracts'
      - Test: "returns detail for R25"
        → expect .id === 'R25', .category === 'arrays_loops'
      - Test: "returns detail for SE01-SE03 (side_effects category)"
        → expect .category === 'side_effects' para los 3
      - Test: "returns detail for DA01-DA04 (anti_patterns category)"
        → expect .category === 'anti_patterns' para los 4
      - Test: "returns null for unknown rule id"
        → expect getCanvasRule('R99') === null
      - Test: "is case-insensitive for rule id"
        → expect getCanvasRule('r01') !== null y .id === 'R01'

    beforeEach: llama _resetCache() importado de canvas-rules para aislar tests.
  </behavior>
  <action>
    Crear app/src/lib/__tests__/canvas-rules.test.ts con los tests descritos arriba.

    Imports:
    ```typescript
    import { describe, it, expect, beforeEach } from 'vitest';
    import { loadRulesIndex, getCanvasRule, _resetCache } from '@/lib/services/canvas-rules';
    ```

    NOTA IMPORTANTE: en este punto canvas-rules.ts y canvas-rules-index.md NO existen todavía.
    El test debe FALLAR (RED de TDD). Task 2 implementa el módulo y Task 3 crea el markdown.
    Después de Task 3 todos los tests deben pasar.

    NO añadir tests de disco que dependan de .planning/knowledge/canvas-nodes-catalog.md
    más allá de los lookups de R01/R10/R13/R25 — ese archivo ya existe (764 líneas) y es
    referencia verbatim para los R01-R25.

    Verificación del RED: el comando de verify debe fallar con error de módulo no encontrado.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/canvas-rules.test.ts 2>&1 | grep -qE "(Cannot find module|Failed to resolve|canvas-rules)"</automated>
  </verify>
  <done>
    Archivo de test existe, contiene los 12 tests descritos, y vitest falla con error de
    módulo no encontrado (prueba de RED state TDD).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canvas-rules.ts module + canvas-rules-index.md</name>
  <files>
    app/src/lib/services/canvas-rules.ts,
    app/data/knowledge/canvas-rules-index.md
  </files>
  <behavior>
    Todos los tests de Task 1 deben pasar (GREEN) una vez este task completa:
    - loadRulesIndex devuelve el markdown cacheado
    - getCanvasRule('R01'..'R25') devuelve detalles del catálogo con category correcta
    - getCanvasRule('SE01'..'SE03') devuelve detalles del index con category='side_effects'
    - getCanvasRule('DA01'..'DA04') devuelve detalles del index con category='anti_patterns'
    - getCanvasRule('R99') === null
    - getCanvasRule es case-insensitive
    - _resetCache() limpia cachedIndex y cachedRules
  </behavior>
  <action>
    PASO A) Crear app/data/knowledge/canvas-rules-index.md

    Contenido VERBATIM de 132-RESEARCH.md sección "Rules Index: Contenido Exacto"
    (líneas 268-322). Son 32 reglas agrupadas en: Data Contracts, Node Responsibilities,
    Arrays & Loops, Instructions Writing, Planning & Testing, Templates,
    Resilience & References, Side Effects Guards (NUEVO), Anti-patterns (NUEVO).

    Requisitos:
    - Cada línea de regla es `- RID: descripción` donde descripción <= 100 chars
    - Encabezados `## Data Contracts`, `## Side Effects Guards`, `## Anti-patterns` presentes
    - Primera línea: `# Canvas Design Rules Index`
    - Segundo párrafo explica que getCanvasRule(rule_id) da detalle on-demand

    PASO B) Crear app/src/lib/services/canvas-rules.ts

    Implementación EXACTA de la sección "Módulo canvas-rules.ts — Diseño" del research
    (líneas 326-415). Puntos clave:

    - Imports: `fs`, `path`
    - Constantes:
      ```typescript
      const RULES_INDEX_PATH = path.join(process.cwd(), 'app/data/knowledge/canvas-rules-index.md');
      const CATALOG_PATH = path.join(process.cwd(), '.planning/knowledge/canvas-nodes-catalog.md');
      ```
    - Cache module-level: `let cachedIndex: string | null = null; let cachedRules: Map<string, RuleDetail> | null = null;`
    - Export RuleDetail interface, loadRulesIndex, getCanvasRule, _resetCache
    - parseRulesFromCatalog: regex `/- \*\*(R\d{2})\*\*\s*(.+?)(?=\n- \*\*R\d{2}|\n\n|\n##)/gs`
      con flag `s` (dotall). Trim + collapse whitespace (`replace(/\n\s+/g, ' ')`).
    - Append SE01-SE03 y DA01-DA04 parseando el propio index con regex `/- (SE\d{2}|DA\d{2}): (.+)/g`
    - categorize(id): función pura que clasifica por prefijo y número como en research líneas 397-408
    - truncate100(s): `s.length <= 100 ? s : s.slice(0, 97) + '...'`
    - getCanvasRule es case-insensitive: `cachedRules.get(ruleId.toUpperCase()) ?? null`

    Path resolution nota: `process.cwd()` en tests vitest = `/home/deskmath/docflow/app`,
    así que RULES_INDEX_PATH debería ser `path.join(process.cwd(), 'data/knowledge/canvas-rules-index.md')`
    (sin el prefijo `app/`) para que funcione tanto en test como en runtime dentro del container.
    Verificar con un log en el primer intento; si el test falla por ENOENT, ajustar.

    NO usar async: fs.readFileSync síncrono está bien porque solo corre en el cold-path
    del Architect phase (que ya es LLM-bound).

    ESLint strict: no imports sin usar. Solo fs y path.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/canvas-rules.test.ts</automated>
  </verify>
  <done>
    - canvas-rules-index.md existe con 32 reglas, cada línea <=100 chars
    - canvas-rules.ts exporta loadRulesIndex, getCanvasRule, _resetCache, RuleDetail
    - Los 12 tests de Task 1 están todos verdes
    - `cd ~/docflow/app && npm run build` pasa sin errores de ESLint
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend catflow.json knowledge tree with SE/DA concepts</name>
  <files>app/data/knowledge/catflow.json</files>
  <action>
    Leer app/data/knowledge/catflow.json actual y añadir entradas en el array `concepts` para
    las 7 reglas nuevas (SE01-SE03, DA01-DA04) para cumplir el protocolo de documentación
    del CLAUDE.md (cada feature nueva debe estar en el knowledge tree).

    Cada entrada tiene la forma:
    ```json
    {
      "id": "SE01",
      "title": "Side Effects Guard Obligatorio",
      "description": "Antes de cada send/write/upload/create → insertar condition guard automático",
      "category": "side_effects",
      "related_rules": ["R01", "R10", "SE02", "SE03"]
    }
    ```

    Las 7 entradas a añadir:
    - SE01: "Antes de cada send/write/upload/create -> insertar condition guard automatico"
    - SE02: "Guard valida que el contrato de entrada tiene TODOS los campos requeridos no vacios"
    - SE03: "Si guard.false -> agent reportador auto-repara via CatBot 1 vez, luego log_knowledge_gap"
    - DA01: "No pases arrays >1 item a nodos con tool-calling interno (usa ITERATOR)"
    - DA02: "No enlaces connectors/skills que el nodo no va a usar"
    - DA03: "No generes URLs con LLM, usa campos especificos del output del tool"
    - DA04: "No dependas de datos fuera del input explicito del nodo"

    Actualizar `updated_at` a la fecha actual ISO (2026-04-10).

    Añadir a `sources`:
    - "app/data/knowledge/canvas-rules-index.md"
    - ".planning/phases/132-canvas-qa-loop-architect-con-auto-review-rules-index-y-side-effect-guards/132-RESEARCH.md"

    Si el JSON falla la validación zod del loader de knowledge tree, ajustar al schema exacto.
    El test de sincronización tools↔knowledge (KTREE-02) NO debe romperse — estas son
    entradas de concepts, no tools, así que no afecta TOOLS[].
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree-sync.test.ts src/lib/__tests__/knowledge-loader.test.ts 2>&1 | grep -qE "(passed|0 failed)" || npx vitest run src/lib/__tests__/canvas-rules.test.ts</automated>
  </verify>
  <done>
    - catflow.json tiene 7 nuevos concepts (SE01..SE03, DA01..DA04)
    - updated_at refrescado
    - sources incluye canvas-rules-index.md
    - Ningún test existente de knowledge tree rompe
  </done>
</task>

</tasks>

<verification>
  <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/canvas-rules.test.ts && npm run build</automated>
</verification>

<success_criteria>
  1. app/data/knowledge/canvas-rules-index.md existe con 32 reglas, estructura markdown agrupada, cada línea de regla <=100 chars (verificado por test automático)
  2. app/src/lib/services/canvas-rules.ts exporta loadRulesIndex, getCanvasRule, _resetCache; getCanvasRule devuelve RuleDetail para R01-R25, SE01-SE03, DA01-DA04 y null para IDs desconocidos
  3. 12 tests de canvas-rules.test.ts verdes
  4. catflow.json conoce las nuevas reglas SE/DA (protocolo CLAUDE.md)
  5. `npm run build` pasa (no unused imports)
</success_criteria>

<output>
Create .planning/phases/132-canvas-qa-loop-architect-con-auto-review-rules-index-y-side-effect-guards/132-01-SUMMARY.md with: changes made, test counts, any path resolution notes for downstream plans.
</output>
