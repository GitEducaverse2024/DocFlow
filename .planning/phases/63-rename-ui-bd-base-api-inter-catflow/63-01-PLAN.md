---
phase: 63-rename-ui-bd-base-api-inter-catflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/layout/sidebar.tsx
  - app/src/app/catflow/page.tsx
  - app/src/app/catflow/[id]/page.tsx
  - app/src/app/catflow/new/page.tsx
autonomous: true
requirements: [REN-01, REN-02, REN-03, REN-04]

must_haves:
  truths:
    - "Sidebar shows 'CatFlow' with Zap icon where 'Tareas' was, linking to /catflow"
    - "/catflow loads and displays the same task list as /tasks"
    - "/tasks continues working without redirect for backward compatibility"
    - "Breadcrumb shows 'CatFlow' label for /catflow routes"
  artifacts:
    - path: "app/src/components/layout/sidebar.tsx"
      provides: "CatFlow nav item with Zap icon linking to /catflow"
      contains: "Zap"
    - path: "app/src/app/catflow/page.tsx"
      provides: "CatFlow list page re-exporting tasks page content"
    - path: "app/src/app/catflow/[id]/page.tsx"
      provides: "CatFlow detail route"
    - path: "app/src/app/catflow/new/page.tsx"
      provides: "CatFlow new route"
  key_links:
    - from: "app/src/components/layout/sidebar.tsx"
      to: "/catflow"
      via: "navItems href"
      pattern: "href.*catflow"
    - from: "app/src/app/catflow/page.tsx"
      to: "/api/tasks"
      via: "fetch call (same as tasks/page.tsx)"
      pattern: "fetch.*api/tasks"
---

<objective>
Rename the sidebar entry from "Tareas" to "CatFlow" with a Zap icon, create /catflow routes that mirror /tasks, and keep /tasks working for backward compatibility.

Purpose: First visible change for v16.0 CatFlow milestone -- users see the new branding immediately.
Output: Sidebar updated, /catflow routes functional, /tasks still works.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/components/layout/sidebar.tsx
@app/src/app/tasks/page.tsx
@app/src/app/tasks/[id]/page.tsx
@app/src/app/tasks/new/page.tsx
@app/src/components/layout/page-header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sidebar nav item and create /catflow routes</name>
  <files>
    app/src/components/layout/sidebar.tsx,
    app/src/app/catflow/page.tsx,
    app/src/app/catflow/[id]/page.tsx,
    app/src/app/catflow/new/page.tsx
  </files>
  <action>
1. In sidebar.tsx:
   - Import `Zap` from lucide-react (add to existing import)
   - Change the tasks nav item from `{ href: '/tasks', labelKey: 'tasks', icon: ClipboardList }` to `{ href: '/catflow', labelKey: 'catflow', icon: Zap }`
   - Keep ClipboardList import if used elsewhere, remove if not
   - The `isActive` logic already handles prefix matching via `pathname.startsWith(item.href)` so /catflow/xxx will highlight correctly

2. Create app/src/app/catflow/page.tsx:
   - This is a "use client" page that imports and re-renders the SAME content as tasks/page.tsx
   - Extract the actual task list component body from tasks/page.tsx into a shared component OR simply duplicate the page with identical fetch logic
   - PREFERRED approach: Create a shared TaskListContent component in app/src/components/tasks/task-list-content.tsx that both pages use. Move the entire body of TasksPage (state, effects, JSX) into TaskListContent. Both catflow/page.tsx and tasks/page.tsx become thin wrappers that render <TaskListContent />.
   - The component uses useTranslations('tasks') -- this is fine since the i18n namespace stays 'tasks' internally (plan 63-04 adds 'catflow' namespace for new keys only)
   - The PageHeader in the shared component should detect pathname: if pathname starts with '/catflow', show Zap icon and t('catflow.title') from catflow namespace; otherwise show ClipboardList and t('title') from tasks namespace. For now (before 63-04 adds catflow namespace), use the tasks namespace title for both -- the catflow i18n will be wired in plan 63-04.

3. Create app/src/app/catflow/[id]/page.tsx:
   - Thin wrapper that renders the same content as tasks/[id]/page.tsx
   - Import the task detail page component and re-export, OR redirect internally
   - PREFERRED: Import the main component from tasks/[id] and render it. If the tasks/[id]/page.tsx is a default export, create a named export wrapper in a shared location.
   - The detail page links (like "back to tasks") should work with both /tasks and /catflow paths

4. Create app/src/app/catflow/new/page.tsx:
   - Same approach: thin wrapper rendering the wizard from tasks/new

5. Keep app/src/app/tasks/page.tsx, tasks/[id]/page.tsx, tasks/new/page.tsx FULLY FUNCTIONAL -- do NOT add redirects or remove them. They must continue working for backward compatibility.

6. For the shared component extraction: create app/src/components/tasks/task-list-content.tsx with the full task list logic moved from tasks/page.tsx. Both page files become ~5-line wrappers.

IMPORTANT: The breadcrumb/PageHeader should show "CatFlow" when on /catflow routes. Use `usePathname()` to detect the route prefix and conditionally render the title/icon. For now, hardcode the Spanish "CatFlow" label -- plan 63-04 will add proper i18n keys.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Sidebar shows "CatFlow" with Zap icon linking to /catflow
    - /catflow loads and displays the same task list as /tasks
    - /catflow/[id] loads the task detail page
    - /catflow/new loads the wizard
    - /tasks, /tasks/[id], /tasks/new all still work without any redirect
    - npm run build passes
  </done>
</task>

</tasks>

<verification>
- Visit /catflow in browser -- task list loads
- Visit /tasks in browser -- task list still loads (no redirect)
- Sidebar highlights "CatFlow" when on /catflow
- Click a task card on /catflow -- navigates to /catflow/[id]
- npm run build passes clean
</verification>

<success_criteria>
Sidebar displays "CatFlow" with Zap icon linking to /catflow. Both /catflow and /tasks routes work independently showing the same task list. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/63-rename-ui-bd-base-api-inter-catflow/63-01-SUMMARY.md`
</output>
