---
phase: 63-rename-ui-bd-base-api-inter-catflow
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/messages/es.json
  - app/messages/en.json
autonomous: true
requirements: [REN-05, REN-06]

must_haves:
  truths:
    - "i18n namespace 'catflow' exists in both es.json and en.json with base keys"
    - "nav label key 'catflow' added to sidebar i18n in both languages"
  artifacts:
    - path: "app/messages/es.json"
      provides: "Spanish catflow namespace and nav.catflow key"
      contains: "catflow"
    - path: "app/messages/en.json"
      provides: "English catflow namespace and nav.catflow key"
      contains: "catflow"
  key_links:
    - from: "app/src/components/layout/sidebar.tsx"
      to: "app/messages/es.json"
      via: "tNav('catflow') reads nav.catflow"
      pattern: "catflow"
---

<objective>
Add i18n keys for CatFlow branding: nav label and base catflow namespace in both es.json and en.json.

Purpose: Sidebar label reads from i18n, and future CatFlow-specific UI text needs a dedicated namespace.
Output: Both message files updated with catflow keys.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/messages/es.json
@app/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add catflow i18n keys to es.json and en.json</name>
  <files>app/messages/es.json, app/messages/en.json</files>
  <action>
1. In app/messages/es.json:

   a. In the "nav" object (around line 37-56), add a "catflow" key:
      ```json
      "catflow": "CatFlow"
      ```
      Place it where "tasks" currently is (line 43). Keep "tasks": "Tareas" as well for backward compat (the /tasks page still uses it).

   b. In the "layout.breadcrumbs" object (around line 60-75), add:
      ```json
      "catflow": "CatFlow"
      ```

   c. Add a new top-level "catflow" namespace object (after "tasks" namespace around line 946):
      ```json
      "catflow": {
        "title": "CatFlow",
        "description": "Gestiona pipelines de procesamiento multi-agente.",
        "newCatflow": "Nuevo CatFlow",
        "listening": "En escucha",
        "notListening": "Sin escucha",
        "triggers": {
          "title": "Triggers",
          "pending": "Pendiente",
          "running": "Ejecutando",
          "completed": "Completado",
          "failed": "Fallido",
          "timeout": "Tiempo agotado"
        }
      }
      ```

2. In app/messages/en.json:

   a. In the "nav" object, add:
      ```json
      "catflow": "CatFlow"
      ```

   b. In the "layout.breadcrumbs" object, add:
      ```json
      "catflow": "CatFlow"
      ```

   c. Add a new top-level "catflow" namespace:
      ```json
      "catflow": {
        "title": "CatFlow",
        "description": "Manage multi-agent processing pipelines.",
        "newCatflow": "New CatFlow",
        "listening": "Listening",
        "notListening": "Not listening",
        "triggers": {
          "title": "Triggers",
          "pending": "Pending",
          "running": "Running",
          "completed": "Completed",
          "failed": "Failed",
          "timeout": "Timed out"
        }
      }
      ```

IMPORTANT: Both files must stay in sync (same keys). Do NOT remove existing "tasks" keys -- they are still used by /tasks routes. The "catflow" namespace is additive. Ensure valid JSON after edits (trailing commas, matching braces).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && node -e "const es = require('./messages/es.json'); const en = require('./messages/en.json'); console.log('nav.catflow ES:', es.nav.catflow); console.log('nav.catflow EN:', en.nav.catflow); console.log('catflow ns ES:', !!es.catflow); console.log('catflow ns EN:', !!en.catflow); console.log('breadcrumbs.catflow ES:', es.layout.breadcrumbs.catflow); console.log('breadcrumbs.catflow EN:', en.layout.breadcrumbs.catflow);"</automated>
  </verify>
  <done>
    - nav.catflow key exists in both es.json ("CatFlow") and en.json ("CatFlow")
    - layout.breadcrumbs.catflow exists in both files
    - Top-level "catflow" namespace exists in both files with title, description, newCatflow, listening, triggers keys
    - Both JSON files are valid (node can parse them)
  </done>
</task>

</tasks>

<verification>
- Both JSON files parse without errors
- nav.catflow key present in both languages
- catflow namespace present in both languages with matching key structure
- npm run build passes
</verification>

<success_criteria>
i18n namespace "catflow" exists in both es.json and en.json with base keys. Sidebar nav label key "catflow" added. Both files valid JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/63-rename-ui-bd-base-api-inter-catflow/63-04-SUMMARY.md`
</output>
