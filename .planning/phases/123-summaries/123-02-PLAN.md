---
phase: 123-summaries
plan: 02
type: execute
wave: 2
depends_on:
  - "123-01"
files_modified:
  - app/src/instrumentation.ts
  - app/src/lib/services/catbot-tools.ts
autonomous: true
requirements:
  - SUMMARY-01
  - SUMMARY-02
  - SUMMARY-03
  - SUMMARY-04
  - SUMMARY-05

must_haves:
  truths:
    - "SummaryService arranca automaticamente al iniciar la app via instrumentation.ts"
    - "CatBot puede listar resumenes del usuario con list_my_summaries"
    - "CatBot puede mostrar un resumen especifico con get_summary"
    - "El scheduler no interfiere con el arranque de la app (delay 2 min)"
  artifacts:
    - path: "app/src/instrumentation.ts"
      provides: "SummaryService.start() registration"
      contains: "SummaryService"
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "list_my_summaries y get_summary tools para CatBot"
      contains: "list_my_summaries"
  key_links:
    - from: "app/src/instrumentation.ts"
      to: "app/src/lib/services/catbot-summary.ts"
      via: "dynamic import + start()"
      pattern: "import.*catbot-summary.*SummaryService\\.start"
    - from: "app/src/lib/services/catbot-tools.ts"
      to: "app/src/lib/catbot-db.ts"
      via: "getSummaries for tool results"
      pattern: "getSummaries"
---

<objective>
Registrar SummaryService en instrumentation.ts y anadir CatBot tools para consultar resumenes.

Purpose: Sin el registro en instrumentation.ts el scheduler no arranca. Sin los tools, CatBot no puede reportar resumenes al usuario (requerido por CLAUDE.md: CatBot como oraculo).
Output: Scheduler activo al arrancar la app, CatBot puede responder "resumen del dia de ayer" y "lista mis resumenes".
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-summaries/123-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From app/src/lib/services/catbot-summary.ts:
```typescript
export class SummaryService {
  static start(): void;
  static stop(): void;
  static async tick(): Promise<void>;
  static async compressDaily(date: Date, userId: string): Promise<string | null>;
  static async compressWeekly(weekStart: Date, userId: string): Promise<string | null>;
  static async compressMonthly(monthStart: Date, userId: string): Promise<string | null>;
}
```

From app/src/lib/catbot-db.ts (existing + Plan 01 additions):
```typescript
export function getSummaries(userId?: string, periodType?: string): SummaryRow[];
export function getConversationsByDateRange(startDate: string, endDate: string, userId?: string): ConversationRow[];
export function summaryExists(userId: string, periodType: string, periodStart: string): boolean;
export function getActiveUserIds(startDate: string, endDate: string): string[];
```

From app/src/instrumentation.ts (current pattern):
```typescript
export async function register() {
  if (process['env']['NEXT_RUNTIME'] === 'nodejs') {
    const { taskScheduler } = await import('@/lib/services/task-scheduler');
    taskScheduler.start();
    if (process['env']['NODE_ENV'] !== 'test') {
      // DrivePollingService, TelegramBotService registered here
    }
  }
}
```

From app/src/lib/services/catbot-tools.ts (tool definition pattern):
```typescript
// Tool definition in CATBOT_TOOLS array:
{
  type: 'function',
  function: {
    name: 'tool_name',
    description: 'Descripcion en espanol.',
    parameters: { type: 'object', properties: {...}, required: [...] },
  },
},

// Tool execution in executeTool() switch:
case 'tool_name': {
  // implementation
  return { name, result: 'formatted string' };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar SummaryService en instrumentation.ts</name>
  <files>app/src/instrumentation.ts</files>
  <action>
    Dentro del bloque `if (process['env']['NODE_ENV'] !== 'test')` en instrumentation.ts, despues del registro de TelegramBotService, anadir:

    ```typescript
    // Start CatBot Summary scheduler (compresses conversations into daily/weekly/monthly summaries)
    try {
      const { SummaryService } = await import('@/lib/services/catbot-summary');
      SummaryService.start();
    } catch (err) {
      console.error('[instrumentation] Failed to start SummaryService:', err);
    }
    ```

    Seguir exactamente el patron de DrivePollingService y TelegramBotService (dynamic import, try-catch, console.error con prefijo [instrumentation]).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run</automated>
  </verify>
  <done>SummaryService.start() se registra en instrumentation.ts dentro del guard NODE_ENV !== 'test', con try-catch y logging de error.</done>
</task>

<task type="auto">
  <name>Task 2: CatBot tools list_my_summaries y get_summary</name>
  <files>app/src/lib/services/catbot-tools.ts</files>
  <action>
    1. **Tool definitions** -- Anadir al array CATBOT_TOOLS (antes del cierre `];`), justo despues de forget_recipe:

    ```typescript
    {
      type: 'function',
      function: {
        name: 'list_my_summaries',
        description: 'Lista los resumenes de conversaciones del usuario (diarios, semanales, mensuales). Muestra periodo, topics y cantidad de decisions.',
        parameters: {
          type: 'object',
          properties: {
            period_type: { type: 'string', enum: ['daily', 'weekly', 'monthly'], description: 'Filtrar por tipo de periodo (opcional)' },
            limit: { type: 'number', description: 'Maximo de resumenes a devolver (default 10)' },
          },
          required: [],
        },
      },
    },
    {
      type: 'function',
      function: {
        name: 'get_summary',
        description: 'Muestra el detalle completo de un resumen especifico: texto del resumen, topics, tools usadas, decisions tomadas y pendientes.',
        parameters: {
          type: 'object',
          properties: {
            summary_id: { type: 'string', description: 'ID del resumen a consultar' },
          },
          required: ['summary_id'],
        },
      },
    },
    ```

    2. **Tool execution** -- Anadir cases en el switch de executeTool():

    ```typescript
    case 'list_my_summaries': {
      const userId = (args.user_id as string) || 'web:default';
      const periodType = args.period_type as string | undefined;
      const limit = (args.limit as number) || 10;
      const summaries = getSummaries(userId, periodType).slice(0, limit);
      if (summaries.length === 0) {
        return { name, result: 'No tienes resumenes todavia. Se generan automaticamente cada dia a partir de tus conversaciones.' };
      }
      const formatted = summaries.map(s => {
        const topics = JSON.parse(s.topics) as string[];
        const decisions = JSON.parse(s.decisions) as string[];
        return `- **${s.period_type}** ${s.period_start} → ${s.period_end} (${s.conversation_count} convs)\n  Topics: ${topics.join(', ') || 'ninguno'}\n  Decisions: ${decisions.length}`;
      }).join('\n');
      return { name, result: `Tienes ${summaries.length} resumen(es):\n\n${formatted}` };
    }

    case 'get_summary': {
      const summaryId = args.summary_id as string;
      if (!summaryId) return { name, result: { error: 'summary_id es requerido' } };
      // getSummaries no filtra por ID, buscar en todos
      const userId = (args.user_id as string) || 'web:default';
      const all = getSummaries(userId);
      const s = all.find(x => x.id === summaryId);
      if (!s) return { name, result: { error: 'Resumen no encontrado' } };
      const topics = JSON.parse(s.topics) as string[];
      const toolsUsed = JSON.parse(s.tools_used) as string[];
      const decisions = JSON.parse(s.decisions) as string[];
      const pending = JSON.parse(s.pending) as string[];
      return { name, result: `**Resumen ${s.period_type}** (${s.period_start} → ${s.period_end})\n\n${s.summary}\n\n**Topics:** ${topics.join(', ') || 'ninguno'}\n**Tools usadas:** ${toolsUsed.join(', ') || 'ninguna'}\n**Decisions:** ${decisions.length > 0 ? '\n' + decisions.map(d => `- ${d}`).join('\n') : 'ninguna'}\n**Pendientes:** ${pending.length > 0 ? '\n' + pending.map(p => `- ${p}`).join('\n') : 'ninguno'}` };
    }
    ```

    3. **Imports** -- Verificar que `getSummaries` ya se importa desde `@/lib/catbot-db`. Si no, anadirlo al import existente.

    4. **Permission gate** -- Ambos tools usan prefijo `list_` y `get_` que estan en el patron `always_allowed` del permission gate (get_ prefix). Verificar que list_my_summaries tambien entra como always_allowed (patron list_ ya deberia existir por list_my_recipes).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - list_my_summaries tool registrado en CATBOT_TOOLS y ejecutable, devuelve lista formateada de resumenes
    - get_summary tool registrado y ejecutable, devuelve detalle completo de un resumen
    - Build pasa sin errores
    - CatBot puede responder a "lista mis resumenes" y "resumen del dia de ayer"
  </done>
</task>

</tasks>

<verification>
cd /home/deskmath/docflow/app && npx vitest run
cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -10
</verification>

<success_criteria>
- SummaryService arranca automaticamente con la app (SUMMARY-01)
- CatBot puede listar resumenes filtrando por tipo de periodo (SUMMARY-02, SUMMARY-03, SUMMARY-04)
- CatBot puede mostrar detalle de un resumen incluyendo decisions acumuladas (SUMMARY-05)
- Build pasa sin errores
- Tests existentes siguen pasando
</success_criteria>

<output>
After completion, create `.planning/phases/123-summaries/123-02-SUMMARY.md`
</output>
