---
phase: 123-summaries
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/lib/catbot-db.ts
  - app/src/lib/services/catbot-summary.ts
  - app/src/lib/__tests__/catbot-summary.test.ts
autonomous: true
requirements:
  - SUMMARY-01
  - SUMMARY-02
  - SUMMARY-03
  - SUMMARY-04
  - SUMMARY-05

must_haves:
  truths:
    - "compressDaily genera un resumen estructurado a partir de conversaciones de un dia"
    - "compressWeekly comprime resumenes diarios en uno semanal acumulando decisions"
    - "compressMonthly comprime resumenes semanales en uno mensual acumulando decisions"
    - "Si no hay conversaciones para un periodo, no se crea resumen (skip silencioso)"
    - "Las decisions de niveles inferiores NUNCA se pierden en la compresion"
    - "Si el resumen ya existe para un periodo, no se crea duplicado"
  artifacts:
    - path: "app/src/lib/services/catbot-summary.ts"
      provides: "SummaryService con compressDaily, compressWeekly, compressMonthly, tick, start"
      exports: ["SummaryService"]
    - path: "app/src/lib/__tests__/catbot-summary.test.ts"
      provides: "Tests unitarios para compresion diaria, semanal, mensual y acumulacion de decisions"
      min_lines: 80
    - path: "app/src/lib/catbot-db.ts"
      provides: "getConversationsByDateRange, summaryExists, getActiveUserIds helpers"
      exports: ["getConversationsByDateRange", "summaryExists", "getActiveUserIds"]
  key_links:
    - from: "app/src/lib/services/catbot-summary.ts"
      to: "app/src/lib/catbot-db.ts"
      via: "import getConversationsByDateRange, saveSummary, getSummaries, summaryExists, getActiveUserIds"
      pattern: "import.*from.*catbot-db"
    - from: "app/src/lib/services/catbot-summary.ts"
      to: "LiteLLM proxy"
      via: "fetch to LITELLM_URL/v1/chat/completions with Libre model"
      pattern: "fetch.*litellm.*chat/completions"
---

<objective>
Implementar SummaryService con compresion jerarquica de conversaciones (daily/weekly/monthly) y los DB helpers necesarios en catbot-db.ts, usando TDD.

Purpose: Es el nucleo de Phase 123 -- sin el servicio de compresion no hay resumenes automaticos. La acumulacion de decisions es el comportamiento critico que garantiza que la informacion no se pierde.
Output: catbot-summary.ts con SummaryService funcional y testeado, catbot-db.ts con helpers de rango de fechas e idempotencia.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-summaries/123-RESEARCH.md

<interfaces>
<!-- Existing catbot-db.ts types and functions the executor needs -->

From app/src/lib/catbot-db.ts:
```typescript
export interface ConversationRow {
  id: string;
  user_id: string;
  channel: string;
  messages: string; // JSON array of {role, content, tool_calls?}
  tools_used: string; // JSON array of tool names
  token_count: number;
  model: string | null;
  page: string | null;
  started_at: string;
  ended_at: string | null;
}

export interface SummaryRow {
  id: string;
  user_id: string;
  period_type: string; // 'daily' | 'weekly' | 'monthly'
  period_start: string;
  period_end: string;
  summary: string;
  topics: string; // JSON
  tools_used: string; // JSON
  decisions: string; // JSON
  pending: string; // JSON
  conversation_count: number;
  created_at: string;
}

// Existing functions:
export function saveSummary(sum: {
  userId?: string; periodType: string; periodStart: string; periodEnd: string;
  summary: string; topics?: string[]; toolsUsed?: string[]; decisions?: string[];
  pending?: string[]; conversationCount?: number;
}): string;

export function getSummaries(userId?: string, periodType?: string): SummaryRow[];
```

From app/src/instrumentation.ts:
```typescript
// Service registration pattern:
export async function register() {
  if (process['env']['NEXT_RUNTIME'] === 'nodejs') {
    // ... existing services ...
    if (process['env']['NODE_ENV'] !== 'test') {
      try {
        const { SomeService } = await import('@/lib/services/some-service');
        SomeService.start();
      } catch (err) {
        console.error('[instrumentation] Failed to start SomeService:', err);
      }
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB helpers + SummaryService con compresion jerarquica (TDD)</name>
  <files>app/src/lib/catbot-db.ts, app/src/lib/services/catbot-summary.ts, app/src/lib/__tests__/catbot-summary.test.ts</files>
  <behavior>
    - Test "compressDaily genera resumen con campos estructurados": Mock getConversationsByDateRange para devolver 2 conversaciones con messages JSON, mock fetch LiteLLM para devolver JSON con summary/topics/tools_used/decisions/pending. Verificar que saveSummary se llama con los campos correctos.
    - Test "compressDaily skip si no hay conversaciones": getConversationsByDateRange devuelve [], verificar que saveSummary NO se llama y retorna null.
    - Test "compressDaily skip si resumen ya existe": summaryExists devuelve true, verificar que no se llama a LLM ni se crea resumen.
    - Test "compressWeekly acumula decisions de dailies": getSummaries devuelve 3 dailies con decisions distintas, mock LLM para devolver su propio set de decisions. Verificar que el resumen semanal contiene la UNION de todas las decisions (dailies + LLM).
    - Test "compressMonthly acumula decisions de weeklies": Mismo patron pero con weeklies.
    - Test "tick ejecuta daily siempre, weekly solo los lunes, monthly solo dia 1": Mock Date, verificar que se llaman los metodos correctos segun el dia.
    - Test "JSON parse fallback si LLM devuelve JSON invalido": fetch retorna texto no-JSON, verificar que se crea resumen minimo desde metadata (tools_used de conversations).
  </behavior>
  <action>
    **RED phase -- tests primero:**

    Crear `app/src/lib/__tests__/catbot-summary.test.ts` con los tests descritos en behavior. Mockear:
    - `@/lib/catbot-db` (getConversationsByDateRange, summaryExists, saveSummary, getSummaries, getActiveUserIds)
    - `global.fetch` para las llamadas a LiteLLM

    **GREEN phase -- implementacion:**

    1. En `app/src/lib/catbot-db.ts`, anadir 3 funciones al final del archivo (antes del cierre):

    ```typescript
    export function getConversationsByDateRange(startDate: string, endDate: string, userId?: string): ConversationRow[] {
      if (userId) {
        return catbotDb.prepare(
          'SELECT * FROM conversation_log WHERE started_at >= ? AND started_at < ? AND user_id = ? ORDER BY started_at ASC'
        ).all(startDate, endDate, userId) as ConversationRow[];
      }
      return catbotDb.prepare(
        'SELECT * FROM conversation_log WHERE started_at >= ? AND started_at < ? ORDER BY started_at ASC'
      ).all(startDate, endDate) as ConversationRow[];
    }

    export function summaryExists(userId: string, periodType: string, periodStart: string): boolean {
      const row = catbotDb.prepare(
        'SELECT id FROM summaries WHERE user_id = ? AND period_type = ? AND period_start = ?'
      ).get(userId, periodType, periodStart);
      return !!row;
    }

    export function getActiveUserIds(startDate: string, endDate: string): string[] {
      const rows = catbotDb.prepare(
        'SELECT DISTINCT user_id FROM conversation_log WHERE started_at >= ? AND started_at < ?'
      ).all(startDate, endDate) as Array<{ user_id: string }>;
      return rows.map(r => r.user_id);
    }
    ```

    2. Crear `app/src/lib/services/catbot-summary.ts` con SummaryService class:

    - `SUMMARY_CHECK_INTERVAL = 60 * 60 * 1000` (1 hora)
    - `BOOT_DELAY = 120_000` (2 min para no interferir con arranque)
    - Modelo: `ollama/gemma3:12b` (Libre tier, zero cost)
    - Temperature: 0.3 (low para extraccion factual)
    - `response_format: { type: 'json_object' }` para JSON mode

    - `start()`: setInterval + setTimeout para primer tick
    - `stop()`: clearInterval (para tests)
    - `tick()`: Obtener distinct user_ids del rango, para cada uno: maybeCompressDaily(). Si es lunes: maybeCompressWeekly(). Si es dia 1: maybeCompressMonthly().
    - `compressDaily(date, userId)`: Check summaryExists -> getConversationsByDateRange -> extractConversationContent (solo user msgs + final assistant) -> callSummaryLLM con DAILY_EXTRACTION_PROMPT -> parse JSON -> saveSummary. Si JSON.parse falla, retry 1 vez. Si retry falla, crear resumen minimo desde metadata.
    - `compressWeekly(weekStart, userId)`: getSummaries(userId, 'daily') filtrado por rango -> accumulateDecisions (Set union) -> callSummaryLLM con WEEKLY_PROMPT -> merge decisions (accumulated + LLM) -> saveSummary.
    - `compressMonthly(monthStart, userId)`: Mismo patron sobre weeklies.
    - `accumulateDecisions(summaries: SummaryRow[])`: Parsea JSON decisions de cada summary, devuelve Set union como array.
    - `callSummaryLLM(prompt, content)`: fetch a `process['env']['LITELLM_URL']` + `/v1/chat/completions`, model `ollama/gemma3:12b`, Bearer `process['env']['LITELLM_API_KEY']`.
    - `extractConversationContent(conversations)`: De cada conversation, parsea messages JSON, extrae solo role=user messages y el ultimo role=assistant message. Trunca a ~4000 chars por conversacion.

    Usar `process['env']['VARIABLE']` (bracket notation) per proyecto convention. Usar `logger` de `@/lib/logger`. Usar `generateId` de `@/lib/utils`.

    **REFACTOR phase:** Limpiar tipos, extraer constantes de prompts.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-summary.test.ts</automated>
  </verify>
  <done>
    - getConversationsByDateRange, summaryExists, getActiveUserIds exportados desde catbot-db.ts
    - SummaryService con compressDaily/Weekly/Monthly funcional
    - Todos los tests pasan: skip sin conversaciones, skip si existe, acumulacion de decisions, fallback JSON invalido
    - Decisions NUNCA se pierden en compresion weekly/monthly (verificado por test)
  </done>
</task>

</tasks>

<verification>
cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-summary.test.ts
cd /home/deskmath/docflow/app && npx vitest run
</verification>

<success_criteria>
- SummaryService comprime conversaciones diarias en resumenes con campos estructurados (SUMMARY-01, SUMMARY-02)
- Compresion weekly (lunes) y monthly (dia 1) funciona sobre resumenes del nivel inferior (SUMMARY-03, SUMMARY-04)
- Decisions se acumulan via Set union en cada nivel de compresion, nunca se pierden (SUMMARY-05)
- Idempotencia: no crea duplicados para el mismo periodo/usuario
- Skip silencioso si no hay conversaciones
- Fallback a metadata si LLM devuelve JSON invalido
- Todos los tests pasan
</success_criteria>

<output>
After completion, create `.planning/phases/123-summaries/123-01-SUMMARY.md`
</output>
