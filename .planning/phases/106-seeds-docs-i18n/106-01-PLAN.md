---
phase: 106-seeds-docs-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/db.ts
  - .planning/knowledge/GUIA_USUARIO.md
  - .planning/knowledge/CONNECTORS.md
  - app/messages/es.json
  - app/messages/en.json
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03, SEED-04, TECH-01]

must_haves:
  truths:
    - "4 nuevos templates seed se insertan en la base de datos al iniciar la app"
    - "Los seeds son idempotentes — re-ejecutar no duplica templates"
    - "Cada seed usa la categoria correcta (corporate, report, commercial, notification)"
    - "GUIA_USUARIO.md documenta el modulo Email Templates"
    - "CONNECTORS.md documenta el conector email_template"
    - "npm run build pasa sin errores"
  artifacts:
    - path: "app/src/lib/db.ts"
      provides: "4 seed templates INSERT OR IGNORE"
      contains: "seed-tpl-corporativa"
    - path: ".planning/knowledge/GUIA_USUARIO.md"
      provides: "Seccion Email Templates"
      contains: "Plantillas de Email"
    - path: ".planning/knowledge/CONNECTORS.md"
      provides: "Entrada conector email_template"
      contains: "email_template"
  key_links:
    - from: "app/src/lib/db.ts"
      to: "email_templates table"
      via: "INSERT OR IGNORE with fixed IDs"
      pattern: "seed-tpl-(corporativa|informe|comercial|notificacion)"
---

<objective>
Crear 4 templates seed en db.ts, actualizar documentacion (GUIA_USUARIO.md, CONNECTORS.md), verificar claves i18n, y confirmar build limpio.

Purpose: Fase final de v24.0 — los templates seed dan contenido inicial al modulo CatPower Templates para que el usuario tenga plantillas profesionales desde el primer arranque.
Output: 4 seeds en db.ts, docs actualizados, build verde.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-seeds-docs-i18n/RESEARCH.md

@app/src/lib/db.ts (lines 4438-4528 — existing seed pattern)
@app/src/lib/types.ts (lines 306-361 — TemplateStructure types)
@.planning/knowledge/GUIA_USUARIO.md
@.planning/knowledge/CONNECTORS.md

<interfaces>
<!-- Tipos clave que los seeds deben cumplir (de app/src/lib/types.ts) -->

```typescript
interface TemplateStructure {
  sections: {
    header: TemplateSection;  // { rows: TemplateRow[] }
    body: TemplateSection;
    footer: TemplateSection;
  };
  styles: {
    backgroundColor: string;
    fontFamily: string;
    primaryColor: string;
    textColor: string;
    maxWidth: number;
  };
}

interface TemplateRow {
  id: string;
  columns: TemplateColumn[];
}

interface TemplateColumn {
  id: string;
  width: string;       // '100%', '50%', '70%', '30%'
  block: TemplateBlock;
}

interface TemplateBlock {
  type: 'logo' | 'image' | 'video' | 'text' | 'instruction';
  src?: string;
  alt?: string;
  width?: number | string;
  align?: 'left' | 'center' | 'right' | 'full';
  url?: string;
  thumbnailUrl?: string;
  content?: string;     // para text blocks
  text?: string;        // para instruction blocks — clave de variable
}
```

<!-- Patron de seed existente en db.ts (linea 4438) -->
El seed actual usa `tplCount === 0` que bloquea nuevos seeds si ya existe el basico.
Los nuevos seeds DEBEN usar `INSERT OR IGNORE` con IDs fijos para ser idempotentes.

<!-- Categorias validas (ya en i18n) -->
general, corporate, commercial, report, notification
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insertar 4 templates seed en db.ts</name>
  <files>app/src/lib/db.ts</files>
  <action>
Despues del bloque seed-template-basic existente (linea ~4461) y ANTES del bloque seed del conector email_template (linea ~4463), insertar un nuevo bloque try/catch con los 4 seeds usando INSERT OR IGNORE.

Patron a seguir:
```typescript
// Seed: 4 plantillas profesionales
try {
  const seedTplStmt = db.prepare(
    `INSERT OR IGNORE INTO email_templates (id, name, description, category, structure, is_active, created_at, updated_at) VALUES (?, ?, ?, ?, ?, 1, ?, ?)`
  );
  const now = new Date().toISOString();

  // SEED-01: Corporativa Educa360 (corporate)
  seedTplStmt.run('seed-tpl-corporativa', 'Corporativa Educa360', ...);
  // SEED-02: Informe de Leads (report)
  seedTplStmt.run('seed-tpl-informe-leads', 'Informe de Leads', ...);
  // SEED-03: Respuesta Comercial (commercial)
  seedTplStmt.run('seed-tpl-respuesta-comercial', 'Respuesta Comercial', ...);
  // SEED-04: Notificacion Interna (notification)
  seedTplStmt.run('seed-tpl-notificacion', 'Notificacion Interna', ...);
} catch (e) { logger.error('system', 'Template seeds error', { error: (e as Error).message }); }
```

Datos exactos de cada seed (del RESEARCH.md):

**SEED-01 — Corporativa Educa360** (id: seed-tpl-corporativa, category: corporate)
- description: "Plantilla corporativa con logo, banner de cabecera, cuerpo con instruccion LLM, y pie con firma y logo pequeno."
- primaryColor: '#2563EB' (azul corporativo)
- header: 2 rows — logo centrado (src:'', alt:'Educa360', width:180, align:'center') + imagen banner (src:'', alt:'Banner', width:'full', align:'full')
- body: 1 row — instruction (text: 'Contenido principal del email corporativo')
- footer: 1 row con 2 columnas — texto firma 70% (content: '**Equipo Educa360**\ninfo@educa360.com\nwww.educa360.com') + logo pequeno 30% (src:'', alt:'Educa360', width:60, align:'right')

**SEED-02 — Informe de Leads** (id: seed-tpl-informe-leads, category: report)
- description: "Plantilla de informe con cabecera violeta, instruccion para tabla de datos, y pie DoCatFlow."
- primaryColor: '#7C3AED' (violeta)
- header: 1 row — text centrado (content: '**Informe de Leads**')
- body: 1 row — instruction (text: 'Tabla de datos de leads e informe ejecutivo')
- footer: 1 row — text (content: 'Generado por **DoCatFlow** | Informe automatico')

**SEED-03 — Respuesta Comercial** (id: seed-tpl-respuesta-comercial, category: commercial)
- description: "Plantilla comercial con logo sutil, cuerpo personalizado por LLM, CTA de reunion, y pie profesional."
- primaryColor: '#059669' (verde)
- header: 1 row — logo izquierda (src:'', alt:'Logo', width:120, align:'left')
- body: 2 rows — instruction (text: 'Cuerpo personalizado de respuesta comercial') + text CTA (content: '**Reserva una reunion con nosotros:**\n[Agendar reunion](https://calendly.com)')
- footer: 1 row — text (content: 'Un saludo,\n**Equipo Comercial**')

**SEED-04 — Notificacion Interna** (id: seed-tpl-notificacion, category: notification)
- description: "Plantilla minimalista para notificaciones internas. Solo instruccion de cuerpo y pie basico."
- primaryColor: '#6B7280' (gris)
- header: rows vacio []
- body: 1 row — instruction (text: 'Contenido de la notificacion interna')
- footer: 1 row — text (content: '---\n*Notificacion automatica de DoCatFlow*')

Estilos comunes para todos: backgroundColor:'#ffffff', fontFamily:'Arial, sans-serif', textColor:'#333333', maxWidth:600.

IDs de rows/columns: usar secuencia unica por template (r1,r2... c1,c2...).

IMPORTANTE: NO modificar el seed basico existente (seed-template-basic). Los nuevos seeds van en un bloque separado despues.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit</automated>
  </verify>
  <done>4 seeds insertados en db.ts con INSERT OR IGNORE, TypeScript compila sin errores, IDs fijos para idempotencia.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar documentacion y verificar i18n + build</name>
  <files>.planning/knowledge/GUIA_USUARIO.md, .planning/knowledge/CONNECTORS.md, app/messages/es.json, app/messages/en.json</files>
  <action>
**2a. GUIA_USUARIO.md** — Anadir seccion "Plantillas de Email" en la zona de modulos principales:
- Que es: editor visual de plantillas email con bloques arrastrables
- Acceso: /catpower/templates
- 5 tipos de bloque: Logo, Imagen, Video, Texto, Instruccion LLM
- Categorias: General, Corporativa, Comercial, Informe, Notificacion
- 5 plantillas incluidas de serie (Basica, Corporativa Educa360, Informe de Leads, Respuesta Comercial, Notificacion Interna)
- Integracion: los agentes en Canvas usan el skill "Maquetador de Email" para seleccionar y rellenar plantillas automaticamente
- Instrucciones LLM: bloques especiales que el agente rellena con contenido real al generar el email

**2b. CONNECTORS.md** — Anadir entrada para el conector email_template:
- Numero: siguiente al ultimo existente (probablemente #10)
- Nombre: Plantillas Email Corporativas
- Tipo: email_template
- ID seed: seed-email-template
- Emoji: paleta de pintor
- 3 tools: list_email_templates (lista templates activos, filtro por categoria), get_email_template (obtiene estructura con bloques), render_email_template (genera HTML final con variables)
- Seguir el formato exacto de las entradas existentes en CONNECTORS.md

**2c. Verificar i18n** — Revisar que es.json y en.json tienen las claves de catpower.templates completas. Segun la investigacion, ya estan completas. Si falta alguna clave nueva introducida en fases 99-105 (buscar "t(" en archivos de templates que no tengan clave correspondiente), anadirla. Si todo esta bien, no tocar.

**2d. Build verificacion** — Ejecutar npm run build para confirmar que todo compila limpio.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build</automated>
  </verify>
  <done>GUIA_USUARIO.md tiene seccion Email Templates, CONNECTORS.md tiene entrada email_template, i18n completo, npm run build pasa sin errores.</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npx tsc --noEmit` — TypeScript compila
2. `cd /home/deskmath/docflow/app && npm run build` — Build completo pasa
3. Buscar los 4 IDs en db.ts: `grep -c 'seed-tpl-' app/src/lib/db.ts` debe dar 4+
4. Verificar docs: `grep -l 'Plantillas de Email' .planning/knowledge/GUIA_USUARIO.md` y `grep -l 'email_template' .planning/knowledge/CONNECTORS.md`
</verification>

<success_criteria>
1. 4 templates seed con IDs fijos en db.ts (seed-tpl-corporativa, seed-tpl-informe-leads, seed-tpl-respuesta-comercial, seed-tpl-notificacion)
2. Cada seed usa INSERT OR IGNORE (idempotente)
3. GUIA_USUARIO.md tiene seccion de Plantillas de Email
4. CONNECTORS.md tiene entrada #10 email_template con 3 tools
5. npm run build pasa sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/106-seeds-docs-i18n/106-01-SUMMARY.md`
</output>
