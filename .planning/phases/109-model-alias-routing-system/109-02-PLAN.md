---
phase: 109-model-alias-routing-system
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - app/src/app/api/agents/generate/route.ts
  - app/src/app/api/skills/generate/route.ts
  - app/src/app/api/workers/generate/route.ts
  - app/src/app/api/testing/generate/route.ts
  - app/src/lib/services/execute-catpaw.ts
  - app/src/lib/services/task-executor.ts
  - app/src/lib/services/catbot-tools.ts
  - app/src/app/api/cat-paws/[id]/chat/route.ts
  - app/src/lib/services/bundle-importer.ts
autonomous: true
requirements: [ALIAS-06, ALIAS-07]

must_haves:
  truths:
    - "Agent/skill/worker generation routes use resolveAlias('generate-content') instead of 'gemini-main'"
    - "Testing generate route uses resolveAlias('generate-content')"
    - "CatPaw execution uses resolveAlias('agent-task') as fallback when paw.model is not set"
    - "Task executor uses resolveAlias('agent-task') instead of 'gemini-main'"
    - "CatBot tools create_cat_paw uses resolveAlias('agent-task') for default model"
    - "Cat-paws chat route uses resolveAlias('agent-task') as fallback"
    - "Bundle importer uses resolveAlias('generate-content') for import default model"
    - "Per-entity model overrides (paw.model) still bypass alias resolution"
    - "npm run build succeeds after all migrations"
  artifacts:
    - path: "app/src/app/api/agents/generate/route.ts"
      provides: "Migrated to resolveAlias('generate-content')"
      contains: "resolveAlias"
    - path: "app/src/lib/services/execute-catpaw.ts"
      provides: "Migrated to resolveAlias('agent-task')"
      contains: "resolveAlias"
    - path: "app/src/lib/services/task-executor.ts"
      provides: "Migrated to resolveAlias('agent-task')"
      contains: "resolveAlias"
  key_links:
    - from: "app/src/app/api/agents/generate/route.ts"
      to: "app/src/lib/services/alias-routing.ts"
      via: "import resolveAlias"
      pattern: "resolveAlias.*generate-content"
    - from: "app/src/lib/services/execute-catpaw.ts"
      to: "app/src/lib/services/alias-routing.ts"
      via: "import resolveAlias"
      pattern: "resolveAlias.*agent-task"
    - from: "app/src/lib/services/task-executor.ts"
      to: "app/src/lib/services/alias-routing.ts"
      via: "import resolveAlias"
      pattern: "resolveAlias.*agent-task"
---

<objective>
Migrate the "easy" subsystems from hardcoded 'gemini-main' to resolveAlias() calls. These are lower-risk callsites: generation routes (agents, skills, workers, testing), CatPaw execution, task executor, catbot-tools default model, cat-paws chat, and bundle-importer.

Purpose: First batch of migrations covering 9 files. These subsystems either already use resolveModel() (easy upgrade) or have simple fallback patterns (string replacement + await).
Output: 9 files migrated, zero hardcoded 'gemini-main' in Plan 02 targets.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-model-alias-routing-system/109-CONTEXT.md
@.planning/phases/109-model-alias-routing-system/109-RESEARCH.md
@.planning/phases/109-model-alias-routing-system/109-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -- the service these migrations will import -->

From app/src/lib/services/alias-routing.ts (created in Plan 01):
```typescript
export async function resolveAlias(alias: string): Promise<string>;
// alias values: 'chat-rag' | 'process-docs' | 'agent-task' | 'catbot' | 'generate-content' | 'embed' | 'canvas-agent' | 'canvas-format'
```

Migration pattern (from RESEARCH.md):
```typescript
// BEFORE:
const model = requestedModel || 'gemini-main';

// AFTER:
import { resolveAlias } from '@/lib/services/alias-routing';
const model = requestedModel || await resolveAlias('generate-content');

// For per-entity overrides (CatPaw, CatBrain) -- override bypasses alias:
const rawModel = paw.model || await resolveAlias('agent-task');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate generation routes + catbot-tools + bundle-importer</name>
  <files>app/src/app/api/agents/generate/route.ts, app/src/app/api/skills/generate/route.ts, app/src/app/api/workers/generate/route.ts, app/src/app/api/testing/generate/route.ts, app/src/lib/services/catbot-tools.ts, app/src/lib/services/bundle-importer.ts</files>
  <action>
    For each file, apply the migration pattern:

    1. **app/api/agents/generate/route.ts** (~line 18): Replace `model || 'gemini-main'` with `model || await resolveAlias('generate-content')`. Add import of resolveAlias at top.

    2. **app/api/skills/generate/route.ts** (~line 15): Same pattern as agents -- `model || await resolveAlias('generate-content')`.

    3. **app/api/workers/generate/route.ts** (~line 15): Same pattern -- `model || await resolveAlias('generate-content')`.

    4. **app/api/testing/generate/route.ts** (~line 100): Replace `model: 'gemini-main'` with `model: await resolveAlias('generate-content')`.

    5. **lib/services/catbot-tools.ts** (~line 867): Replace `'gemini-main'` default model in create_cat_paw with `await resolveAlias('agent-task')`. Note: line 58 is schema description text -- update description text only (not a runtime call), mentioning "configured via alias routing" instead of hardcoded model name.

    6. **lib/services/bundle-importer.ts** (~line 144): Replace import default model `'gemini-main'` with `await resolveAlias('generate-content')`.

    For ALL files:
    - Add `import { resolveAlias } from '@/lib/services/alias-routing';` at top
    - Ensure the containing function is already async (API route handlers are; check services)
    - Use `await` for resolveAlias call
    - Do NOT change any other logic -- only the model string resolution

    Run `cd /home/deskmath/docflow/app && npm run build` after all changes.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 6 files migrated with no 'gemini-main' hardcoded in generation routes, catbot-tools default, or bundle-importer
    - npm run build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate CatPaw + Task executor + Cat-paws chat route</name>
  <files>app/src/lib/services/execute-catpaw.ts, app/src/lib/services/task-executor.ts, app/src/app/api/cat-paws/[id]/chat/route.ts</files>
  <action>
    These files already use resolveModel() or similar patterns -- migration is upgrading to resolveAlias:

    1. **lib/services/execute-catpaw.ts** (~line 465): Currently calls resolveModel(). Replace the fallback `'gemini-main'` with `await resolveAlias('agent-task')`. CRITICAL: Per-entity override pattern -- if `paw.model` is set, use it directly (validated via Discovery separately). Only fall back to resolveAlias when no per-entity model. Pattern:
       ```typescript
       const rawModel = paw.model || await resolveAlias('agent-task');
       ```
       Remove the existing resolveModel() call if it was only used for the hardcoded fallback. If resolveModel() was also validating the model, keep it for paw.model validation only.

    2. **lib/services/task-executor.ts** (~lines 22, 499, 570): Replace all `'gemini-main'` references with `await resolveAlias('agent-task')`. Check all three locations. These are fallback defaults for task step execution.

    3. **app/api/cat-paws/[id]/chat/route.ts** (~line 480): Replace `paw.model || process['env']['CHAT_MODEL'] || 'gemini-main'` with `paw.model || await resolveAlias('agent-task')`. The resolveAlias already handles CHAT_MODEL env fallback internally.

    For ALL files:
    - Add `import { resolveAlias } from '@/lib/services/alias-routing';`
    - Preserve per-entity model overrides (paw.model, task step model) -- these bypass alias
    - Use `await` on all resolveAlias calls
    - Ensure async context exists

    Run `cd /home/deskmath/docflow/app && npm run build` after all changes.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 3 files migrated: execute-catpaw, task-executor, cat-paws chat route
    - Per-entity model overrides preserved (paw.model still works as direct override)
    - No 'gemini-main' hardcoded in any Plan 02 target file
    - npm run build succeeds
  </done>
</task>

</tasks>

<verification>
- `cd /home/deskmath/docflow/app && npm run build` -- succeeds
- `cd /home/deskmath/docflow/app/src && grep -rn "'gemini-main'" app/api/agents/generate/ app/api/skills/generate/ app/api/workers/generate/ app/api/testing/generate/ lib/services/execute-catpaw.ts lib/services/task-executor.ts lib/services/catbot-tools.ts app/api/cat-paws/ lib/services/bundle-importer.ts 2>/dev/null` -- returns empty (no hardcoded refs remain in Plan 02 targets)
- `cd /home/deskmath/docflow/app/src && grep -rn "resolveAlias" app/api/agents/generate/ app/api/skills/generate/ app/api/workers/generate/ lib/services/execute-catpaw.ts lib/services/task-executor.ts 2>/dev/null | wc -l` -- shows migration count
</verification>

<success_criteria>
- Zero 'gemini-main' hardcoded in any of the 9 Plan 02 target files
- All 9 files import and use resolveAlias() with correct alias name
- Per-entity overrides (paw.model) preserved -- bypass alias resolution
- npm run build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/109-model-alias-routing-system/109-02-SUMMARY.md`
</output>
