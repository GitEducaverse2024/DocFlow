---
phase: 126-catbot-knowledge-protocol
plan: 02
type: execute
wave: 2
depends_on: [126-01]
files_modified:
  - app/src/lib/services/catbot-prompt-assembler.ts
  - app/src/lib/__tests__/catbot-prompt-assembler.test.ts
autonomous: true
requirements: [KPROTO-01, KPROTO-04, KPROTO-05]

must_haves:
  truths:
    - "El PromptAssembler incluye una seccion 'Protocolo de Conocimiento' en el system prompt"
    - "Las instrucciones del protocolo cubren los 4 tools de knowledge: query_knowledge, search_documentation, save_learned_entry, log_knowledge_gap"
    - "El prompt instruye a CatBot a llamar log_knowledge_gap cuando query_knowledge devuelve 0 resultados"
    - "El reasoning protocol incluye consultar knowledge antes de clasificar como COMPLEJO"
  artifacts:
    - path: "app/src/lib/services/catbot-prompt-assembler.ts"
      provides: "buildKnowledgeProtocol() function + reasoning protocol modification"
      contains: "Protocolo de Conocimiento"
    - path: "app/src/lib/__tests__/catbot-prompt-assembler.test.ts"
      provides: "Tests for KPROTO-01, KPROTO-04, KPROTO-05"
      contains: "Protocolo de Conocimiento"
  key_links:
    - from: "app/src/lib/services/catbot-prompt-assembler.ts"
      to: "build() sections array"
      via: "sections.push({ id: 'knowledge_protocol', priority: 1, content: buildKnowledgeProtocol() })"
      pattern: "knowledge_protocol.*priority.*1"
    - from: "buildReasoningProtocol()"
      to: "query_knowledge reference"
      via: "text instruction before COMPLEJO"
      pattern: "query_knowledge.*COMPLEJO"
---

<objective>
Anadir la seccion P1 "Protocolo de Conocimiento" al PromptAssembler y modificar el reasoning protocol para que CatBot sepa cuando usar cada herramienta de knowledge y reporte gaps automaticamente.

Purpose: CatBot necesita instrucciones explicitas sobre como usar su sistema de conocimiento. Sin esto, los tools existen pero CatBot no sabe cuando usarlos estrategicamente ni cuando reportar gaps.
Output: PromptAssembler actualizado con knowledge protocol, reasoning protocol modificado, tests extendidos.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/126-catbot-knowledge-protocol/126-RESEARCH.md
@.planning/phases/126-catbot-knowledge-protocol/126-01-SUMMARY.md

@app/src/lib/services/catbot-prompt-assembler.ts
@app/src/lib/__tests__/catbot-prompt-assembler.test.ts
</context>

<interfaces>
<!-- Key contracts from Plan 01 that this plan references -->

From app/src/lib/services/catbot-prompt-assembler.ts (section push pattern):
```typescript
// P1 sections (already existing):
sections.push({ id: 'user_profile', priority: 1, content: buildUserProfileSection(ctx) });
sections.push({ id: 'reasoning_protocol', priority: 1, content: buildReasoningProtocol() });
sections.push({ id: 'matched_recipe', priority: 1, content: recipeContent });
sections.push({ id: 'page_knowledge', priority: 1, content: pageKnowledge });
sections.push({ id: 'platform_overview', priority: 1, content: buildPlatformOverview() });
sections.push({ id: 'skills_protocols', priority: 1, content: buildSkillsProtocols() });
sections.push({ id: 'canvas_protocols', priority: 1, content: buildCanvasProtocols() });
```

From app/src/lib/services/catbot-prompt-assembler.ts (buildReasoningProtocol, line 572):
```typescript
function buildReasoningProtocol(): string {
  return `## Protocolo de Razonamiento Adaptativo
  // ... SIMPLE, MEDIO, COMPLEJO levels ...
  // COMPLEJO section needs knowledge consultation step added
  `;
}
```

From app/src/lib/__tests__/catbot-prompt-assembler.test.ts (test pattern):
```typescript
import { build, PromptContext } from '../services/catbot-prompt-assembler';
const baseCtx: PromptContext = { hasSudo: false, catbotConfig: {} };
it('includes something', () => {
  const result = build(baseCtx);
  expect(result).toContain('expected text');
});
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildKnowledgeProtocol() P1 section + reasoning protocol update + tests</name>
  <files>app/src/lib/services/catbot-prompt-assembler.ts, app/src/lib/__tests__/catbot-prompt-assembler.test.ts</files>
  <behavior>
    - Test: build(baseCtx) output contains "Protocolo de Conocimiento" (KPROTO-01)
    - Test: build(baseCtx) output contains "query_knowledge" in knowledge protocol section (KPROTO-01)
    - Test: build(baseCtx) output contains "search_documentation" in knowledge protocol section (KPROTO-01)
    - Test: build(baseCtx) output contains "save_learned_entry" in knowledge protocol section (KPROTO-01)
    - Test: build(baseCtx) output contains "log_knowledge_gap" in knowledge protocol section (KPROTO-01, KPROTO-04)
    - Test: build(baseCtx) output contains instruction to call log_knowledge_gap when 0 results (KPROTO-04)
    - Test: build(baseCtx) output contains "query_knowledge" near "COMPLEJO" in reasoning protocol (KPROTO-05)
  </behavior>
  <action>
1. **Write failing tests first** in catbot-prompt-assembler.test.ts. Add a new describe block "Knowledge Protocol (KPROTO)":
   - Test KPROTO-01: `build(baseCtx)` contains "Protocolo de Conocimiento"
   - Test KPROTO-01: `build(baseCtx)` contains all 4 tool names (query_knowledge, search_documentation, save_learned_entry, log_knowledge_gap)
   - Test KPROTO-04: `build(baseCtx)` contains "log_knowledge_gap" AND "0 resultados" (instruction to auto-call on empty results)
   - Test KPROTO-05: `build(baseCtx)` contains "query_knowledge" AND reasoning protocol references it before COMPLEJO. Check that the output contains a string like "consulta query_knowledge" near "COMPLEJO".

2. **Create buildKnowledgeProtocol() function** in catbot-prompt-assembler.ts (after buildReasoningProtocol, before buildRecipeSection):
   - Returns the protocol text as specified in RESEARCH.md Pattern 4
   - Keep it CONCISE (under 800 chars to avoid Pitfall 2 — token budget pressure on Libre tier)
   - Must mention all 4 tools: query_knowledge, search_documentation, save_learned_entry, log_knowledge_gap
   - Must include the "gap obligatorio" rule: when query_knowledge returns 0 results AND CatBot doesn't have the answer, MUST call log_knowledge_gap before responding
   - Must include the escalation chain: query_knowledge -> search_documentation -> log_knowledge_gap

3. **Register the section** in build() function as P1, after reasoning_protocol and before matched_recipe:
   ```typescript
   sections.push({ id: 'knowledge_protocol', priority: 1, content: buildKnowledgeProtocol() });
   ```

4. **Modify buildReasoningProtocol()** (KPROTO-05):
   - In the COMPLEJO section, add before "Accion: Razona el enfoque...":
     "Antes de clasificar como COMPLEJO, consulta query_knowledge para verificar si ya tienes informacion relevante que simplifique el problema."
   - This is a single line addition, do NOT rewrite the entire function.

5. **Run tests** to verify all pass.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts -x</automated>
  </verify>
  <done>buildKnowledgeProtocol() existe como P1 section, menciona los 4 tools, incluye regla de gap obligatorio con log_knowledge_gap, reasoning protocol referencia query_knowledge antes de COMPLEJO, todos los tests verdes.</done>
</task>

</tasks>

<verification>
- `cd ~/docflow/app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts -x` -- all prompt assembler tests pass (existing + new)
- `cd ~/docflow/app && npx vitest run` -- full suite green
- Manual: Ask CatBot about a non-existent feature, verify it calls log_knowledge_gap (KPROTO-04 runtime behavior)
</verification>

<success_criteria>
- build() output contiene "Protocolo de Conocimiento" con instrucciones de los 4 tools
- build() output contiene regla explicita de llamar log_knowledge_gap cuando query_knowledge devuelve 0 resultados
- buildReasoningProtocol() menciona consultar query_knowledge antes de clasificar como COMPLEJO
- La seccion de knowledge protocol es concisa (< 800 chars) para no explotar el budget en Libre tier
- Todos los tests verdes incluyendo los existentes de PromptAssembler
</success_criteria>

<output>
After completion, create `.planning/phases/126-catbot-knowledge-protocol/126-02-SUMMARY.md`
</output>
