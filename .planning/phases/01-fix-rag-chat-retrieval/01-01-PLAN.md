---
phase: 01-fix-rag-chat-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/api/projects/[id]/chat/route.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - CHAT-06
  - CHAT-07

must_haves:
  truths:
    - "El chat devuelve contenido indexado cuando el usuario pregunta algo que existe en los chunks RAG"
    - "El chat usa exactamente los mismos servicios compartidos (ollama.ts, qdrant.ts) que 'Probar consulta'"
    - "Los logs del chat muestran query, cantidad de chunks, scores y longitud del contexto"
    - "npm run build pasa sin errores"
  artifacts:
    - path: "app/src/app/api/projects/[id]/chat/route.ts"
      provides: "Endpoint de chat RAG reescrito con servicios compartidos"
      contains: "import { ollama }"
    - path: "app/src/app/api/projects/[id]/chat/route.ts"
      provides: "Exporta force-dynamic para evitar prerenderizado estatico"
      contains: "export const dynamic"
  key_links:
    - from: "app/src/app/api/projects/[id]/chat/route.ts"
      to: "app/src/lib/services/ollama.ts"
      via: "import { ollama } — ollama.getEmbedding()"
      pattern: "ollama\\.getEmbedding"
    - from: "app/src/app/api/projects/[id]/chat/route.ts"
      to: "app/src/lib/services/qdrant.ts"
      via: "import { qdrant } — qdrant.search()"
      pattern: "qdrant\\.search"
    - from: "app/src/app/api/projects/[id]/chat/route.ts"
      to: "LiteLLM /v1/chat/completions"
      via: "fetch con contexto de chunks como system prompt"
      pattern: "chat/completions"
---

<objective>
Reescribir el endpoint de chat para que use los servicios compartidos ollama.ts y qdrant.ts en vez de llamadas fetch manuales duplicadas, igualando la logica de busqueda de "Probar consulta" que ya funciona correctamente.

Purpose: El chat actualmente devuelve "no tengo informacion" porque sus llamadas HTTP manuales a Ollama/Qdrant fallan silenciosamente o difieren de la logica probada. Al usar los servicios compartidos, el chat encontrara los mismos chunks que "Probar consulta".

Output: Un archivo `chat/route.ts` reescrito que encuentra y devuelve contenido indexado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Servicios compartidos que el executor debe usar directamente -->

From app/src/lib/services/ollama.ts:
```typescript
export const ollama = {
  async getEmbedding(text: string, model: string = 'nomic-embed-text'): Promise<number[]>,
  guessModelFromVectorSize(vectorSize: number): string,
};
```

From app/src/lib/services/qdrant.ts:
```typescript
export const qdrant = {
  async getCollectionInfo(name: string): Promise<any>,  // returns { result: { config: { params: { vectors: { size } } } } }
  async search(name: string, vector: number[], limit: number = 5): Promise<any>,  // returns { result: [...] }
};
```

From app/src/app/api/projects/[id]/rag/query/route.ts (referencia — el patron a seguir):
```typescript
// Este endpoint YA funciona correctamente y encuentra chunks.
// El chat debe seguir exactamente este patron de busqueda.
import { qdrant } from '@/lib/services/qdrant';
import { ollama } from '@/lib/services/ollama';

const collectionInfo = await qdrant.getCollectionInfo(project.rag_collection);
const vectorSize = collectionInfo.result?.config?.params?.vectors?.size || 768;
const model = ollama.guessModelFromVectorSize(vectorSize);
const queryVector = await ollama.getEmbedding(query, model);
const searchResults = await qdrant.search(project.rag_collection, queryVector, limit);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir chat/route.ts con servicios compartidos</name>
  <files>app/src/app/api/projects/[id]/chat/route.ts</files>
  <action>
Reescribir completamente `app/src/app/api/projects/[id]/chat/route.ts`. El nuevo archivo debe:

1. **Imports**: Importar `ollama` de `@/lib/services/ollama`, `qdrant` de `@/lib/services/qdrant`, `db` de `@/lib/db`, `NextResponse` de `next/server`.

2. **Export force-dynamic**: Agregar `export const dynamic = 'force-dynamic';` al inicio (lee env vars, debe ser dinamico).

3. **POST handler** — Seguir este flujo exacto (copiado del endpoint query que funciona):
   a. Extraer `projectId` de params, `message` del body JSON.
   b. Validar que `message` existe, retornar 400 si no.
   c. Consultar proyecto de SQLite: `SELECT * FROM projects WHERE id = ?`
   d. Validar `rag_enabled` y `rag_collection`, retornar 400 si no.
   e. **LOG**: `console.log('[Chat] Consulta recibida:', message)`
   f. Obtener info de coleccion: `const collectionInfo = await qdrant.getCollectionInfo(project.rag_collection)`
   g. Si no existe coleccion, retornar 404.
   h. Determinar modelo de embedding: `const vectorSize = collectionInfo.result?.config?.params?.vectors?.size || 768; const model = ollama.guessModelFromVectorSize(vectorSize);`
   i. Generar embedding: `const queryVector = await ollama.getEmbedding(message, model)`
   j. Buscar en Qdrant con limite 10: `const searchResults = await qdrant.search(project.rag_collection, queryVector, 10)`
   k. Extraer resultados: `const results = searchResults.result || []`
   l. **LOG**: `console.log('[Chat] Chunks encontrados:', results.length)` y `console.log('[Chat] Scores:', results.map((r: any) => r.score))`
   m. NO filtrar por score_threshold — usar todos los resultados.
   n. Construir contexto: `const contextChunks = results.map((r: any, i: number) => \`[Fuente \${i+1}] \${r.payload.text}\`).join('\n\n')`
   o. **LOG**: `console.log('[Chat] Longitud del contexto:', contextChunks.length, 'caracteres')`
   p. Llamar a LiteLLM (esta parte se mantiene igual):
      - URL: `process['env']['LITELLM_URL'] || 'http://192.168.1.49:4000'`
      - API key: `process['env']['LITELLM_API_KEY'] || 'sk-antigravity-gateway'`
      - Model: `process['env']['CHAT_MODEL'] || 'gemini-main'`
      - System prompt: `Eres el bot experto del proyecto "${project.name}". Responde basandote UNICAMENTE en el siguiente contexto extraido de la documentacion del proyecto. Si la informacion no esta en el contexto, di que no tienes esa informacion.\n\nContexto:\n${contextChunks}`
      - User message: `message`
   q. Extraer reply de `chatData.choices[0]?.message?.content`
   r. Retornar `{ reply, sources: results }`
   s. Catch: `console.error('[Chat] Error:', error)`, retornar 500.

4. **Todos los comentarios y logs en espanol** (sin acentos en strings es OK, con acentos tambien).

5. **NO usar**: llamadas fetch directas a Ollama ni a Qdrant. Solo servicios compartidos.

6. **Usar bracket notation** para env vars: `process['env']['VAR']` (NO process.env.VAR).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit app/src/app/api/projects/[id]/chat/route.ts 2>&1 | head -20; grep -c "ollama\." src/app/api/projects/[id]/chat/route.ts; grep -c "qdrant\." src/app/api/projects/[id]/chat/route.ts; grep "force-dynamic" src/app/api/projects/[id]/chat/route.ts</automated>
  </verify>
  <done>chat/route.ts usa ollama.getEmbedding y qdrant.search (servicios compartidos), busca 10 resultados, tiene force-dynamic, logs diagnosticos en espanol, y compila sin errores TypeScript.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar build completo y revisar coherencia</name>
  <files>app/src/app/api/projects/[id]/chat/route.ts</files>
  <action>
1. Ejecutar `cd /home/deskmath/docflow/app && npm run build` y verificar que pasa sin errores.
2. Si hay errores de build, corregirlos en chat/route.ts (errores tipicos: tipos faltantes, imports incorrectos).
3. Verificar que el archivo NO contiene ninguna llamada `fetch` directa a Ollama (`/api/embed`) ni a Qdrant (`/collections/`). Las unicas llamadas fetch permitidas son a LiteLLM (`/v1/chat/completions`).
4. Verificar que `export const dynamic = 'force-dynamic'` esta presente.
5. Verificar que el limite de busqueda es 10 (no 5).
6. Verificar que no hay filtrado por score_threshold.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build pasa exitosamente. El archivo chat/route.ts no tiene fetch directo a Ollama ni Qdrant (solo a LiteLLM), usa limit 10, tiene force-dynamic, y no filtra por score.</done>
</task>

</tasks>

<verification>
1. `grep "import.*ollama" app/src/app/api/projects/[id]/chat/route.ts` — confirma import del servicio compartido
2. `grep "import.*qdrant" app/src/app/api/projects/[id]/chat/route.ts` — confirma import del servicio compartido
3. `grep "force-dynamic" app/src/app/api/projects/[id]/chat/route.ts` — confirma export dinamico
4. `grep "limit.*10\|, 10)" app/src/app/api/projects/[id]/chat/route.ts` — confirma limite 10
5. `grep "console.log" app/src/app/api/projects/[id]/chat/route.ts` — confirma logs diagnosticos
6. `cd app && npm run build` — confirma build exitoso
</verification>

<success_criteria>
1. chat/route.ts importa y usa ollama.getEmbedding() y qdrant.search() (mismos servicios que rag/query)
2. Busqueda con limite 10, sin filtro de score
3. Logs: query recibida, chunks encontrados con scores, longitud del contexto
4. CHAT_MODEL default gemini-main, LiteLLM call se mantiene
5. export const dynamic = 'force-dynamic' presente
6. npm run build pasa sin errores
7. Todos los comentarios y logs en espanol
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-rag-chat-retrieval/01-01-SUMMARY.md`
</output>
