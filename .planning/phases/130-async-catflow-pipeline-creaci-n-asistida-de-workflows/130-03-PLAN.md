---
phase: 130-async-catflow-pipeline-creaci-n-asistida-de-workflows
plan: 03
type: execute
wave: 3
depends_on:
  - "130-02"
files_modified:
  - app/src/lib/services/intent-job-executor.ts
  - app/src/lib/services/canvas-flow-designer.ts
  - app/src/lib/__tests__/canvas-flow-designer.test.ts
autonomous: true
requirements:
  - PIPE-04
must_haves:
  truths:
    - "scanResources() retorna {catPaws, catBrains, skills, connectors} con datos reales de docflow.db"
    - "El architect phase recibe resources en el user prompt y responde con flow_data.nodes/edges mapeados a recursos reales"
    - "Si LLM architect devuelve needs_cat_paws, el executor pausa con pipeline_phase='awaiting_user' y serializa la lista en progress_message.cat_paws_needed"
    - "Node types generados por el architect estan restringidos a la lista valida (agent, catpaw, catbrain, condition, iterator, multiagent, scheduler, checkpoint, connector); los invalidos marcan el job como failed con error claro"
    - "Canvas creado tiene flow_data JSON parseable con nodes + edges arrays"
  artifacts:
    - path: "app/src/lib/services/canvas-flow-designer.ts"
      provides: "validateFlowData(flow_data): {valid, errors} helper + VALID_NODE_TYPES const + scanCanvasResources(db) helper"
      min_lines: 60
    - path: "app/src/lib/services/intent-job-executor.ts"
      provides: "scanResources usa scanCanvasResources helper; architect phase valida flow_data antes de INSERT canvas; needs_cat_paws path completo con persistencia en progress_message"
      contains: "validateFlowData"
    - path: "app/src/lib/__tests__/canvas-flow-designer.test.ts"
      provides: "Tests unit de validateFlowData + scanCanvasResources con mocked db"
      min_lines: 80
  key_links:
    - from: "app/src/lib/services/intent-job-executor.ts"
      to: "app/src/lib/services/canvas-flow-designer.ts"
      via: "import { validateFlowData, scanCanvasResources, VALID_NODE_TYPES }"
      pattern: "canvas-flow-designer"
    - from: "app/src/lib/services/intent-job-executor.ts"
      to: "docflow.db canvases table"
      via: "db.prepare('INSERT INTO canvases ...').run(canvasId, name, description, 'mixed', 'idle', JSON.stringify(flow_data))"
      pattern: "INSERT INTO canvases"
---

<objective>
Extraer Canvas Flow Designer como modulo reutilizable (scanCanvasResources + validateFlowData + VALID_NODE_TYPES), integrarlo en IntentJobExecutor para validar el output del arquitecto antes de INSERT, y cubrir completamente el path needs_cat_paws (pausa + persistencia + notificacion stub que Plan 04 conectara).

Purpose: Garantizar que el LLM architect no puede crashear el canvas executor con node types invalidos (Pitfall 8) y aislar la logica de resource scanning / flow_data shape validation en un modulo testeable.
Output: canvas-flow-designer.ts con helpers, intent-job-executor.ts ampliado con validacion + flujo needs_cat_paws completo, tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/130-async-catflow-pipeline-creaci-n-asistida-de-workflows/130-RESEARCH.md
@.planning/phases/130-async-catflow-pipeline-creaci-n-asistida-de-workflows/130-02-PLAN.md
@app/src/lib/services/canvas-executor.ts
@app/src/lib/services/intent-job-executor.ts

<interfaces>
From canvas-executor.ts lines 461-600:
- Node types reconocidos en el switch: 'agent', 'catpaw', 'catbrain', 'condition', 'iterator', 'multiagent', 'scheduler', 'checkpoint', 'connector'
- Shape de data por tipo (para documentar el ARCHITECT_PROMPT si necesita refinamiento):
  - agent / catpaw: { agentId, instructions, useRag? }
  - catbrain: { catbrainId, ragQuery }
  - condition: { condition }
  - connector: { connectorId, operation }

From docflow.db (db.ts):
- cat_paws: id, name, description, mode, system_prompt, is_active
- catbrains: id, name, description
- skills: id, name, description
- connectors: id, name, type
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — canvas-flow-designer test scaffold (RED)</name>
  <files>app/src/lib/__tests__/canvas-flow-designer.test.ts</files>
  <behavior>
    validateFlowData:
    - Input valido (nodes con types validos + edges) → {valid: true, errors: []}
    - Node con type='pipeline' (invalido) → {valid: false, errors: conteniendo 'pipeline'}
    - Node sin id → invalid
    - Edge sin source o target → invalid
    - Edge referenciando node inexistente → invalid
    - flow_data sin nodes o sin edges → invalid

    scanCanvasResources(db):
    - Devuelve objeto {catPaws, catBrains, skills, connectors} con arrays
    - Usa LIMIT 50 en queries (no devuelve >50 por tabla)
    - Si tabla no existe (mocked db throws), devuelve array vacio sin crashear

    VALID_NODE_TYPES: const array exportado con los 9 types conocidos.
  </behavior>
  <action>
    Crea app/src/lib/__tests__/canvas-flow-designer.test.ts:
    1. Import { validateFlowData, scanCanvasResources, VALID_NODE_TYPES } from '@/lib/services/canvas-flow-designer'
    2. Describe "VALID_NODE_TYPES" con 1 it() assertEquals array incluye los 9 types
    3. Describe "validateFlowData" con 6-8 it() cubriendo los casos del behavior. Usa objetos literales para casos de prueba:
       ```ts
       const validFlow = { nodes: [{id:'n1', type:'agent', data:{agentId:'a'}}, {id:'n2', type:'catbrain', data:{catbrainId:'cb'}}], edges: [{id:'e1', source:'n1', target:'n2'}] };
       ```
    4. Describe "scanCanvasResources" con 3 it():
       - Mock db con `{prepare: vi.fn(() => ({all: vi.fn(() => [{id:'1', name:'paw'}])}))}` → devuelve estructura con el paw
       - Mock db que throws en una table → array vacio, no crashea
       - Verifica LIMIT 50 en SQL strings via `expect(db.prepare).toHaveBeenCalledWith(expect.stringContaining('LIMIT 50'))`

    Ejecuta: debe fallar (archivo canvas-flow-designer.ts no existe aun).
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/canvas-flow-designer.test.ts 2>&1 | grep -E "(FAIL|PASS|fail)"</automated>
  </verify>
  <done>
    Archivo >=80 lineas, >=10 it() blocks, vitest falla con "module not found".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: canvas-flow-designer module + intent-job-executor integration (GREEN)</name>
  <files>app/src/lib/services/canvas-flow-designer.ts, app/src/lib/services/intent-job-executor.ts</files>
  <behavior>
    canvas-flow-designer.ts:
    - exporta VALID_NODE_TYPES (readonly tuple)
    - exporta validateFlowData(fd: unknown): {valid: boolean; errors: string[]}
      - Check fd es objeto, nodes array, edges array
      - Check cada node tiene id string y type ∈ VALID_NODE_TYPES
      - Check cada edge tiene source/target que existen en nodes
    - exporta scanCanvasResources(db): Record<string, unknown[]>
      - try/catch por tabla (independiente)
      - LIMIT 50 consistente

    intent-job-executor.ts:
    - scanResources() delega a scanCanvasResources(db)
    - Tras architect phase, antes de INSERT INTO canvases, llama validateFlowData(design.flow_data)
    - Si invalid: updateIntentJob con status='failed', error = `Architect output invalid: ${errors.join('; ')}`, return
    - needs_cat_paws path: updateIntentJob con pipeline_phase='awaiting_user', progress_message incluye cat_paws_needed array + message claro, llama notifyUserCatPawApproval (stub logger.info por ahora, Plan 04 lo completa)
  </behavior>
  <action>
    **app/src/lib/services/canvas-flow-designer.ts (nuevo):**
    ```ts
    export const VALID_NODE_TYPES = [
      'agent', 'catpaw', 'catbrain', 'condition',
      'iterator', 'multiagent', 'scheduler', 'checkpoint', 'connector',
    ] as const;
    export type CanvasNodeType = typeof VALID_NODE_TYPES[number];

    export function validateFlowData(fd: unknown): { valid: boolean; errors: string[] } {
      const errors: string[] = [];
      if (!fd || typeof fd !== 'object') return { valid: false, errors: ['flow_data not an object'] };
      const obj = fd as { nodes?: unknown; edges?: unknown };
      if (!Array.isArray(obj.nodes)) errors.push('nodes not an array');
      if (!Array.isArray(obj.edges)) errors.push('edges not an array');
      if (errors.length) return { valid: false, errors };

      const nodeIds = new Set<string>();
      for (const n of obj.nodes as Array<Record<string, unknown>>) {
        if (typeof n.id !== 'string') { errors.push('node missing id'); continue; }
        if (!VALID_NODE_TYPES.includes(n.type as CanvasNodeType)) {
          errors.push(`node ${n.id} has invalid type ${String(n.type)}`);
        }
        nodeIds.add(n.id);
      }
      for (const e of obj.edges as Array<Record<string, unknown>>) {
        if (typeof e.source !== 'string' || typeof e.target !== 'string') {
          errors.push('edge missing source/target'); continue;
        }
        if (!nodeIds.has(e.source)) errors.push(`edge source ${e.source} not found`);
        if (!nodeIds.has(e.target)) errors.push(`edge target ${e.target} not found`);
      }
      return { valid: errors.length === 0, errors };
    }

    type DbLike = { prepare(sql: string): { all(...params: unknown[]): unknown[] } };

    export function scanCanvasResources(db: DbLike): Record<string, unknown[]> {
      const safe = (sql: string): unknown[] => {
        try { return db.prepare(sql).all() ?? []; } catch { return []; }
      };
      return {
        catPaws: safe("SELECT id, name, description, mode, system_prompt FROM cat_paws WHERE is_active = 1 LIMIT 50"),
        catBrains: safe("SELECT id, name, description FROM catbrains LIMIT 50"),
        skills: safe("SELECT id, name, description FROM skills LIMIT 50"),
        connectors: safe("SELECT id, name, type FROM connectors LIMIT 50"),
      };
    }
    ```

    **app/src/lib/services/intent-job-executor.ts (edit):**
    1. `import { validateFlowData, scanCanvasResources } from './canvas-flow-designer';`
    2. Reemplaza `scanResources()` privado para delegar a `scanCanvasResources(db)`.
    3. Despues de `const design = this.parseJSON(...)` y ANTES del bloque `if (design.needs_cat_paws...)`, agrega:
    ```ts
    const validation = validateFlowData(design.flow_data);
    if (!validation.valid) {
      updateIntentJob(job.id, {
        status: 'failed',
        error: `Architect output invalid: ${validation.errors.join('; ')}`,
      });
      return;
    }
    ```
    4. Mejora el bloque needs_cat_paws: incluye el array completo en progress_message.cat_paws_needed para que Plan 04 (notification) lo lea, y llama `await this.notifyUserCatPawApproval(job, design.needs_cat_paws)` (stub logger.info con los nombres).

    5. NO imports sin usar. Si IntentJobRow solo se usa como tipo, usar `import type`.

    Corre los tests de Plan 02 + Plan 03 para asegurar no-regresion.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/canvas-flow-designer.test.ts src/lib/__tests__/intent-job-executor.test.ts && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    Ambos tests 100% verdes. validateFlowData cubre los 6+ casos. scanCanvasResources resiliente. intent-job-executor.ts rechaza flow_data invalido con error claro. npm run build exitoso.
  </done>
</task>

</tasks>

<verification>
- canvas-flow-designer.test.ts verde
- intent-job-executor.test.ts sigue verde (no regresion)
- npm run build exitoso
</verification>

<success_criteria>
- PIPE-04: Canvas Flow Designer valida node types, rechaza invalid, maneja needs_cat_paws pause con persistencia completa
</success_criteria>

<output>
Crear `.planning/phases/130-async-catflow-pipeline-creaci-n-asistida-de-workflows/130-03-SUMMARY.md` al completar.
</output>
