---
phase: 130-async-catflow-pipeline-creaci-n-asistida-de-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/catbot-db.ts
  - app/src/lib/services/catbot-tools.ts
  - app/data/knowledge/settings.json
  - app/src/lib/__tests__/intent-jobs.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-07
  - PIPE-08
must_haves:
  truths:
    - "catbot.db contiene tabla intent_jobs con los 14 campos del schema y acepta INSERT/UPDATE/SELECT"
    - "CatBot puede llamar queue_intent_job y recibir un job_id persistido"
    - "list_my_jobs filtra por context.userId y nunca devuelve jobs de otros usuarios"
    - "cancel_job, approve_pipeline, execute_approved_pipeline y post_execution_decision estan registrados en TOOLS[] con permission gate correcto"
    - "Las 3 tools async (execute_catflow, execute_task, process_source_rag) aparecen con metadata ASYNC visible en getToolsForLLM output"
    - "El test KTREE-02 (knowledge-tools-sync) pasa con las 6 nuevas tools en settings.json"
  artifacts:
    - path: "app/src/lib/catbot-db.ts"
      provides: "CREATE TABLE intent_jobs + IntentJobRow interface + CRUD (createIntentJob, updateIntentJob, getIntentJob, listJobsByUser, getNextPendingJob, countStuckPipelines)"
      contains: "CREATE TABLE IF NOT EXISTS intent_jobs"
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "6 nuevas tools en TOOLS[] + ASYNC_TOOLS const map + executeTool cases + permission gate + USER_SCOPED_TOOLS entries"
      contains: "queue_intent_job"
    - path: "app/data/knowledge/settings.json"
      provides: "6 nombres de tool nuevos en tools[] para satisfacer KTREE-02"
      contains: "queue_intent_job"
    - path: "app/src/lib/__tests__/intent-jobs.test.ts"
      provides: "Unit tests CRUD + ejecucion de las 6 tools + aislamiento por userId + post_execution_decision branches"
      min_lines: 150
  key_links:
    - from: "app/src/lib/services/catbot-tools.ts"
      to: "app/src/lib/catbot-db.ts"
      via: "import { createIntentJob, updateIntentJob, getIntentJob, listJobsByUser, type IntentJobRow }"
      pattern: "from ['\"]@/lib/catbot-db['\"]"
    - from: "app/src/lib/services/catbot-tools.ts"
      to: "context.userId + context.channel + context.channelRef"
      via: "executeTool case usa context fields (NO args) para queue_intent_job y list_my_jobs"
      pattern: "context\\??\\.userId"
    - from: "app/data/knowledge/settings.json"
      to: "app/src/lib/services/catbot-tools.ts"
      via: "tools[] array contiene los 6 nombres nuevos"
      pattern: "queue_intent_job"
    - from: "app/src/lib/services/catbot-tools.ts"
      to: "ASYNC_TOOLS metadata map"
      via: "getToolsForLLM inyecta sufijo (ASYNC - estimated Ns) en description"
      pattern: "ASYNC_TOOLS\\["
---

<objective>
Crear la tabla intent_jobs + CRUD en catbot.db, registrar las 6 nuevas tools de CatBot (queue_intent_job, list_my_jobs, cancel_job, approve_pipeline, execute_approved_pipeline, post_execution_decision), marcar tres tools existentes como ASYNC via un const map ASYNC_TOOLS, y sincronizar settings.json para satisfacer KTREE-02.

Purpose: Fundacion persistente que los planes 02-05 usan. Sin este plan no existe job ni herramienta para encolarlo.
Output: Tabla intent_jobs lista, 6 exports CRUD, 6 tools LLM wired, async metadata visible, y test Wave 0 probando CRUD + ejecucion + aislamiento + post_execution branches.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-async-catflow-pipeline-creaci-n-asistida-de-workflows/130-CONTEXT.md
@.planning/phases/130-async-catflow-pipeline-creaci-n-asistida-de-workflows/130-RESEARCH.md
@.planning/phases/129-intent-queue-promesas-persistentes-de-catbot/129-01-PLAN.md
@app/src/lib/catbot-db.ts
@app/src/lib/services/catbot-tools.ts
@app/data/knowledge/settings.json

<interfaces>
From app/src/lib/catbot-db.ts (patterns to mirror):
- Lines 34-112: catbotDb.exec() template literal — append CREATE TABLE intent_jobs dentro de este bloque
- Lines 103-111: knowledge_gaps table schema — pattern exacto para intent_jobs
- Lines 188-196: KnowledgeGapRow interface — pattern para IntentJobRow
- Lines 511-555: knowledge_gaps CRUD — pattern para intent_jobs CRUD
- Phase 129 IntentRow + CRUD en el mismo archivo: mirror directo

From app/src/lib/services/catbot-tools.ts:
- TOOLS[] array — append 6 nuevas entries siguiendo shape de intent tools (Phase 129)
- executeTool switch — append 6 cases
- getToolsForLLM permission gate (approx line 1007-1010) — agregar allowlist entries
- USER_SCOPED_TOOLS list (Phase 124/129 decision) — agregar los 6 nuevos tool names
- execute_catflow (linea ~249), execute_task, process_source_rag — marcar ASYNC via const map

IntentJobRow shape (copiar del RESEARCH pattern 1):
```ts
export interface IntentJobRow {
  id: string;
  intent_id: string | null;
  user_id: string;
  channel: string;
  channel_ref: string | null;
  pipeline_phase: 'pending'|'strategist'|'decomposer'|'architect'|'awaiting_approval'|'awaiting_user'|'running'|'completed'|'failed'|'cancelled';
  tool_name: string | null;
  tool_args: string | null; // JSON
  canvas_id: string | null;
  status: 'pending'|'running'|'completed'|'failed'|'cancelled';
  progress_message: string; // JSON
  result: string | null;
  error: string | null;
  created_at: string;
  updated_at: string;
  completed_at: string | null;
}
```

ASYNC_TOOLS map:
```ts
const ASYNC_TOOLS: Record<string, { estimated_duration_ms: number }> = {
  execute_catflow: { estimated_duration_ms: 120_000 },
  execute_task: { estimated_duration_ms: 180_000 },
  process_source_rag: { estimated_duration_ms: 240_000 },
};
```
Inyectar `(ASYNC - estimated ${ms/1000}s)` al final de description en getToolsForLLM.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write intent-jobs test scaffold (RED)</name>
  <files>app/src/lib/__tests__/intent-jobs.test.ts</files>
  <behavior>
    CRUD de intent_jobs:
    - createIntentJob({userId, toolName, toolArgs}) inserta fila con status='pending', pipeline_phase='pending'
    - updateIntentJob({progressMessage: {...}}) serializa a JSON en columna progress_message
    - getIntentJob(id) devuelve la fila parseable
    - listJobsByUser('user:a') NO devuelve jobs de 'user:b' (aislamiento)
    - getNextPendingJob() devuelve el mas antiguo pendiente
    - countStuckPipelines() cuenta rows en running >30min

    Tools ejecutables:
    - executeTool('queue_intent_job', {tool_name: 'execute_catflow', original_request: 'x'}, {userId: 'test:u', channel: 'web'}) devuelve {job_id, queued: true}
    - executeTool('list_my_jobs', {}, {userId: 'test:u'}) devuelve los jobs del userId solamente
    - executeTool('cancel_job', {job_id, reason}) marca status='cancelled', pipeline_phase='cancelled'
    - executeTool('post_execution_decision', {job_id, action: 'keep_template'}) hace UPDATE canvases SET is_template = 1
    - executeTool('post_execution_decision', {job_id, action: 'save_recipe'}) llama saveMemory con triggerPatterns derivado de progress_message.goal
    - executeTool('post_execution_decision', {job_id, action: 'delete'}) hace DELETE FROM canvases WHERE id = ?
    - approve_pipeline rechaza si pipeline_phase != 'awaiting_approval'

    Aislamiento: USER_SCOPED_TOOLS incluye los 6 nuevos → cross-user retorna SUDO_REQUIRED.
  </behavior>
  <action>
    Crea app/src/lib/__tests__/intent-jobs.test.ts siguiendo el patron exacto de app/src/lib/__tests__/catbot-intents.test.ts (Phase 129). Estructura:

    1. `vi.hoisted(() => { process.env.CATBOT_DB_PATH = '/tmp/test-intent-jobs.db'; })` ANTES de cualquier import. Mock heavy deps (db.ts canvases/saveMemory) via vi.mock si es necesario.
    2. Import estatico de createIntentJob, updateIntentJob, getIntentJob, listJobsByUser, getNextPendingJob, countStuckPipelines (desde @/lib/catbot-db) y executeTool (desde @/lib/services/catbot-tools).
    3. `beforeEach`: DELETE FROM intent_jobs para aislamiento.
    4. Describe blocks:
       - "intent_jobs CRUD" con 5-6 it() cubriendo create/get/update/list/getNext/countStuck
       - "queue_intent_job tool" — crea via executeTool, verifica DB row
       - "list_my_jobs tool" — user isolation (dos usuarios, cada uno ve solo lo suyo)
       - "cancel_job tool" — transiciona a cancelled
       - "approve_pipeline tool" — rechaza fuera de awaiting_approval, acepta dentro (mock fetch a /api/canvas/{id}/execute)
       - "post_execution_decision tool" — tres subtests para keep_template / save_recipe / delete. Mock saveMemory y db.prepare para canvases.
       - "USER_SCOPED_TOOLS enforcement" — intento de list_my_jobs con userId='user:a' y job de 'user:b' debe resultar en jobs vacios.

    Usa vi.mock para db.ts (prepare().run() y prepare().get() spies) y para catbot-db saveMemory.

    Ejecuta el test: DEBE FALLAR porque no existe catbot-db.ts IntentJobRow ni catbot-tools cases aun (RED de TDD).

    NO importes IntentJobRow como type si no lo usas (ESLint no-unused-vars bloqueara el build).
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/intent-jobs.test.ts 2>&1 | grep -E "(FAIL|fail|PASS)"</automated>
  </verify>
  <done>
    Archivo existe con >=150 lineas, al menos 12 it() blocks, y `npx vitest run` lo ejecuta y falla con errores de "module not found" o "function is not defined" (no con syntax errors).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: intent_jobs schema + IntentJobRow + CRUD en catbot-db.ts (GREEN)</name>
  <files>app/src/lib/catbot-db.ts</files>
  <behavior>
    - CREATE TABLE IF NOT EXISTS intent_jobs con los 14 campos + 3 indices (status, user_status, phase)
    - IntentJobRow interface exportada
    - createIntentJob(job) — INSERT con generateId(), defaults para channel='web', tool_args=JSON.stringify({}), progress_message='{}'
    - updateIntentJob(id, patch) — UPDATE dinamico con Partial<Pick>, progressMessage serializado a JSON, completed_at set cuando status transiciona a completed/failed/cancelled
    - getIntentJob(id), listJobsByUser(userId, opts?), getNextPendingJob(), countStuckPipelines()
    - Todas las funciones exportadas
  </behavior>
  <action>
    Edita app/src/lib/catbot-db.ts:

    1. Dentro del catbotDb.exec() template literal (lineas 34-112), despues del bloque de `intents` de Phase 129, agrega:
    ```sql
    CREATE TABLE IF NOT EXISTS intent_jobs (
      id TEXT PRIMARY KEY,
      intent_id TEXT,
      user_id TEXT NOT NULL,
      channel TEXT DEFAULT 'web',
      channel_ref TEXT,
      pipeline_phase TEXT DEFAULT 'pending',
      tool_name TEXT,
      tool_args TEXT,
      canvas_id TEXT,
      status TEXT DEFAULT 'pending',
      progress_message TEXT DEFAULT '{}',
      result TEXT,
      error TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      completed_at TEXT
    );
    CREATE INDEX IF NOT EXISTS idx_intent_jobs_status ON intent_jobs(status);
    CREATE INDEX IF NOT EXISTS idx_intent_jobs_user_status ON intent_jobs(user_id, status);
    CREATE INDEX IF NOT EXISTS idx_intent_jobs_phase ON intent_jobs(pipeline_phase);
    ```

    2. Agrega IntentJobRow interface exportada (copia del snippet en interfaces).

    3. Agrega las 6 funciones CRUD siguiendo EXACTAMENTE el pattern del RESEARCH Pattern 1 (lineas 202-282). Usa `catbotDb.prepare(...)` (consistency con resto del archivo), generateId() desde @/lib/utils.

    4. El UPDATE dinamico debe:
       - Siempre incluir "updated_at = datetime('now')"
       - Set "completed_at = datetime('now')" cuando patch.status es completed/failed/cancelled
       - Usar JSON.stringify para progress_message
       - Escapar correctamente params con better-sqlite3 positional

    NO importar tipos que no uses. NO dejar comentarios tombstone.

    Corre el test: varios it() deben pasar (CRUD puro). Los tests de tools seguiran fallando hasta Task 3.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/intent-jobs.test.ts -t "intent_jobs CRUD"</automated>
  </verify>
  <done>
    Bloque "intent_jobs CRUD" del test pasa. TypeScript compila sin errores. `grep -c "intent_jobs" app/src/lib/catbot-db.ts` devuelve >=8.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 6 tools CatBot + ASYNC_TOOLS map + executeTool cases + permission gate + settings.json sync</name>
  <files>app/src/lib/services/catbot-tools.ts, app/data/knowledge/settings.json</files>
  <behavior>
    - TOOLS[] contiene las 6 nuevas entries con function.name exacto
    - ASYNC_TOOLS const map declarado encima de TOOLS[]
    - getToolsForLLM inyecta `(ASYNC - estimated ${Math.round(ms/1000)}s)` al final de description para execute_catflow, execute_task, process_source_rag
    - executeTool switch maneja los 6 cases (queue_intent_job, list_my_jobs, cancel_job, approve_pipeline, execute_approved_pipeline, post_execution_decision)
    - Permission gate: queue_intent_job, list_my_jobs, execute_approved_pipeline son always_allowed; cancel_job, approve_pipeline, post_execution_decision requieren manage_intent_jobs (default-allow cuando !allowedActions.length)
    - USER_SCOPED_TOOLS incluye queue_intent_job, list_my_jobs, cancel_job, approve_pipeline, post_execution_decision
    - settings.json tools[] contiene los 6 nombres nuevos
  </behavior>
  <action>
    **app/src/lib/services/catbot-tools.ts:**

    1. Encima de `const TOOLS: ChatTool[] = [`, agrega:
    ```ts
    const ASYNC_TOOLS: Record<string, { estimated_duration_ms: number }> = {
      execute_catflow: { estimated_duration_ms: 120_000 },
      execute_task: { estimated_duration_ms: 180_000 },
      process_source_rag: { estimated_duration_ms: 240_000 },
    };
    ```

    2. Agrega los 6 tool entries al TOOLS[] (copia literal del RESEARCH Pattern 3 lineas 317-413). Ubicalos despues del ultimo intent tool de Phase 129 para locality.

    3. En getToolsForLLM (grep "getToolsForLLM"), en el map que serializa TOOLS, agrega logica que si `ASYNC_TOOLS[t.function.name]` existe, concatena ` (ASYNC - estimated ${Math.round(ASYNC_TOOLS[t.function.name]!.estimated_duration_ms / 1000)}s)` al final de description. NO mutar TOOLS; usar spread para crear copia.

    4. En el permission gate (approx linea 1007-1010), agrega:
    ```ts
    if (name === 'queue_intent_job' || name === 'list_my_jobs' || name === 'execute_approved_pipeline') return true;
    if ((name === 'cancel_job' || name === 'approve_pipeline' || name === 'post_execution_decision')
        && (allowedActions.includes('manage_intent_jobs') || !allowedActions.length)) return true;
    ```

    5. En el array USER_SCOPED_TOOLS (grep "USER_SCOPED_TOOLS"), agrega strings: 'queue_intent_job', 'list_my_jobs', 'cancel_job', 'approve_pipeline', 'post_execution_decision'.

    6. executeTool switch: agrega los 6 cases siguiendo RESEARCH Pattern 3 lineas 418-488. Para approve_pipeline usa baseUrl desde `process['env']['INTERNAL_BASE_URL'] || 'http://localhost:3000'`. Para post_execution_decision importa saveMemory desde @/lib/catbot-db y db desde @/lib/db (si no esta importado). execute_approved_pipeline actua igual que approve_pipeline pero sin la verificacion de phase (uso interno).

    7. Imports: agregar al import de @/lib/catbot-db → `createIntentJob, listJobsByUser, getIntentJob, updateIntentJob, saveMemory, type IntentJobRow`. Usar `import type` solo si IntentJobRow no se referencia en runtime.

    **app/data/knowledge/settings.json:**

    Dentro del array `tools[]`, agrega los 6 strings:
    - "queue_intent_job"
    - "list_my_jobs"
    - "cancel_job"
    - "approve_pipeline"
    - "execute_approved_pipeline"
    - "post_execution_decision"

    Mantener orden alfabetico si el archivo lo esta; sino agregar al final. Actualizar `updated_at` del JSON a fecha actual (KTREE-01).

    **Evita:**
    - NO modificar execute_catflow, execute_task, process_source_rag inline con campos async/estimated_duration_ms (OpenAI tools API rechaza fields extras). Toda la metadata async va SOLO en ASYNC_TOOLS map.
    - NO imports sin usar (ESLint break build).
    - NO comentarios "// added for phase 130"; codigo limpio.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/intent-jobs.test.ts src/lib/__tests__/knowledge-tools-sync.test.ts && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Todo el test intent-jobs.test.ts verde. knowledge-tools-sync.test.ts verde con las 6 tools nuevas sincronizadas. `npm run build` exitoso (no unused imports, no type errors). `grep -c "queue_intent_job\|list_my_jobs\|cancel_job\|approve_pipeline\|execute_approved_pipeline\|post_execution_decision" app/data/knowledge/settings.json` devuelve 6.
  </done>
</task>

</tasks>

<verification>
- Test intent-jobs.test.ts pasa completo (CRUD + 6 tools + aislamiento + post_execution branches)
- knowledge-tools-sync.test.ts pasa
- `cd app && npm run build` exitoso (ESLint strict + type check)
- `grep -c "ASYNC_TOOLS" app/src/lib/services/catbot-tools.ts` >=2 (declaracion + uso)
- settings.json tools[] contiene los 6 nuevos nombres
</verification>

<success_criteria>
- PIPE-01: tabla intent_jobs + CRUD exportada + test verde
- PIPE-02 (parcial): ASYNC_TOOLS metadata visible + queue_intent_job tool callable
- PIPE-07 (parcial): post_execution_decision 3 branches implementados
- PIPE-08 (parcial): list_my_jobs con user isolation + progress_message parsing + knowledge sync
</success_criteria>

<output>
Crear `.planning/phases/130-async-catflow-pipeline-creaci-n-asistida-de-workflows/130-01-SUMMARY.md` al completar.
</output>
