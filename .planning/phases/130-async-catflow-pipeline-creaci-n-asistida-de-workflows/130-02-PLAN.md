---
phase: 130-async-catflow-pipeline-creaci-n-asistida-de-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - "130-01"
files_modified:
  - app/src/lib/services/intent-job-executor.ts
  - app/src/lib/services/catbot-pipeline-prompts.ts
  - app/src/lib/services/catbot-prompt-assembler.ts
  - app/src/instrumentation.ts
  - app/src/lib/logger.ts
  - app/src/lib/__tests__/intent-job-executor.test.ts
  - app/src/lib/__tests__/catbot-prompt-assembler.test.ts
autonomous: true
requirements:
  - PIPE-02
  - PIPE-03
  - PIPE-04
must_haves:
  truths:
    - "IntentJobExecutor singleton arranca tras BOOT_DELAY=60s y ejecuta tick() cada 30s sin solapar con IntentWorker (45s) ni AlertService (30s)"
    - "Un job pending avanza por strategist → decomposer → architect phases en una sola tick cuando los tres LLM calls devuelven JSON valido"
    - "Un job con pipeline_phase='architect_retry' saltea strategist/decomposer y ejecuta solo la fase architect reusando goal+tasks persistidos en progress_message"
    - "getNextPendingJob selecciona jobs con pipeline_phase IN ('pending','architect_retry')"
    - "El executor hace ONE job per tick (currentJobId guard bloquea reentry)"
    - "Cada phase llama a callLLM() (fetch directo a LiteLLM con response_format json_object) — NO re-entra a /api/catbot/chat"
    - "progress_message se actualiza como JSON estructurado {phase, goal, tasks, resources, message} en cada transicion"
    - "Si LLM falla, job queda status='failed' con error string — no loop infinito"
    - "Architect pausa a pipeline_phase='awaiting_user' si design.needs_cat_paws esta presente, persiste cat_paws_needed[] y cat_paws_resolved=false en progress_message, SIN crear canvas"
    - "Orphan cleanup on start(): jobs en estado intermedio tras reinicio quedan failed con error='Abandoned on restart'"
    - "buildComplexTaskProtocol() esta registrado como P1 en PromptAssembler con <800 chars"
    - "instrumentation.ts invoca IntentJobExecutor.start() tras IntentWorker.start()"
  artifacts:
    - path: "app/src/lib/services/catbot-pipeline-prompts.ts"
      provides: "STRATEGIST_PROMPT, DECOMPOSER_PROMPT, ARCHITECT_PROMPT consts ~500 chars cada uno"
      min_lines: 30
    - path: "app/src/lib/services/intent-job-executor.ts"
      provides: "IntentJobExecutor singleton con static start/stop/tick + branch architect_retry + private helpers (callLLM, parseJSON, scanResources, buildStrategistInput, notifyProgress stubs)"
      min_lines: 150
      contains: "class IntentJobExecutor"
    - path: "app/src/lib/services/catbot-prompt-assembler.ts"
      provides: "buildComplexTaskProtocol() function + registration como seccion P1 en build()"
      contains: "buildComplexTaskProtocol"
    - path: "app/src/instrumentation.ts"
      provides: "IntentJobExecutor.start() invocation tras IntentWorker.start()"
      contains: "IntentJobExecutor"
    - path: "app/src/lib/logger.ts"
      provides: "'intent-job-executor' agregado al LogSource union"
      contains: "intent-job-executor"
    - path: "app/src/lib/__tests__/intent-job-executor.test.ts"
      provides: "Tests TDD del state machine con mocked callLLM (happy path + CatPaw pause + architect_retry resume + LLM error)"
      min_lines: 140
  key_links:
    - from: "app/src/lib/services/intent-job-executor.ts"
      to: "app/src/lib/catbot-db.ts"
      via: "import { getNextPendingJob, updateIntentJob, getIntentJob, type IntentJobRow }"
      pattern: "@/lib/catbot-db"
    - from: "app/src/lib/services/intent-job-executor.ts"
      to: "LiteLLM HTTP endpoint"
      via: "private callLLM() fetch a process.env.LITELLM_URL/v1/chat/completions con response_format json_object"
      pattern: "v1/chat/completions"
    - from: "app/src/lib/services/intent-job-executor.ts"
      to: "app/src/lib/services/catbot-pipeline-prompts.ts"
      via: "import { STRATEGIST_PROMPT, DECOMPOSER_PROMPT, ARCHITECT_PROMPT }"
      pattern: "catbot-pipeline-prompts"
    - from: "app/src/instrumentation.ts"
      to: "app/src/lib/services/intent-job-executor.ts"
      via: "await import + IntentJobExecutor.start()"
      pattern: "intent-job-executor"
    - from: "app/src/lib/services/catbot-prompt-assembler.ts"
      to: "buildComplexTaskProtocol()"
      via: "sections.push({id: 'complex_task_protocol', priority: 1, content: buildComplexTaskProtocol()})"
      pattern: "complex_task_protocol"
---

<objective>
Implementar el motor async: IntentJobExecutor singleton que drives el pipeline de 3 fases (estratega → despiezador → arquitecto) via llamadas directas a LiteLLM con system prompts especializados, mas la seccion P1 "Protocolo de Tareas Complejas" en PromptAssembler que le dice a CatBot cuando encolar jobs. Incluye el branch architect_retry que reanuda jobs pausados por needs_cat_paws tras la aprobacion del usuario (el loop se cierra en Plan 04 Task 4).

Purpose: Sin este plan, los jobs creados en Plan 01 quedan huerfanos. Este plan los procesa. El branch architect_retry previene el fallo BLOCKER 1 (awaiting_user sin resume path).
Output: Background worker arrancado desde instrumentation.ts, 3 prompts especializados, protocol section inyectada en CatBot, tests TDD del state machine incluyendo el resume path.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/130-async-catflow-pipeline-creaci-n-asistida-de-workflows/130-RESEARCH.md
@.planning/phases/130-async-catflow-pipeline-creaci-n-asistida-de-workflows/130-01-PLAN.md
@app/src/lib/services/intent-worker.ts
@app/src/lib/services/alert-service.ts
@app/src/lib/services/canvas-executor.ts
@app/src/lib/services/catbot-prompt-assembler.ts
@app/src/instrumentation.ts
@app/src/lib/logger.ts

<interfaces>
From app/src/lib/services/intent-worker.ts (Phase 129) — pattern EXACTO:
- Class with static `intervalId`, `timeoutId`, `start()`, `stop()`, `tick()`
- BOOT_DELAY + CHECK_INTERVAL consts
- try/catch inside tick; never throws

From app/src/lib/services/canvas-executor.ts lines 97-125 — callLLM template:
- fetch a `${LITELLM_URL}/v1/chat/completions`
- headers: Content-Type + Authorization Bearer
- body: {model, messages, temperature, max_tokens, response_format}
- parse data.choices[0].message.content

From app/src/lib/services/catbot-prompt-assembler.ts:
- Existing `buildIntentProtocol()` y `buildKnowledgeProtocol()` — template exacto para buildComplexTaskProtocol
- Inside build() method, sections.push({id, priority, content}) pattern (buscar `sections.push({ id: 'intent_protocol'`)

From app/src/instrumentation.ts:
- Existing try/await-import/IntentWorker.start() block — copiar pattern

From app/src/lib/logger.ts:
- LogSource type union — agregar 'intent-job-executor' como valor adicional

From Plan 01 (catbot-db.ts):
- getNextPendingJob(), updateIntentJob(id, patch), getIntentJob(id), IntentJobRow
- getNextPendingJob DEBE seleccionar `WHERE status='pending' AND pipeline_phase IN ('pending','architect_retry')` para que el Plan 04 resume funcione

From @/lib/db (docflow.db):
- db.prepare('SELECT id, name, description, mode, system_prompt FROM cat_paws WHERE is_active = 1 LIMIT 50').all()
- catbrains, skills, connectors tables para scanResources()
- INSERT INTO canvases (id, name, description, mode, status, flow_data) — usado en arquitecto phase

CallLLM env vars (process['env'] bracket notation per MEMORY.md):
- process['env']['LITELLM_URL'] || 'http://litellm:4000'
- process['env']['LITELLM_API_KEY'] || 'sk-antigravity-gateway'
- process['env']['CATBOT_PIPELINE_MODEL'] || 'ollama/gemma3:12b'

Prompts target: <500 chars each, instruct JSON-only output, fit within 4000 max_tokens.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write intent-job-executor + prompt-assembler test scaffolds (RED)</name>
  <files>app/src/lib/__tests__/intent-job-executor.test.ts, app/src/lib/__tests__/catbot-prompt-assembler.test.ts</files>
  <behavior>
    intent-job-executor.test.ts:
    - happy path: job pending → tick() con 3 callLLM mocks (goal, tasks, design) → job termina en awaiting_approval con canvas_id populado
    - pause path: architect devuelve needs_cat_paws[] → job queda en awaiting_user con progress_message.cat_paws_needed populado y cat_paws_resolved=false, canvas_id null
    - resume path (architect_retry): insert job con pipeline_phase='architect_retry' y progress_message={goal, tasks, cat_paws_resolved:true}; tick() hace solo 1 callLLM (architect) y termina en awaiting_approval con canvas_id
    - error path: callLLM rejecta en strategist → job status='failed' con error conteniendo 'litellm'
    - currentJobId guard: si se llama tick() mientras currentJobId !== null, skippea sin tocar DB
    - orphan cleanup on start(): jobs con status='pending' y pipeline_phase='strategist' (pre-existentes) quedan status='failed' tras start()
    - parseJSON fallback: si LLM devuelve markdown-fenced JSON, strip fences y reintenta antes de fail

    catbot-prompt-assembler.test.ts (extend existente):
    - buildComplexTaskProtocol string <800 chars
    - incluye keywords: "Protocolo de Tareas Complejas", "queue_intent_job", "ASYNC", "60s", "awaiting_approval", "post_execution_decision"
    - build() output incluye la seccion registrada como P1 (priority 1)
  </behavior>
  <action>
    **intent-job-executor.test.ts (nuevo):**
    1. `vi.hoisted(() => { process.env.CATBOT_DB_PATH = '/tmp/test-intent-job-executor.db'; })` antes de imports.
    2. vi.mock de @/lib/db y @/lib/services/notifications (stubs).
    3. Import IntentJobExecutor, createIntentJob, getIntentJob, updateIntentJob.
    4. beforeEach: DELETE FROM intent_jobs; reset vi.spyOn; reset IntentJobExecutor.currentJobId (via `(IntentJobExecutor as any).currentJobId = null`).
    5. Describe "IntentJobExecutor state machine" con 7 it() cubriendo lo del behavior (incluyendo resume path).
    6. Mock callLLM via `vi.spyOn(IntentJobExecutor as any, 'callLLM').mockResolvedValueOnce(...).mockResolvedValueOnce(...).mockResolvedValueOnce(...)` con JSON strings canned.
    7. scanResources debe estar stubbed via vi.spyOn para devolver `{catPaws: [], catBrains: [], skills: [], connectors: []}` (evitar hit a docflow.db real).
    8. Happy path asserts: `job.pipeline_phase === 'awaiting_approval'`, `job.canvas_id` truthy.
    9. Pause path mock: architect devuelve `{name, description, flow_data:{nodes:[],edges:[]}, needs_cat_paws:[{name:'X',system_prompt:'Y',reason:'Z'}]}`. Assert: `job.pipeline_phase === 'awaiting_user'`, `job.canvas_id === null`, progress_message parseado tiene cat_paws_needed array con length 1 y cat_paws_resolved=false.
    10. Resume path: manualmente crear job con pipeline_phase='architect_retry', progress_message JSON con {goal:'G', tasks:[{name:'T1'}], cat_paws_resolved:true}. vi.spyOn callLLM con UN solo mockResolvedValueOnce (solo architect). Llamar tick(). Assert: solo 1 call a callLLM, phase='awaiting_approval', canvas_id truthy.
    11. Error path: `mockRejectedValueOnce(new Error('litellm 500'))`. Assert: status='failed', error contiene 'litellm'.
    12. Orphan cleanup: insert manualmente un row con status='pending' y pipeline_phase='strategist', luego llama `IntentJobExecutor.cleanupOrphans()` (expuesto como static). Assert: status='failed', error='Abandoned on restart'.

    **catbot-prompt-assembler.test.ts (extend):**
    Agrega describe "buildComplexTaskProtocol" con 3 it():
    - `expect(buildComplexTaskProtocol().length).toBeLessThan(800)`
    - `expect(buildComplexTaskProtocol()).toMatch(/queue_intent_job/)` y otras keywords
    - Build complete prompt y verificar que incluye "Protocolo de Tareas Complejas"

    Nota: buildComplexTaskProtocol debe exportarse o testearse via reflexion. Si no exportada, `const { buildComplexTaskProtocol } = await import('@/lib/services/catbot-prompt-assembler')` tras hacer export.

    Corre tests: DEBEN fallar (IntentJobExecutor no existe aun).
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/intent-job-executor.test.ts src/lib/__tests__/catbot-prompt-assembler.test.ts 2>&1 | grep -E "(FAIL|PASS|fail)"</automated>
  </verify>
  <done>
    Ambos archivos existen, al menos 10 it() blocks combinados (incluye resume path), y vitest falla con "module not found" o "undefined is not a function".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IntentJobExecutor singleton + 3 pipeline prompts + logger source + architect_retry branch (GREEN)</name>
  <files>app/src/lib/services/catbot-pipeline-prompts.ts, app/src/lib/services/intent-job-executor.ts, app/src/lib/logger.ts, app/src/lib/catbot-db.ts</files>
  <behavior>
    catbot-pipeline-prompts.ts:
    - Exporta 3 consts STRATEGIST_PROMPT, DECOMPOSER_PROMPT, ARCHITECT_PROMPT
    - Cada uno ~500 chars, instruye responder SOLO con JSON con shape exacto
    - ARCHITECT_PROMPT lista los node types validos del canvas executor (agent, catpaw, catbrain, condition, iterator, multiagent, scheduler, checkpoint, connector) para evitar Pitfall 8

    catbot-db.ts (update minor a Plan 01):
    - getNextPendingJob query DEBE ser: `SELECT * FROM intent_jobs WHERE status='pending' AND pipeline_phase IN ('pending','architect_retry') ORDER BY created_at ASC LIMIT 1`
    - (Si Plan 01 ya lo dejo solo con pipeline_phase='pending', este plan lo amplia. Si Plan 01 ya preveia el IN, no-op.)

    intent-job-executor.ts:
    - class IntentJobExecutor con static intervalId, timeoutId, currentJobId (guard)
    - BOOT_DELAY = 60_000, CHECK_INTERVAL = 30_000
    - start() ejecuta cleanupOrphans() SYNCRONO antes del setTimeout(tick)
    - stop() limpia timers
    - tick() — skip si currentJobId, sino getNextPendingJob; set currentJobId; try/catch/finally con finally resetando currentJobId
    - **Branching dentro de tick por pipeline_phase:**
      - Si job.pipeline_phase === 'architect_retry':
        - Parse progress_message JSON → extrae goal y tasks persistidos
        - Verifica progress_message.cat_paws_resolved === true (invariante; si false, marca failed con error 'architect_retry sin cat_paws_resolved')
        - Update phase a 'architect'
        - scanResources() (vera los CatPaws recien creados por approve_catpaw_creation)
        - callLLM(ARCHITECT, {goal, tasks, resources}) → design
        - Continua con el flujo estandar de validacion + needs_cat_paws o INSERT canvas
      - Si job.pipeline_phase ∈ ('pending','strategist'): flujo completo 3 fases
        - Update phase a 'strategist' ANTES del primer LLM call (evita Pitfall 4)
        - Fase 1: callLLM(STRATEGIST, input) → parseJSON → update progress_message
        - Fase 2: callLLM(DECOMPOSER, {goal, original}) → tasks
        - Fase 3: scanResources() → callLLM(ARCHITECT, {goal, tasks, resources}) → design
    - Si design.needs_cat_paws.length > 0: update a awaiting_user con progress_message={...prev, cat_paws_needed: design.needs_cat_paws, cat_paws_resolved: false, message: 'Esperando aprobacion para crear CatPaws'}, return (NO crea canvas). El resume lo hace Plan 04 Task 4 via approve_catpaw_creation tool.
    - Si no: INSERT INTO canvases, update job a awaiting_approval con canvas_id
    - callLLM privado: fetch LiteLLM con response_format json_object, temperature 0.3, max_tokens 4000
    - parseJSON privado: try JSON.parse; si falla, strip markdown fences y reintenta; si falla, throw
    - scanResources privado: db.prepare(...) a catPaws/catBrains/skills/connectors (LIMIT 50)
    - notifyProgress / sendProposal / notifyUserCatPawApproval: stubs que el Plan 04 implementara completamente (logger.info placeholder)
    - cleanupOrphans static: UPDATE intent_jobs SET status='failed', error='Abandoned on restart' WHERE status='pending' AND pipeline_phase NOT IN ('pending','architect_retry','awaiting_approval','awaiting_user')

    logger.ts: agregar 'intent-job-executor' al LogSource union type.
  </behavior>
  <action>
    1. Crea app/src/lib/services/catbot-pipeline-prompts.ts con los 3 consts exactos del RESEARCH Pattern 6 lineas 751-782. En ARCHITECT_PROMPT agrega la lista de node types validos: "agent | catpaw | catbrain | condition | iterator | multiagent | scheduler | checkpoint | connector. No inventes otros."

    2. Edita app/src/lib/catbot-db.ts: modifica getNextPendingJob para que el WHERE clause incluya `pipeline_phase IN ('pending','architect_retry')`. Esto habilita que IntentJobExecutor recoja jobs tras el approve_catpaw_creation (Plan 04 Task 4).

    3. Crea app/src/lib/services/intent-job-executor.ts siguiendo el RESEARCH Pattern 5 (lineas 546-694) y Pattern 6 (lineas 714-745). Puntos clave:
       - `import { getNextPendingJob, updateIntentJob, getIntentJob, type IntentJobRow } from '@/lib/catbot-db';`
       - `import db from '@/lib/db';` (para scanResources + canvas insert)
       - `import { generateId } from '@/lib/utils';`
       - `import { logger } from '@/lib/logger';`
       - `import { STRATEGIST_PROMPT, DECOMPOSER_PROMPT, ARCHITECT_PROMPT } from './catbot-pipeline-prompts';`
       - NO importar notifications aun (Plan 04 lo hace)
       - IMPORTANTE: el branching por pipeline_phase debe ser lo primero tras `this.currentJobId = job.id`:
         ```ts
         if (job.pipeline_phase === 'architect_retry') {
           const prev = JSON.parse(job.progress_message || '{}') as { goal?: unknown; tasks?: unknown; cat_paws_resolved?: boolean };
           if (!prev.cat_paws_resolved) {
             updateIntentJob(job.id, { status: 'failed', error: 'architect_retry sin cat_paws_resolved' });
             return;
           }
           updateIntentJob(job.id, { pipeline_phase: 'architect' });
           const resources = this.scanResources();
           const designRaw = await this.callLLM(ARCHITECT_PROMPT, JSON.stringify({ goal: prev.goal, tasks: prev.tasks, resources }));
           const design = this.parseJSON(designRaw);
           await this.finalizeDesign(job, design, prev.goal, prev.tasks);
           return;
         }
         // branch full: pending/strategist → ejecutar las 3 fases
         updateIntentJob(job.id, { pipeline_phase: 'strategist' });
         // ... resto igual, llamar finalizeDesign al final
         ```
       - `finalizeDesign(job, design, goal, tasks)` helper: valida flow_data (Plan 03 lo agregara), si needs_cat_paws persiste en progress_message y pausa, sino INSERT canvas y awaiting_approval.
       - En scanResources, si las tablas no existen en test context (mocked db), catch el error y devolver arrays vacios
       - cleanupOrphans debe usar catbotDb import o una funcion exportada desde catbot-db. Sugerencia: agregar export `cleanupOrphanJobs()` a catbot-db.ts (simple UPDATE) y llamarlo desde start().

    4. En app/src/lib/logger.ts, busca el LogSource union y agrega 'intent-job-executor' como otro literal string.

    5. Acceso a env vars: usar `process['env']['LITELLM_URL']` bracket notation (MEMORY.md).

    6. **Anti-patterns a evitar:**
       - NO re-entrar a /api/catbot/chat (usar fetch directo a LiteLLM)
       - NO procesar multiples jobs por tick
       - NO hardcodear flow_data templates (dejar al LLM)
       - NO wrapper transactions LLM calls
       - NO unused imports
       - NO re-ejecutar strategist/decomposer en architect_retry (waste de LLM calls + inconsistencia)

    Corre intent-job-executor.test.ts: casos happy path / pause / resume (architect_retry) / error / currentJobId guard deben pasar.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/intent-job-executor.test.ts</automated>
  </verify>
  <done>
    intent-job-executor.test.ts 100% verde (incluye resume path architect_retry). catbot-pipeline-prompts.ts existe con 3 exports. logger.ts incluye 'intent-job-executor'. getNextPendingJob acepta architect_retry. `npm run build` exitoso.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: buildComplexTaskProtocol P1 section + instrumentation registration</name>
  <files>app/src/lib/services/catbot-prompt-assembler.ts, app/src/instrumentation.ts</files>
  <behavior>
    - buildComplexTaskProtocol() exportado, <800 chars, contiene instrucciones queue_intent_job/list_my_jobs/cancel_job/post_execution_decision
    - Registrado en build() como seccion P1 con id 'complex_task_protocol'
    - instrumentation.ts arranca IntentJobExecutor.start() despues de IntentWorker.start() (BOOT_DELAY=60s staggered tras 45s de IntentWorker)
  </behavior>
  <action>
    **app/src/lib/services/catbot-prompt-assembler.ts:**

    1. Agrega funcion exportada `buildComplexTaskProtocol(): string` siguiendo RESEARCH Pattern 4 (lineas 507-529). Target <800 chars (verificar con String length). Debe mencionar explicitamente: "Protocolo de Tareas Complejas", tools ASYNC (execute_catflow, execute_task, process_source_rag), queue_intent_job, list_my_jobs, cancel_job, awaiting_approval, post_execution_decision con las 3 acciones (keep_template, save_recipe, delete).

    2. En el metodo build() (buscar patrones tipo `sections.push({ id: 'intent_protocol'`), agrega:
    ```ts
    try {
      sections.push({ id: 'complex_task_protocol', priority: 1, content: buildComplexTaskProtocol() });
    } catch { /* graceful */ }
    ```
    Ubicar despues del intent_protocol push.

    3. Export explicit: `export function buildComplexTaskProtocol()` (el test lo importa por nombre).

    **app/src/instrumentation.ts:**

    Despues del bloque que arranca IntentWorker (buscar `IntentWorker.start()`), agrega:
    ```ts
    try {
      const { IntentJobExecutor } = await import('@/lib/services/intent-job-executor');
      IntentJobExecutor.start();
    } catch (err) {
      console.error('[instrumentation] Failed to start IntentJobExecutor:', err);
    }
    ```

    NO quitar ni modificar otros arranques. Respetar el orden BOOT: AlertService (30s) → IntentWorker (45s) → IntentJobExecutor (60s) — esto ya esta garantizado por el BOOT_DELAY en cada clase; en instrumentation solo llamamos start() secuencialmente.

    **Evita:** comentarios tombstone ("// new in phase 130"), unused imports.

    Corre tests: catbot-prompt-assembler.test.ts debe pasar completo.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts src/lib/__tests__/intent-job-executor.test.ts && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    Ambos tests verdes. `grep -c "IntentJobExecutor" app/src/instrumentation.ts` >=2. `grep -c "buildComplexTaskProtocol" app/src/lib/services/catbot-prompt-assembler.ts` >=2 (definicion + push). npm run build exitoso.
  </done>
</task>

</tasks>

<verification>
- intent-job-executor.test.ts verde (state machine completo incluyendo architect_retry resume)
- catbot-prompt-assembler.test.ts verde con section assertions
- npm run build exitoso (no type errors, no unused imports)
- Docker build green (patron habitual)
</verification>

<success_criteria>
- PIPE-02: buildComplexTaskProtocol inyectado como P1, instrucciones presentes
- PIPE-03: IntentJobExecutor procesa 3 fases con callLLM directo (no /api/catbot/chat re-entry), one-per-tick guard, orphan cleanup on start
- PIPE-04 (parcial): branch architect_retry permite reanudar jobs pausados por needs_cat_paws (cierre del loop en Plan 04 Task 4)
</success_criteria>

<output>
Crear `.planning/phases/130-async-catflow-pipeline-creaci-n-asistida-de-workflows/130-02-SUMMARY.md` al completar.
</output>
</content>
</invoke>