---
phase: 36-playwright-setup-test-specs
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - app/e2e/pages/chat.pom.ts
  - app/e2e/pages/agents.pom.ts
  - app/e2e/pages/workers.pom.ts
  - app/e2e/pages/skills.pom.ts
  - app/e2e/pages/tasks.pom.ts
  - app/e2e/specs/chat.spec.ts
  - app/e2e/specs/agents.spec.ts
  - app/e2e/specs/workers.spec.ts
  - app/e2e/specs/skills.spec.ts
  - app/e2e/specs/tasks.spec.ts
autonomous: true
requirements:
  - E2E-06
  - E2E-07
  - E2E-08
  - E2E-09
  - E2E-10

must_haves:
  truths:
    - "Chat spec sends a message and verifies response area appears"
    - "Agents spec lists agents, creates a custom agent, edits it, deletes it"
    - "Workers spec lists workers, creates one, edits it, deletes it"
    - "Skills spec lists skills, creates one, edits it, deletes it"
    - "Tasks spec lists tasks, verifies templates visible, creates from template wizard"
  artifacts:
    - path: "app/e2e/pages/chat.pom.ts"
      provides: "ChatPOM with sendMessage, exampleQuestions locators"
      exports: ["ChatPOM"]
    - path: "app/e2e/pages/agents.pom.ts"
      provides: "AgentsPOM with CRUD methods"
      exports: ["AgentsPOM"]
    - path: "app/e2e/pages/workers.pom.ts"
      provides: "WorkersPOM with CRUD methods"
      exports: ["WorkersPOM"]
    - path: "app/e2e/pages/skills.pom.ts"
      provides: "SkillsPOM with CRUD methods"
      exports: ["SkillsPOM"]
    - path: "app/e2e/pages/tasks.pom.ts"
      provides: "TasksPOM with list/create/wizard methods"
      exports: ["TasksPOM"]
  key_links:
    - from: "app/e2e/specs/agents.spec.ts"
      to: "app/e2e/pages/agents.pom.ts"
      via: "import AgentsPOM"
      pattern: "import.*AgentsPOM"
    - from: "app/e2e/specs/tasks.spec.ts"
      to: "app/e2e/pages/tasks.pom.ts"
      via: "import TasksPOM"
      pattern: "import.*TasksPOM"
---

<objective>
Create POMs and E2E specs for chat, agents, workers, skills, and tasks sections.

Purpose: Cover E2E-06 through E2E-10 with typed Page Object Models and spec files.
Output: 5 POM files and 5 spec files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-playwright-setup-test-specs/36-RESEARCH.md
@.planning/phases/36-playwright-setup-test-specs/36-01-SUMMARY.md

@app/e2e/pages/base.page.ts
@app/e2e/helpers/test-data.ts
@app/src/components/chat/chat-panel.tsx
@app/src/app/agents/page.tsx
@app/src/app/workers/page.tsx
@app/src/app/skills/page.tsx
@app/src/app/tasks/page.tsx
@app/src/app/tasks/new/page.tsx
</context>

<interfaces>
<!-- Base POM from Plan 01 -->
```typescript
export class BasePage {
  readonly page: Page;
  readonly sidebar: Locator;
  constructor(page: Page);
  async navigateTo(path: string): Promise<void>;
  async waitForApi(urlPattern: string | RegExp): Promise<Response>;
  async getSidebarLink(label: string): Promise<Locator>;
}
```

<!-- Test data helpers from Plan 01 -->
```typescript
export const TEST_PREFIX = '[TEST]';
export function testName(base: string): string;
```

<!-- Sidebar nav labels for reference -->
Agentes (/agents), Docs Workers (/workers), Skills (/skills), Tareas (/tasks)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create POMs for chat, agents, workers, skills, tasks</name>
  <files>app/e2e/pages/chat.pom.ts, app/e2e/pages/agents.pom.ts, app/e2e/pages/workers.pom.ts, app/e2e/pages/skills.pom.ts, app/e2e/pages/tasks.pom.ts</files>
  <action>
Create 5 POM files extending BasePage. Read actual component source files to extract exact Spanish labels.

1. **chat.pom.ts** (ChatPOM):
   - Constructor takes page (chat is within project context /projects/[id] pipeline)
   - Locator for message input (textarea or input for typing messages)
   - Locator for send button
   - Method `sendMessage(text: string)`: fill input, click send or press Enter
   - Locator for message list / chat messages container
   - Locator for example questions (preguntas de ejemplo) — look at chat-panel.tsx for exact text
   - Locator for streaming cursor (the blinking block character during streaming)
   - Locator for response area where assistant messages appear

2. **agents.pom.ts** (AgentsPOM):
   - goto() navigates to '/agents'
   - Locator for agent list/grid
   - Method `createAgent(name: string, opts?: {model?: string, description?: string})`: click create button, fill form fields (read agents/page.tsx for exact form labels), submit
   - Method `findAgent(name: string)`: locator for agent card/row containing name
   - Method `editAgent(name: string, updates: {description?: string})`: click edit, modify fields, save
   - Method `deleteAgent(name: string)`: click delete, confirm dialog
   - Locator for OpenClaw agents section (merged list from /api/agents)

3. **workers.pom.ts** (WorkersPOM):
   - goto() navigates to '/workers'
   - Locator for workers list
   - Method `createWorker(name: string, opts?: {type?: string})`: click create, fill form, submit
   - Method `findWorker(name: string)`: locator for worker item
   - Method `editWorker(name: string, updates: object)`: edit flow
   - Method `deleteWorker(name: string)`: delete with confirmation

4. **skills.pom.ts** (SkillsPOM):
   - goto() navigates to '/skills'
   - Locator for skills list
   - Method `createSkill(name: string, opts?: {description?: string})`: click create, fill form, submit
   - Method `findSkill(name: string)`: locator for skill item
   - Method `editSkill(name: string, updates: object)`: edit flow
   - Method `deleteSkill(name: string)`: delete with confirmation

5. **tasks.pom.ts** (TasksPOM):
   - goto() navigates to '/tasks'
   - Locator for tasks list
   - Locator for template cards (predefined task templates)
   - Method `findTemplate(name: string)`: locator for template
   - Method `createFromTemplate(templateName: string, taskName: string)`: click template, fill wizard (4 steps — read tasks/new/page.tsx for step labels), complete
   - Method `findTask(name: string)`: locator for task in list
   - Locator for task status badges

CRITICAL: Read each page's source file to extract EXACT Spanish labels, button text, form field labels, and placeholder text. Use getByRole, getByText, getByLabel, getByPlaceholder — NOT CSS class selectors.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && ls e2e/pages/chat.pom.ts e2e/pages/agents.pom.ts e2e/pages/workers.pom.ts e2e/pages/skills.pom.ts e2e/pages/tasks.pom.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>5 POM files exist with typed locators using Spanish labels from actual components. All extend BasePage. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E specs for chat, agents, workers, skills, tasks</name>
  <files>app/e2e/specs/chat.spec.ts, app/e2e/specs/agents.spec.ts, app/e2e/specs/workers.spec.ts, app/e2e/specs/skills.spec.ts, app/e2e/specs/tasks.spec.ts</files>
  <action>
Create 5 spec files using corresponding POMs. Use test.describe.serial() for CRUD flows. All test data uses testName().

1. **chat.spec.ts** (E2E-06):
   ```
   test.describe.serial('Chat RAG')
   - Setup: create [TEST] project via API in beforeAll, need at least 1 source
   - test: chat panel shows message input
     → Navigate to project chat tab
     → Verify message input and send button visible
   - test: example questions are displayed
     → Verify at least one example/suggested question is visible
   - test: send message shows response area
     → Type a test message, send it
     → Verify user message appears in chat
     → Verify response area/assistant bubble appears (streaming or loading indicator)
     → Do NOT wait for full LLM response — verify UI state only
   - Teardown: delete [TEST] project via API
   ```
   NOTE: Chat depends on LLM. Verify UI interaction, not response quality.

2. **agents.spec.ts** (E2E-07):
   ```
   test.describe.serial('Agentes')
   - test: agents page loads with list
     → Navigate to /agents
     → Verify page heading and agent list/grid visible
   - test: list shows OpenClaw + custom agents
     → Verify agent items exist (the merged list from GET /api/agents)
   - test: create custom agent
     → Use AgentsPOM.createAgent(testName('Agente E2E'), {description: 'Test agent'})
     → Verify success feedback (toast or redirect)
   - test: edit custom agent
     → Find the created agent, edit description
     → Verify changes saved
   - test: delete custom agent
     → Delete the [TEST] agent
     → Verify it disappears from list
   ```

3. **workers.spec.ts** (E2E-08):
   ```
   test.describe.serial('Docs Workers')
   - test: workers page loads with list
     → Navigate to /workers
     → Verify page heading and list visible
   - test: create worker
     → Use WorkersPOM.createWorker(testName('Worker E2E'))
     → Verify success
   - test: edit worker
     → Edit the created worker
     → Verify changes
   - test: delete worker
     → Delete the [TEST] worker
     → Verify removal
   ```

4. **skills.spec.ts** (E2E-09):
   ```
   test.describe.serial('Skills')
   - test: skills page loads with list
     → Navigate to /skills
     → Verify page heading and list visible
   - test: create skill
     → Use SkillsPOM.createSkill(testName('Skill E2E'), {description: 'Test skill'})
     → Verify success
   - test: edit skill
     → Edit the created skill
     → Verify changes
   - test: delete skill
     → Delete the [TEST] skill
     → Verify removal
   ```

5. **tasks.spec.ts** (E2E-10):
   ```
   test.describe.serial('Tareas')
   - test: tasks page loads with list
     → Navigate to /tasks
     → Verify page heading visible
   - test: task templates are visible
     → Verify at least one template card exists
   - test: create task from template wizard
     → Click a template
     → Verify wizard opens with multiple steps (4 steps per tasks/new/page.tsx)
     → Fill step 1: task name with testName('Tarea E2E')
     → Navigate through wizard steps (Next buttons)
     → Complete/submit the wizard
     → Verify task created (redirect or success feedback)
   - Teardown: delete [TEST] task via API (DELETE /api/tasks/:id) if needed
   ```

For agents/workers/skills specs: Use afterAll to clean up via API if the delete test fails.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && ls e2e/specs/chat.spec.ts e2e/specs/agents.spec.ts e2e/specs/workers.spec.ts e2e/specs/skills.spec.ts e2e/specs/tasks.spec.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>5 spec files exist covering E2E-06 through E2E-10. CRUD specs use serial ordering. Chat spec verifies UI states not LLM output. All use [TEST] prefix. Build passes.</done>
</task>

</tasks>

<verification>
- All 5 POM files exist under e2e/pages/
- All 5 spec files exist under e2e/specs/
- POMs use Spanish labels from actual components
- Specs use [TEST] prefix and serial ordering for CRUD
- npm run build passes
</verification>

<success_criteria>
5 POM files and 5 spec files created. POMs reference correct Spanish UI labels. Specs cover full behavior for E2E-06 through E2E-10. Chat spec verifies UI states, not LLM output quality.
</success_criteria>

<output>
After completion, create `.planning/phases/36-playwright-setup-test-specs/36-03-SUMMARY.md`
</output>
