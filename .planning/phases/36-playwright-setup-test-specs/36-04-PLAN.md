---
phase: 36-playwright-setup-test-specs
plan: 04
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - app/e2e/pages/canvas.pom.ts
  - app/e2e/pages/connectors.pom.ts
  - app/e2e/pages/catbot.pom.ts
  - app/e2e/pages/settings.pom.ts
  - app/e2e/specs/canvas.spec.ts
  - app/e2e/specs/connectors.spec.ts
  - app/e2e/specs/catbot.spec.ts
  - app/e2e/specs/dashboard.spec.ts
  - app/e2e/specs/settings.spec.ts
autonomous: true
requirements:
  - E2E-11
  - E2E-12
  - E2E-13
  - E2E-14
  - E2E-15

must_haves:
  truths:
    - "Canvas spec lists canvases, creates with wizard, opens editor with START node, drags nodes, connects, saves"
    - "Connectors spec lists types, creates connector, tests connection, verifies templates"
    - "CatBot spec opens floating panel, sends message, verifies contextual suggestions change by page"
    - "Dashboard spec verifies summary cards with data, token chart, activity, storage"
    - "Settings spec verifies API keys section, processing section, CatBot section"
  artifacts:
    - path: "app/e2e/pages/canvas.pom.ts"
      provides: "CanvasPOM with wizard/editor/drag/connect methods"
      exports: ["CanvasPOM"]
    - path: "app/e2e/pages/connectors.pom.ts"
      provides: "ConnectorsPOM with CRUD methods"
      exports: ["ConnectorsPOM"]
    - path: "app/e2e/pages/catbot.pom.ts"
      provides: "CatBotPOM with panel open/close, send, suggestions"
      exports: ["CatBotPOM"]
    - path: "app/e2e/pages/settings.pom.ts"
      provides: "SettingsPOM with section locators"
      exports: ["SettingsPOM"]
    - path: "app/e2e/specs/canvas.spec.ts"
      provides: "E2E-11 canvas spec"
    - path: "app/e2e/specs/dashboard.spec.ts"
      provides: "E2E-14 dashboard spec"
  key_links:
    - from: "app/e2e/specs/canvas.spec.ts"
      to: "app/e2e/pages/canvas.pom.ts"
      via: "import CanvasPOM"
      pattern: "import.*CanvasPOM"
    - from: "app/e2e/specs/catbot.spec.ts"
      to: "app/e2e/pages/catbot.pom.ts"
      via: "import CatBotPOM"
      pattern: "import.*CatBotPOM"
---

<objective>
Create POMs and E2E specs for canvas, connectors, CatBot, dashboard, and settings sections.

Purpose: Cover E2E-11 through E2E-15 with typed Page Object Models and spec files.
Output: 4 POM files (canvas, connectors, catbot, settings) and 5 spec files. Dashboard spec reuses DashboardPOM from Plan 02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-playwright-setup-test-specs/36-RESEARCH.md
@.planning/phases/36-playwright-setup-test-specs/36-01-SUMMARY.md

@app/e2e/pages/base.page.ts
@app/e2e/pages/dashboard.pom.ts
@app/e2e/helpers/test-data.ts
@app/src/app/canvas/page.tsx
@app/src/app/connectors/page.tsx
@app/src/components/catbot/catbot-panel.tsx
@app/src/app/settings/page.tsx
@app/src/app/page.tsx
@app/src/components/canvas/node-config-panel.tsx
</context>

<interfaces>
<!-- Base POM from Plan 01 -->
```typescript
export class BasePage {
  readonly page: Page;
  readonly sidebar: Locator;
  constructor(page: Page);
  async navigateTo(path: string): Promise<void>;
}
```

<!-- DashboardPOM from Plan 02 (already created) -->
```typescript
export class DashboardPOM extends BasePage {
  async goto(): Promise<void>;
  // summary cards, chart, activity, storage locators
}
```

<!-- Test data helpers -->
```typescript
export const TEST_PREFIX = '[TEST]';
export function testName(base: string): string;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create POMs for canvas, connectors, catbot, settings</name>
  <files>app/e2e/pages/canvas.pom.ts, app/e2e/pages/connectors.pom.ts, app/e2e/pages/catbot.pom.ts, app/e2e/pages/settings.pom.ts</files>
  <action>
Create 4 POM files extending BasePage. Read actual component source files for exact Spanish labels.

1. **canvas.pom.ts** (CanvasPOM):
   - goto() navigates to '/canvas'
   - Locator for canvas list/grid
   - Method `createCanvas(name: string)`: click create/new button, fill wizard (read canvas page source for wizard steps — likely name + description), submit
   - Method `findCanvas(name: string)`: locator for canvas item in list
   - Method `openCanvas(name: string)`: click to open editor
   - Locator for editor area (the ReactFlow canvas container)
   - Locator for START node (always present in editor)
   - Method `addNode(type: string)`: interact with node palette/toolbar to add a node
   - Method `dragNode(nodeLocator: Locator, deltaX: number, deltaY: number)`: use page.mouse for drag
   - Method `saveCanvas()`: click save button
   - Method `deleteCanvas(name: string)`: delete with confirmation

2. **connectors.pom.ts** (ConnectorsPOM):
   - goto() navigates to '/connectors'
   - Locator for connector type cards/list
   - Method `createConnector(name: string, type: string, config?: object)`: click create, select type, fill form, submit
   - Method `findConnector(name: string)`: locator for connector item
   - Method `testConnection(name: string)`: click test button on connector card
   - Locator for connection test result indicator (success/fail badge)
   - Locator for suggested templates section
   - Method `deleteConnector(name: string)`: delete with confirmation

3. **catbot.pom.ts** (CatBotPOM):
   - Constructor takes page (CatBot is a floating panel available on every page)
   - Locator for CatBot trigger button (floating cat button at bottom-right)
   - Method `open()`: click trigger to open panel
   - Method `close()`: click close button or trigger again
   - Locator for CatBot panel container
   - Locator for message input in CatBot
   - Method `sendMessage(text: string)`: fill input, send
   - Locator for CatBot message list
   - Locator for contextual suggestions (chips/buttons that change per page)
   - Method `getSuggestions()`: returns text of visible suggestion buttons

4. **settings.pom.ts** (SettingsPOM):
   - goto() navigates to '/settings'
   - Locator for API keys section (look at settings/page.tsx for section headings)
   - Locator for processing section
   - Locator for CatBot section
   - Method `getApiKeyFields()`: locators for API key input fields
   - Locator for save/update buttons in each section

CRITICAL: Read each component's source file to extract EXACT Spanish labels.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && ls e2e/pages/canvas.pom.ts e2e/pages/connectors.pom.ts e2e/pages/catbot.pom.ts e2e/pages/settings.pom.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>4 POM files exist with typed locators using Spanish labels. All extend BasePage. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E specs for canvas, connectors, catbot, dashboard, settings</name>
  <files>app/e2e/specs/canvas.spec.ts, app/e2e/specs/connectors.spec.ts, app/e2e/specs/catbot.spec.ts, app/e2e/specs/dashboard.spec.ts, app/e2e/specs/settings.spec.ts</files>
  <action>
Create 5 spec files using corresponding POMs.

1. **canvas.spec.ts** (E2E-11):
   ```
   test.describe.serial('Canvas')
   - test: canvas page loads with list
     → Navigate to /canvas, verify page heading and list visible
   - test: create canvas with wizard
     → Use CanvasPOM.createCanvas(testName('Canvas E2E'))
     → Verify creation succeeds (redirect to editor or success feedback)
   - test: editor opens with START node
     → Open the created canvas
     → Verify editor area visible
     → Verify START node exists in the editor
   - test: drag nodes in editor
     → If node palette available, add a node
     → Drag a node to a new position using mouse events
     → Verify node moved (position changed)
   - test: save canvas
     → Click save button
     → Verify save confirmation
   - test: delete canvas
     → Go back to list, delete the [TEST] canvas
     → Verify removal
   - Teardown: cleanup via API if delete test fails
   ```

2. **connectors.spec.ts** (E2E-12):
   ```
   test.describe.serial('Conectores')
   - test: connectors page loads with type list
     → Navigate to /connectors
     → Verify connector type cards/options are visible
   - test: create connector
     → Use ConnectorsPOM.createConnector(testName('Conector E2E'), 'webhook')
     → Verify creation succeeds
   - test: test connection
     → Find created connector, click test button
     → Verify test result indicator appears (success or failure — both are valid UI states)
   - test: suggested templates visible
     → Verify templates/suggestions section exists
   - test: delete connector
     → Delete the [TEST] connector
     → Verify removal
   ```

3. **catbot.spec.ts** (E2E-13):
   ```
   test.describe('CatBot')
   - test: CatBot floating button visible on dashboard
     → Navigate to '/'
     → Verify CatBot trigger button exists
   - test: open CatBot panel
     → Click trigger button
     → Verify CatBot panel opens with message input
   - test: send message to CatBot
     → Type a test message, send
     → Verify user message appears in CatBot chat
     → Verify response area appears (do NOT wait for LLM completion)
   - test: contextual suggestions change by page
     → Navigate to /projects, open CatBot, note suggestions
     → Navigate to /tasks, open CatBot, check suggestions differ
     → Verify suggestions content differs between pages
   - test: close CatBot panel
     → Close the panel
     → Verify panel is hidden
   ```
   NOTE: CatBot depends on LLM. Verify UI interaction, not response content.

4. **dashboard.spec.ts** (E2E-14):
   ```
   test.describe('Dashboard')
   - test: dashboard loads with summary cards
     → Navigate to '/'
     → Verify summary cards are visible (projects count, sources count, etc.)
     → Verify at least one card shows a number (not loading state)
   - test: token usage chart area exists
     → Verify chart container/recharts element is present
   - test: recent activity section exists
     → Verify activity section heading and list exist
   - test: storage section exists
     → Verify storage info section is visible
   ```
   Uses DashboardPOM created in Plan 02.

5. **settings.spec.ts** (E2E-15):
   ```
   test.describe('Configuracion')
   - test: settings page loads
     → Navigate to /settings
     → Verify page heading visible
   - test: API keys section visible
     → Verify API keys section exists with input fields
     → Verify fields are present (do NOT verify actual key values)
   - test: processing section visible
     → Verify processing configuration section exists
   - test: CatBot section visible
     → Verify CatBot configuration section exists
   ```
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && ls e2e/specs/canvas.spec.ts e2e/specs/connectors.spec.ts e2e/specs/catbot.spec.ts e2e/specs/dashboard.spec.ts e2e/specs/settings.spec.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>5 spec files exist covering E2E-11 through E2E-15. Canvas uses serial for CRUD. CatBot verifies contextual suggestions change. Dashboard checks data presence. Settings verifies section visibility. Build passes.</done>
</task>

</tasks>

<verification>
- All 4 POM files exist under e2e/pages/
- All 5 spec files exist under e2e/specs/
- POMs use Spanish labels from actual components
- Canvas spec covers wizard + editor + drag + save flow
- CatBot spec tests contextual suggestion changes across pages
- npm run build passes
</verification>

<success_criteria>
4 POM files and 5 spec files created. POMs reference correct Spanish UI labels. Specs cover full behavior for E2E-11 through E2E-15. CatBot and canvas specs handle their unique interaction patterns (floating panel, drag-and-drop).
</success_criteria>

<output>
After completion, create `.planning/phases/36-playwright-setup-test-specs/36-04-SUMMARY.md`
</output>
