---
phase: 36-playwright-setup-test-specs
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - app/e2e/pages/sidebar.pom.ts
  - app/e2e/pages/dashboard.pom.ts
  - app/e2e/pages/projects.pom.ts
  - app/e2e/pages/sources.pom.ts
  - app/e2e/pages/processing.pom.ts
  - app/e2e/pages/rag.pom.ts
  - app/e2e/specs/navigation.spec.ts
  - app/e2e/specs/projects.spec.ts
  - app/e2e/specs/sources.spec.ts
  - app/e2e/specs/processing.spec.ts
  - app/e2e/specs/rag.spec.ts
autonomous: true
requirements:
  - E2E-01
  - E2E-02
  - E2E-03
  - E2E-04
  - E2E-05

must_haves:
  truths:
    - "Navigation spec verifies sidebar loads, all nav links work, breadcrumb updates, footer visible, CatBot floating button visible"
    - "Projects spec creates a [TEST] project, verifies it in list, opens it to see pipeline, deletes it"
    - "Sources spec uploads a file, verifies it appears with Ready status, changes mode, searches, deletes"
    - "Processing spec selects agent/model/skills, triggers processing, verifies loading state and history"
    - "RAG spec triggers indexing, verifies progress bar, checks stats cards, submits query and verifies response area, re-indexes"
  artifacts:
    - path: "app/e2e/pages/sidebar.pom.ts"
      provides: "SidebarPOM with nav link locators"
      exports: ["SidebarPOM"]
    - path: "app/e2e/pages/projects.pom.ts"
      provides: "ProjectsPOM with create/list/delete methods"
      exports: ["ProjectsPOM"]
    - path: "app/e2e/pages/sources.pom.ts"
      provides: "SourcesPOM with upload/search/mode-change/delete methods"
      exports: ["SourcesPOM"]
    - path: "app/e2e/specs/navigation.spec.ts"
      provides: "E2E-01 navigation spec"
    - path: "app/e2e/specs/projects.spec.ts"
      provides: "E2E-02 projects spec"
  key_links:
    - from: "app/e2e/specs/navigation.spec.ts"
      to: "app/e2e/pages/sidebar.pom.ts"
      via: "import SidebarPOM"
      pattern: "import.*SidebarPOM"
    - from: "app/e2e/specs/projects.spec.ts"
      to: "app/e2e/pages/projects.pom.ts"
      via: "import ProjectsPOM"
      pattern: "import.*ProjectsPOM"
---

<objective>
Create POMs and E2E specs for navigation, projects, sources, processing, and RAG sections.

Purpose: Cover the first 5 E2E requirements (E2E-01 through E2E-05) with typed Page Object Models and spec files.
Output: 6 POM files (sidebar, dashboard, projects, sources, processing, rag) and 5 spec files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-playwright-setup-test-specs/36-RESEARCH.md
@.planning/phases/36-playwright-setup-test-specs/36-01-SUMMARY.md

@app/e2e/pages/base.page.ts
@app/e2e/helpers/test-data.ts
@app/src/components/layout/sidebar.tsx
@app/src/app/page.tsx
@app/src/app/projects/page.tsx
@app/src/app/projects/new/page.tsx
@app/src/app/projects/[id]/page.tsx
@app/src/components/sources/source-manager.tsx
@app/src/components/sources/source-list.tsx
@app/src/components/process/process-panel.tsx
@app/src/components/rag/rag-panel.tsx
</context>

<interfaces>
<!-- Sidebar nav items from sidebar.tsx — use these exact labels for locators -->
Dashboard, Proyectos, Agentes, Docs Workers, Skills, Tareas, Canvas, Conectores, Notificaciones, Configuracion, Estado del Sistema

<!-- Routes -->
/, /projects, /agents, /workers, /skills, /tasks, /canvas, /connectors, /notifications, /settings, /system

<!-- Base POM from Plan 01 -->
```typescript
export class BasePage {
  readonly page: Page;
  readonly sidebar: Locator;
  constructor(page: Page);
  async navigateTo(path: string): Promise<void>;
  async waitForApi(urlPattern: string | RegExp): Promise<Response>;
  async getSidebarLink(label: string): Promise<Locator>;
}
```

<!-- Test data helpers from Plan 01 -->
```typescript
export const TEST_PREFIX = '[TEST]';
export function testName(base: string): string;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create POMs for sidebar, dashboard, projects, sources, processing, RAG</name>
  <files>app/e2e/pages/sidebar.pom.ts, app/e2e/pages/dashboard.pom.ts, app/e2e/pages/projects.pom.ts, app/e2e/pages/sources.pom.ts, app/e2e/pages/processing.pom.ts, app/e2e/pages/rag.pom.ts</files>
  <action>
Create 6 POM files, all extending BasePage. Use `getByRole`, `getByText`, `getByLabel`, `getByPlaceholder` locators with SPANISH text. Read the actual component source files to extract exact labels, button text, and placeholder text.

1. **sidebar.pom.ts** (SidebarPOM):
   - Locators for each nav link using exact labels from sidebar.tsx navItems: 'Dashboard', 'Proyectos', 'Agentes', 'Docs Workers', 'Skills', 'Tareas', 'Canvas', 'Conectores', 'Notificaciones', 'Configuracion', 'Estado del Sistema'
   - Method `allNavLinks()` returning array of {label, href} objects
   - Method `clickNav(label: string)` that clicks the nav link
   - Locator for footer area (service status indicators at bottom of sidebar)
   - Locator for CatBot floating button (look for the CatBot trigger in the layout -- it's a floating button with cat icon)
   - Locator for notification bell
   - Locator for breadcrumb area (look at layout.tsx or page headers for breadcrumb elements)

2. **dashboard.pom.ts** (DashboardPOM):
   - goto() navigates to '/'
   - Locators for summary cards (look at app/src/app/page.tsx for card titles -- likely "Proyectos", "Fuentes", "Agentes", "Tareas")
   - Locator for token usage chart area (recharts container)
   - Locator for recent activity section
   - Locator for storage section

3. **projects.pom.ts** (ProjectsPOM):
   - goto() navigates to '/projects'
   - Locator for "Nuevo Proyecto" button/link
   - Method `createProject(name: string, description?: string)`: clicks new project, fills name field (look at projects/new/page.tsx for exact label -- likely getByLabel or getByPlaceholder), fills description, submits
   - Method `findProjectInList(name: string)`: locator for project card containing the name text
   - Method `openProject(name: string)`: clicks on the project card/link
   - Method `deleteProject(name: string)`: finds delete button, clicks, confirms dialog
   - Locator for project search input
   - Locator for pipeline nav (5 steps: Fuentes, Procesamiento, RAG, Chat, Bot)

4. **sources.pom.ts** (SourcesPOM):
   - Constructor takes page (no standalone route -- sources are within /projects/[id])
   - Method `uploadFile(filePath: string)`: uses page.setInputFiles() on the hidden file input (react-dropzone uses input[type=file])
   - Locator for source list items
   - Method `findSource(name: string)`: locator for source item containing name
   - Method `deleteSource(name: string)`: find delete button within source item, click, confirm
   - Method `searchSources(query: string)`: fill search input
   - Locator for source status badge ("Ready", "Pendiente", etc.)
   - Method `changeMode(mode: string)`: find the source mode selector (read source-manager.tsx for exact element -- likely a select, radio group, or toggle for switching between file/URL/note modes), select the given mode value, verify UI updates to show the corresponding input area

5. **processing.pom.ts** (ProcessingPOM):
   - Constructor takes page (within project context)
   - Locator for agent selector dropdown
   - Locator for model selector dropdown
   - Method `selectAgent(name: string)`: interact with agent dropdown
   - Method `triggerProcessing()`: click process button (look at process-panel.tsx for exact button text -- likely "Procesar" or "Generar")
   - Locator for loading/streaming indicator
   - Locator for version history list

6. **rag.pom.ts** (RagPOM):
   - Constructor takes page (within project context)
   - Method `triggerIndex()`: click index button (look at rag-panel.tsx -- likely "Indexar" or "Crear indice")
   - Locator for progress bar during indexing
   - Locators for stats cards (chunks count, collection info)
   - Method `queryRag(text: string)`: fill query input, click submit button (read rag-panel.tsx for exact submit button text/locator)
   - Locator for query response area (the container that shows RAG query results)
   - Method `reindex()`: click re-index button

CRITICAL: Read each component's source file before writing the POM to get EXACT Spanish labels and element structure. Do NOT guess labels.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && ls e2e/pages/sidebar.pom.ts e2e/pages/dashboard.pom.ts e2e/pages/projects.pom.ts e2e/pages/sources.pom.ts e2e/pages/processing.pom.ts e2e/pages/rag.pom.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>6 POM files exist with typed locators using Spanish labels extracted from actual component source. All extend BasePage. SourcesPOM includes changeMode method. RagPOM includes queryRag with submit and response area locator. SidebarPOM includes breadcrumb and footer locators. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E specs for navigation, projects, sources, processing, RAG</name>
  <files>app/e2e/specs/navigation.spec.ts, app/e2e/specs/projects.spec.ts, app/e2e/specs/sources.spec.ts, app/e2e/specs/processing.spec.ts, app/e2e/specs/rag.spec.ts</files>
  <action>
Create 5 spec files. Each imports `test` and `expect` from '@playwright/test' and the corresponding POM. Use `test.describe.serial()` for specs that need ordered CRUD operations. All test data uses `testName()` helper with [TEST] prefix.

1. **navigation.spec.ts** (E2E-01):
   ```
   test.describe.serial('Navegacion')
   - test: sidebar loads with all nav links
     -> Instantiate SidebarPOM, verify nav element visible
     -> For each navItem label, verify link is visible
   - test: all sidebar links navigate correctly
     -> For each nav link, click and verify URL changes to expected href
     -> Wait for networkidle after each navigation
   - test: breadcrumb updates after navigation
     -> Navigate to /projects via sidebar
     -> Verify breadcrumb area contains text "Proyectos" (use SidebarPOM breadcrumb locator)
     -> Navigate to /settings via sidebar
     -> Verify breadcrumb updates to contain "Configuracion"
   - test: footer and service status visible
     -> Navigate to any page (e.g. '/')
     -> Verify footer/service-status area is visible (use SidebarPOM footer locator)
     -> Verify at least one service status indicator is present within the footer area
   - test: CatBot floating button is visible
     -> Navigate to any page, verify CatBot trigger button exists
   - test: notification bell is visible
     -> Verify bell icon exists in sidebar header area
   ```

2. **projects.spec.ts** (E2E-02):
   ```
   test.describe.serial('Proyectos')
   - test: create project
     -> Use ProjectsPOM.createProject(testName('Proyecto E2E'), 'Descripcion de prueba')
     -> Verify redirect to project detail page or success toast
   - test: project appears in list
     -> Navigate to /projects
     -> Verify testName('Proyecto E2E') appears in list
   - test: open project shows pipeline
     -> Click on the project
     -> Verify pipeline nav shows 5 steps (Fuentes, Procesamiento, RAG, Chat, Bot)
   - test: delete project with confirmation
     -> Delete the test project
     -> Verify confirmation dialog appears, confirm
     -> Verify project no longer in list
   ```

3. **sources.spec.ts** (E2E-03):
   ```
   test.describe.serial('Fuentes')
   - Setup: create [TEST] project first via API (fetch POST /api/projects)
   - test: upload file appears in list
     -> Navigate to project, go to sources tab
     -> Upload e2e/fixtures/sample.txt via setInputFiles
     -> Wait for upload completion
     -> Verify file appears in source list
   - test: source shows Ready status
     -> Verify uploaded source has status badge
   - test: change source mode (cambiar modo)
     -> Use SourcesPOM.changeMode() to switch to a different source mode (e.g. from file upload to URL or note mode)
     -> Verify the UI updates: the input area changes to match the selected mode (e.g. URL input field appears, or note editor appears)
     -> Switch back to file mode to confirm toggling works
   - test: search filters sources
     -> Type in search input, verify filtering works
   - test: delete source
     -> Delete the uploaded source
     -> Verify it disappears from list
   - Teardown: delete [TEST] project via API
   ```

4. **processing.spec.ts** (E2E-04):
   ```
   test.describe.serial('Procesamiento')
   - Setup: create [TEST] project via API, upload a source
   - test: processing panel shows agent/model selectors
     -> Navigate to project processing tab
     -> Verify agent selector and model selector are visible
   - test: trigger processing shows loading state
     -> Select an agent (if available, otherwise skip)
     -> Click process button
     -> Verify loading/streaming indicator appears
     -> Do NOT wait for LLM completion -- just verify UI state
   - test: processing history visible
     -> Verify version history section exists (may be empty if processing didn't complete)
   - Teardown: delete [TEST] project via API
   ```
   NOTE: Processing depends on LLM service. Test verifies UI states, NOT output quality.

5. **rag.spec.ts** (E2E-05):
   ```
   test.describe.serial('RAG')
   - Setup: create [TEST] project via API with a source
   - test: RAG panel shows index button
     -> Navigate to project RAG tab
     -> Verify index/create button is visible
   - test: trigger indexing shows progress
     -> Click index button
     -> Verify progress indicator appears (bar or spinner)
     -> Do NOT wait for completion if Qdrant/Ollama unavailable
   - test: RAG stats cards appear
     -> Verify stats section exists (chunks count, collection info)
   - test: RAG query submission and response
     -> Verify query input field and submit button are present
     -> Use RagPOM.queryRag('test query') to type a query and click submit
     -> Verify response area container appears or becomes visible (use RagPOM response area locator)
     -> Handle Qdrant-unavailable gracefully: if the query fails with a service error, verify the error message is displayed in the UI rather than an unhandled crash (use try/catch or soft assertion -- expect the response area OR an error toast/message to be visible)
   - Teardown: delete [TEST] project via API
   ```
   NOTE: RAG depends on Qdrant + Ollama. Test verifies UI states exist, not that indexing completes. Query test verifies the submission flow works, not the quality of results.

For specs 3-5 that need a project: Create the project in a `test.beforeAll` block using Playwright's `request` fixture to POST /api/projects with [TEST] prefix name. Delete in `test.afterAll`. This ensures cleanup even if tests fail.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && ls e2e/specs/navigation.spec.ts e2e/specs/projects.spec.ts e2e/specs/sources.spec.ts e2e/specs/processing.spec.ts e2e/specs/rag.spec.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>5 spec files exist covering E2E-01 through E2E-05. Navigation spec includes breadcrumb and footer tests. Sources spec includes "cambiar modo" test step. RAG spec includes query submission with response area verification. Each uses corresponding POM, [TEST] prefix for data, serial ordering for CRUD flows. Specs that depend on external services (LLM, Qdrant) verify UI states not output quality. Build passes.</done>
</task>

</tasks>

<verification>
- All 6 POM files exist under e2e/pages/
- All 5 spec files exist under e2e/specs/
- POMs use Spanish labels matching actual component source
- Specs use [TEST] prefix via testName() helper
- Specs with CRUD use test.describe.serial()
- navigation.spec.ts has breadcrumb and footer tests
- sources.spec.ts has "cambiar modo" test step
- rag.spec.ts has query submission and response verification
- npm run build passes
</verification>

<success_criteria>
6 POM files and 5 spec files created. All TypeScript compiles. POMs reference correct Spanish UI labels. Specs cover the full behavior described in E2E-01 through E2E-05 requirements including: breadcrumbs/footer (E2E-01), mode change (E2E-03), and RAG query submission (E2E-05).
</success_criteria>

<output>
After completion, create `.planning/phases/36-playwright-setup-test-specs/36-02-SUMMARY.md`
</output>
