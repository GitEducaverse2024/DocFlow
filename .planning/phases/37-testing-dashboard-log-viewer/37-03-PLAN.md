---
phase: 37-testing-dashboard-log-viewer
plan: 03
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - app/src/components/testing/test-run-history.tsx
  - app/src/components/testing/test-result-detail.tsx
  - app/src/components/testing/test-ai-generator.tsx
  - app/src/app/testing/page.tsx
autonomous: true
requirements: [TEST-06, TEST-07, TEST-08]

must_haves:
  truths:
    - "Historial tab shows last 10 test runs with timestamp, status, and aggregate counts"
    - "Different runs are visually comparable (consistent layout, color-coded status)"
    - "Failed tests show error message from results_json"
    - "Failed tests show screenshot as base64 image if available"
    - "Failed tests show test code from the spec file"
    - "AI generate button opens a dialog, user selects section, LLM returns spec code"
  artifacts:
    - path: "app/src/components/testing/test-run-history.tsx"
      provides: "History tab with last 10 runs"
      min_lines: 50
    - path: "app/src/components/testing/test-result-detail.tsx"
      provides: "Failed test detail panel"
      min_lines: 60
    - path: "app/src/components/testing/test-ai-generator.tsx"
      provides: "AI test generation dialog"
      min_lines: 70
  key_links:
    - from: "app/src/components/testing/test-run-history.tsx"
      to: "useTestRunner"
      via: "runs prop from hook"
      pattern: "runs.*TestRun"
    - from: "app/src/components/testing/test-ai-generator.tsx"
      to: "/api/testing/generate"
      via: "fetch POST"
      pattern: "fetch.*api/testing/generate"
---

<objective>
Build the Historial tab with run history, failed test detail view (error + screenshot + code), and AI test generation feature.

Purpose: Users need to compare test runs over time, diagnose failures, and generate new tests with AI assistance.
Output: test-run-history.tsx, test-result-detail.tsx, test-ai-generator.tsx, updated page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-testing-dashboard-log-viewer/37-RESEARCH.md
@.planning/phases/37-testing-dashboard-log-viewer/37-01-SUMMARY.md

@app/src/app/testing/page.tsx
</context>

<interfaces>
<!-- From Plan 01 APIs and Plan 02 hook -->

TestRun type (from use-test-runner.ts):
```typescript
interface TestResult { title: string; file: string; status: 'passed' | 'failed' | 'skipped'; duration: number }
interface TestRun { id: string; type: string; section: string | null; status: string; total: number; passed: number; failed: number; skipped: number; duration_seconds: number; results_json: TestResult[]; created_at: string }
```

POST /api/testing/generate:
```typescript
// Request: { section: string }
// Response: { code: string }
```

useTestRunner hook exports:
```typescript
{ runs: TestRun[], latestRun: TestRun | null, isRunning: boolean, runOutput: string, loading: boolean, runTests: (section?: string) => void, fetchResults: () => void }
```

Playwright screenshot location: `test-results/` directory relative to app root.
Spec file locations: `e2e/specs/{section}.spec.ts`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create history list + failed test detail components</name>
  <files>
    app/src/components/testing/test-run-history.tsx,
    app/src/components/testing/test-result-detail.tsx
  </files>
  <action>
    **test-run-history.tsx** — Client component receiving `runs: TestRun[]` as prop.
    - Render a list of the last 10 test runs as cards/rows.
    - Each run shows: relative time (e.g. "hace 5 min" — compute from created_at), type badge ("Completa" or section name), status badge (green "Pasaron" / red "Fallaron" / yellow "Interrumpido"), and aggregate counts: total/passed/failed/skipped.
    - Duration shown as "{X}s".
    - Use a compact table-like layout with alternating row backgrounds (zinc-900/zinc-950).
    - Color-coded status: green bg for passed, red bg for failed, amber bg for interrupted/timedout.
    - If runs is empty, show "Sin historial de ejecuciones" message.
    - Make each run clickable — when clicked, expand to show the results_json list (reuse the same individual test display pattern from test-section-list). Use `useState<string | null>(null)` to track expanded run ID.
    - Failed tests in the expanded view should render a `<TestResultDetail>` component.

    **test-result-detail.tsx** — Client component receiving `result: TestResult` as prop. This is used when a test has status 'failed'.
    - Show error message: Extract from result object. Note: The current results_json schema has `{ title, file, status, duration }` — the error message may not be in the data. Add a fetch to get screenshot and code on-demand.
    - **Screenshot**: Make a fetch to a new inline API call or use the test-results directory. Since screenshots are on disk and need to be served: add a small fetch to `/api/testing/screenshot?file={encoded-test-title}` — BUT to keep this simpler and avoid a new endpoint, read screenshots in the results route and include as optional base64 in results_json. ALTERNATIVE (simpler): In this component, just show a placeholder "[Screenshot no disponible]" if no screenshot data is present. The actual screenshot embedding can be handled by enriching the results endpoint later. For now, show what's available.
    - **Test code**: Fetch the spec file content. Make a client-side fetch to `/api/testing/generate` endpoint repurposed — NO, that's AI generation. Instead, add a small utility: read the spec file path from `result.file`, and use an inline fetch. SIMPLEST approach: Add `code` field reading in the results API route (Plan 01 already returns results_json). For this plan, create the component to accept optional `screenshot?: string` and `code?: string` props, and show them when available.
    - Display sections: (1) Error area with red bg, monospace font. (2) Screenshot area with img tag (src as data URL if base64). (3) Code area with pre/code block, syntax-highlighted with monospace font and zinc-900 bg.
    - Use ChevronDown/ChevronUp to toggle detail visibility.

    **IMPORTANT**: To populate screenshot and code data, update the GET /api/testing/results endpoint to enrich failed test results. Add to Task 2 the enrichment logic in page.tsx or as a separate fetch.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>History list renders last 10 runs with time, status, counts. Expandable to show individual results. Failed test detail component shows error/screenshot/code sections.</done>
</task>

<task type="auto">
  <name>Task 2: Create AI generator dialog + wire into page</name>
  <files>
    app/src/components/testing/test-ai-generator.tsx,
    app/src/app/testing/page.tsx
  </files>
  <action>
    **test-ai-generator.tsx** — Client component with a dialog/modal for AI test generation.
    - Trigger: A button "Generar test con IA" (Sparkles icon) that opens the dialog.
    - Dialog content:
      - Dropdown/select to choose a section from the 15 available sections.
      - "Generar" button (violet-600) that calls POST /api/testing/generate with `{ section }`.
      - Loading state: show Loader2 spinner while waiting for LLM response.
      - Result area: Show generated code in a `<pre><code>` block with monospace font, zinc-900 bg, overflow-x-auto.
      - "Copiar" button (Copy icon) that copies the generated code to clipboard using `navigator.clipboard.writeText()`.
      - "Cerrar" button to dismiss.
    - Use the Dialog component from `@/components/ui/dialog` (already exists in project).
    - All text in Spanish.

    **page.tsx** — Wire the Historial tab and AI generator:
    - Replace the Historial tab placeholder with `<TestRunHistory runs={runs} />` where `runs` comes from `useTestRunner`.
    - Add the `<TestAiGenerator />` component to the page header area, next to the "Ejecutar todos" button. Show it as a secondary button (zinc-800 bg, zinc-300 text).
    - Ensure the Resultados tab content from Plan 02 is preserved (do NOT overwrite, only add to Historial tab and header).
    - Import TestRunHistory and TestAiGenerator.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Historial tab shows last 10 runs with expandable details. Failed tests show error/screenshot/code. AI generator button opens dialog, generates code via LLM, shows result with copy button. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd app && npm run build` completes without errors
- Historial tab shows test run history with timestamps and counts
- Failed test details show error info
- AI generator dialog opens, accepts section, returns code
</verification>

<success_criteria>
- Last 10 test runs visible in Historial tab with visual status
- Runs are expandable to show individual test results
- Failed tests display available diagnostic info
- AI generator produces Playwright spec code via LLM
- Generated code is copyable to clipboard
</success_criteria>

<output>
After completion, create `.planning/phases/37-testing-dashboard-log-viewer/37-03-SUMMARY.md`
</output>
