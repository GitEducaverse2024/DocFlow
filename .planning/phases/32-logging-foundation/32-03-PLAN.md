---
phase: 32-logging-foundation
plan: 03
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - app/src/app/api/agents/route.ts
  - app/src/app/api/agents/create/route.ts
  - app/src/app/api/agents/generate/route.ts
  - app/src/app/api/agents/[id]/route.ts
  - app/src/app/api/agents/[id]/files/route.ts
  - app/src/app/api/workers/route.ts
  - app/src/app/api/workers/[id]/route.ts
  - app/src/app/api/workers/generate/route.ts
  - app/src/app/api/skills/route.ts
  - app/src/app/api/skills/[id]/route.ts
  - app/src/app/api/skills/generate/route.ts
  - app/src/app/api/skills/import/route.ts
  - app/src/app/api/skills/openclaw/route.ts
  - app/src/app/api/settings/route.ts
  - app/src/app/api/settings/processing/route.ts
  - app/src/app/api/settings/models/route.ts
  - app/src/app/api/settings/api-keys/route.ts
  - app/src/app/api/settings/api-keys/[provider]/route.ts
  - app/src/app/api/settings/api-keys/[provider]/test/route.ts
  - app/src/app/api/dashboard/summary/route.ts
  - app/src/app/api/dashboard/activity/route.ts
  - app/src/app/api/dashboard/usage/route.ts
  - app/src/app/api/dashboard/top-models/route.ts
  - app/src/app/api/dashboard/top-agents/route.ts
  - app/src/app/api/dashboard/storage/route.ts
  - app/src/app/api/projects/route.ts
  - app/src/app/api/projects/[id]/route.ts
  - app/src/app/api/projects/[id]/sources/route.ts
  - app/src/app/api/projects/[id]/sources/[sid]/route.ts
  - app/src/app/api/projects/[id]/sources/reorder/route.ts
  - app/src/app/api/projects/[id]/stats/route.ts
  - app/src/app/api/mcp/[projectId]/route.ts
  - app/src/app/api/health/route.ts
autonomous: true
requirements: [LOG-02]

must_haves:
  truths:
    - "Agent generation endpoints log LLM calls"
    - "Worker generation endpoints log LLM calls"
    - "Skill generation endpoints log LLM calls"
    - "Settings API key test endpoints log external service calls"
    - "All catch blocks in remaining API routes use logger.error"
  artifacts:
    - path: "app/src/app/api/agents/generate/route.ts"
      provides: "Agent generation with structured logging"
      contains: "logger.info('agents'"
    - path: "app/src/app/api/settings/api-keys/[provider]/test/route.ts"
      provides: "API key test with structured logging"
      contains: "logger.info('settings'"
    - path: "app/src/app/api/health/route.ts"
      provides: "Health check with structured logging"
      contains: "logger"
  key_links:
    - from: "app/src/app/api/agents/generate/route.ts"
      to: "app/src/lib/logger.ts"
      via: "import { logger }"
      pattern: "logger\\.(info|error)\\('agents'"
---

<objective>
Integrate the structured logger into the remaining 33 API routes: agents, workers, skills, settings, dashboard, projects CRUD, sources, MCP, and health. These are the secondary routes that complete LOG-02 coverage.

Purpose: Complete the "logger integrated in all main endpoints" requirement. After this plan, zero console.log/error/warn calls remain in any API route.
Output: All remaining API route files using structured logger calls.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-logging-foundation/32-RESEARCH.md
@.planning/phases/32-logging-foundation/32-01-SUMMARY.md

<interfaces>
From app/src/lib/logger.ts (after Plan 01):
```typescript
export type LogLevel = 'info' | 'warn' | 'error';
export type LogSource =
  | 'processing' | 'chat' | 'rag' | 'catbot'
  | 'tasks' | 'canvas' | 'connectors' | 'system'
  | 'agents' | 'workers' | 'skills' | 'settings';

export const logger = {
  info: (source: LogSource, msg: string, meta?: Record<string, unknown>) => void,
  warn: (source: LogSource, msg: string, meta?: Record<string, unknown>) => void,
  error: (source: LogSource, msg: string, meta?: Record<string, unknown>) => void,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate logger into agents, workers, skills, and settings routes</name>
  <files>
    app/src/app/api/agents/route.ts,
    app/src/app/api/agents/create/route.ts,
    app/src/app/api/agents/generate/route.ts,
    app/src/app/api/agents/[id]/route.ts,
    app/src/app/api/agents/[id]/files/route.ts,
    app/src/app/api/workers/route.ts,
    app/src/app/api/workers/[id]/route.ts,
    app/src/app/api/workers/generate/route.ts,
    app/src/app/api/skills/route.ts,
    app/src/app/api/skills/[id]/route.ts,
    app/src/app/api/skills/generate/route.ts,
    app/src/app/api/skills/import/route.ts,
    app/src/app/api/skills/openclaw/route.ts,
    app/src/app/api/settings/route.ts,
    app/src/app/api/settings/processing/route.ts,
    app/src/app/api/settings/models/route.ts,
    app/src/app/api/settings/api-keys/route.ts,
    app/src/app/api/settings/api-keys/[provider]/route.ts,
    app/src/app/api/settings/api-keys/[provider]/test/route.ts
  </files>
  <action>
For each file, add `import { logger } from '@/lib/logger';` and replace ALL console.log/error/warn calls.

Source mapping:
- agents/* → source: 'agents'
- workers/* → source: 'workers'
- skills/* → source: 'skills'
- settings/* → source: 'settings'

For LLM-calling routes (agents/generate, workers/generate, skills/generate):
- logger.info at POST start: `logger.info('agents', 'Generando agente', { model })`
- logger.info on success: `logger.info('agents', 'Agente generado', { agentId })`
- logger.error in catch

For API key test route (settings/api-keys/[provider]/test):
- logger.info at start: `logger.info('settings', 'Probando API key', { provider })`
- logger.info/error on result

For CRUD routes (agents/route.ts, workers/route.ts, etc.):
- logger.info for POST/PUT/DELETE mutations only
- logger.error in catch blocks
- Do NOT add logger.info for GET list/detail operations

Rules:
- NEVER log API key values — only log the provider name
- For agent/worker/skill creation, log the name/ID but not full configuration
- Remove all console.log/error/warn after replacement
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 19 route files import logger, use correct source identifiers, zero console.log/error/warn remain, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Integrate logger into dashboard, projects, sources, MCP, and health routes</name>
  <files>
    app/src/app/api/dashboard/summary/route.ts,
    app/src/app/api/dashboard/activity/route.ts,
    app/src/app/api/dashboard/usage/route.ts,
    app/src/app/api/dashboard/top-models/route.ts,
    app/src/app/api/dashboard/top-agents/route.ts,
    app/src/app/api/dashboard/storage/route.ts,
    app/src/app/api/projects/route.ts,
    app/src/app/api/projects/[id]/route.ts,
    app/src/app/api/projects/[id]/sources/route.ts,
    app/src/app/api/projects/[id]/sources/[sid]/route.ts,
    app/src/app/api/projects/[id]/sources/reorder/route.ts,
    app/src/app/api/projects/[id]/stats/route.ts,
    app/src/app/api/mcp/[projectId]/route.ts,
    app/src/app/api/health/route.ts
  </files>
  <action>
For each file, add `import { logger } from '@/lib/logger';` and replace ALL console.log/error/warn calls.

Source mapping:
- dashboard/* → source: 'system'
- projects/* (CRUD, not chat/process/rag) → source: 'system'
- sources/* → source: 'system'
- mcp/* → source: 'system'
- health → source: 'system'

For dashboard routes (all GET, read-only):
- logger.error in catch blocks ONLY
- Do NOT add logger.info — these are high-frequency polling queries

For projects CRUD:
- logger.info for POST (create) and DELETE operations
- logger.error in catch blocks
- Do NOT log GET operations

For sources:
- logger.info for POST (upload), DELETE operations
- logger.error in catch blocks

For MCP route:
- logger.info for incoming MCP requests with metadata
- logger.error in catch blocks

For health route:
- logger.error in catch blocks only (health checks are very frequent)

Rules:
- Same as previous plans: no sensitive data, extract error.message, remove console calls
- Dashboard routes should be minimal logging — they poll frequently
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 14 route files import logger, zero console.log/error/warn remain in any API route across the entire codebase, build succeeds</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npm run build` — must succeed
2. `grep -rn "console\.\(log\|error\|warn\)" app/src/app/api/ | grep -v node_modules | wc -l` — must return 0
3. `grep -rn "console\.\(log\|error\|warn\)" app/src/lib/services/ | grep -v node_modules | wc -l` — must return 0 (from Plan 01)
</verification>

<success_criteria>
- All 33 remaining route files use structured logger
- Zero console.log/error/warn calls in ANY API route file across the entire app/src/app/api/ directory
- Combined with Plan 01 (services) and Plan 02 (core routes), LOG-02 is fully satisfied
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/32-logging-foundation/32-03-SUMMARY.md`
</output>
