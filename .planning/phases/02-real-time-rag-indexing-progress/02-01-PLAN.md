---
phase: 02-real-time-rag-indexing-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/services/rag-jobs.ts
  - app/src/app/api/projects/[id]/rag/status/route.ts
  - app/src/app/api/projects/[id]/rag/create/route.ts
  - app/scripts/rag-worker.mjs
  - app/src/components/rag/rag-panel.tsx
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
  - PROG-04
  - PROG-05

must_haves:
  truths:
    - "Durante la indexación RAG, la UI muestra una barra de progreso visual con porcentaje basado en chunksProcessed/chunksTotal"
    - "El texto descriptivo del paso actual se actualiza en tiempo real durante la indexación"
    - "El tiempo transcurrido es visible durante la indexación"
    - "Al completar, un toast muestra resumen con N vectores indexados"
    - "npm run build pasa sin errores"
  artifacts:
    - path: "app/src/lib/services/rag-jobs.ts"
      provides: "Job tracker con campos chunksProcessed y chunksTotal"
      contains: "chunksProcessed"
    - path: "app/src/app/api/projects/[id]/rag/status/route.ts"
      provides: "Endpoint que devuelve chunksProcessed y chunksTotal"
      contains: "chunksProcessed"
    - path: "app/scripts/rag-worker.mjs"
      provides: "Worker que escribe chunksProcessed y chunksTotal al archivo de estado"
      contains: "chunksProcessed"
    - path: "app/src/components/rag/rag-panel.tsx"
      provides: "Barra de progreso visual con porcentaje"
      contains: "chunksProcessed"
  key_links:
    - from: "app/scripts/rag-worker.mjs"
      to: "/tmp/rag-{projectId}.json"
      via: "writeStatus con chunksProcessed y chunksTotal"
      pattern: "chunksProcessed"
    - from: "app/src/app/api/projects/[id]/rag/create/route.ts"
      to: "app/src/lib/services/rag-jobs.ts"
      via: "ragJobs.updateProgress con datos estructurados"
      pattern: "chunksProcessed"
    - from: "app/src/components/rag/rag-panel.tsx"
      to: "app/src/app/api/projects/[id]/rag/status/route.ts"
      via: "Polling cada 2s, lee chunksProcessed/chunksTotal para barra de progreso"
      pattern: "chunksProcessed"
---

<objective>
Añadir progreso estructurado (chunksProcessed/chunksTotal) a toda la cadena RAG: worker → archivo de estado → job tracker → status endpoint → rag-panel UI con barra de progreso visual.

Purpose: El usuario actualmente ve solo texto genérico durante la indexación. Con datos estructurados, la UI puede mostrar una barra de progreso real con porcentaje y paso actual.

Output: Barra de progreso visual en rag-panel durante indexación con porcentaje, paso actual, y tiempo transcurrido.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Cadena de datos de progreso: worker → status file → create route → rag-jobs → status endpoint → rag-panel -->

Archivo de estado del worker (/tmp/rag-{projectId}.json) — formato actual:
```json
{ "status": "running", "progress": "Embedding 15/44...", "updatedAt": 1234567890 }
```

Formato deseado — añadir chunksProcessed y chunksTotal:
```json
{ "status": "running", "progress": "Generando embedding 15/44...", "chunksProcessed": 15, "chunksTotal": 44, "updatedAt": 1234567890 }
```

RagJob interface actual (rag-jobs.ts):
```typescript
export interface RagJob {
  id: string;
  projectId: string;
  status: 'running' | 'completed' | 'error';
  progress: string;
  startedAt: number;
  completedAt?: number;
  error?: string;
  chunksCount?: number;
}
```

Campos a añadir: chunksProcessed?: number; chunksTotal?: number;

Status endpoint actual devuelve:
```json
{ "jobId": "...", "status": "running", "progress": "...", "startedAt": ..., "chunksCount": ... }
```

Campos a añadir: chunksProcessed, chunksTotal

rag-panel.tsx ya tiene:
- Polling cada 2s (startPolling/stopPolling)
- Timer de elapsed time (ragElapsed)
- Log history (getRagLogs)
- Toast en completado
Lo que falta: barra de progreso visual con porcentaje
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir chunksProcessed/chunksTotal a rag-worker.mjs</name>
  <files>app/scripts/rag-worker.mjs</files>
  <action>
En `app/scripts/rag-worker.mjs`, modificar la función `writeStatus` y las llamadas a ella para incluir `chunksProcessed` y `chunksTotal`:

1. En el loop de embeddings (línea ~194), cambiar:
   ```javascript
   // Antes:
   const msg = `Embedding ${i + 1}/${total}...`;
   writeStatus('running', msg);

   // Después:
   const msg = `Generando embedding ${i + 1}/${total}...`;
   writeStatus('running', msg, { chunksProcessed: i + 1, chunksTotal: total });
   ```

2. Antes del loop, cuando hace chunking, añadir chunksTotal:
   ```javascript
   writeStatus('running', 'Dividiendo en chunks...', { chunksProcessed: 0, chunksTotal: 0 });
   // Después de generar chunks:
   writeStatus('running', `${chunks.length} chunks generados. Iniciando embeddings...`, { chunksProcessed: 0, chunksTotal: chunks.length });
   ```

3. En `writeStatus('completed', ...)`, incluir chunksTotal:
   ```javascript
   writeStatus('completed', 'Completado', { chunksCount: total, chunksProcessed: total, chunksTotal: total });
   ```

No cambiar la firma de writeStatus — ya acepta `extra = {}` como tercer parámetro.
  </action>
  <verify>
    <automated>grep -c "chunksProcessed" app/scripts/rag-worker.mjs; grep -c "chunksTotal" app/scripts/rag-worker.mjs</automated>
  </verify>
  <done>rag-worker.mjs escribe chunksProcessed y chunksTotal en cada actualización de estado durante el loop de embeddings.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar rag-jobs.ts y status/route.ts</name>
  <files>app/src/lib/services/rag-jobs.ts, app/src/app/api/projects/[id]/rag/status/route.ts</files>
  <action>
1. En `app/src/lib/services/rag-jobs.ts`:
   a. Añadir campos a la interface RagJob:
      ```typescript
      chunksProcessed?: number;
      chunksTotal?: number;
      ```
   b. Actualizar `updateProgress` para aceptar datos estructurados:
      ```typescript
      updateProgress(projectId: string, progress: string, chunksProcessed?: number, chunksTotal?: number) {
        const job = jobs.get(projectId);
        if (job) {
          job.progress = progress;
          if (chunksProcessed !== undefined) job.chunksProcessed = chunksProcessed;
          if (chunksTotal !== undefined) job.chunksTotal = chunksTotal;
        }
      },
      ```

2. En `app/src/app/api/projects/[id]/rag/status/route.ts`:
   Añadir `chunksProcessed` y `chunksTotal` a la respuesta JSON:
   ```typescript
   return NextResponse.json({
     jobId: job.id,
     status: job.status,
     progress: job.progress,
     startedAt: job.startedAt,
     completedAt: job.completedAt,
     error: job.error,
     chunksCount: job.chunksCount,
     chunksProcessed: job.chunksProcessed,
     chunksTotal: job.chunksTotal,
   });
   ```

3. En `app/src/app/api/projects/[id]/rag/create/route.ts`:
   Actualizar el polling que lee el status file para pasar chunksProcessed/chunksTotal a ragJobs:
   ```typescript
   if (data.status === 'running') {
     ragJobs.updateProgress(projectId, data.progress || 'Procesando...', data.chunksProcessed, data.chunksTotal);
   }
   ```
  </action>
  <verify>
    <automated>grep "chunksProcessed" app/src/lib/services/rag-jobs.ts app/src/app/api/projects/[id]/rag/status/route.ts app/src/app/api/projects/[id]/rag/create/route.ts | wc -l</automated>
  </verify>
  <done>RagJob interface tiene chunksProcessed/chunksTotal, updateProgress los acepta, create/route.ts los pasa del status file, y status/route.ts los devuelve en la respuesta.</done>
</task>

<task type="auto">
  <name>Task 3: Barra de progreso visual en rag-panel.tsx</name>
  <files>app/src/components/rag/rag-panel.tsx</files>
  <action>
En `app/src/components/rag/rag-panel.tsx`:

1. Añadir estado para progreso estructurado:
   ```typescript
   const [chunksProcessed, setChunksProcessed] = useState(0);
   const [chunksTotal, setChunksTotal] = useState(0);
   ```

2. En el callback de polling (startPolling), actualizar los estados cuando hay datos:
   ```typescript
   if (data.status === 'running') {
     setProgressMsg(data.progress || 'Indexando...');
     if (data.chunksProcessed !== undefined) setChunksProcessed(data.chunksProcessed);
     if (data.chunksTotal !== undefined) setChunksTotal(data.chunksTotal);
   } else if (data.status === 'completed') {
     // ... existente, añadir reset:
     setChunksProcessed(0);
     setChunksTotal(0);
   }
   ```

3. Resetear en los handlers de error e idle también:
   ```typescript
   } else if (data.status === 'error') {
     // ... existente, añadir:
     setChunksProcessed(0);
     setChunksTotal(0);
   } else if (data.status === 'idle') {
     // ... existente, añadir:
     setChunksProcessed(0);
     setChunksTotal(0);
   }
   ```

4. Añadir barra de progreso visual DENTRO del bloque `{isIndexing && (...)}`, ANTES del log de indexación existente. Mostrar solo cuando chunksTotal > 0:
   ```tsx
   {isIndexing && (
     <div className="bg-zinc-950 border border-zinc-800 rounded-lg p-4 space-y-3">
       {/* Barra de progreso */}
       {chunksTotal > 0 && (
         <div className="space-y-2">
           <div className="flex items-center justify-between text-sm">
             <span className="text-zinc-300 font-medium">
               {chunksProcessed}/{chunksTotal} chunks
             </span>
             <span className="text-violet-400 font-mono">
               {Math.round((chunksProcessed / chunksTotal) * 100)}%
             </span>
           </div>
           <div className="w-full h-2 bg-zinc-800 rounded-full overflow-hidden">
             <div
               className="h-full bg-violet-500 rounded-full transition-all duration-500"
               style={{ width: `${Math.round((chunksProcessed / chunksTotal) * 100)}%` }}
             />
           </div>
         </div>
       )}

       {/* Paso actual y tiempo */}
       <div className="flex items-center justify-between">
         <p className="text-sm text-violet-400 animate-pulse">{progressMsg}</p>
         <p className="text-xs text-zinc-500 font-mono">
           {Math.floor(ragElapsed / 60)}:{(ragElapsed % 60).toString().padStart(2, '0')}
         </p>
       </div>

       {/* Log de indexación */}
       <div className="space-y-1 text-xs font-mono text-zinc-400 max-h-32 overflow-y-auto">
         {getRagLogs().map((log, i) => (
           <p key={i} className={log.done ? 'text-emerald-400' : 'text-violet-400 animate-pulse'}>
             {log.done ? '✓' : '↻'} {log.text}
           </p>
         ))}
       </div>
     </div>
   )}
   ```

5. Reemplazar el bloque existente `{isIndexing && (...)}` que está justo después del botón "Indexar documentos" (alrededor de línea 396-410) con el nuevo bloque de arriba. El nuevo bloque combina la barra de progreso + el log existente en una sola card.

6. El toast de completado ya existe y dice `Indexación completada: ${data.chunksCount} chunks`. Verificar que funciona — si `chunksCount` viene del endpoint, está bien. Si no, asegurar que `data.chunksCount` esté disponible.

7. Todos los textos en español.
  </action>
  <verify>
    <automated>grep -c "chunksProcessed" app/src/components/rag/rag-panel.tsx; grep -c "chunksTotal" app/src/components/rag/rag-panel.tsx; grep "bg-violet-500 rounded-full" app/src/components/rag/rag-panel.tsx</automated>
  </verify>
  <done>rag-panel.tsx muestra barra de progreso visual con porcentaje, paso actual descriptivo, y tiempo transcurrido durante indexación RAG.</done>
</task>

<task type="auto">
  <name>Task 4: Verificar build completo</name>
  <files>app/src/components/rag/rag-panel.tsx, app/src/lib/services/rag-jobs.ts, app/src/app/api/projects/[id]/rag/status/route.ts, app/src/app/api/projects/[id]/rag/create/route.ts</files>
  <action>
1. Ejecutar `cd /home/deskmath/docflow/app && npm run build` y verificar que pasa sin errores.
2. Si hay errores de build, corregirlos (errores típicos: tipos faltantes, imports incorrectos).
3. Verificar que rag-panel.tsx no tiene imports sin usar ni variables sin usar.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build pasa exitosamente con todos los cambios de Phase 2.</done>
</task>

</tasks>

<verification>
1. `grep "chunksProcessed" app/scripts/rag-worker.mjs` — worker escribe datos estructurados
2. `grep "chunksProcessed" app/src/lib/services/rag-jobs.ts` — job tracker tiene campos
3. `grep "chunksProcessed" app/src/app/api/projects/[id]/rag/status/route.ts` — endpoint los devuelve
4. `grep "chunksProcessed" app/src/app/api/projects/[id]/rag/create/route.ts` — create route los pasa
5. `grep "chunksProcessed" app/src/components/rag/rag-panel.tsx` — UI los usa para barra
6. `cd app && npm run build` — build exitoso
</verification>

<success_criteria>
1. Worker escribe chunksProcessed/chunksTotal en archivo de estado
2. Job tracker almacena y status endpoint devuelve chunksProcessed/chunksTotal
3. rag-panel muestra barra de progreso con porcentaje (ej: "15/44 — 34%")
4. Paso actual descriptivo se actualiza en tiempo real
5. Tiempo transcurrido visible
6. Toast al completar con N vectores
7. npm run build pasa sin errores
8. Todos textos en español
</success_criteria>

<output>
After completion, create `.planning/phases/02-real-time-rag-indexing-progress/02-01-SUMMARY.md`
</output>
