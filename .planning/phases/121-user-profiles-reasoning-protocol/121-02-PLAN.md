---
phase: 121-user-profiles-reasoning-protocol
plan: 02
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - app/src/app/api/catbot/chat/route.ts
  - app/src/lib/services/telegram-bot.ts
autonomous: true
requirements: [PROFILE-01, PROFILE-04, PROFILE-05]

must_haves:
  truths:
    - "La primera peticion a /api/catbot/chat crea automaticamente un user_profile en catbot.db"
    - "El user_id en Telegram es 'telegram:{chat_id}' y en web es 'web:default'"
    - "El perfil del usuario se inyecta en el system prompt via PromptAssembler.build()"
    - "Al final de cada conversacion con tool calls, el perfil se actualiza automaticamente si hay preferencias nuevas"
    - "El Telegram bot envia user_id en el body de la peticion a /api/catbot/chat"
  artifacts:
    - path: "app/src/app/api/catbot/chat/route.ts"
      provides: "Pre-flight profile load + post-conversation update"
      contains: "ensureProfile"
    - path: "app/src/lib/services/telegram-bot.ts"
      provides: "user_id field en request body"
      contains: "user_id"
  key_links:
    - from: "app/src/app/api/catbot/chat/route.ts"
      to: "app/src/lib/services/catbot-user-profile.ts"
      via: "import { deriveUserId, ensureProfile, updateProfileAfterConversation }"
      pattern: "ensureProfile|updateProfileAfterConversation"
    - from: "app/src/app/api/catbot/chat/route.ts"
      to: "app/src/lib/services/catbot-prompt-assembler.ts"
      via: "buildPrompt({...userProfile})"
      pattern: "userProfile"
    - from: "app/src/lib/services/telegram-bot.ts"
      to: "app/src/app/api/catbot/chat/route.ts"
      via: "user_id field in POST body"
      pattern: "user_id"
---

<objective>
Integrar UserProfileService en route.ts (pre-flight profile load + post-conversation update) y corregir Telegram bot para enviar user_id.

Purpose: Conecta la logica de perfiles (Plan 01) con el flujo real de chat, completando el ciclo de auto-creacion y actualizacion de perfiles.
Output: route.ts carga/crea perfil antes de cada conversacion, lo pasa al PromptAssembler, y actualiza preferencias despues. Telegram envia chat_id.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-user-profiles-reasoning-protocol/121-RESEARCH.md
@.planning/phases/121-user-profiles-reasoning-protocol/121-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From app/src/lib/services/catbot-user-profile.ts (created in Plan 01):
```typescript
export function deriveUserId(channel?: string, chatId?: string): string;
export function ensureProfile(userId: string, channel?: string): ProfileRow;
export function extractPreferencesFromTools(toolResults: Array<{name: string; args: Record<string, unknown>; result: unknown}>): ProfileUpdate | null;
export function generateInitialDirectives(profile: ProfileRow): string;
export function updateProfileAfterConversation(userId: string, toolResults: Array<{name: string; args: Record<string, unknown>; result: unknown}>): void;
```

From app/src/lib/services/catbot-prompt-assembler.ts (extended in Plan 01):
```typescript
export interface PromptContext {
  page?: string;
  channel?: 'web' | 'telegram';
  hasSudo: boolean;
  catbotConfig: { /* existing */ };
  userProfile?: {
    display_name: string | null;
    initial_directives: string | null;
    known_context: string;
    communication_style: string | null;
    preferred_format: string | null;
  };
}
```

From app/src/app/api/catbot/chat/route.ts (current):
```typescript
// Line ~48: body destructuring includes channel?: 'web' | 'telegram'
// Line ~81-87: buildPrompt({page, channel, hasSudo, catbotConfig})
// Line ~111,246: allToolResults array accumulated during tool calls
// Line ~224,389: post-conversation logging (both streaming and non-streaming paths)
```

From app/src/lib/services/telegram-bot.ts (current):
```typescript
// Line ~504: body: JSON.stringify({
//   messages: [...], context: { page: 'telegram', channel: 'telegram' },
//   channel: 'telegram', sudo_active: sudoActive, stream: false,
// })
// NOTE: Missing user_id / chat_id field
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: route.ts — pre-flight profile load + PromptAssembler wiring</name>
  <files>app/src/app/api/catbot/chat/route.ts</files>
  <action>
    Modificar route.ts para integrar el servicio de perfiles:
    
    1. Agregar imports al inicio:
    ```typescript
    import { deriveUserId, ensureProfile, updateProfileAfterConversation } from '@/lib/services/catbot-user-profile';
    ```
    
    2. Agregar `user_id` al destructuring del body (linea ~48):
    ```typescript
    const { ..., user_id: bodyUserId } = body as {
      // ... existing fields
      user_id?: string; // NEW: Telegram sends 'telegram:{chatId}'
    };
    ```
    
    3. Despues de la linea `const effectiveChannel = channel || context?.channel;` (linea ~81), agregar pre-flight profile:
    ```typescript
    // Pre-flight: load or create user profile
    const userId = bodyUserId || deriveUserId(effectiveChannel as string | undefined, undefined);
    const profile = ensureProfile(userId, effectiveChannel as string | undefined);
    ```
    
    4. Extender la llamada a buildPrompt (linea ~82-87) para incluir userProfile:
    ```typescript
    const systemPrompt = buildPrompt({
      page: context?.page,
      channel: effectiveChannel as 'web' | 'telegram' | undefined,
      hasSudo: !!sudoActive,
      catbotConfig,
      userProfile: {
        display_name: profile.display_name,
        initial_directives: profile.initial_directives,
        known_context: profile.known_context,
        communication_style: profile.communication_style,
        preferred_format: profile.preferred_format,
      },
    });
    ```
    
    5. Despues del logging post-conversacion (ambos paths: streaming ~linea 224 y non-streaming ~linea 389), agregar:
    ```typescript
    // Post-conversation: update profile from tool patterns
    try {
      if (allToolResults.length > 0) {
        updateProfileAfterConversation(userId, allToolResults);
      }
    } catch (e) {
      logger.warn('catbot', 'Failed to update profile after conversation', { error: (e as Error).message });
    }
    ```
    
    IMPORTANTE: Agregar el post-conversation update en AMBOS paths (streaming y non-streaming). El streaming path tiene el bloque post-conversacion alrededor de linea 224, el non-streaming alrededor de linea 389. En ambos casos, insertar justo despues del logger.info('catbot', 'Respuesta ... generada').
    
    PITFALL-4: No llamar upsertProfile para incrementar interaction_count en pre-flight. ensureProfile solo crea si no existe. El increment ocurre solo en post-conversation update via updateProfileAfterConversation.
    PITFALL-5: Pasar userId correcto al guardar conversacion si hay save calls.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>route.ts compila sin errores. Pre-flight crea perfil, lo pasa a buildPrompt, post-conversation actualiza preferencias en ambos paths (streaming + non-streaming).</done>
</task>

<task type="auto">
  <name>Task 2: Telegram bot — enviar user_id en request body</name>
  <files>app/src/lib/services/telegram-bot.ts</files>
  <action>
    Modificar `handleCatBotMessage()` en telegram-bot.ts (linea ~504) para incluir `user_id` en el body de la peticion a /api/catbot/chat:
    
    ```typescript
    body: JSON.stringify({
      messages: [{ role: 'user', content: text }],
      context: { page: 'telegram', channel: 'telegram' },
      channel: 'telegram',
      sudo_active: sudoActive,
      stream: false,
      user_id: `telegram:${chatId}`,  // NEW: PROFILE-05 consistent user_id
    }),
    ```
    
    PITFALL-1: chatId ya es parametro de handleCatBotMessage(chatId: number, text: string). Solo necesita interpolarlo como string.
    
    Esto resuelve PROFILE-05 para Telegram y evita que todos los usuarios Telegram compartan el mismo perfil (PITFALL: "telegram:unknown" fallback).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Telegram bot envia user_id: "telegram:{chatId}" en cada peticion a /api/catbot/chat. TypeScript compila sin errores.</done>
</task>

</tasks>

<verification>
cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-user-profile.test.ts src/lib/__tests__/catbot-prompt-assembler.test.ts --reporter=verbose && npx tsc --noEmit --pretty 2>&1 | head -30
</verification>

<success_criteria>
- route.ts crea perfil automaticamente en pre-flight (PROFILE-01)
- route.ts pasa userProfile a buildPrompt (PROFILE-03 via PromptAssembler)
- route.ts actualiza perfil post-conversacion en ambos paths streaming/non-streaming (PROFILE-04)
- Telegram bot envia user_id: 'telegram:{chatId}' (PROFILE-05)
- TypeScript compila sin errores
- Tests existentes siguen verdes
</success_criteria>

<output>
After completion, create `.planning/phases/121-user-profiles-reasoning-protocol/121-02-SUMMARY.md`
</output>
