---
phase: 121-user-profiles-reasoning-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/services/catbot-user-profile.ts
  - app/src/lib/__tests__/catbot-user-profile.test.ts
  - app/src/lib/services/catbot-prompt-assembler.ts
  - app/src/lib/__tests__/catbot-prompt-assembler.test.ts
autonomous: true
requirements: [PROFILE-01, PROFILE-02, PROFILE-03, PROFILE-04, PROFILE-05, REASON-01, REASON-02, REASON-03, REASON-04, REASON-05]

must_haves:
  truths:
    - "UserProfileService.ensureProfile() crea un perfil nuevo si no existe, o retorna el existente"
    - "UserProfileService.deriveUserId() devuelve 'web:default' para web y 'telegram:{chat_id}' para Telegram"
    - "generateInitialDirectives() genera un parrafo descriptivo a partir de los campos del perfil"
    - "extractPreferencesFromTools() extrae preferencias de un array de tool results sin LLM call"
    - "PromptAssembler inyecta seccion de perfil de usuario a prioridad P1 con cap de 500 chars"
    - "PromptAssembler inyecta protocolo de razonamiento a prioridad P1 con niveles simple/medio/complejo"
    - "El protocolo de razonamiento incluye la condicion de skip por Capa 0 (recipe)"
  artifacts:
    - path: "app/src/lib/services/catbot-user-profile.ts"
      provides: "UserProfileService: ensureProfile, deriveUserId, extractPreferencesFromTools, generateInitialDirectives, updateProfileAfterConversation"
      min_lines: 80
    - path: "app/src/lib/__tests__/catbot-user-profile.test.ts"
      provides: "Tests para auto-create, userId format, preference extraction, directive generation"
      min_lines: 60
    - path: "app/src/lib/services/catbot-prompt-assembler.ts"
      provides: "buildUserProfileSection + buildReasoningProtocol sections en build()"
      contains: "buildUserProfileSection"
    - path: "app/src/lib/__tests__/catbot-prompt-assembler.test.ts"
      provides: "Tests para profile section y reasoning protocol en prompt assembly"
      contains: "reasoning"
  key_links:
    - from: "app/src/lib/services/catbot-user-profile.ts"
      to: "app/src/lib/catbot-db.ts"
      via: "import { upsertProfile, getProfile } from catbot-db"
      pattern: "upsertProfile|getProfile"
    - from: "app/src/lib/services/catbot-prompt-assembler.ts"
      to: "PromptContext.userProfile"
      via: "buildUserProfileSection reads ctx.userProfile"
      pattern: "userProfile"
---

<objective>
Crear UserProfileService y extender PromptAssembler con seccion de perfil de usuario y protocolo de razonamiento adaptativo.

Purpose: Establece toda la logica de negocio (servicio de perfiles + prompt sections) que luego se wirea en route.ts y Telegram en Plan 02.
Output: UserProfileService con tests, PromptAssembler extendido con profile section + reasoning protocol.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-user-profiles-reasoning-protocol/121-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/src/lib/catbot-db.ts:
```typescript
export interface ProfileRow {
  id: string;
  display_name: string | null;
  channel: string;
  personality_notes: string | null;
  communication_style: string | null;
  preferred_format: string | null;
  known_context: string; // JSON
  initial_directives: string | null;
  interaction_count: number;
  last_seen: string | null;
  created_at: string;
  updated_at: string;
}

export function upsertProfile(profile: {
  id: string;
  displayName?: string;
  channel?: string;
  personalityNotes?: string;
  communicationStyle?: string;
  preferredFormat?: string;
  knownContext?: Record<string, unknown>;
  initialDirectives?: string;
}): void;

export function getProfile(id: string): ProfileRow | undefined;
```

From app/src/lib/services/catbot-prompt-assembler.ts:
```typescript
export interface PromptSection {
  id: string;
  priority: 0 | 1 | 2 | 3;
  content: string;
}

export interface PromptContext {
  page?: string;
  channel?: 'web' | 'telegram';
  hasSudo: boolean;
  catbotConfig: {
    model?: string;
    personality?: string;
    allowed_actions?: string[];
    instructions_primary?: string;
    instructions_secondary?: string;
    personality_custom?: string;
  };
  stats?: { catbrainsCount: number; catpawsCount: number; tasksCount: number; listeningCount: number };
}

export function build(ctx: PromptContext): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UserProfileService con tests</name>
  <files>app/src/lib/services/catbot-user-profile.ts, app/src/lib/__tests__/catbot-user-profile.test.ts</files>
  <behavior>
    - deriveUserId('web', undefined) returns 'web:default'
    - deriveUserId('telegram', '12345') returns 'telegram:12345'
    - deriveUserId('telegram', undefined) returns 'telegram:unknown'
    - deriveUserId(undefined, undefined) returns 'web:default'
    - ensureProfile(userId) creates new profile if none exists, returns ProfileRow
    - ensureProfile(userId) returns existing profile without re-creating if it exists
    - ensureProfile does NOT increment interaction_count (solo crea si no existe, no actualiza)
    - extractPreferencesFromTools([{name:'bash_execute',...}]) returns {communicationStyle:'technical'}
    - extractPreferencesFromTools([{name:'canvas_create',...},{name:'canvas_update',...}]) includes {knownContext:{uses_canvas_frequently:true}}
    - extractPreferencesFromTools([{name:'send_email', args:{connector_id:'gmail'}, ...}]) includes preferred_email_connector
    - extractPreferencesFromTools([]) returns null
    - generateInitialDirectives(profile with display_name + channel web) includes nombre and web
    - generateInitialDirectives(profile with channel telegram) mentions Telegram + respuestas concisas
    - generateInitialDirectives(profile with communication_style) mentions estilo
    - updateProfileAfterConversation(userId, toolResults) calls upsertProfile only if preferences detected, regenerates directives
  </behavior>
  <action>
    Primero crear el test file `catbot-user-profile.test.ts` con todos los tests arriba (RED).
    
    Luego crear `catbot-user-profile.ts` con:
    
    1. `deriveUserId(channel?: string, chatId?: string): string` — Formato consistente PROFILE-05.
    
    2. `ensureProfile(userId: string, channel?: string): ProfileRow` — Llama getProfile(userId). Si no existe, llama upsertProfile({id: userId, channel: channel || 'web'}) y retorna getProfile(userId)!. NO incrementa interaction_count (evitar Pitfall 4: double-count). Para evitar el doble increment, NO llamar upsertProfile si el profile ya existe.
    
    3. `extractPreferencesFromTools(toolResults: Array<{name: string; args: Record<string, unknown>; result: unknown}>): ProfileUpdate | null` — Analiza tool names para extraer preferencias:
       - bash_execute, service_manage → communicationStyle: 'technical'
       - 2+ canvas_* tools → knownContext.uses_canvas_frequently = true
       - send_email con connector_id → knownContext.preferred_email_connector
       - explain_feature frecuente → communicationStyle: 'learning'
       Retorna null si no hay updates. ANTI-PATTERN-3: zero-cost, sin LLM call.
    
    4. `generateInitialDirectives(profile: ProfileRow): string` — Genera parrafo descriptivo desde campos del perfil (PROFILE-03). Incluye display_name, channel, communication_style, preferred_format, known_context. Max 500 chars.
    
    5. `updateProfileAfterConversation(userId: string, toolResults: Array<...>): void` — Orquesta: extractPreferencesFromTools → si hay updates, upsertProfile con las preferencias + regenera initial_directives con generateInitialDirectives.

    IMPORTANTE: Los tests deben mockear catbot-db (vi.mock) para no necesitar SQLite real en tests.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-user-profile.test.ts --reporter=verbose</automated>
  </verify>
  <done>UserProfileService exporta 5 funciones, todos los tests pasan, zero LLM calls en extraction</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PromptAssembler — profile section + reasoning protocol</name>
  <files>app/src/lib/services/catbot-prompt-assembler.ts, app/src/lib/__tests__/catbot-prompt-assembler.test.ts</files>
  <behavior>
    - build() con userProfile inyecta seccion 'user_profile' a priority 1
    - buildUserProfileSection con initial_directives incluye texto de directivas
    - buildUserProfileSection con known_context JSON incluye contexto
    - buildUserProfileSection cap 500 chars para directives + 500 para known_context
    - buildUserProfileSection sin userProfile retorna string vacio
    - build() siempre inyecta seccion 'reasoning_protocol' a priority 1
    - reasoning protocol contiene "Nivel SIMPLE" con detectores listar/consultar
    - reasoning protocol contiene "Nivel MEDIO" con detectores crear/modificar
    - reasoning protocol contiene "Nivel COMPLEJO" con detectores disenar/arquitectura
    - reasoning protocol contiene "Capa 0" con instruccion de skip por recipe
    - reasoning protocol NO contiene instrucciones de anunciar clasificacion al usuario
  </behavior>
  <action>
    Primero extender los tests en `catbot-prompt-assembler.test.ts` con describe blocks nuevos para "profile section" y "reasoning protocol" (RED).
    
    Luego modificar `catbot-prompt-assembler.ts`:
    
    1. Extender PromptContext con campo opcional `userProfile`:
    ```typescript
    userProfile?: {
      display_name: string | null;
      initial_directives: string | null;
      known_context: string; // JSON
      communication_style: string | null;
      preferred_format: string | null;
    };
    ```
    
    2. Crear `buildUserProfileSection(ctx: PromptContext): string` — Si ctx.userProfile existe, genera seccion con:
       - "## Directivas del usuario\n{initial_directives}" (cap 500 chars)
       - "## Contexto conocido del usuario\n{entries de known_context}" (cap 500 chars)
       - "Estilo de comunicacion preferido: {communication_style}" si existe
       - Retorna '' si no hay userProfile o todo vacio
    
    3. Crear `buildReasoningProtocol(): string` — Retorna el texto del protocolo de razonamiento adaptativo con:
       - Nivel SIMPLE (detectores: listar, consultar, mostrar, navegar, explicar)
       - Nivel MEDIO (detectores: crear, modificar, configurar, cambiar, actualizar, enviar)
       - Nivel COMPLEJO (detectores: disenar pipeline, arquitectura multi-agente, resolver problema complejo, migrar, optimizar, diagnosticar)
       - Capa 0 Fast Path: si hay recipe memorizada, ejecutar directamente
       - Reglas generales: default a accion, max 1 pregunta medio, nunca anunciar clasificacion
       Usar exactamente el texto del RESEARCH.md Pattern 3.
    
    4. En build(), insertar las nuevas secciones:
       - `{ id: 'user_profile', priority: 1, content: buildUserProfileSection(ctx) }` — despues de instructions_primary
       - `{ id: 'reasoning_protocol', priority: 1, content: buildReasoningProtocol() }` — despues de user_profile
       Ambas con try/catch graceful como las demas secciones P1.
    
    PITFALL-3: Cap directives a 500 chars + known_context a 500 chars para no comer budget.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts --reporter=verbose</automated>
  </verify>
  <done>PromptAssembler.build() con userProfile genera seccion de perfil P1, siempre genera reasoning protocol P1 con 3 niveles + Capa 0 skip. Tests existentes siguen verdes.</done>
</task>

</tasks>

<verification>
cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-user-profile.test.ts src/lib/__tests__/catbot-prompt-assembler.test.ts --reporter=verbose
</verification>

<success_criteria>
- UserProfileService exporta deriveUserId, ensureProfile, extractPreferencesFromTools, generateInitialDirectives, updateProfileAfterConversation
- PromptContext tiene campo userProfile opcional
- build() genera seccion user_profile P1 y reasoning_protocol P1
- Reasoning protocol tiene 3 niveles + Capa 0 + reglas generales
- Todos los tests pasan (nuevos + existentes)
- Zero LLM calls en profile extraction (tool patterns only)
</success_criteria>

<output>
After completion, create `.planning/phases/121-user-profiles-reasoning-protocol/121-01-SUMMARY.md`
</output>
