---
phase: 121-user-profiles-reasoning-protocol
plan: 03
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - app/src/lib/services/catbot-tools.ts
  - app/data/knowledge/settings.json
autonomous: false
requirements: [PROFILE-02, PROFILE-03]

must_haves:
  truths:
    - "CatBot puede consultar su propio perfil de usuario activo con un tool call"
    - "CatBot puede actualizar el display_name del usuario via tool call"
    - "El knowledge tree de settings documenta la funcionalidad de user profiles"
  artifacts:
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "Tool get_user_profile y update_user_profile"
      contains: "get_user_profile"
    - path: "app/data/knowledge/settings.json"
      provides: "Documentacion de user profiles en knowledge tree"
      contains: "user_profile"
  key_links:
    - from: "app/src/lib/services/catbot-tools.ts"
      to: "app/src/lib/catbot-db.ts"
      via: "getProfile, upsertProfile"
      pattern: "getProfile|upsertProfile"
---

<objective>
Agregar tools de CatBot para consultar y actualizar perfiles de usuario, y documentar la feature en el knowledge tree.

Purpose: Per CLAUDE.md, toda feature nueva debe ser verificable por CatBot. Los tools permiten que CatBot consulte y gestione perfiles, y el knowledge tree le ensenya sobre la feature.
Output: 2 CatBot tools (get_user_profile, update_user_profile) + knowledge tree actualizado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-user-profiles-reasoning-protocol/121-RESEARCH.md
@.planning/phases/121-user-profiles-reasoning-protocol/121-01-SUMMARY.md

<interfaces>
From app/src/lib/catbot-db.ts:
```typescript
export function getProfile(id: string): ProfileRow | undefined;
export function upsertProfile(profile: { id: string; displayName?: string; channel?: string; communicationStyle?: string; preferredFormat?: string; knownContext?: Record<string, unknown>; initialDirectives?: string; }): void;
```

From app/src/lib/services/catbot-tools.ts (pattern for adding tools):
```typescript
// Tools are registered as objects with:
// { type: 'function', function: { name, description, parameters } }
// Execution is handled in executeTool() switch statement
// Permission categories: always_allowed, permission-gated, sudo-required
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CatBot tools — get_user_profile + update_user_profile</name>
  <files>app/src/lib/services/catbot-tools.ts</files>
  <action>
    Agregar 2 tools nuevos a catbot-tools.ts:
    
    1. **get_user_profile** (always_allowed):
       - description: "Consulta el perfil del usuario activo. Muestra nombre, canal, estilo de comunicacion, preferencias y directivas."
       - parameters: { user_id: { type: 'string', description: 'ID del usuario (ej: web:default, telegram:12345). Si no se proporciona, usa web:default' } }
       - user_id es optional con default 'web:default'
       - Execution: llama getProfile(user_id), retorna el profile formateado legiblemente o "No se encontro perfil para {user_id}"
       - Categoria: always_allowed (consultar el propio perfil no requiere permisos especiales)
    
    2. **update_user_profile** (permission-gated):
       - description: "Actualiza el perfil del usuario. Puede cambiar nombre, estilo de comunicacion o formato preferido."
       - parameters: { user_id: { type: 'string' }, display_name?: string, communication_style?: string, preferred_format?: string }
       - Execution: llama upsertProfile con los campos proporcionados
       - Despues de actualizar, regenera initial_directives llamando generateInitialDirectives del UserProfileService y hace otro upsertProfile con las nuevas directives
       - Categoria: permission-gated (modificar perfil necesita permiso explicito)
    
    Import necesario:
    ```typescript
    import { getProfile, upsertProfile } from '@/lib/catbot-db';
    import { generateInitialDirectives } from '@/lib/services/catbot-user-profile';
    ```
    
    Seguir el patron exacto de tools existentes para registrar en la lista de tools y en el switch de executeTool().
    Agregar los nombres a las listas de permisos correspondientes (always_allowed para get, NORMAL_PERMISSION_TOOLS para update).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>get_user_profile y update_user_profile registrados en catbot-tools, compila sin errores</done>
</task>

<task type="auto">
  <name>Task 2: Knowledge tree — documentar user profiles</name>
  <files>app/data/knowledge/settings.json</files>
  <action>
    Leer settings.json actual y agregar una seccion sobre User Profiles en el area apropiada. Agregar en el array de `concepts` o crear subseccion:
    
    - Concepto: "user_profiles" — Perfiles de usuario auto-creados por canal (web/Telegram). CatBot recuerda preferencias, estilo de comunicacion, y contexto conocido.
    - Tools: get_user_profile (consultar perfil), update_user_profile (modificar perfil)
    - howto: "Para consultar tu perfil: 'muestrame mi perfil'. Para cambiar tu nombre: 'cambiar mi nombre a X'. Para ver perfil de Telegram: especificar user_id telegram:{chat_id}"
    - dont: "No revelar perfil de un usuario a otro sin sudo activo"
    
    Tambien agregar en `tools` array las 2 tools nuevas con sus descripciones.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && node -e "const j=require('../data/knowledge/settings.json'); const s=JSON.stringify(j); if(!s.includes('user_profile')) {process.exit(1)} else {console.log('OK: settings.json includes user_profile')}"</automated>
  </verify>
  <done>settings.json documenta user profiles con tools, howto y restricciones</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacion humana — perfiles + razonamiento + CatBot tools</name>
  <files>N/A</files>
  <action>
    Verificacion manual end-to-end de toda la fase 121. No hay codigo que escribir — solo verificar que todo funciona.
  </action>
  <verify>
    Verificacion manual:
    1. Abrir CatBot en la web y enviar cualquier mensaje. Verificar que se creo un perfil en catbot.db:
       - En terminal: `sqlite3 /home/deskmath/docflow/app/data/catbot.db "SELECT * FROM user_profiles WHERE id='web:default'"`
       - Debe mostrar un registro con channel='web'
    
    2. Preguntar a CatBot: "muestrame mi perfil" — debe usar get_user_profile y mostrar los datos
    
    3. Pedir a CatBot: "cambia mi nombre a Test User" — debe usar update_user_profile
    
    4. Verificar que el prompt incluye razonamiento adaptativo:
       - Pedir algo simple: "lista mis catbrains" — debe ejecutar directamente sin preguntas
       - Pedir algo medio: "crea un agente nuevo" — debe proponer configuracion y confirmar
    
    5. Enviar un mensaje desde Telegram y verificar perfil telegram:
       - `sqlite3 /home/deskmath/docflow/app/data/catbot.db "SELECT * FROM user_profiles WHERE id LIKE 'telegram:%'"`
  </verify>
  <done>Usuario confirma que perfiles se crean, tools funcionan, y razonamiento adaptativo se observa en comportamiento de CatBot</done>
</task>

</tasks>

<verification>
cd /home/deskmath/docflow/app && npx vitest run --reporter=verbose && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- CatBot tiene tools get_user_profile y update_user_profile funcionales
- Knowledge tree de settings documenta user profiles
- Verificacion humana confirma: auto-creacion de perfiles, tools de CatBot, razonamiento adaptativo, perfiles Telegram
</success_criteria>

<output>
After completion, create `.planning/phases/121-user-profiles-reasoning-protocol/121-03-SUMMARY.md`
</output>
