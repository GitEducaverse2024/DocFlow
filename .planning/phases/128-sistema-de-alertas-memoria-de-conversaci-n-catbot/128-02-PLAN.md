---
phase: 128-sistema-de-alertas-memoria-de-conversacion-catbot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/services/catbot-conversation-memory.ts
  - app/src/lib/__tests__/catbot-conversation-memory.test.ts
  - app/src/app/api/catbot/chat/route.ts
autonomous: true
requirements:
  - CONVMEM-01
  - CONVMEM-02

must_haves:
  truths:
    - "CatBot en web envia al LLM los 10 mensajes recientes completos + resumen compactado de hasta 30 mensajes anteriores"
    - "Si hay <= 10 mensajes, se envian todos sin compactacion"
    - "Cuando el usuario introduce sudo, el contexto previo de la conversacion se preserva intacto"
    - "La compactacion usa LLM (ollama/gemma3:12b) para preservar semantica, no truncacion"
  artifacts:
    - path: "app/src/lib/services/catbot-conversation-memory.ts"
      provides: "buildConversationWindow() + compactMessages()"
      exports: ["buildConversationWindow", "compactMessages"]
    - path: "app/src/lib/__tests__/catbot-conversation-memory.test.ts"
      provides: "Tests unitarios para conversation memory"
  key_links:
    - from: "app/src/app/api/catbot/chat/route.ts"
      to: "catbot-conversation-memory.ts"
      via: "buildConversationWindow(userMessages)"
      pattern: "buildConversationWindow"
    - from: "catbot-conversation-memory.ts"
      to: "LiteLLM /v1/chat/completions"
      via: "compactMessages fetch call"
      pattern: "ollama/gemma3:12b"
---

<objective>
Implementar el sistema de memoria de conversacion para CatBot web: ventana de 10 mensajes recientes completos + compactacion LLM de hasta 30 mensajes anteriores. Integrar en route.ts para que sudo no pierda contexto.

Purpose: CatBot actualmente envia TODOS los mensajes al LLM, lo que explota tokens en conversaciones largas. Con windowing inteligente, las conversaciones largas son sostenibles y el contexto se preserva semanticamente.
Output: catbot-conversation-memory.ts con buildConversationWindow(), tests, integracion en route.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-sistema-de-alertas-memoria-de-conversaci-n-catbot/128-RESEARCH.md

@app/src/app/api/catbot/chat/route.ts (linea 127-129: llmMessages = system + userMessages)
@app/src/lib/services/catbot-summary.ts (patron LLM compaction con ollama/gemma3:12b)

<interfaces>
<!-- Current message flow in route.ts (line 127-129) -->
```typescript
// BEFORE (sends ALL messages):
const llmMessages: ChatMessage[] = [
  { role: 'system', content: systemPrompt },
  ...userMessages,  // <-- ALL messages from frontend
];

// AFTER (windowed):
const windowedMessages = await buildConversationWindow(userMessages);
const llmMessages: ChatMessage[] = [
  { role: 'system', content: systemPrompt },
  ...windowedMessages,
];
```

<!-- ChatMessage type used throughout -->
```typescript
interface ChatMessage {
  role: 'system' | 'user' | 'assistant' | 'tool';
  content: string;
  tool_calls?: any[];
  tool_call_id?: string;
}
```

<!-- LLM compaction pattern from catbot-summary.ts -->
```typescript
const litellmUrl = process['env']['LITELLM_URL'] || 'http://localhost:4000';
const res = await fetch(`${litellmUrl}/v1/chat/completions`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer sk-antigravity-gateway' },
  body: JSON.stringify({
    model: 'ollama/gemma3:12b',
    messages: [...],
    max_tokens: 512,
    temperature: 0.3,
  }),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildConversationWindow + compactMessages con tests</name>
  <files>app/src/lib/services/catbot-conversation-memory.ts, app/src/lib/__tests__/catbot-conversation-memory.test.ts</files>
  <behavior>
    - Test: mensajes <= 10 se retornan sin cambio
    - Test: 15 mensajes -> 5 compactados + 10 recientes (compacted como system message al inicio)
    - Test: 40 mensajes -> 30 compactados + 10 recientes
    - Test: 50 mensajes -> solo los 40 mas recientes se consideran (30 compactados + 10 recientes), mensajes >40 se descartan
    - Test: mensajes con role 'tool' y 'assistant' se manejan correctamente en compactacion
    - Test: si compactMessages falla (LLM error), retorna fallback "[No se pudo resumir]" y devuelve solo los 10 recientes
    - Test: compactMessages produce un string con contexto resumido
    - Test: mensajes de sudo (role 'user', content incluye password) se incluyen en el window normalmente (contexto preservado)
    - Test: resultado del windowing tiene system message de contexto previo + mensajes recientes
  </behavior>
  <action>
1. Crear catbot-conversation-memory.ts con dos funciones exportadas:

   a. `compactMessages(messages: ChatMessage[]): Promise<string>`
      - Convierte mensajes a texto: `${role}: ${content}` por linea
      - Trunca input a 4000 chars (cap como en SummaryService)
      - Llama a LiteLLM con ollama/gemma3:12b, temp 0.3, max_tokens 512
      - Prompt de sistema (en espanol): "Resume los siguientes mensajes de conversacion preservando: temas discutidos, decisiones tomadas, herramientas ejecutadas, contexto relevante. Maximo 300 palabras. Solo texto."
      - Si fetch falla o respuesta invalida, retornar '[No se pudo resumir la conversacion previa]'
      - Usar `process['env']['LITELLM_URL']` (bracket notation)

   b. `buildConversationWindow(messages: ChatMessage[], opts?: { recentCount?: number; compactCount?: number }): Promise<ChatMessage[]>`
      - Defaults: recentCount=10, compactCount=30
      - Si messages.length <= recentCount: retornar messages sin cambio
      - Separar: recentMsgs = messages.slice(-recentCount), olderMsgs = messages.slice(max(0, length-recentCount-compactCount), length-recentCount)
      - Si olderMsgs vacio: retornar recentMsgs
      - Compactar olderMsgs via compactMessages()
      - Retornar: [{ role: 'system', content: `[Resumen de conversacion previa (${olderMsgs.length} mensajes)]: ${compacted}` }, ...recentMsgs]

   CACHING: Implementar cache simple en memoria:
   - Variable module-level: `let compactionCache: { key: string; result: string } | null = null`
   - Key = hash de olderMsgs (JSON.stringify + length como proxy rapido)
   - Si key coincide, retornar cached result sin llamar LLM
   - Esto evita re-compactar en cada mensaje cuando los older messages no cambiaron

2. Crear tests con vitest. Mockear fetch para simular LLM responses. Testear todos los escenarios del behavior block.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/catbot-conversation-memory.test.ts -x</automated>
  </verify>
  <done>buildConversationWindow retorna ventana correcta de 10+compact para todos los escenarios, compactMessages usa LLM con fallback, cache evita llamadas duplicadas, tests pasan</done>
</task>

<task type="auto">
  <name>Task 2: Integrar buildConversationWindow en route.ts</name>
  <files>app/src/app/api/catbot/chat/route.ts</files>
  <action>
1. Importar buildConversationWindow de '@/lib/services/catbot-conversation-memory' al inicio de route.ts.

2. En route.ts, linea ~127-129, cambiar:
   ```typescript
   // ANTES:
   const llmMessages: ChatMessage[] = [
     { role: 'system', content: systemPrompt },
     ...userMessages,
   ];

   // DESPUES:
   const windowedMessages = await buildConversationWindow(userMessages);
   const llmMessages: ChatMessage[] = [
     { role: 'system', content: systemPrompt },
     ...windowedMessages,
   ];
   ```

3. Esto es el UNICO cambio en route.ts. La funcion buildConversationWindow maneja toda la logica de windowing. No modificar nada mas del flujo existente.

4. Verificar que el cambio no rompe el tool-calling loop (lineas ~132+): los tool call results se appenden a llmMessages DESPUES del windowing inicial, lo cual es correcto — el windowing solo afecta los mensajes del historial, no los tool results del turno actual.

5. CONVMEM-02 (sudo preserva contexto): Ya funciona por diseno. El frontend (catbot-panel.tsx) envia TODOS los mensajes en el array messages (incluyendo pre-sudo). buildConversationWindow opera sobre ese array completo. No hay nada que limpie mensajes al hacer sudo — el sudoPromptVisible state es independiente del messages state.
  </action>
  <verify>
    <automated>cd ~/docflow/app && npx vitest run src/lib/__tests__/catbot-conversation-memory.test.ts -x && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>route.ts usa buildConversationWindow para enviar 10 recientes + compactados al LLM, sudo preserva contexto, build sin errores</done>
</task>

</tasks>

<verification>
- `cd ~/docflow/app && npx vitest run src/lib/__tests__/catbot-conversation-memory.test.ts -x` pasa
- `cd ~/docflow/app && npm run build` sin errores
- route.ts importa y usa buildConversationWindow antes de construir llmMessages
</verification>

<success_criteria>
- buildConversationWindow retorna 10 mensajes recientes + resumen compactado de hasta 30 anteriores
- Mensajes <= 10 se envian sin compactacion
- Compactacion usa ollama/gemma3:12b via LiteLLM con fallback si falla
- Cache en memoria evita re-compactar cuando older messages no cambian
- route.ts integrado: userMessages pasan por buildConversationWindow
- Sudo no pierde contexto (array messages incluye todo el hilo)
- Tests unitarios pasan
- Build sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/128-sistema-de-alertas-memoria-de-conversaci-n-catbot/128-02-SUMMARY.md`
</output>
