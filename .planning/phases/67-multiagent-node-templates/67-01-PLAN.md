---
phase: 67-multiagent-node-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/canvas/nodes/multiagent-node.tsx
autonomous: true
requirements: [MA-02]

must_haves:
  truths:
    - "MultiAgentNode renders with purple-600 border and bg-purple-950/80 background"
    - "Input handle on left with purple color (#9333ea)"
    - "output-response handle at 35% (green #16a34a) and output-error handle at 65% (red #dc2626) on right"
    - "Mode badge shows sync/async, target CatFlow name displayed"
    - "Execution status indicators match StorageNode pattern (running/completed/failed/waiting/skipped)"
  artifacts:
    - path: "app/src/components/canvas/nodes/multiagent-node.tsx"
      provides: "MultiAgentNode React component"
      exports: ["MultiAgentNode"]
      min_lines: 80
  key_links: []
---

<objective>
Create the MultiAgentNode canvas component with purple color scheme, dual output handles (output-response green, output-error red), mode badge, and execution status feedback.

Purpose: Visual node for triggering other CatFlows from the canvas editor.
Output: `app/src/components/canvas/nodes/multiagent-node.tsx`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/67-multiagent-node-templates/67-RESEARCH.md

@app/src/components/canvas/nodes/storage-node.tsx
@app/src/components/canvas/nodes/scheduler-node.tsx
</context>

<interfaces>
<!-- Executor needs these patterns from existing nodes -->

From scheduler-node.tsx (multi-handle pattern):
```typescript
// Output handles with unique IDs and positioned at specific %
<Handle type="source" position={Position.Right} id="output-true"
  style={{ top: '35%', background: '#16a34a', width: 10, height: 10 }} />
// Labels positioned absolute next to handles
<span className="absolute text-[9px] text-green-400 font-medium pointer-events-none select-none"
  style={{ right: -12, top: 'calc(35% - 7px)' }}>...</span>
```

From storage-node.tsx (execution status + data casting pattern):
```typescript
const execStatus = (data as Record<string, unknown>).executionStatus as string | undefined;
const isRunning = execStatus === 'running';
// ... borderClass with ternary chain
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create MultiAgentNode component</name>
  <files>app/src/components/canvas/nodes/multiagent-node.tsx</files>
  <action>
Create `app/src/components/canvas/nodes/multiagent-node.tsx` following the StorageNode structure (storage-node.tsx) for the overall layout and the SchedulerNode (scheduler-node.tsx) for multi-handle positioning.

Component structure:
1. `"use client"` directive at top
2. Import `Handle, Position, type NodeProps` from `@xyflow/react`
3. Import `Network, Check, X, Clock, Loader2` from `lucide-react` (Network icon for multi-agent concept)
4. Import `useTranslations` from `next-intl`
5. Export named function `MultiAgentNode({ data, selected }: NodeProps)`

Data typing (cast `data` to typed object):
```typescript
const nodeData = data as {
  label?: string;
  target_task_id?: string;
  target_task_name?: string;
  execution_mode?: 'sync' | 'async';
  payload_template?: string;
  timeout?: number;
};
```

Execution status handling -- identical to StorageNode lines 16-28:
- Extract `executionStatus` from data
- Derive `isRunning`, `isCompleted`, `isFailed`, `isWaiting`, `isSkipped` booleans
- Build `borderClass` with purple theme: `border-purple-600` default, `border-purple-400` selected

Color scheme:
- Background: `bg-purple-950/80`
- Border default: `border-purple-600`, selected: `border-purple-400`
- Icon color: `text-purple-400`
- Label color: `text-purple-100`
- Dimensions: `w-[240px] min-h-[100px]`

Handle layout:
- **Input** (left): `background: '#9333ea'` (purple-600), width 10, height 10
- **output-response** (right, top 35%): `id="output-response"`, `background: '#16a34a'` (green), with checkmark label
- **output-error** (right, top 65%): `id="output-error"`, `background: '#dc2626'` (red), with X label

Labels next to handles (follow SchedulerNode pattern lines 95-100, 109-114):
- Green checkmark at `right: -12, top: 'calc(35% - 7px)'`
- Red X at `right: -12, top: 'calc(65% - 7px)'`

Body content:
1. Header row: Network icon + label (nodeData.label or t('nodes.multiagent'))
2. Target CatFlow name: show `nodeData.target_task_name` in `text-xs text-zinc-400` if set
3. Mode badge: `text-[10px] px-1.5 py-0.5 rounded-full bg-purple-800/60 text-purple-300` showing t(`nodes.multiagentMode.${nodeData.execution_mode || 'sync'}`)

Execution status indicator -- identical to StorageNode lines 67-74:
- Absolute positioned top-right corner with Loader2 spin / Check / X / Clock icons
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>MultiAgentNode component exists with purple colors, input handle, output-response (green, 35%) and output-error (red, 65%) handles, mode badge, target name display, and execution status indicators. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (TypeScript check)
- File exists at `app/src/components/canvas/nodes/multiagent-node.tsx`
- Exports `MultiAgentNode` function
</verification>

<success_criteria>
MultiAgentNode component renders purple node with dual output handles following the exact pattern of SchedulerNode multi-handles and StorageNode execution status.
</success_criteria>

<output>
After completion, create `.planning/phases/67-multiagent-node-templates/67-01-SUMMARY.md`
</output>
