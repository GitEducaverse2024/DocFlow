---
phase: 115-tab-proveedores
plan: 02
type: execute
wave: 2
depends_on: [115-01]
files_modified:
  - app/src/app/settings/page.tsx
autonomous: true
requirements: [PROV-04]

must_haves:
  truths:
    - "La seccion de API Keys separada ya no aparece en la pagina de Settings"
    - "ProviderCard y PROVIDER_META ya no existen en page.tsx"
    - "ModelPricingSettings se mantiene (Phase 116 la necesita)"
    - "El resto de Settings (Processing, Telegram, etc.) sigue funcionando"
  artifacts:
    - path: "app/src/app/settings/page.tsx"
      provides: "Cleaned settings page without old ProviderCard/PROVIDER_META"
  key_links:
    - from: "page.tsx"
      to: "model-center-shell.tsx"
      via: "import ModelCenterShell (unchanged)"
      pattern: "import.*ModelCenterShell"
---

<objective>
Remove the old ProviderCard component, PROVIDER_META constant, and ProviderConfig interface from Settings page.tsx to eliminate the duplicate API Keys section. Keep ModelPricingSettings (needed by Phase 116).

Purpose: PROV-04 requires no duplicate API key management sections. The old code was preserved with eslint-disable for Phase 115 reuse; now that TabProveedores is built, clean it up.
Output: Slimmer page.tsx with no dead provider-management code.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-tab-proveedores/115-CONTEXT.md

<interfaces>
<!-- What to remove from page.tsx -->

Lines to remove (approximate — verify by reading file):
- ProviderConfig interface (lines 19-28) — now defined locally in tab-proveedores.tsx
- PROVIDER_META constant (lines 30-36) — now defined locally in tab-proveedores.tsx
- ProviderCard function (lines 38-304, with eslint-disable comment) — replaced by TabProveedores accordion cards
- Any unused imports that were only used by ProviderCard (Eye, EyeOff, Trash2, FlaskConical, Check, X if unused elsewhere)

Lines to KEEP:
- ModelPricingSettings function (line 427+, with eslint-disable) — Phase 116 needs it
- ModelCenterShell import and usage
- ProcessingSettings, TelegramSettings, and all other Settings sections
- All imports still used by remaining code
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ProviderCard, PROVIDER_META, and ProviderConfig from page.tsx</name>
  <files>app/src/app/settings/page.tsx</files>
  <action>
1. Read the full `app/src/app/settings/page.tsx` file
2. Remove the `ProviderConfig` interface (lines ~19-28)
3. Remove the `PROVIDER_META` constant (lines ~30-36)
4. Remove the `ProviderCard` function and its eslint-disable comment (lines ~38-304)
5. Check which imports from lucide-react are now unused after removing ProviderCard:
   - ProviderCard uses: Eye, EyeOff, Check, X, Trash2, FlaskConical
   - Check if any of these are used elsewhere in the file. If not, remove them from the import statement
   - Keep: Loader2, Plug, Palette, Cpu, DollarSign, Plus, Cat, Settings, Shield, ShieldCheck, Send, Play, Pause, Power, Users, ChevronRight, ChevronLeft, UserPlus (used by other components)
6. Check if Badge import is still used after removal — likely yes (ModelPricingSettings uses it)
7. Do NOT remove ModelPricingSettings or its eslint-disable comment (Phase 116 needs it)
8. Verify no remaining code references ProviderCard, ProviderConfig, or PROVIDER_META

**IMPORTANT:** Only remove dead code. Do not change any functional code. The page should render exactly the same as before since ProviderCard was already unused (marked with eslint-disable).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ProviderCard, PROVIDER_META, and ProviderConfig removed from page.tsx. No duplicate API key management code exists. Build passes. ModelPricingSettings preserved for Phase 116.</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npm run build` passes with zero errors
2. `grep -c "ProviderCard\|PROVIDER_META" app/src/app/settings/page.tsx` returns 0
3. `grep -c "ModelPricingSettings" app/src/app/settings/page.tsx` returns >= 1 (preserved)
4. Navigate to `/settings` — all remaining sections render correctly
</verification>

<success_criteria>
- ProviderCard function, PROVIDER_META, and ProviderConfig are gone from page.tsx
- No unused imports remain (build passes without warnings for removed code)
- ModelPricingSettings is preserved with its eslint-disable comment
- All other Settings sections (Processing, Telegram, etc.) unaffected
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/115-tab-proveedores/115-02-SUMMARY.md`
</output>
