# Phase 15 Plan: Rebranding Visual — DocFlow → DoCatFlow

**Goal:** Transform the app identity from DocFlow to DoCatFlow with new logo, colors, typography, and consistent visual language.

**Requirements:** BRAND-01, BRAND-02, BRAND-03, BRAND-04, BRAND-05, BRAND-06, BRAND-07

---

## Task 1: Sidebar Header — Logo + DoCatFlow Name + Version

**File:** `app/src/components/layout/sidebar.tsx`

**Requirements:** BRAND-01, BRAND-05

**Changes:**

1. **Import logo**: Add `import Image from 'next/image'` and import the logo from `@/../../Images/logo.jpg` (capital I — already exists at `app/Images/logo.jpg`, 1024x1024)

2. **Replace header block** (lines 38-43). Current:
```tsx
<div className="flex items-center gap-3 px-3 py-4">
  <div className="w-8 h-8 rounded-lg bg-violet-500 flex items-center justify-center text-white font-bold text-sm">D</div>
  <span className="text-lg font-semibold text-zinc-50">DocFlow</span>
</div>
```

New:
```tsx
<div className="flex items-center gap-3 px-3 py-4">
  <Image
    src={logoImg}
    alt="DoCatFlow"
    width={32}
    height={32}
    className="rounded-full object-cover"
  />
  <div className="flex flex-col">
    <span className="text-lg font-semibold text-zinc-50">
      Do<span style={{ color: '#8B6D8B' }}>Cat</span>Flow
    </span>
    <span className="text-xs text-zinc-500">v1.0</span>
  </div>
</div>
```

3. **Update active nav link styling** (line ~57). Current:
```tsx
'bg-violet-500/10 text-violet-400'
```

New:
```tsx
'bg-gradient-to-r from-violet-600/20 to-purple-600/10 border-l-2 border-violet-500 text-violet-400'
```

**Verification:** Sidebar shows circular logo, "DoCatFlow" with mauve "Cat", "v1.0" underneath, active links have left border + gradient.

---

## Task 2: Layout Metadata — Title, Description, Favicon

**File:** `app/src/app/layout.tsx`

**Requirements:** BRAND-02, BRAND-07

**Changes:**

1. **Update metadata** (lines 11-12). Current:
```tsx
title: 'DocFlow',
description: 'Gestión de proyectos de documentación inteligente',
```

New:
```tsx
title: 'DoCatFlow',
description: 'Intelligent Workflow & Cat-Driven Solutions',
```

2. **Font weights**: Inter is already imported via `next/font/google`. Update the import to explicitly include weights if not already:
```tsx
const inter = Inter({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
});
```

3. **Favicon**: The existing `app/src/app/favicon.ico` will be replaced. Since we can't programmatically generate an ICO from JPG in the build, create a simple approach:
   - Use a script or manual step to create favicon from logo
   - OR use the `<link rel="icon">` approach with a PNG/JPG crop
   - Simplest: add to metadata in layout.tsx:
```tsx
icons: {
  icon: '/logo-favicon.png',
},
```
   - Copy a cropped version of the logo to `app/public/logo-favicon.png` (32x32 or 64x64)

**Approach for favicon:** Since we can't run image processing in the build, use `sharp` or a Bash command with ImageMagick/ffmpeg to create `app/public/logo-favicon.png` from `app/Images/logo.jpg`. If tools aren't available, use the JPG directly as `<link rel="icon" type="image/jpeg">`.

**Verification:** Browser tab shows "DoCatFlow" title, new favicon, correct description.

---

## Task 3: Rename DocFlow → DoCatFlow Across Codebase

**Requirements:** BRAND-03

**Files to modify (code files only — documentation separately):**

| File | What to change |
|------|---------------|
| `app/src/app/layout.tsx` | metadata.title (done in Task 2) |
| `app/src/components/layout/sidebar.tsx` | Header text (done in Task 1) |
| `app/src/app/settings/page.tsx` | Any "DocFlow" reference in UI text |
| `app/src/app/agents/page.tsx` | Any "DocFlow" reference |
| `app/src/app/page.tsx` | Dashboard title/references |
| `app/src/lib/db.ts` | Any seed data referencing "DocFlow" |
| `app/src/app/api/agents/create/route.ts` | Prompt/description text |
| `app/src/app/api/projects/[id]/bot/create/route.ts` | Prompt/description text |
| `app/src/app/api/health/route.ts` | Health check response text |
| `app/src/components/system/system-health-panel.tsx` | UI labels |
| `app/src/components/sources/youtube-input.tsx` | Any reference |

**Strategy:** Use grep to find all occurrences of "DocFlow" (case-sensitive) in `app/src/` and replace with "DoCatFlow". Be careful with:
- Don't rename in import paths or file paths
- Don't break Docker/config references
- Documentation files (.md) in root — rename but keep clarity

**Verification:** `grep -ri "docflow" app/src/ --include="*.ts" --include="*.tsx"` returns 0 results for plain "DocFlow" (only "DoCatFlow").

---

## Task 4: Button Gradients

**Requirements:** BRAND-04

**Pattern to replace across all component files:**

Current pattern:
```tsx
bg-violet-600 hover:bg-violet-500
```

New pattern:
```tsx
bg-gradient-to-r from-violet-600 to-purple-700 hover:from-violet-500 hover:to-purple-600
```

Also replace the less common:
```tsx
bg-violet-600 hover:bg-violet-700
```
With same gradient.

**Files affected** (22+ instances across):
- `app/src/components/rag/rag-panel.tsx`
- `app/src/components/process/process-panel.tsx`
- `app/src/components/agents/agent-creator.tsx`
- `app/src/components/chat/chat-panel.tsx`
- `app/src/app/connectors/page.tsx`
- `app/src/app/tasks/page.tsx`
- `app/src/app/tasks/new/page.tsx`
- `app/src/app/tasks/[id]/page.tsx`
- `app/src/app/agents/page.tsx`
- `app/src/app/settings/page.tsx`
- `app/src/app/page.tsx`
- `app/src/app/projects/new/page.tsx`
- Other components with primary buttons

**Strategy:** Use `grep -rn "bg-violet-600" app/src/` to find all instances, then apply the gradient replacement. Be selective — only replace primary action buttons, NOT badges, decorative elements, or status indicators.

**Exclusions:** Do NOT change:
- `bg-violet-500/10` (badge backgrounds)
- `text-violet-400` (text colors)
- `border-violet-500` (borders)
- `from-violet-500 to-emerald-500` (existing chat/pipeline gradients)

**Verification:** All primary CTA buttons show gradient. Build passes.

---

## Task 5: Card and Input Styling

**Requirements:** BRAND-06

**Card pattern changes:**

Current:
```tsx
bg-zinc-900 border border-zinc-800
```

New:
```tsx
bg-zinc-900/80 border border-zinc-800/50 hover:border-violet-800/30 transition-colors
```

**Input focus pattern — add to globals.css:**

```css
/* In globals.css, add a utility or override */
input:focus, textarea:focus, select:focus {
  --tw-ring-color: rgb(168 85 247 / 0.5); /* purple-500/50 */
}
```

Or apply via Tailwind classes where inputs are used: add `focus:ring-purple-500/50` to input components.

**Files affected:**
- `app/src/components/chat/chat-panel.tsx`
- `app/src/components/sources/source-manager.tsx`
- `app/src/components/rag/rag-panel.tsx`
- `app/src/app/connectors/page.tsx`
- `app/src/app/tasks/new/page.tsx`
- `app/src/app/settings/page.tsx`
- Other pages with card/input patterns

**Strategy:** Focus on the most visible cards (dashboard, connectors, tasks list, project detail). Don't change every single `bg-zinc-900` — only those that are actual content cards.

**Verification:** Cards have subtle hover border effect. Input focus shows purple ring.

---

## Task 6: Typography Verification

**Requirements:** BRAND-07

**Current state:** Inter is already imported in layout.tsx via `next/font/google`. Need to verify weights are specified.

**Changes:**
- Ensure Inter import includes weights: `['400', '500', '600', '700']`
- No other changes needed — Inter is already the default font

**Verification:** `npm run build` passes. Font renders correctly.

---

## Task 7: Build Verification

**All tasks above must pass:** `cd ~/docflow/app && npm run build`

**Checklist:**
1. No TypeScript errors from logo import or new patterns
2. No unused imports
3. All pages render without errors
4. Favicon loads correctly

---

## Execution Order

```
Task 1 (Sidebar) ──┐
Task 2 (Layout)  ──┤
Task 3 (Rename)  ──┼── Can run sequentially, each is independent
Task 4 (Buttons) ──┤
Task 5 (Cards)   ──┤
Task 6 (Typography)┘
         │
    Task 7 (Build verification)
```

**Recommended:** Execute tasks 1-6 sequentially (they touch different files but some overlap), then verify build.

---

## Success Criteria

1. ✅ Sidebar shows circular logo + "DoCatFlow" with mauve "Cat" + "v1.0"
2. ✅ Browser tab: "DoCatFlow" title + cat favicon
3. ✅ Zero "DocFlow" references in app/src/ (only "DoCatFlow")
4. ✅ Primary buttons have violet→purple gradient
5. ✅ Active sidebar links have left border + gradient background
6. ✅ Cards have hover:border-violet-800/30 effect
7. ✅ Inter font with proper weights (400/500/600/700)
8. ✅ `npm run build` passes without errors
