---
phase: 161-ui-enrutamiento-oracle-e2e
plan: 05
type: execute
wave: 2
depends_on: ["161-02"]
files_modified:
  - app/src/components/settings/model-center/tab-enrutamiento.tsx
  - app/messages/es.json
  - app/messages/en.json
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Each row in the Enrutamiento tab has an expand chevron that reveals an inline advanced panel"
    - "Expanded panel shows a 'Inteligencia' dropdown (off/low/medium/high) ONLY when capabilities.supports_reasoning === true"
    - "Expanded panel shows a 'max_tokens' numeric input always, with placeholder = capabilities.max_tokens_cap (or 'sin definir' fallback), helper 'máx {cap}'"
    - "Expanded panel shows a 'thinking_budget' numeric input when capabilities.supports_reasoning === true, helper '≤ max_tokens (actual: {current})'"
    - "reasoning_effort dropdown auto-saves on select via PATCH /api/alias-routing with optimistic update + toast + revert on error (mirrors applyModelChange pattern)"
    - "max_tokens + thinking_budget use dirty-state + explicit Guardar button, empty input serializes as null (CFG-02j reset semantics)"
    - "Visual validation: border-red when max_tokens > cap OR thinking_budget > max_tokens (does NOT block save; backend validator is source of truth)"
    - "Zero regression on collapsed row: the main 4-col grid renders byte-identical for users who never expand"
  artifacts:
    - path: "app/src/components/settings/model-center/tab-enrutamiento.tsx"
      provides: "Expand-row panel with 3 conditional controls + dirty-save flow"
      min_lines: 520
    - path: "app/messages/es.json"
      provides: "i18n keys for new UI copy in Spanish"
      contains: "inteligencia"
    - path: "app/messages/en.json"
      provides: "i18n keys for new UI copy in English"
      contains: "inteligencia"
  key_links:
    - from: "tab-enrutamiento.tsx fetch('/api/aliases')"
      to: "EnrichedAliasRow consumer with capabilities + reasoning config"
      via: "useState<EnrichedAliasRow[]> + type-safe read of capabilities.* fields"
      pattern: "capabilities.supports_reasoning"
    - from: "Guardar button onClick"
      to: "PATCH /api/alias-routing"
      via: "fetch PATCH with body including reasoning_effort + max_tokens + thinking_budget"
      pattern: "method: 'PATCH'"
    - from: "reasoning_effort Select onValueChange"
      to: "PATCH /api/alias-routing auto-save"
      via: "fetch PATCH with partial body (optimistic + revert pattern)"
      pattern: "reasoning_effort"
---

<objective>
Add the UI expand-row panel to the Enrutamiento tab with 3 conditional controls (Inteligencia dropdown, max_tokens input, thinking_budget input) that honour the capabilities returned by `/api/aliases` (Plan 02). Delivers UI-01, UI-02, UI-03. Preserves the existing 4-col grid byte-identically for users who never expand a row (zero regression filter per CONTEXT.md).

Purpose: This is the user-facing parity with CatBot's programmatic control (`set_catbot_llm` tool from Phase 160-03). The backend validator (Phase 159-03) is the final source of truth; this UI is cooperative (visual hints, not hard blocks).

Output: Extended tab-enrutamiento.tsx (~520 lines, up from 358), new i18n keys in both es.json + en.json, no new deps (all UI primitives already available per CONTEXT.md § Reusable Assets).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/161-ui-enrutamiento-oracle-e2e/161-CONTEXT.md

@app/src/components/settings/model-center/tab-enrutamiento.tsx
@app/messages/es.json
@app/messages/en.json

<interfaces>
<!-- Enriched /api/aliases shape from Plan 02 — THIS IS THE CONTRACT THE UI CONSUMES -->

Response body from GET /api/aliases (post-Plan-02):
```typescript
{
  aliases: Array<{
    alias: string;
    model_key: string;
    description: string;
    is_active: number;
    reasoning_effort: 'off' | 'low' | 'medium' | 'high' | null;
    max_tokens: number | null;
    thinking_budget: number | null;
    capabilities: {
      supports_reasoning: boolean | null;
      max_tokens_cap: number | null;
      is_local: boolean | null;
    } | null;
  }>;
}
```

PATCH /api/alias-routing body (unchanged, already supported by Phase 159-03):
```typescript
{
  alias: string;
  model_key: string;
  reasoning_effort?: 'off' | 'low' | 'medium' | 'high' | null;
  max_tokens?: number | null;
  thinking_budget?: number | null;
}
```

Existing UI primitives (all available in `app/src/components/ui/`):
- `Input`, `Select`/`SelectTrigger`/`SelectContent`/`SelectItem`/`SelectValue`, `Label`, `Button`, `Badge`
- `Collapsible` / `CollapsibleTrigger` / `CollapsibleContent` from shadcn

Current file has `applyModelChange` at L148-178 — copy this pattern for `reasoning_effort` auto-save.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AliasRow type + data fetch to consume enriched shape</name>
  <files>app/src/components/settings/model-center/tab-enrutamiento.tsx</files>
  <action>
    In `tab-enrutamiento.tsx`, modify the `AliasRow` interface at L29-34 to match the new enriched shape from `/api/aliases` (keep interface name `AliasRow` — existing references use it). New shape:

    ```typescript
    interface AliasRow {
      alias: string;
      model_key: string;
      description: string;
      is_active: number;
      reasoning_effort: 'off' | 'low' | 'medium' | 'high' | null;
      max_tokens: number | null;
      thinking_budget: number | null;
      capabilities: {
        supports_reasoning: boolean | null;
        max_tokens_cap: number | null;
        is_local: boolean | null;
      } | null;
    }
    ```

    The existing `useEffect` fetch at L92-119 already does `data.aliases` — compatible with the new shape (array at `.aliases` key unchanged). No changes needed there beyond the type. Confirm by running `npm run build` after the type edit.

    Add new state for expanded rows + dirty tracking near L79:
    ```typescript
    const [expandedAlias, setExpandedAlias] = useState<string | null>(null);
    const [dirtyRows, setDirtyRows] = useState<Record<string, { max_tokens?: number | null; thinking_budget?: number | null }>>({});
    ```

    Add helper to build target capabilities from currently selected model (since user may change model, need cap of TARGET not current row):
    ```typescript
    const getTargetCapabilities = (alias: string): { supports_reasoning: boolean | null; max_tokens_cap: number | null; is_local: boolean | null } | null => {
      const row = aliases.find(a => a.alias === alias);
      if (!row) return null;
      // For the expand panel, use the ROW's capabilities (reflects currently assigned model).
      // The target cap on model-change is handled by /api/aliases refresh after PATCH.
      return row.capabilities;
    };
    ```

    AVOID: Do NOT fetch `/api/models` client-side for capabilities lookup — Plan 02 already includes capabilities per row in `/api/aliases`. AVOID: Do NOT introduce a second state for "target model cap" when dropdown changes — UI simplicity trumps edge-case polish; after auto-save of model change the row refetches its enriched shape.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "tab-enrutamiento"</automated>
  </verify>
  <done>
    - `AliasRow` interface matches Plan 02 enriched shape
    - No TS errors in tab-enrutamiento.tsx
    - New state hooks declared
  </done>
</task>

<task type="auto">
  <name>Task 2: Add expand-row panel with 3 conditional controls + dirty-save flow</name>
  <files>app/src/components/settings/model-center/tab-enrutamiento.tsx, app/messages/es.json, app/messages/en.json</files>
  <action>
    Step 1 — Add i18n keys to `app/messages/es.json` inside `settings.modelCenter.enrutamiento` block (around L2651-2670, append before closing brace):
    ```json
    "expandir": "Expandir",
    "colapsar": "Colapsar",
    "avanzado": "Opciones avanzadas",
    "inteligencia": "Inteligencia (reasoning)",
    "inteligenciaOff": "Desactivado",
    "inteligenciaLow": "Baja",
    "inteligenciaMedium": "Media",
    "inteligenciaHigh": "Alta",
    "maxTokens": "Tokens máximos",
    "maxTokensHelper": "máx {cap}",
    "maxTokensHelperSinCap": "sin definir",
    "thinkingBudget": "Presupuesto thinking",
    "thinkingBudgetHelper": "≤ max_tokens (actual: {current})",
    "thinkingBudgetHelperSinMax": "≤ max_tokens",
    "guardar": "Guardar",
    "guardado": "Cambios guardados",
    "guardadoError": "Error al guardar",
    "sinSoporteReasoning": "Este modelo no soporta reasoning",
    "capabilitiesDesconocidas": "Capacidades desconocidas"
    ```

    Step 2 — Add the same keys to `app/messages/en.json` (same block, English translations):
    ```json
    "expandir": "Expand",
    "colapsar": "Collapse",
    "avanzado": "Advanced options",
    "inteligencia": "Intelligence (reasoning)",
    "inteligenciaOff": "Off",
    "inteligenciaLow": "Low",
    "inteligenciaMedium": "Medium",
    "inteligenciaHigh": "High",
    "maxTokens": "Max tokens",
    "maxTokensHelper": "max {cap}",
    "maxTokensHelperSinCap": "unset",
    "thinkingBudget": "Thinking budget",
    "thinkingBudgetHelper": "≤ max_tokens (current: {current})",
    "thinkingBudgetHelperSinMax": "≤ max_tokens",
    "guardar": "Save",
    "guardado": "Changes saved",
    "guardadoError": "Save error",
    "sinSoporteReasoning": "This model does not support reasoning",
    "capabilitiesDesconocidas": "Unknown capabilities"
    ```

    Step 3 — In `tab-enrutamiento.tsx`, add handlers:

    (a) `applyReasoningEffort(alias: string, value: 'off' | 'low' | 'medium' | 'high')` — copy the `applyModelChange` pattern (L148-178). Optimistic update of `aliases` state, PATCH body `{alias, model_key: row.model_key, reasoning_effort: value}`, toast success/error + revert on fail.

    (b) `saveRow(alias: string)` — triggered by Guardar button. Reads `dirtyRows[alias]`, constructs PATCH body including the current `reasoning_effort` + dirty values (empty string → null per CFG-02j reset), PATCH /api/alias-routing, on success clears the dirty entry + toasts t('guardado'), on fail toasts t('guardadoError') with backend error message.

    (c) `updateDirtyField(alias, field, value)` — controlled-input onChange that writes to `dirtyRows` state. Numeric parsing: empty → null, valid number → Number(value), else keep previous.

    Step 4 — JSX: wrap each existing row (currently L256-321 `<div key={a.alias} ...>`) in a fragment:
    - The existing 4-col grid stays exactly as-is (zero regression).
    - Append a new `<div>` AFTER the 4-col grid, containing the chevron toggle button (set `expandedAlias` to null or current alias).
    - Conditionally render (when `expandedAlias === a.alias`) a `<div>` with the 3-control panel:
      - **Inteligencia Select**: render only if `a.capabilities?.supports_reasoning === true`. Options: off/low/medium/high. Value = `a.reasoning_effort ?? 'off'`. onValueChange → `applyReasoningEffort(a.alias, v)`. If `a.capabilities === null`, render a `<p>` with `t('capabilitiesDesconocidas')` instead of hiding the control entirely (UX hint).
      - **max_tokens Input**: always visible. `type="number"`. Placeholder = `a.capabilities?.max_tokens_cap?.toString() ?? t('maxTokensHelperSinCap')`. Value = `dirtyRows[a.alias]?.max_tokens !== undefined ? (dirtyRows[a.alias].max_tokens ?? '') : (a.max_tokens ?? '')`. onChange → `updateDirtyField(a.alias, 'max_tokens', v)`. Border-red class when `dirty.max_tokens > a.capabilities?.max_tokens_cap`. Helper: `t('maxTokensHelper', {cap: a.capabilities?.max_tokens_cap ?? '?'})`.
      - **thinking_budget Input**: visible only when `a.capabilities?.supports_reasoning === true`. Same controlled-input pattern. Helper includes current max_tokens: `t('thinkingBudgetHelper', {current: effectiveMaxTokens})`. Border-red when `dirty.thinking_budget > effectiveMaxTokens`.
      - **Guardar Button**: enabled only when `dirtyRows[a.alias]` has at least one entry. onClick → `saveRow(a.alias)`.

    Step 5 — Chevron icon: use `ChevronDown` from lucide-react (already in the codebase per existing imports in nearby settings components — add to the top-of-file import line). Rotate 180deg when expanded.

    AVOID: Do NOT replace the main 4-col grid — append the expand-row beneath each row div. Zero-regression filter: existing tests/screenshots of collapsed UI must remain visually identical. AVOID: Do NOT add hard blocks on invalid input — validation is visual (border-red) only. AVOID: Do NOT omit the `current: {max_tokens}` interpolation in the thinking_budget helper — UX wants to show the effective cap. AVOID: Do NOT parse empty string as 0 — it's `null` (CFG-02j reset). AVOID: Do NOT use `supports_reasoning === null` as "hide the dropdown" — CONTEXT.md says "visible solo si capabilities.supports_reasoning === true", explicit null → show fallback text with t('capabilitiesDesconocidas'). AVOID: Do NOT store dirty values in the aliases array directly — corrupts the source of truth for cancellation; use the separate `dirtyRows` object. AVOID: Do NOT call `/api/alias-routing` with every keystroke — PATCH storm, see CONTEXT.md § UX validación.

    Step 6 — Refetch `/api/aliases` after any successful PATCH (model change OR reasoning_effort change OR saveRow) to reflect the new capabilities in case the user changed to a model with different caps. Simple approach: abstract the initial useEffect fetch into a `loadAliases()` callback, call it after successful PATCH.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app &amp;&amp; npm run build 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `npm run build` succeeds (no TS errors, no ESLint unused-vars per MEMORY.md lint-as-error rule)
    - es.json + en.json have matching new keys (18 keys each under `settings.modelCenter.enrutamiento`)
    - Manual smoke (post-Docker-rebuild, see verification below): open tab Enrutamiento → collapsed rows look identical to pre-change → click chevron → panel expands with 3 controls (or 1 control for non-reasoning models) → change reasoning_effort on a reasoning model → auto-saves with toast → edit max_tokens → Guardar button enables → click → toast "Cambios guardados" → refresh → values persist
    - Grep check: `grep -c 'inteligencia\|maxTokens\|thinkingBudget' app/messages/es.json` ≥ 10 (new keys)
  </done>
</task>

</tasks>

<verification>
- Build: `cd app && npm run build` succeeds.
- Docker rebuild (MEMORY.md feedback_docker_restart.md): `docker compose build --no-cache docflow-app && docker compose up -d docflow-app && docker exec -u root docflow-app chown -R nextjs:nodejs /app/data/ && docker restart docflow-app`.
- Manual UI smoke: `/settings/model-center` → tab Enrutamiento → existing 4-col grid unchanged on every row → each row has chevron → clicking expands panel with correct conditional controls (dropdown ONLY for reasoning models) → reasoning_effort auto-save toasts → max_tokens + thinking_budget Guardar roundtrip persists.
- Zero regression: collapsed row visual identical to pre-change (pixel-level).
</verification>

<success_criteria>
- UI-01: Inteligencia dropdown visible only for `supports_reasoning === true` ✓
- UI-02: max_tokens input always visible with placeholder = cap + helper ✓
- UI-03: thinking_budget input visible for reasoning models + helper refs max_tokens ✓
- Auto-save works on reasoning_effort dropdown (mirror of applyModelChange)
- Dirty+save works on max_tokens/thinking_budget (Guardar explicit button)
- Backend validation errors shown as toasts (cooperative UX, backend is source of truth)
- Zero visual regression on collapsed rows
</success_criteria>

<output>
After completion, create `.planning/phases/161-ui-enrutamiento-oracle-e2e/161-05-SUMMARY.md`
</output>
