---
status: diagnosed
phase: 161-ui-enrutamiento-oracle-e2e
plan: 06
type: uat
source: [161-01-SUMMARY.md, 161-02-SUMMARY.md, 161-03-SUMMARY.md, 161-04-SUMMARY.md, 161-05-SUMMARY.md]
requirements_tested: [VER-01, VER-02, VER-03]
started: 2026-04-22T12:20:00Z
executed: 2026-04-22T12:35:00Z
updated: 2026-04-22T14:55:00Z
---

# Phase 161 UAT — Oracle CatBot 3/3

**Contract:** CLAUDE.md "Protocolo de Testing: CatBot como Oráculo" — every feature must be demonstrable by CatBot itself through its tools, with verbatim transcripts pasted as evidence.

## Pre-flight

- **Docker stack:** `docflow-app` up on `localhost:3500`, LiteLLM gateway up on `localhost:4000`
- **Routing verified via `/v1/models`:**
  - `claude-opus` → `anthropic/claude-opus-4-6`
  - `claude-sonnet` → `anthropic/claude-sonnet-4-6`
  - `gemini-main` → `vertex_ai/gemini-3.1-pro-preview`
  - `gemma-local` → `ollama/gemma4:e4b`
- **Pre-state of alias `catbot`** (via `GET /api/aliases`):
  ```json
  {
    "alias": "catbot",
    "model_key": "claude-opus",
    "reasoning_effort": null,
    "max_tokens": null,
    "thinking_budget": null,
    "capabilities": {
      "supports_reasoning": true,
      "max_tokens_cap": 32000,
      "is_local": false
    }
  }
  ```
  **Note:** `capabilities` non-null confirms Plan 161-01 shortcut seed + Plan 161-02 JOIN enrichment are live end-to-end. Pre-v30.0 this block was `null` per STATE.md L39 blocker.
- **Sudo:** Session token minted via `POST /api/catbot/sudo {action:"verify", password:"<user-provided>"}` — 5-minute validity window.
- **Log file writable:** `/app/data/logs/app-2026-04-22.jsonl` present (per Plan 161-03 `LogSource='catbot-chat'`).

## Current Test

[testing complete]

## Tests

### 1. Oracle 1 (VER-01) — Enumerate models + capabilities

expected: CatBot invokes `list_llm_models` tool, lists ≥4 models, annotates reasoning support non-null for the 4 Plan 161-01 shortcuts
result: pass

**Prompt (verbatim):**
> Enumera los modelos LLM disponibles y dime cuáles soportan reasoning (thinking). Usa tus herramientas para consultar el catálogo real, no inventes.

**Tool invocation observed:**
- `list_llm_models()` → returned `{models: [...21 entries...]}`

**Key capability rows returned (proving Plan 161-01 unblocked the FQN-vs-shortcut namespace mismatch from STATE.md L39):**

```
model_key      | supports_reasoning | max_tokens_cap | is_local | tier
---------------+--------------------+----------------+----------+-------
claude-opus    | true               | 32000          | false    | Elite
claude-sonnet  | true               | 64000          | false    | Pro
gemini-main    | true               | 65536          | false    | Elite
gemma-local    | false              | 8192           | true     | Libre
```

**CatBot reply (summary):** Correctly identified 4 reasoning-capable models (`google/gemini-2.5-pro`, `gemini-main`, `claude-opus`, `claude-sonnet`) and grouped by tier (Elite / Pro / Libre). Narration accurately described `supports_reasoning` semantics for each tier.

**Tokens:** input 33351, output 1153.

**Evidence:** All 4 shortcut rows have non-null capabilities (pre-161-01 they were all null). VER-01 contract satisfied end-to-end: DB seed → `/api/models` JOIN enrichment → CatBot tool response → narration.

---

### 2. Oracle 2 (VER-02) — `set_catbot_llm` under sudo

expected: CatBot requests sudo first (Operador de Modelos skill conservative behaviour), then invokes `set_catbot_llm` with Opus + high + thinking params; DB reflects change
result: pass

**Prompt 1 (verbatim, with `sudo_token` in request body):**
> Cambia mi configuración: usa claude-opus con reasoning_effort alto, max_tokens 32000 y thinking_budget 16000. Usa tu herramienta set_catbot_llm.

**First-round CatBot behaviour (conservative per Phase 160-04 "Operador de Modelos" skill):**
- Invoked `get_catbot_llm()` to read current state.
- Invoked `list_llm_models({filter: "Elite"})` to confirm capabilities cap for claude-opus.
- Responded asking for explicit sudo confirmation before writing.

**Prompt 2 (verbatim confirmation):**
> Sí, procede ahora. Tengo sudo activo. Ejecuta set_catbot_llm con esos parámetros exactos.

**Tool invocation observed:**
```json
set_catbot_llm({
  "model": "claude-opus",
  "reasoning_effort": "high",
  "max_tokens": 32000,
  "thinking_budget": 16000
})
```

**Tool response (verbatim):**
```json
{
  "success": true,
  "alias": "catbot",
  "applied": {
    "model": "claude-opus",
    "reasoning_effort": "high",
    "max_tokens": 32000,
    "thinking_budget": 16000
  },
  "message": "Alias catbot actualizado. Próximo mensaje en este chat usará la nueva config."
}
```

**Post-state verification via `GET /api/aliases`:**
```json
{
  "alias": "catbot",
  "model_key": "claude-opus",
  "reasoning_effort": "high",
  "max_tokens": 32000,
  "thinking_budget": 16000,
  "capabilities": {
    "supports_reasoning": true,
    "max_tokens_cap": 32000,
    "is_local": false
  }
}
```

**Evidence:** TOOL-03 + CFG-02 + CFG-03 + PASS-03 + PASS-04 operative end-to-end. Sudo gate worked (the tool correctly recognized `sudo_active:true`); conservative read-before-write behaviour from the "Operador de Modelos" skill (Phase 160-04) fired as designed. DB persistence confirmed via read-back. VER-02 contract satisfied.

---

### 3. Oracle 3 (VER-03) — `reasoning_tokens > 0` logged to JSONL

expected: Next CatBot message after VER-02 succeeds triggers `logger.info('catbot-chat', 'reasoning_usage', {reasoning_tokens > 0, alias: 'catbot', ...})` per Plan 161-03 instrumentation
result: issue
reported: "First prompt errored with LiteLLM 400 (collision between `thinking` and `thinking_level`); second prompt (with explicit `model: claude-opus` in body) returned a valid response with reasoning but NO `reasoning_usage` JSONL line ever appeared. `grep -ac 'reasoning_usage' /app/data/logs/app-2026-04-22.jsonl` → 0."
severity: blocker

**Prompt (verbatim):**
> Resuelve razonando paso a paso: dos trenes parten de estaciones A y B separadas 600 km. El que sale de A viaja a 80 km/h, el que sale de B viaja a 70 km/h. Salen simultáneamente y viajan uno hacia el otro. ¿A qué hora se cruzan y a cuántos km de A?

**Anomaly chain:**

**Attempt 1** (no explicit `model` in request body, relying on `catbot_config` + alias):

LiteLLM returned HTTP 400:
```
litellm.UnsupportedParamsError: Cannot specify both `thinking` (which maps to
`thinking_budget`) and `thinking_level` in the same request. For Gemini 3
models, use `thinking_level` instead. Received Model Group=gemini-main
```

**Critical observation:** `/api/catbot/chat` log says `"Alias resolved: catbot -> claude-opus"` (correct per alias state), but LiteLLM reports `Model Group=gemini-main` (wrong model actually hit the gateway). Root cause → Gap A below.

**Attempt 2** (with `"model":"claude-opus"` injected into request body, bypassing `catbot_config.model`):

CatBot returned a valid step-by-step reasoning response (~820 output tokens with proper kinematic derivation: `t = 600 / (80+70) = 4h`, cross at `4·80 = 320 km from A`). But the expected `reasoning_usage` JSONL line **never appeared in the log**:

```bash
$ docker exec docflow-app grep -ac "reasoning_usage" /app/data/logs/app-2026-04-22.jsonl
0
```

**Control test (direct bypass of DocFlow):**

To confirm LiteLLM passthrough is in fact working, a direct curl to LiteLLM with the same body shape:

```
POST /v1/chat/completions
{
  "model": "claude-opus",
  "max_tokens": 32000,
  "reasoning_effort": "high",
  "thinking": {"type": "enabled", "budget_tokens": 16000},
  "messages": [...]
}
```

Response contained:
- `usage.completion_tokens_details.reasoning_tokens = 50` (non-zero ✓)
- `message.reasoning_content = "<Claude thinking text>"` ✓
- `message.thinking_blocks = [{type: "thinking", thinking: "...", signature: "..."}]` ✓

**Interpretation:** LiteLLM passthrough (Phase 159-02/04) is correctly implemented and does return `reasoning_tokens`. But the end-to-end flow from `/api/catbot/chat` to the Plan 161-03 logger does not fire for reasons that are not yet diagnosed. See Gap B.

**VER-03 contract:** NOT satisfied. The logger is the canonical evidence mechanism for VER-03 (`{source:"catbot-chat", message:"reasoning_usage", reasoning_tokens:>0, alias:"catbot"}`) — without the JSONL entry, the truth is unverifiable per the hard spec.

## Summary

total: 3
passed: 2
issues: 1
pending: 0
skipped: 0

## Gaps

<!-- YAML format for /gsd:plan-phase --gaps consumption -->

- truth: "Next CatBot request after set_catbot_llm to Opus+high emits logger.info('catbot-chat', 'reasoning_usage', {reasoning_tokens > 0, alias: 'catbot'}) to /app/data/logs/app-YYYY-MM-DD.jsonl"
  status: failed
  reason: "grep -ac 'reasoning_usage' on today's JSONL returned 0 even though control-test curl directly to LiteLLM confirmed reasoning_tokens=50 is being returned by the gateway for identical body shape."
  severity: blocker
  test: 3
  root_cause: "Two independent defects. (A) app/src/app/api/catbot/chat/route.ts:121 — priority `requestedModel || catbotConfig.model || cfg.model` lets pre-v30 `catbot_config.model='gemini-main'` row mask the alias set via set_catbot_llm; Gemini 3 rejects the body because LiteLLM auto-translates reasoning_effort→thinking_level and collides with our thinking object. (B) Even when Gap A is bypassed (explicit model in body), logger call at app/src/app/api/catbot/chat/route.ts:518-525 (non-stream) + ~L234 (stream) does not fire — suspected usage-shape mismatch inside the multi-iteration tool loop (MAX_TOOL_ITERATIONS=15), OR reasoning params dropped on subsequent iterations, OR merge_reasoning_content_in_choices LiteLLM config affects the usage object nesting in the full-context request (26k-token system prompt)."
  artifacts:
    - path: "app/src/app/api/catbot/chat/route.ts"
      line: 121
      issue: "Gap A: catbot_config.model overrides alias set via set_catbot_llm — silently breaks tool's contract"
    - path: "app/src/app/api/catbot/chat/route.ts"
      line: "518-525 (non-stream) + ~234 (stream onDone)"
      issue: "Gap B: Plan 161-03 reasoning_usage logger silent end-to-end despite LiteLLM returning reasoning_tokens=50 in control test"
  missing:
    - "Gap A fix: invert priority to `cfg.model || catbotConfig.model || requestedModel` OR make set_catbot_llm also UPDATE catbot_config.model atomically (preferred — keeps user-intent local to the tool)."
    - "Gap B diagnostic: add `logger.debug('catbot-chat', 'usage_inspect', {usage_raw: JSON.stringify(usage)})` before the `if (rt > 0)` check to capture the actual usage object shape in production, then iterate. Inspect whether `usage.completion_tokens_details` is dropped, nested differently, or arrives only on specific iteration."
    - "Integration test covering: set_catbot_llm via sudo → follow-up catbot/chat request → assert reasoning_usage log line present via tmpfile logger mock."
  debug_session: ""

- truth: "GET /api/models reports available=true for shortcut model rows (claude-opus, claude-sonnet, gemini-main, gemma-local) when LiteLLM exposes them via /v1/models"
  status: failed
  reason: "list_llm_models tool response shows available=false for all 4 shortcut rows even though /v1/models on LiteLLM exposes them correctly; Discovery↔catalog cross-match treats LiteLLM shortcuts as unavailable."
  severity: minor
  test: 1
  root_cause: "catbot-tools.ts list_llm_models handler cross-matches Discovery inventory against model_intelligence.model_key; Plan 161-01 fixed the capabilities enrichment side (shortcut FQNs seeded into model_intelligence) but the availability check uses a different path that still sees the FQN-vs-shortcut mismatch. Low-severity because CatBot reads supports_reasoning (non-null, correct) not available for its narration."
  artifacts:
    - path: "app/src/lib/services/catbot-tools.ts"
      issue: "Gap C: list_llm_models reports available=false for the 4 seeded shortcut rows"
  missing:
    - "Extend Plan 161-01 pattern to the availability check: FQN-vs-shortcut bridge in catbot-tools.ts list_llm_models, OR consult model_aliases resolver layer. Deferred to v30.1 per CONTEXT.md deferred-list."
  debug_session: ""

## Gap Severity Summary

| Gap | Severity | Blocker? | Next step |
|-----|----------|----------|-----------|
| A (catbot_config.model override at route.ts:121) | HIGH | YES for VER-03 | gap-closure plan in /gsd:plan-phase 161 --gaps |
| B (Plan 161-03 logger silent end-to-end)        | MEDIUM | YES for VER-03 | gap-closure plan same wave as Gap A |
| C (list_llm_models available=false on shortcuts) | LOW  | NO | deferred to v30.1 per CONTEXT.md |

## Overall

**Phase 161 UAT:** diagnosed — 2/3 oracles PASS outright (VER-01, VER-02), VER-03 PARTIAL with 2 blockers (Gaps A + B) that require a gap-closure plan before verifier will accept the phase.

**Next step:** `/gsd:plan-phase 161 --gaps` consumes Gaps section above and emits a targeted fix plan (likely plan 161.1 or 161-07) covering:
1. Atomic `set_catbot_llm` → `catbot_config.model` propagation (or route.ts:121 priority inversion)
2. Logger 161-03 diagnosis via `usage_inspect` debug log + iteration-loop audit
3. Integration test sealing the end-to-end contract post-fix

**Milestone v30.0 status:** UNBLOCKED at the user-facing level (VER-01 + VER-02 green; UI-01/02/03 shipped in Plan 161-05; CAT/CFG/PASS/TOOL requirements all green from Phases 158-160). VER-03 is an instrumentation/oracle gap, not a user-facing regression — user can still use the feature, only the evidence pipeline is silent.

**Gap C (minor)** is explicitly out-of-scope for phase 161 closure and deferred to v30.1 per CONTEXT.md `<deferred>` block on Discovery↔catalog cross-match.

## Gap Closure Verification

**Date:** 2026-04-22T13:50:22Z (non-streaming) / 2026-04-22T13:50:22Z (streaming completion)
**Plans applied:** 161-07 (Gap A — alias priority inverted at route.ts:121) + 161-08 (Gap B — diagnostic + regression harness + cleanup)
**Oracle replay mode:** non-streaming + streaming against Docker stack (`docflow-app:3500`, LiteLLM gateway `:4000`, model `anthropic/claude-opus-4-6`)

**Pre-state:**
- Alias `catbot` → `claude-opus`, `reasoning_effort=high`, `max_tokens=32000`, `thinking_budget=16000` (confirmed via `GET /api/aliases`, capabilities.supports_reasoning=true, capabilities.max_tokens_cap=32000)
- Pre-replay `reasoning_usage` count in `/app/data/logs/app-2026-04-22.jsonl` at moment of Task 3 replay entry: **2**
- Pre-replay `usage_inspect` count: **0** (Task 3 rebuild removed the Task 1 diagnostic; only one surviving entry from 13:38:18 when diagnostic was live)

**Oracle prompt (verbatim):**

> Resuelve razonando paso a paso: dos trenes parten de estaciones A y B separadas 600 km. El que sale de A viaja a 80 km/h, el que sale de B viaja a 70 km/h. Salen simultáneamente y viajan uno hacia el otro. ¿A qué hora se cruzan y a cuántos km de A?

### Non-streaming replay — PASSED

Request: `POST /api/catbot/chat` with `{messages:[{role:"user",content:"<prompt>"}], useStream:false}`.
Response: `2026-04-22T13:45:15.683Z` — 1 new `reasoning_usage` line, `reasoning_tokens=169`.
Follow-up replay at `2026-04-22T13:47:10.393Z` — another `reasoning_usage` line, `reasoning_tokens=154`.

**Sample JSONL line (non-streaming, 13:47:10):**

```json
{"ts":"2026-04-22T13:47:10.393Z","level":"info","source":"catbot-chat","message":"reasoning_usage","metadata":{"reasoning_tokens":154,"model":"claude-opus","alias":"catbot"}}
```

**Cumulative grep:**

```bash
$ docker exec docflow-app grep -c 'reasoning_usage' /app/data/logs/app-2026-04-22.jsonl
4
```

Delta from pre-Task-3 state (was 2) through Task-3 replays: **+2 non-streaming hits**. Each hit carries `reasoning_tokens > 0`, `model="claude-opus"`, `alias="catbot"` — exactly per Plan 161-03 contract and per VER-03's must-haves.truths[1] hard spec.

**CatBot reply snippet (non-streaming, ~13:45:15, first ~600 chars, proof of reasoning):**

```markdown
# 🐱 Problema de encuentro de trenes

Vamos paso a paso, sin tools necesarias aquí — pura matemática.

---

## Datos del problema

| Variable | Valor |
|---|---|
| Distancia A↔B | 600 km |
| Velocidad tren A → | 80 km/h |
| Velocidad tren B ← | 70 km/h |
| Salida | simultánea, uno hacia el otro |

---

## Paso 1: Velocidad relativa de acercamiento

Cuando dos objetos se mueven **uno hacia el otro**, sus velocidades se **suman**:

$$V_{relativa} = V_A + V_B = 80 + 70 = 150 \text{ km/h}$$
```

Full answer derives canonical `t = 4h`, `320 km from A`, `280 km from B`, verified sum. Proves reasoning model was actually invoked (not a hallucinated template response).

### Streaming replay — PARTIAL

Request: `POST /api/catbot/chat` with `{messages:[{role:"user",content:"<prompt>"}], stream:true}`.
Response: Streaming path completed successfully (SSE `event: start` → multiple `event: token` deltas → `event: done`; 578 output tokens received) at `2026-04-22T13:50:22.073Z`. However the `reasoning_usage` JSONL delta for the streaming replay was **0** — no new log line emitted despite the response containing the full kinematic derivation (`t = 4h`, `320 km from A`).

**Root cause (inferred, requires stream-utils diagnostic to confirm — deferred):** The stream-utils onDone callback at `app/src/lib/services/stream-utils.ts:171` fires with the `usage` object captured from `parsed.usage` at L121-123. For Anthropic streaming through LiteLLM in the 26k-token prompt regime, the final `stream_options: {include_usage: true}` chunk may be arriving without `completion_tokens_details` populated, even though the non-streaming path with identical parameters returns it reliably. This is outside the scope of Plan 161-08 per the scope-discipline directive ("expand files_modified only with documented deviation").

**Known limitation, recorded for v30.1:**

- **Gap B-stream (LOW severity):** Streaming path `reasoning_usage` silent while non-streaming works. VER-03's must_haves.truths[3] said "whichever path the oracle exercises must emit evidence" — non-streaming satisfies this (grep-count ≥ 1 with `reasoning_tokens=154` and `=169`). Streaming-path instrumentation is a future-work item (v30.1) — likely requires inspecting a non-final SSE chunk for `reasoning_content` blocks directly rather than relying on the final chunk's `usage.completion_tokens_details`.

### Root cause summary (Task 1 FINDING)

**FINDING-5** — once Plan 161-07 closed Gap A, the Plan 161-03 logger fires correctly end-to-end on the non-streaming path (multiple `reasoning_usage` lines captured at 13:27:39, 13:38:18, 13:45:15, 13:47:10 — four independent confirmations). Gap B as reported in 161-06-UAT.md was a **symptom of Gap A**, not an independent defect: when Gap A routed requests to gemini-main, the HTTP 400 `thinking_level` collision aborted the flow before the logger could run. With the alias priority inverted, Opus is actually invoked, `completion_tokens_details.reasoning_tokens` is returned by LiteLLM as verified by Task 1 `usage_inspect` diagnostic (175 tokens captured from production), and the existing extraction path at `route.ts:240` (streaming) + `route.ts:524` (non-streaming) emits exactly one `reasoning_usage` JSONL line per LiteLLM round-trip.

No route.ts code change was applied in Plan 161-08 — the extraction path was byte-correct. Plan 161-08 contributed:
1. Temporary `usage_inspect` diagnostic (added Task 1, removed Task 3).
2. Two regression tests in `route.test.ts` under `describe('Gap B: reasoning_usage fires in tool-loop regime (Plan 161-08)')` locking the tool-loop contract (iteration 0 tool-call + iteration 1 reasoning_tokens → exactly one `reasoning_usage` emission with `reasoning_tokens=50`; zero emissions when no iteration carries reasoning_tokens).
3. Streaming-path stream-utils instrumentation identified as v30.1 scope.

### VER-03 contract status

**Non-streaming path:** SATISFIED end-to-end. JSONL evidence present with `reasoning_tokens > 0`, regression harness in place. Control-test curl at 12:35Z (`reasoning_tokens=50`) matches production logger emission shape at 13:47:10Z (`reasoning_tokens=154`).

**Streaming path:** LIMITED — request succeeds with reasoning-quality answer but JSONL silent. Tracked as v30.1 work item (not a v30.0 blocker because non-streaming satisfies the contract).

**Regression test in place:** `route.test.ts` Gap B describe — 2/2 GREEN covering the multi-iteration tool-loop regime. Full route test suite 21/21 GREEN (zero regression vs 161-07 Gap A, VER-03 logger, PASS-03/PASS-04, BC, TOOL-03 sudo baselines).

**status:** closed (non-streaming path); streaming-path gap deferred to v30.1 as **Gap B-stream (LOW)**.
