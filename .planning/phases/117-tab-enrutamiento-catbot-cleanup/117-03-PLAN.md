---
phase: 117-tab-enrutamiento-catbot-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/settings/model-center/tab-enrutamiento.tsx
autonomous: true
gap_closure: true
requirements:
  - ROUTING-01
  - ROUTING-02
  - ROUTING-03
  - ROUTING-04
  - CATBOT-01
  - CATBOT-02
  - CATBOT-03

must_haves:
  truths:
    - "El dropdown de modelo muestra modelos de proveedores conectados con estilo normal y modelos de proveedores desconectados en gris con warning icon"
    - "Al seleccionar un modelo de proveedor desconectado aparece dialogo de confirmacion; al seleccionar modelo de proveedor conectado se aplica directamente"
  artifacts:
    - path: "app/src/components/settings/model-center/tab-enrutamiento.tsx"
      provides: "TabEnrutamiento with correct ProviderHealth field mapping"
      contains: "provider: string"
  key_links:
    - from: "tab-enrutamiento.tsx connectedProviders useMemo"
      to: "/api/models/health response"
      via: "p.provider field access"
      pattern: "map.*p\\.provider"
---

<objective>
Fix the ProviderHealth interface field name mismatch in tab-enrutamiento.tsx that causes connectedProviders Set to always be empty.

Purpose: The local ProviderHealth interface uses `name` but the API returns `provider`. This makes all models appear as unavailable (ROUTING-02 blocked) and the confirmation dialog fire for every model change (ROUTING-04 blocked).
Output: Corrected field mappings so connectedProviders works correctly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-tab-enrutamiento-catbot-cleanup/117-VERIFICATION.md

<interfaces>
<!-- The API response from /api/models/health uses these field names (from health.ts): -->
ProviderHealth from API:
- provider: string (NOT "name")
- status: 'connected' | 'error'
- model_count: number (NOT "models_count")
- latency_ms: number
- error: string | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ProviderHealth interface field names to match API response</name>
  <files>app/src/components/settings/model-center/tab-enrutamiento.tsx</files>
  <action>
Three targeted changes in tab-enrutamiento.tsx:

1. **Line 49**: In the local `ProviderHealth` interface, change `name: string` to `provider: string`
2. **Line 51**: In the same interface, change `models_count: number` to `model_count: number` (consistency fix)
3. **Line 127**: In the `connectedProviders` useMemo, change `.map((p) => p.name)` to `.map((p) => p.provider)`

Do NOT change anything else. These 3 lines are the complete fix. The AliasHealth interface and all other code is correct.

Root cause: The local interface was defined with wrong field names that don't match what `/api/models/health` actually returns. At runtime `p.name` is `undefined`, so `connectedProviders` Set is always empty.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && grep -n "provider: string" app/src/components/settings/model-center/tab-enrutamiento.tsx | head -5 && grep -n "model_count:" app/src/components/settings/model-center/tab-enrutamiento.tsx | head -5 && grep -n "p\.provider" app/src/components/settings/model-center/tab-enrutamiento.tsx | head -5 && echo "--- Confirm no p.name remnants ---" && grep -n "p\.name" app/src/components/settings/model-center/tab-enrutamiento.tsx | head -5 && echo "--- Build check ---" && cd app && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ProviderHealth interface has `provider: string` (not `name`)
    - ProviderHealth interface has `model_count: number` (not `models_count`)
    - connectedProviders useMemo uses `p.provider` (not `p.name`)
    - Build passes without errors
    - connectedProviders will now correctly contain connected provider names at runtime
  </done>
</task>

</tasks>

<verification>
1. grep confirms `provider: string` in ProviderHealth interface (not `name`)
2. grep confirms `p.provider` in useMemo (not `p.name`)
3. `next build` passes without type errors
4. Human verification: Navigate to /settings?tab=enrutamiento, open a model dropdown — connected provider models should appear normal, disconnected provider models should appear grey with warning icon
</verification>

<success_criteria>
- The 3 lines are changed and build passes
- ROUTING-02 unblocked: dropdown correctly distinguishes available vs unavailable models
- ROUTING-04 unblocked: confirmation dialog only appears for unavailable model selections
</success_criteria>

<output>
After completion, create `.planning/phases/117-tab-enrutamiento-catbot-cleanup/117-03-SUMMARY.md`
</output>
