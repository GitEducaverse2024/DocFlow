---
phase: 114-centro-de-modelos-shell-tab-resumen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/settings/model-center/model-center-shell.tsx
  - app/src/components/settings/model-center/tab-resumen-placeholder.tsx
  - app/src/components/settings/model-center/tab-proveedores-placeholder.tsx
  - app/src/components/settings/model-center/tab-modelos-placeholder.tsx
  - app/src/components/settings/model-center/tab-enrutamiento-placeholder.tsx
  - app/src/app/settings/page.tsx
  - app/messages/es.json
  - app/messages/en.json
autonomous: true
requirements: [TABS-01, TABS-02, TABS-03, TABS-04]

must_haves:
  truths:
    - "Settings page shows a 'Centro de Modelos' section replacing the old dispersed sections (API Keys, ModelIntelligence, ModelPricing, Embeddings)"
    - "Four tabs are visible: Resumen, Proveedores, Modelos, Enrutamiento"
    - "Clicking a tab updates the URL query param (?tab=resumen, ?tab=proveedores, etc.)"
    - "Navigating directly to /settings?tab=modelos opens the Modelos tab"
    - "All new UI strings have translations in es.json and en.json"
  artifacts:
    - path: "app/src/components/settings/model-center/model-center-shell.tsx"
      provides: "Tab shell with URL persistence and 4 tab panels"
      exports: ["ModelCenterShell"]
    - path: "app/src/app/settings/page.tsx"
      provides: "Settings page with ModelCenterShell replacing old sections"
    - path: "app/messages/es.json"
      provides: "Spanish translations for modelCenter namespace"
      contains: "modelCenter"
    - path: "app/messages/en.json"
      provides: "English translations for modelCenter namespace"
      contains: "modelCenter"
  key_links:
    - from: "app/src/app/settings/page.tsx"
      to: "app/src/components/settings/model-center/model-center-shell.tsx"
      via: "import ModelCenterShell"
      pattern: "ModelCenterShell"
    - from: "app/src/components/settings/model-center/model-center-shell.tsx"
      to: "URL query params"
      via: "useSearchParams + useRouter for tab persistence"
      pattern: "searchParams.*tab"
---

<objective>
Create the "Centro de Modelos" tab shell in Settings that replaces the old dispersed sections (API Keys, Model Intelligence, Model Pricing, Embeddings) with a single section containing 4 navigable tabs. Tab active state persists in URL for deep linking. Placeholder content in non-Resumen tabs.

Purpose: Foundation for all subsequent model management UI (phases 114-02, 115, 116, 117)
Output: ModelCenterShell component, settings page rewired, i18n keys
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-health-api/113-01-SUMMARY.md

<interfaces>
<!-- Tabs component from shadcn/ui (already installed) -->
From app/src/components/ui/tabs.tsx:
```typescript
// Uses @base-ui/react tabs primitives
export { Tabs, TabsList, TabsTrigger, TabsContent, tabsListVariants }
// Tabs accepts: orientation, className
// TabsTrigger accepts: value (implicit via children ordering), className
// TabsContent accepts: value (implicit), className
// NOTE: base-ui tabs use index-based or value-based tab selection
```

From app/src/lib/services/health.ts:
```typescript
export interface ProviderHealth {
  provider: string;
  status: 'connected' | 'error';
  latency_ms: number | null;
  model_count: number;
  error: string | null;
}

export interface AliasHealth {
  alias: string;
  configured_model: string;
  resolved_model: string | null;
  resolution_status: 'direct' | 'fallback' | 'error';
  error: string | null;
  latency_ms: number;
}

export interface HealthResult {
  providers: ProviderHealth[];
  aliases: AliasHealth[];
  checked_at: string;
  cached: boolean;
}
```

<!-- Current Settings page sections to replace (lines 1683-1738 of page.tsx): -->
<!-- - Section: API Keys (line 1683) -->
<!-- - Section: Model Intelligence (line 1710) -->
<!-- - Section: Model Pricing (line 1713) -->
<!-- - Section: Embeddings (line 1724-1738) -->
<!-- Keep: ProcessingSettings, CatBotSettings, CatBotSecurity, TelegramSettings, Connections, Preferences -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModelCenterShell with tab navigation and URL persistence</name>
  <files>
    app/src/components/settings/model-center/model-center-shell.tsx,
    app/src/components/settings/model-center/tab-resumen-placeholder.tsx,
    app/src/components/settings/model-center/tab-proveedores-placeholder.tsx,
    app/src/components/settings/model-center/tab-modelos-placeholder.tsx,
    app/src/components/settings/model-center/tab-enrutamiento-placeholder.tsx
  </files>
  <action>
    Create `app/src/components/settings/model-center/model-center-shell.tsx`:

    1. 'use client' component that imports Tabs, TabsList, TabsTrigger, TabsContent from `@/components/ui/tabs`
    2. Uses `useSearchParams()` from next/navigation to read `?tab=` param and `useRouter()` to update it
    3. Default tab: 'resumen' if no param present
    4. Valid tabs: 'resumen', 'proveedores', 'modelos', 'enrutamiento'
    5. On tab change: `router.replace(\`/settings?tab=\${newTab}\`, { scroll: false })` to persist in URL without scroll
    6. Uses `useTranslations('settings.modelCenter')` for all labels
    7. Section header: Cpu icon + "Centro de Modelos" title + description
    8. Tab bar styled for dark theme: bg-zinc-900/80 border border-zinc-800, active tab violet highlight
    9. Each TabsContent renders a placeholder component for now (simple Card with "Coming soon" text)
    10. Tab Resumen placeholder imports nothing special yet - just shows placeholder text (Plan 02 will replace it)

    IMPORTANT: Check how base-ui Tabs work. The shadcn tabs.tsx uses `@base-ui/react/tabs` which is index-based by default. Use the `value` prop approach if available, or use index mapping: resumen=0, proveedores=1, modelos=2, enrutamiento=3. Read the tabs.tsx source to confirm the API before implementing.

    Create 4 simple placeholder components in the same directory:
    - `tab-resumen-placeholder.tsx` - "Resumen placeholder" (replaced in Plan 02)
    - `tab-proveedores-placeholder.tsx` - "Proveedores - coming in Phase 115"
    - `tab-modelos-placeholder.tsx` - "Modelos - coming in Phase 116"
    - `tab-enrutamiento-placeholder.tsx` - "Enrutamiento - coming in Phase 117"

    Each placeholder: 'use client', imports Card/CardContent, shows centered text in zinc-400.

    Dark theme conventions (from CODING_RULES):
    - Background: bg-zinc-950
    - Cards: bg-zinc-900/80 border border-zinc-800
    - Hover: hover:border-violet-800/30
    - Active: violet-500
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <done>ModelCenterShell component exists with 4 tabs, URL param persistence via useSearchParams, placeholder panels for each tab. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire Settings page and add i18n translations</name>
  <files>
    app/src/app/settings/page.tsx,
    app/messages/es.json,
    app/messages/en.json
  </files>
  <action>
    **Settings page (app/src/app/settings/page.tsx):**

    1. Import ModelCenterShell from `@/components/settings/model-center/model-center-shell`
    2. REPLACE these sections with a single `<ModelCenterShell />`:
       - Section "API Keys" (lines ~1683-1704): the provider cards with Key icon — REMOVE entire section
       - `<ModelIntelligenceSection />` (line 1710) — REMOVE
       - `<ModelPricingSettings />` (line 1713) — REMOVE
       - Section "Embeddings" (lines ~1724-1738) — REMOVE
    3. KEEP these sections untouched (in their current order after ModelCenterShell):
       - `<ProcessingSettings />` (line 1707) — KEEP, move BEFORE ModelCenterShell
       - `<CatBotSettings />` — KEEP
       - `<CatBotSecurity />` — KEEP
       - `<TelegramSettings />` — KEEP
       - Section "Connections" — KEEP
       - Section "Preferences" — KEEP
    4. The ProviderCard component and PROVIDER_META can remain in the file for now (Phase 115 will move them into the Proveedores tab). They just won't be rendered in the main settings page anymore.
    5. Remove unused imports if any (ModelIntelligenceSection import).

    **i18n es.json — add under `settings` key a new `modelCenter` object:**
    ```json
    "modelCenter": {
      "title": "Centro de Modelos",
      "description": "Gestiona proveedores, modelos, costes y routing desde un solo lugar",
      "tabs": {
        "resumen": "Resumen",
        "proveedores": "Proveedores",
        "modelos": "Modelos",
        "enrutamiento": "Enrutamiento"
      },
      "placeholder": {
        "resumen": "Dashboard de salud del ecosistema",
        "proveedores": "Gestion de proveedores y API keys",
        "modelos": "Fichas MID y clasificacion de modelos",
        "enrutamiento": "Tabla de routing de aliases"
      }
    }
    ```

    **i18n en.json — add equivalent under `settings`:**
    ```json
    "modelCenter": {
      "title": "Model Center",
      "description": "Manage providers, models, costs and routing from one place",
      "tabs": {
        "resumen": "Overview",
        "proveedores": "Providers",
        "modelos": "Models",
        "enrutamiento": "Routing"
      },
      "placeholder": {
        "resumen": "Ecosystem health dashboard",
        "proveedores": "Provider and API key management",
        "modelos": "MID cards and model classification",
        "enrutamiento": "Alias routing table"
      }
    }
    ```

    Do NOT remove the existing `modelIntelligence` i18n keys (other components still reference them until Phase 115-117 clean up).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Settings page renders ModelCenterShell instead of dispersed sections. Build succeeds. Navigating to /settings shows "Centro de Modelos" with 4 tabs. URL updates on tab click.</done>
</task>

</tasks>

<verification>
1. `cd /home/deskmath/docflow/app && npm run build` completes without errors
2. Settings page has a "Centro de Modelos" section with 4 visible tabs
3. Clicking tabs updates URL to ?tab=resumen, ?tab=proveedores, etc.
4. Direct navigation to /settings?tab=modelos opens the correct tab
5. Old sections (API Keys standalone, Embeddings, separate Model Intelligence, Model Pricing) are gone
6. ProcessingSettings, CatBot, Telegram, Connections, Preferences sections remain
7. es.json and en.json both contain `settings.modelCenter` keys
</verification>

<success_criteria>
- Build passes with zero errors
- 4 tabs navigable with URL deep linking
- Old dispersed sections replaced by unified Centro de Modelos
- i18n complete in both languages
</success_criteria>

<output>
After completion, create `.planning/phases/114-centro-de-modelos-shell-tab-resumen/114-01-SUMMARY.md`
</output>
