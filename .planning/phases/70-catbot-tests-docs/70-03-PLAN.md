---
phase: 70-catbot-tests-docs
plan: 03
type: execute
wave: 2
depends_on: ["70-01", "70-02"]
files_modified:
  - app/messages/es.json
  - app/messages/en.json
autonomous: true
requirements: [BUILD-01, BUILD-02]

must_haves:
  truths:
    - "All new UI text added in phases 63-70 has i18n keys in both es.json and en.json"
    - "npm run build passes without TypeScript errors"
    - "No hardcoded Spanish strings exist in new components without i18n keys"
  artifacts:
    - path: "app/messages/es.json"
      provides: "Spanish translations including all catflow, scheduler, storage, multiagent keys"
      contains: "catflow"
    - path: "app/messages/en.json"
      provides: "English translations matching es.json keys"
      contains: "catflow"
  key_links:
    - from: "app/messages/es.json"
      to: "app/messages/en.json"
      via: "matching key structure"
      pattern: "catflow"
---

<objective>
Audit i18n keys for completeness across es.json and en.json, fix any missing keys, and validate clean build.

Purpose: Ensure all new UI text from v16.0 has proper i18n coverage and the project builds cleanly.
Output: Updated es.json/en.json with any missing keys, verified clean build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@app/messages/es.json
@app/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix i18n keys</name>
  <files>app/messages/es.json, app/messages/en.json</files>
  <action>
Run a systematic i18n audit:

**Step 1: Extract all t() calls from new v16.0 components.**
Search for `t('` patterns in these directories/files added or modified in phases 63-70:
- `app/src/components/canvas/nodes/` (scheduler-node, storage-node, multiagent-node, start-node, output-node)
- `app/src/components/canvas/panels/` (config panels for new nodes)
- `app/src/components/canvas/canvas-editor.tsx` (copy/paste toasts, sidebar panel)
- `app/src/app/catflow/` (CatFlow page components)
- Any files referencing catflow namespace

Use: `grep -rn "t('" app/src/components/canvas/nodes/ app/src/components/canvas/panels/ app/src/app/catflow/ 2>/dev/null | grep -oP "t\('[^']+'\)" | sort -u`

Also check: `grep -rn "useTranslations\|getTranslations" app/src/components/canvas/ app/src/app/catflow/ 2>/dev/null` to find which namespaces are used.

**Step 2: Cross-reference with es.json and en.json.**
For each namespace.key found in Step 1, verify it exists in both JSON files. List any missing keys.

**Step 3: For each missing key:**
- Add to es.json with the Spanish text (derive from component context or hardcoded strings nearby)
- Add to en.json with the English translation
- Ensure the key structure matches (same nesting level, same namespace)

**Step 4: Verify key parity.**
Run: `node -e "const es=Object.keys(JSON.flatten(require('./messages/es.json'))); const en=Object.keys(JSON.flatten(require('./messages/en.json'))); const missing=es.filter(k=>!en.includes(k)); console.log('Missing in en:', missing.length);"` (or similar approach with jq)

More practically: compare top-level namespace keys between es.json and en.json. For any namespace that exists in one but not the other, add it.

If no keys are missing (likely since phases 63-69 summaries indicate i18n was done per-plan), confirm the audit passed with no changes needed.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && node -e "const es=require('./messages/es.json'); const en=require('./messages/en.json'); const esK=Object.keys(es); const enK=Object.keys(en); const missingInEn=esK.filter(k=>!enK.includes(k)); const missingInEs=enK.filter(k=>!esK.includes(k)); console.log('Missing in en:', missingInEn); console.log('Missing in es:', missingInEs); if(missingInEn.length+missingInEs.length>0) process.exit(1);"</automated>
  </verify>
  <done>All top-level namespace keys match between es.json and en.json; no missing i18n keys for v16.0 components</done>
</task>

<task type="auto">
  <name>Task 2: Validate clean build</name>
  <files></files>
  <action>
Run `npm run build` in the app directory to validate the entire project builds cleanly after all v16.0 changes.

```bash
cd /home/deskmath/docflow/app && npm run build
```

If any errors occur:
1. **TypeScript errors**: Fix type issues in the files indicated
2. **Import errors**: Fix missing imports or incorrect paths
3. **i18n missing key warnings**: Add missing keys found in Task 1 approach
4. **ESLint errors**: Fix or add appropriate eslint-disable comments (consistent with project pattern of using eslint-disable-next-line for no-explicit-any on DB queries)

Re-run build until it passes cleanly.

Note: The build runs against the local app directory. It should complete without errors. If there are pre-existing warnings (not errors), those are acceptable.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build exits with code 0, no TypeScript errors, no build failures</done>
</task>

</tasks>

<verification>
- i18n parity check passes (Task 1 verify command)
- `npm run build` passes with exit code 0
</verification>

<success_criteria>
- All v16.0 UI text has i18n keys in both es.json and en.json
- npm run build passes without errors
- Phase 70 is complete, v16.0 CatFlow milestone ready
</success_criteria>

<output>
After completion, create `.planning/phases/70-catbot-tests-docs/70-03-SUMMARY.md`
</output>
