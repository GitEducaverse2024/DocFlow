---
phase: 118-foundation-catbot-db-knowledge-tree
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/knowledge/_index.json
  - app/data/knowledge/catboard.json
  - app/data/knowledge/catbrains.json
  - app/data/knowledge/catpaw.json
  - app/data/knowledge/catflow.json
  - app/data/knowledge/canvas.json
  - app/data/knowledge/catpower.json
  - app/data/knowledge/settings.json
  - app/src/lib/knowledge-tree.ts
  - app/src/lib/__tests__/knowledge-tree.test.ts
autonomous: true
requirements: [INFRA-03, INFRA-04, INFRA-05]

must_haves:
  truths:
    - "Existen 7 archivos JSON de conocimiento + _index.json en app/data/knowledge/"
    - "Cada JSON sigue el schema: id, name, path, description, endpoints, tools, concepts, howto, dont, common_errors, success_cases, sources"
    - "Todo el contenido de FEATURE_KNOWLEDGE y las secciones relevantes de buildSystemPrompt() estan migrados a los JSONs"
    - "El loader knowledge-tree.ts puede cargar y validar los JSONs con zod"
  artifacts:
    - path: "app/data/knowledge/_index.json"
      provides: "Indice de todos los archivos de conocimiento"
      contains: "catboard"
    - path: "app/data/knowledge/catbrains.json"
      provides: "Conocimiento de CatBrains"
      contains: "catbrains"
    - path: "app/src/lib/knowledge-tree.ts"
      provides: "Loader con schema zod y funciones de lectura"
      exports: ["loadKnowledgeIndex", "loadKnowledgeArea", "getAllKnowledgeAreas", "KnowledgeEntry", "KnowledgeIndex"]
    - path: "app/src/lib/__tests__/knowledge-tree.test.ts"
      provides: "Tests de existencia, schema y cobertura"
      min_lines: 50
  key_links:
    - from: "app/src/lib/knowledge-tree.ts"
      to: "app/data/knowledge/*.json"
      via: "fs.readFileSync + JSON.parse + zod validation"
      pattern: "readFileSync.*knowledge"
    - from: "app/data/knowledge/*.json"
      to: "FEATURE_KNOWLEDGE entries"
      via: "content migration"
      pattern: "tools.*endpoints.*concepts"
---

<objective>
Crear el arbol de conocimiento JSON que reemplaza FEATURE_KNOWLEDGE y las secciones hardcodeadas del system prompt, con un loader validado por zod.

Purpose: Separar el contenido de conocimiento del codigo, haciendolo estructurado, versionable y consultable por PromptAssembler (Phase 119).
Output: 7 JSON files + _index.json + knowledge-tree.ts loader + tests green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-foundation-catbot-db-knowledge-tree/118-RESEARCH.md

@app/src/lib/services/catbot-tools.ts (FEATURE_KNOWLEDGE a migrar, lineas 789-923)
@app/src/app/api/catbot/chat/route.ts (buildSystemPrompt() a descomponer, lineas 42-348)

<interfaces>
<!-- Schema del knowledge tree definido en RESEARCH.md -->
```typescript
interface KnowledgeEntry {
  id: string;           // unique key, e.g. "catbrains"
  name: string;         // display name, e.g. "CatBrains"
  path: string;         // URL path, e.g. "/catbrains"
  description: string;  // 1-2 paragraph description
  endpoints: string[];  // relevant API endpoints
  tools: string[];      // CatBot tools related to this area
  concepts: string[];   // key concepts
  howto: string[];      // step-by-step guides
  dont: string[];       // anti-patterns
  common_errors: Array<{ error: string; cause: string; solution: string }>;
  success_cases: string[];
  sources: string[];    // paths to .planning/ docs
}

interface KnowledgeIndex {
  version: string;
  updated: string;
  areas: Array<{ id: string; file: string; name: string; description: string }>;
}
```

<!-- Mapping de FEATURE_KNOWLEDGE keys a JSON files (del RESEARCH.md) -->
catbrains, proyectos, rag -> catbrains.json
agentes, catpaws, workers -> catpaw.json
tareas, catflow, iterator, reglas_canvas -> catflow.json
canvas (from buildSystemPrompt) -> canvas.json
conectores, gmail, holded, mcp, openclaw, linkedin, searxng, websearch, skills, templates, catpower -> catpower.json
catboard, dashboard -> catboard.json
centro_de_modelos, modelos, enrutamiento, cattools -> settings.json
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests + knowledge-tree.ts loader con schema zod</name>
  <files>app/src/lib/__tests__/knowledge-tree.test.ts, app/src/lib/knowledge-tree.ts</files>
  <behavior>
    - Test "files exist": _index.json y los 7 JSON files existen en app/data/knowledge/
    - Test "schema": cada JSON pasa validacion zod del KnowledgeEntry schema
    - Test "index valid": _index.json tiene version, updated, y areas con 7 entradas que apuntan a archivos reales
    - Test "coverage": todas las keys de FEATURE_KNOWLEDGE estan cubiertas por al menos un JSON (verificar que tools[] o concepts[] mencionen las keys)
    - Test "loader functions": loadKnowledgeIndex() y loadKnowledgeArea(id) retornan datos correctos
  </behavior>
  <action>
    1. Crear tests primero (RED):
       - Los tests deben verificar los files en disco (no mocks) para asegurar integridad real
       - El test de coverage debe importar o tener la lista de FEATURE_KNOWLEDGE keys y verificar que cada una esta cubierta en algun JSON

    2. Crear `app/src/lib/knowledge-tree.ts`:
       - Definir zod schemas: KnowledgeEntrySchema y KnowledgeIndexSchema
       - Exportar tipos TypeScript derivados de los schemas
       - `loadKnowledgeIndex()`: lee _index.json, valida con zod, cachea en memoria (module-level cache)
       - `loadKnowledgeArea(id: string)`: lee {id}.json, valida con zod, cachea en memoria
       - `getAllKnowledgeAreas()`: lee todas las areas del indice y retorna array de KnowledgeEntry
       - Usar `path.join(process.cwd(), 'data', 'knowledge', ...)` para las rutas
       - Cache a nivel de modulo (const cache = new Map()) para evitar relectura en cada request

    3. Ejecutar tests -- deben fallar porque los JSON files no existen aun.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <done>knowledge-tree.ts con schema zod y funciones de lectura. Tests escritos (fallaran hasta que Task 2 cree los JSONs).</done>
</task>

<task type="auto">
  <name>Task 2: Crear 7 JSON files de conocimiento + _index.json con contenido migrado</name>
  <files>app/data/knowledge/_index.json, app/data/knowledge/catboard.json, app/data/knowledge/catbrains.json, app/data/knowledge/catpaw.json, app/data/knowledge/catflow.json, app/data/knowledge/canvas.json, app/data/knowledge/catpower.json, app/data/knowledge/settings.json</files>
  <action>
    1. Leer FEATURE_KNOWLEDGE completo de `app/src/lib/services/catbot-tools.ts` (lineas 789-923) para extraer todo el contenido.

    2. Leer buildSystemPrompt() completo de `app/src/app/api/catbot/chat/route.ts` (lineas 42-348) para extraer secciones de conocimiento.

    3. Crear `app/data/knowledge/` directorio si no existe.

    4. Crear cada JSON file siguiendo el schema KnowledgeEntry. Distribuir contenido segun el mapping del RESEARCH.md:

       **catboard.json**: Dashboard, health checks, metricas del sistema. FEATURE_KNOWLEDGE keys: catboard, dashboard.

       **catbrains.json**: Proyectos, fuentes, procesamiento, RAG. FEATURE_KNOWLEDGE keys: catbrains, proyectos, rag. Ejemplo completo en RESEARCH.md.

       **catpaw.json**: Agentes, workers, habilidades de agentes. FEATURE_KNOWLEDGE keys: agentes, catpaws, workers.

       **catflow.json**: Tareas, CatFlow pipelines, iteradores, reglas de canvas (lo operativo). FEATURE_KNOWLEDGE keys: tareas, catflow, iterator, reglas_canvas.

       **canvas.json**: Editor visual de canvas, nodos, ejecucion. Content from buildSystemPrompt() canvas sections (lineas 220-348). FEATURE_KNOWLEDGE keys: canvas-related.

       **catpower.json**: Skills, conectores, templates. FEATURE_KNOWLEDGE keys: conectores, gmail, holded, mcp, openclaw, linkedin, searxng, websearch, skills, templates, catpower.

       **settings.json**: Centro de modelos, enrutamiento, configuracion, cattools. FEATURE_KNOWLEDGE keys: centro_de_modelos, modelos, enrutamiento, cattools. Content from buildSystemPrompt() model intelligence section.

    5. Crear `_index.json` con:
       ```json
       {
         "version": "1.0.0",
         "updated": "2026-04-08",
         "areas": [
           { "id": "catboard", "file": "catboard.json", "name": "CatBoard", "description": "Dashboard y health checks" },
           { "id": "catbrains", "file": "catbrains.json", "name": "CatBrains", "description": "Proyectos, fuentes, procesamiento y RAG" },
           { "id": "catpaw", "file": "catpaw.json", "name": "CatPaw", "description": "Agentes y workers" },
           { "id": "catflow", "file": "catflow.json", "name": "CatFlow", "description": "Tareas y pipelines" },
           { "id": "canvas", "file": "canvas.json", "name": "Canvas", "description": "Editor visual y ejecucion" },
           { "id": "catpower", "file": "catpower.json", "name": "CatPower", "description": "Skills, conectores y templates" },
           { "id": "settings", "file": "settings.json", "name": "Settings", "description": "Centro de modelos y configuracion" }
         ]
       }
       ```

    6. Cada JSON DEBE tener todos los campos del schema (id, name, path, description, endpoints, tools, concepts, howto, dont, common_errors, success_cases, sources). Si un campo no tiene contenido relevante, usar array vacio `[]`.

    7. Los `sources` deben apuntar a archivos reales de `.planning/` cuando existan. No inventar paths.

    8. El contenido debe estar en espanol (consistente con la plataforma).

    9. Correr los tests de Task 1 -- ahora deben pasar todos (GREEN).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts --reporter=verbose</automated>
  </verify>
  <done>7 JSON files + _index.json creados con contenido migrado de FEATURE_KNOWLEDGE y buildSystemPrompt(). Schema validado por zod. Test de cobertura confirma que todas las FEATURE_KNOWLEDGE keys estan cubiertas.</done>
</task>

</tasks>

<verification>
- `cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts --reporter=verbose` -- todos los tests pasan
- `cd /home/deskmath/docflow/app && npm run build` -- build no se rompe
- Verificar manualmente que los JSON files tienen contenido sustancial (no placeholders vacios)
</verification>

<success_criteria>
- 7 JSON files + _index.json existen en app/data/knowledge/
- Cada JSON pasa validacion zod del schema KnowledgeEntry
- FEATURE_KNOWLEDGE keys estan cubiertas por los JSONs
- Secciones relevantes de buildSystemPrompt() migradas a los JSONs
- knowledge-tree.ts loader funciona con cache en memoria
- Tests green
</success_criteria>

<output>
After completion, create `.planning/phases/118-foundation-catbot-db-knowledge-tree/118-02-SUMMARY.md`
</output>
