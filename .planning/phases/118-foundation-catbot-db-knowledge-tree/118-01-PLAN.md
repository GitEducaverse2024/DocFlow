---
phase: 118-foundation-catbot-db-knowledge-tree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/catbot-db.ts
  - app/src/lib/__tests__/catbot-db.test.ts
autonomous: true
requirements: [INFRA-01, INFRA-02]

must_haves:
  truths:
    - "catbot.db se crea automaticamente en app/data/ al importar catbot-db.ts"
    - "Las 5 tablas existen con el schema correcto: user_profiles, user_memory, conversation_log, summaries, knowledge_learned"
    - "Las funciones CRUD exportadas permiten insertar, leer, actualizar y borrar en todas las tablas"
  artifacts:
    - path: "app/src/lib/catbot-db.ts"
      provides: "catbot.db connection + migrations + CRUD functions"
      exports: ["default (catbotDb)", "saveConversation", "getConversations", "getConversation", "deleteConversation", "getProfile", "upsertProfile", "saveMemory", "getMemories", "saveSummary", "getSummaries", "saveLearnedEntry", "getLearnedEntries"]
    - path: "app/src/lib/__tests__/catbot-db.test.ts"
      provides: "Unit tests for DB creation and CRUD"
      min_lines: 80
  key_links:
    - from: "app/src/lib/catbot-db.ts"
      to: "better-sqlite3"
      via: "Database constructor with WAL mode"
      pattern: "new Database.*catbot\\.db"
    - from: "app/src/lib/catbot-db.ts"
      to: "@/lib/utils"
      via: "generateId() for IDs"
      pattern: "generateId"
---

<objective>
Crear catbot.db como base de datos SQLite independiente con las 5 tablas requeridas y el modulo catbot-db.ts con funciones CRUD para todas las tablas.

Purpose: Establecer la infraestructura de persistencia que toda la inteligencia de CatBot necesita (conversaciones, perfiles, memoria, resumenes, conocimiento aprendido).
Output: catbot-db.ts funcional con tests unitarios green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-foundation-catbot-db-knowledge-tree/118-RESEARCH.md

@app/src/lib/db.ts (patron exacto a replicar para catbot-db.ts)

<interfaces>
<!-- Patron de db.ts que catbot-db.ts debe seguir exactamente -->
From app/src/lib/db.ts:
```typescript
import Database from 'better-sqlite3';
import path from 'path';
import fs from 'fs';
import { logger } from './logger';

const dbPath = process['env']['DATABASE_PATH'] || path.join(process.cwd(), 'data', 'docflow.db');
const dbDir = path.dirname(dbPath);
if (!fs.existsSync(dbDir)) { fs.mkdirSync(dbDir, { recursive: true }); }
const db = new Database(dbPath);
try {
  db.pragma('journal_mode = WAL');
  db.pragma('busy_timeout = 5000');
} catch {}
db.exec(`CREATE TABLE IF NOT EXISTS ...`);
export default db;
```

From app/src/lib/utils.ts:
```typescript
export function generateId(): string; // Project convention for all IDs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests + catbot-db.ts con schema y CRUD</name>
  <files>app/src/lib/__tests__/catbot-db.test.ts, app/src/lib/catbot-db.ts</files>
  <behavior>
    - Test "creates tables": importar catbot-db.ts crea las 5 tablas (verificar con PRAGMA table_info)
    - Test "CRUD conversation_log": saveConversation inserta, getConversation recupera, getConversations lista, deleteConversation borra
    - Test "CRUD user_profiles": upsertProfile crea/actualiza, getProfile recupera por id
    - Test "CRUD user_memory": saveMemory inserta, getMemories filtra por user_id
    - Test "CRUD summaries": saveSummary inserta, getSummaries filtra por user_id y period_type
    - Test "CRUD knowledge_learned": saveLearnedEntry inserta, getLearnedEntries filtra por knowledge_path y validated status
    - Test usa DB temporal (in-memory o tmp file) para no contaminar datos reales
  </behavior>
  <action>
    1. Crear el test file primero con los tests descritos en behavior. Usar una DB en :memory: o tmp path para aislamiento. Los tests deben fallar inicialmente (RED).

    2. Crear `app/src/lib/catbot-db.ts` siguiendo el patron EXACTO de db.ts:
       - `process['env']['CATBOT_DB_PATH']` (bracket notation, NUNCA dot notation)
       - WAL mode + busy_timeout=5000 en try-catch
       - CREATE TABLE IF NOT EXISTS para las 5 tablas con el schema exacto del RESEARCH.md (seccion "catbot.db Schema")
       - Usar `generateId()` de `@/lib/utils` para todos los IDs generados

    3. Schema de las 5 tablas (copiar exacto del RESEARCH.md):
       - user_profiles: id TEXT PK, display_name, channel DEFAULT 'web', personality_notes, communication_style, preferred_format, known_context JSON DEFAULT '{}', initial_directives, interaction_count DEFAULT 0, last_seen, created_at, updated_at
       - user_memory: id TEXT PK, user_id TEXT NOT NULL, trigger_patterns JSON, steps JSON, preferences JSON DEFAULT '{}', source_conversation_id, success_count DEFAULT 0, last_used, created_at
       - conversation_log: id TEXT PK, user_id DEFAULT 'web:default', channel DEFAULT 'web', messages JSON NOT NULL, tools_used JSON DEFAULT '[]', token_count DEFAULT 0, model, page, started_at, ended_at
       - summaries: id TEXT PK, user_id DEFAULT 'web:default', period_type NOT NULL, period_start, period_end, summary NOT NULL, topics JSON, tools_used JSON, decisions JSON, pending JSON, conversation_count DEFAULT 0, created_at
       - knowledge_learned: id TEXT PK, knowledge_path NOT NULL, category NOT NULL, content NOT NULL, learned_from DEFAULT 'usage', confidence REAL DEFAULT 0.5, validated INTEGER DEFAULT 0, access_count DEFAULT 0, created_at, updated_at

    4. Funciones CRUD exportadas (named exports, NO metodos de clase):
       - saveConversation(conv: { userId?, channel?, messages: any[], toolsUsed?: string[], tokenCount?: number, model?: string, page?: string }): string (returns id)
       - getConversation(id: string): ConversationRow | undefined
       - getConversations(userId?: string, limit?: number): ConversationRow[]
       - deleteConversation(id: string): void
       - upsertProfile(profile: { id: string, displayName?: string, channel?: string, ... }): void
       - getProfile(id: string): ProfileRow | undefined
       - saveMemory(mem: { userId: string, triggerPatterns: string[], steps: any[], preferences?: any, sourceConversationId?: string }): string
       - getMemories(userId: string): MemoryRow[]
       - saveSummary(sum: { userId?, periodType, periodStart, periodEnd, summary, topics?, toolsUsed?, decisions?, pending?, conversationCount? }): string
       - getSummaries(userId?: string, periodType?: string): SummaryRow[]
       - saveLearnedEntry(entry: { knowledgePath, category, content, learnedFrom? }): string
       - getLearnedEntries(opts?: { knowledgePath?, validated?: boolean }): LearnedRow[]

    5. Definir tipos TypeScript para los Row types (ConversationRow, ProfileRow, MemoryRow, SummaryRow, LearnedRow) y exportarlos.

    6. Exportar la instancia de DB como default export: `export default catbotDb`
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-db.test.ts --reporter=verbose</automated>
  </verify>
  <done>Las 5 tablas se crean al importar catbot-db.ts. Todas las funciones CRUD funcionan correctamente. Tests green. El modulo sigue el patron exacto de db.ts.</done>
</task>

</tasks>

<verification>
- `cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-db.test.ts --reporter=verbose` -- todos los tests pasan
- `cd /home/deskmath/docflow/app && npm run build` -- build no se rompe con el nuevo modulo
</verification>

<success_criteria>
- catbot-db.ts existe y sigue el patron de db.ts (WAL, busy_timeout, bracket env notation)
- Las 5 tablas se crean automaticamente al importar el modulo
- CRUD funcional para conversation_log, user_profiles, user_memory, summaries, knowledge_learned
- Tests unitarios cubren creacion de tablas y operaciones CRUD para cada tabla
- Build de Next.js sigue pasando
</success_criteria>

<output>
After completion, create `.planning/phases/118-foundation-catbot-db-knowledge-tree/118-01-SUMMARY.md`
</output>
