# Plan 66-04: Palette + NODE_TYPES Registration + i18n

## Goal
Register the StorageNode in NODE_TYPES, NODE_DIMENSIONS, getDefaultNodeData, getMiniMapNodeColor, palette, and MODE_ALLOWED_TYPES. Add all i18n keys for the storage node in both es.json and en.json.

## Depends On
- 66-01 (StorageNode component must exist to import)
- 66-02 (Config panel must have storage form to function)
- 66-03 (Executor must handle 'storage' type)

## Requirements Addressed
- STOR-01: Storage node type registered in NODE_TYPES, visible in palette with teal colors and HardDrive icon

## Tasks

### Task 1: Register storage in canvas-editor.tsx
**File**: `app/src/components/canvas/canvas-editor.tsx`

#### 1a. Add import (after line 42, the SchedulerNode import):
```typescript
import { StorageNode } from './nodes/storage-node';
```

#### 1b. Add to NODE_TYPES (line 56, before `output: OutputNode`):
```typescript
storage: StorageNode,
```

The NODE_TYPES block should become:
```typescript
const NODE_TYPES = {
  start: StartNode,
  agent: AgentNode,
  catbrain: CatBrainNode,
  project: CatBrainNode,
  connector: ConnectorNode,
  checkpoint: CheckpointNode,
  merge: MergeNode,
  condition: ConditionNode,
  scheduler: SchedulerNode,
  storage: StorageNode,
  output: OutputNode,
} as const;
```

#### 1c. Add to NODE_DIMENSIONS (line 70, before `output`):
```typescript
storage: { width: 240, height: 110 },
```

#### 1d. Add case to getDefaultNodeData (after line 117 `};` of scheduler case, before `case 'output'` on line 118):
```typescript
    case 'storage': return {
      label: t('nodeDefaults.storage'),
      storage_mode: 'local',
      filename_template: '{title}_{date}_{time}.md',
      subdir: '',
      connectorId: null,
      use_llm_format: false,
      format_instructions: '',
      format_model: '',
    };
```

#### 1e. Add case to getMiniMapNodeColor (after line 134, `case 'scheduler'`, before `default`):
```typescript
    case 'storage':    return '#0d9488'; // teal
```

### Task 2: Add storage to node palette
**File**: `app/src/components/canvas/node-palette.tsx`

#### 2a. Add HardDrive import (line 6). Current import:
```typescript
import { Play, Plug, UserCheck, GitMerge, GitBranch, Flag, Timer } from 'lucide-react';
```
Change to:
```typescript
import { Play, Plug, UserCheck, GitMerge, GitBranch, Flag, Timer, HardDrive } from 'lucide-react';
```

#### 2b. Add storage entry to PALETTE_ITEMS (after line 24 scheduler entry, before line 25 output entry):
```typescript
  { type: 'storage',   icon: HardDrive,   color: 'text-teal-400' },
```

#### 2c. Add 'storage' to all MODE_ALLOWED_TYPES sets (lines 30-34). Add 'storage' before 'output' in each set:
```typescript
const MODE_ALLOWED_TYPES: Record<string, Set<string>> = {
  agents:    new Set(['start', 'agent', 'checkpoint', 'merge', 'condition', 'scheduler', 'storage', 'output']),
  catbrains: new Set(['start', 'catbrain', 'checkpoint', 'merge', 'condition', 'scheduler', 'storage', 'output']),
  projects:  new Set(['start', 'catbrain', 'checkpoint', 'merge', 'condition', 'scheduler', 'storage', 'output']),
  mixed:     new Set(['start', 'agent', 'catbrain', 'connector', 'checkpoint', 'merge', 'condition', 'scheduler', 'storage', 'output']),
};
```

### Task 3: Add all storage i18n keys
**File**: `app/messages/es.json`

#### 3a. In `canvas.nodes` (after line 1449, the closing `}` of `schedulerHandles`, before the closing `}` of `nodes` on line 1450):
Add these entries:
```json
      "storage": "Almacenamiento",
      "storageLocal": "Local",
      "storageConnector": "Conector",
      "storageBoth": "Local + Conector"
```

#### 3b. In `canvas.nodeDefaults` (after line 1462, `"scheduler": "Scheduler"`, before the closing `}` on line 1463):
```json
      "storage": "Almacenamiento"
```

#### 3c. In `canvas.nodeConfig` (after line 1545, the closing `}` of `scheduler`, before the closing `}` of `nodeConfig` on line 1546):
```json
      "storage": {
        "mode": "Modo de almacenamiento",
        "modeLocal": "Archivo local",
        "modeConnector": "Conector externo",
        "modeBoth": "Local + Conector",
        "filenameTemplate": "Plantilla de nombre",
        "filenameHelp": "Variables: {date}, {time}, {run_id}, {title}",
        "subdir": "Subdirectorio",
        "connector": "Conector",
        "noConnector": "Sin conector",
        "useLlmFormat": "Formatear con LLM antes de guardar",
        "formatInstructions": "Instrucciones de formato",
        "formatPlaceholder": "Ej: Formatea como tabla markdown con columnas...",
        "formatModel": "Modelo (opcional)"
      }
```

#### 3d. In `canvas.palette` (after line 1555, `"scheduler": "Timer"`, before `"output"` on line 1556):
```json
      "storage": "Storage",
```

#### 3e. In `canvas.palette.tooltips` (after line 1565, the scheduler tooltip, before `"output"` on line 1566):
```json
        "storage": "Storage — guarda resultados en archivos o conectores",
```

**File**: `app/messages/en.json`

Apply the same structure with English values:

#### 3f. In `canvas.nodes` (after `schedulerHandles` closing `}`, before `nodes` closing `}`):
```json
      "storage": "Storage",
      "storageLocal": "Local",
      "storageConnector": "Connector",
      "storageBoth": "Local + Connector"
```

#### 3g. In `canvas.nodeDefaults` (after `"scheduler": "Scheduler"`, before closing `}`):
```json
      "storage": "Storage"
```

#### 3h. In `canvas.nodeConfig` (after `scheduler` section closing `}`, before `nodeConfig` closing `}`):
```json
      "storage": {
        "mode": "Storage mode",
        "modeLocal": "Local file",
        "modeConnector": "External connector",
        "modeBoth": "Local + Connector",
        "filenameTemplate": "Filename template",
        "filenameHelp": "Variables: {date}, {time}, {run_id}, {title}",
        "subdir": "Subdirectory",
        "connector": "Connector",
        "noConnector": "No connector",
        "useLlmFormat": "Format with LLM before saving",
        "formatInstructions": "Format instructions",
        "formatPlaceholder": "E.g.: Format as markdown table with columns...",
        "formatModel": "Model (optional)"
      }
```

#### 3i. In `canvas.palette` (after `"scheduler": "Timer"`, before `"output"`):
```json
      "storage": "Storage",
```

#### 3j. In `canvas.palette.tooltips` (after scheduler tooltip, before `"output"` tooltip):
```json
        "storage": "Storage — saves results to files or connectors",
```

## Verification
- [ ] Storage node appears in palette with HardDrive icon and teal color in all canvas modes (agents, catbrains, projects, mixed)
- [ ] Dragging storage from palette onto canvas creates node with default data (local mode, `{title}_{date}_{time}.md` filename template)
- [ ] Storage node renders correctly with teal colors
- [ ] MiniMap shows storage nodes in teal (#0d9488)
- [ ] Config panel shows storage form when node is selected
- [ ] All i18n keys resolve (no missing translation warnings in console)
- [ ] `cd ~/docflow/app && npm run build` passes
- [ ] `cd ~/docflow/app && npx vitest run` passes (if existing tests exist)

## Estimated Complexity
Medium -- Touches 4 files with small additions each; i18n keys are straightforward but numerous. ~100 lines of additions total.
