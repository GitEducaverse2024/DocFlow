---
phase: 81-integration-tests-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/api/cat-paws/[id]/chat/route.ts
  - .planning/CONNECTORS.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TDOC-02, TDOC-03, TDOC-04]

must_haves:
  truths:
    - "CatPaw system prompt includes safe delete behavior documentation"
    - "CatPaw system prompt includes lead notes field names (title+desc)"
    - "CatPaw system prompt includes contact search client-side filtering note"
    - "CONNECTORS.md has a Holded API Critical Field Reference section"
    - "STATE.md reflects v18.0 as complete with all 5 phases done"
    - "PROJECT.md lists v18.0 features in Validated section"
  artifacts:
    - path: "app/src/app/api/cat-paws/[id]/chat/route.ts"
      provides: "Extended operational guide with safe delete, notes, contact search"
      contains: "pending_confirmation"
    - path: ".planning/CONNECTORS.md"
      provides: "Holded API critical field reference table"
      contains: "Holded API"
    - path: ".planning/STATE.md"
      provides: "v18.0 completion state"
      contains: "complete"
    - path: ".planning/PROJECT.md"
      provides: "v18.0 in Validated section"
      contains: "v18.0"
  key_links:
    - from: "app/src/app/api/cat-paws/[id]/chat/route.ts"
      to: "holded-mcp tools"
      via: "system prompt operational guide"
      pattern: "GUIA OPERATIVA MCP"
---

<objective>
Update CatPaw system prompt and project documentation to reflect all critical field fixes from v18.0 (phases 77-80).

Purpose: The CatPaw operational guide in chat/route.ts is the primary way the LLM learns how to use Holded MCP tools correctly. Missing documentation for safe deletes, lead notes, and contact search means the LLM will use these tools incorrectly. Project docs must also reflect milestone completion.

Output: Extended system prompt, CONNECTORS.md with Holded reference, updated STATE.md and PROJECT.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONNECTORS.md
@.planning/phases/81-integration-tests-documentation/RESEARCH.md

<interfaces>
<!-- From chat/route.ts lines 334-387: the existing operational guide block -->
<!-- The guide is injected when `openAITools.length > 0` (MCP tools loaded) -->
<!-- It's a template string pushed to systemParts array -->
<!-- Lines 373-383 already document: duration seconds, costHour, holdedUserId, startTmp/endTmp, invoices, leads funnelId -->
<!-- MISSING: safe delete, lead notes title+desc, contact search client-side -->

<!-- From execute-catpaw.ts: Does NOT load MCP tools (no openAITools). -->
<!-- Uses direct connector fetch calls. Operational guide NOT needed here. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CatPaw operational guide with missing critical fields</name>
  <files>app/src/app/api/cat-paws/[id]/chat/route.ts</files>
  <action>
Read the file and locate the operational guide block (lines 334-387). Add three new paragraphs BEFORE the closing "Si una herramienta falla..." line (line 385). Do NOT rewrite existing content — only insert new lines.

Insert these three blocks after the "LEADS CRM" paragraph (after line 383) and before "Si una herramienta falla" (line 385):

```
NOTAS DE LEADS: Para crear una nota en un lead, los campos son title (obligatorio) y desc (opcional). NO usar el campo "text" — no existe en la API de Holded.

BUSQUEDA DE CONTACTOS: holded_search_contact obtiene TODOS los contactos y filtra por nombre en el cliente (la API de Holded no tiene filtro por nombre). La busqueda es case-insensitive.

ELIMINACION SEGURA: Todas las operaciones DELETE requieren confirmacion por email. Al llamar un tool de eliminacion, el sistema devuelve status "pending_confirmation" y envia un email al administrador con enlace de confirmar/cancelar. El recurso NO se elimina hasta que el usuario confirma via email. Informa al usuario que debe revisar su email para confirmar la eliminacion.
```

Important:
- Keep text in Spanish (project convention)
- Keep it concise — this goes into LLM system prompt tokens
- Do NOT modify execute-catpaw.ts — it does not load MCP tools and does not need the operational guide
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && grep -c "pending_confirmation\|NOTAS DE LEADS\|BUSQUEDA DE CONTACTOS\|ELIMINACION SEGURA" app/src/app/api/cat-paws/\[id\]/chat/route.ts</automated>
  </verify>
  <done>grep returns 4 (all three new sections plus pending_confirmation present in the file). Existing guide content unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add Holded critical field reference to CONNECTORS.md and update project docs</name>
  <files>.planning/CONNECTORS.md, .planning/STATE.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <action>
**CONNECTORS.md** — Append a new section before the closing timestamp line (line 588). Add:

```markdown
---

## Holded API — Critical Field Reference

Campos criticos descubiertos durante la auditoria v18.0 (phases 77-80). Referencia rapida para desarrollo y debugging.

| Modulo | Tool | Campo | Valor Correcto | Error Comun |
|--------|------|-------|-----------------|-------------|
| Time Tracking | holded_register_time | duration | Segundos (1h=3600, 8h=28800) | Enviar horas directas |
| Time Tracking | holded_register_time | userId | holdedUserId del empleado (GET /employees/{id}) | Usar el id interno |
| Time Tracking | holded_register_time | costHour | Requerido, puede ser 0 | Omitirlo |
| Timesheets | holded_create_timesheet | startTmp/endTmp | Unix timestamp string (ej: '1742205600') | Enviar HH:MM |
| Timesheets | holded_create_timesheet | timezone | Europe/Madrid (CET +1 / CEST +2) | Asumir UTC |
| CRM | holded_create_lead_note | title | Requerido | Usar campo "text" |
| CRM | holded_create_lead_note | desc | Opcional (cuerpo de la nota) | Usar campo "text" |
| CRM | holded_create_lead | stageId | Passthrough directo (no transformar) | — |
| Contacts | holded_search_contact | — | Client-side filtering (API no filtra por nombre) | Esperar filtro server-side |
| Deletes | Todos (14 tools) | — | requestDelete() + email confirmation | DELETE directo |

### Protocolo de Resolucion de Entidades

Antes de operar sobre cualquier entidad Holded: Listar/Buscar -> Identificar (desambiguar si varios) -> Retener ID -> Ejecutar.

### Safe Delete Flow

1. Tool de eliminacion llama `requestDelete()` con recurso y callback
2. Sistema genera token unico + envia email HTML al admin
3. Email contiene boton Confirmar y boton Cancelar
4. Solo al confirmar se ejecuta el DELETE real en la API de Holded
5. Token expira en 24h. Tokens usados o expirados muestran error
```

**STATE.md** — Update to reflect v18.0 completion:
- Change `status: in_progress` to `status: complete` in frontmatter
- Update `last_updated` to current date
- Update `last_activity` to reflect phase 81 completion
- Update `completed_phases: 4` to `5`, `total_plans: 5` to `7`, and `completed_plans: 5` to `7` (adding 81-01 and 81-02)
- Update Current Position to Phase 81 complete
- Update Phase 81 row in table to show complete
- Add `milestone_name` update if needed

**PROJECT.md** — Move v18.0 items from "Active" to "Validated" section:
- Move the three v18.0 bullet points from "### Active" to "### Validated" (after the v17.0 entries around line 83)
- Add checkmark prefix
- Update "Current Milestone" section at bottom to show v18.0 as COMPLETE
- Add v18.0 milestone summary to "## Milestone History" section (after v17.0 entry around line 370):
  ```
  ### v18.0 — Holded MCP: Auditoria API + Safe Deletes (COMPLETE)
  - Auditoria y correccion de 7 bugs criticos en campos API (duration, userId, timestamps, notas)
  - Sistema Safe Delete: confirmacion por email con tokens para 14 DELETE tools
  - Tests de integracion contra API real de Holded
  - System prompt CatPaw actualizado con campos criticos
  - 5 phases (77-81), ~26 requirements, all complete
  ```

**ROADMAP.md** — Update Phase 81 progress:
- Mark Phase 81 checkbox as `[x]` (line 27: `- [ ] **Phase 81:` → `- [x] **Phase 81:`)
- Update progress table row 81 (line 121) from `0/? | Not started | -` to `2/2 | Complete | 2026-03-24`
- Update milestone status line from `(active)` to `(complete)` in the milestone list
- Note: Plans are already listed at lines 91-94 — no need to add plan links
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow && grep -c "Critical Field Reference" .planning/CONNECTORS.md && grep "status:" .planning/STATE.md | head -1 && grep -c "v18.0.*COMPLETE" .planning/PROJECT.md</automated>
  </verify>
  <done>CONNECTORS.md contains "Critical Field Reference" section. STATE.md shows status: complete. PROJECT.md has v18.0 marked COMPLETE in milestone history. ROADMAP.md shows Phase 81 as complete with 2 plans listed.</done>
</task>

</tasks>

<verification>
1. `grep -n "NOTAS DE LEADS\|BUSQUEDA DE CONTACTOS\|ELIMINACION SEGURA\|pending_confirmation" app/src/app/api/cat-paws/[id]/chat/route.ts` — all 4 matches
2. `grep -n "Critical Field Reference\|Safe Delete Flow\|requestDelete" .planning/CONNECTORS.md` — section exists with table and flow
3. `grep "status: complete" .planning/STATE.md` — milestone complete
4. `grep "v18.0.*COMPLETE" .planning/PROJECT.md` — milestone in history
5. `cd /home/deskmath/docflow/app && npx next build 2>&1 | tail -5` — build succeeds (only route.ts modified in app/)
</verification>

<success_criteria>
- CatPaw system prompt has 3 new documentation blocks (safe delete, lead notes, contact search) without breaking existing guide content
- CONNECTORS.md has a Holded critical field reference table with all 10 field mappings
- STATE.md shows v18.0 as complete with 5/5 phases and 7/7 plans
- PROJECT.md has v18.0 in Validated section and milestone history
- ROADMAP.md shows Phase 81 complete with 2 plans
- `npm run build` passes (no TypeScript/syntax errors introduced)
</success_criteria>

<output>
After completion, create `.planning/phases/81-integration-tests-documentation/81-02-SUMMARY.md`
</output>
