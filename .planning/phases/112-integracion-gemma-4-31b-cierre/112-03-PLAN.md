---
phase: 112-integracion-gemma-4-31b-cierre
plan: 03
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - .planning/knowledge/model-onboarding.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [GEMMA-08]
must_haves:
  truths:
    - "There exists a documented procedure of EXACTLY 3 steps for adding a new LLM to DocFlow"
    - "The doc lives in an operator-discoverable location (.planning/knowledge/)"
    - "Milestone v25.0 closure is reflected in ROADMAP.md and STATE.md"
  artifacts:
    - path: ".planning/knowledge/model-onboarding.md"
      provides: "3-step procedure for onboarding a new LLM"
      contains: "## Paso"
      min_lines: 25
  key_links:
    - from: ".planning/knowledge/model-onboarding.md"
      to: "Settings > Modelos UI + /api/discovery/refresh endpoint"
      via: "direct references in step text"
      pattern: "discovery/refresh"
---

<objective>
Write the 3-step onboarding doc (GEMMA-08 deliverable) and record milestone v25.0 closure in project state. The doc is the operator runbook for "how do I add model X?" going forward.

Purpose: GEMMA-08 explicitly requires EXACTLY 3 steps. Scope discipline — this is not a 10-page manual. It is 3 numbered steps that anyone can follow.
Output: `.planning/knowledge/model-onboarding.md` (3 steps) + ROADMAP/STATE updated to reflect v25.0 closure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-integracion-gemma-4-31b-cierre/112-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 3-step onboarding doc</name>
  <files>.planning/knowledge/model-onboarding.md</files>
  <action>
    Create `.planning/knowledge/model-onboarding.md` with EXACTLY 3 `## Paso` sections. Structure:

    ```markdown
    # Anadir un nuevo LLM al ecosistema DocFlow

    > Procedimiento de 3 pasos. Valido para modelos Ollama locales y providers de API (OpenAI / Anthropic / Google / Groq).

    ## Paso 1 — Instalar el modelo

    **Ollama local:**
    ```bash
    docker exec docflow-ollama ollama pull <modelo>:<tag>
    # Ej: docker exec docflow-ollama ollama pull gemma4:31b
    ```

    **Provider de API:** anadir la API key correspondiente en el archivo `.env` (OPENAI_API_KEY, ANTHROPIC_API_KEY, GOOGLE_API_KEY, GROQ_API_KEY) y reiniciar el contenedor docflow-app.

    ## Paso 2 — Refrescar Discovery

    En **Settings > Modelos**, pulsa "Refrescar inventario".

    (O via API: `curl -X POST http://localhost:3500/api/discovery/refresh`)

    El modelo debe aparecer en la lista de inventario con estado activo. MID crea automaticamente una ficha basica: tier=Libre para modelos Ollama locales, tier=Pro para modelos de API.

    ## Paso 3 — Clasificar en MID

    En **Settings > Modelos**, edita la ficha del modelo recien aparecido:

    - **Tier**: Elite / Pro / Libre segun calidad y coste
    - **Capacidades**: marca las reales (chat, function_calling, thinking, vision, audio, Xk_context)
    - **Descripcion de mejor uso**: 1-2 frases sobre cuando usarlo
    - **Scores**: reasoning / coding / creativity / speed / multilingual (1-10)

    CatBot usa esta ficha al responder "que modelo me recomiendas para X?", asi que mentir aqui se traduce en malas recomendaciones.

    ---

    **Opcional:** en **Settings > Modelos > Alias Routing**, apunta algun alias de intencion (chat-rag, process-docs, agent-task, etc.) al nuevo modelo si quieres que se use en produccion.
    ```

    The doc must have exactly 3 `## Paso` headings (verified by grep in verify).
  </action>
  <verify>
    <automated>test -f .planning/knowledge/model-onboarding.md && [ "$(grep -c '^## Paso' .planning/knowledge/model-onboarding.md)" = "3" ]</automated>
  </verify>
  <done>Doc exists, has exactly 3 "## Paso" sections, references real endpoints and UI sections.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP and STATE for v25.0 closure</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    1. Edit `.planning/ROADMAP.md`:
       - Mark Phase 112 checkbox `[x]` with completion date `2026-04-04`
       - Update the table row for "112. Integracion Gemma 4:31B + Cierre" to `Complete | 2026-04-04`
       - In the Milestones list, change "v25.0 Model Intelligence Orchestration ... (in progress)" to "(shipped 2026-04-04)"

    2. Edit `.planning/STATE.md`:
       - Frontmatter: update `status: verifying` -> `status: complete`, `completed_phases: 5` -> `6`, update `last_activity` and `last_updated`
       - Current Position: Phase 112 of 112, all plans complete
       - Progress bar: `[██████████] 100%`
       - Milestone History: move v25.0 from ACTIVE to COMPLETE, add summary line

    Do NOT touch the Phase Overview table's legacy rows. Only update the Phase 112 row to `COMPLETE (3/3 plans)`.
  </action>
  <verify>
    <automated>grep -q "Phase 112.*completed 2026-04-04\|112.*Complete.*2026-04-04" .planning/ROADMAP.md && grep -q "status: complete" .planning/STATE.md</automated>
  </verify>
  <done>ROADMAP shows Phase 112 and milestone v25.0 shipped; STATE.md reflects milestone closure.</done>
</task>

</tasks>

<verification>
- `.planning/knowledge/model-onboarding.md` exists, 3 `## Paso` sections
- ROADMAP.md shows Phase 112 complete, milestone v25.0 shipped
- STATE.md shows status: complete, 6/6 phases
</verification>

<success_criteria>
GEMMA-08 satisfied (3-step doc exists, is exactly 3 steps, documents real procedure). Milestone v25.0 closure recorded in project planning state.
</success_criteria>

<output>
After completion, create `.planning/phases/112-integracion-gemma-4-31b-cierre/112-03-SUMMARY.md`
</output>
