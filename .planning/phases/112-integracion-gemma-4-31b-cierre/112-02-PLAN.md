---
phase: 112-integracion-gemma-4-31b-cierre
plan: 02
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - .planning/phases/112-integracion-gemma-4-31b-cierre/112-UAT.md
autonomous: false
requirements: [GEMMA-04, GEMMA-05, GEMMA-06, GEMMA-07]
must_haves:
  truths:
    - "CatBot responds to 'que modelos tengo' with real inventory including gemma4:31b, tiers, and best-use"
    - "CatBot follows diagnostic protocol when user reports poor model output and suggests escalation to Gemma 4 / Elite"
    - "A brand new Ollama model installed post-phase-start is auto-detected, auto-classified in MID, and CatBot can recommend it"
    - "CatBot sugiere modelos adecuados por nodo en canvas (via canvas_get/recommend_model_for_task)"
  artifacts:
    - path: ".planning/phases/112-integracion-gemma-4-31b-cierre/112-UAT.md"
      provides: "Manual UAT scenario tracker mirroring 110-UAT.md format with 4 scenarios (A/B/C/D)"
      contains: "## Tests"
  key_links:
    - from: "CatBot chat"
      to: "get_model_landscape / recommend_model_for_task / canvas_get tools"
      via: "CatBot tool-call pipeline (Phase 110)"
      pattern: "tool_call"
---

<objective>
Execute the four E2E validation scenarios from the phase roadmap as manual UAT, tracked in 112-UAT.md. This is the core acceptance test for milestone v25.0 — proving the full pipeline (Discovery → MID → Alias → CatBot orchestrator → UI) works end-to-end with a real model.

Purpose: LLM output is non-deterministic, so automated assertions would be brittle. Manual UAT by the user, mirroring the proven Phase 110 UAT format, is the honest validation approach.
Output: A populated 112-UAT.md with pass/fail verdicts for scenarios A/B/C/D, and a record of any issues found for gap closure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-integracion-gemma-4-31b-cierre/112-RESEARCH.md
@.planning/phases/110-catbot-como-orquestador-de-modelos/110-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 112-UAT.md with 4 scenarios ready for user execution</name>
  <files>.planning/phases/112-integracion-gemma-4-31b-cierre/112-UAT.md</files>
  <action>
    Create 112-UAT.md mirroring the structure of 110-UAT.md. Include YAML frontmatter (status: in_progress, phase: 112-integracion-gemma-4-31b-cierre, source: [112-01-SUMMARY.md], started: now), a "Current Test" block set to scenario A, and 4 "### N. Name" blocks.

    Scenarios:

    **A. Escalation diagnostic (GEMMA-04)**
    expected: Tell CatBot "el modelo Libre que usa chat-rag no me esta dando buenos resumenes, son muy pobres". CatBot should (1) follow the diagnostic protocol, (2) check current routing via get_model_landscape/resolveAlias, (3) suggest escalating to gemma4:31b (Pro) or an Elite tier model, (4) provide justification referencing MID capabilities.

    **B. Canvas per-node model suggestions (GEMMA-05)**
    expected: Open any existing canvas in the UI, then in CatBot say "revisa los nodos de mi canvas actual y sugiereme el modelo optimo para cada uno". CatBot calls canvas_get, returns model_suggestion per node: output nodes → Libre, agent nodes → tier based on instruction complexity. At least one agent node should get a Pro/Elite suggestion.

    **C. "Que modelos tengo" inventory (GEMMA-06)**
    expected: Ask CatBot "que modelos tengo disponibles?". CatBot calls get_model_landscape and responds with inventory grouped by tier. gemma4:31b must appear with its real capabilities (vision, thinking, 256K context) and Pro tier.

    **D. New model auto-detection (GEMMA-07)**
    expected: (user runs `docker exec docflow-ollama ollama pull phi3:mini` on the host) then `curl -X POST http://localhost:3500/api/discovery/refresh`. Ask CatBot "que modelos nuevos tengo disponibles?". CatBot should (1) list phi3:mini in its landscape answer, (2) when asked "me lo recomiendas para clasificar docs?" CatBot includes it in recommend_model_for_task output with auto_created flag honored. Verify via `curl /api/mid | jq '.models[] | select(.model_key=="ollama/phi3:mini")'` that tier=Libre and auto_created=1.

    Include empty "result:" and "reported:" fields so user can fill them during UAT, plus a "## Summary" block with total/passed/issues/pending counters, and a "## Gaps" section for gap closure input.
  </action>
  <verify>
    <automated>test -f .planning/phases/112-integracion-gemma-4-31b-cierre/112-UAT.md && grep -c "^### " .planning/phases/112-integracion-gemma-4-31b-cierre/112-UAT.md | grep -q 4</automated>
  </verify>
  <done>112-UAT.md exists with 4 test scenario sections and frontmatter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User executes 4 UAT scenarios against live system</name>
  <files>.planning/phases/112-integracion-gemma-4-31b-cierre/112-UAT.md</files>
  <action>PAUSE for user to execute 4 CatBot UAT scenarios against the live system and record results in 112-UAT.md. See how-to-verify below for instructions.</action>
  <what-built>UAT scaffold ready. User must now drive real conversations with CatBot and record results.</what-built>
  <how-to-verify>
    Open the UAT doc: `.planning/phases/112-integracion-gemma-4-31b-cierre/112-UAT.md`

    For each of the 4 scenarios (A/B/C/D), follow the "expected" steps exactly, and fill in:
    - `result: pass` if CatBot behaves as described
    - `result: issue` + `reported: "..."` if it deviates (be specific about what it did wrong)
    - `severity: minor|major|blocker`

    **Scenario D pre-step (manual):**
    ```bash
    docker exec docflow-ollama ollama pull phi3:mini
    curl -X POST http://localhost:3500/api/discovery/refresh
    curl -s http://localhost:3500/api/mid | jq '.models[] | select(.model_key=="ollama/phi3:mini")'
    # Expect: tier=Libre, auto_created=1
    ```

    After all 4 scenarios are filled, update the "## Summary" counters and set frontmatter `status: complete` if all pass, or `status: diagnosed` if there are issues to address.

    This is the acceptance gate for milestone v25.0.
  </how-to-verify>
  <resume-signal>Type "UAT complete" with summary (e.g., "4 pass" or "3 pass, 1 issue in Scenario B: ...")</resume-signal>
  <verify>Human confirms all 4 scenarios in 112-UAT.md have result filled and Summary block updated</verify>
  <done>112-UAT.md has 4 scenarios with result=pass or result=issue+reported, Summary counters updated, frontmatter status=complete or diagnosed</done>
</task>

</tasks>

<verification>
- 112-UAT.md has all 4 scenarios filled with result + any reported issues
- Summary block updated with final counts
- Frontmatter status = complete or diagnosed
</verification>

<success_criteria>
GEMMA-04, GEMMA-05, GEMMA-06, GEMMA-07 all have a verified result in 112-UAT.md. Any failing scenario is captured as a gap with enough detail to feed `/gsd:plan-phase --gaps` if needed.
</success_criteria>

<output>
After completion, create `.planning/phases/112-integracion-gemma-4-31b-cierre/112-02-SUMMARY.md` referencing 112-UAT.md for detailed results.
</output>
