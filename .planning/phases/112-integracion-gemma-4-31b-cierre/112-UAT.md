---
status: in_progress
phase: 112-integracion-gemma-4-31b-cierre
source: [112-01-SUMMARY.md]
started: 2026-04-07T14:25:00Z
updated: 2026-04-07T14:25:00Z
---

## Current Test

number: 1
name: Escalation Diagnostic
expected: |
  Tell CatBot "el modelo Libre que usa chat-rag no me esta dando buenos resumenes, son muy pobres". CatBot should (1) follow diagnostic protocol, (2) check current routing via get_model_landscape/resolveAlias, (3) suggest escalating to gemma4:31b (Pro) or Elite tier model, (4) provide justification referencing MID capabilities.
awaiting: user response

## Tests

### 1. Escalation Diagnostic (GEMMA-04)

expected: Tell CatBot "el modelo Libre que usa chat-rag no me esta dando buenos resumenes, son muy pobres". CatBot should (1) follow the diagnostic protocol, (2) check current routing via get_model_landscape/resolveAlias, (3) suggest escalating to gemma4:31b (Pro) or an Elite tier model, (4) provide justification referencing MID capabilities.
result:
reported:
severity:

### 2. Canvas Per-Node Model Suggestions (GEMMA-05)

expected: Open any existing canvas in the UI, then in CatBot say "revisa los nodos de mi canvas actual y sugiereme el modelo optimo para cada uno". CatBot calls canvas_get, returns model_suggestion per node: output nodes -> Libre, agent nodes -> tier based on instruction complexity. At least one agent node should get a Pro/Elite suggestion.
result:
reported:
severity:

### 3. "Que modelos tengo" Inventory (GEMMA-06)

expected: Ask CatBot "que modelos tengo disponibles?". CatBot calls get_model_landscape and responds with inventory grouped by tier. gemma4:31b must appear with its real capabilities (vision, thinking, 256K context) and Pro tier.
result:
reported:
severity:

### 4. New Model Auto-Detection (GEMMA-07)

expected: |
  Pre-step (manual):
  ```bash
  docker exec docflow-ollama ollama pull phi3:mini
  curl -X POST http://localhost:3500/api/discovery/refresh
  curl -s http://localhost:3500/api/mid | jq '.models[] | select(.model_key=="ollama/phi3:mini")'
  # Expect: tier=Libre, auto_created=1
  ```
  Then ask CatBot "que modelos nuevos tengo disponibles?". CatBot should (1) list phi3:mini in its landscape answer, (2) when asked "me lo recomiendas para clasificar docs?" CatBot includes it in recommend_model_for_task output with auto_created flag honored. Verify via `curl /api/mid | jq '.models[] | select(.model_key=="ollama/phi3:mini")'` that tier=Libre and auto_created=1.
result:
reported:
severity:

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0

## Gaps

(none yet -- fill during UAT execution)
