---
phase: 144-evaluation-gate-eval
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/144-evaluation-gate-eval/144-CONSTRUCTION-TEST.md
autonomous: false
requirements: [EVAL-02]

must_haves:
  truths:
    - "CatBot construye un CatFlow de email classifier completo (8 nodos) sin intervencion manual"
    - "El canvas resultante es legible en el editor (nodos con labels descriptivos, edges con sourceHandle)"
    - "CatBot reporta paso a paso durante la construccion (check/cross marks)"
    - "El canvas es ejecutable end-to-end (estructura valida verificada via canvas_get)"
  artifacts:
    - path: ".planning/phases/144-evaluation-gate-eval/144-CONSTRUCTION-TEST.md"
      provides: "Registro completo del test de construccion autonoma"
      contains: "Resultado"
  key_links:
    - from: "CatBot /api/catbot/chat"
      to: "Canvas en editor"
      via: "canvas tools (add_node, add_edge, set_start_input)"
      pattern: "canvas_add_node|canvas_add_edge"
---

<objective>
Probar que CatBot puede construir un CatFlow de email classifier completo sin intervencion manual — el canvas resultante debe ser legible en el editor, ejecutable end-to-end, y CatBot debe reportar paso a paso.

Purpose: Demostrar que CatBot ha aprendido de las fases 138-143 y puede actuar como constructor autonomo de CatFlows de calidad.
Output: 144-CONSTRUCTION-TEST.md con registro del test y evaluacion del resultado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@auditoria-catflow.md (descripcion del pipeline Email Classifier como referencia)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pedir a CatBot que construya el Email Classifier CatFlow completo</name>
  <files>.planning/phases/144-evaluation-gate-eval/144-CONSTRUCTION-TEST.md</files>
  <action>
Enviar UN SOLO prompt a CatBot pidiendole que construya el CatFlow completo de email classifier. CatBot debe usar sus canvas tools autonomamente sin que le demos instrucciones paso a paso.

**Prompt a enviar:**

"Construye un CatFlow completo de Email Classifier para Educa360. El flujo debe:
1. Recibir emails (START con initialInput de 3 emails de prueba)
2. Normalizar los emails a JSON estructurado (from, subject, body, date, message_id, thread_id)
3. Clasificar cada email por producto Educa360 (K12, Simulator, REVI, Educaverse) y asignar template_id
4. Filtrar spam (condition: si tiene producto asignado)
5. Buscar contexto en CatBrain para emails validos
6. Generar respuesta profesional usando la plantilla correcta
7. Enviar via Gmail
8. Output final con resumen

Usa los CatPaws y recursos que consideres apropiados. Reporta cada paso."

Ejecutar via curl:
```bash
curl -s -N -X POST http://localhost:3500/api/catbot/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"PROMPT_COMPLETO","conversation_id":"eval-144-construction","model":"gemini-main"}' \
  2>&1 | timeout 300 cat
```

Timeout de 5 minutos — la construccion puede requerir 15+ tool calls.

Si CatBot escala a async (CatFlow), esperar a que complete y obtener el resultado via:
```bash
curl -s http://localhost:3500/api/catbot/conversations/eval-144-construction
```

Despues de que CatBot termine, verificar el canvas resultante:
1. Obtener lista de canvases: `curl -s http://localhost:3500/api/canvas` y buscar el nuevo canvas
2. Obtener flow_data: `curl -s http://localhost:3500/api/canvas/{id}`
3. Verificar estructura: contar nodos, edges, comprobar labels, instructions, sourceHandle

Registrar todo en 144-CONSTRUCTION-TEST.md con formato:

```markdown
# Test de Construccion Autonoma — CatBot v28.0

## Prompt Enviado
[prompt completo]

## Respuesta de CatBot
[texto completo incluyendo reporting paso a paso]

## Tools Ejecutadas
[lista de tool_calls con parametros clave]

## Canvas Resultante
- **Canvas ID:** {id}
- **Nombre:** {name}
- **Nodos:** {count} — [lista con label, type, has_instructions, model]
- **Edges:** {count} — [lista con source->target, sourceHandle si condition]

## Evaluacion

### Criterio 1: Completitud (8 nodos esperados)
[SI/NO + detalle]

### Criterio 2: Legibilidad en editor (labels descriptivos, edges correctos)
[SI/NO + detalle]

### Criterio 3: Ejecutabilidad (estructura valida, instructions con data contracts)
[SI/NO + detalle]

### Criterio 4: Reporting paso a paso (check/cross marks)
[SI/NO + detalle]

### Criterio 5: Sin intervencion manual (un solo prompt)
[SI/NO + detalle. Si escalo a async cuenta como SI si completo correctamente]

## Resultado
**PASS / FAIL**
[Justificacion]
```
  </action>
  <verify>
    <automated>test -f .planning/phases/144-evaluation-gate-eval/144-CONSTRUCTION-TEST.md && grep -q "Resultado" .planning/phases/144-evaluation-gate-eval/144-CONSTRUCTION-TEST.md</automated>
  </verify>
  <done>144-CONSTRUCTION-TEST.md contiene el registro completo del test con evaluacion de los 5 criterios y resultado PASS/FAIL</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacion humana del CatFlow construido</name>
  <files>.planning/phases/144-evaluation-gate-eval/144-CONSTRUCTION-TEST.md</files>
  <action>
Checkpoint de verificacion humana. El usuario debe:
1. Abrir 144-CONSTRUCTION-TEST.md y revisar que CatBot construyo el CatFlow sin prompts adicionales
2. Abrir el canvas en el editor visual (http://localhost:3500) y confirmar:
   a. Los nodos son visibles con labels descriptivos
   b. Las conexiones son correctas (no hay edges despues de OUTPUT)
   c. Las instrucciones de cada nodo son legibles y tienen data contracts
3. Opcionalmente ejecutar el canvas y verificar que procesa los emails correctamente
4. Confirmar que CatBot reporto paso a paso durante la construccion
  </action>
  <verify>
    <automated>grep -q "PASS\|FAIL" .planning/phases/144-evaluation-gate-eval/144-CONSTRUCTION-TEST.md</automated>
  </verify>
  <done>Usuario confirma que el CatFlow es legible en editor, ejecutable, y construido autonomamente por CatBot</done>
</task>

</tasks>

<verification>
- 144-CONSTRUCTION-TEST.md existe con registro completo
- Canvas creado por CatBot tiene 7+ nodos con labels
- Canvas tiene edges correctos (no despues de OUTPUT, sourceHandle en conditions)
- CatBot reporto progreso durante la construccion
- Un solo prompt fue necesario (sin intervencion manual)
</verification>

<success_criteria>
CatBot construye el CatFlow de email classifier en un solo prompt, el canvas es legible en el editor, tiene estructura ejecutable, y CatBot reporto paso a paso. Resultado: PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/144-evaluation-gate-eval/144-02-SUMMARY.md`
</output>
