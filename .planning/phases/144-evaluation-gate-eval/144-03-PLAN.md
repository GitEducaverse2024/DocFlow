---
phase: 144-evaluation-gate-eval
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/services/catbot-tools.ts
  - app/src/lib/services/catbot-prompt-assembler.ts
  - app/src/lib/__tests__/canvas-tools-fixes.test.ts
autonomous: true
requirements: [EVAL-01, EVAL-02]
gap_closure: true

must_haves:
  truths:
    - "canvas_get devuelve has_instructions, instructions (truncado a 200 chars), model y agentId para cada nodo"
    - "El protocolo de complejidad excluye solicitudes de construccion de canvas del clasificador async"
    - "CatBot puede ejecutar 8+ canvas tool calls sin escalamiento async cuando construye un canvas"
  artifacts:
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "canvas_get enriquecido con datos de nodo completos"
      contains: "has_instructions"
    - path: "app/src/lib/services/catbot-prompt-assembler.ts"
      provides: "Excepcion de complejidad para canvas construction"
      contains: "canvas"
    - path: "app/src/lib/__tests__/canvas-tools-fixes.test.ts"
      provides: "Tests para canvas_get enriquecido"
      contains: "canvas_get"
  key_links:
    - from: "canvas_get response"
      to: "CatBot verification"
      via: "has_instructions field in node map"
      pattern: "has_instructions"
    - from: "buildComplexityProtocol"
      to: "CatBot classification"
      via: "Exception for canvas construction"
      pattern: "canvas|construir|construye"
---

<objective>
Corregir los 2 bugs criticos de codigo que bloquean el gate de 85/100: (1) canvas_get no expone instructions/model/agentId de los nodos, haciendo que CatBot piense que las instrucciones no se guardaron, y (2) el protocolo de complejidad clasifica construccion de canvas (4-8 nodos) como "complex" forzando escalamiento async innecesario.

Purpose: Estos 2 fixes representan un estimado de +10-13 puntos en la scorecard (Test 6: +5-6, Tests 7/10: +5-7), suficiente para alcanzar 80-83/100 que combinado con Plan 04 supere 85/100.
Output: Codigo corregido y testeado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-evaluation-gate-eval/144-VERIFICATION.md

<interfaces>
<!-- canvas_get node mapping (catbot-tools.ts lines 2193-2199) — CURRENT (broken): -->
```typescript
nodes: flowData.nodes.map((n: Record<string, unknown>) => ({
  id: n.id,
  type: n.type,
  label: (n.data as Record<string, unknown>)?.label,
  position: n.position,
  model_suggestion: suggestModelForNode(n, midModels),
})),
```

<!-- buildComplexityProtocol (catbot-prompt-assembler.ts line 685) — CURRENT: -->
```typescript
### COMPLEJA si >=1:
- >3 ops secuenciales (entra... luego... despues...)
- Agregacion temporal (Q1, mes, ano, trimestre)
- >2 servicios externos (Holded+Drive+Email, n8n+RAG)
- Entrega formateada (informe, email maquetado)
- Comparacion cross-source o analisis+accion
```

<!-- buildNodeSummary helper (catbot-tools.ts line ~1450): -->
```typescript
function buildNodeSummary(node: Record<string, unknown>) {
  const data = node.data as Record<string, unknown>;
  return {
    nodeId: node.id,
    label: data.label,
    type: node.type,
    model: data.model || null,
    has_instructions: Boolean(data.instructions),
    has_agent: Boolean(data.agentId),
    has_skills: Array.isArray(data.skills) && data.skills.length > 0,
    has_connectors: Boolean(data.connectorId) || (Array.isArray(data.extraConnectors) && data.extraConnectors.length > 0),
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enriquecer canvas_get para exponer instructions, model y agentId de cada nodo</name>
  <files>app/src/lib/services/catbot-tools.ts, app/src/lib/__tests__/canvas-tools-fixes.test.ts</files>
  <behavior>
    - Test: canvas_get response includes has_instructions=true when node has instructions
    - Test: canvas_get response includes instructions_preview (truncated to 200 chars) when node has instructions
    - Test: canvas_get response includes model when node has explicit model
    - Test: canvas_get response includes agentId and agentName when node has agent
    - Test: canvas_get response includes has_instructions=false when node has no instructions
  </behavior>
  <action>
1. Agregar tests al final de canvas-tools-fixes.test.ts en un nuevo describe block "GAP-CLOSURE: canvas_get expone datos completos de nodos". Seedear un canvas con un nodo agent que tenga instructions, model y agentId, y otro nodo sin esos campos. Verificar que canvas_get devuelve los campos enriquecidos.

IMPORTANTE: El mock de canvas_get usa fetch(`${baseUrl}/api/canvas/${canvasId}`) internamente (linea 2169). Para testear canvas_get se necesita mockear fetch o usar un approach diferente. La forma mas directa es testear la logica de mapeo directamente. ALTERNATIVA: Como canvas_get hace fetch al API, y el test no tiene server corriendo, necesitamos mockear global.fetch para devolver el flow_data del canvas. Usar vi.stubGlobal('fetch', ...) para mockear la respuesta del API de canvas.

2. Modificar el mapeo de nodos en canvas_get (catbot-tools.ts, linea ~2193) para incluir campos adicionales:

```typescript
nodes: flowData.nodes.map((n: Record<string, unknown>) => {
  const data = n.data as Record<string, unknown> || {};
  const instructions = (data.instructions as string) || '';
  return {
    id: n.id,
    type: n.type,
    label: data.label,
    position: n.position,
    model: data.model || null,
    agentId: data.agentId || null,
    agentName: data.agentName || null,
    has_instructions: Boolean(instructions),
    instructions_preview: instructions.length > 200 ? instructions.slice(0, 200) + '...' : instructions || null,
    has_skills: Array.isArray(data.skills) && data.skills.length > 0,
    has_connectors: Boolean(data.connectorId) || (Array.isArray(data.extraConnectors) && data.extraConnectors.length > 0),
    model_suggestion: suggestModelForNode(n, midModels),
  };
}),
```

El campo `instructions_preview` se trunca a 200 chars para no sobrecargar el contexto de CatBot, pero permite que CatBot verifique que las instrucciones estan guardadas y ver su contenido resumido.

3. Correr tests: `cd ~/docflow/app && npx vitest run src/lib/__tests__/canvas-tools-fixes.test.ts`
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/canvas-tools-fixes.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>canvas_get devuelve has_instructions, instructions_preview, model, agentId, agentName para cada nodo. Tests pasan.</done>
</task>

<task type="auto">
  <name>Task 2: Excluir construccion de canvas del clasificador de complejidad async</name>
  <files>app/src/lib/services/catbot-prompt-assembler.ts</files>
  <action>
Modificar `buildComplexityProtocol()` en catbot-prompt-assembler.ts (linea 685) para agregar una excepcion explicita para construccion de canvas. El problema es que "construir un canvas con 5 nodos" tiene >3 ops secuenciales (canvas_create, add_node x5, add_edge x5) pero NO es una tarea que deba escalarse a async — es una tarea de tools directas.

Agregar ANTES de la seccion "### COMPLEJA si >=1:" una excepcion:

```
### EXCEPCION CANVAS (SIEMPRE simple):
Solicitudes de construccion, modificacion o configuracion de canvas son SIEMPRE simple, independientemente del numero de operaciones. Esto incluye:
- "construye/crea un canvas/CatFlow con N nodos"
- "anade/agrega nodos al canvas"
- "configura/modifica el canvas"
- "conecta los nodos"
Canvas tools (canvas_create, canvas_add_node, canvas_add_edge, canvas_update_node, canvas_set_start_input) son operaciones locales rapidas — NO requieren CatFlow asincrono.
Si la solicitud menciona "canvas", "CatFlow" en contexto de construccion, o pide crear/modificar nodos: clasificar como [COMPLEXITY:simple].
```

Tambien modificar la seccion de ejemplos SIMPLES para incluir canvas:

Cambiar:
```
Ej SIMPLES: "lista mis CatBrains", "ejecuta catflow X", "crea CatPaw Y"
```
Por:
```
Ej SIMPLES: "lista mis CatBrains", "ejecuta catflow X", "crea CatPaw Y", "construye un canvas con 8 nodos", "anade un nodo AGENT al canvas"
```

Verificar con build que no hay errores de sintaxis: `cd ~/docflow/app && npx next build 2>&1 | tail -5` (o simplemente verificar que el archivo se importa correctamente con vitest).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts 2>&1 | tail -10 && grep -c 'EXCEPCION CANVAS' /home/deskmath/docflow/app/src/lib/services/catbot-prompt-assembler.ts</automated>
  </verify>
  <done>buildComplexityProtocol contiene excepcion para canvas construction (verificado con grep). CatBot clasificara canvas construction como simple.</done>
</task>

</tasks>

<verification>
- canvas_get response incluye has_instructions, instructions_preview, model, agentId para nodos
- buildComplexityProtocol tiene excepcion CANVAS
- Todos los tests existentes siguen pasando
- `npm run build` en app/ no tiene errores
</verification>

<success_criteria>
Los 2 bugs criticos estan corregidos: (1) canvas_get expone datos de instrucciones para que CatBot pueda verificar persistencia, (2) canvas construction no se clasifica como "complex". Estimacion: +10-13 puntos en scorecard.
</success_criteria>

<output>
After completion, create `.planning/phases/144-evaluation-gate-eval/144-03-SUMMARY.md`
</output>
