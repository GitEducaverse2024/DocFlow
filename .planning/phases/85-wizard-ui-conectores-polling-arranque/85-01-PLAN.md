---
phase: 85-wizard-ui-conectores-polling-arranque
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/connectors/drive-folder-picker.tsx
  - app/src/app/connectors/page.tsx
  - app/messages/es.json
  - app/messages/en.json
autonomous: true
requirements: [WIZ-05, WIZ-08, WIZ-09]

must_haves:
  truths:
    - "DriveFolderPicker renders a lazy-loaded tree of Drive folders with breadcrumb navigation"
    - "DriveSubtitle shows SA email or OAuth account + root folder name for google_drive connectors"
    - "Drive card in /conectores has sky-500 badge and opens the Google Drive wizard on click"
  artifacts:
    - path: "app/src/components/connectors/drive-folder-picker.tsx"
      provides: "Lazy-loaded folder tree with breadcrumb and select button"
      min_lines: 80
    - path: "app/src/app/connectors/page.tsx"
      provides: "DriveSubtitle component + wizard open logic for google_drive type"
      contains: "DriveSubtitle"
    - path: "app/messages/es.json"
      provides: "i18n keys for google_drive type + drive wizard"
      contains: "google_drive"
  key_links:
    - from: "app/src/components/connectors/drive-folder-picker.tsx"
      to: "/api/connectors/google-drive/[id]/browse"
      via: "fetch with parent_id param"
      pattern: "browse\\?parent_id="
    - from: "app/src/app/connectors/page.tsx"
      to: "app/src/components/connectors/google-drive-wizard.tsx"
      via: "import + state toggle"
      pattern: "GoogleDriveWizard|driveWizardOpen"
---

<objective>
Create the DriveFolderPicker reusable component, wire the /conectores page with DriveSubtitle and Drive card click handler, and add all i18n keys needed for Drive UI.

Purpose: Foundation components and page wiring that the wizard (Plan 02) will consume. Separating the picker and page integration keeps the wizard plan focused on its 4-step flow.
Output: drive-folder-picker.tsx, updated connectors/page.tsx, i18n keys in es.json + en.json
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@app/src/app/connectors/page.tsx
@app/src/components/connectors/gmail-wizard.tsx
@app/src/app/api/connectors/google-drive/[id]/browse/route.ts
@app/src/lib/types.ts
@app/messages/es.json
@app/messages/en.json

<interfaces>
<!-- Key types the executor needs from Phase 82 -->

From app/src/lib/types.ts:
```typescript
export type DriveAuthMode = 'service_account' | 'oauth2';

export interface GoogleDriveConfig {
  auth_mode: DriveAuthMode;
  sa_email?: string;
  sa_credentials_encrypted?: string;
  client_id?: string;
  client_secret_encrypted?: string;
  refresh_token_encrypted?: string;
  oauth_email?: string;
  root_folder_id?: string;
  root_folder_name?: string;
}

export interface DriveFile {
  id: string;
  name: string;
  mimeType: string;
  modifiedTime?: string;
  size?: number;
  parents?: string[];
  webViewLink?: string;
}

export type DriveOperation = 'upload' | 'download' | 'list' | 'create_folder';
```

From app/src/app/api/connectors/google-drive/[id]/browse/route.ts:
```typescript
// GET /api/connectors/google-drive/[id]/browse?parent_id=root
// Returns: { folders: DriveFile[] }
```

From app/src/app/connectors/page.tsx:
```typescript
// Existing patterns:
// - GmailSubtitle component for gmail type subtitles
// - gmailWizardOpen state + setGmailWizardOpen for wizard toggling
// - TYPE_CONFIG record with google_drive entry (fields: [])
// - typeColors record with google_drive: sky-500 colors
// - Type grid onClick: if (type === 'gmail') open wizard
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DriveFolderPicker component + i18n keys</name>
  <files>app/src/components/connectors/drive-folder-picker.tsx, app/messages/es.json, app/messages/en.json</files>
  <action>
Create `drive-folder-picker.tsx` following the Gmail wizard pattern (dark theme, useTranslations).

**DriveFolderPicker props:**
```typescript
interface DriveFolderPickerProps {
  connectorId: string;       // needed for browse API
  value?: string;            // selected folder ID
  valueName?: string;        // selected folder display name
  onChange: (folderId: string, folderName: string) => void;
  disabled?: boolean;
}
```

**Behavior:**
1. Initially shows a button "Seleccionar carpeta" with folder icon. If `valueName` is set, show it as the selected folder.
2. On click, expand an inline tree panel (not a separate dialog).
3. Fetch folders from `GET /api/connectors/google-drive/${connectorId}/browse?parent_id=root` on first expand.
4. Each folder row: folder icon + name + expand arrow (if has children). Click arrow = lazy-load children from same endpoint with `parent_id={folderId}`.
5. Breadcrumb trail at top: "Mi Drive > Folder1 > Subfolder". Clicking a breadcrumb item navigates back.
6. "Seleccionar" button at bottom of tree to confirm selection. Calls `onChange(folderId, folderName)`.
7. Loading state: Loader2 spinner while fetching folders.
8. Error state: show error message with retry button.
9. Empty state: "No hay carpetas" message.

**Styling:** Dark theme per conventions:
- Container: `bg-zinc-900/50 border border-zinc-800 rounded-lg`
- Folder rows: `hover:bg-zinc-800/50`, indent with `pl-{level*4}`
- Breadcrumb: `text-xs text-zinc-400`, active crumb `text-sky-400`
- Selected folder: `bg-sky-500/10 border-sky-500/30`

**i18n keys:** Add to `connectors` namespace in both es.json and en.json:

```json
{
  "connectors": {
    "types": {
      "google_drive": {
        "label": "Google Drive",
        "description": "Conecta con Google Drive para indexar y gestionar archivos"
      }
    },
    "drive": {
      "steps": ["Tipo de cuenta", "Credenciales", "Prueba de conexion", "Confirmacion"],
      "stepTitles": {
        "1": "Elige el tipo de autenticacion",
        "2sa": "Configura Service Account",
        "2oauth": "Configura OAuth2",
        "3": "Verificando conexion...",
        "4": "Conector configurado"
      },
      "step1": {
        "saTitle": "Service Account",
        "saDescription": "Recomendado. Usa un archivo JSON de credenciales de Google Cloud. No requiere interaccion del usuario.",
        "saRecommended": "Recomendado",
        "oauthTitle": "OAuth2",
        "oauthDescription": "Usa Client ID y Secret de Google Cloud. Requiere autorizacion del usuario via navegador."
      },
      "step2sa": {
        "dropZoneLabel": "Arrastra el archivo JSON de Service Account aqui",
        "dropZoneHint": "o haz clic para seleccionar",
        "jsonUploaded": "Archivo cargado",
        "saEmail": "Email de la Service Account",
        "rootFolder": "Carpeta raiz",
        "rootFolderHint": "Selecciona la carpeta de Drive que quieres indexar",
        "helpTitle": "Como configurar una Service Account",
        "helpSteps": [
          "Ve a Google Cloud Console > IAM & Admin > Service Accounts",
          "Crea una Service Account nueva o usa una existente",
          "Genera una clave JSON y descargala",
          "Comparte la carpeta de Drive con el email de la SA"
        ]
      },
      "step2oauth": {
        "clientId": "Client ID",
        "clientSecret": "Client Secret",
        "generateUrl": "Generar URL de autorizacion",
        "waitingCallback": "Esperando autorizacion...",
        "authSuccess": "Autorizacion exitosa",
        "rootFolder": "Carpeta raiz"
      },
      "step3": {
        "authenticating": "Autenticando...",
        "listingFiles": "Listando archivos...",
        "verifyingPermissions": "Verificando permisos...",
        "retry": "Reintentar",
        "success": "Conexion exitosa",
        "error": "Error en la prueba"
      },
      "step4": {
        "ready": "Listo",
        "saEmail": "Service Account",
        "oauthEmail": "Cuenta OAuth2",
        "rootFolder": "Carpeta raiz",
        "filesFound": "archivos encontrados",
        "usageTitle": "Como usar este conector",
        "usageCanvas": "En Canvas: usa un nodo CONNECTOR tipo Google Drive",
        "usageCatBrain": "En CatBrains: anade una fuente tipo Google Drive",
        "usageCatBot": "En CatBot: usa los comandos list_drive_files, upload_to_drive"
      },
      "folderPicker": {
        "selectFolder": "Seleccionar carpeta",
        "myDrive": "Mi Drive",
        "select": "Seleccionar",
        "noFolders": "No hay carpetas",
        "loading": "Cargando carpetas...",
        "error": "Error al cargar carpetas",
        "retry": "Reintentar"
      },
      "subtitle": {
        "serviceAccount": "Service Account",
        "oauth2": "OAuth2"
      },
      "nav": {
        "back": "Atras",
        "next": "Siguiente",
        "create": "Crear conector",
        "cancel": "Cancelar"
      },
      "toasts": {
        "created": "Conector Google Drive creado",
        "testSuccess": "Conexion verificada correctamente",
        "testFailed": "Error en la prueba de conexion",
        "jsonInvalid": "El archivo JSON no es valido",
        "jsonMissing": "Sube el archivo JSON de la Service Account"
      }
    }
  }
}
```

Add equivalent English translations in en.json. Use `const t = useTranslations('connectors')` and access via `t('drive.folderPicker.selectFolder')` etc.

**CRITICAL conventions:**
- `process['env']['VARIABLE']` for any env access (unlikely here but follow pattern)
- `import { generateId } from '@/lib/utils'` if IDs needed
- All visible text via i18n, Spanish as primary
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>DriveFolderPicker component exists with lazy-loaded tree, breadcrumb, and select. All i18n keys for Drive wizard and type present in both es.json and en.json.</done>
</task>

<task type="auto">
  <name>Task 2: Wire /conectores page with DriveSubtitle + wizard open</name>
  <files>app/src/app/connectors/page.tsx, app/src/components/connectors/google-drive-wizard.tsx</files>
  <action>
**Step 1: Create minimal wizard stub** so the page can import it without error. Create `google-drive-wizard.tsx` with:
```typescript
'use client';

interface GoogleDriveWizardProps {
  open: boolean;
  onClose: () => void;
  onCreated: () => void;
}

export function GoogleDriveWizard({ open, onClose, onCreated }: GoogleDriveWizardProps) {
  if (!open) return null;
  return (
    <div>
      {/* Full implementation in Plan 02 */}
      <p>Google Drive Wizard placeholder</p>
    </div>
  );
}
```

**Step 2: Update connectors/page.tsx:**

1. Add import: `import { GoogleDriveWizard } from '@/components/connectors/google-drive-wizard'`
2. Add import of `GoogleDriveConfig` from `@/lib/types`
3. Add state: `const [driveWizardOpen, setDriveWizardOpen] = useState(false)`
4. In the type grid `onClick`: add `if (type === 'google_drive') { setDriveWizardOpen(true); return; }` alongside the existing gmail check
5. In the Sheet type selector grid: add same check for `google_drive` type (pattern already exists for gmail at line ~764)

6. **Add DriveSubtitle component** (follow GmailSubtitle pattern):
```typescript
function DriveSubtitle({ connector }: { connector: Connector }) {
  const t = useTranslations('connectors');
  const config = (() => {
    try {
      return typeof connector.config === 'string'
        ? JSON.parse(connector.config) as Partial<GoogleDriveConfig>
        : null;
    } catch { return null; }
  })();

  const authLabel = config?.auth_mode === 'oauth2'
    ? t('drive.subtitle.oauth2')
    : t('drive.subtitle.serviceAccount');
  const email = config?.auth_mode === 'oauth2'
    ? config?.oauth_email
    : config?.sa_email;

  return (
    <div className="flex flex-col">
      <p className="text-xs text-sky-400/70">{authLabel}</p>
      {email && (
        <p className="text-xs text-zinc-500 truncate max-w-[200px]">{email}</p>
      )}
      {config?.root_folder_name && (
        <p className="text-xs text-zinc-600 truncate max-w-[200px]">{config.root_folder_name}</p>
      )}
    </div>
  );
}
```

7. In the connector list table, update the subtitle rendering (around line 615):
```typescript
{connector.type === 'gmail' ? (
  <GmailSubtitle connector={connector} />
) : connector.type === 'google_drive' ? (
  <DriveSubtitle connector={connector} />
) : connector.description ? (
  <p className="text-xs text-zinc-500 truncate max-w-[200px]">
    {connector.description}
  </p>
) : null}
```

8. **Add GoogleDriveWizard render** at the bottom, after the GmailWizard (around line 860):
```typescript
<GoogleDriveWizard
  open={driveWizardOpen}
  onClose={() => setDriveWizardOpen(false)}
  onCreated={() => { setDriveWizardOpen(false); fetchConnectors(); }}
/>
```

The `google_drive` type already exists in TYPE_CONFIG and typeColors (sky-500 colors) from Phase 82, so the Drive card badge is already rendering with the correct color. The key change is making it open the wizard instead of the generic sheet.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Clicking Drive card in /conectores opens wizard dialog (stub). DriveSubtitle renders SA email or OAuth account + root folder for google_drive connectors. google_drive type badge shows with sky-500 color. Build passes.</done>
</task>

</tasks>

<verification>
1. TypeScript build: `cd ~/docflow/app && npx tsc --noEmit` passes with no errors
2. Next.js build: `cd ~/docflow/app && npm run build` completes successfully
3. DriveFolderPicker component exports correctly and accepts connectorId prop
4. DriveSubtitle renders for google_drive type connectors
5. Drive card in type grid opens driveWizardOpen state
6. All new i18n keys present in both es.json and en.json
</verification>

<success_criteria>
- drive-folder-picker.tsx exists with lazy-loaded tree, breadcrumb navigation, and folder selection
- /conectores page shows DriveSubtitle for google_drive connectors
- Clicking Drive type card opens the wizard (stub for now)
- i18n keys for all Drive wizard and type strings in both languages
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/85-wizard-ui-conectores-polling-arranque/85-01-SUMMARY.md`
</output>
