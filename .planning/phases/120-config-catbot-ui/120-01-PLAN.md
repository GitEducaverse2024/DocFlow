---
phase: 120-config-catbot-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/services/catbot-prompt-assembler.ts
  - app/src/app/api/catbot/chat/route.ts
  - app/src/lib/__tests__/catbot-prompt-assembler.test.ts
autonomous: true
requirements: [CONFIG-01, CONFIG-02, CONFIG-04]

must_haves:
  truths:
    - "instructions_primary del catbot_config se inyecta en el prompt como seccion P0"
    - "instructions_secondary del catbot_config se inyecta en el prompt como seccion P2"
    - "personality_custom del catbot_config se inyecta tras la personalidad base en la seccion identity"
    - "route.ts pasa instructions_primary, instructions_secondary y personality_custom a PromptAssembler"
  artifacts:
    - path: "app/src/lib/services/catbot-prompt-assembler.ts"
      provides: "Secciones de instrucciones primarias (P0), secundarias (P2), y personality_custom en identity"
      contains: "instructions_primary"
    - path: "app/src/app/api/catbot/chat/route.ts"
      provides: "Type actualizado que incluye los nuevos campos de config"
      contains: "instructions_primary"
    - path: "app/src/lib/__tests__/catbot-prompt-assembler.test.ts"
      provides: "Tests unitarios para instrucciones y personality_custom"
      contains: "instructions_primary"
  key_links:
    - from: "app/src/app/api/catbot/chat/route.ts"
      to: "app/src/lib/services/catbot-prompt-assembler.ts"
      via: "catbotConfig object passed to buildPrompt()"
      pattern: "catbotConfig.*instructions_primary"
---

<objective>
Wiring backend: PromptAssembler inyecta instrucciones primarias/secundarias y personalidad custom desde catbot_config, y route.ts pasa los campos nuevos.

Purpose: Los nuevos campos de config (instructions_primary, instructions_secondary, personality_custom) ya existen en el tipo PromptContext pero no se usan en build(). route.ts tiene un type hardcodeado que descarta estos campos. Este plan cierra ambos gaps.
Output: PromptAssembler inyecta los 3 campos nuevos en el prompt con prioridades correctas. Tests unitarios verifican el comportamiento.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-config-catbot-ui/120-RESEARCH.md
@.planning/phases/119-promptassembler/119-01-SUMMARY.md

<interfaces>
<!-- PromptContext ya tiene los campos, solo falta usarlos en build() -->

From app/src/lib/services/catbot-prompt-assembler.ts:
```typescript
export interface PromptContext {
  page?: string;
  channel?: 'web' | 'telegram';
  hasSudo: boolean;
  catbotConfig: {
    model?: string;
    personality?: string;
    allowed_actions?: string[];
    instructions_primary?: string;     // Existe en tipo, NO se usa en build()
    instructions_secondary?: string;   // Existe en tipo, NO se usa en build()
  };
  stats?: { catbrainsCount: number; catpawsCount: number; tasksCount: number; listeningCount: number; };
}
```

From app/src/app/api/catbot/chat/route.ts (line 65):
```typescript
// PROBLEMA: Este type descarta instructions_primary/secondary/personality_custom
let catbotConfig: { model?: string; personality?: string; allowed_actions?: string[] } = {};
```

From app/src/lib/services/catbot-prompt-assembler.ts build() function (line 528-619):
```typescript
// build() tiene secciones P0 (identity, tool_instructions), P1 (page knowledge, overview, skills, canvas, telegram),
// P2 (model intelligence, sudo, holded, canvas diagnostics), P3 (troubleshooting, email).
// NO hay secciones para instructions_primary, instructions_secondary, ni personality_custom.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PromptAssembler inyecta instrucciones y personality_custom + tests TDD</name>
  <files>app/src/lib/services/catbot-prompt-assembler.ts, app/src/lib/__tests__/catbot-prompt-assembler.test.ts</files>
  <behavior>
    - Test: build() con instructions_primary en catbotConfig retorna prompt que contiene el texto de instrucciones primarias
    - Test: build() con instructions_secondary en catbotConfig retorna prompt que contiene el texto de instrucciones secundarias
    - Test: build() con personality_custom en catbotConfig retorna prompt que contiene el texto custom de personalidad
    - Test: instructions_primary se inyecta como P0 (nunca truncada) y instructions_secondary como P2 (puede truncarse)
    - Test: instructions_primary de mas de 2500 chars se trunca con "..." como defensa en profundidad
  </behavior>
  <action>
RED: Anadir 5 tests al describe existente en catbot-prompt-assembler.test.ts:
1. `instructions_primary injected as P0 section` — build con `catbotConfig: { instructions_primary: 'Responde siempre en ingles' }` → resultado contiene 'Responde siempre en ingles'
2. `instructions_secondary injected as P2 section` — build con `catbotConfig: { instructions_secondary: 'Contexto: somos una empresa de marketing' }` → resultado contiene 'Contexto: somos una empresa de marketing'
3. `personality_custom injected in prompt` — build con `catbotConfig: { personality_custom: 'usa analogias de cocina' }` → resultado contiene 'usa analogias de cocina'
4. `instructions_primary is P0 (not truncated by budget)` — build con modelo libre + instructions_primary largo → resultado AUN contiene instructions_primary. Comparar con instructions_secondary que podria truncarse.
5. `instructions_primary truncated at 2500 chars` — build con instructions_primary de 3000 chars → resultado contiene solo los primeros ~2500 chars

GREEN: Modificar catbot-prompt-assembler.ts:

1. Anadir `personality_custom?: string` al tipo catbotConfig en PromptContext (ya tiene instructions_primary/secondary).

2. En build(), ANTES de las secciones P1, anadir:
```typescript
// P0: User primary instructions (always included, never truncated)
if (ctx.catbotConfig.instructions_primary?.trim()) {
  const text = ctx.catbotConfig.instructions_primary.slice(0, 2500);
  sections.push({
    id: 'instructions_primary',
    priority: 0,
    content: `## Instrucciones del administrador\n${text}${ctx.catbotConfig.instructions_primary.length > 2500 ? '...' : ''}`,
  });
}
```

3. Despues de las secciones P1, anadir:
```typescript
// P2: User secondary instructions (context, can be truncated)
if (ctx.catbotConfig.instructions_secondary?.trim()) {
  sections.push({
    id: 'instructions_secondary',
    priority: 2,
    content: `## Contexto adicional del administrador\n${ctx.catbotConfig.instructions_secondary}`,
  });
}
```

4. En buildIdentitySection(), tras la linea de personalidad (donde dice "Tu estilo de comunicacion es {personality}"), anadir:
```typescript
if (ctx.catbotConfig.personality_custom?.trim()) {
  lines.push(`\nInstrucciones adicionales de personalidad del administrador: ${ctx.catbotConfig.personality_custom}`);
}
```

Verificar que los 5 tests nuevos + los 8 existentes pasan.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts -x</automated>
  </verify>
  <done>13 tests pasan (8 existentes + 5 nuevos). instructions_primary inyectado como P0 con truncamiento a 2500 chars. instructions_secondary como P2. personality_custom en la seccion identity.</done>
</task>

<task type="auto">
  <name>Task 2: route.ts actualiza type de catbotConfig para pasar campos nuevos</name>
  <files>app/src/app/api/catbot/chat/route.ts</files>
  <action>
En route.ts linea 65, cambiar el type annotation de catbotConfig de:
```typescript
let catbotConfig: { model?: string; personality?: string; allowed_actions?: string[] } = {};
```
a:
```typescript
let catbotConfig: { model?: string; personality?: string; personality_custom?: string; allowed_actions?: string[]; instructions_primary?: string; instructions_secondary?: string } = {};
```

Esto permite que JSON.parse() de catbot_config (que ya contiene los campos nuevos si el usuario los guardo desde la UI) pase los valores a buildPrompt() sin descartarlos.

NO cambiar nada mas en route.ts — el objeto catbotConfig ya se pasa completo a buildPrompt() en linea 86.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts -x && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build exitoso sin errores de TypeScript. catbotConfig en route.ts acepta instructions_primary, instructions_secondary y personality_custom. Los campos fluyen desde la DB hasta PromptAssembler sin perderse.</done>
</task>

</tasks>

<verification>
- `cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts` — 13 tests pasan
- `cd /home/deskmath/docflow/app && npm run build` — build exitoso sin errores TS
</verification>

<success_criteria>
- instructions_primary se inyecta en el prompt como seccion P0 (nunca truncada, max 2500 chars)
- instructions_secondary se inyecta como seccion P2 (puede truncarse si el budget es bajo)
- personality_custom se anade a la seccion identity tras la personalidad base
- route.ts no descarta los campos nuevos de catbot_config
- Todos los tests pasan (existentes + nuevos)
</success_criteria>

<output>
After completion, create `.planning/phases/120-config-catbot-ui/120-01-SUMMARY.md`
</output>
