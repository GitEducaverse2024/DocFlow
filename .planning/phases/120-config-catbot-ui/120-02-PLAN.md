---
phase: 120-config-catbot-ui
plan: 02
type: execute
wave: 2
depends_on: ["120-01"]
files_modified:
  - app/src/app/settings/page.tsx
  - app/messages/es.json
  - app/messages/en.json
autonomous: false
requirements: [CONFIG-01, CONFIG-02, CONFIG-03, CONFIG-04]

must_haves:
  truths:
    - "El usuario ve campos de texto para instrucciones primarias y secundarias en Settings > CatBot"
    - "El usuario ve un textarea de personalidad custom debajo del dropdown de personalidad"
    - "Las 9 acciones normales aparecen como checkboxes agrupadas por categoria"
    - "Las acciones sudo protegidas aparecen como checkboxes en seccion separada"
    - "Los cambios se guardan correctamente y persisten al recargar la pagina"
  artifacts:
    - path: "app/src/app/settings/page.tsx"
      provides: "CatBotSettings expandido con instrucciones, personalidad custom y permisos completos"
      contains: "instructions_primary"
    - path: "app/messages/es.json"
      provides: "Claves i18n para todos los textos nuevos de la UI"
      contains: "instructionsPrimary"
    - path: "app/messages/en.json"
      provides: "Claves i18n en ingles para los textos nuevos"
      contains: "instructionsPrimary"
  key_links:
    - from: "app/src/app/settings/page.tsx"
      to: "/api/settings"
      via: "fetch POST con catbot_config JSON"
      pattern: "catbot_config.*instructions_primary"
    - from: "app/src/app/settings/page.tsx"
      to: "/api/catbot/sudo"
      via: "fetch POST con update_config action para protected_actions"
      pattern: "update_config.*protected_actions"
---

<objective>
UI expandida en Settings > CatBot: campos de instrucciones primarias/secundarias, textarea de personalidad custom, y checkboxes de permisos completas (9 acciones normales + 5 sudo).

Purpose: El usuario necesita poder configurar el comportamiento de CatBot desde la UI. Los campos backend ya estan wired (Plan 01), ahora falta la interfaz.
Output: CatBotSettings con todos los controles, i18n completo, persistencia funcional.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-config-catbot-ui/120-RESEARCH.md
@.planning/phases/120-config-catbot-ui/120-01-SUMMARY.md

<interfaces>
<!-- CatBotSettings component actual (settings/page.tsx lineas 384-523) -->

Estado actual del config state:
```typescript
const [config, setConfig] = useState({
  model: 'gemini-main',
  personality: 'friendly',
  allowed_actions: ['create_projects', 'create_agents', 'create_tasks', 'create_connectors', 'navigate'],
});
```

Acciones normales actuales en UI (solo 5 de 9):
```typescript
const actionKeys = ['create_catbrains', 'create_agents', 'create_tasks', 'create_connectors', 'navigate'];
// Faltan en UI: 'manage_models', 'manage_canvas', 'send_emails', 'manage_templates'
```

Acciones sudo protegidas (de catbot_sudo config):
```typescript
protected_actions: ['bash_execute', 'service_manage', 'file_operation', 'credential_manage', 'mcp_bridge']
```

API de sudo para update_config:
```typescript
// POST /api/catbot/sudo con body:
{ action: 'update_config', duration_minutes, protected_actions, enabled }
```

CatBotSecurity component (lineas 119-370 en settings/page.tsx):
- Ya lee catbot_sudo config
- Ya tiene checkboxes para protected_actions
- Ya usa update_config action para guardar
```

i18n actual (settings.catbot):
```json
{
  "title": "CatBot -- Asistente IA",
  "modelLabel": "Modelo LLM",
  "personality": "Personalidad",
  "allowedActions": "Acciones permitidas",
  "actions": {
    "create_catbrains": "Crear CatBrains",
    "create_agents": "Crear agentes",
    "create_tasks": "Crear tareas",
    "create_connectors": "Crear conectores",
    "navigate": "Navegar entre paginas"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandir CatBotSettings con instrucciones, personality_custom y permisos completos</name>
  <files>app/src/app/settings/page.tsx, app/messages/es.json, app/messages/en.json</files>
  <action>
**PASO 1: Actualizar i18n** (es.json y en.json)

Anadir las siguientes claves bajo `settings.catbot`:

```json
{
  "instructionsPrimary": "Instrucciones primarias",
  "instructionsPrimaryHint": "Texto siempre inyectado en el prompt de CatBot. Max 2000 caracteres.",
  "instructionsPrimaryPlaceholder": "Ej: Responde siempre en espanol. Cuando el usuario pida crear un agente, sugiere siempre un nombre descriptivo.",
  "instructionsSecondary": "Instrucciones secundarias",
  "instructionsSecondaryHint": "Contexto adicional de menor prioridad. Puede truncarse si el modelo tiene budget limitado.",
  "instructionsSecondaryPlaceholder": "Ej: Somos una empresa de marketing digital. Nuestros clientes principales son pymes.",
  "personalityCustom": "Personalidad personalizada",
  "personalityCustomHint": "Texto libre que complementa la personalidad seleccionada arriba.",
  "personalityCustomPlaceholder": "Ej: Usa analogias de cocina para explicar conceptos tecnicos.",
  "charCount": "{count} / {max} caracteres",
  "actionsGroupContent": "Contenido",
  "actionsGroupNavigation": "Navegacion",
  "actionsGroupModels": "Modelos e integraciones",
  "actions": {
    "create_catbrains": "Crear CatBrains",
    "create_agents": "Crear agentes",
    "create_tasks": "Crear tareas",
    "create_connectors": "Crear conectores",
    "navigate": "Navegar entre paginas",
    "manage_models": "Gestionar modelos y alias",
    "manage_canvas": "Gestionar canvas (crear/editar)",
    "send_emails": "Enviar emails",
    "manage_templates": "Gestionar plantillas de email"
  }
}
```

Anadir equivalentes en en.json con textos en ingles.

**PASO 2: Expandir config state**

Cambiar el useState inicial de CatBotSettings:
```typescript
const [config, setConfig] = useState({
  model: 'gemini-main',
  personality: 'friendly',
  personality_custom: '',
  instructions_primary: '',
  instructions_secondary: '',
  allowed_actions: ['create_projects', 'create_agents', 'create_tasks', 'create_connectors', 'navigate'],
});
```

**PASO 3: Expandir la UI** (dentro del CardContent, despues del dropdown de personalidad y antes de las acciones)

3a. Despues del dropdown de personalidad, anadir textarea de personality_custom:
```tsx
<div>
  <Label className="text-zinc-300 text-sm">{t('catbot.personalityCustom')}</Label>
  <textarea
    value={config.personality_custom}
    onChange={e => setConfig(prev => ({ ...prev, personality_custom: e.target.value }))}
    placeholder={t('catbot.personalityCustomPlaceholder')}
    maxLength={500}
    rows={2}
    className="w-full max-w-lg rounded-md bg-zinc-950 border border-zinc-800 text-zinc-50 px-3 py-2 text-sm mt-1 focus:outline-none focus:border-violet-500 resize-none"
  />
  <p className="text-xs text-zinc-500 mt-1">{t('catbot.personalityCustomHint')}</p>
</div>
```

3b. Anadir seccion de instrucciones primarias con contador de caracteres:
```tsx
<div>
  <Label className="text-zinc-300 text-sm">{t('catbot.instructionsPrimary')}</Label>
  <textarea
    value={config.instructions_primary}
    onChange={e => setConfig(prev => ({ ...prev, instructions_primary: e.target.value.slice(0, 2000) }))}
    placeholder={t('catbot.instructionsPrimaryPlaceholder')}
    maxLength={2000}
    rows={3}
    className="w-full max-w-lg rounded-md bg-zinc-950 border border-zinc-800 text-zinc-50 px-3 py-2 text-sm mt-1 focus:outline-none focus:border-violet-500 resize-none"
  />
  <div className="flex justify-between max-w-lg">
    <p className="text-xs text-zinc-500 mt-1">{t('catbot.instructionsPrimaryHint')}</p>
    <p className="text-xs text-zinc-500 mt-1">{config.instructions_primary.length} / 2000</p>
  </div>
</div>
```

3c. Anadir seccion de instrucciones secundarias:
```tsx
<div>
  <Label className="text-zinc-300 text-sm">{t('catbot.instructionsSecondary')}</Label>
  <textarea
    value={config.instructions_secondary}
    onChange={e => setConfig(prev => ({ ...prev, instructions_secondary: e.target.value }))}
    placeholder={t('catbot.instructionsSecondaryPlaceholder')}
    rows={3}
    className="w-full max-w-lg rounded-md bg-zinc-950 border border-zinc-800 text-zinc-50 px-3 py-2 text-sm mt-1 focus:outline-none focus:border-violet-500 resize-none"
  />
  <p className="text-xs text-zinc-500 mt-1">{t('catbot.instructionsSecondaryHint')}</p>
</div>
```

3d. Expandir actionKeys para incluir las 9 acciones normales, agrupadas por categoria:
```typescript
const actionGroups = {
  content: ['create_catbrains', 'create_agents', 'create_tasks', 'create_connectors'],
  navigation: ['navigate'],
  models: ['manage_models', 'manage_canvas', 'send_emails', 'manage_templates'],
};
```

Reemplazar el listado plano de checkboxes por grupos con subheadings:
```tsx
<div>
  <Label className="text-zinc-300 text-sm mb-2 block">{t('catbot.allowedActions')}</Label>
  <div className="space-y-3">
    {Object.entries(actionGroups).map(([group, keys]) => (
      <div key={group}>
        <p className="text-xs text-zinc-500 uppercase tracking-wide mb-1">
          {t(`catbot.actionsGroup${group.charAt(0).toUpperCase() + group.slice(1)}`)}
        </p>
        <div className="space-y-1.5 ml-1">
          {keys.map(key => (
            <label key={key} className="flex items-center gap-2 cursor-pointer">
              <Checkbox
                checked={config.allowed_actions.includes(key)}
                onCheckedChange={() => toggleAction(key)}
              />
              <span className="text-sm text-zinc-300">{t(`catbot.actions.${key}`)}</span>
            </label>
          ))}
        </div>
      </div>
    ))}
    <label className="flex items-center gap-2 opacity-50 cursor-not-allowed">
      <Checkbox checked={false} disabled />
      <span className="text-sm text-zinc-500">{t('catbot.deleteDisabled')}</span>
    </label>
  </div>
</div>
```

**IMPORTANTE:** No tocar CatBotSecurity. Las protected_actions de sudo YA se editan ahi (lineas 340-360 de settings/page.tsx). CONFIG-03 se cumple porque: (1) acciones normales estan en CatBotSettings con los 9 keys, (2) acciones sudo estan en CatBotSecurity que ya existe. Ambas secciones son visibles en la misma pagina de Settings.

**PASO 4: Verificar que handleSave envia todos los campos**

El handleSave actual ya envia `JSON.stringify(config)` -- como config ahora incluye los campos nuevos, se guardan automaticamente. No necesita cambios.

**Convencion dark theme:** Usar bg-zinc-950 para inputs, border-zinc-800, text-zinc-50. Focus: focus:border-violet-500.
**Convencion i18n:** Todas las strings visibles via t('catbot.xxx'). Cero texto hardcodeado.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Build exitoso. UI de CatBotSettings muestra: (1) textarea de personality_custom debajo del dropdown, (2) textarea de instrucciones primarias con contador 0/2000, (3) textarea de instrucciones secundarias, (4) 9 checkboxes de acciones normales agrupadas por categoria. Todas las claves i18n existen en es.json y en.json.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacion visual y funcional de Settings CatBot expandido</name>
  <what-built>CatBot Settings expandido con instrucciones primarias/secundarias, personalidad custom y 9 permisos agrupados. El backend ya inyecta los campos nuevos en el prompt (Plan 01).</what-built>
  <how-to-verify>
    1. Abrir http://localhost:3500/settings
    2. En la seccion CatBot, verificar que aparecen los campos nuevos:
       - Debajo del dropdown de personalidad: textarea "Personalidad personalizada"
       - Instrucciones primarias: textarea con contador de caracteres (max 2000)
       - Instrucciones secundarias: textarea sin limite visible
       - Acciones permitidas: 9 checkboxes en 3 grupos (Contenido, Navegacion, Modelos e integraciones)
    3. Escribir algo en instrucciones primarias, guardar, recargar pagina -- el texto persiste
    4. Escribir algo en personality_custom, guardar, recargar -- persiste
    5. Marcar/desmarcar acciones nuevas (manage_models, send_emails, etc.), guardar, recargar -- persisten
    6. Verificar con CatBot: abrir chat, preguntar algo. Si las instrucciones primarias dicen "responde en ingles", CatBot deberia responder en ingles
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe los problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- `cd /home/deskmath/docflow/app && npm run build` -- build exitoso
- Verificacion visual en http://localhost:3500/settings
- Verificacion funcional: config se guarda y recarga correctamente
- Verificacion con CatBot: instrucciones primarias se reflejan en comportamiento
</verification>

<success_criteria>
- CatBotSettings muestra 3 textareas nuevos (personality_custom, instructions_primary, instructions_secondary)
- instructions_primary tiene limite de 2000 chars con contador visible
- 9 acciones normales agrupadas en 3 categorias con checkboxes
- Los campos nuevos se persisten en catbot_config y se recargan al abrir la pagina
- CatBot refleja las instrucciones primarias en su comportamiento
</success_criteria>

<output>
After completion, create `.planning/phases/120-config-catbot-ui/120-02-SUMMARY.md`
</output>
