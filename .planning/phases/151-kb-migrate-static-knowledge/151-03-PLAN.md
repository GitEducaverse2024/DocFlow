---
phase: 151-kb-migrate-static-knowledge
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .docflow-kb/protocols/orquestador-catflow.md
  - .docflow-kb/runtime/strategist.prompt.md
  - .docflow-kb/runtime/decomposer.prompt.md
  - .docflow-kb/runtime/architect.prompt.md
  - .docflow-kb/runtime/canvas-qa.prompt.md
  - .docflow-kb/runtime/agent-autofix.prompt.md
  - .planning/phases/151-kb-migrate-static-knowledge/migration-log-plan-03.md
  - skill_orquestador_catbot_enriched.md
autonomous: true

requirements: [KB-12, KB-13, KB-14]

must_haves:
  truths:
    - "`skill_orquestador_catbot_enriched.md` (raíz del repo, 890 líneas, 14 PARTES) existe ahora como `.docflow-kb/protocols/orquestador-catflow.md` con frontmatter completo"
    - "Las 5 constantes exportadas de `app/src/lib/services/catbot-pipeline-prompts.ts` (STRATEGIST_PROMPT, DECOMPOSER_PROMPT, ARCHITECT_PROMPT, CANVAS_QA_PROMPT, AGENT_AUTOFIX_PROMPT) existen como 5 archivos atómicos en `.docflow-kb/runtime/*.prompt.md` con frontmatter `type: runtime`"
    - "El archivo `catbot-pipeline-prompts.ts` NO se modifica funcionalmente — las constantes siguen exportadas con el mismo nombre y el mismo string literal (contrato intacto con callers). El cambio de 'hardcoded → KB-loaded' es Phase 152 (KB CatBot Consume). Este plan SOLO extrae el contenido al KB como documentación paralela"
    - "`skill_orquestador_catbot_enriched.md` en la raíz del repo recibe el redirect stub markdown estándar (NO se borra, Phase 155 lo moverá)"
    - "Los 5 `.prompt.md` siguen en el código COMO ESTÁN (hardcoded). Este plan no toca `catbot-pipeline-prompts.ts`. La migración documental al KB es lectura paralela, no reemplazo de fuente"
    - "Cada archivo `runtime/*.prompt.md` incluye en el body un bloque de código ```text con el prompt VERBATIM (preservando backticks, variables `{{RULES_INDEX}}`, saltos de línea) — para que cuando Phase 152 haga `loadPrompt(name)` no haya deriva con el código"
    - "Cada archivo `runtime/*.prompt.md` declara en `source_of_truth`: `[{source: typescript, path: app/src/lib/services/catbot-pipeline-prompts.ts, export: <NAME>_PROMPT}]`"
    - "Todos los archivos migrados pasan `node scripts/validate-kb.cjs`"
    - "Migration log lives OUTSIDE `.docflow-kb/` (in `.planning/phases/151-kb-migrate-static-knowledge/migration-log-plan-03.md`) — same rationale as Plans 01/02: validate-kb.cjs walks dotfiles and requires frontmatter on every `.md` inside `.docflow-kb/`"
  artifacts:
    - path: ".docflow-kb/protocols/orquestador-catflow.md"
      provides: "Full skill Orquestador (14 PARTES) migrated with frontmatter"
      contains: "PARTE 1"
    - path: ".docflow-kb/runtime/"
      provides: "5 prompt atoms extracted verbatim from catbot-pipeline-prompts.ts"
      contains: "strategist.prompt.md"
    - path: ".planning/phases/151-kb-migrate-static-knowledge/migration-log-plan-03.md"
      provides: "Mapping of 2 sources → 6 destinations with verbatim-preservation invariants (lives outside KB)"
  key_links:
    - from: "skill_orquestador_catbot_enriched.md (root)"
      to: ".docflow-kb/protocols/orquestador-catflow.md"
      via: "redirect stub at top of original"
      pattern: "MOVED to .docflow-kb"
    - from: ".docflow-kb/runtime/*.prompt.md"
      to: "app/src/lib/services/catbot-pipeline-prompts.ts"
      via: "source_of_truth frontmatter field points back to typescript export"
      pattern: "export: .*_PROMPT"
    - from: ".docflow-kb/runtime/architect.prompt.md"
      to: "loadRulesIndex() at call-time"
      via: "body preserves `{{RULES_INDEX}}` placeholder verbatim"
      pattern: "\\{\\{RULES_INDEX\\}\\}"
---

<objective>
Migrar el silo C (skill orquestador en raíz) + el silo F (system prompts hardcoded en `catbot-pipeline-prompts.ts`) al KB. Skill → `protocols/orquestador-catflow.md`. Prompts → 5 archivos atómicos en `runtime/*.prompt.md` con contenido VERBATIM (preservando `{{RULES_INDEX}}`, estructura Markdown embebida, JSON contracts literales).

Purpose: PRD KB Fase 3 §7 "System prompts de código → `runtime/*.prompt.md` + código los lee desde ahí". Este plan hace la MITAD 1 (extrae a KB); Phase 152 hará la MITAD 2 (`loadPrompt(name)` en código). Crítico: los prompts en TS NO se tocan — el contrato con el IntentJobExecutor y canvas-auto-repair debe mantenerse byte-idéntico.

Output: 1 protocol atom (orquestador, 890 líneas) + 5 runtime prompt atoms + redirect stub en la skill raíz.

**IMPORTANT — migration log location:** `migration-log-plan-03.md` vive en `.planning/phases/151-kb-migrate-static-knowledge/` (fuera del KB). Razón idéntica a Plans 01/02.

CRITICAL NON-MODIFICATION ZONE: `app/src/lib/services/catbot-pipeline-prompts.ts` — NO SE TOCA. Phase 152 lo modifica. Si se modifica aquí, rompe IntentJobExecutor + canvas-auto-repair.ts que leen las constantes directamente.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/ANALYSIS-knowledge-base-architecture.md
@skill_orquestador_catbot_enriched.md
@app/src/lib/services/catbot-pipeline-prompts.ts
@.docflow-kb/_schema/frontmatter.schema.json
@.docflow-kb/_schema/tag-taxonomy.json
@scripts/validate-kb.cjs
@CLAUDE.md

<interfaces>
<!-- Frontmatter schema (same as Plan 01) — see 151-01-PLAN.md <interfaces> block -->

<!-- Prompt exports in catbot-pipeline-prompts.ts (verified via grep): -->
- Line 16: `export const STRATEGIST_PROMPT = \`...\`` (~3 lines)
- Line 20: `export const DECOMPOSER_PROMPT = \`...\`` (~5 lines)
- Line 26: `export const ARCHITECT_PROMPT = \`...\`` (~160 lines, contains `{{RULES_INDEX}}` placeholder)
- Line 188: `export const CANVAS_QA_PROMPT = \`...\`` (~70 lines)
- Line 257: `export const AGENT_AUTOFIX_PROMPT = \`...\`` (~25 lines)

Total file: 283 lines. Each export is a single template-literal string.

<!-- Target frontmatter for runtime prompts: -->
```yaml
---
id: runtime-<name>-prompt           # e.g. runtime-strategist-prompt
type: runtime
subtype: pipeline-prompt
lang: es                            # prompts are in Spanish (per content)
title: <Name> Prompt
summary: <what the prompt does, <200 chars>
tags: [catflow, canvas, <role from taxonomy>]
audience: [catbot, architect, developer]
status: active
created_at: 2026-04-20T00:00:00Z
created_by: catbot:phase-151
version: 1.0.0
updated_at: 2026-04-20T00:00:00Z
updated_by: catbot:phase-151
source_of_truth:
  - source: typescript
    path: app/src/lib/services/catbot-pipeline-prompts.ts
    export: <NAME>_PROMPT
change_log:
  - { version: 1.0.0, date: 2026-04-20, author: catbot:phase-151, change: "Extracted verbatim from catbot-pipeline-prompts.ts — code path still uses the TS constant; KB copy is parallel read until Phase 152 migrates loadPrompt()" }
ttl: never
---
```

NOTE on source_of_truth: the schema accepts either `null` OR an array. Array items don't have a fixed item-schema in the current validator (inspect validate-kb.cjs: source_of_truth shape is permissive as long as it's array/null). Use the shape shown above — the semantic contract is documented in the frontmatter even if the validator doesn't enforce item keys.

<!-- Skill orquestador PARTEs (from grep "^## PARTE"): 14 PARTES -->
PARTE 1: ARQUITECTURA (line 18)
PARTE 2: PROTOCOLO DE DECISION ANTES DE CREAR (line 110)
PARTE 3: PROTOCOLO DE CREACION DE CATPAW (line 170)
PARTE 4: PROTOCOLO DE CONSTRUCCION DE CANVAS (line 262)
PARTE 5: CATALOGO DE CONECTORES CONOCIDOS (line 369)
PARTE 6: REGLAS DE INTERACCION CON EL USUARIO (line 401)
PARTE 7: EJEMPLO COMPLETO: Flujo LeadHunting (line 449)
PARTE 8: ERRORES COMUNES Y COMO EVITARLOS (line 516)
PARTE 9: PROMPT RAPIDO PARA GENERAR SYSTEM PROMPTS (line 531)
PARTE 10: MAPA DE DATOS REALES POR TIPO DE NODO (line 564)
PARTE 11: EJECUCION AVANZADA: TOOL-CALLING EN CATPAWS (line 640)
PARTE 12: CADENA DE DATOS ENTRE NODOS (line 702)
PARTE 13: DIAGNOSTICO DE EJECUCIONES Y TROUBLESHOOTING (line 785)
PARTE 14: TIPOS DE NODO DEL CANVAS vs CATPAWS (line 832)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 5 prompts VERBATIM from catbot-pipeline-prompts.ts into runtime/*.prompt.md</name>
  <files>
    .docflow-kb/runtime/strategist.prompt.md
    .docflow-kb/runtime/decomposer.prompt.md
    .docflow-kb/runtime/architect.prompt.md
    .docflow-kb/runtime/canvas-qa.prompt.md
    .docflow-kb/runtime/agent-autofix.prompt.md
  </files>
  <action>
Extract each of the 5 `export const <NAME>_PROMPT = \`...\`;` template literals from `app/src/lib/services/catbot-pipeline-prompts.ts` into a separate KB file.

For each prompt:

1. Locate the export in the TS file (line numbers in `<interfaces>` above).
2. Capture EVERYTHING between the opening backtick and the closing backtick. This is the VERBATIM body.
3. Preserve:
   - All `{{VARIABLE}}` placeholders (e.g. `{{RULES_INDEX}}` in ARCHITECT_PROMPT).
   - All escaped backticks `\\\`` — when embedded in a ```text fence, they remain `\\\`` OR can be unescaped to \` (safer: unescape, since the target is Markdown not a TS string). Document the choice in `change_log`.
   - All embedded JSON blocks.
   - All newlines.
4. Write the file with structure:

```markdown
---
<frontmatter per <interfaces> block>
---

# <Title>

## Purpose

<1-paragraph description based on the JSDoc comment at the top of the TS file OR on first-principles inspection of the prompt>

## Prompt source of truth

- **TypeScript export:** `<NAME>_PROMPT` in `app/src/lib/services/catbot-pipeline-prompts.ts`
- **Callers (as of Phase 151):** <from grep or inspection — e.g. `IntentJobExecutor.run()` for strategist/decomposer/architect/qa; `canvas-auto-repair.ts` for agent-autofix>
- **Template variables:** <list `{{X}}` placeholders, where resolution happens; e.g. `{{RULES_INDEX}}` → `loadRulesIndex()` at IntentJobExecutor.run() call-time>

## Prompt body (VERBATIM from TS export)

\`\`\`text
<exact content of the template literal>
\`\`\`

## Change log

See frontmatter `change_log`.
```

**Per-prompt specifics:**

- **strategist.prompt.md:**
  - summary: "Fase strategist del pipeline async: recibe request del usuario y produce goal + success_criteria + estimated_steps en JSON"
  - tags: `[catflow, canvas, ops]`

- **decomposer.prompt.md:**
  - summary: "Fase decomposer: parte un goal en 3-8 tareas atómicas con depends_on + expected_output"
  - tags: `[catflow, canvas, ops]`

- **architect.prompt.md:**
  - summary: "Fase architect: construye canvas ejecutable desde goal+tasks+resources, declara `data.role` por nodo, consume rules index via {{RULES_INDEX}}"
  - tags: `[catflow, canvas, extractor, transformer, synthesizer, renderer, emitter, guard, reporter]`
  - CRITICAL: body contains `{{RULES_INDEX}}` — preserve byte-identical; document under "Template variables".

- **canvas-qa.prompt.md:**
  - summary: "Canvas QA role-aware v135: audita canvas_proposal leyendo data.role por nodo antes de aplicar reglas condicionales"
  - tags: `[catflow, canvas, testing, guard]`

- **agent-autofix.prompt.md:**
  - summary: "Canvas Auto-Reparador: un condition guard falló justo antes de un nodo con side effects; propone fix ajustando instructions del nodo problemático o upstream"
  - tags: `[catflow, canvas, safety]`

**Invariant check:**
After writing, run a diff between the extracted body (content between the ```text fences) and the original template literal body. They must be identical except for the 2 allowed transformations (escaped backtick un-escaping, trimming leading/trailing newline from template literal). Preserve ALL indentation, all markdown-in-prompt, all JSON, all embedded ```json blocks.

**Extraction tooling:**
Use Node.js inline to extract safely:
```bash
node -e "
const src = require('fs').readFileSync('app/src/lib/services/catbot-pipeline-prompts.ts', 'utf8');
const re = /export const (\w+)_PROMPT = \`([\s\S]*?)\`;/g;
let m;
while ((m = re.exec(src))) console.log('=== ' + m[1] + ' ==='), console.log(m[2].replace(/\\\\\`/g, '\`').trim());
"
```

Use this extraction to populate the ```text fences. Do NOT copy-paste from the IDE view — escape sequences get lost.
  </action>
  <verify>
    <automated>node scripts/validate-kb.cjs 2>&1 | tail -5 && ls .docflow-kb/runtime/*.prompt.md | wc -l | grep -q '^5$' && grep -q "{{RULES_INDEX}}" .docflow-kb/runtime/architect.prompt.md && grep -q "data.role" .docflow-kb/runtime/canvas-qa.prompt.md && echo OK</automated>
  </verify>
  <done>
    - 5 `.prompt.md` files exist under `.docflow-kb/runtime/`
    - Each has the prompt body inside a ```text fence
    - `{{RULES_INDEX}}` preserved in architect.prompt.md
    - `source_of_truth` in frontmatter points back to the TS export
    - `app/src/lib/services/catbot-pipeline-prompts.ts` is BYTE-IDENTICAL to before this task (NOT modified)
    - `validate-kb.cjs` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate skill_orquestador_catbot_enriched.md to .docflow-kb/protocols/orquestador-catflow.md + write migration log</name>
  <files>
    .docflow-kb/protocols/orquestador-catflow.md
    skill_orquestador_catbot_enriched.md
    .planning/phases/151-kb-migrate-static-knowledge/migration-log-plan-03.md
  </files>
  <action>
**Part A — Migrate skill to protocols:**

1. Read `skill_orquestador_catbot_enriched.md` (890 lines, 14 PARTES per interfaces block).
2. Write `.docflow-kb/protocols/orquestador-catflow.md`:
   - Frontmatter:
     ```yaml
     id: protocol-orquestador-catflow
     type: protocol
     subtype: skill-orquestador
     lang: es
     title: "Orquestador Inteligente DoCatFlow"
     summary: "Skill maestra de CatBot: lógica de decisión para reutilizar/crear CatPaws, estructurar canvas, vincular conectores. 14 PARTES cubriendo arquitectura, decisión, creación, canvas-building, catálogos, interacción, ejemplos, errores, data contracts, tool-calling avanzado, diagnóstico."
     tags: [canvas, catflow, catpaw, connector, catbrain]
     audience: [catbot, architect, developer]
     status: active
     created_at: 2026-04-20T00:00:00Z
     created_by: catbot:phase-151
     version: 2.0.0
     # NOTE: version 2.0.0 (not 1.0.0) because the source file header says "Version 2.0 — Marzo 2026" — preserve semver of source protocol
     updated_at: 2026-04-20T00:00:00Z
     updated_by: catbot:phase-151
     source_of_truth:
       - source: file
         path: skill_orquestador_catbot_enriched.md
     change_log:
       - { version: 2.0.0, date: 2026-04-20, author: catbot:phase-151, change: "Migrated verbatim from repo root to KB protocols/; 14 PARTES preserved" }
     ttl: never
     ---
     ```
   - Body: the ENTIRE original content of `skill_orquestador_catbot_enriched.md` MINUS its own `# SKILL: Orquestador Inteligente DoCatFlow` H1 + `## Version 2.0 — Marzo 2026` line (those are now in frontmatter). Start body with `# Orquestador Inteligente DoCatFlow` then immediately `## PARTE 1 — ARQUITECTURA DE DOCATFLOW (lo que CatBot debe saber)`.
   - Preserve: all code blocks, all tables, all PARTE 1..14 headings and content, all internal cross-references.

3. Run `wc -l .docflow-kb/protocols/orquestador-catflow.md`. Expected: ~890 lines body + ~15 lines frontmatter ≈ 905 ± 10 lines.

**Part B — Redirect stub in the raíz original:**

Inject at the TOP of `skill_orquestador_catbot_enriched.md` (line 1, BEFORE existing content):

```markdown
> **⚠️ MOVED to `.docflow-kb/protocols/orquestador-catflow.md`** during Phase 151 (2026-04-20).
>
> This file lives in the repo root as a leftover from pre-KB era. The canonical location is now the KB.
> Content below preserved for reference — new edits MUST happen in the KB.
> Eliminación física: Phase 155 (cleanup final).

---

<original content, unchanged>
```

**Part C — Write migration log (OUTSIDE the KB):**

`.planning/phases/151-kb-migrate-static-knowledge/migration-log-plan-03.md` (NOT `.docflow-kb/.migration-log-plan-03.md` — placing it in the KB would fail validate-kb.cjs because it walks dotfiles):
```markdown
# Migration log — Plan 151-03 (skill + system prompts migration)

## Silo C — Skill orquestador (root)

| Source | Destination |
|--------|-------------|
| skill_orquestador_catbot_enriched.md (root, 890 lines) | .docflow-kb/protocols/orquestador-catflow.md |

Stub strategy: redirect at top, content preserved until Phase 155.

## Silo F — Runtime prompts (typescript hardcoded)

| TS Export | Line | Destination |
|-----------|------|-------------|
| STRATEGIST_PROMPT | 16 | .docflow-kb/runtime/strategist.prompt.md |
| DECOMPOSER_PROMPT | 20 | .docflow-kb/runtime/decomposer.prompt.md |
| ARCHITECT_PROMPT | 26 | .docflow-kb/runtime/architect.prompt.md |
| CANVAS_QA_PROMPT | 188 | .docflow-kb/runtime/canvas-qa.prompt.md |
| AGENT_AUTOFIX_PROMPT | 257 | .docflow-kb/runtime/agent-autofix.prompt.md |

**NON-MODIFICATION invariant:** `app/src/lib/services/catbot-pipeline-prompts.ts` is NOT modified by this plan. Phase 152 owns the refactor from "hardcoded" to "loadPrompt()". KB copies are parallel reads until then — `source_of_truth` in each runtime/*.prompt.md frontmatter points back to the TS export.

**Template variable preservation:**
- `{{RULES_INDEX}}` in architect.prompt.md → resolved at call-time by IntentJobExecutor via loadRulesIndex().
```

**Invariants:**
- `skill_orquestador_catbot_enriched.md` NOT deleted, only prepended with stub.
- `.docflow-kb/protocols/orquestador-catflow.md` passes `validate-kb.cjs`.
- `app/src/lib/services/catbot-pipeline-prompts.ts` UNCHANGED (`git diff app/src/lib/services/catbot-pipeline-prompts.ts` shows zero changes).
- NO `.md` files inside `.docflow-kb/` that start with `.` — migration log is in the phase dir.
  </action>
  <verify>
    <automated>test -f .docflow-kb/protocols/orquestador-catflow.md && wc -l .docflow-kb/protocols/orquestador-catflow.md | awk '{if($1<850) exit 1}' && head -3 skill_orquestador_catbot_enriched.md | grep -q "MOVED to" && git diff --quiet app/src/lib/services/catbot-pipeline-prompts.ts && node scripts/validate-kb.cjs 2>&1 | tail -5 && test -f .planning/phases/151-kb-migrate-static-knowledge/migration-log-plan-03.md && echo OK</automated>
  </verify>
  <done>
    - `.docflow-kb/protocols/orquestador-catflow.md` exists (~900 lines, 14 PARTES preserved)
    - `skill_orquestador_catbot_enriched.md` has redirect stub at top; original content intact
    - `app/src/lib/services/catbot-pipeline-prompts.ts` NOT modified (git diff empty for this file)
    - `.planning/phases/151-kb-migrate-static-knowledge/migration-log-plan-03.md` exists
    - `validate-kb.cjs` exits 0
  </done>
</task>

</tasks>

<verification>
```bash
# Core invariants
node scripts/validate-kb.cjs

# Runtime prompts extracted (5 files)
ls .docflow-kb/runtime/*.prompt.md | wc -l   # 5

# Protocol migrated (1 file, 14 PARTES)
grep -c "^## PARTE" .docflow-kb/protocols/orquestador-catflow.md   # 14

# Critical non-modification
git diff --stat app/src/lib/services/catbot-pipeline-prompts.ts   # empty output

# Template variable preserved
grep "{{RULES_INDEX}}" .docflow-kb/runtime/architect.prompt.md   # at least 1 match

# Redirect in root skill
head -3 skill_orquestador_catbot_enriched.md | grep "MOVED to"

# Migration log OUTSIDE the KB
test -f .planning/phases/151-kb-migrate-static-knowledge/migration-log-plan-03.md
ls .docflow-kb/.migration-log*.md 2>/dev/null | wc -l   # 0
```
</verification>

<success_criteria>
- 5 `.prompt.md` files under `.docflow-kb/runtime/` with verbatim prompt bodies (KB-12)
- 1 protocol file `orquestador-catflow.md` under `.docflow-kb/protocols/` with all 14 PARTES (KB-12)
- Skill original has redirect stub at top (KB-13)
- `catbot-pipeline-prompts.ts` UNCHANGED — Phase 152 owns the loadPrompt() refactor
- All migrated files pass `validate-kb.cjs` (KB-14)
- `{{RULES_INDEX}}` placeholder preserved byte-identical in architect.prompt.md
- Migration log lives in the phase directory (NOT in `.docflow-kb/`)
</success_criteria>

<output>
After completion, create `.planning/phases/151-kb-migrate-static-knowledge/151-03-SUMMARY.md`
</output>
</content>
</invoke>