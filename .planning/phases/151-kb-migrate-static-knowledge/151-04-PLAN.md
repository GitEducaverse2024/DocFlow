---
phase: 151-kb-migrate-static-knowledge
plan: 04
type: execute
wave: 2
depends_on: ['151-01', '151-02', '151-03']
files_modified:
  - .docflow-kb/_index.json
  - .docflow-kb/_header.md
  - .docflow-kb/_manual.md
  - .planning/phases/151-kb-migrate-static-knowledge/migration-log.md
  - .planning/knowledge/catpaw-catalog.md
  - .planning/knowledge/connectors-catalog.md
  - .planning/knowledge/email-templates-catalog.md
  - .planning/knowledge/skills-catalog.md
  - .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md
autonomous: false

requirements: [KB-12, KB-13, KB-14]

must_haves:
  truths:
    - "`node scripts/validate-kb.cjs` exits 0 across the ENTIRE migrated KB (Plan 01 + 02 + 03 outputs combined, ~55 new files + existing Phase 150 files)"
    - "The 4 auto-synced DB catalogs in `.planning/knowledge/` (catpaw-catalog.md, connectors-catalog.md, email-templates-catalog.md, skills-catalog.md) get redirect stubs pointing at `.docflow-kb/resources/<entity>/` (those live under resources/ from Phase 150, not re-migrated)"
    - "`node scripts/kb-sync.cjs --full-rebuild` regenerates `_index.json` and `_header.md` reflecting the new post-migration counts across all subdirectories (`domain/concepts/`, `domain/taxonomies/`, `domain/architecture/`, `rules/`, `protocols/`, `runtime/`, `incidents/`, `guides/`)"
    - "`.docflow-kb/_header.md` shows non-zero counts for at least: `rules: >=25`, `incidents: >=10`, `protocols: >=3`, `runtime: 5`, `guides: >=8`, `concepts: >=6`, `taxonomies: >=2`, `architecture: >=1`"
    - "`.planning/phases/151-kb-migrate-static-knowledge/migration-log.md` aggregates the 3 per-plan logs into one global source→destination table for the full phase. Migration logs live OUTSIDE `.docflow-kb/` (NOT `.docflow-kb/.migration-log.md`) because validate-kb.cjs walks dotfiles and requires frontmatter; placing a process-metadata log inside the KB would break KB-14."
    - "`.docflow-kb/_manual.md` gets a new section '## Contenido migrado en Phase 151' listing the 3 silos migrated and the counts per destination subdirectory"
    - "Oracle test per CLAUDE.md protocol: CatBot prompt executed asking it to list/read a rule (e.g. 'dame el contenido de R10') — if CatBot has read tool it should return content; if not, gap documented in 151-VERIFICATION.md for Phase 152 to address. The oracle placeholder `<PASTE_VERBATIM_AFTER_HUMAN_RUNS_THE_PROMPT>` MUST be replaced by real transcript before Task 3 is considered done."
    - "Every original file that was supposed to get a redirect (from Plans 01, 02, 03 combined, plus Task 1 of this plan) in fact has one — verified via grep for 'MOVED to .docflow-kb' OR '__redirect' OR 'REPLACED by auto-synced' OR 'LEGACY' in the expected files"
  artifacts:
    - path: ".docflow-kb/_index.json"
      provides: "Regenerated index reflecting ~60 new files across 8 subdirectories post-Phase-151"
    - path: ".docflow-kb/_header.md"
      provides: "Count lines for rules, incidents, protocols, runtime, guides, concepts, taxonomies, architecture"
    - path: ".docflow-kb/_manual.md"
      provides: "New section '## Contenido migrado en Phase 151' + updated directory map"
      contains: "Contenido migrado en Phase 151"
    - path: ".planning/phases/151-kb-migrate-static-knowledge/migration-log.md"
      provides: "Global migration log merging the 3 per-plan logs with totals and NON-modified-files caveats (lives outside KB)"
    - path: ".planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md"
      provides: "End-to-end evidence: validate-kb exit 0, count parity, redirect audit, CatBot oracle transcript, KB-12/13/14 evidence blocks"
      contains: "KB-12"
  key_links:
    - from: "scripts/kb-sync.cjs --full-rebuild"
      to: ".docflow-kb/_index.json + _header.md"
      via: "regenerates both from the frontmatters of every .md under .docflow-kb/"
      pattern: "regenerateHeaderFile"
    - from: ".planning/knowledge/catpaw-catalog.md"
      to: ".docflow-kb/resources/catpaws/"
      via: "redirect stub at top — DB-synced content already lives in resources/ from Phase 150"
      pattern: "REPLACED by auto-synced"
    - from: ".planning/phases/151-.../151-VERIFICATION.md"
      to: "KB-12, KB-13, KB-14 evidence"
      via: "structured section per requirement"
      pattern: "## KB-1[234]"
---

<objective>
Cerrar la fase 151 validando toda la migración, regenerando índice + header del KB, añadiendo redirects a los 4 catálogos auto-synced (catpaw/connectors/templates/skills que Phase 150 migró a `resources/` pero cuyos originales en `.planning/knowledge/` siguen ahí), actualizando `_manual.md`, corriendo el oracle test CatBot, y dejando `151-VERIFICATION.md` con evidencia de los 3 requirements (KB-12, KB-13, KB-14 — este plan genera la evidencia de cierre para todos, aunque el trabajo atómico de KB-12 se hace en Plans 01-03).

Purpose: Plans 01-03 son puramente aditivos (escriben al KB, añaden stubs). Este plan es el cierre: valida globalmente, regenera estructuras maestras, audita que todos los redirects estén presentes, ejecuta el oráculo CatBot.

Output: `_index.json` regenerado, `_header.md` regenerado, `_manual.md` actualizado, 4 redirects adicionales, log global (fuera del KB), `151-VERIFICATION.md` con evidencia.

**IMPORTANT — migration log location:** `migration-log.md` (aggregate) vive en `.planning/phases/151-kb-migrate-static-knowledge/migration-log.md`. NO `.docflow-kb/.migration-log.md`. Razón: `scripts/validate-kb.cjs` usa `readdirSync` que devuelve dotfiles y falla con "archivo sin frontmatter" si encuentra un `.md` dentro del KB sin frontmatter — bloquearía KB-14.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/ANALYSIS-knowledge-base-architecture.md
@.planning/phases/151-kb-migrate-static-knowledge/151-01-PLAN.md
@.planning/phases/151-kb-migrate-static-knowledge/151-02-PLAN.md
@.planning/phases/151-kb-migrate-static-knowledge/151-03-PLAN.md
@.docflow-kb/_manual.md
@.docflow-kb/_header.md
@scripts/validate-kb.cjs
@scripts/kb-sync.cjs
@CLAUDE.md

<interfaces>
<!-- kb-sync.cjs --full-rebuild behavior (from Phase 149 + 150 SUMMARYs): -->
- Walks `.docflow-kb/**/*.md` (excluding _archived/, _header.md, _manual.md)
- Reads every frontmatter
- Writes `_index.json` with entries[] + indexes.by_type + indexes.by_tag + header.counts
- Regenerates `_header.md` from `_index.json.header.counts` (Phase 150-04 Plan closed this gap)
- Exit 0 on success, exit 1 if any file fails validation

<!-- Current header.counts shape (from Phase 150-04 SUMMARY): -->
```json
{
  "catpaws_active": 9,
  "connectors_active": 4,
  "skills_active": 39,
  "catbrains_active": 1,
  "templates_active": 9,
  "canvases_active": 2,
  "rules": 0,
  "incidents_resolved": 0,
  "features_documented": 0
}
```

Phase 151 adds new subdirectories that `kb-sync.cjs regenerateHeader()` may not know about (concepts, taxonomies, architecture, protocols, runtime, guides). If kb-sync only emits the Phase 150 resource subtypes + rules/incidents/features, Task 2 must either:
  (a) Append extra count lines to `_header.md` directly post-rebuild, OR
  (b) Extend `regenerateHeaderFile()` in kb-sync.cjs.

Prefer (a) for this plan — (b) is out of scope (would touch CLI header logic, risks regression to Phase 150 tests).

<!-- Redirect stub canonical formats (shared across Plans 01/02/03): -->
Markdown:
```markdown
> **⚠️ MOVED to `.docflow-kb/<destination>`** during Phase 151 (2026-04-20).
>
> Content below preserved for reference until Phase 155 (cleanup final).
> New edits MUST happen in the KB.

---

<original content>
```

JSON (Plan 02):
```json
{
  "__redirect": "⚠️ MOVED to .docflow-kb/<dest>. ...",
  "__redirect_destinations": ["<path>"],
  <existing keys>
}
```

Catalog stub (this plan's Task 1, for DB-synced originals):
```markdown
> **⚠️ REPLACED by auto-synced KB resources** during Phase 150 + 151.
>
> The canonical data now lives at `.docflow-kb/resources/<entity>/*.md` (regenerated from DB via `node scripts/kb-sync.cjs --full-rebuild --source db`).
> This human-authored catalog is preserved below for reference only — likely stale vs DB truth.
> Eliminación física: Phase 155 (cleanup final).

---

<original content>
```

<!-- ORACLE PLACEHOLDER CONVENTION: -->
When Claude writes `151-VERIFICATION.md`, it inserts the literal string `PASTE_VERBATIM_AFTER_HUMAN_RUNS_THE_PROMPT` as a sentinel. The automated verify for Task 3 greps for absence of this sentinel — if present, it means the human did NOT run the CatBot oracle yet (or forgot to paste the transcript), and the verify MUST fail to prevent the checkpoint from marking done prematurely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add REPLACED-stubs to 4 DB-synced catalog originals + write aggregate migration log</name>
  <files>
    .planning/knowledge/catpaw-catalog.md
    .planning/knowledge/connectors-catalog.md
    .planning/knowledge/email-templates-catalog.md
    .planning/knowledge/skills-catalog.md
    .planning/phases/151-kb-migrate-static-knowledge/migration-log.md
  </files>
  <action>
**Part A — 4 catalog redirects:**

These 4 files in `.planning/knowledge/` are human-readable snapshots of DB tables. Phase 150 populated live data into `.docflow-kb/resources/<entity>/` directly from DB. The originals were never migrated atom-by-atom — they just need redirect stubs pointing at the DB-synced `resources/` subdirectory.

For each of the 4 files, inject at TOP (before existing content) the "REPLACED by auto-synced" stub from the `<interfaces>` block above. Destinations:
- `catpaw-catalog.md` → `.docflow-kb/resources/catpaws/`
- `connectors-catalog.md` → `.docflow-kb/resources/connectors/`
- `email-templates-catalog.md` → `.docflow-kb/resources/email-templates/`
- `skills-catalog.md` → `.docflow-kb/resources/skills/`

Invariant: original content below stub intact, file still readable as Markdown.

**Part B — Aggregate migration log `.planning/phases/151-kb-migrate-static-knowledge/migration-log.md`:**

IMPORTANT: this file lives in the PHASE DIRECTORY, not inside `.docflow-kb/`. Do NOT create `.docflow-kb/.migration-log.md` — validate-kb.cjs will reject it as a `.md` without frontmatter (it walks dotfiles via readdirSync; only `_header.md` and `_manual.md` are in EXCLUDED_FILENAMES).

Merge the 3 per-plan logs into one global log:

```markdown
# Migration log — Phase 151 (KB Migrate Static Knowledge)

**Phase:** 151
**Executed:** 2026-04-20
**Plans:** 151-01, 151-02, 151-03, 151-04
**Requirements closed:** KB-12 (full 3-origin migration), KB-13 (redirects), KB-14 (schema validation)

## Why this log lives outside `.docflow-kb/`

`scripts/validate-kb.cjs` walks every `.md` under `.docflow-kb/` via `readdirSync` (including dotfiles) and enforces universal frontmatter. Migration logs are process metadata, not KB content — they belong in the phase directory. Dotfile logs inside the KB (e.g. `.docflow-kb/.migration-log.md`) would fail validation with "archivo sin frontmatter" and break KB-14.

## Silos migrated

### Silo A — `app/data/knowledge/*.json` (7 files) — Plan 151-02
(table from migration-log-plan-02.md: 7 JSONs split into 5 concepts + 8 guides)

### Silo B — `.planning/knowledge/*.md` (12 files) — Plans 151-01 + 151-04
(table from migration-log-plan-01.md: 6 MDs split into 40 atoms;
 plus 4 DB-synced catalogs + user-guide + model-onboarding + mejoras-sistema-modelos covered across Plans 01/02/04)

### Silo C — `skill_orquestador_catbot_enriched.md` (root) — Plan 151-03
(table from migration-log-plan-03.md: → protocols/orquestador-catflow.md)

### Silo F — System prompts (typescript hardcoded) — Plan 151-03
(table from migration-log-plan-03.md: 5 exports → 5 runtime/*.prompt.md)

## Destination counts (post-migration)

| KB subdirectory | Files added by Phase 151 |
|-----------------|-------------------------|
| rules/ | 25 (R01..R25) |
| incidents/ | 10 (INC-01..INC-10) |
| protocols/ | 3 (catflow-inbound-review, connector-logs-redaction, orquestador-catflow) |
| runtime/ | 5 (strategist, decomposer, architect, canvas-qa, agent-autofix) |
| domain/concepts/ | 6 (canvas-node, catpaw, catflow, catbrain, canvas, catpower) |
| domain/taxonomies/ | 2 (node-roles, canvas-modes) |
| domain/architecture/ | 1 (holded-mcp-api) |
| guides/ | 8 (how-to-use-{catpaws,catflows,catbrains,canvases}, catboard, settings, user-guide, model-onboarding) |
| **Total new files** | **~60** |

## Redirects added (exhaustive audit)

| Original | Target(s) | Redirect type |
|----------|-----------|---------------|
| .planning/knowledge/canvas-nodes-catalog.md | rules/ + domain/concepts/canvas-node.md | markdown stub (Plan 01) |
| .planning/knowledge/incidents-log.md | incidents/ | markdown stub (Plan 01) |
| .planning/knowledge/proceso-catflow-revision-inbound.md | protocols/catflow-inbound-review.md | markdown stub (Plan 01) |
| .planning/knowledge/connector-logs-redaction-policy.md | protocols/connector-logs-redaction.md | markdown stub (Plan 01) |
| .planning/knowledge/holded-mcp-api.md | domain/architecture/holded-mcp-api.md | markdown stub (Plan 01) |
| .planning/knowledge/mejoras-sistema-modelos.md | (legacy, Phase 155 → .docflow-legacy/) | LEGACY stub (Plan 01) |
| .planning/knowledge/user-guide.md | guides/user-guide.md | markdown stub (Plan 02) |
| .planning/knowledge/model-onboarding.md | guides/model-onboarding.md | markdown stub (Plan 02) |
| .planning/knowledge/catpaw-catalog.md | resources/catpaws/ | REPLACED stub (Plan 04) |
| .planning/knowledge/connectors-catalog.md | resources/connectors/ | REPLACED stub (Plan 04) |
| .planning/knowledge/email-templates-catalog.md | resources/email-templates/ | REPLACED stub (Plan 04) |
| .planning/knowledge/skills-catalog.md | resources/skills/ | REPLACED stub (Plan 04) |
| app/data/knowledge/catpaw.json | concepts/catpaw.md + guides/how-to-use-catpaws.md | __redirect key (Plan 02) |
| app/data/knowledge/catflow.json | concepts/catflow.md + guides/how-to-use-catflows.md | __redirect key (Plan 02) |
| app/data/knowledge/catbrains.json | concepts/catbrain.md + guides/how-to-use-catbrains.md | __redirect key (Plan 02) |
| app/data/knowledge/canvas.json | concepts/canvas.md + guides/how-to-use-canvases.md | __redirect key (Plan 02) |
| app/data/knowledge/catboard.json | guides/catboard.md | __redirect key (Plan 02) |
| app/data/knowledge/catpower.json | concepts/catpower.md | __redirect key (Plan 02) |
| app/data/knowledge/settings.json | guides/settings.md | __redirect key (Plan 02) |
| app/data/knowledge/canvas-nodes-catalog.md | rules/ + concepts/canvas-node.md (dup) | markdown stub (Plan 02) |
| app/data/knowledge/canvas-rules-index.md | rules/ (dup) | markdown stub (Plan 02) |
| skill_orquestador_catbot_enriched.md (root) | protocols/orquestador-catflow.md | markdown stub (Plan 03) |

Total: 21 redirects across 3 filesystem locations.

## NON-modified files (critical contract preservation)

- `app/src/lib/services/catbot-pipeline-prompts.ts` — Phase 152 owns the loadPrompt() refactor
- `CLAUDE.md` — out of scope per phase constraints
- `app/data/knowledge/_index.json` + `_template.json` — PromptAssembler still reads them until Phase 152

## Deferred to Phase 155 (cleanup final)

- Physical deletion of all originals with redirect stubs
- Move of `.planning/knowledge/mejoras-sistema-modelos.md` to `.docflow-legacy/milestone-retrospectives/`
- Update of `CLAUDE.md` §"Documentación de referencia" to point at `.docflow-kb/` instead of `.planning/knowledge/`
```

**Invariant after task:** `grep -rlc "MOVED to .docflow-kb\|REPLACED by auto-synced\|__redirect\|LEGACY" .planning/knowledge/ app/data/knowledge/ skill_orquestador_catbot_enriched.md | wc -l` counts ≥20 files with stubs.
  </action>
  <verify>
    <automated>for f in .planning/knowledge/catpaw-catalog.md .planning/knowledge/connectors-catalog.md .planning/knowledge/email-templates-catalog.md .planning/knowledge/skills-catalog.md; do head -3 "$f" | grep -q "REPLACED by auto-synced" || { echo "MISSING $f"; exit 1; }; done && test -f .planning/phases/151-kb-migrate-static-knowledge/migration-log.md && grep -q "KB-12" .planning/phases/151-kb-migrate-static-knowledge/migration-log.md && grep -q "KB-13" .planning/phases/151-kb-migrate-static-knowledge/migration-log.md && grep -q "KB-14" .planning/phases/151-kb-migrate-static-knowledge/migration-log.md && ls .docflow-kb/.migration-log*.md 2>/dev/null | wc -l | grep -q '^0$' && echo OK</automated>
  </verify>
  <done>
    - 4 DB-synced catalogs have REPLACED stubs
    - `.planning/phases/151-kb-migrate-static-knowledge/migration-log.md` is the single source of truth for the phase's migration
    - All 3 requirements (KB-12, KB-13, KB-14) referenced in the log
    - Zero migration-log `.md` files inside `.docflow-kb/`
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate _index.json + _header.md + update _manual.md with Phase 151 section</name>
  <files>
    .docflow-kb/_index.json
    .docflow-kb/_header.md
    .docflow-kb/_manual.md
  </files>
  <action>
**Part A — Run full rebuild of index + header:**

```bash
node scripts/kb-sync.cjs --full-rebuild
```

Expected: exit 0. Emits lines like `"_index.json + _header.md regenerados"`. If exit != 0, STOP — something in Plans 01/02/03 produced an invalid frontmatter. Fix before continuing (likely candidate: a tag not in taxonomy, a missing conditional field like `last_accessed_at` when `ttl: managed`, OR a stray `.migration-log*.md` inside `.docflow-kb/` left over from a previous execution — delete those).

After the rebuild, inspect `_index.json.header.counts`. The Phase 150 shape has 9 keys (6 resource counts + rules + incidents_resolved + features_documented). Phase 151 adds many entries under `rules/` and `incidents/` (so those two counts go from 0 to ≥25 and ≥10) but also adds entries in subdirectories that kb-sync.cjs likely does NOT have count-keys for: protocols, runtime, concepts, taxonomies, architecture, guides.

**Part B — Patch `_header.md` to include ALL post-151 counts:**

Read the regenerated `.docflow-kb/_header.md` after Part A. If it's missing count lines for the Phase 151 subdirectories, append them. The final `_header.md` must resemble:

```markdown
# KB Header — <ISO timestamp from regeneration>

## Resource counts

- CatPaws activos: 9
- Connectors activos: 4
- Skills activos: 39
- CatBrains activos: 1
- Templates activos: 9
- Canvases activos: 2

## Knowledge counts (Phase 151)

- Rules: 25
- Incidents resolved: 10
- Protocols: 3
- Runtime prompts: 5
- Concepts: 6
- Taxonomies: 2
- Architecture docs: 1
- Guides: 8

## Top tags (auto-generated from index)

<from _index.json.header.top_tags — keep as kb-sync emits it>

## Last changes

<from _index.json.header.last_changes — keep as kb-sync emits it>
```

Derivation: counts for the new subdirectories come from `ls .docflow-kb/<subdir>/*.md | wc -l`. Do NOT invent values; read from disk.

**Part C — Update `.docflow-kb/_manual.md`:**

Append (NOT replace) a new section at the END of `_manual.md`:

```markdown
## Contenido migrado en Phase 151

Phase 151 (2026-04-20) completó la migración de los silos de conocimiento estático al KB. Antes de esta fase, el `_manual.md` describía la estructura vacía; ahora refleja el estado poblado.

### Silos migrados

1. **`.planning/knowledge/*.md`** (12 files) → atomizado en `rules/` (25), `incidents/` (10), `protocols/` (3), `domain/` (9), `guides/` (2). Originales con redirect stub hasta Phase 155.
2. **`app/data/knowledge/*.json`** (7 JSONs) → split en `domain/concepts/` (5) + `guides/` (7). JSONs con clave `__redirect` + `__redirect_destinations`.
3. **`skill_orquestador_catbot_enriched.md`** (raíz) → `protocols/orquestador-catflow.md` (14 PARTES, 890 líneas).
4. **Runtime prompts hardcoded** (`catbot-pipeline-prompts.ts`) → extracción paralela a `runtime/*.prompt.md` (5 archivos). El código sigue usando las constantes TS hasta Phase 152.

### Navegación post-151

- **¿Reglas de diseño de canvas?** → `rules/R01-*.md` ... `R25-*.md`
- **¿Incidentes históricos resueltos?** → `incidents/INC-*.md`
- **¿Cómo usar un CatPaw/CatFlow/CatBrain?** → `guides/how-to-use-*.md`
- **¿Qué es un canvas node?** → `domain/concepts/canvas-node.md`
- **¿Los 7 roles funcionales?** → `domain/taxonomies/node-roles.md`
- **¿Cómo funciona la API Holded MCP?** → `domain/architecture/holded-mcp-api.md`
- **¿Cómo orquestar un canvas completo?** → `protocols/orquestador-catflow.md`
- **¿Qué prompt usa el strategist/architect/qa del pipeline?** → `runtime/*.prompt.md`

### Archivos originales NO borrados

Todos los originales en `.planning/knowledge/`, `app/data/knowledge/`, y la skill en raíz reciben redirect stub apuntando al nuevo destino en el KB. La eliminación física está planificada para Phase 155 (cleanup final). Ver `.planning/phases/151-kb-migrate-static-knowledge/migration-log.md` para el mapping completo.

### Nota sobre logs de migración

Los logs de migración de esta fase (global + por plan) viven en `.planning/phases/151-kb-migrate-static-knowledge/migration-log{,-plan-01,-plan-02,-plan-03}.md`, NO dentro de `.docflow-kb/`. Razón: `validate-kb.cjs` exige frontmatter universal a todo `.md` del KB, incluidos dotfiles.
```

**Invariant:** `_manual.md` passes `validate-kb.cjs` (it's excluded as a stub per validate-kb.cjs EXCLUDED_FILENAMES — verify this still holds by re-running the validator).
  </action>
  <verify>
    <automated>node scripts/kb-sync.cjs --full-rebuild 2>&1 | tail -3 && node scripts/validate-kb.cjs 2>&1 | tail -3 && test -f .docflow-kb/_header.md && grep -q "Rules:" .docflow-kb/_header.md && grep -q "Incidents" .docflow-kb/_header.md && grep -q "Contenido migrado en Phase 151" .docflow-kb/_manual.md && echo OK</automated>
  </verify>
  <done>
    - `_index.json` and `_header.md` regenerated via `kb-sync.cjs --full-rebuild`
    - `_header.md` contains count lines for at least rules, incidents, protocols, runtime, guides, concepts, taxonomies, architecture
    - `_manual.md` has new `## Contenido migrado en Phase 151` section
    - `validate-kb.cjs` still exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Write 151-VERIFICATION.md, run CatBot oracle test, pause for user approval</name>
  <files>
    .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md
  </files>
  <action>
Claude writes `.planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md` with the structured evidence below, runs the automated checks inline, pastes the output, and then pauses for human interaction to complete the CatBot oracle prompt and approve phase close.

**Step 1 — Write the verification file with this skeleton (Claude fills the <PLACEHOLDERS> from real commands):**

```markdown
# Phase 151 — KB Migrate Static Knowledge — VERIFICATION

**Status:** ready_for_close
**Date:** 2026-04-20

## KB-12: Migración completa de los 3 orígenes

**Evidence:**
- Silo A (`app/data/knowledge/*.json`) — 7 JSONs migrated → 5 concepts + 8 guides. Proof:
  `ls .docflow-kb/domain/concepts/*.md .docflow-kb/guides/*.md | wc -l` → <ACTUAL_COUNT>
- Silo B (`.planning/knowledge/*.md`) — 12 MDs migrated → 40 atoms. Proof:
  `ls .docflow-kb/rules/R*.md .docflow-kb/incidents/INC-*.md | wc -l` → <ACTUAL_COUNT, expected >=35>
- Silo C (skill orquestador raíz) — migrated → `protocols/orquestador-catflow.md`. Proof:
  `grep -c "^## PARTE" .docflow-kb/protocols/orquestador-catflow.md` → <ACTUAL, expected 14>
- Silo F (runtime prompts TS) — 5 exports extracted → `runtime/*.prompt.md`. Proof:
  `ls .docflow-kb/runtime/*.prompt.md | wc -l` → <ACTUAL, expected 5>

**Status:** [COMPLETE | GAP — <description>]

## KB-13: Redirects en archivos originales

**Evidence (from .planning/phases/151-kb-migrate-static-knowledge/migration-log.md redirect audit table):**
- 6 MDs in `.planning/knowledge/` (original Silo B set) with markdown stub
- 4 DB-synced catalogs in `.planning/knowledge/` with REPLACED stub
- 2 MDs in `.planning/knowledge/` (user-guide, model-onboarding) with markdown stub
- 7 JSONs in `app/data/knowledge/` with `__redirect` key
- 2 duplicate MDs in `app/data/knowledge/` with markdown stub
- 1 MD in repo root (skill orquestador) with markdown stub

Proof (paste actual outputs):
```
$ grep -rlc "MOVED to .docflow-kb\|REPLACED by auto-synced\|LEGACY" .planning/knowledge/
<ACTUAL_OUTPUT>
$ grep -l "__redirect" app/data/knowledge/*.json
<ACTUAL_OUTPUT>
$ head -3 skill_orquestador_catbot_enriched.md | grep "MOVED to"
<ACTUAL_OUTPUT>
```

**Status:** [COMPLETE | GAP — <description>]

## KB-14: Validación schema de cada archivo migrado

**Evidence:**
```
$ node scripts/validate-kb.cjs
<PASTE_VERBATIM_VALIDATOR_OUTPUT>
$ echo $?
<0 OR 1>
```

**Status:** [COMPLETE | GAP — <description>]

## Count parity post-rebuild

| Subdirectory | Count (from _header.md) | Spot-check command | Actual |
|--------------|-------------------------|--------------------|--------|
| rules/ | 25 | `ls .docflow-kb/rules/R*.md \| wc -l` | <N> |
| incidents/ | 10 | `ls .docflow-kb/incidents/INC-*.md \| wc -l` | <N> |
| protocols/ | 3 | `ls .docflow-kb/protocols/*.md \| wc -l` | <N> |
| runtime/ | 5 | `ls .docflow-kb/runtime/*.prompt.md \| wc -l` | <N> |
| domain/concepts/ | 6 | `ls .docflow-kb/domain/concepts/*.md \| wc -l` | <N> |
| domain/taxonomies/ | 2 | `ls .docflow-kb/domain/taxonomies/*.md \| wc -l` | <N> |
| domain/architecture/ | 1 | `ls .docflow-kb/domain/architecture/*.md \| wc -l` | <N> |
| guides/ | 8 | `ls .docflow-kb/guides/*.md \| wc -l` | <N> |

## CatBot Oracle (per CLAUDE.md §Protocolo de Testing)

**Prompt sent to CatBot:**
"Lee el contenido de la regla R10 del knowledge base y resúmelo en 3 bullets. Si no tienes acceso al KB aún, responde 'NO TENGO ACCESO' para que lo documente como gap."

**CatBot response:**
PASTE_VERBATIM_AFTER_HUMAN_RUNS_THE_PROMPT

**Interpretación:**
- Si CatBot pudo leer R10 → oráculo satisfactorio (raro hoy; Phase 152 formaliza el acceso vía get_kb_entry/search_kb)
- Si CatBot respondió "NO TENGO ACCESO" → gap documentado, es la razón exacta de Phase 152 (KB CatBot Consume). No bloquea Phase 151 porque el objetivo de 151 es crear el KB, no consumirlo
- Si CatBot alucinó el contenido → gap crítico: el KB está, CatBot no puede verlo Y no admite ignorarlo. Investigar antes de Phase 152

**Gap documented (if applicable):**
- Phase 152 (KB CatBot Consume) adds `get_kb_entry(id)` + `search_kb({tags, type, audience, search})` tools + includes `_header.md` in every prompt-assembler injection. Until Phase 152, CatBot cannot read the KB files. This is by design.

## NON-modified files (contract preservation)

Verified via `git diff --stat`:
```
$ git diff --stat app/src/lib/services/catbot-pipeline-prompts.ts
<SHOULD_BE_EMPTY>
$ git diff --stat CLAUDE.md
<SHOULD_BE_EMPTY>
```

## Summary

Phase 151 closes all 3 requirements (KB-12, KB-13, KB-14). ~60 new KB files, 21 redirect stubs, `_index.json` + `_header.md` regenerated, oracle gap documented for Phase 152.
```

**IMPORTANT on the sentinel placeholder:** use the literal token `PASTE_VERBATIM_AFTER_HUMAN_RUNS_THE_PROMPT` (no angle brackets) in the skeleton. The automated verify for Task 3 greps for its ABSENCE to confirm the human actually pasted the CatBot transcript. If the sentinel is still in the file, the verify fails and the checkpoint cannot be marked done.

**Step 2 — Claude runs the automated checks inline and fills <ACTUAL> placeholders:**

```bash
# Counts
ls .docflow-kb/domain/concepts/*.md .docflow-kb/guides/*.md | wc -l
ls .docflow-kb/rules/R*.md .docflow-kb/incidents/INC-*.md | wc -l
grep -c "^## PARTE" .docflow-kb/protocols/orquestador-catflow.md
ls .docflow-kb/runtime/*.prompt.md | wc -l

# Redirects
grep -rlc "MOVED to .docflow-kb\|REPLACED by auto-synced\|LEGACY" .planning/knowledge/
grep -l "__redirect" app/data/knowledge/*.json
head -3 skill_orquestador_catbot_enriched.md | grep "MOVED to"

# Validator
node scripts/validate-kb.cjs; echo "exit=$?"

# Non-modification
git diff --stat app/src/lib/services/catbot-pipeline-prompts.ts
git diff --stat CLAUDE.md
```

**Step 3 — Pause for human:**

Stop here. User must:
1. Open a CatBot session (UI or telegram bot)
2. Send the oracle prompt (see §CatBot Oracle in the VERIFICATION file)
3. REPLACE the literal string `PASTE_VERBATIM_AFTER_HUMAN_RUNS_THE_PROMPT` in `151-VERIFICATION.md` with the verbatim CatBot response (paste it below `**CatBot response:**`)
4. Reply "approved" in this session to resume, OR paste issues for a --gaps revision plan

Do NOT mark the phase as closed until human approves AND the sentinel has been replaced. The automated verify below greps for absence of the sentinel — if it is still present, verify fails.
  </action>
  <verify>
    <automated>test -f .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md && grep -c "^## KB-1" .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md | grep -q '^3$' && grep -q "CatBot Oracle" .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md && grep -q "NON-modified files" .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md && ! grep -q "PASTE_VERBATIM_AFTER_HUMAN_RUNS_THE_PROMPT" .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md && ! grep -q "PASTE_VERBATIM_VALIDATOR_OUTPUT" .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md && echo OK</automated>
  </verify>
  <done>
    - `151-VERIFICATION.md` exists with sections for KB-12, KB-13, KB-14, count parity, oracle transcript, NON-modified files
    - Automated evidence populated by Claude (counts, validator output, git diff) — no `PASTE_VERBATIM_VALIDATOR_OUTPUT` sentinel remaining
    - User has pasted CatBot oracle response — `PASTE_VERBATIM_AFTER_HUMAN_RUNS_THE_PROMPT` sentinel replaced with real transcript
    - User has replied "approved"
    - If approved: phase ready for `/gsd:verify-phase 151`
  </done>
</task>

</tasks>

<verification>
End-to-end verification chain:

```bash
# 1. Entire KB passes schema validation
node scripts/validate-kb.cjs
echo "Validator exit: $?"

# 2. Index and header are in sync with disk
node scripts/kb-sync.cjs --full-rebuild
# On second consecutive run, should produce 0 changes (idempotence from Phase 150)

# 3. Every expected original has a redirect
grep -rl "MOVED to .docflow-kb\|REPLACED by auto-synced\|LEGACY" .planning/knowledge/ | wc -l   # expect 12
grep -l "__redirect" app/data/knowledge/*.json | wc -l   # expect 7
head -3 skill_orquestador_catbot_enriched.md | grep "MOVED to"

# 4. Non-modification of the TS prompt file
git diff --stat app/src/lib/services/catbot-pipeline-prompts.ts | wc -l   # expect 0 (empty)

# 5. VERIFICATION file exists with all 3 requirement sections + no sentinel remaining
grep -c "^## KB-1" .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md   # expect 3
grep -c "PASTE_VERBATIM" .planning/phases/151-kb-migrate-static-knowledge/151-VERIFICATION.md   # expect 0 after human paste

# 6. No stray migration logs inside the KB (would break validate-kb.cjs)
ls .docflow-kb/.migration-log*.md 2>/dev/null | wc -l   # expect 0
```
</verification>

<success_criteria>
- `validate-kb.cjs` exits 0 across the entire Phase 151-populated KB (KB-14)
- 4 additional redirects added by Task 1 (DB-synced catalogs), bringing the total to 21 (KB-13)
- `_index.json` + `_header.md` regenerated post-migration with counts for all new subdirectories (KB-14 indirectly)
- `_manual.md` has new `## Contenido migrado en Phase 151` section (KB-12 documentation)
- `.planning/phases/151-kb-migrate-static-knowledge/migration-log.md` aggregates Plans 01/02/03 logs with requirement-closure table
- `151-VERIFICATION.md` contains KB-12/13/14 evidence blocks + CatBot oracle transcript (sentinel replaced) + NON-modified files audit
- `catbot-pipeline-prompts.ts` git-diff empty (contract preserved — Phase 152 owns loadPrompt refactor)
- User checkpoint approved for phase close
- Zero migration-log `.md` files inside `.docflow-kb/` (preserves KB-14)
</success_criteria>

<output>
After completion, create `.planning/phases/151-kb-migrate-static-knowledge/151-04-SUMMARY.md`
</output>
</content>
</invoke>
