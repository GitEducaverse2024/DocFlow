---
phase: 119-promptassembler
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/services/catbot-tools.ts
  - app/src/lib/__tests__/knowledge-tree.test.ts
  - app/data/knowledge/catboard.json
  - app/data/knowledge/catbrains.json
  - app/data/knowledge/catpaw.json
  - app/data/knowledge/catflow.json
  - app/data/knowledge/canvas.json
  - app/data/knowledge/catpower.json
  - app/data/knowledge/settings.json
autonomous: true
requirements: [PROMPT-04, PROMPT-05]

must_haves:
  truths:
    - "CatBot puede usar query_knowledge para consultar el knowledge tree por area o fulltext"
    - "query_knowledge devuelve conceptos, howto, dont y errores comunes relevantes"
    - "explain_feature usa internamente loadKnowledgeArea() en vez de FEATURE_KNOWLEDGE dict"
    - "Los sources de cada JSON del knowledge tree apuntan a docs reales en .planning/"
    - "CatBot puede profundizar con search_documentation usando los paths de sources"
  artifacts:
    - path: "app/src/lib/services/catbot-tools.ts"
      provides: "query_knowledge tool + explain_feature migrado a knowledge tree"
      contains: "query_knowledge"
    - path: "app/data/knowledge/catboard.json"
      provides: "Sources populados con paths a .planning/"
      contains: "sources"
    - path: "app/src/lib/__tests__/knowledge-tree.test.ts"
      provides: "Test de sources no vacios en knowledge JSONs"
  key_links:
    - from: "app/src/lib/services/catbot-tools.ts"
      to: "app/src/lib/knowledge-tree.ts"
      via: "import loadKnowledgeArea, getAllKnowledgeAreas for query_knowledge"
      pattern: "loadKnowledgeArea|getAllKnowledgeAreas"
    - from: "app/data/knowledge/*.json sources"
      to: ".planning/*.md"
      via: "sources arrays pointing to documentation files"
      pattern: "\\.planning/"
---

<objective>
Anadir el tool query_knowledge a CatBot para consultar el knowledge tree on-demand, migrar explain_feature para usar el knowledge tree en vez del dict FEATURE_KNOWLEDGE, y popular los arrays sources en los 7 JSON del knowledge tree.

Purpose: CatBot necesita acceder a conocimiento que no esta en su system prompt (PROMPT-04), y los sources deben apuntar a docs reales para que pueda profundizar (PROMPT-05).

Output: query_knowledge tool funcional, explain_feature migrado, sources populados
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-promptassembler/119-RESEARCH.md

@app/src/lib/services/catbot-tools.ts
@app/src/lib/knowledge-tree.ts
@app/src/lib/__tests__/knowledge-tree.test.ts
@app/data/knowledge/_index.json

<interfaces>
<!-- Knowledge tree loader from Phase 118 -->
From app/src/lib/knowledge-tree.ts:
```typescript
export function loadKnowledgeIndex(): KnowledgeIndex;
export function loadKnowledgeArea(id: string): KnowledgeEntry;
export function getAllKnowledgeAreas(): KnowledgeEntry[];
// KnowledgeEntry has: id, name, path, description, endpoints, tools,
// concepts, howto, dont, common_errors, success_cases, sources
```

<!-- Existing explain_feature tool in catbot-tools.ts -->
From app/src/lib/services/catbot-tools.ts (line ~789):
```typescript
const FEATURE_KNOWLEDGE: Record<string, string> = {
  // 25 keys with hardcoded descriptions
};
// Line ~1136: case 'explain_feature' uses FEATURE_KNOWLEDGE dict
```

<!-- Tool definition pattern in catbot-tools.ts -->
```typescript
// Tools are defined as OpenAI function calling format:
{ type: 'function', function: { name: string, description: string, parameters: { type: 'object', properties: {...} } } }
// Handlers are in executeTool() switch statement
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: query_knowledge tool + explain_feature migration</name>
  <files>app/src/lib/services/catbot-tools.ts</files>
  <action>
    1. **Importar knowledge-tree.ts** al inicio de catbot-tools.ts:
       ```typescript
       import { loadKnowledgeArea, getAllKnowledgeAreas, type KnowledgeEntry } from '@/lib/knowledge-tree';
       ```

    2. **Anadir tool definition query_knowledge** en el array de tools (junto a explain_feature):
       ```typescript
       {
         type: 'function',
         function: {
           name: 'query_knowledge',
           description: 'Consulta el arbol de conocimiento de DoCatFlow. Busca informacion sobre areas de la plataforma por path o por texto libre. Usa esto cuando necesites informacion que no esta en tu prompt actual.',
           parameters: {
             type: 'object',
             properties: {
               area: { type: 'string', description: 'ID del area: catboard, catbrains, catpaw, catflow, canvas, catpower, settings. Omitir para buscar en todas.' },
               query: { type: 'string', description: 'Texto a buscar en conceptos, howto, errores y reglas del area' },
             },
           },
         },
       }
       ```

    3. **Anadir handler query_knowledge** en el switch de executeTool():
       - Si `area` viene definido: `loadKnowledgeArea(area)` y devolver formatKnowledgeResult(entry, query)
       - Si no viene area: `getAllKnowledgeAreas()`, buscar por keyword matching en concepts, howto, dont, common_errors (usando `.toLowerCase().includes(query.toLowerCase())`), devolver top 3 matches con score
       - `formatKnowledgeResult(entry, query)`: devolver { area: entry.name, description: entry.description, concepts (filtrados por query si hay query), howto (filtrados), dont (filtrados), common_errors (filtrados), sources: entry.sources }
       - Score function: contar cuantos campos (concepts, howto, dont, common_errors[].error) contienen el query text
       - Error handling: try-catch, devolver { error: "Area 'X' no encontrada" } si falla

    4. **Migrar explain_feature** (case 'explain_feature' ~line 1136):
       - ANTES: busca en FEATURE_KNOWLEDGE dict
       - DESPUES: busca primero en knowledge tree via loadKnowledgeArea() matcheando el feature name contra area IDs y names. Si no encuentra match exacto, busca el feature como keyword en todos los areas (mismo scoring que query_knowledge).
       - Formatear resultado como texto (no JSON) para mantener la experiencia de chat: "## {area.name}\n{area.description}\n\n### Conceptos...\n..."
       - Si no encuentra nada en knowledge tree, devolver un mensaje generico: "No tengo informacion especifica sobre '{feature}'. Puedes buscar en la documentacion con search_documentation."

    5. **Eliminar FEATURE_KNOWLEDGE dict** (~line 789, ~150 lineas) completamente. Ya no se usa.

    6. **Anadir query_knowledge a la lista de always_allowed** (linea ~938 donde se filtran tools por allowed_actions) — query_knowledge debe ser siempre accesible, igual que explain_feature y navigate_to.

    7. **Mantener el nombre explain_feature** (no renombrar) — per Research Open Question 2: el LLM ya sabe llamar explain_feature, cambiar el nombre disrumpe el comportamiento.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build && grep -c "FEATURE_KNOWLEDGE" src/lib/services/catbot-tools.ts | grep "^0$"</automated>
  </verify>
  <done>query_knowledge tool definido y funcional en catbot-tools.ts. explain_feature usa knowledge tree internamente. FEATURE_KNOWLEDGE dict eliminado. Build compila sin errores.</done>
</task>

<task type="auto">
  <name>Task 2: Popular sources en knowledge JSONs + test</name>
  <files>app/data/knowledge/catboard.json, app/data/knowledge/catbrains.json, app/data/knowledge/catpaw.json, app/data/knowledge/catflow.json, app/data/knowledge/canvas.json, app/data/knowledge/catpower.json, app/data/knowledge/settings.json, app/src/lib/__tests__/knowledge-tree.test.ts</files>
  <action>
    1. **Listar docs disponibles en .planning/:**
       ```bash
       ls .planning/*.md .planning/**/*.md
       ```
       Tambien considerar: README.md, progressSesion*.md

    2. **Popular sources en cada JSON** con paths relativos a docs relevantes:

       - **catboard.json**: `.planning/PROJECT.md`, `.planning/ROADMAP.md` (overview del proyecto)
       - **catbrains.json**: `.planning/research/FEATURES.md`, `progressSesion2-14.md` (historial de CatBrains)
       - **catpaw.json**: `.planning/research/FEATURES.md`, `.planning/milestones/v20.0-ROADMAP.md` (CatPaw Directory), `.planning/milestones/v21.0-ROADMAP.md` (Skills Directory)
       - **catflow.json**: `.planning/research/FEATURES.md`, `.planning/milestones/v16.0-ROADMAP.md` (CatFlow)
       - **canvas.json**: `.planning/research/FEATURES.md`, `.planning/milestones/v16.0-ROADMAP.md` (CatFlow = canvas)
       - **catpower.json**: `.planning/milestones/v24.0-ROADMAP.md` (CatPower email templates), `.planning/milestones/v21.0-ROADMAP.md` (Skills)
       - **settings.json**: `.planning/milestones/v25.0-ROADMAP.md` (Model Intelligence), `.planning/milestones/v25.1-ROADMAP.md` (Centro de Modelos), `.planning/research/ARCHITECTURE.md`

       **CRITICO:** Antes de poner un path en sources, verificar que el archivo existe con `ls`. No poner paths a archivos inexistentes. Si un archivo referenciado no existe, usar solo los que existan.

    3. **Formato de sources:** Array de strings con paths relativos al root del proyecto:
       ```json
       "sources": [
         ".planning/PROJECT.md",
         ".planning/milestones/v16.0-ROADMAP.md"
       ]
       ```

    4. **Anadir test en knowledge-tree.test.ts:**
       ```typescript
       describe('sources population (PROMPT-05)', () => {
         it('every knowledge area has at least one source', () => {
           const areas = getAllKnowledgeAreas();
           for (const area of areas) {
             expect(area.sources.length, `${area.id} has no sources`).toBeGreaterThan(0);
           }
         });

         it('sources point to existing files', () => {
           const areas = getAllKnowledgeAreas();
           for (const area of areas) {
             for (const source of area.sources) {
               // Sources should be relative paths that exist
               expect(source).toMatch(/\.(md|json|txt)$/);
             }
           }
         });
       });
       ```

    5. **Usar git add -f** para los JSON modificados (estan en app/data/ que es gitignored pero con negation para knowledge/).
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts -x</automated>
  </verify>
  <done>Los 7 JSON del knowledge tree tienen sources no vacios apuntando a docs reales en .planning/. Test verifica que todas las areas tienen al menos un source. knowledge-tree.test.ts pasa.</done>
</task>

</tasks>

<verification>
1. Tests knowledge-tree pasan: `cd app && npx vitest run src/lib/__tests__/knowledge-tree.test.ts`
2. Build completo pasa: `cd app && npm run build`
3. FEATURE_KNOWLEDGE eliminado: `grep -c "FEATURE_KNOWLEDGE" app/src/lib/services/catbot-tools.ts` devuelve 0
4. query_knowledge existe: `grep "query_knowledge" app/src/lib/services/catbot-tools.ts` muestra definicion y handler
5. Sources no vacios: `grep -c '"sources": \[\]' app/data/knowledge/*.json` devuelve 0
6. Suite completa: `cd app && npx vitest run`
</verification>

<success_criteria>
- query_knowledge tool registrado en catbot-tools.ts con handler funcional
- explain_feature usa knowledge tree internamente (FEATURE_KNOWLEDGE dict eliminado)
- Los 7 JSON del knowledge tree tienen sources populados con paths a docs existentes
- Tests de knowledge-tree pasan incluyendo el nuevo test de sources
- Build compila sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/119-promptassembler/119-02-SUMMARY.md`
</output>
