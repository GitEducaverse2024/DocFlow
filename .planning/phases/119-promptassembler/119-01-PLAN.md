---
phase: 119-promptassembler
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/lib/services/catbot-prompt-assembler.ts
  - app/src/lib/__tests__/catbot-prompt-assembler.test.ts
  - app/src/app/api/catbot/chat/route.ts
autonomous: true
requirements: [PROMPT-01, PROMPT-02, PROMPT-03]

must_haves:
  truths:
    - "El system prompt de CatBot se ensambla dinamicamente desde knowledge tree + config"
    - "El prompt cambia segun la pagina actual del usuario (ej: /catflow inyecta catflow.json)"
    - "Si el prompt excede el presupuesto de tokens, las secciones P3 se truncan primero, luego P2"
    - "Las secciones P0 (identidad, tool instructions) nunca se truncan"
    - "buildSystemPrompt() ya no existe en route.ts — reemplazada por PromptAssembler.build()"
  artifacts:
    - path: "app/src/lib/services/catbot-prompt-assembler.ts"
      provides: "PromptAssembler con build(), assembleWithBudget(), y page-to-area mapping"
      exports: ["build", "PromptContext", "PromptSection"]
    - path: "app/src/lib/__tests__/catbot-prompt-assembler.test.ts"
      provides: "Tests unitarios para build, page context, y budget truncation"
      min_lines: 60
    - path: "app/src/app/api/catbot/chat/route.ts"
      provides: "Chat endpoint usando PromptAssembler.build() en vez de buildSystemPrompt()"
  key_links:
    - from: "app/src/app/api/catbot/chat/route.ts"
      to: "app/src/lib/services/catbot-prompt-assembler.ts"
      via: "import { build } from catbot-prompt-assembler"
      pattern: "import.*catbot-prompt-assembler"
    - from: "app/src/lib/services/catbot-prompt-assembler.ts"
      to: "app/src/lib/knowledge-tree.ts"
      via: "loadKnowledgeArea() for page-specific knowledge"
      pattern: "loadKnowledgeArea|loadKnowledgeIndex"
    - from: "app/src/lib/services/catbot-prompt-assembler.ts"
      to: "app/src/lib/db.ts"
      via: "DB queries for stats (catbrains count, etc.)"
      pattern: "db\\.prepare"
---

<objective>
Crear el modulo PromptAssembler que reemplaza buildSystemPrompt() en route.ts con ensamblaje dinamico desde knowledge tree, config y contexto de pagina, con presupuesto de tokens por tier.

Purpose: Eliminar el system prompt hardcodeado (~300 lineas) y reemplazarlo con un sistema modular, mantenible y consciente del contexto de pagina. Es el cambio de mayor riesgo del milestone (toca el core de CatBot).

Output: catbot-prompt-assembler.ts funcional, tests unitarios, route.ts migrado
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-promptassembler/119-RESEARCH.md

@app/src/app/api/catbot/chat/route.ts
@app/src/lib/knowledge-tree.ts
@app/src/lib/db.ts
@app/src/lib/services/catbot-tools.ts

<interfaces>
<!-- Knowledge tree loader from Phase 118 -->
From app/src/lib/knowledge-tree.ts:
```typescript
export type KnowledgeEntry = {
  id: string; name: string; path: string; description: string;
  endpoints: string[]; tools: string[]; concepts: string[]; howto: string[];
  dont: string[]; common_errors: { error: string; cause: string; solution: string }[];
  success_cases: string[]; sources: string[];
};
export type KnowledgeIndex = {
  version: string; updated: string;
  areas: { id: string; file: string; name: string; description: string }[];
};
export function loadKnowledgeIndex(): KnowledgeIndex;
export function loadKnowledgeArea(id: string): KnowledgeEntry;
export function getAllKnowledgeAreas(): KnowledgeEntry[];
```

From app/src/lib/db.ts:
```typescript
import Database from 'better-sqlite3';
const db: Database.Database;
export default db;
```

From app/src/lib/services/alias-routing.ts:
```typescript
export function resolveAlias(alias: string): Promise<string>;
export function getAllAliases(opts?: { active_only: boolean }): Array<{ alias: string; model_key: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PromptAssembler module con TDD (RED -> GREEN)</name>
  <files>app/src/lib/services/catbot-prompt-assembler.ts, app/src/lib/__tests__/catbot-prompt-assembler.test.ts</files>
  <behavior>
    - Test "build() returns non-empty string": build({}) devuelve un string no vacio con identidad de CatBot
    - Test "build() includes identity section": El resultado contiene "CatBot" y "DoCatFlow"
    - Test "build() includes page-specific knowledge for /catflow": Cuando page="/catflow", el resultado contiene conceptos de catflow.json (ej: "CatFlow", "pipeline")
    - Test "build() includes page-specific knowledge for /settings": Cuando page="/settings", el resultado contiene conceptos de settings.json (ej: "Centro de Modelos")
    - Test "build() does NOT include other areas when page is set": Cuando page="/catflow", el resultado NO contiene conceptos exclusivos de catbrains.json
    - Test "budget truncation removes P3 sections first": Con un budget muy bajo (ej: 2000 chars), las secciones P3 (troubleshooting) se omiten mientras P0 (identidad) permanece
    - Test "P0 sections are never truncated even over budget": Incluso con budget=100, las secciones P0 (identidad, tool instructions) estan presentes
    - Test "different model tiers get different budgets": Libre tier tiene budget menor que Pro, Pro menor que Elite
  </behavior>
  <action>
    **RED phase:** Crear app/src/lib/__tests__/catbot-prompt-assembler.test.ts con los 8 tests descritos arriba. Todos deben fallar porque el modulo no existe.

    **GREEN phase:** Crear app/src/lib/services/catbot-prompt-assembler.ts con:

    1. **Interfaces:** `PromptSection` (id, priority 0-3, content), `PromptContext` (page?, channel?, hasSudo, catbotConfig, stats?)

    2. **build(ctx: PromptContext): string** — Funcion principal que:
       - Crea secciones P0: identidad+personalidad (texto estatico, ~30 lineas del actual buildSystemPrompt lineas 171-209), tool instructions (lineas 211-217)
       - Crea seccion P1: page-specific knowledge via `getPageKnowledge(ctx.page)` usando loadKnowledgeArea() + formatKnowledgeForPrompt()
       - Crea seccion P1: platform overview desde _index.json (resumen de todas las areas)
       - Crea seccion P1: skills protocols (Orquestador CatFlow, Arquitecto de Agentes — lineas 275-301)
       - Crea seccion P1: canvas protocols (lineas 219-348)
       - Crea seccion P1: telegram adaptation (si channel=telegram — lineas 392-403)
       - Crea seccion P2: model intelligence (dynamic — getAllAliases() con try-catch, lineas 112-166)
       - Crea seccion P2: sudo section (si hasSudo — lineas 63-91)
       - Crea seccion P2: holded section (dynamic — getHoldedTools(), lineas 93-110)
       - Crea seccion P2: canvas diagnostics + execution knowledge (lineas 303-348)
       - Crea seccion P3: troubleshooting table (lineas 248-267)
       - Crea seccion P3: email protocol (lineas 268-273)
       - Llama assembleWithBudget(sections, budget)

    3. **PAGE_TO_AREA mapping:** `/` -> catboard, `/catbrains` -> catbrains, `/agents` -> catpaw, `/catflow` -> catflow, `/canvas` -> canvas, `/skills` -> catpower, `/settings` -> settings, `/tasks` -> catflow, `/workers` -> catpaw, `/connectors` -> catpower. Usar prefix matching para rutas con parametros (ej: `/catbrains/abc123` -> catbrains).

    4. **assembleWithBudget(sections, budgetChars):** Sort by priority, include all P0 always, then fill P1/P2/P3 until budget reached. Skip sections that don't fit.

    5. **getBudget(model?):** Map model name substrings to tiers: gemma/llama/qwen -> Libre (16000 chars = ~4K tokens), sonnet/gpt-4o/flash -> Pro (32000 chars), opus/gemini-2.5-pro -> Elite (64000 chars). Default: Pro.

    6. **getStats():** Query DB for catbrains count, catpaws count, tasks count, listening count. Con try-catch individual (graceful degradation).

    7. **Cada section builder tiene try-catch con fallback vacio** para evitar que un fallo en una seccion rompa todo el prompt.

    IMPORTANTE — Contenido audit del buildSystemPrompt() actual:
    - Migrar TODO el contenido: identidad (171-178), personalidad, DoCatFlow overview (180-194), stack (196-203), contexto actual (205-208), sudo (63-91), Holded (93-110), model intelligence (112-166), tool instructions (211-217), canvas protocols (219-348), search_documentation (236-245), troubleshooting (248-267), email (268-273), Orquestador skill (275-287), Arquitecto skill (289-301), canvas diagnostics (303-306), canvas execution (308-348), telegram (392-403).
    - NO perder ninguna seccion. Usar la tabla de la Research (Pattern 1) como checklist.
    - Las secciones que no tienen JSON correspondiente (identidad, sudo rules, Holded rules, skills protocols, canvas protocols, tool instructions) van como texto estatico en el assembler — per Research Open Question 1.

    Correr tests: `cd app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts`
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts -x</automated>
  </verify>
  <done>8 tests pasan. PromptAssembler.build() produce prompt con identidad, page knowledge, budget truncation. El modulo exporta build, PromptContext, PromptSection.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar PromptAssembler en route.ts y eliminar buildSystemPrompt()</name>
  <files>app/src/app/api/catbot/chat/route.ts</files>
  <action>
    1. **Importar PromptAssembler:** `import { build as buildPrompt } from '@/lib/services/catbot-prompt-assembler';`

    2. **Reemplazar la llamada en POST handler (linea ~390):**
       - ANTES: `let systemPrompt = buildSystemPrompt(context || {}, !!sudoActive);`
       - DESPUES: `let systemPrompt = buildPrompt({ page: context?.page, channel: effectiveChannel as 'web' | 'telegram' | undefined, hasSudo: !!sudoActive, catbotConfig });`

    3. **Eliminar la seccion de Telegram** que se anade despues (lineas 393-403) porque PromptAssembler ya incluye la seccion de Telegram condicionalmente via channel en PromptContext.

    4. **Eliminar buildSystemPrompt() completa** (lineas 42-349) y las funciones helper getSudoConfig() si se mueven al assembler. Mantener getSudoConfig() en route.ts porque se usa para la validacion de sudo token.

    5. **Eliminar imports que ya no se necesiten** en route.ts si buildSystemPrompt usaba imports que el assembler ahora maneja internamente (getHoldedTools, getAllAliases se importan en el assembler).

    6. **Verificar que el build pasa:** `cd app && npm run build`

    CRITICO: No eliminar nada del POST handler excepto buildSystemPrompt y su llamada. El tool-calling loop, streaming, sudo validation, etc. deben permanecer intactos.
  </action>
  <verify>
    <automated>cd /home/deskmath/docflow/app && npm run build && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts -x</automated>
  </verify>
  <done>route.ts usa PromptAssembler.build() en vez de buildSystemPrompt(). La funcion buildSystemPrompt() ya no existe en route.ts. El build compila sin errores. Los tests del assembler siguen pasando.</done>
</task>

</tasks>

<verification>
1. Tests unitarios del assembler pasan: `cd app && npx vitest run src/lib/__tests__/catbot-prompt-assembler.test.ts`
2. Build completo pasa: `cd app && npm run build`
3. Suite completa de tests pasa: `cd app && npx vitest run`
4. buildSystemPrompt() no existe en route.ts: `grep -n "function buildSystemPrompt" app/src/app/api/catbot/chat/route.ts` devuelve vacio
5. PromptAssembler es el unico source del system prompt: `grep -n "buildPrompt\|PromptAssembler" app/src/app/api/catbot/chat/route.ts` muestra la llamada
</verification>

<success_criteria>
- PromptAssembler.build() produce un system prompt que contiene identidad, personalidad, platform overview, tool instructions, page-specific knowledge, y todas las secciones condicionales (sudo, holded, model intelligence, telegram)
- El prompt cambia cuando se pasa page="/catflow" vs page="/settings"
- Con budget bajo, secciones P3 se omiten pero P0 permanece
- buildSystemPrompt() eliminada de route.ts sin romper el flujo de chat
- Build y tests verdes
</success_criteria>

<output>
After completion, create `.planning/phases/119-promptassembler/119-01-SUMMARY.md`
</output>
